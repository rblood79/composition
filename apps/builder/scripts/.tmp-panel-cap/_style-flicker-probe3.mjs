import { chromium } from "playwright";
import { resolve } from "node:path";
const READY = () => Boolean(window.__composition_STORE__ && window.__composition_STORE__.getState().currentPageId && document.querySelector(".app:not(.builder-booting)") && document.querySelector('[data-testid="skia-canvas-unified"]'));
const browser = await chromium.launch({ headless: false });
const page = await (await browser.newContext({ storageState: resolve("apps/builder/scripts/.auth-session.json"), viewport: { width: 1600, height: 1200 } })).newPage();
await page.goto("http://localhost:5173/dashboard", { waitUntil: "networkidle" });
await page.getByText(/^PPEA$/).first().click();
await page.waitForURL(/\/builder\/[^/?]+$/, { timeout: 60000 }); await page.waitForFunction(READY, undefined, { timeout: 90000 }); await page.waitForTimeout(1500);
const panel = async (name, want) => { const btn = page.getByRole("button", { name, exact: true }).first(); const pressed = await btn.getAttribute("aria-pressed"); if ((pressed === "true") !== want) { await btn.click(); await page.waitForTimeout(400); } };
const P = '[data-panel-id="properties"]';
const info = await page.evaluate(() => { const s = window.__composition_STORE__.getState(); const els = s.elements.filter(e => e.page_id === s.currentPageId); const pick = els.find(e => /button|textfield|select|checkbox|switch|slider/i.test(e.type)) ?? els.find(e => e.type !== "body"); s.setSelectedElement(pick.id); return { total: els.length, pick: pick.type, id: pick.id, hasResponsive: els.filter(e => e.responsive).length }; });
console.log("project", JSON.stringify(info));
await page.waitForTimeout(800); await panel("Properties", true);
await page.evaluate((P) => { document.querySelectorAll(`${P} .section-caret[aria-expanded="false"]`).forEach((c) => c.click()); }, P); await page.waitForTimeout(500);
await page.getByRole("button", { name: /Compare Mode|비교 모드/ }).first().click(); await page.waitForTimeout(3000);
const fr = page.frames().find(f => f !== page.mainFrame() && /preview/.test(f.url()));
const OBS = () => { const log = (window.__MUT = []); const desc = (n) => { if (n.nodeType === 3) n = n.parentNode; const e = n; return `${e.tagName?.toLowerCase()}${e.id ? "#" + e.id : ""}${typeof e.className === "string" && e.className ? "." + e.className.split(" ").slice(0,2).join(".") : ""}`; };
  const obs = new MutationObserver((ms) => { for (const m of ms) { const t = m.target; const tag = (t.nodeType === 3 ? t.parentNode : t).tagName; const styleish = tag === "STYLE" || tag === "LINK" || [...m.addedNodes, ...m.removedNodes].some(n => n.tagName === "STYLE" || n.tagName === "LINK"); log.push({ styleish, type: m.type, attr: m.attributeName, target: desc(t), old: m.oldValue?.slice(0,80), now: m.type === "attributes" ? t.getAttribute(m.attributeName)?.slice(0,80) : m.type === "characterData" ? "(text)" : "+" + [...m.addedNodes].map(desc).join(",") + " / -" + [...m.removedNodes].map(desc).join(",") }); } });
  obs.observe(document.documentElement, { subtree: true, childList: true, attributes: true, attributeOldValue: true, characterData: true }); };
await page.evaluate(OBS); await fr.evaluate(OBS);
const controls = page.locator(`${P} .react-aria-ToggleButton, ${P} [role="switch"], ${P} [role="checkbox"]`);
const n = Math.min(await controls.count(), 8); console.log("controls", n);
for (let k = 0; k < n; k++) { const c = controls.nth(k); if (!(await c.isVisible().catch(() => false))) continue; const label = (await c.getAttribute("aria-label")) || (await c.textContent())?.trim().slice(0, 20);
  await page.evaluate(() => (window.__MUT.length = 0)); await fr.evaluate(() => (window.__MUT.length = 0)); await c.click({ timeout: 3000 }).catch(() => {}); await page.waitForTimeout(700);
  const mb = await page.evaluate(() => window.__MUT); const mf = await fr.evaluate(() => window.__MUT);
  const sum = (ms) => { const o = {}; for (const m of ms) { const key = `${m.type}:${m.attr ?? ""}:${m.target}`; o[key] = (o[key] ?? 0) + 1; } return Object.entries(o).sort((a, b) => b[1] - a[1]).slice(0, 5).map(([k, v]) => `${k}×${v}`).join(" | "); };
  console.log(`[${k}] ${label}\n   builder styleish=${mb.filter(m => m.styleish).length}/${mb.length}\n   iframe  styleish=${mf.filter(m => m.styleish).length}/${mf.length}: ${sum(mf)}`);
  const st = [...mb, ...mf].filter(m => m.styleish); if (st.length) console.log("   STYLEISH:", JSON.stringify(st.slice(0, 6))); }
await browser.close();
