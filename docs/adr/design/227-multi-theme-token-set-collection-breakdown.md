# ADR-227 구현 설계: 다중 테마 — 문서 소유 토큰 세트 컬렉션

정본: [ADR-227](../227-multi-theme-token-set-collection.md)

작성일: 2026-09-20. 코드 사실은 이 날짜의 main (`81d96353f`) 실측이다 — 착수 시 Phase 0 에서 재실측한다.

## 1. 전제 lock-in

- base / 응용: ADR-110 (canonical `themes`/`tokens` 필드 · adapter) 과 ADR-143 (`tokens` 델타 저장 · `design_themes` store 폐기) 이 base 이고, 이 ADR 은 그 필드의 **모양** (단일 프리셋 → 이름 있는 토큰 세트 컬렉션) 과 **소유 축** (border 폭 · shadow 를 테마로) 을 더하는 응용이다. catalog rule (ADR-142/912) 의 TokenRef 참조 구조는 바꾸지 않는다.
- schema: `CompositionDocument.themes` 의 타입이 바뀐다 (단일 `ThemeSnapshot` → `ThemesCollection`). `tokens` 필드는 그대로 두되 의미가 "활성 테마의 델타" 에서 "테마 무관 사용자 정의 토큰" 으로 좁아진다 (§3). 1회 hydration migration.
- 의존 방향: 110/143 → 227 한 방향. 227 이 `resolveCanonicalToken` · `themesAdapter` · `themeConfigStore` 를 **확장**하고 새 SSOT 를 만들지 않는다 — 정본은 여전히 canonical document 다.
- fork 아님: 사용자 요구 (2026-09-20 "여러 theme 를 가질 수 있어야 한다 · Components 페이지에서 기본 요소가 theme 따라 보인다") 의 실행 ADR. 요소/페이지 스코프 테마 (대안 C) 는 이 ADR 에서 **유보** — 별도 ADR 이 아니라 재개 조건만 둔다 (Decision 3). "기본 요소" 의 전집화는 [ADR-228](228-palette-wide-reusable-origins-breakdown.md) — 직교, 2026-09-21 사용자 정정으로 초안 Phase 5 (catalog read-only 섹션) 철회.
- sub-phase 분할 없음 (Phase 0~5, Phase 5 는 검증만). 리뷰 수리로 누락된 generator·layout·migration·history 소비자를 §5에 포함했다. 과거 22/25파일 추정은 폐기하고 Phase 0에서 실제 경로를 중복 제거해 확정한다.

## 2. Phase 0 코드 사실 표 (2026-09-20 실측, `81d96353f`)

| #   | 사실                                                                                                                                                                                                                                                                                                        | 경로:라인                                                                                                         |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| F1  | `ThemeSnapshot` 은 단일 객체 — `tint · darkMode · neutral · radiusScale` + `customTokens?` ("향후 확장" 주석). 이름 · id · 목록 없음                                                                                                                                                                        | `packages/shared/src/types/composition-document.types.ts:61-73`                                                   |
| F2  | `TokensSnapshotEntry { type, value, source: "spec-token" \| "user-defined" }` · `TokensSnapshot = Record<string, Entry>`. 문서에는 seed 와 다른 **델타만** 저장 (`buildTokensDelta` / `mergeTokensSnapshot`)                                                                                                | 같은 파일 `:85-101` · `apps/builder/src/adapters/canonical/variablesAdapter.ts:205-240`                           |
| F3  | `resolveCanonicalToken(ref, doc)` = `doc.tokens?.[key]?.value` — 테마 개념 없음, 문서 1 : 값 1                                                                                                                                                                                                              | `variablesAdapter.ts:186-198`                                                                                     |
| F4  | 런타임 테마 = `themeConfigStore` 전역 싱글턴 (`tint · darkMode · neutral · radiusScale · baseTypography` + `themeVersion`). `setTint` → `tintToSkiaColors` (lightColors/darkColors mutation) → `themeVersion+1` → `notifyLayoutChange` → localStorage persist                                               | `apps/builder/src/stores/themeConfigStore.ts:41-47 · 132-138 · 168-200`                                           |
| F5  | 문서 ↔ store 는 `themesAdapter` (`snapshotThemesFromConfig` call-time 직렬화 · `applyCanonicalThemes` DI) 이고, 로드 시 적용은 **env flag `VITE_ADR110_P2_THEMES_WRITE_THROUGH === "true"` 뒤에 있다** — 기본 off 면 정본은 사실상 localStorage                                                             | `apps/builder/src/adapters/canonical/themesAdapter.ts:1-60` · `builder/main/BuilderCore.tsx:640-661`              |
| F6  | catalog rule 은 hex 리터럴 0 · TokenRef 2,333 (`{color.*}` 1,319 · `{typography.*}` 523 · `{radius.*}` 429 · `{focus.*}` 38 · `{spacing.*}` 17 · `{shadow.*}` 7). 색 이름 상위: transparent 224 · neutral 206 · base 134 · neutral-subtle 75 · accent 60 · border 56 · layer-2 52 · layer-1 49              | `packages/shared/src/catalog/generated/componentRulesTable.ts` (grep 집계)                                        |
| F7  | rule 의 숫자 리터럴: `height` 418 · `gap` 189 · `paddingX` 182 · `paddingY` 112 · `iconSize` 110 · **`borderWidth` 43 (0:3 · 1:37 · 2:3)**                                                                                                                                                                  | 같은 파일 (grep 집계)                                                                                             |
| F8  | DOM leg 의 tint 는 CSS 변수 한 줄 `--tint: var(--blue)` (oklch 프리셋 12) + `[data-accent="red"]` **요소 단위 accent override** (Radix 패턴) 가 이미 있다. Skia 는 `data-accent` 를 읽지 않는다 (소비처 = `LayoutRenderers.tsx` renderCard · `Card.binding.ts:84`)                                          | `packages/shared/src/components/styles/theme/preview-system.css:7-45` · `LayoutRenderers.tsx`                     |
| F9  | Publish 는 `applyThemeConfig({tint, neutral, radiusScale})` 로 `--tint` · `--color-neutral-N` alias · radius 를 `<style>` 로 주입 — `document.themes` 를 읽는 유일한 산출물 소비처. neutral 자기 참조 순환 함정 (ADR-193 P2 실측) 기록됨                                                                    | `apps/publish/src/App.tsx:188-215`                                                                                |
| F10 | Themes 패널 = tint 12 · dark · neutral · radius 5 · typography 프리셋 선택 (401 줄). 토큰 값 편집 UI 0 · 테마 목록 0. IndexedDB `design_themes` / ThemeStudio (`themeStore` · `TokenService` · `ThemeService`) 는 ADR-143 P4 (DB_VERSION 19) 에서 dead 로 폐기                                              | `apps/builder/src/builder/panels/themes/ThemesPanel.tsx:239-260` · `lib/db/indexedDB/adapter.ts:253-258`          |
| F11 | Skia 색 정본은 `@composition/specs` 의 `lightColors` / `darkColors` (ResolvedTokenMap) — `tintToSkiaColors` 가 프리셋 oklch → hex 로 **파생** (color-mix black 정합 함수 포함). 사용자가 hex 를 직접 주면 파생 규칙 (hover/pressed 단계 · dark 반전) 을 같이 정해야 한다                                    | `apps/builder/src/utils/theme/tintToSkiaColors.ts:1-60`                                                           |
| F12 | Components 페이지 = 시스템 페이지 `page-components` (`pageRole:"components"`) — 템플릿 origin (ListBoxItem 등, `ensureTemplateOrigins` 시드) + 사용자 reusable origin + 그 instance/slot 저작 자리. 팔레트 66 중 reusable origin 은 5 (Toolbar · Form · IconButton · InlineAlert · Card) — 전집화는 ADR-228 | `apps/builder/src/builder/pages/systemComponentsPage.ts` · `builder/components/reusableCompositeOrigins.ts:40-50` |

## 3. 스키마 · migration · resolve 계약

2026-09-21 리뷰 수리 기준 HEAD `a7c85b70f`. §2는 최초 조사 기록이며 아래 소비 경로 보완과 함께 Phase 0에서 재확인한다.

### 3.1 저장 모양

```ts
export interface ThemeDefinition {
  id: string;
  name: string;
  preset: Omit<ThemeSnapshot, "customTokens">; // 매 resolve 때 seed 생성 입력
  tokens: TokensSnapshot; // seed(preset)와 다른 명시 값만
}
export interface ThemesCollection {
  active: string;
  items: Record<string, ThemeDefinition>;
  order: string[];
}
// CompositionDocument.themes?: ThemesCollection
// CompositionDocument.tokens?: TokensSnapshot // 테마 무관 user-defined만
```

preset은 최초 생성 후 버리는 값이 아니라 델타를 복원하는 seed 생성 입력이다. 전환마다 새 seed에서 시작하므로 A에만 있던 override가 B에 남으면 안 된다. id는 기존 프로젝트의 ID 생성기를 재사용하고, active/items/order 참조 무결성을 hydration에서 검사한다.

소유 축은 color/typography/radius/border.width/shadow/focus다. size/spacing은 density 축으로 유보한다. 기존 `TokensSnapshotEntry.value`의 scalar 모양을 유지한다. shadow는 CSS shadow 문자열, focus는 color 및 폭/offset scalar로 표현한다. 합성 `{focus.ring.default|inset}`는 이 scalar를 읽는 recipe이며 객체를 TokensSnapshot에 저장하지 않는다.

### 3.2 최초 migration과 기존 실효값 보존 (H3)

순수 함수의 입력은 `migrateThemesField(doc, { legacyConfig, legacyWriteThrough, source })`다. caller가 **같은 projectId**의 기존 localStorage를 읽어 전달한다. source는 local-project/import를 구분하며 import에는 이 기기의 legacyConfig를 섞지 않는다.

| 입력 상태                                                | 최초 실효값 선택                                                                                                                  |
| -------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| 유효한 ThemesCollection                                  | 문서만 사용. localStorage 및 구 flag 무시                                                                                         |
| 구 문서 + legacy write-through off                       | 유효한 프로젝트별 legacy 필드 → DEFAULT_THEME_SELECTION. 구 document.themes는 현행에서 적용되지 않으므로 stale 값으로 덮지 않는다 |
| 구 문서 + legacy write-through on + 유효한 ThemeSnapshot | 구 document.themes 4필드 우선. baseTypography는 legacy → DEFAULT_BASE_TYPOGRAPHY                                                  |
| 구 문서 + legacy write-through on + themes 부재/무효     | legacy 필드 → 기본값                                                                                                              |
| 외부 import, legacy 입력 없음                            | 유효한 구 document.themes → 기본값                                                                                                |

`legacyWriteThrough`는 제거 전 기존 부팅 정책의 입력이다. old default-off 세션에서 실제로 보이던 값이 우선이며, 최초 이관 뒤에는 flag를 사용하지 않는다. 없는 필드마다 fallback하고 유효하지 않은 값은 보고한다.

- 기존 spec-token 델타를 Default 테마 tokens로 옮기고 user-defined는 root에 남긴다. legacy baseTypography는 `typography.base-font-family`(string), `typography.base-font-size`(px number), `typography.base-line-height`(unitless number)의 seed 대비 델타로 승계한다. 같은 키의 구 명시 델타는 보존하고 충돌 사례를 fixture로 고정한다.
- 유효한 구 `customTokens`가 발견되면 알려진 키는 델타로 정규화한다. 미지원 값은 조용히 버리지 않고 보존 또는 명시 migration 오류로 남긴다. Phase 0에서 실제 저장 fixture 여부를 조사한다.
- canonical 문서를 DB에 **성공적으로 저장한 뒤** 컬렉션 모양 자체를 migration 완료 표식으로 삼는다. 실패 시 완료 처리 및 legacy 축소 금지, 기존 설정으로 재시도 가능해야 한다.
- 저장 성공 이후 localStorage는 projectId별 활성 id 캐시만 담는다. 새 컬렉션 로드는 항상 문서 우선이다. migration은 History를 만들지 않는다.
- BC 범위: 구 문서 100%에 themes 타입 검사, themes 컬렉션화와 tokens 분할이 필요하다. 로컬 실효값·import 출처별 fixture 수를 Phase 0에서 집계한다. rollback은 사전 문서와 legacy 설정 백업 복원으로 정의한다. 새 다중 테마를 preset 4값으로 줄이는 역변환은 손실이 있어 무손실 rollback으로 부르지 않는다.

### 3.3 production 소비 계약 (H1)

`resolveCanonicalToken`은 현재 production renderer 호출자가 없다. 이 함수만 바꾸는 것으로 완료하지 않는다. 문서에서 파생한 활성 snapshot을 원자적으로 설치하고 기존 production `resolveToken` 및 각 geometry/focus 소비자가 **같은 snapshot**을 읽도록 연결한다. 파생 snapshot은 쓰기 정본이 아니다.

| 축           | 값·단위                                                                   | Skia/레이아웃 소비                                              | DOM/Publish 소비                                                                         |
| ------------ | ------------------------------------------------------------------------- | --------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| color        | 유효한 CSS 색, Skia는 동일 색으로 정규화                                  | `resolveToken` color 및 paint                                   | 기존 의미 매핑: accent → `--accent`, neutral → `--fg`, layer-1 → `--bg-overlay`          |
| typography   | text 크기/line-height px, base-line-height만 unitless, font-family string | typography resolver + text 측정/캐시 + 기존 baseTypography 경로 | `--text-*`/line-height 변수 및 기존 body font-family/font-size/line-height 적용          |
| radius       | 유한한 비음수 px                                                          | radius resolver + shape/layout                                  | `--radius-*`, px 직렬화                                                                  |
| border.width | 유한한 비음수 px                                                          | border resolver + implicitStyles/크기 산식 + paint              | `--border-width-none                                                                     | thin | thick`, px 직렬화 |
| shadow       | CSS shadow 문자열                                                         | theme별 shadow resolver + 기존 shadow parser/paint              | `--shadow-*`                                                                             |
| focus        | color + width px + offset px(음수 허용)                                   | ring recipe 및 focus paint                                      | `--focus-ring`, `--focus-ring-width`, `--focus-ring-offset`, `--focus-ring-inset-offset` |

CSS 매핑은 `tokenResolver.ts::tokenToCSSVar`와 `colorTokenToCss.ts`의 실제 이름을 공유/대조한다. 미등록 키를 임의 변수명으로 emit하지 않는다. `toReactStyle`은 base CSS 생성기가 아닌 override 전용이므로 base 배선은 CSSGenerator와 generated CSS를 통해 검증한다. shared/specs 의존 방향을 지키고 공유 순수 해석·CSS helper는 하위 패키지에 둔다.

해석 순서: preset seed → root user-defined fallback → 활성 테마 명시 델타 → **미명시 파생 키** 계산. 명시 hover/pressed 값은 파생보다 우선한다. accent만 편집하면 hover=85% black mix, pressed=75% black mix 등 기존 파생 규칙을 같은 함수로 계산한다. default light/dark preset 파생 후 명시 델타를 적용하며, 별도 dark 델타 세트는 만들지 않는다.

현재 hover/pressed CSS는 `color-mix(...)` 표현식을 직접 쓴다. 명시 파생 키 편집을 지원하도록 generator와 inline color mapper를 `var(--accent-hover, color-mix(...))` 같은 override 슬롯으로 연결한다. 기존 기본 시각은 유지하며 사용자가 입력한 파생값은 두 leg에서 우선한다. delta 삭제·테마 변경 시 이전 슬롯을 제거/교체한다.

한 번의 commit은 snapshot 설치 → typography/layout/paint 캐시 무효화 → themeVersion 및 notifyLayoutChange 각 1회 → Preview 동기화 순서다. **노드별 snapshot 생성은 없지만 필요한 노드 재해석·재레이아웃은 허용**하며 총비용을 측정한다. Preview ready/reload에 최신 snapshot을 재전송하고 이전 테마 CSS를 완전히 교체한다. Publish 초기 로드도 동일 helper·snapshot을 사용한다.

### 3.4 border 참조화의 전체 경로 (H2)

현행 `spec.types.ts:997`는 number, `CSSGenerator.ts:777-785,1003-1004`는 무조건 px, `tokenResolver.ts:35-55`는 border 미지원이다. layout `implicitStyles.ts:443`와 `utils.ts:3333,3528`도 숫자만 읽는다.

- spec/catalog size 타입에 number 또는 BorderWidthTokenRef를 허용한다. token.types의 category/StrictTokenRef/validation과 border seed(0/1/2)를 함께 확장한다.
- CSSGenerator의 border shorthand와 border-width 모든 채널을 number→px, TokenRef→CSS var로 분기한다. `{border.width.thin}px` 출력은 금지한다.
- `resolveToken`, `toSkiaStyle`, implicitStyles, size별 contentHeight 산식 등 모든 소비자는 계산 전에 같은 활성 snapshot에서 px를 구한다. 비숫자→1 fallback으로 새 토큰을 숨기지 않는다.
- Phase 0에서 숫자 43곳뿐 아니라 문자열 "1px", shorthand, 조건부 border 채널도 목록화한다. 동등한 폭은 같은 토큰으로 연결하고 의도적 예외는 위치·사유를 gate에 명시한다. 임의 rule 시각 재설계는 하지 않는다.
- G3은 seed 상태의 변경 전후 Δ0에 더해 thin=3, thick=4에서 paint/외곽/content box를 대조한다. 생성 CSS parse 및 border 토큰 resolve를 검사한다.

## 4. Phase 별 작업

### Phase 0 — inventory freeze

- F1~F12 및 §3 소비 경로 재실측. production resolveToken·CSS mapper·focus·text 측정·layout·publish caller, 구 설정 출처와 fixture 수를 기록한다.
- border 숫자/문자열/shorthand inventory와 변경 파일 집합을 중복 제거해 확정한다. 추정 대비 증가 시 같은 ADR의 inventory를 보강한다.
- **G0**: caller 배선 표·migration 행렬·sweep/예외 목록·변경 파일 수 확정. 미연결 소비자 0.

### Phase 1 — 스키마 · migration · adapter

- §3.1~3.2 구현. BuilderCore에서 legacy 입력을 캡처하고 저장 성공 후 기본 write-through로 전환한다.
- themesAdapter/variablesAdapter는 snapshot의 활성 항목을 다루고 persist는 기존 canonical 저장 경로를 사용한다.
- 추가/삭제/이름 변경/활성 전환/토큰 편집은 runCanonicalMutation 및 history event/replay에 연결한다. snapshot 재생도 동일 적용 경로를 쓴다.
- **G1**: migration 표 모든 경우 + 비기본 typography + doc/legacy 충돌 + 저장 실패/재시도 + reload 멱등 + 백업 복원. 기존 실효 시각 Δ0, 미편집 테마 tokens={}.

### Phase 2 — 카테고리별 resolve · DOM/Publish 배선

- §3.3의 snapshot, token mapping, 파생값 우선순위, 이전 델타 제거, Preview ready 및 publish 초기 적용을 구현한다.
- **G2**: 전수 Default/사용자 테마 + color/typography/radius/shadow/focus 각각의 비기본값 및 reset, light/dark, 명시 hover/pressed 우선순위. Skia/Preview/Publish 결과를 ADR-198 실제 브라우저 oracle로 대조한다. border 편집 검증은 Phase 3 G3에서 수행한다.

### Phase 3 — border 전체 소비자 확장과 sweep

- §3.4 타입·seed·validator·CSSGenerator·paint·implicit layout·size 산식을 먼저 연결하고 rule을 참조화한다.
- **G3**: 숫자 리터럴 0 ratchet(확정 예외는 별도 명시) + 원복 RED + 유효 CSS + seed 전후 Δ0 + thin=3/thick=4 외곽/content box 일치. G2 회귀 확인.

### Phase 4 — Themes 패널과 사용자 History

- 목록/복제/이름/삭제/활성 + color·typography·radius·border·shadow·focus 편집, 프리셋 채우기 및 reset. 마지막 테마 삭제 금지.
- 28/32 두 티어, `1fr 1fr 28px`, i18n ko/en. 쓰기는 runCanonicalMutation.
- **G4**: live 생성→전환→편집→reset→reload→Undo/Redo. Canvas·Preview·Properties 반영, 사용자 전환 History entry 1건. 600요소 총 전환 프레임 ≤25ms.

### Phase 5 — Components 페이지의 테마 표면

- 신규 UI 없이 origin/instance/slot을 검증한다. 228 전에는 기존 reusable 5 + template origin, 228 뒤에는 그 확정 eligible 집합으로 G5 재실행.
- **G5**: 전환으로 canonical children 구조 무변화, 사용자 전환 entry 1건, 파생 렌더/페이지 처리의 **추가 entry 0건**. 같은 테마 재선택은 no-op 0건. Undo/Redo 후 올바른 테마 복귀, 600요소 총 전환 프레임 ≤25ms.

## 5. 파일 경계 (리뷰 보강 — Phase 0에서 실제 경로/수 확정)

| 영역              | 포함할 경로·책임                                                                                                                               |
| ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| schema/migration  | shared composition-document types, themesAdapter/variablesAdapter/index, BuilderCore, legacy 설정 read 및 DB 저장 성공 처리, migration fixture |
| 토큰 runtime      | specs token.types/spec.types, primitives border/focus/typography/radius/shadows, tokenResolver, 공통 snapshot/해석/CSS helper                  |
| Skia/layout       | toSkiaStyle, implicitStyles, engines/utils size 산식, text 측정 및 캐시 invalidation, focus/shadow paint                                       |
| DOM               | CSSGenerator, colorTokenToCss, 생성 CSS 명령, 기존 baseTypography 적용, useThemeMessenger와 수신/ready 경로                                    |
| canonical/history | themeConfigStore, mutation action, canonicalHistoryEvents, historyActions replay, persist 연결                                                 |
| UI/publish        | ThemesPanel/ThemeList/TokenEditor, ko/en, publish App 및 theme 적용                                                                            |
| 검증              | category resolve·CSS·layout·migration·history 인접 테스트, literal ratchet, ADR-198 및 live/perf 하니스                                        |

초기 22/25파일 추정은 위 누락 경계를 포함하지 못했다. 확정 수는 G0 산출물이며 이전 숫자를 승인 상한처럼 재사용하지 않는다. 생성물은 직접 편집하지 않고 source 변경 뒤 재생성한다.

## 6. 유보 항목

- 요소/페이지 스코프: 사용자 요구 시 재개. 노드별 scope resolve 및 frame 비용을 별도로 검증한다.
- size/spacing density: 이번 color/typography/radius/border/shadow/focus 변경과 분리한다.
- 테마별 light/dark 이중 델타: 유보. darkMode는 현재 테마 preset 안의 값이며 같은 명시 델타를 양 모드에 적용한다.

## 7. 리뷰 수리 상태

2026-09-21: round 1 H1/H2/H3/M4를 §3~~5와 본문 Gates에 반영했다. 이는 설계 수리이며 구현 G0~~G5는 UNVERIFIED, Proposed 유지.

## 8. Phase 0 — inventory freeze (G0, 2026-09-22, main `f0d2ccce6`)

§2 F1~~F12 재실측 결과 — 바뀐 것과 §3 소비 경로 확정. 이 절이 Phase 1~~5 의 입력이다.

### 8.1 재실측 (F1~F12 → 현재)

| ID  | 현재 사실 (2026-09-22)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 경로:라인                                                                                                                                                                                                                                                     |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1  | 그대로 — `ThemeSnapshot { tint, darkMode, neutral, radiusScale, customTokens? }`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `packages/shared/src/types/composition-document.types.ts:61-73`                                                                                                                                                                                               |
| F3  | 그대로 — `resolveCanonicalToken` **production 호출자 0** (adapter 자기 참조 + 주석뿐)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `variablesAdapter.ts:186-198` · grep                                                                                                                                                                                                                          |
| F4  | 그대로 — `themeConfigStore` 싱글턴 · localStorage 키 `composition-theme-config-<projectId>` = `{tint, darkMode, neutral, radiusScale, baseTypography}` · `initThemeConfig(projectId)` 가 부팅 시 읽고 `applyThemeDerivations` (tint·neutral·radius 셋 항상 같이) 로 specs 맵 mutation                                                                                                                                                                                                                                                                                                             | `apps/builder/src/stores/themeConfigStore.ts:79-125 · 149-165 · 306-345`                                                                                                                                                                                      |
| F5  | 그대로 + **`VITE_ADR110_P2_THEMES_WRITE_THROUGH` 는 `.env`/`.env.example` 어디에도 없다 (항상 off)** → §3.2 행렬의 `legacyWriteThrough` 는 실전에서 항상 `false`                                                                                                                                                                                                                                                                                                                                                                                                                                  | `BuilderCore.tsx:640` · grep                                                                                                                                                                                                                                  |
| F13 | **`doc.themes` / `doc.tokens` 는 production 에서 한 번도 쓰이지 않는다** — 쓰는 곳은 `legacyToCanonical(…, { getThemeConfig, getTokens })` 뿐인데 ADR-116 이후 production 호출자 0 (storeBridge 는 주석만). 따라서 **기존 문서 100% 가 "themes 부재"** — §3.2 행렬 2행 (구 문서 + off) 이 유일한 실전 경로이고 "유효한 구 ThemeSnapshot" 행은 import/테스트 fixture 로만 닿는다                                                                                                                                                                                                                   | `adapters/canonical/index.ts:151 · 310-329` · grep                                                                                                                                                                                                            |
| F6  | TokenRef 집계 갱신: `{radius.*}` 429 → **radius.none 183 · md 71 · sm 48 · lg 48 · full 37 · xl 29 · xs 10 · 2xl 3** · `{typography.*}` = text-sm 121 · base 106 · lg 86 · xs 83 · 2xs 19 · xl 15 … + `--line-height` 계열 · `{shadow.sm/md/lg}` 3/2/2 · `{focus.ring.default}` 37 · `{focus.ring.inset}` 1 · `{spacing.xs/2xs/sm}` 8/5/4                                                                                                                                                                                                                                                         | `componentRulesTable.ts` (grep)                                                                                                                                                                                                                               |
| F7  | **border inventory 갱신** — `borderWidth` 숫자 **48** (0:3 · 1:42 · 2:3; 09-20 의 43 에서 ADR-223 등으로 +5) · `borderWidth: "1px"` **6** · `border: "1px solid var(--border)"` **8** · `border: "1px solid"` **2** · `outline: "2px solid var(--accent)"` **17** (focus 축 후보) · TokenRef 만 든 `border: "{color.*}"` 123 (색 축, 폭은 별도 키)                                                                                                                                                                                                                                                | 같은 파일 (grep)                                                                                                                                                                                                                                              |
| F8  | 그대로 — DOM tint = `--tint: var(--blue)` + `[data-accent]` (Skia 소비 0)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `preview-system.css:7-45`                                                                                                                                                                                                                                     |
| F9  | Publish = builder 가 payload 에 `themeConfig {tint, neutral, radiusScale}` 를 실어 보내고 (`BuilderCore.tsx:1171`) publish `applyThemeConfig` 가 `--tint` · neutral alias · `--radius-base` 를 주입. **`generateThemeCSS.ts` 는 호출자 0 (dead)**                                                                                                                                                                                                                                                                                                                                                 | `apps/publish/src/App.tsx:193-220 · 390-401` · `utils/theme/generateThemeCSS.ts`                                                                                                                                                                              |
| F10 | 그대로 — ThemesPanel 401 줄 프리셋 선택만 · `useThemeMessenger.sendBaseTypography` 만 쓴다 (`sendThemeTokens` 호출자 0)                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `ThemesPanel.tsx:236-301` · `hooks/useThemeMessenger.ts`                                                                                                                                                                                                      |
| F11 | 그대로 — `tintToSkiaColors` 가 accent 5 + chart 팔레트 N 을 oklch → hex 로 파생 (`accent-hover` = `mixWithBlackSrgb(accent, 85)` · pressed 75 — DOM `color-mix(in srgb, var(--accent) 85%, black)` 와 같은 식) · `neutralToSkiaColors` · `radiusScaleToSkia` (BASE 0/4/6/8/12/9999 × factor 0/0.5/1/1.5/2)                                                                                                                                                                                                                                                                                        | `utils/theme/tintToSkiaColors.ts:130-200` · `radiusScaleToSkia.ts`                                                                                                                                                                                            |
| F12 | **바뀜** — Components 페이지 = catalog origin 57 (ADR-228) + item template/조합 자식 ref (229) + 상태 변형 23 (230). "컴포넌트 전집이 테마를 입는 표면" 이 이미 있다 → R5 해소, G5 는 이 집합으로 1회                                                                                                                                                                                                                                                                                                                                                                                             | `components/catalogOrigins.ts` · `stateVariantOrigins.ts`                                                                                                                                                                                                     |
| F14 | **DOM 채널 = `THEME_VARS` postMessage** `{name, value, isDark}[]` — tint 1 (`--tint: var(--<preset>)`) · neutral 11 (hex) · radius 8 (`--radius-xs…4xl` × factor) 를 light/dark 두 벌 + `SET_DARK_MODE` + `THEME_BASE_TYPOGRAPHY`. 보내는 곳 = BuilderCore 의 store subscribe (tint/neutral/radiusScale/darkMode 변경) + iframe ready 초기 1회. 받는 곳 = `preview/messaging/messageHandler.ts:240`                                                                                                                                                                                               | `BuilderCore.tsx:708-799` · `messageHandler.ts:68-82 · 240-250`                                                                                                                                                                                               |
| F15 | **Skia 채널 = specs 모듈 맵 mutation** — `lightColors/darkColors` (키: accent 5 · neutral 5 · negative/informative/positive/notice + subtle/strong · base/raised/layer-1/layer-2/elevated/disabled · border 3 · transparent/white/black · chart N) · `typography` (text-2xs~5xl + `--line-height` + sans/mono) · `radius` (none/xs/sm/md/lg/xl/2xl/full) · `lightShadows/darkShadows`. production `resolveToken(ref, theme)` 소비자 **21 파일** (Skia 4 · layout 2 · text 2 · overlay 1 · Styles 패널 3 · shared 2 · specs 4 · 기타 3) — 전부 같은 맵을 읽으므로 **맵 설치 = 활성 snapshot 설치** | `packages/specs/src/renderers/utils/tokenResolver.ts:20-60` · `primitives/{colors,typography,radius,shadows}.ts` · grep                                                                                                                                       |
| F16 | typography 축 두 갈래 — (a) rule TokenRef `{typography.text-*}` → specs `typography` 맵 (Skia `specTextStyle` · layout `utils.ts`/`cssResolver.ts` · DOM 생성 CSS `--text-*`) · (b) `baseTypography {fontFamily, fontSize, lineHeight}` = 문서 body 기본 (Skia `styleConverter`/`cssResolver` · Preview `THEME_BASE_TYPOGRAPHY` → body style). §3.2 의 `typography.base-*` 3 키가 (b)                                                                                                                                                                                                             | `canvas/utils/specTextStyle.ts:305-315` · `layout/engines/{utils,cssResolver}.ts` · `messageHandler.ts:248`                                                                                                                                                   |
| F17 | focus 축 — DOM 만: `{focus.ring.default                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | inset}` recipe (`outline: var(--focus-ring-width) solid var(--focus-ring)`+ offset) · **Skia 는 focus ring 을 그리지 않는다** (ADR-150 경계,`buildSpecNodeData.ts:2174` 주석). 두 leg 대칭 검증 대상 아님 — DOM 변수 3 (`--focus-ring`·`-width`·`-offset`) 만 | `tokenResolver.ts:337-350` · `CSSGenerator.ts:1268` |
| F18 | 쓰기 경로 — canonical root 필드 mutation helper `mutateActiveDoc` (clone-on-write · documentVersion) 이 있고 `runCanonicalMutation` + history event 가 ADR-184/185 경로. `themes`/`tokens` 전용 action 은 0 → Phase 1 신설                                                                                                                                                                                                                                                                                                                                                                        | `stores/canonical/canonicalDocumentStore.ts:950-985`                                                                                                                                                                                                          |

### 8.2 소비자 배선 표 (§3.3 확정 — "같은 snapshot" 의 실체)

활성 테마를 해석한 결과 = **`ResolvedThemeSnapshot`** (순수, shared) = `{ colors: {light, dark}, typography, radius, shadows: {light, dark}, border: {none, thin, thick}, focus: {color, width, offset, insetOffset}, base: {fontFamily, fontSize, lineHeight}, cssVars: {light: [...], dark: [...]} }`. 설치 함수 하나 `installThemeSnapshot(snapshot)` (builder) 가 아래를 **한 번에** 한다 — 노드별 snapshot 0.

| 축           | Skia/레이아웃 소비 (설치 = 맵 덮어쓰기)                                                                                                        | DOM 소비 (설치 = `THEME_VARS` 한 벌)                                                                                                            | Publish                                           |
| ------------ | ---------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- |
| color        | `lightColors/darkColors` 키 전부 (`tintToSkiaColors`/`neutralToSkiaColors` 가 하던 파생을 snapshot 이 대신)                                    | `--tint` (preset 이면 `var(--<preset>)`, hex 편집이면 `--accent` 직접) · `--color-neutral-N` · 명시 파생 키는 `--accent-hover` 등 override 슬롯 | 같은 `cssVars` 를 payload 로 (`themeConfig` 대체) |
| typography   | `typography` 맵 (text-* · line-height) + `baseTypography` (Skia `styleConverter`/`cssResolver` · text 측정 캐시 무효화 = `notifyLayoutChange`) | `--text-*` / `--text-*--line-height` + `THEME_BASE_TYPOGRAPHY`                                                                                  | 같은 변수 + body font                             |
| radius       | `radius` 맵 8 키 (px)                                                                                                                          | `--radius-xs…4xl` (현행 8) + `--radius-base`                                                                                                    | 같은 변수                                         |
| border.width | 신설 `border` 맵 `{none:0, thin:1, thick:2}` — `resolveToken` `border` category + implicitStyles/size 산식 (Phase 3)                           | `--border-width-none                                                                                                                            | thin                                              | thick` | 같은 변수 |
| shadow       | `lightShadows/darkShadows` 맵                                                                                                                  | `--shadow-sm                                                                                                                                    | md                                                | lg`    | 같은 변수 |
| focus        | (Skia 소비 0 — ADR-150)                                                                                                                        | `--focus-ring` · `--focus-ring-width` · `--focus-ring-offset` · `--focus-ring-inset-offset`                                                     | 같은 변수                                         |

commit 순서 (§3.3): 맵 설치 → `themeVersion+1` 1회 → `notifyLayoutChange` 1회 → `THEME_VARS` 1회 (store subscribe 는 `activeThemeId`/`themesVersion` 하나만 본다). Preview ready 재전송은 같은 `cssVars`.

### 8.3 migration 입력 (§3.2 확정)

- `legacyConfig` = localStorage `composition-theme-config-<projectId>` (F4) — 없으면 null. `legacyWriteThrough` = 항상 `false` (F5). `source` = "local-project" (dashboard open) / "import" (파일 import 경로 — `importProject` 계열에서 legacyConfig 를 넘기지 않는다).
- 실전 경로는 행렬 2행 하나: **구 문서 (themes 부재) + off → legacy 필드 있으면 그것, 없으면 `DEFAULT_THEME_SELECTION` + `DEFAULT_BASE_TYPOGRAPHY`**. 나머지 행은 unit fixture 로 고정 (import 에 legacy 미혼입 · 유효 구 ThemeSnapshot 우선 · 무효값 보고).
- 기존 `doc.tokens` 델타는 실전 0 (F13) — 있으면 spec-token 은 Default 테마 델타로, user-defined 는 root 로 (fixture).
- 완료 표식 = DB 저장 성공 뒤 문서의 `themes` 가 컬렉션 모양. localStorage 는 저장 성공 뒤 `{ activeThemeId }` 캐시로 축소 (구 키 삭제는 하지 않고 덮어쓴다 — rollback 은 구 키 백업 `composition-theme-config-<projectId>.pre227`).
- History 0 (migration) · 이후 사용자 편집은 `runCanonicalMutation` (F18).

### 8.4 변경 파일 집합 (중복 제거, Phase 별)

| Phase | 파일 (신규 ★)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 수  |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- |
| 1     | shared `composition-document.types.ts` (ThemesCollection · ThemeDefinition) · ★ shared `theme/themesCollection.ts` (기본 컬렉션 · 검증 · 델타 helper · migrate 순수 함수) · `adapters/canonical/themesAdapter.ts` (컬렉션 read/apply) · `variablesAdapter.ts` (tokens = user-defined 만) · `stores/canonical/canonicalDocumentStore.ts` (themes action 5: add/remove/rename/setActive/setToken) · `canonicalHistoryEvents.ts` + replay · `BuilderCore.tsx` (legacy 캡처 → migrate → 저장 성공 → 캐시 축소) · `themeConfigStore.ts` (persist 축소 · 문서 우선) · ★ `__tests__/adr227ThemesMigration.test.ts`                                                                                                    | 10  |
| 2     | ★ shared `theme/resolveThemeSnapshot.ts` (preset seed → root user-defined → 델타 → 파생 · cssVars) · ★ builder `utils/theme/installThemeSnapshot.ts` (맵 설치 + version + notify + THEME_VARS) · `tintToSkiaColors.ts`/`neutralToSkiaColors.ts`/`radiusScaleToSkia.ts` (seed 생성기로 재사용, mutation 은 install 로) · `BuilderCore.tsx` (THEME_VARS 를 cssVars 로 · publish payload) · `preview/messaging/messageHandler.ts` (THEME_VARS 교체 = 이전 벌 제거) · `apps/publish/src/App.tsx` (cssVars 적용) · `tokenResolver.ts` COLOR_TOKEN_TO_CSS hover/pressed override 슬롯 · `colorTokenToCss.ts` 대조 · `generateThemeCSS.ts` 삭제(dead, 승인 후) · ★ `adr227-theme-parity-live.mjs` (ADR-198 하니스 축) | 11  |
| 3     | specs `token.types.ts` (border category) · `spec.types.ts` (borderWidth: number \| TokenRef) · ★ `primitives/border.ts` · `tokenResolver.ts` (border case) · `CSSGenerator.ts` (border-width 채널) · shared `toSkiaStyle.ts` · builder `implicitStyles.ts` · `engines/utils.ts` · `componentRulesTable.ts` (48+6+8+2 참조화) · ★ `borderWidthLiteral.static.test.ts` (ratchet)                                                                                                                                                                                                                                                                                                                                 | 10  |
| 4     | `panels/themes/ThemesPanel.tsx` (+ ★ `ThemeList.tsx` · ★ `TokenEditor.tsx` · CSS) · i18n ko/en · `useThemeMessenger.ts` 정리                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 6   |
| 5     | 검증만 — ★ `adr227-components-theme-live.mjs` · `perf-baseline.mjs` (theme-switch class)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 2   |

합계 ≈ 39 (신규 11). §5 의 22/25 추정 폐기.

### 8.5 G0 판정

- caller 배선 표 (8.2) · migration 행렬 입력 (8.3) · border sweep (F7: 숫자 48 + 문자열 6 + shorthand 10 + outline 17 — outline 은 focus 축이라 Phase 3 예외 목록에 두고 rule 재설계 금지) · 변경 파일 39 확정. 미연결 소비자 0 — resolveToken 21 파일이 같은 맵을 읽는다 (F15).
- 전제 무변경 — 스키마/의존 방향/SSOT 경계 모두 §1 그대로. 축소: R5 해소 (F12) → Phase 5 는 1회.

## 9. Phase 기록

### Phase 1 — 스키마 · migration · adapter · 문서 우선 쓰기 (G1 PASS, 2026-09-22)

- **스키마**: `ThemePreset` (= ThemeSnapshot 4 필드) · `ThemeDefinition { id, name, preset, tokens }` · `ThemesCollection { active, items, order }` — `CompositionDocument.themes?: ThemesCollection` (구 단일 모양은 `unknown` 으로 읽어 migration). `tokens` root 는 user-defined 만 (주석 갱신).
- **순수 모듈** `packages/shared/src/theme/themesCollection.ts`: `DEFAULT_THEME_ID/NAME/PRESET` · `BASE_TYPOGRAPHY_TOKEN_KEYS` (`typography.base-font-family|size|line-height`) · `isThemePreset/isThemeDefinition/isThemesCollection` · `readLegacyThemeSnapshot` · `normalizeThemesCollection` (active 부재 → order[0] · order 순열 보정 · id 불일치 · 변경 0 이면 같은 객체) · `createThemesCollection` · `getActiveTheme` · **`migrateThemesField(doc, {legacyConfig, legacyWriteThrough, source, baseTypographySeed})`** (§3.2 행렬 5행 + 구 customTokens 정규화 + root spec-token → Default 델타 · user-defined 잔류 · 무효 entry 경고 · 재실행 멱등) · 항목 연산 `addTheme/removeTheme(마지막 금지·활성 이웃 승계)/renameTheme/setActiveTheme/setThemePreset/setThemeToken(null=seed 복귀)/duplicateTheme`. unit 14.
- **adapter** `themesAdapter.ts`: `snapshotThemesFromConfig` → 컬렉션 · `readCanonicalThemes` → 활성 preset (컬렉션 · 구 snapshot 둘 다) · `applyCanonicalThemes` + `setBaseTypography` (델타 3 키 → body 서체) · `readBaseTypographyFromTheme`. env flag 게이트 제거 (항상 문서 우선). `themes.test.ts` 9 로 갱신.
- **canonical action** `setThemes(collection)` (`CanonicalDocumentActions` + store `mutateActiveDoc`, 같은 참조 no-op).
- **history** `type: "theme"` + `themeEvent { kind, before, after, themeName }` (컬렉션 전체 before/after — N × 델타라 작다) · `applyThemeHistoryEntry` (canonical `setThemes` + 등록 콜백 `registerThemeHistoryApplier` 로 런타임 재적용 + persist) · undo/redo/goToIndex early-branch (snapshot-restore 뒤 — 정적 게이트의 40줄 창 안) · `syncDatabaseForEntries` skip · addEntry DEV guard 면제 · IndexedDB 계약 · 패널 라벨 5 키 (`history.entryTheme*`, ko/en formatted) · 아이콘 SwatchBook. 정적 게이트 +1 · 왕복 unit 5.
- **쓰기 진입점** `panels/themes/themeActions.ts` (page-guide 어법): `readThemesCollection` · `applyActiveThemeToRuntime` (활성 preset + base typography → themeConfigStore setter, 같은 값 skip · `markThemeActiveCache`) · `commitThemes` (history 1 + setThemes + 재적용 + persist, 무변경 0) · `addThemeFromActive/addTheme/removeTheme/renameTheme/setActiveTheme/setActiveThemePreset/setThemeToken/setActiveThemeBaseTypography`. **ThemesPanel 의 tint/dark/neutral/radius/typography 핸들러가 이 진입점을 탄다** (컬렉션 부재 = migration 전 창에만 종전 store 직접).
- **승계** (`BuilderCore.tsx` 로드 뒤): `readLegacyThemeConfig(projectId)` (캐시 모양 `migrated` 면 null) → `migrateThemesField` → 변경이면 `setDocument` + `persistActiveCanonicalDocument` → **저장 성공 뒤에만** `markThemeDocumentOwned` (legacy 를 `.pre227` 로 백업 · localStorage 를 `{migrated, activeThemeId}` 캐시로 축소, 이후 setter persist 는 캐시만) → `applyActiveThemeToRuntime`. 저장 실패면 legacy 유지 + 다음 부팅 재시도. `themeConfigStore` 는 파생 런타임 (`__composition_THEME_CONFIG__` dev 핸들).
- **G1**: unit — 행렬 5행 + tokens 분할 + 멱등 (shared 14) · 왕복/캐시 축소/백업/멱등 (builder 5) · adapter 9 · 정적 1. **live** `adr227-themes-migration-live.mjs` **6/6** (headed · Compare Mode 없음): M1 새 프로젝트 = 컬렉션 + 캐시 모양 · M2 legacy localStorage (purple · dark · zinc · lg · fontSize 20) + IndexedDB root `themes` 삭제 → reload → preset 승계 · fontSize 델타 · `.pre227` 백업 · 캐시 축소 · 런타임 동일 (실효 시각 Δ0) · M3 Themes 패널 Red 클릭 → 문서 tint=red · history +1 · accent Button Skia 픽셀 `[54,96,240]` → `[197,54,49]` · M4 reload 보존 · M5 Undo → purple `[140,64,219]` · Redo → red. builder 6936 PASS · shared 1390 PASS · type-check PASS.
- 하니스 함정: 픽셀 오라클은 `variant:"accent"` Button (default `primary` 는 neutral 채움이라 tint 무반응) · Themes 패널이 캔버스 왼쪽 위를 덮으므로 focusOn 은 (700, 420).
- 남은 것 (Phase 2~): 런타임 설치를 `installThemeSnapshot` 한 번으로 (지금은 setter 4~~5 회 = notifyLayoutChange 4~~5 회) · 토큰 델타 (color hex · radius px …) 의 실제 소비 (지금은 base typography 3 키만 런타임에 닿는다 — 나머지 델타는 저장만) · DOM `THEME_VARS` 를 cssVars 로 · Publish payload.

### Phase 2 — 카테고리별 resolve · 1회 설치 · DOM/Publish 배선 (G2 PASS — Canvas live · Preview/Publish 는 unit 고정 + 사용자 확인 대상, 2026-09-22)

- **resolve** `apps/builder/src/utils/theme/resolveThemeSnapshot.ts` — `resolveThemeSnapshot(theme, {rootTokens?, baseTypographySeed})` 순수 함수. 우선순위 = preset seed (`createAccentColorTokens(tint)` · `resolveNeutralColorTokens(neutral, mode)` · `resolveRadiusTokens(radiusScale)` · specs 서체/shadow seed 를 import 시점에 캡처) → root user-defined (fallback) → 테마 델타 → 파생 (미명시 `<key>-hover/pressed` 는 base 에서 `mixWithBlackSrgb`; 명시가 항상 우선). `color.accent` hex 는 **tint 대체** — `hexToOklch` 로 (c, h) 를 뽑아 accent 5 단계 + chart 팔레트를 Skia 와 같은 식으로 파생, DOM 은 `--tint: #hex`. 결과 `{ lightColors, darkColors, typography, radius, lightShadows, darkShadows, baseTypography, darkMode, cssVars[{name,value,isDark}] (name+isDark 로 dedupe), warnings }`. border 카테고리는 경고만 (Phase 3).
- **설치** `installThemeSnapshot.ts` — `installThemeSnapshot(snapshot)` 1회: specs 토큰 맵 (`lightColors/darkColors/typography/radius/lightShadows/darkShadows`) 덮어쓰기 → `themeConfigStore.applyResolvedTheme` **set 1 (themeVersion +1)** + persist → `notifyLayoutChange` 1 → geometry 축 (typography · radius · base) 이 바뀐 때만 등록 콜백 (`registerThemeLayoutInvalidator`: `bumpLayoutThemeEpoch` + `invalidateLayout`) → Preview `THEME_VARS` (`replace: true`) + `SET_DARK_MODE` + `THEME_BASE_TYPOGRAPHY`. 종전 setter 4~~5 회 (notify 4~~5) → 1 회. `getCurrentThemeSnapshot` 은 iframe ready 재전송과 publish payload 가 읽는다.
- **DOM 채널**: `runtimeStore.setThemeVars(vars, replace)` — replace 면 이전 테마의 override 벌 (예: A 의 `--fg`) 이 남지 않는다. hover/pressed override 슬롯: `tokenResolver.ts` / `colorTokenToCss.ts` 의 `*-hover|pressed` 항목이 `var(--X-hover, color-mix(...))`, `.button-base` 상태 배경이 `var(--button-color-hover|pressed, color-mix(...))`, CSSGenerator button-base 는 **파생 토큰 (`{color.<k>-hover}`) 일 때만** `--button-color-hover: var(--X-hover)` 를 낸다 — 기본 시각 무변경 (미명시면 슬롯이 invalid → 종전 color-mix 폴백). 생성 CSS 8 파일 재생성.
- **Publish**: payload `themeVars/themeBaseTypography/themeDarkMode` → `apps/publish/src/App.tsx applyThemeVars` (light/dark 블록 · body 서체 · `.dark` 클래스). 구 `themeConfig` 는 폴백 유지.
- **페이지 레이아웃 캐시**: `scene/layoutCache.ts` 시그니처가 `theme=<epoch>` 로 시작 — 요소 props 만 읽던 시그니처라 테마 typography/radius 변경이 캐시 hit 로 무시되던 결함 (live S5 에서 발견: Skia 노드 fontSize 24 인데 폭 72 그대로). 종전 `setBaseTypography` 경로도 같은 구멍이었다.
- **radius 대칭 수리**: 종전 Skia `radiusScale` 은 xs/2xl 을 스케일하지 않았고 DOM 은 했다 → `RADIUS_BASE_PX` 하나로 두 leg 통일 (Phase 0 F 표에 없던 비대칭).
- **G2 unit 13** — `resolveThemeSnapshot.test.ts` 11 (seed · lg 배율 · accent hex 대체 · neutral 명시 + 파생 · 명시 hover 우선 · typography px/비율 · base/radius/shadow/focus · reset · root fallback + 경고 · hexToOklch 왕복 · 설치 = 맵 덮기 + version 1 + notify 1 + postMessage 3) · `runtimeStore.themeVars.test.ts` 2 (병합 · replace). builder **6949 PASS** · shared 1390 · specs 1378 · type-check PASS.
- **G2 live** `apps/builder/scripts/adr227-theme-snapshot-live.mjs` **8/8** (headed · Canvas 만 — Skia 픽셀 · layout map · store · reload): S1 기준 (accent 파랑 `[54,96,240]` · primary 검정 · 모서리 = 배경) · S2 `color.accent #00a000` → 초록 `[0,141,0]` · primary 불변 · themeVersion +1 한 번 · reset → 파랑 · S3 `color.neutral #ff00ff` → primary 마젠타 · accent 불변 · S4 `radius.md 0` → 모서리 픽셀 = 채움 · reset → 배경 · S5 `typography.text-sm 24` → Button 폭 72 → 104 → 72 (재레이아웃) · S6 `darkMode: dark` → primary 채움 `[245,245,245]` → light 검정 · S7 델타 2 → reload → 초록 + 각진 모서리 (문서 정본) · 페이지 오류 0.
- **사용자 확인 대상 (Compare Mode 미개방 — 사용자 지시)**: Preview iframe 의 `THEME_VARS` replace 결과 · hover/pressed 슬롯 · Publish `applyThemeVars`. unit 이 고정하고 있고 live 는 Canvas 만 돌렸다.
- 남은 것: border (Phase 3) · Themes 패널 토큰 편집 UI (Phase 4 — 지금은 `__composition_THEME_ACTIONS__.setThemeToken` dev 핸들) · shadow/focus 델타는 resolve 가 받지만 live 오라클 없음 (Skia 는 shadow 를 페이지 배경 없이 그리고 focus 는 DOM 만).

### Phase 3 — border 폭 토큰 참조화 · 전체 소비자 확장 (G3 PASS — Canvas live · DOM 은 unit 고정 + 사용자 확인 대상, 2026-09-22)

- **토큰**: specs `BorderWidthTokens { none 0 · thin 1 · thick 2 }` (`primitives/border.ts` — 활성 테마가 덮어쓰는 맵) · `BorderWidthTokenRef` = `{border.width.<k>}` (3-segment: category `border`, name `width.<k>`) · `StrictTokenRef`/`TokenCategories`/`isValidTokenRef` 확장 · `DEFAULT_BORDER_WIDTH_TOKEN = {border.width.thin}` (종전 소비자마다 흩어져 있던 `?? 1` 의 단일 원천). CSS 변수 seed `--border-width-none|thin|thick` (`shared-tokens.css`, 단일 정의).
- **resolver**: `resolveToken` border case · `tokenToCSSVar` → `var(--border-width-<k>)` · `cssVarToTokenRef` 역변환 · **`resolveBorderWidthPx(value, fallback?)`** (숫자 통과 · 토큰 해석 · "Npx" 파싱 · 미지정 = thin 현재값 · 해석 불가는 fallback — 1 로 숨기지 않는다) · **`borderWidthToCSS`** (숫자 → px · 토큰 → var · 미지정 → thin 변수, `{…}px` 출력 없음).
- **타입**: `SizeSpec.borderWidth` · `ContainerStylesSchema.borderWidth` = `number | BorderWidthTokenRef` (shared `ComponentRuleSize.borderWidth` 는 이미 `number | string`).
- **DOM 채널**: CSSGenerator 3 채널 (variant `border: … solid` · containerStyles `border:` shorthand · size `border-width:`) 전부 `borderWidthToCSS` — 생성 CSS 28 파일 `1px` → `var(--border-width-thin)` (computed 동일). rule 의 raw CSS `border: "1px solid …"` 10 → `var(--border-width-thin) solid …`. Preview/Publish 는 Phase 2 cssVars 에 `--border-width-*` 3 줄이 얹힌다.
- **Skia/layout 채널** (모두 `resolveBorderWidthPx`): `buildCatalogShapes` (box border) · `skiaPrimitives` GridListItem card · `implicitStyles` (input rows 산식 · size 축 implicit 주입 · Button 컨테이너 bw) · `engines/utils.ts` (`deriveSizeConfig.borderWidth` 는 **getter** — 모듈 로드 시 고정하면 테마 전환 뒤 stale, 원복 RED 확인 · SelectTrigger/DatePicker content-box 산식 2) · `resolveContainerStylesFallback` (`"1px"` 였던 layout 채널이 토큰 → 숫자로 해석돼 들어온다 — 종전과 같은 값).
- **rule 참조화** (`componentRulesTable.ts` 63 행): `borderWidth` 숫자 47 (0:3 → none · 1:41 → thin · 2:3 → thick; F7 의 48 은 주석 1 줄 포함) · `"1px"` 6 → thin · `border: "1px solid var(--border)"` 8 + `"1px solid"` 2 → `var(--border-width-thin) solid …`. **ratchet** `packages/shared/src/catalog/__tests__/borderWidthLiteral.static.test.ts` (값 walk + 소스 텍스트 + 예외 개수 고정).
- **확정 예외** (테마 축 밖, 위치·사유): `outline: "2px solid var(--accent)"` 17 = focus 축 (ADR-150) · `"border-bottom": "1px solid var(--border)"` 10 = Table/ListBox 행·섹션 구분선 (divider 축 — Skia 도 리터럴 1, F7 inventory 에 없던 항목이라 여기 기록) · 생성기 archetype 블록 Slider thumb `border: 2px solid var(--bg)` (Skia thumb 도 2) · skiaPrimitives 의 selection checkbox 2 · selected card 2 (DOM `[data-selected]{border-width:2px}` 짝, 리터럴 쌍).
- **테마 축**: `resolveThemeSnapshot` border 카테고리 (`border.width.<k>` 숫자 ≥ 0 → `border` 맵 + `--border-width-<k>`; 무효는 경고) · `installThemeSnapshot` 이 specs `borderWidth` 맵을 덮고 geometry 판정에 border 포함 (레이아웃 캐시 epoch).
- **찾은 결함**: DropZone 기본 `props.style.borderWidth: "2px"` 인라인 (레이아웃 엔진이 border 를 읽게 하려던 미러) 이 두 leg 에서 테마 thick 을 가렸다 (live B3 첫 실행 stroke 2 유지) → 인라인 제거 (`createDefaultDropZoneProps` · FileUpload factory 자식). layout 은 implicit 주입, DOM 은 생성 CSS `border-width: var(--border-width-thick)` — seed Δ0. 기존 문서의 저장된 인라인은 저작값으로 그대로 (양 leg 우선).
- **G3 unit**: specs tokenResolver +7 · CSSGenerator containerStyles +1 (기본 thin 변수 기대로 1 수정) · shared ratchet 4 · builder `borderWidthThemeRead` 2 (getter 원복 RED) · `dropZoneBorderThemeRead` 2 · `resolveThemeSnapshot` +1 (border 3/4 · 음수 경고 · install 맵) · 기대값 갱신 (fallback 테스트 `"1px"` → 1, 드리프트 snapshot 1, tag chip · skia rule 토큰). specs **1386** · shared **1394** · builder **6954** PASS · type-check PASS · codex:preflight PASS.
- **G3 live** `apps/builder/scripts/adr227-border-token-live.mjs` **5/5** (headed · Canvas 만): B1 secondary Button md 기준 — Skia stroke 1 · layout 30 · (x+0) 픽셀 `[212,212,212]` 테두리 / 안쪽 `[250,250,250]` · B2 `border.width.thin` 3 → stroke 3 · 높이 34 · 폭 95 → 99 · (x+2) 도 테두리 · reset → 1 / 30 / 95 (Δ0) · B3 DropZone thick 2 → `border.width.thick` 4 → 4 · Button 불변 1 · reset 2 · B4 thin 3 → reload → 34 / 3 (문서 정본) · 페이지 오류 0. **G2 회귀** `adr227-theme-snapshot-live.mjs` 8/8.
- **사용자 확인 대상 (Compare Mode 미개방)**: Preview/Publish 의 `border-width: var(--border-width-*)` 와 THEME_VARS 의 `--border-width-*` 3 줄 (unit 고정).
