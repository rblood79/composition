# ADR-229 Design Breakdown — TagGroup item template origin + 저작 조합층의 origin 안 instance

> 본문: [229](../completed/229-collection-item-template-and-nested-origin-instances.md). 상태 Accepted (2026-09-21, Phase 0 완료 — §7). 이 문서가 Phase·파일·게이트 절차의 정본이다 — ADR 본문에는 포인터만.

## 1. 전제 lock-in (fork 4 질문)

1. **base / 응용**: base = ADR-147/148/161 (collection item template · ref-composite 컨테이너) + ADR-228 (팔레트 전 항목 origin · instance 계약). 이 ADR 은 그 **응용** — 같은 모델을 TagGroup 과 조합 origin 의 자식에 적용한다. 새 추상 0.
2. **schema 직교성**: canonical 스키마 변경 0 — `type:"ref"` · `slot: string[]` · `metadata.slotRole` 은 148/161 의 것. 새 필드 없음. 공용 slot vocabulary에 avatar를 additive 확장하며 D2 PropContract는 유지한다. 228 의 instance 계약 (명시 initialProps = origin 과 다른 키) 을 자식 ref 에도 그대로.
3. **선행 ADR 전제 reverse 검증**: 148 의 "template origin 은 컨테이너 origin `slot[0]`" 방향 그대로 (TagList 가 slot 보유자). 228 의 "sub-part 는 origin 화하지 않는다 (RAC 컴포넌트만)" 그대로 — 사용자 판정 (a) 가 재확인 (2026-09-21 AskUserQuestion).
4. **codex 1차 진입 시점**: 이 breakdown §2 inventory 가 확정된 뒤 (Phase 0 착수 전) — 전제 결함은 §2 표에서 잡힌다.

사용자 confirm 기록: 2026-09-21 AskUserQuestion "(a) 저작 조합만 먼저 (Recommended)" 선택 — 메모리 `project-origin-instance-composition-scope-2026-09`.

## 2. 코드 사실 inventory (2026-09-21, main `7c1cb78e3`)

| ID  | 사실                                                                                                                                                                                                                                                                                                                                                                   | 경로:라인                                                                                                                                                                                                                                                                                                 |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1  | ListBox item template: origin id 상수 · slot 자식 Icon/Text(label)/Text(description) `{icon}`/`{label}`/`{description}` · `metadata.slotRole` · `ensureTemplateOrigins` 로 시드                                                                                                                                                                                        | `components/listbox/listBoxTemplateOrigins.ts:4-60`                                                                                                                                                                                                                                                       |
| F2  | 행 projection 이 template origin 을 읽는 경로: `resolveListBoxTemplateOriginId` (ref → master `slot[0]`) · `resolveGridListTemplateOriginId` · `templateOriginNode` 의 `props.style` + `resolveSlotComposition(children)` 을 행/카드에 주입, `projection.templateOriginId` 보존                                                                                        | `canvasSceneNode.ts:574, 640-651, 857-867, 1285-1300`                                                                                                                                                                                                                                                     |
| F3  | TagGroup projection: `resolveDataBoundTagProjection` (owner TagGroup `items` 우선) → `appendTagRowProjection` — rowsGroup `projection.kind:"tag-rows"` `templateOriginId: null` · chip `type:"Tag"` props `{children: row.label, style:{width:"fit-content"}, _isSelected, variant, size, isDisabled, icon, avatar, allowsRemoving}`                                   | `canvasSceneNode.ts:1984-2150, 2729, 2820`                                                                                                                                                                                                                                                                |
| F4  | 조합 origin 의 primitive 자식이 plain: Form `__field-{n}` TextField (자식 Label/Input/FieldError) · `__actions` ButtonGroup > `__action-{n}` Button · Toolbar Button/Separator · IconButton root Button · ADR-228 generic seed 가 factory definition 자식을 그대로 (ButtonGroup Button×2 · Pagination Button×5)                                                        | `form/formTemplateOrigins.ts:80-160` · `toolbar/toolbarTemplateOrigins.ts:36-76` · `iconbutton/iconButtonTemplateOrigins.ts:172` · `factories/definitions/DisplayComponents.ts:177-195` · `NavigationComponents.ts:152-195` · `catalogOrigins.ts:85-130` (`flattenDefinitionChildren`/`nestSeedElements`) |
| F5  | **정정 (h1)**: `materializeOverrideChildren`만 중첩 master 해소·propsSchema 재바인딩을 지원한다. 일반 origin.children을 복제하는 `materializeSyntheticDescendants`는 ref를 미해소로 남기며 outer loop는 원래 input만 순회한다. 일반 자식 경로 확장이 선행 구현 대상이다.                                                                                               | `adapters/canonical/canonicalRefResolution.ts:546-563, 682-737, 786`                                                                                                                                                                                                                                      |
| F6  | Preview 의 template 소비: `App.tsx` 가 resolved 노드에서 `component-listbox` master `slot` → item origin → `compositionOf(originId)` 를 renderContext `listBoxTemplateSlotComposition / gridListTemplateSlotComposition / menuItem` 으로 주입 · DOM ListBox 렌더러는 anchor → context → 리터럴 순 fallback                                                             | `preview/App.tsx:286-397, 831-859` · `shared/src/renderers/SelectionRenderers.tsx:190-225`                                                                                                                                                                                                                |
| F7  | DOM TagGroup 렌더: `renderTagGroup` 이 items → `<Tag>` · `renderTagLeadingSlot(item)` (avatar > icon) · chip 스타일은 hand CSS `TagGroup.css .react-aria-Tag` (생성 CSS 없음)                                                                                                                                                                                          | `shared/src/renderers/CollectionRenderers.tsx:239-410` · `shared/src/components/styles/TagGroup.css:34-66`                                                                                                                                                                                                |
| F8  | origin repair 는 existing `props`/`children` 을 보존한다 (손 seed `repairOrigin` · generic `repairCatalogOrigin`) — 기존 문서의 origin 자식은 새 seed 모양으로 바뀌지 않는다                                                                                                                                                                                           | `form/formTemplateOrigins.ts:187-200` · `catalogOrigins.ts:244-270`                                                                                                                                                                                                                                       |
| F9  | **정정 (m3)**: originIds는 dependency 생성기가 아니며 repair callback 반환 노드만 보충한다 (`ensureTemplateOrigins.ts:92-108`). ensurer 등록: `REUSABLE_ORIGIN_ENSURERS` (손 seed 5 + ListBox/GridList + generic 50) · entry ↔ ensurer 불변식 테스트 · `ensureTemplateOrigins(document, originIds, repair)` 가 Components body 안 제자리 보정 + 누락만 append (228 h2) | `components/reusableCompositeOrigins.ts:40-75` · `components/ensureTemplateOrigins.ts:84-120`                                                                                                                                                                                                             |
| F10 | 생성 경로: 팔레트 추가는 origin id 가 있으면 `type:"ref"` 만 (228) · complex definition 의 canonical subtree 변환은 origin seed 와 같은 `flattenDefinitionChildren`                                                                                                                                                                                                    | `hooks/useElementCreator.ts` (`resolveCreationParentForType` · ref 생성) · `catalogOrigins.ts:85-130`                                                                                                                                                                                                     |
| F11 | instance 계약: 명시 initialProps = origin 과 다른 키 (+chartType) · 소유권 = 값 diff `diffRefPropsAgainstMaster` (chartType 예외) · 편집 읽기는 resolved (`getFirstProjectableNodeResolvedProps` · `useCanonicalPropertyResolvedElement`)                                                                                                                              | ADR-228 본문 §Decision · `stores/canonical/canonicalTraversalHelpers.ts` · `panels/properties/hooks/useCanonicalPropertyRead.ts`                                                                                                                                                                          |
| F12 | 범위 밖 근거: field 가족 Label/Input/FieldError · picker SelectTrigger/DateInput 은 read-only sub-part (DOM parent props self-compose · 시각 = parent rule delegation)                                                                                                                                                                                                 | `.claude/rules/ssot-hierarchy.md` §D3 read-only sub-part · `isDelegatedSubpartChild` (`@composition/shared`)                                                                                                                                                                                              |

**리뷰 반증 확인**: h1 일반 origin-child ref는 Toolbar instance 안에서도 `type:"ref"`로 남음; m2 `slotRole:"avatar"`는 공용 `slotRoles.ts` vocabulary에 없어 제거됨; m3 originIds에 Button id를 넣어도 callback이 반환하지 않으면 생성되지 않음. 현재 코드의 진단 RED이며 구현 완료 근거가 아니다.

**미실측 (Phase 0 이 채운다)**: (U1) 2단 override — Form instance 에서 `<instance>/component-form__action-1` 을 편집하면 어느 노드의 어느 필드에 저장되는가 (instance `children` override path? Button origin?) · reload 뒤 두 leg 값 · Undo. (U2) `flattenDefinitionChildren` 이 자식 type 이 reusable 이면 ref 를 만들 때 안정 id (`<originId>__<n>`) 와 propsSchema 재바인딩이 충돌하지 않는가. (U3) Tag chip 이 `Tag` rule 로 그려질 때 template origin root style 을 어느 층에 얹는가 (chip props.style 병합 vs scene props 주입) — ListBox 행의 `originStyle` 주입 지점과 같은지.

**Phase 0 실측 (2026-09-21, §7)**: U1 확정 — 저장 위치는 **바깥 instance 의 `descendants["<자식 ref path>[/<nested 자식>]"]` 하나**, 조합 origin · Button origin 무오염 (unit `editorPresentationCommitAdapter.test` + live). U2·U3 은 Phase 1·2 에서 각각 채운다. 추가 사실:

| ID  | 사실                                                                                                                                                                                                                                                                      | 경로:라인                                                                                                                |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| F13 | scene 층은 origin 자식 ref 의 `type` 을 이미 master type 으로 바꾸고 `.ref` 만 남긴다 (ADR-161) — 해소기의 ref 판정은 `type === "ref"` 가 아니라 `getCanonicalRefTarget` 로 (type 판정이면 live 에서 Form instance 안 TextField ref 가 자식 0 인 24px 빈 상자로 그려졌다) | `canvasSceneNode.ts` `visit` (ADR-161 주석) · `canonicalRefResolution.ts` `materializeSyntheticDescendants` ADR-229 분기 |
| F14 | Preview 축의 mode C 판정이 `"children" in override` 라 Text/Label `children` 문자열 patch 가 `.map` 크래시 — mode C = **배열** children 만 (Skia 축 `propsFromDescendantPatch` 와 같은 판정)                                                                              | `resolvers/canonical/index.ts` `applyOverrideToNode` · `adapters/canonical/instanceResolver.ts`                          |
| F15 | synthetic 자식 (`<instance>/<path>`) 은 1-level 이든 nested 든 Properties/Styles 패널이 빈 상태 ("Select an element") — 패널의 selected 파생이 projectable canonical 노드만 읽는다. 캔버스 선택 규약은 클릭 = instance root · 더블클릭 = synthetic 자식 드릴인            | `stores/index.ts` `getActiveCanonicalSelectedElement` · `panels/properties/hooks/useCanonicalPropertyRead.ts`            |
| F16 | presentation lane 의 ref-descendant target 은 nested ref 를 거치는 깊은 path 를 걷지 못했다 (`findDescendantNode` 가 master 자식만) — ref 자식을 만나면 그 origin 으로 열어 계속 내려간다 (끝점이 ref 면 origin ⊕ 자식 props)                                             | `presentation/editorPresentationCommitAdapter.ts` `findDescendantNode` / `openNestedRefChild`                            |

**Phase 1 실측 (2026-09-21, §7)**: U3 확정 — Tag chip 의 template root style 은 **chip `props.style` 병합** (scene 주입 · ListBox 행 `templateAnchorStyle` 과 같은 층, 별도 `originStyle` 채널 없음), fills 는 chip scene 노드 `fills` (ListBox 행 동형). 추가 사실:

| ID  | 사실                                                                                                                                                                                                                                                                                                                                                                       | 경로:라인                                                                                                                      |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| F17 | Preview 의 TagGroup chip 은 `renderTagGroup` 이 items 로 `<Tag>` JSX 를 만들어 넘기던 경로 (Path 2) 와 `TagGroup.tsx` rows 경로 (dataBinding) 둘이었다 — template 을 selected 상태별로 적용하려면 chip 렌더러가 하나여야 해서 Path 2 도 `items` prop 으로 rows 경로에 합류시켰다 (maxRows 미러도 leading 을 같이 잰다)                                                     | `shared/src/renderers/CollectionRenderers.tsx` `renderTagGroup` · `shared/src/components/TagGroup.tsx` rows 경로               |
| F18 | layout 의 catalog size 축 fallback 이 Tag rule `sizes[size].height` (md 30) 를 chip 의 **명시 높이**로 넣는다 — DOM chip (hand CSS) 은 높이 선언 없이 line-height + padding + border 로 자라서 template padding 을 키우면 DOM 38 / Canvas 30 이 됐다. template 이 있으면 chip style 에 `height: "auto"` 를 같이 실어 fallback 을 푼다                                      | `layout/engines/implicitStyles.ts` `resolveContainerStylesFallback` L3 size 축 · `canvasSceneNode.ts` `appendTagRowProjection` |
| F19 | DOM chip 의 `line-height` 는 비율 토큰 (`--text-sm--line-height: calc(1.25 / 0.875)`) 이라 font-size 18 에서 25.71 로 자라는데 Canvas 는 rule 의 px 20 을 고정으로 읽었다 (DOM 43.7 / Canvas 38). template 이 fontSize 를 바꾸면 rule `lineHeight/fontSize` 비율을 **unitless 배율**로 같이 싣는다 — 숫자 lineHeight 는 `parseLineHeight` 계약상 배율 (25.7 을 실으면 480) | `canvasSceneNode.ts` `withTagChipLineHeight` · `layout/engines/utils.ts` `applyTagChipTemplateStyle`                           |
| F20 | `catalogOrigins` ↔ `reusableCompositeOrigins` 는 `mainDocumentNormalization` ← `adapters/canonical/index` ← `elementUtils` ← factory definitions 순환 안에 있다 — 모듈 본문에서 seed 상수를 읽으면 진입점이 seed 쪽 (테스트) 일 때 TDZ. ensurer 맵을 `getReusableOriginEnsurers()` 함수로 (첫 호출 평가)                                                                   | `components/reusableCompositeOrigins.ts`                                                                                       |

**Phase 2 실측 (2026-09-21, §7)**: U2 확정 — generic seed 의 자식 ref 는 안정 id (`<originId>__<n>`) 그대로 두고 `type:"ref"` + `ref` 만 바뀐다 (propsSchema 미복제 A″ 라 재바인딩 충돌 없음 · `component-buttongroup__2` 가 Form instance 안에서 `<inst>/ButtonGroup/component-buttongroup__2` 로 3단 해소). 추가 사실:

| ID  | 사실                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 경로:라인                                                                                                                                                          |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| F21 | Preview 해소기의 descendants path segment 는 **id 고정**이었고 builder 축 (`getCanonicalRefPathSegment` — canonical 노드는 name → id · store 의 synthetic id · 패널 쓰기 키) 은 **name** 이라, name 을 가진 조합 자식 (Form seed "ButtonGroup" · "TextField/Name" · Toolbar "Button/Action 1") 의 patch 를 Preview 만 못 읽었다 (Phase 0 fixture 는 name 이 없어 못 잡음). Preview 도 같은 SSOT 로                                                                                                                                                                            | `resolvers/canonical/index.ts` `applyDescendantsToTree`                                                                                                            |
| F22 | instance `descendants` 만 바꾸는 쓰기 (props 빈 `updateAndSave`) 는 `layoutVersion` 을 올리지 않아 Skia 가 stale rect/글자로 남았다 — descendants patch 도 재레이아웃 대상                                                                                                                                                                                                                                                                                                                                                                                                    | `stores/inspectorActions.ts` `updateAndSave` `hasResponsiveChange`                                                                                                 |
| F23 | segment (name) 자체가 `/` 를 품는다 ("TextField/Name") — presentation lane 의 `findDescendantNode` 가 path 를 `/` 로 쪼개 그 자식을 못 찾았다 (Styles 편집 target 없음). 남은 path 앞부분과 segment 를 통째로 맞춘다 (긴 segment 우선)                                                                                                                                                                                                                                                                                                                                        | `presentation/editorPresentationCommitAdapter.ts` `findDescendantNode`                                                                                             |
| F24 | 변환 보류 실측 (신규 문서 1건): CardView 의 Card 자식 3 — 자식 0 ≠ Card origin 자식 4 (`subtree-mismatch`) → plain 유지 + warn. 그 외 조합 origin 의 reusable-type 자식은 전부 변환 (Form/Toolbar/ButtonGroup/Pagination/AvatarGroup/Nav/ToggleButtonGroup/CheckboxGroup/DisclosureGroup/FileUpload/DatePicker·DateRangePicker 의 Calendar), item template origin 의 slot 자식 (`metadata.slotRole`) 은 규칙 밖                                                                                                                                                               | `components/originChildRefs.ts` · live `adr229-composite-children-live.mjs`                                                                                        |
| F26 | descendants path 키는 두 규약이 공존한다 — canonical 스키마의 **id path** (`"Box/Slot"`, page-frame slot fill 을 `convertPageLayout` 이 만든다 · Preview 해소기의 종전 유일 규약) 와 builder Skia/store 축의 **segment path** (`getCanonicalRefPathSegment`: name → id — synthetic id · 패널 쓰기 키). F21 을 segment 로만 고치면 page-frame slot fill (TC2) 이 깨진다 — Preview 는 id 를 먼저 보고 segment 로도 맞춘다 (name 없는 노드는 둘이 같다)                                                                                                                          | `resolvers/canonical/index.ts` `applyDescendantsToTree` · `scopeInheritedDescendants`                                                                              |
| F27 | Tag item template slot 을 TagList 자식에 두면 (Phase 1) Properties 가 그 slot 배열을 사용자 fill 대상으로 읽어 TagGroup **instance** 에 "Slot Fill · Target slot: TagList" 가 뜨고, origin 에는 ListBox 의 "Slot" 절이 뜨지 않았다 (사용자 지적 2026-09-21). slot 보유자를 **TagGroup root** (`component-taggroup.slot`, ListBox/GridList 와 같은 자리) 로 옮기고 slot host policy 에 TagGroup 을 ListBox 대칭으로 등록 (후보 = Tag item origin 2). Phase 1 당일 시드의 TagList slot (표준 두 id) 은 repair 가 root 로 옮긴다 · 두 leg 해소기는 root → TagList(legacy) → 상수 | `taggroup/tagGroupTemplateOrigins.ts` `withTemplateSlot` · `components/slotHostPolicy.ts` · `canvasSceneNode.ts` `resolveTagTemplateOriginIds` · `preview/App.tsx` |
| F28 | (Phase 3 live 실측) Tag item origin 의 icon slot `fontSize` 20 을 Skia 가 **그리기** 는 `_leadingSlotSize` 로 읽어 20 으로 그렸는데, chip **박스** 폭 (`calculateContentWidth` tag 분기) 과 wrap/maxRows 접힘 측정 (`resolveTagWrapLayout`) 은 catalog 상수 14 를 더해 Preview 보다 6px 좁았다 (Alpha 107 vs 113.12). label slot `fontWeight` 700 도 접힘 측정만 400 으로 쟀다 (chip leaf 폭은 style.fontWeight 를 읽어 정상). 세 곳이 같은 숫자 (`_leadingSlotSize` · owner TagList `_slots` 의 `readLeadingSlotSize` · chipStyle.fontWeight) 를 읽게 수리                   | `layout/engines/utils.ts` `resolveTagLeadingExtraWidth(item, slotSize?)` · `resolveTagWrapLayout` `leadingSlotSizes` · taglist 높이 분기                           |
| F29 | (Phase 3 G3) ADR-228 parity 하니스 plain arm 의 `strip` 이 자식을 `type`/`slot` 만 복제해 Phase 2 뒤 조합 origin 의 ref 자식이 대상 없는 ref 가 됐다 (11 family FAIL 의 유일 원인) — ref 자식만 `ref` · instance 미러 (componentRole/masterId) · `componentName` · `name` · `descendants` 를 같이 옮기면 58/58                                                                                                                                                                                                                                                                | `scripts/adr228-instance-parity-live.mjs` `seedArm`                                                                                                                |
| F30 | (사용자 지적 2026-09-21, Phase 3 후속) origin Properties Slot 절의 "+" (`handleInsertDefault`) 는 host 에 ref 자식을 넣는다 — Frame 가족은 그게 slot 내용이고 ListBox/GridList 는 ADR-148 template anchor 다. TagGroup 은 chip 이 `items[]` 데이터라 (ADR-097 Addendum 1) ref 자식이 root 에 생겨도 TagList 에 아무것도 안 그려졌다. TagGroup host 의 Tag item template "+" 는 **item 등록** (`{id, label:"New Tag"}`, selected variant 는 selectedKeys 까지 — props 쓰기 1회) · 정책 `resolveSlotInsertAction(host, candidate)` 하나 (다른 host 는 종전 ref 자식)            | `components/slotHostPolicy.ts` `resolveSlotInsertAction` · `panels/properties/FrameSlotSection.tsx` `handleInsertDefault`                                          |
| F25 | Components body 의 store `elements` 뷰는 세션 중 `component-listbox-item-selected` 의 fill 파생 `style.backgroundColor` 가 붙었다 (reload 뒤) 떨어지는 뷰 드리프트가 있다 (canonical 무관 · Phase 2 밖) — 재hydration Δ0 는 로드 직후 ↔ reload 직후로 잰다                                                                                                                                                                                                                                                                                                                    | live `componentsBodySnapshot`                                                                                                                                      |

## 3. Phase

### Phase 0 — inventory freeze + 2단 override 실측 (G0)

- §2 U1~U3와 리뷰 반증을 인접 회귀로 고정한다. 기존 plain Form을 배치하는 것으로 대체하지 않는다. Button origin + Toolbar/Form origin의 **실제 자식 ref** + outer instance인 proposed fixture를 구성한다.
- `canonicalRefResolution.ts` 일반 source-child 경로에 nested master 해소를 구현한다. nested master props → 조합 자식 patch → 바깥 instance descendant patch 순으로 적용하고, 자식 master propsSchema로 재바인딩한다. 기존 override.children 경로와 결과를 대조한다. 깊은 descendant path의 주소 안정성·원본 불변·순환 참조의 유한 종료를 같은 경계에서 검증한다.
- 타입 Button 및 origin 속성 상속을 확인한 뒤 실제 Builder(headed)에서 안쪽 Button 편집 → instance 저장 위치/IndexedDB 확인 → reload → 두 leg 값 → Undo/Redo를 검증한다. outer instance 편집이 조합 origin이나 Button origin을 오염시키지 않아야 한다.
- 이 공통 경로가 GREEN이 되기 전에는 Phase 2의 신규 ref seed를 활성화하지 않는다. 공통 해소 실패 시 §5의 전체 보류 조건을 적용한다.

### Phase 1 — TagGroup item template origin

- `components/taggroup/tagGroupTemplateOrigins.ts` 신설: `TAG_ITEM_DEFAULT_ORIGIN_ID = "component-tag-item-default"` · `TAG_ITEM_SELECTED_ORIGIN_ID` · slot 자식 Icon `{icon}` (optional) · Avatar `{avatar}` (optional) · Text label `{label}` · `metadata.type:"tag-template-origin"` · `ensureTagGroupTemplateOrigins` (ListBox 동형). `component-taggroup` 의 TagList 자식 (`__2`) 에 `slot: [default, selected]` — generic seed (`catalogOrigins.ts`) 가 TagGroup 을 만들 때 TagList 에 slot 을 붙이도록 `pickCanonicalExtras` 확장 또는 TagGroup 을 `TEMPLATE_ORIGIN_REUSABLE_TYPES` 에 넣고 손 ensurer 로 (ListBox/GridList 와 같은 길 — 후자 권장, 근거: slot 보유자가 origin 자식이라 generic 경로가 표현 못 함).
- `REUSABLE_ORIGIN_ENSURERS` 에 등록, `componentRegistrationContract.test.ts` 불변식.
- `packages/shared/src/catalog/slotRoles.ts`의 `SLOT_ROLES`에 `avatar`를 추가한다. `getSlotRole`/`resolveSlotComposition`/`readSlotComposition` 및 소비자 shape 판독이 role을 보존하도록 한다. Avatar src 데이터는 row.avatar에서 공급하고 존재·optional·style·순서는 slot composition에서 읽는다. 기존 icon/label 계약은 그대로 유지한다.
- `canvasSceneNode.ts`: `resolveTagTemplateOriginId(sourceNode, getDocumentNodesById)` (TagList 의 slot[0] · ref 면 master 의 TagList 자식) · selected 는 `metadata.variant === "selected"` → slot[1] → 상수. `appendTagRowProjection` 이 `templateOriginNode` 의 `props.style` (chip root) + `resolveSlotComposition(children)` (icon/avatar 존재·순서·slot style) 을 chip props 에 주입, `projection.templateOriginId` 기록. `_isSelected` chip 은 selected origin.
- Preview: `App.tsx templateSlotCompositions` 에 `tag` / `tagSelected` 축 → renderContext → `renderTagGroup` 이 chip `style` + slot 구성 (활성 slot + 데이터가 있는 avatar > icon 중 하나를 선택; icon 부재로 avatar를 숨기지 않음; 선택된 leading과 label은 slot 순서로 배치; 각 avatar/icon/label style 전달) 적용.
- 레이아웃: `utils.ts` `resolveTagChipMetric` / `computeTagFoldKeep` 가 origin root style 의 padding/fontSize 를 읽도록 — chip 폭 추정이 template 을 모르면 fold 판정이 어긋난다 (ADR-907 Layer D 동일 resolver 원칙).
- 테스트 (원복 RED): avatar vocabulary/reader 왕복, avatar-only·icon-only·둘 다 있음·둘 다 없음·slot 삭제/재정렬/스타일 변경을 Skia/DOM에서 같은 정책으로 확인. resolver 3 (ref/origin/fallback) · projection 주입 (style · slot 순서 · optional 부재) · Preview 구성 주입 · seed 불변식 · `tagGroupSideHeight` 회귀.

### Phase 2 — 저작 조합층 자식의 ref 화

- 규칙 하나: `toOriginChildSeed(node)` — `getReusableCompositeOriginId(node.type)` 이 있고 node 가 sub-part 가 아니면 (`isDelegatedSubpartChild` false) `{ id, type:"ref", ref: originId, name, props: diffAgainstOrigin(node.props), metadata }`. `diffAgainstOrigin` = 228 의 명시 initialProps 규칙 (origin 과 다른 키만).
- 적용 지점: 손 seed (Form `__action-{n}` · `__field-{n}` TextField · Toolbar Button · IconButton 은 root 가 Button 이라 제외) · generic seed `flattenDefinitionChildren` (ButtonGroup · Pagination · 그 외 definition 자식 중 reusable 타입) · 생성 경로 `useElementCreator` 의 같은 변환.
- **2단 seed (m3)**: 진입 시 existing origin ID 집합을 먼저 보관한다. ① 각 단일 소유 ensurer가 기존 plain 기본 seed 및 Tag template을 보충한 뒤 문서 전체 유효 origin index를 완성한다. ② 진입 시 없었던 조합 origin의 자식만 index를 읽어 ref로 변환한다. 처음 보충된 plain 자식을 사용자 기존 자식으로 오인해 변환을 건너뛰지 않는다. 기존 origin의 자식은 일절 변환하지 않는다.
- `diffAgainstOrigin`은 ①의 완성된 props를 사용한다. 순회 순서가 달라도 같은 결과여야 한다. 참조 누락/순환 dependency는 조용히 props:{}로 대체하지 않고 진단해 해당 ref 전환을 차단한다. `originIds`는 각 ensurer의 수집·배치 대상만 유지하며 다른 owner의 origin을 생성·repair하기 위한 집합으로 확장하지 않는다.
- 전환은 children/descendant override·fills/sizing/responsive 등 기존 canonical 의미도 보존해야 한다. 조합 자식의 독자 subtree를 target origin 기본 subtree로 조용히 대체하지 않는다. Phase 0 inventory에서 해당 차이를 확정하고 기존 override 형식으로 표현하지 못하면 전환을 보류한다.
- 기존 origin: repair가 children과 배치를 보존 (F8). 신규 여부는 문서 전체가 아니라 진입 시 origin ID 존재 여부로 판단한다. 테스트는 기존 origin과 신규 보충 origin이 섞인 문서도 포함하며 G4의 비교 범위를 따른다.
- 2단 override: Phase 0 실측 결과에 맞춰 `diffRefPropsAgainstMaster` 가 중첩 instance 의 master 를 (조합 origin 의 자식 ref 가 가리키는) Button origin 으로 잡는지 unit — 원복 RED.
- Navigator/Properties: 중첩 instance 선택 시 필드 = plain Button 과 같음 (HC). instance 배지는 표시. **F15 (Phase 0 실측)**: synthetic 자식은 1-level 부터 Properties/Styles 가 빈 상태라 HC 를 만족하려면 패널의 selected 파생 (`getActiveCanonicalSelectedElement` · `useCanonicalPropertyElement*`) 이 `<instance>/<path>` 를 resolved synthetic 노드 (origin ⊕ 자식 patch ⊕ 바깥 patch) 로 읽고, 쓰기는 ref-descendant target (`resolveEditorPresentationTarget`) 과 같은 `descendants` 경로로 가야 한다 — 이 phase 의 실제 범위 (신규 InspectorFieldKind 0 은 그대로).

### Phase 3 — 두 leg parity + 성능 (G2 · G3)

- live 하니스 `adr229-nested-origin-live.mjs` (headed Playwright · `.auth-session.json` · `waitReady`): ① Tag item origin slot style (icon fontSize · label fontWeight) + root style (padding) 변경 → TagGroup instance Skia rect (`__composition_LAYOUT_DEBUG__`) + 픽셀 (`focusStage` + 빈 캡처 가드) + Preview computed 동시 Δ ② `component-button` props 변경 → Form/Toolbar instance 안 Button 두 leg ③ 중첩 instance 편집 → 저장 → reload ④ ADR-228 parity 56 pair 회귀 0 (`adr228-instance-parity-live.mjs`).
- 성능: `perf-baseline.mjs --fixture-kind button-refs` 계열에 Form instance 100 fixture 추가 — 같은 세션 A/B, `scene.build` p95 ≤ +1 ms. TagGroup 100×8 projection 별도 계측.

### Phase 4 — BC · 문서 · 종결 (G4)

- **기존 저작 범위**: plain 노드 및 기존 origin의 props/children/순서/배치 직렬화 불변. 처음 없는 template을 보충하는 일과 기존 subtree 변경을 구분한다.
- **최초 보충량**: Phase 0에서 문서별 누락 root/descendant/shell 및 예상 Δnode·Δbyte를 확정한다. 두 Tag item origin이 모두 없으면 root 2 + 각 Icon/Avatar/Text 자식 3으로 기본 Δnode 8 (필요 shell은 별도); 일부 존재하면 그만큼 차감한다. 새 조합 자식 ref 변환으로 달라지는 node/byte는 신규 seed 예상값에 포함한다. 예상 밖 증감은 실패다.
- **재hydration**: 첫 보충 뒤 두 번째 실행은 문서 전체 Δnode=0·Δbyte=0 및 직렬화 동일.
- **롤백**: 신규 ref seed만 원복하고 일반 nested-ref 해소기는 유지한 상태로 이미 저장된 중첩 ref 문서를 연다. 해소기까지 원복해 읽기를 깨뜨리는 롤백은 허용하지 않는다.
- README/CHANGELOG · ADR `### Live Exercise` · reviews/229.md · 후속 후보 (Tabs/Breadcrumbs/Tree/Table/CardView/ToggleButtonGroup/Nav/Pagination item template) 를 Consequences 에.

## 4. 파일 변경표 (추정 — G0 inventory 로 확정)

| 영역      | 파일                                                                                                                                                                                                                                                                     | 변경                                                         |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------ |
| seed      | `components/taggroup/tagGroupTemplateOrigins.ts` (신설) · `reusableCompositeOrigins.ts` · `catalogOrigins.ts` (`TEMPLATE_ORIGIN_REUSABLE_TYPES` + TagGroup · `flattenDefinitionChildren` ref 규칙) · `form/formTemplateOrigins.ts` · `toolbar/toolbarTemplateOrigins.ts` | Tag item origin 2 · 조합 자식 ref 화                         |
| scene     | `workspace/canvas/scene/canvasSceneNode.ts`                                                                                                                                                                                                                              | `resolveTagTemplateOriginId` · `appendTagRowProjection` 주입 |
| layout    | `layout/engines/utils.ts` (`resolveTagChipMetric` · fold)                                                                                                                                                                                                                | origin root style read-through                               |
| preview   | `preview/App.tsx` · `shared/src/renderers/CollectionRenderers.tsx` · `shared/src/renderers/types` (renderContext 필드)                                                                                                                                                   | `tag` 구성 주입 · chip 적용                                  |
| 생성      | `hooks/useElementCreator.ts`                                                                                                                                                                                                                                             | 자식 ref 규칙 공유                                           |
| 소유권    | `adapters/canonical/canonicalRefResolution.ts` (일반 origin-child ref 실체화) · `adapters/canonical/*` (`diffRefPropsAgainstMaster` 중첩)                                                                                                                                | Phase 0 결과에 따라                                          |
| slot 계약 | `packages/shared/src/catalog/slotRoles.ts` · `catalog/__tests__/slotRoles.test.ts` · Skia/DOM slot 소비자                                                                                                                                                                | avatar vocabulary·reader·leading 선택·배치                   |
| 테스트    | 위 모듈 옆 `*.test.ts` · `componentRegistrationContract.test.ts` · `tagGroupSideHeight.test.ts` · `scripts/adr229-nested-origin-live.mjs`                                                                                                                                | RED→GREEN · live                                             |

추정 파일 15~20 (리뷰에서 확인한 공용 resolver/slot 계약 및 인접 테스트 포함; G0에서 확정). 실측이 1.5× 를 넘으면 Phase 0 inventory 보강 커밋으로 흡수 (M3 — fork 사유 아님).

## 5. 축소안 (G0 실패 시)

- 일반 origin-child ref 해소/2단 소유권이 실패하면 **모든 조합 자식 ref 전환을 보류**한다. Form/Toolbar도 같은 경로라 축소만으로 회피할 수 없다. TagGroup 축은 독립 gate를 충족할 때만 진행하며 ADR 전체 완료로 보고하지 않는다.
- 공통 경로는 통과하고 generic 고유 seed 변환에서만 실패한 경우에 한해 Form/Toolbar 손 seed **2종**으로 범위를 축소한다. 남은 generic 대상은 미완료로 기록하고 승인된 전체 범위의 완료와 구분한다.

## 6. 함정 (선행 ADR 에서 승계)

- 228 h1: ref 의 유효 타입은 문서 index (`findCanonicalNodeType`) 로 — 중첩 ref 자식의 nesting guard (Button ⊂ ButtonGroup) 도 같은 경로.
- 228 h2: origin 보존 테스트는 사용자가 실제로 바꾼 상태 (역순 · frame 안 · 자식 편집) 로.
- 228 parity 함정: Skia 픽셀 leg 는 stage 를 뷰포트 안에 (`focusStage`) + 빈 캡처 가드 + 선택/hover 해제.
- 09-21 chip 상자: Tag rule 은 hand CSS 와 자동으로 안 맞는다 — DOM 실측을 같은 하니스에서 읽는다 (`taggroup-chip-text-live.mjs`).
- ADR-923 P5: sub-part 판정은 `isDelegatedSubpartChild` 하나로 — Phase 2 의 ref 화 제외 조건도 이 술어.

## 7. Phase 기록

### Phase 0 — 2026-09-21 (G0 PASS)

- **구현** (동작 변경, `src` 4 파일): `adapters/canonical/canonicalRefResolution.ts` — 일반 source-child 경로가 ref 자식 (`getCanonicalRefTarget`, F13) 을 만나면 nested master 로 해소 (master props → 조합 자식 patch → 바깥 instance patch, `getStackedDescendantPatch` 소유자 스택 `[바깥 instance, 안쪽 ref…]`) 하고 nested master 의 자식을 nested propsSchema 로 재바인딩해 실체화 · origin 조회는 `lookupMaster` (ADR-228 G4 map 우선) · 순환은 `visitedSourceIds` 로 유한 종료 · `resolvers/canonical/index.ts` — mode A patch 가 닿은 ref 자식을 `resolveNestedRefChild` 로 열고 바깥 instance 의 깊은 path (`<ref path>/…`) 를 상대 path 로 좁혀 자식 ref 의 `descendants` 위에 얹음 (바깥 관여 시 id 기준 cache 우회) · mode C = 배열 children 만 (F14, `instanceResolver.ts` 도 같은 판정) · `presentation/editorPresentationCommitAdapter.ts` — `findDescendantNode` 가 ref 자식을 origin 으로 열어 깊은 path 를 걷는다 (F16).
- **원복 RED**: `canonicalRefResolution.test.ts` ADR-229 블록 7 (실체화 · 3단 우선순위 · 원본 불변 · 깊은 path · 재바인딩 · 순환 · mode C 동형 — 구현 전 6/6 RED, scene 모양 1 은 live 결함 재현 후 추가) · `resolver.test.ts` 3 (mode A ref 자식 · 깊은 path · TC8 회귀 — 2/3 RED) · `editorPresentationCommitAdapter.test.ts` 1 (nested path style 편집 → 바깥 instance descendants, origin 불변 — RED).
- **회귀**: 인접 스위트 (utils/resolvers/scene/renderers/preview/navigator/components/adapters) 179 파일 1,296 PASS · presentation 32 파일 244 PASS · type-check PASS · 전체 builder 스위트 §7 하단.
- **live** (headed Playwright `apps/builder/scripts/adr229-nested-origin-live.mjs`, 12/12): proposed fixture = Components 페이지 `adr229-form` origin (Form, reusable — 자식이 실제 `ref` → `component-textfield` (label "Name") · `ref` → `component-button` (children "Save")) + 사용자 페이지 instance `adr229-form-1`. Skia 는 depth-2 까지 실체화 (TextField 의 Label/Input/FieldError rect · field 56px · 안쪽 Button 30px = origin) · Preview 는 BUTTON + origin `variant` 상속 + "Save", TextField 는 label "Name" 으로 self-compose · 캔버스 클릭 = instance root, 더블클릭 = `adr229-form-1/adr229-form__action` · 안쪽 Button 편집 (children "Go" · borderRadius 14) 은 `adr229-form-1.descendants["adr229-form__action"]` 에만 저장, `adr229-form__action.props` = {children:"Save"} · `component-button` 그대로 · Skia rect = Preview rect (68×30) · Preview "Go" + 14px · Undo → 원상 · Redo → 복귀 · reload → descendants · 두 leg · Components body 순서/자식 Δ0 (241 → 241) · page error 0.
- **판정**: 공통 해소기 GREEN → §5 전체 보류 조건 미해당, Phase 1·2 진행. **발견 (F15)**: synthetic 자식의 패널 표면 부재는 nested 고유가 아니라 1-level 부터의 기존 한계 (대조군 `adr229-tf-1/component-textfield__1` 도 "Select an element") — HC "Properties 필드 = plain 과 같음" 은 Phase 2 실제 범위로 옮긴다. 편집 저장 경로 자체 (presentation lane · `updateElement` descendants mirror) 는 nested 에서도 바깥 instance 하나에 닿는다.

### Phase 1 — 2026-09-21 (G1 unit PASS · live 8/8)

- **구현** (동작 변경): `packages/shared/src/catalog/slotRoles.ts` — `avatar` role + `ITEM_TEMPLATE_AUTHORING_LAYOUT_STYLE_KEYS` / `resolveItemTemplateChipStyle` (root style 에서 저작 layout 키 제외 + label slot typography fold) / `readLeadingSlotSize` (두 leg 공용) · `components/taggroup/tagGroupTemplateOrigins.ts` (신설) — `component-tag-item-default` / `-selected` (slot 자식 Icon `{icon}` optional · Avatar `{avatar}` optional · Text `{label}`, root 는 저작 조합 layout style) + `component-taggroup` 은 generic 과 같은 트리 (`buildCatalogOrigin`) 에 TagList 자식 `slot: [default, selected]` (결손 보충), `TEMPLATE_ORIGIN_REUSABLE_TYPES` 에 TagGroup (generic 49) · `reusableCompositeOrigins.ts` — `getReusableOriginEnsurers()` (F20) · `canvasSceneNode.ts` — `resolveTagTemplateOriginIds` (TagList `slot` → ref instance 면 master 의 TagList 자식 → 상수) · `resolveTagItemTemplateStyle` · `appendTagRowProjection` 이 chip 에 root style (+ `height: auto` F18 · lineHeight 배율 F19) · fills · slot 존재 gating (`isSlotEnabled`) · `_leadingSlotSize` · `projection.templateOriginId`, TagList props 에 `_tagTemplateStyle` / `_slots` · `canonicalSceneModel.ts` — instance pass 에 문서 노드 조회 전달 · `layout/engines/utils.ts` — `resolveTagWrapLayout({ chipStyle })` (padding longhand/shorthand · fontSize · lineHeight → chip 치수) · `packages/specs` `resolveLeadingSlot` — `props._leadingSlotSize` 가 지름/glyph 크기를 덮음 · Preview — `renderer.types.ts` `tagTemplate` · `App.tsx` 가 master TagList slot → origin → `resolveItemTemplateChipStyle` 로 주입 · `renderTagGroup` Path 2 → `items` prop (F17) · `TagGroup.tsx` rows 경로가 chip 마다 base ⊕ selected style · `renderTagLeadingSlot(item, composition)` 존재 gating + slot 크기 inline (미러 동일).
- **판단 (통상 구현)**: slot 순서 중 "leading 이 label 뒤" 는 두 leg 모두 leading 앞 고정 (RAC Tag 에 trailing avatar 어휘가 없고 Skia rule 도 좌측 슬롯 하나) — 순서는 icon/avatar/label 존재·gating 에만 쓴다. leading slot 의 style 은 크기 채널 (icon `fontSize` · avatar `width`) 만 두 leg 에 실었다 (색 등은 미배선 — 후속).
- **원복 RED** (구현 파일만 원복, 테스트 유지): shared `slotRoles.test` + `tagGroupItemTemplate.test` 7/30 · specs `leadingAvatarSlot.test` 1/5 · builder `canvasSceneNode.test` 6 · `tagGroupSideHeight.test` 3 · `tagGroupTemplateOrigins.test` / `catalogOrigins.test` 는 모듈 부재로 import 실패.
- **회귀**: shared 133 파일 1,376 PASS · specs 111 파일 1,378 PASS · builder components·canvas·factories·preview·adapters 382 파일 3,005 PASS · type-check PASS · 전체 builder 스위트 §7 하단.
- **live** (headed Playwright `apps/builder/scripts/adr229-tag-template-live.mjs`, 8/8): 신규 프로젝트에 item origin 2 (Icon/Avatar/Text) + `component-taggroup__2.slot` 시드 · instance (items 에 avatar/icon/없음 3종, selectedKeys b) chip 기준 Skia 30 = Preview 30, avatar > icon > 없음, 폭 차 ≤ 3 · default origin padding 24/8 → Skia 38 = Preview 38 · padding-left 24px · 폭 +24 · Avatar slot 자식 삭제 → avatar 데이터 chip 이 두 leg 모두 icon (Skia 폭 −2) · label slot fontSize 18 → Preview 18px · Skia 18 · 높이 43.71 = 43.7 (line-height 비율) · Undo ×3 → 원상 (30 · avatar · 12px) · reload → 편집 보존 (38 두 leg) · Components body Δ0 (245 → 245, origin 63) · page error 0. 잡은 결함: Preview 무반응 (F17 — JSX 경로가 template 을 못 받음) · Canvas 높이 30 고정 (F18) · Canvas lineHeight 480 (F19 배율 계약).
- **판정**: G1 (Phase 1 몫) PASS. Phase 3 이 이 하니스를 parity/성능 (G2·G3) 으로 확장한다.

### Phase 2 — 2026-09-21 (G1 unit PASS · live 9/9)

- **구현** (동작 변경): `components/originChildRefs.ts` (신설) — 규칙 하나 `toOriginChildSeed(node, ctx)`: `getReusableOriginId(type)` 이 있고 sub-part (`isDelegatedSubpartChild`) · slot 자식 (`metadata.slotRole`) 이 아니면 `{ id, type:"ref", ref, name, metadata, props: diffPropsAgainstOrigin (228 규칙 · style 키 단위), descendants? }` — 자식 subtree 는 origin subtree ⊕ root props factory 전파 (`applyFactoryPropagation`) 의 **기대 subtree** 와 대조해 전파 밖 차이만 mode A patch 로, 표현 못 하는 차이 (자식 수/type · responsive/slot/sizing · `type`/배열 `children` 등 예약 키) · origin 부재 · 순환은 진단 + plain 유지 (조용한 대체 0) · `convertNewOriginChildrenToRefs(doc, { existingOriginIds })` 2단 seed ② · `reusableCompositeOrigins.ts` — 진입 시 origin id 집합 보관 → ensurer 전부 (plain 보충) → 신규 origin 자식만 변환 (기존 origin 불변) + 보류 warn · `utils/propagationRegistry.ts` — TextField `type → Input.type` (factory 트리와 같은 축, 없어서 seed/편집 전파 모두 stale) · `factories/utils/originChildRefElements.ts` (신설) + `elementCreation.ts` `doc` 옵션 + `ComponentFactory.createComponent` — 생성 경로 (reusable origin 없는 complex: Navigation 의 Link · ColorPicker 의 ColorField) 도 같은 규칙 · **F15**: `stores/canonical/syntheticDescendantLookup.ts` (신설) — `<instance>/<path>` 를 `resolveCanonicalRefTree` (instance 하나 · canonical children map) 로 해소한 lookup (문서 참조당 캐시) · `stores/index.ts` `getActiveCanonicalSelectedElement` · `useCanonicalPropertyRead.ts` (element/type/name/value/children) · `useEditContract.ts` (ref 를 떼어 (A) primitive accepts = plain 과 같은 필드) · `PropertiesPanel.tsx` patch source · `inspectorActions.ts` — `getInspectorElementById` synthetic 읽기 + `updateAndSave` synthetic id 는 바깥 instance `descendants[path]` 로 (`buildInstanceDescendantPatches` 재사용) + `updateSelectedPropertiesWithChildren` synthetic 선택 분기 + descendants 쓰기의 layoutVersion bump (F22) · `resolvers/canonical/index.ts` Preview 가 id path 와 segment path 둘 다 읽음 (F21 · F26) · `editorPresentationCommitAdapter.ts` `findDescendantNode` 통째 매칭 (F23).
- **판단 (통상 구현)**: plain 자식에 없는 키는 origin 값 채택 (patch 는 삭제를 표현하지 못한다) · 규칙은 조합 origin 의 reusable-type 자식 전부에 적용 (breakdown "그 외 definition 자식 중 reusable 타입") — CardView 의 Card 3 만 보류 (F24) · synthetic 자식의 계약은 유효값 (origin ⊕ patch) 이라 필드 전부 isOverridden 로 보이되 쓰기는 descendants 한 곳 · 손 seed (Form/Toolbar) 파일은 무변경 (변환은 post-pass).
- **원복 RED** (src 11 파일 역적용 · 신설 모듈/테스트 유지): 7 — `originChildRefs.test` 3 (TextField type 전파 · 2단 seed · 재hydration/혼재) · `originChildRefElements.test` 1 · `resolver.test` F21 1 · `editorPresentationCommitAdapter.test` F23 1 · `syntheticDescendantLookup.test` 쓰기 1. `canonicalMutations.test` 중첩 diff 1 은 기존 동작 고정 (GREEN 유지). 순수 규칙 테스트 (`toOriginChildSeed` leaf/sub-part/진단) 는 신설 모듈 자체라 원복 대상 밖.
- **회귀**: builder stores·panels·adapters·components·factories·presentation·utils·hooks·scene·preview·resolvers 505 파일 4,031 PASS · type-check PASS · ADR-113/116 grep gate — 신설 2 파일 allowlist (canonical `RefNode.descendants` 를 만드는 seed 층) · 전체 builder 스위트 §7 하단.
- **live** (headed Playwright `apps/builder/scripts/adr229-composite-children-live.mjs`, 9/9): 신규 프로젝트 seed — Form 자식 `ref:component-textfield ×2 + ref:component-buttongroup` (props `{label, placeholder, isRequired}` 만) · Toolbar `ref:component-button ×3` + Separator plain · ButtonGroup/Pagination 자식 ref · Components body 218 node / 43,888 B → Form instance: Skia 3단 (ButtonGroup ref → Button ref) rect · TextField ref 의 Label rect · 안쪽 Button 30 = origin 30 · Preview input 2 (Name/Email) + BUTTON Cancel/Save → synthetic Save (`<inst>/ButtonGroup/component-buttongroup__2`) 선택 → **Properties 제목 Button · Variant/Size 필드 · Text 'Save' 입력** (plain 과 같음) → 'Go' 입력 → `descendants["ButtonGroup/component-buttongroup__2"]` 하나 · Form/ButtonGroup/Button origin 무오염 · Preview 'Go' · Skia 글자 'Go' · 폭 68 = 68 → TextField 자식 (`<inst>/TextField/Name`, segment 에 `/`) 선택 → Label 'Full name' → `descendants["TextField/Name"].label` (+ 전파 `…/component-textfield__1`) · Preview label 'Full name' · Skia Label 폭 62 = 62 → Undo ×2 원상 / Redo ×2 복귀 → reload 보존 · Components body 로드 직후 ↔ reload 직후 Δnode 0 · Δbyte 0 · 직렬화 동일 · page error 0 · 보류 진단 = CardView Card 3 만. 잡은 결함: F21 (Preview 만 patch 무반응) · F22 (Skia stale) · F23 (Styles lane target 없음).
- **live 캔버스 축** (`apps/builder/scripts/adr229-canvas-origin-live.mjs`, 9/9 — Compare Mode 없이 origin·instance·slot 자체): Components 페이지에서 Form origin 의 ref 자식 (TextField · 그 Label · ButtonGroup ref 안 Button ref) · Toolbar Button ref · ButtonGroup Button ref 전부 layout rect + 글자 (Save · Action 1 · Name) → origin 자식 `component-form__field-1` 선택 → Properties TextField 필드 · Label 'Nome' → 그 ref 노드 props (+ 자기 descendants 의 Label 전파) 만, TextField/Form origin 무오염 → 사용자 페이지 Form instance Label 'Nome' 동반 → `component-button` children 'OK' → Toolbar/ButtonGroup origin 안 Button ref · instance 안 Button 은 자기 patch (Action 1 · Save) 유지 · `style.width 200` (전파 밖 축) → 전부 폭 200 (무patch 키 상속; `size` 는 ButtonGroup/Toolbar 가 자식에 전파하는 축이라 origin 값이 가려진다 — 설계된 우선순위) → Tag item origin 의 slot 자식 Icon/Avatar/Text 는 plain · rect · Avatar 선택 → Properties Avatar. 함정: origin 편집은 영향 대화상자 (`EditingSemanticsImpactDialogHost`, in-app) 를 지나야 store 에 실린다 — 하니스가 Continue 를 누른다.
- **TagGroup slot 자리 정정 (F27)**: `component-taggroup.slot = [default, selected]` (root) · TagList 자식 slot 제거/이동 · slot host policy TagGroup 추가 — unit (`tagGroupTemplateOrigins.test` 이동/보존 · `slotHostPolicy.test` · `canvasSceneNode.test` root 우선 + legacy 폴백) · live 프로브: instance Properties "Target slot" 없음 · origin Properties "Slot · 2 recommendations · Tag/Default · Tag/Selected" (ListBox 과 같음) · `adr229-tag-template-live.mjs` 8/8 재통과 (read-through 두 leg 불변).
- **Phase 3 로 넘김**: ADR-228 parity 하니스의 plain arm 은 origin subtree 를 `type`/`slot` 만 복제해 (`strip`) ref 자식이 대상 없는 ref 가 된다 — Phase 2 뒤 11 family (ButtonGroup · Pagination · AvatarGroup · Nav · ToggleButtonGroup · CheckboxGroup · DisclosureGroup · FileUpload · DatePicker · DateRangePicker; CardView 는 PASS) 가 그 이유로 FAIL. ref arm 은 정상 (Save 71×30 · 68×30). 하니스 plain arm 이 `ref`/`descendants` 를 옮기도록 고치는 것은 Phase 3 parity 항목.

### Phase 3 — 2026-09-21 (G2 live 7/7 + parity 58/58 · G3 PASS)

- **구현** (동작 변경 — live 가 잡은 F28): `layout/engines/utils.ts` — `resolveTagLeadingExtraWidth(item, slotSize?)` (template slot 크기가 catalog 상수 14/16 을 덮는다) · chip leaf 폭 (`calculateContentWidth` tag 분기) 이 projection 이 실은 `_leadingSlotSize` 를 읽음 · `resolveTagWrapLayout` 에 `leadingSlotSizes` (owner TagList `_slots` 의 `readLeadingSlotSize`) + 글자 폭을 `chipStyle.fontWeight` 로 측정 (종전 400 고정). 그리기 (`buildCatalogShapes` `_leadingSlotSize`) · 박스 · 접힘 측정 세 곳이 같은 숫자.
- **하니스** (동작 변경 0): `scripts/adr228-instance-parity-live.mjs` plain arm 이 ref 자식의 `ref` · instance 미러 · `componentName` · `name` · `descendants` 를 같이 복제 (F29) · `scripts/adr229-two-leg-parity-live.mjs` (신설, G2) · `scripts/perf-baseline.mjs` fixture `forms`/`form-refs` (Form origin subtree 를 ref 까지 plain 으로 펼친 격자 vs `component-form` instance 격자 — 같은 시각 결과의 ref 0%/100%) · `taggroups`/`taggroup-refs` (8 item chip projection).
- **원복 RED** (`utils.ts` 역적용): `tagLeadingIconMetric.test` 3 (slot 크기 인자 · `_leadingSlotSize` 20 → 폭 +6 · 접힘 측정 icon 20/fontWeight 700 — weight 는 단위 측정기가 무감이라 스텁 측정기로). **회귀**: builder layout engines · scene · taggroup 80 파일 719 PASS · type-check PASS.
- **G2 live** (headed Playwright `apps/builder/scripts/adr229-two-leg-parity-live.mjs`, 7/7): 신규 프로젝트에 TagGroup (items 3, icon 2) · Form · Toolbar instance → Skia 기준 chip 30 · 캡처 비어 있지 않음 (916,609 px) → **Compare Mode 전에 Skia 먼저** (반폭이 면적 Δ 를 지운다): icon slot `fontSize` 20 → icon chip 폭 +6 (80→86 · 73→79) · Gamma 불변 · 픽셀 Δ>0 → label slot `fontWeight` 700 → 전 chip 폭 증가 · 글자 weight 700 · 픽셀 Δ>0 → root padding 24/8 → 높이 38 · 폭 +24 · 픽셀 Δ>0 → Compare Mode: Preview icon 20px (상자 20) · font-weight 700 · padding-left 24px · 높이 38 = 38 · **폭 Alpha 113 = 113.12 · Beta 104 = 104.63 · Gamma 101 = 101.98** (수리 전 Skia 107/98/101 — F28) → `component-button` `style.width` 200 (영향 대화상자 Continue) → Form instance 안 Save (ButtonGroup ref 안 Button ref, 3단) · Toolbar instance 안 Action 1: Skia 68→200 · 62→200, Preview Save/Cancel 68→200 · Action 1~3 60.5→200, 자기 patch 글자 유지 → synthetic Save 선택 → Properties Text 'Go' → `descendants["ButtonGroup/component-buttongroup__2"]` 하나 · Skia 'Go' · Preview 'Go' (폭 200 유지) → reload → chip 38 · 24px · 700 · icon 20px 두 leg · Button 폭 200 두 leg · 'Go' · Components body 로드 직후 ↔ reload Δnode 0 · Δbyte 0 · 직렬화 동일 · page error 0 · dialog 0.
- **G2 parity 회귀** (`adr228-instance-parity-live.mjs --headed`, **58/58** — Phase 2 직후 11 family FAIL 은 전부 하니스 plain arm (F29)): ButtonGroup · Pagination · AvatarGroup · Nav · ToggleButtonGroup · CheckboxGroup · DisclosureGroup · FileUpload · DatePicker · DateRangePicker · CardView 포함 56 pair Skia rect 열 same · Skia 픽셀 diff 0 · DOM diff 0 (Popover/Tooltip 은 DOM 미출력이라 Skia 만) · page error 0.
- **G3** (`pnpm perf:baseline -- --lane frame --seed-count 100 --headed --classes edit`, 같은 세션 A/B · `scene.build` 직접 계측 · run 당 표본 9/10 이라 p95 ≈ max): **Form 100** (각 TextField ref 2 + ButtonGroup ref (Button ref 2) — plain 펼침 ≈ 1,400 노드) 7회 p95 plain 3.4 · 4.0 · 3.6 · 4.0 · 4.1 · 4.9 · 3.7 (median **4.0**) vs ref 7.5 · 4.6 · 4.4 · 4.7 · 4.7 · 6.2 · 5.2 (median **4.7**) → **median +0.7 ≤ +1 PASS** · 평균 +1.4 (ref arm 에만 7.5/6.2 spike 2회 — 표본 10 의 max) · p50 median 3.4 → 3.9 (+0.5) · render.frame p95 Δ −0.3 ~ +0.3 (사용자 체감 프레임 0). **TagGroup 100×8** 2회: scene.build p95 7.3/7.1 → 7.1/5.9 (−0.2 · −1.2) · render.frame p95 5.5/5.4 → 5.7/5.4 · record.content pan p95 4.4/4.6 → 12.4/4.0 (1회 spike, 2회째 0) — **PASS**. 유보 (LOW): ref arm 의 scene.build spike (표본 10 의 max 가 2회 6 ms 대) 는 ADR-228 잔여 (a) 와 같은 가설 (instance 당 merge 3회 · 중첩은 nested 마다 `resolveCanonicalRefElement` + `materializeSyntheticDescendants`) — resolved instance identity 캐시 후보. 수치: scratchpad `perf/g3-*` (local).
- **판단 (통상 구현)**: G2 하니스는 `adr229-nested-origin-live.mjs` (Phase 0, 614 줄) 를 확장하지 않고 별도 파일 — Phase 0 하니스는 손 fixture (`adr229-form`) 기준이라 palette origin 기준 두 leg 동시 Δ 와 섞이면 판정이 흐려진다 · G3 의 plain arm 은 "instance 가 없던 과거 문서" 가 아니라 **같은 시각 결과를 plain 으로 펼친 트리** (ADR-228 G4 의 `buttons`/`button-refs` 와 같은 정의).
- **후속 (사용자 지적, 같은 날) — Slot 절 "+" 가 TagList 에 item 등록 (F30)**: `slotHostPolicy.resolveSlotInsertAction` — TagGroup host + Tag item template 후보 → `{kind:"collection-item", itemsKey:"items", selected}` (selected = `metadata.variant === "selected"` 또는 표준 selected origin id), 그 외 `{kind:"child"}` (Frame · ListBox/GridList 종전) · `FrameSlotSection.handleInsertDefault` 가 collection-item 이면 `updateElementProps(host, { items: [...items, {id, label:"New Tag"}], selectedKeys? })` 한 번 (ref 자식 0). unit: `slotHostPolicy.test` 1 · `FrameSlotSection.test` 1 (legacy merge 가 root `Tag` element 를 items 로 흡수해 fixture 는 canonical 에 직접 시드) — 원복 RED 2. **live** (`apps/builder/scripts/adr229-slot-insert-live.mjs`, 5/5): origin 선택 → Slot 절 Insert Tag/Default → items 4→5 ('New Tag') · selectedKeys 불변 · ref 자식 0 · Skia chip 5 → Insert Tag/Selected → items 6 · selectedKeys = [새 id] · Skia chip 6 → items 를 상속하는 instance: Preview chip 6 ('New Tag' 2, 하나 selected) = Skia 6 · items 소유 instance 불변 (Own 1) · page error 0. 하니스 함정: Preview 의 maxRows 측정 미러 (`inert aria-hidden` TagList) 가 chip 을 두 배로 센다 — `[aria-hidden]` 안은 제외.
- **후속 (사용자 판정, 같은 날) — Components 페이지는 data binding 이 없는 영역**: origin 은 테마 손질 대상이라 collection 의 외부 데이터 연결 (`dataBinding` kind `binding` — Data 절 + 「새 테이블」) 은 instance 의 축이다. `panels/properties/componentsPageFields.ts` `omitDataBindingOnComponentsPage(fields, node)` (`page_id === page-components`) 를 `CatalogEditContractEditor.semanticFields` 앞단에 — 정적 `items` 편집은 유지 (origin 의 견본 · instance 상속). unit 2 · live 프로브 (headed): TagGroup instance 「새 테이블」 액션 1 / origin TagGroup · ListBox · Table · Select 0.
- **후속 ADR 후보 (사용자 판정, 같은 날 — 229 범위 밖)**: Components 페이지는 각 컴포넌트를 **상태별 (선택됨 · 비활성 · hover · pressed …) 별도 origin** 으로 분해해 그 상태의 스타일을 정의·표시하는 자리다 (Tag/Default · Tag/Selected 패턴을 전 컴포넌트로). 지금은 ListBoxItem · Tag 만 selected 변형 origin 이 있고 그 외 상태 스타일은 catalog rule fill state 토큰 (D3, 노드 아님) — 정본 위치 재판정이 필요해 **229 종결 후 새 ADR** (사용자 `/create-adr`, 전제 = 상태별 별도 origin). Consequences 에 기록.

### Phase 4 — 2026-09-21 (G4 PASS · Implemented 종결)

- **unit** `components/__tests__/adr229BackwardCompat.test.ts` 4: "229 이전 문서" = ensurer 단계 ① 만 (plain 자식) − Tag item origin − TagGroup root slot + 사용자 저작 (Toolbar 자식 라벨 편집 · origin 순서 변경 · 사용자 페이지 plain Button + Form instance descendants) → 229 hydration: 기존 노드 id 별 자기 직렬화 (props · 필드 · 자식 id 순서) 불변 — 유일한 차이 = `component-taggroup.slot` 추가, 보충 origin 2 는 body 끝 · **최초 보충 Δnode 8 · Δbyte 1,959 = Tag item origin 2 직렬화 (1,891) + slot 필드 (68), 정확히** · 재hydration Δ0 · 롤백 = `convertNewOriginChildrenToRefs` 를 전부 기존 취급으로 끈 hydration 이 문서 불변이고 `resolveCanonicalRefTree` 가 저장된 중첩 ref (Form instance 의 ButtonGroup ref 안 Button ref + descendants 'Go') 를 실체화.
- **live** `apps/builder/scripts/adr229-bc-live.mjs` 7/7 (headed): Playwright 프로필의 IndexedDB 는 run 마다 비어 옛 프로젝트가 없다 → 새 프로젝트의 **저장된 canonical 문서를 `db.documents.put` 으로 228 모양으로 재작성** (Tag item origin 삭제 · slot 제거 · Form/Toolbar/ButtonGroup 의 ref 자식 3/3/2 를 origin subtree plain 으로) → reload (= 기존 문서 열기): 조합 origin 자식 plain 유지 (ref 0) · Tag item origin 2 + slot 보충 · Δnode 8 (Δbyte 1,473 — 스냅샷 투영 기준) · 기존 노드 자기 직렬화 불변 → 재reload Δnode 0 · Δbyte 0 → 그 문서에 TagGroup instance → chip Skia 30 = Preview 30 · Alpha icon. 함정: system origin root 는 store `removeElement` 가 막는다 (ADR-228 가드) — 문서 재작성은 DB 경로로.
- **문서**: ADR → `completed/` (Status Implemented · Gate 결과 표 · `### Live Exercise` · Consequences 후속 후보 4) · README (공지 · 열림 목록 제거 · 완료 표 · 완료 이력) · CHANGELOG · `reviews/229.md` Round 4 실행자 종결 선언 (HIGH 0).
