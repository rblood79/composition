# ADR-923 Implementation Breakdown: display 이원 계약 — TS IFC 시뮬레이션 제거·엔진 outer/inner 직결 (C′)

> 본 문서는 ADR-923 의 구현 상세 정본이다. ADR 본문은 결정·위험·게이트만 담고, Phase / 파일 경계 / 체크리스트 / 측정 절차는 여기에만 둔다. **r1 개정 2026-08-31** — Codex 설계 리뷰(reviews/923.md) 반영: r0 의 "단일 display 문자열 직결" 을 outer/inner 이원 계약으로, baseline 을 입력에서 출력 계약으로, B 갈래(S4/S7/S8·persisted migration) 는 §8 로 분리. **r2 개정 2026-08-31** — Claude 리뷰 round 2 반영: TS flex/grid 분기 3 사이트(S9) 를 Phase 5 제거 목록에 추가, Phase 4 는 동작 무변경 준비로 축소하고 catalog 전환·`getElementDisplay` catalog 파생은 Phase 5 단일 commit 으로 이동, 패널 `useFlexAlignmentKeys`, 순수 inline code 0 명세, 라인 drift 갱신. **r3 개정 2026-08-31** — Codex round 3: S9 에 TS 운반 타입 union 확장(`layoutTypes.ts:11` / `taffyDisplayAdapter.ts:124`) 추가, G0 캡처 지점을 `buildTreeBatch` 인자 spy 로 고정, Phase 1 은 `classify_child_display` 가 아니라 `parse_display` 직접 사용. **r4 개정 2026-08-31** — Codex 판정 3건 (reviews/923.md round 5): HC2 = 정확값 + 같은 역할 box + production 소비 경로 기준, evidence 13 은 Q4 판정 후보 (확정 전환 Button·ToggleButton 2, Menu 는 `MenuTrigger > Button` 대조, 나머지 10 은 Q4 캡처 후 전환 / 투영 예외 분류); G3 = 고정 N=3 before/after 교대 paired ratio 중앙값 ≤ 1.05 (단일 run 폐기, Phase 0 단일 값은 참고치).

## 1. 문제 정의와 범위

### 1.1 결정 경계

ADR-923 은 **display 의 이중 표현**과 **어댑터가 문서에 없는 의미를 만들어내는 자리**를 없앤다.

- 엔진 경계의 display 는 CSS 값 1개. 엔진이 `display.rs` 로 outer(부모 flow 참여) / inner(자기 solver) 를 해석한다.
- catalog 의 Canvas 전용 display override(DOM `inline-flex` ↔ Canvas `flex`) 를 제거하고 패널을 고친다.
- `INLINE_BLOCK_TAGS` 를 default-display resolver 와 intrinsic-measurement capability 로 분리한다.
- baseline 을 `NodeLayout` 출력으로 상향 전파한다.
- 그 뒤 TS IFC 시뮬레이션 층을 제거한다.

바꾸지 않는 것: canonical 스키마, persisted 문서(migration 없음), catalog 의 시각 값(색·크기·폰트), Preview/publish CSS 생성, D1 DOM/ARIA, D2 props. **B 갈래(S4/S7/S8 ingress 정규화·persisted migration) 는 §8 로 분리 — 본 ADR 은 capability matrix seed 선언까지.**

### 1.2 Domain

| Domain         | ADR-923 영향                                                                                                                                       |
| -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1 DOM/접근성  | 없음. Preview 는 기존 runtime 그대로.                                                                                                              |
| D2 Props/API   | 없음. 새 prop 없음.                                                                                                                                |
| D3 시각 스타일 | **layout flow 는 D3**. catalog `containerStyles.display` 의 Canvas 전용 override 제거 = 두 consumer 가 같은 display 값을 받도록 하는 D3 대칭 복구. |

### 1.3 No-fork lock-in (adr-writing.md 4 질문)

1. **base/응용 분류**: ADR-923 은 ADR-916(자체 엔진, Implemented) 과 ADR-198(픽셀 게이트, Accepted) 의 소비자. 둘의 선행 ADR 이 아니다. ADR-198 Phase 6 은 본 ADR 의 차등 케이스를 입력으로 쓰지만 본 ADR 을 기다리지 않는다.
2. **스키마 직교성**: 지속 스키마·persisted 문서 변경 0. wasm 경계 계약(`NodeLayout.baseline` 출력 + `vertical-align`/`line-height` 입력) 은 런타임 계약이지 문서 스키마가 아니다.
3. **의존 방향 반전 점검**: "TS 어댑터가 엔진 제약을 보완한다" 는 ADR-009 시절(Taffy) 전제였고 ADR-916 endgame(`dd5a6e403`) 으로 소멸. r1 에서 하나 더 — "패널 결함을 catalog override 로 보완한다"(2026-06-27) 도 같은 종류의 역방향 보완이며 본 ADR 이 되돌린다.
4. **조기 관점 점검**: scope 는 §1.1 로 동결. B 갈래는 사용자 결정(2026-08-31) 으로 분리. 완전한 IFC(대안 D) 는 기각.

## 2. 실측 인벤토리 (2026-08-31, r0 + r1, 코드 0 변경)

### 2.1 display 가 갈라지는 자리 (r1 핵심)

| 소비자                     | Button 이 받는 display                     | 출처                                                                                                                                                                                                  |
| -------------------------- | ------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DOM (Preview/publish)      | `inline-flex`                              | `packages/shared/src/components/styles/Button.css:14`                                                                                                                                                 |
| 부모의 자식 분류 (TS)      | `inline-block`                             | `fullTreeLayout.ts:1797 getElementDisplay` → `utils.ts:4400 INLINE_BLOCK_TAGS`                                                                                                                        |
| 자식 자신의 style (TS)     | `flex`                                     | `fullTreeLayout.ts:1031 resolveContainerStylesFallback` ← `componentRulesTable.ts:927` (ToggleButton `:13108` 동일)                                                                                   |
| 자식 style → 엔진 (TS, r2) | `inline-flex` 라도 `"flex"`                | `fullTreeLayout.ts:1057-1060` flex 분기 → `TaffyFlexEngine.ts:111-112` 정규화 (`toTaffyDisplay` 우회); inline-grid 는 `:1077-1084` `{display:"grid"}`; `taffyDisplayAdapter.ts:510-512` inner 만 반환 |
| 엔진 line item 판정        | `"inline-block"` 문자열 일치만 code 1      | `tree.rs:4609`                                                                                                                                                                                        |
| 엔진 solver 선택           | `"flex"/"inline-flex"` → Flex, 그 외 Block | `tree.rs:3598 classify_container_display` (`:1185/:1373/:2552` 호출)                                                                                                                                  |
| `display.rs` outer/inner   | (미배선)                                   | `display.rs:20-45,134-170` — tree.rs 참조 0                                                                                                                                                           |

### 2.2 치환·근사가 일어나는 자리 (capability matrix seed)

| #   | 자리                                                                                                                                                                                                                                                                                                                               | 내용                                                                           | 엔진                                                               | 본 ADR 처치                                               |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ | ------------------------------------------------------------------ | --------------------------------------------------------- |
| S1  | `taffyDisplayAdapter.ts:526-536 toTaffyDisplay`                                                                                                                                                                                                                                                                                    | block 부모 + inline-level 자식 → flex row wrap                                 | block.rs line box 구현                                             | Phase 5 제거                                              |
| S2  | `taffyDisplayAdapter.ts:436-440 needsBlockChildFullWidth` + `fullTreeLayout.ts:2386` §5.5                                                                                                                                                                                                                                          | S1 안 block 형제 width:100% 보정 (명시 폭이면 미보정 → ADR-198 발산)           | block.rs auto→stretch                                              | Phase 5 제거                                              |
| S3  | `taffyDisplayAdapter.ts:244-248 INLINE_BLOCK_LEAF_CONFIG`                                                                                                                                                                                                                                                                          | inline-block 자신 → block leaf + grow/shrink 0                                 | outer=inline 이 line item                                          | Phase 5 제거                                              |
| S9  | (r2) `fullTreeLayout.ts:1060-1061,1077-1084` flex/grid 분기 + `TaffyFlexEngine.ts:110-117` + `taffyDisplayAdapter.ts:510-512` + (r6) `fullTreeLayout.ts` `patchBatchStyleFromImplicit`→`toBatchDisplay` (post-order implicit patch · 2-pass 재-enrich 공유 — raw display 복사가 1차 writer 정규화를 덮던 경로를 union 강제로 봉합) | inline-flex/inline-grid 의 outer 를 지우고 inner(`flex`/`grid`) 만 엔진에 전달 | display.rs outer 해석 (Phase 1 배선)                               | Phase 5 제거 (CSS 값 통과 — 운반 타입 union 2곳 포함, r3) |
| S6  | `taffyDisplayAdapter.ts:202 resolveInlineBlockAlignItems`                                                                                                                                                                                                                                                                          | vertical-align → flex alignItems 근사                                          | block.rs `vertical_align` 필드 (tree.rs 미소비)                    | Phase 2 배선 후 Phase 5 제거                              |
| S5  | `utils.ts:4570 needsWidth` (`INLINE_BLOCK_TAGS`)                                                                                                                                                                                                                                                                                   | intrinsic width/height 측정 활성화                                             | 측정은 TS 소관(텍스트·self-render leaf)                            | Phase 4 **분리 유지** (capability 목록)                   |
| S4  | `taffyDisplayAdapter.ts:521-523`                                                                                                                                                                                                                                                                                                   | 순수 `display:inline` 요소 → block 격상                                        | inline box 없음 (code 0/1/2)                                       | §8 (B 갈래) — seed 만                                     |
| S7  | float / clear / writing-mode / 다단                                                                                                                                                                                                                                                                                                | 속성 무시 (class 변경 아님)                                                    | 미구현 (ADR-916 1-C)                                               | §8 — seed 만                                              |
| S8  | grid subgrid / intrinsic track / dense / baseline                                                                                                                                                                                                                                                                                  | 속성 무시 / 폴백                                                               | 미구현 (ADR-916 1-B — round 33 seed 실측으로 subgrid·dense 재확증) | §8 — seed 만                                              |

### 2.3 baseline 현황 (r1)

- `tree.rs:4672-4674` — `vertical_align/baseline/line_height` 를 0 / 0 / AUTO 로 고정 전달 ("미소비" 주석 `:4590-4592`).
- `block.rs:370-390 flush_line_box` — `item.baseline` 으로 `line_baseline` 을 잡고 `VALIGN_BASELINE` 항목을 `start_y + line_baseline - item.baseline` 에 둔다 → 입력이 0 이라 전부 top 정렬로 퇴화.
- `NodeLayout` (`tree.rs:296`) = `{x, y, width, height}` — baseline 출력 없음.
- TS `calculateBaseline` (`utils.ts:5121`) — production caller 0 (re-export 만). 자체 폰트 근사라 중첩 inline-flex 의 마지막 in-flow baseline 을 표현 못 함.

### 2.4 `INLINE_BLOCK_TAGS` 24 항목 (r1 — 두 개념 분류는 Phase 0)

`button submitbutton fancybutton togglebutton badge progresscircle type chip checkbox radio switch togglebuttongroup toolbar statuslight link linkbutton breadcrumb icon menu tab disclosureheader …` (`utils.ts:4400-4432`). 두 역할: (a) 기본 display 가 inline-level 인 타입, (b) self-render/텍스트 leaf 라 intrinsic 측정을 켜야 하는 타입. catalog inline-\* display 선언 30건과는 다른 집합.

### 2.5 ingress / live mutation (r1 — B 갈래 분리 근거)

| 경로               | 실측                                                                                                                                                                                                                                                                                           | hydration chain 경유 |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------------------: |
| Style 패널         | `useStyleActions.ts:51 updateSelectedStyle` → store 직접                                                                                                                                                                                                                                       |          ✗           |
| AI assistant       | `services/ai/tools/updateElement.ts:98 updateElementProps` → store 직접                                                                                                                                                                                                                        |          ✗           |
| 팔레트 factory     | `factories/definitions/{DateColor,Form,Overlay}Components.ts` `display:"block"` 11곳                                                                                                                                                                                                           |          ✗           |
| 프로젝트 hydration | `usePageManager.ts:385` migration chain → **IndexedDB persist-back (원값 소실)**                                                                                                                                                                                                               |          ✓           |
| pencil import      | Phase 0 실측 — `collectPencilProps`(`pencil-adapter.types.ts:258-268`) 가 pen 키를 props 로 그대로 복사, display 파생·정규화 0. production 경로는 `importPayloadAdapter.ts:32` ← `importRegistry.ts:144` fetch → 레지스트리 캐시(비영속, ref 해석 전용) — store 도 chain 도 아님 (evidence §E) | ✗ (레지스트리 캐시)  |

### 2.6 낡은 명명

비테스트 파일의 Taffy 식별자 — 코드 라인 기준 7 파일 (`engines/{TaffyFlexEngine,TaffyBlockEngine,persistentTaffyTree,taffyDisplayAdapter,fullTreeLayout,utils}.ts`, `wasm-bindings/layoutTypes.ts`), 파일명 기준 5 (`TaffyGridEngine.ts` 포함) — 합집합 8 (r2 재집계; r1 의 14 는 재현 안 됨). 헤더 `:20 "=== Taffy 시뮬레이션 규칙 ==="`, `:521`.

## 3. Phase 분해

각 Phase 는 독립 commit 가능 상태로 끝난다. HIGH 위험 귀속 Phase 는 **5 (cutover)** 하나. Rust 변경(1·2) 은 seam 미배선 상태에서 cargo test 로 먼저 닫고, TS 배선은 별도 commit. **동작을 바꾸는 TS 변경(catalog 전환 · `getElementDisplay` catalog 파생 · 시뮬레이션 제거 · flex/grid 분기 정규화 제거) 은 전부 Phase 5 단일 commit** — Phase 4 는 동작 무변경 준비만 (r2 m2: 분리 commit 의 중간 상태는 IFC 시뮬레이션이 꺼진 채 flex 분기가 `flex` 를 넘겨 Button 이 세로로 쌓인다).

### Phase 0 — 선행 검사 + inventory freeze + baseline (G0)

- [x] **선행 검사 1케이스** — 완료 2026-08-31 `07c724a37`, 결과 §6 (`apps/builder/tests/parity/displayContract.browser.test.ts` 신규): style 없는 catalog Button 을 block 부모 아래 두고 (a) wasm 경계에 도달한 display 문자열 — `build_tree_batch` 입력을 그대로 캡처 — 이 `inline-flex` 인가, (b) block 부모의 `solve_block` 에서 line item 인가, (c) Button subtree 가 flex solver 를 탔는가 (`__layoutExplain` / trace) 를 단언. **(a) 캡처 지점 (r3l1)**: `pipelineLeg`(`tests/parity/harness.ts:223-270`, 실 진입점 `calculateFullTreeLayout`) 실행 중 `CompositionEngineLayout.prototype.buildTreeBatch`(`wasm-bindings/compositionEngine.ts:167-170`) 를 `vi.spyOn` 해 JSON 인자(`persistentTaffyTree.ts:183-188` 가 직렬화한 batch) 를 받아 Button 노드의 `style.display` 를 읽는다. `engineLeg`(`harness.ts:114-135`) 는 fixture style 로 batch 를 직접 만들어 프로덕션 변환을 우회하므로 (a) 에 쓸 수 없다. binary protocol 은 비활성(`wasm.rs:87-90`) 이라 JSON 경로만 본다. **현재 (a) 는 `flex`, (b) 는 부모 자체가 flex 로 바뀜, (c) 만 참** 임을 실패 테스트(`it.fails`) 로 고정 → 전제 확증. (a) 의 출처는 catalog `:927` 를 거친 `fullTreeLayout.ts:1057-1060` flex 분기 → `TaffyFlexEngine.ts:112` 이지 `toTaffyDisplay` 가 아니다 — catalog 를 `inline-flex` 로 바꿔도 같은 분기가 `flex` 로 정규화하므로 S9 제거 없이는 (a) 가 통과할 수 없다 (r2 m1).
- [x] **완료 2026-08-31 `e6d08237d` (Codex 판독 r4 반영)** — evidence §A: 123 rule 중 top-level display 16, 정확값 불일치 13 ((i) Button·ToggleButton · (iii) Menu(B7) · structure≠DOM FileTrigger·Skeleton · catalog 없음 8), outer 차이 6; DOM 충돌 3 · 미선언 17 은 판정 대기. Canvas 전용 display override 전수 (Button `:927`·ToggleButton `:13108` 확인됨): `componentRulesTable.ts` 의 top-level `containerStyles.display` 와 `packages/shared/src/components/styles/*.css` 의 display 를 rule 별로 대조한 표 → §6.
- [x] **완료 2026-08-31** — evidence §B: 24 = B 6 (progresscircle·togglebuttongroup·toolbar·disclosureheader·calendarheader·calendargrid — display 역할이 틀림) · AB 11 · ? 7 (rule 없는 5 + menu(B7) + dateinput). `INLINE_BLOCK_TAGS` 24 항목 분류표: 항목마다 `defaultDisplay(catalog)` / `intrinsicMeasure(이유: 텍스트 leaf | self-render leaf | 합성 leaf)` / `둘 다` → §6. 분류 불가 항목은 사유 기록.
- [x] **완료 2026-08-31** — evidence §C: factory 11곳 전부 leaf → 조합 0 (51 정의 전수도 0), fixture smoke 0, **실 문서 미측정 (사용자 export 필요)**. BC 정량 — (a) factory 11곳의 block 컨테이너가 inline-level 자식을 갖는지 정적 집계, (b) 로컬 프로젝트 스캔(`scripts/adr-923/scan-block-inline.mjs`): "block 컨테이너 + outer=inline 자식" 컨테이너 수 / 전체 컨테이너 수 = 영향 %, 영향 문서 수 / 전체 문서 수 → §6 ("평균 재직렬화 파일 수" 는 C′ 에서 persisted 변경 0 이라 무의미 — 대체).
- [x] **완료 2026-08-31** — evidence §D: arm F p50 21.0 / p95 23.9 ms (5,193) · arm B p50 90.2 / p95 97.2 ms (5,001), `adr923LayoutBaseline.browser.test.ts` (기본 skip, `VITE_ADR923_BASELINE=1`). 성능 baseline — 5k fixture `computeLayout` p50/p95 + block 컨테이너 비율 높은 arm 별도 → §6.
- [x] **완료 2026-08-31** — evidence §E: display 파생·정규화 0, 레지스트리 캐시 경로(비영속), block + inline-level 조합 유입 가능. pencil import 경로가 display 를 어떻게 넣는지 실측 (§2.5 채움 — B 갈래 입력).
- [x] ADR-156 차등 하니스에 "어댑터 우회 + 엔진 직접 호출" 진입점 존재 확인 (r3) — `tests/parity/harness.ts:114-135 engineLeg`(엔진 직접) / `:223-310 pipelineLeg`(프로덕션).

### Phase 1 — `display.rs` 배선: outer → line item, inner → solver (Rust, seam 무변경)

- [x] **완료 `5822f2496`** — `display::parse_display` 직접 사용. `classify_child_display`/`ChildDisplayClass` 는 **삭제** (inline-flex/inline-grid 를 Block 으로 분류 — line item 판정에 쓰면 안 됨, r3 과제 3; 크레이트 내 참조 0). 대신 `display::is_atomic_inline_level(d)` (outer=inline ∧ inner∈{flow-root,flex,grid}) 신설. `block.rs:144-170` 은 code==1 만 line item 조건, `:211-218` 이 code 2 (empty block).
- [x] **완료** — `classify_container_display` → `container_display_of(parse_display(d))` (inner 만: Flex/Grid/Flow|FlowRoot|None→Block). 호출부 `:1373`/`:2552` 무변경, `solve_node`(구 `:1185`) 는 `effective_display` 경유 (blockify) — r6l2 정정. `node_establishes_bfc`(구 `:4244` 문자열 4종 매칭) 도 `inner ∈ {Flex, Grid}` 로 — 같은 의미의 5번째 문자열 매칭 사이트.
- [x] **완료** — `write_block_item` display code = `is_atomic_inline_level(parse_display(child)) ? 1 : 0`. 순수 inline 0 (S4), empty-block(2) 는 intake 가 내지 않음 (block.rs 사전 분류 코드, 유지). 실측: block(300) > inline-flex×2 → 같은 줄 x 0/60, 폭 shrink-to-fit 60 (flex solve 의 content 반환값) · inline-grid → 폭 track 합 100, 뒤의 inline-block 이 같은 줄.
- [x] **완료** — `LayoutTree::effective_display(handle)`: 자기 `parse_display` + 부모가 flex/grid 컨테이너면 `display::blockify` (outer=block, inner 유지). `solve_node` 가 이것으로 solver 를 고른다 (inner 만 쓰므로 동작 등가 — outer 소비자는 Phase 2 baseline). 문자열 판 `blockify_display` 는 `blockify` 에 위임.
- [x] **완료** — tree.rs `adr923_*` **7** + display.rs 2 (순증가 +8 = 신규 9 − 삭제 `classify_child` 1, r6l1 정정): inline-flex line box 공유 · inner=flex 가로 배치 · inline-grid line item + grid inner · 순수 inline block 격상 유지 · inline-block 회귀 · flex 부모 blockify (outer=block, inner 유지) · classify inner-only. RED 3/6 (inline-flex/grid 가 300 으로 stretch) → GREEN 333/333. golden 15 · tree_golden 11 · layout_trace 10 전량 PASS, fixture 무변경.
- [x] **완료** — `display.rs` 모듈 doc 을 "배선 (ADR-923 Phase 1)" 절로 갱신 (4 소비 지점 + TS 무변경 근거), 미이식 목록은 Phase 4/5 대상으로 재기술. 회귀: wasm 재빌드 후 builder parity 33 files / 930 pass (`displayContract` it.fails 유지, catalogComponentBox 2 = 기존 실패) · layout unit 48 files / 393 pass. clippy 신규 경고 0 (rustfmt 는 저장소 미적용).
- [x] **r6h1 수리 (2026-09-01, Codex 판독 round 6 HIGH)** — "seam 무변경 → 프로덕션 무변경" 주장이 반증됐다: `fullTreeLayout.ts` `patchBatchStyleFromImplicit` (post-order implicit patch, 2-pass 재-enrich `:3019` 공유) 가 `typeof val === "string"` 분기로 `display` 를 **raw 복사**해 `buildNodeStyle` 의 정규화(S9) 를 덮었다 — Label 공통 주입(flexShrink) 이 style 을 clone 하면 `inline-flex` 가 wasm 경계에 도달 (재현: `seamDisplayInvariant` RED `["inline-flex","block","block"]`). Phase 1 이후 엔진이 그것을 line item 으로 읽어 프로덕션 배치가 phase 경계 밖에서 바뀐다. 수리: `toBatchDisplay` (blockify → flex/grid/none 외 block = 1차 writer 규칙) 로 운반 union `TaffyDisplay` 강제 → 수리 전 동작(inline-flex → flex, code 0) 과 등가. HC7 가드: `tests/parity/seamDisplayInvariant.browser.test.ts` (Phase 1~4 불변식, Phase 5 에서 S9 와 함께 삭제/반전). S9 4번째 사이트로 §2.1·Phase 5·ADR R11 등재. 근본 원인: seam 검증이 1차 writer(`buildNodeStyle`) 만 보고 post-order patch 소비 경로를 빠뜨렸다.

### Phase 2 — baseline 출력 계약 + 입력 3종 (Rust + wasm 경계) — **완료 2026-09-01**

- [x] **완료** — `NodeLayout.baseline: f32` (`tree.rs`). 의미: border-box 상단 기준 in-flow baseline. **내부는 센티널 `BASELINE_NONE`(-1) = 원천 없음** — 경계 출력(`get_layouts_batch`/`getLayout`)과 block intake 가 height(bottom) 폴백으로 해소 (CSS 2.1 §10.8.1). 폴백값을 저장하지 않는 이유: 부모 stretch 가 height 를 덮으면 stale + "원천 없음"과 "실측==height" 구분 불가 (컨테이너 전파는 원천 있는 자식만).
- [x] **완료** — leaf: `NodeStyle.leafBaseline: Option<f32>` 입력 (+자기 pad/border-top 로 border-box 좌표 승격). TS 공급: `enrichWithIntrinsicSize` 가 텍스트 leaf 에 contentMin/MaxWidth 와 같은 채널로 주입 — 첫 줄 half-leading 모델 `(lineHeight − fontHeight)/2 + ascent` (**측정 체인은 `measureFontMetrics`** — Canvas 2D TextMetrics 캐시. 원문의 "Skia Paragraph 측정값" 은 렌더 시점에만 존재해 layout-build 시점 공급원이 될 수 없음 — 스칼라 2종과 같은 체인이 정합). dead `calculateBaseline` 클러스터 폐기 (production caller 0, re-export 2곳 제거).
- [x] **완료** — container 출력: `solve_block` = 마지막 line box baseline (`block_layout` trailing meta 3번째 `lastLineBaseline` 이 정확값; 마지막 in-flow 가 line box 아니면 마지막 원천 보유 자식) · `solve_flex` = 첫 원천 보유 item (§8.5 — baseline 참여 판정은 S8 미구현이라 근사) · `solve_grid` = 첫 row 첫 원천 item. **산출 위치는 tree.rs placement 루프** — flex.rs/grid.rs 커널 계약 무변경 (자식 baseline 은 `NodeLayout` 에 있고 커널은 그것을 모른다; 원문 "{flex,grid}.rs 컨테이너 baseline 출력" 의 실현 위치 편차).
- [x] **완료** — `write_block_item` 슬롯 16/17/18 "미소비" 해소: `vertical_align`(**`Option<String>` 키워드 → `vertical_align_code` u8 매핑** — 원문 `Option<u8>` 대신, flex enum 과 같은 "tree.rs 가 CSS 키워드 경계" 패턴) · `baseline`(자식 stored, 센티널은 block.rs 가 border-box bottom 해소) · `line_height: Option<f32>`(px — TS 가 배율·단위 선해석). _(Phase 3 정정: 폴백 = bottom margin edge(r7) · 슬롯 18 은 S4 예약, 실소비는 컨테이너 strut(r8l1))_ NodeStyle 54필드 (가드 `nodestyle_field_contract_guard` 54=54+0 갱신; 선언 위치는 tree.rs — cascade/style.rs 아님).
- [x] **완료** — wasm 경계: `get_layouts_batch` stride **4→5** (`[x,y,w,h,baseline]`, tree_golden/기존 테스트 stride 갱신) + `getLayout` JSON `baseline` 추가. TS: `flatToLayoutMap` stride 5 + `LayoutResult.baseline?` · 통과 지점은 **`applyCommonTaffyStyle`(3 브랜치 공용 choke point) + `taffyStyleToRecord`** (원문 "TaffyBlockEngine 에서 통과" 의 실현 위치 편차 — Block/Flex/Grid 브랜치 전부 커버). 검증: `adr923BaselineInputs.test.ts` 6 (lineHeight 배율→px 등) + `seamBaselineContract.browser.test.ts` 3 (스칼라 주입·writer 전수·출력 stride).
- [x] **완료** — cargo 340(+7: tree `adr923_p2_*` 5 · block 2) · golden 15 · layout_trace 10 · tree_golden 11 전량 PASS — **golden 상수 갱신 0** (Chrome 값 원칙 위반 없음; baseline 은 golden 대조 제외, Phase 3 케이스로). parity 934 pass (기존 catalogComponentBox 2 실패만). clippy 신규 0.
- [x] **r6h1 동일 기전 재발 수리 (2026-09-01, Phase 2 중 발견)** — 신규 경계 필드도 post-order patch 함정을 연다: 2-pass 재-enrich 의 `patchBatchStyleFromImplicit` 가 CSS-형 `lineHeight("20px")` 를 raw 복사 → `updateStyleRaw` 의 `Option<f32>` serde 폭발 → **WASM 경로 전면 null** (parity 12건 연쇄 실패 — display 때의 조용한 오배치와 달리 전면 사망. RED 재현: R8-d 형 wrap 케이스). 수리: patch 루프에 lineHeight px coerce 분기 (`parseLineHeight`, fontSize 는 modStyle→batchStyle 순) + `seamBaselineContract.browser.test.ts` 가 **buildTreeBatch + updateStyleRaw 두 writer** 를 캡처 (메모리 `feedback-seam-invariant-verify-all-batch-writers` 재발 절). 신규 경계 필드 체크리스트: 1차 writer 해석 + patch coerce 분기 + 두 writer spy 가드.
- [x] **Codex 판독 round 7 수리 (2026-09-01, `9a51479a5`) — Phase 2 닫힘** — r7m1 (MEDIUM): `solve_grid` 컨테이너 baseline 후보를 children source 순서가 아니라 **placement row-major(셀 y,x) 첫 원천**으로 선택 (명시 `gridRowStart` 역순 RED 42 → 기대 4 → GREEN; flex 는 document order 가 CSS §8.5 계약이라 유지). r7m2 (MEDIUM): unitless lineHeight 환산 기준 = inline fontSize → **상속 computed fontSize**, 기본 16 폴백 제거 — `applyCommonTaffyStyle` 4번째 param `computedFontSize`, `elementToTaffyStyle` `_computedStyle` 실사용, `elementToTaffyBlockStyle`/grid 분기 + `patchBatchStyleFromImplicit` 2 call site (implicit patch 는 부모 computedStyle 기반 `resolveStyle`, 2-pass 는 `childComputed` 재사용). r7l1: seam `createNodeRaw` 3번째 writer spy — 체크리스트의 "두 writer" 는 **모든 writer 3개** (buildTreeBatch/updateStyleRaw/createNodeRaw) 로 정정. r7l3: NodeStyle 49 주석 4곳 → 54. r7l2 는 실측 기각 — slot16 단독 원복 FAIL(판별 성립); 관찰 PASS 는 슬롯 16+17 동시 원복의 slot17=0 마스킹이며 그때도 baseline 정렬 테스트가 suite RED 유지. 검증: cargo **341**(+1) · parity 934 (기존 2 실패만) · seam 4/4 · layout unit **401**(+2) · type-check 0 · clippy 신규 0 (`doc_lazy_continuation` 6건은 diff 밖 기존 위치).

### Phase 3 — Chrome 차등 증명 (G1 전반) — **완료 2026-09-01**

- [x] **완료 (23 케이스)** — 차등 케이스 ≥ 12 (ADR-156 하니스, 실 Chrome `getBoundingClientRect`): inline-block 2개 한 줄 / wrap / **명시 폭 block 형제**(ADR-198 재현) / auto 폭 block 형제 / vertical-align top·middle·bottom·baseline / line-height 명시 / 자식 margin / empty block 형제 / 부모 padding / **inline-flex 컨테이너(Button) + 텍스트 leaf baseline** / inline-grid / **(r6 관찰) atomic inline-flex·inline-grid 의 shrink-to-fit — wrap 자식 (폭 100 부모 아래 80px×2: CSS fit-content 100 vs one-pass 80) · percentage descendant 재해소** — 현 경로는 block 부모의 definite available 로 1회 solve 한 `content_w` 를 line item 폭으로 그대로 쓴다 (`tree.rs` solve_block intake → `block.rs` inline path; flex 재-solve 는 indefinite available 일 때만). Phase 1 결함이 아니라 Phase 3 차등·수리 대상 (Codex round 6 과제 3). **(r7 관찰, Codex round 7)** border-box bottom vs CSS margin-edge 폴백 차이 (`block.rs` intake `child_h`) · block 자식 텍스트 leaf 의 `leafBaseline` 미주입 (needsWidth 조건 의도 잔존 — flex/grid 자식·intrinsic 폭만 주입) · top/bottom-only line box 의 line baseline 0 (`flush_line_box` — baseline 참여자 없는 줄) · column flex 의 첫-item 근사 (§8.5 cross-axis baseline 그룹 미구현).
- [x] **완료** — `adr923ChromeDifferential.browser.test.ts`: engineLeg(어댑터 우회, raw CSS display → `buildTreeBatch`) 게이트 + pipelineLeg 대조군 기록 (`tests/parity/.artifacts/adr923-phase3-differential.json` — vitest browser 가 통과 테스트 콘솔을 숨겨 파일로 내보냄). 대조군 = **발산 18 / 정합 51** (69 케이스 — 61 케이스 18/43 · 51 케이스 18/33 · 42 케이스 18/24 · 39 케이스 18/21 · 27 케이스 18/9; 종전 16/23 표기는 r8m1 오집계, 실측 15/23) (현 IFC 시뮬레이션↔Chrome 실측 격차, Phase 5 가 닫을 대상). 표·수리 상세: [evidence/923-phase3-differential.md](../evidence/923-phase3-differential.md).
- [x] **Codex 판독 round 8 수리 (2026-09-01, `efb56a888`) — Phase 3 재닫힘 (Codex 재판독 대기)** — r8h1 middle = **baseline 앵커** (line 중앙 배치 반증 — asc/desc 를 mbox/2 씩 밀어 baseline 이동, Chrome valign-middle-tall: ib20+ib60 → a.y 10) · r8h2 마지막 line box flush 높이 → trailing meta 4번째 `inFlowBottom` + 컨테이너 auto-height max (strut-last-line: root 40 — 기존 strut 케이스는 전부 tail 보유라 관측 밖) · r8m2 overflow:clip 을 BFC·baseline 억제 **양쪽에서 제외** (BFC 는 Codex 확증 — css-overflow-3; baseline 은 오라클 ib-overflow-clip-baseline(dom a.y 20 = last line box 유지)이 Codex "clip 포함 타당" 판정을 반증) · r8m1 대조군 오집계 정정 (실측 15/23 → 27 케이스 18/9) · r8l1 slot 17 margin edge/slot 18 S4 예약 주석 6곳 · r8l2 프로덕션 wrap intrinsic-min pipelineLeg 게이트 (첫 실행 GREEN — 수리 5 의 프로덕션 반영 실증). 차등 23→**27/27** · cargo 352(+6) · parity 962 (기존 2) · layout unit 401 · type-check 0.
- [x] **Codex 판독 round 9 수리 (2026-09-01) — Phase 3 재닫힘 (Codex 재판독 대기)** — r9h1 HIGH 확증: flex item overflow:clip 이 §4.5 content floor 를 잃음 → overflow 판정 3곳(BFC·block baseline·flex §4.5)을 **단일 scroll-container 술어** (`is_scrollable_overflow` — scroll/auto/hidden 만, css-flexbox-1 §4.5 non-scrollable + css-overflow-3 §3.1) 로 통합, 양축 판정 (Chrome flex-item-clip-auto-min 80 / hidden 60 / cross-hidden 60) + 프로덕션 shorthand `overflow:"clip"` pipelineLeg 게이트 · r9m2 MEDIUM 확증 (root cause 2겹): ① self-collapsing box 를 intake 가 분류하지 않아 (`DISPLAY_EMPTY_BLOCK` 발행 주체 부재 — cargo 단위에서만 생존) empty block 의 margin 이 관통하지 않았다 → `write_block_item` §8.3.1 조건으로 코드 2 발행 ② 꼬리 margin chain 포함(can_collapse_bottom=false)/탈출 미구분 + 컨테이너 auto height 가 자식 bbox 를 부풀림 → inFlowBottom 단독 (trailing-empty-block-escape 10 · trailing-margin-contained 31 · -contained 41 · empty-first-chain-through-wrap 40) · **인접 수리** (같은 §8.3.1 모델, Chrome 7 케이스 중 5 발산): `bfc_flag` 가 BFC 자식의 자기 margin 을 합산·차단 (tree.rs 가 자식 solve can_collapse 로 이미 차단 → 이중 차단 제거; bfc-sibling-top-collapse 30 · bfc-last-child-margin-escape 30 · bfc-first-child-top-escape wrap.h 10) + line box 는 margin 을 collapse 하지 않음 (block-margin-then-line-box 20) · r9m1 "clip UI 미노출·실효 0" 공시 정정 (Style Panel Clip 직접 노출 — 실효 = 수리 5·9·10~12, 휴면 = line item 본체) · r9l1 clip baseline 규범 귀속 css-align-3 §9.1 "block-axis scroll container … block-end margin edge" · r9l2 slot 18 요약 주석 3곳. 차등 27→**39 케이스 + 게이트 2 = 41/41** (신규 12 중 RED 10) · cargo 362(+10) · parity 975 (기존 2) · layout unit 401 · type-check 0 · clippy 신규 0. **후속 ① (사용자 승인)**: `height:0` 명시 self-collapsing — 자식 `solve_block` 의 재귀 플래그 `self_collapsing` + leaf intake explicit-0 규칙 (Chrome height-zero-self-collapsing b.y 40, 대조군 2 PASS) → 차등 **42 케이스 + 게이트 2 = 44/44** · cargo 363 · parity 978. **후속 ②**: DC-6 overflow cap 은 Phase 4 항목으로 등재 (아래). Live: 실 빌더(localhost:5173) TEST 프로젝트 재로드 — Skia 전 페이지 정상 렌더, Page 2 wrap 카드 배치 불변(Desert Sunset·Hiking Trail 한 줄 + Mountain Sunrise 줄바꿈) 확대 확인, Home Button/avatar 스택 정상, 콘솔 에러 0 (로드 시점 포함). r9h1 프로덕션 경로(패널 shorthand overflow:clip → 어댑터 → wasm)는 pipelineLeg 게이트가 고정.
- [x] **Codex 판독 round 10 수리 (2026-09-01) — Phase 3 재닫힘 (Codex 재판독 대기)** — HIGH 1·MEDIUM 3·LOW 3 전부 확증·fixed: r10h1 텍스트 leaf 는 height:0 이어도 line box 가 있어 self-collapsing 아님 → `leafBaseline` 을 line box 신호로 (엔진 leaf 경로) + TS `enrichWithIntrinsicSize` 의 early return 2곳·`needsWidth` 결합이 block 부모 텍스트 leaf 의 스칼라 공급을 막던 것 수리 (Chrome b.y 60 / 종전 40) · r10m1 absolute 자식만 있는 컨테이너 self-collapsing — 판정을 leaf 경로로 이관, intake 는 플래그만 (단일 원천; `write_block_item` 규칙 제거) · r10m2 adjoining margin **집합** `MarginSet` (최대 양수 + 최소 음수 — 이항 누적은 3+ 부호 혼합에서 결합 순서 의존; 형제·관통·hoist 3층 경계를 (pos, neg) 로, `FIELD_COUNT` 19→21, golden 기대값 무변경; Chrome 20 / 종전 35) · r10m3 auto height 0 하한 (Chrome 2 / −8) + 인접: self-collapsing 근거를 in-flow bottom ≤ 0 에서 자식 코드 전수로 (Chrome 60 / 40) · LOW 3 (원복 (c) 5건 표기 · 대조군 수치 · bfc_flag 주석) 정정. 차등 **51 케이스 + 게이트 5 = 56/56** (신규 9 중 RED 8 + 게이트 3 중 RED 2) · cargo 368 · parity 990 · layout unit 401 · type-check 0 · Live (Components 텍스트 leaf 배치 불변). 상세: [reviews/923.md](../reviews/923.md) Round 10.
- [x] **Codex 판독 round 11 수리 (2026-09-01) — Phase 3 재닫힘 (Codex 재판독 대기)** — HIGH 1·MEDIUM 2·LOW 2 전부 fixed (r11m1 은 절반 확증 — `min-height: 0` 조건은 §8.3.1 self-collapsing 조건이라 Chrome 이 정정): r11h1 whitespace-only 텍스트 line box 신호 → `textLeafRendersContent` (CSS Text 3 §4.1.1 white-space 처리 후 내용; Chrome normal `" "` 40 / 종전 60 · pre·nbsp·pre-line `"\n"` 60) · r11m1 부모 bottom adjoining = §8.3.1 height auto (height:50 b.y 50 / 종전 70 · height:0 은 auto 아님 0 / 20) + Blink 모델: min/max-height 가 **바인딩**되면 strut 미전파 (min-height:30 → p.h 30·45 · max-height:10 → 25; 미바인딩 min-height:10 은 접힘 40 — `min_h > 0` 일괄 포함 55 반증) · r11m2 baseline 영향 공시 정정 (payload 공급 ≠ 소비 — Phase 5 + S8 이후 조건) · r11l1 원복 실측 기록 · r11l2 주석 9곳. 차등 **61 케이스 + 게이트 14 = 75/75** · cargo 369 · parity 1009 · layout unit 401 · type-check 0. 상세: [evidence/923-phase3-differential.md](../evidence/923-phase3-differential.md) 수리 19~~21.
- [x] **Codex 판독 round 12 수리 (2026-09-01) — Phase 3 재닫힘 (Codex 재판독 대기)** — HIGH 1·MEDIUM 2·LOW 4 전부 fixed (+ 과제 5 관찰을 수리로): r12h1 상속 white-space → 신호가 cssResolver computed 를 읽음 (Chrome 부모 pre + 공백만 60 / 종전 40) · r12l3 children 정규화 (`resolveTextLeafContent`) · r12m1 percentage min-height 세로 ctx (indefinite → 0, Chrome 40 / 35) · r12m2 min/max clamp 순서 max-then-min (`block.rs clamp_size` + root fixup + grid 트랙 sweep — Chrome min 30 > max 10 → 30 / 종전 10) · 과제 5 root 명시 height 의 min/max clamp · r12l1/l2 게이트 (flex 폭 width:auto 격리 — 프로덕션 Text 기본 width:100% 는 대조군 밖 · height:0 abs containing block) · r12l4 Blink 소스 인용. 차등 **68 케이스 + 게이트 19 = 87/87** · cargo 370 · parity 1023 · layout unit 408 · type-check 0. 상세: [evidence/923-phase3-differential.md](../evidence/923-phase3-differential.md) 수리 22~~26.
- [x] **Codex 판독 round 13 수리 (2026-09-01) — Phase 3 재닫힘 (Codex 재판독 대기)** — MEDIUM 2·LOW 4 전부 fixed + r13l4 요청 게이트가 연 결함 1 수리: r13m1 inline `white-space: inherit`/`unset` 이 raw 로 소비돼 computed(부모 pre) 무시 → `resolveTextLeafWhiteSpace` (inline 구체값 → computed → normal) (Chrome 60 / 종전 40) · r13m2 텍스트 원천 SSOT 부재 (신호/측정/Skia/Preview 순서 상이) → binding content(`children`) 단일 원천: `resolveTextLeafContent` children-only + 텍스트 leaf 측정 5 지점 + Skia/측정/overlay `maskNonContentTextProps` + Preview Description children 선행 (Chrome flex box.x 10 / 종전 label 폭 211) · r13l4 #69 root min-width:250+max-width:100 첫 실행 RED (c.w 300 / Chrome 250) → `compute_layout` 이 auto 폭 root 의 used 폭을 solve 전에 clamp (ADR-170 §1 모델) · r13l1/l2/l3 문서 정정 (body 경계 · `+` · 커밋 공시). 차등 **69 케이스 + 게이트 24 = 93/93** · 대조군 18/51 · cargo 371 (adr923 43) · layout unit 411 · type-check 0. Phase 4 진입은 사용자 승인 후.
- [x] **Codex 판독 round 14 수리 (2026-09-01) — Phase 3 재닫힘 (Codex 재판독 대기)** — MEDIUM 2·LOW 2 전부 fixed: r14m1 cssResolver 가 cascade 키워드를 trim 없이 소문자화만 해 `" INHERIT "` 가 raw 로 남음 → trim + 소문자, `resolveTextLeafWhiteSpace` computed 우선 (r14l1 근거 정정) (Chrome 60 / 종전 40) · r14m2 round 13 의 binding-선언 기반 label/text 차단 (33 primitive) 이 **Pencil import `props.text` writer** 와 collection item `label` 을 지움 → 철회; 텍스트 원천 SSOT 를 writer 인벤토리로 재정의 — `resolveTextLeafContent` children → text · Skia `buildCatalogShapes` `label || children || text || placeholder` · Preview generic `text` fallback (Chrome `text` 만 Text 60 / 종전 40) · r14l2 문서. 차등 **69 케이스 + 게이트 27 = 96/96** · 대조군 18/51 · parity 1032 · layout unit 412 · specs 858 · type-check 0. Phase 4 진입은 사용자 승인 후.
- [x] **Codex 판독 round 15 수리 (2026-09-01, `04503eebd`) — Phase 3 재닫힘 (Codex 재판독 대기)** — MEDIUM 1 확증·fixed: r15m1 writer 인벤토리가 AI `create_element`/`update_element` 의 열린 props writer 를 누락해 "label/title writer 0" 이 거짓 (AI Text `{children:"Text", label:"AI Label"}` → Skia "AI Label" / Preview·레이아웃 "Text"; Button/Column 도달) → root cause 는 consumer 별 원천 순서 → `@composition/specs` `textSource.ts` 타입별 계약 단일 모듈을 Skia·레이아웃·Preview 가 전부 위임 (기본 `children` · 텍스트 leaf+FieldError `children → text` · ListBoxItem/GridListItem/Menu `label → children` · field leaf `placeholder`; 계약 밖 키는 전 표면 공동 무시). 게이트 specs 20 · layout 3 · Preview 10 (원복 RED 6/1/1) · Chrome pin 1. 차등 **69 케이스 + 게이트 28 = 97/97** · 대조군 18/51 · parity 1033 · layout unit 415 · specs 873 · shared 926 · type-check 0. 상세: [evidence/923-phase3-differential.md](../evidence/923-phase3-differential.md) 수리 32. Phase 4 진입은 사용자 승인 후.
- [x] **Codex 판독 round 16 수리 (2026-09-01, `89155edea`) — Phase 3 재닫힘 (label 축; Codex 재판독 대기)** — MEDIUM 1 확증·fixed (label 축): r16m1 composite parent `label` 은 노드 자기 텍스트 계약이 아니라 propagation registry 가 canonical Label 자식으로 잇는 별개 축인데 ColorField 만 규칙이 없었다 (spec 시절부터 `size` 만, factory parent label 도 없음) → Preview "Changed Color" / Skia·레이아웃 "Color". 수리: registry `label → Label.children` 3 가족 (factory sweep 이 CheckboxGroup/RadioGroup 추가 검출) · ColorField factory parent label · Preview 그룹 라벨 parent 우선 · legacy migration. 게이트 다리 5 (sweep RED 3 가족) · migration 3 · 자체 발견 수리 34 (overlay 텍스트 편집 reader/writer 를 계약에 위임, 게이트 5). 차등 97/97 · parity 1033 · layout unit 415 · builder unit 1612 · shared 926 · type-check 0. description/errorMessage 축은 field 전 가족 Skia 미투영 (round 15 이전부터) — ADR-923 범위 밖 — 사용자 결정 2026-09-01 (별도 작업). 상세: [evidence/923-phase3-differential.md](../evidence/923-phase3-differential.md) 수리 33. Phase 4 진입은 사용자 승인 후.
- [x] **Codex 판독 round 17 수리 (2026-09-01, `6e8d5ed39`) — Phase 3 재닫힘 (Codex 재판독 대기)** — MEDIUM 3·LOW 2 전부 확증·fixed, 판독 3건을 같은 형태 sweep 으로: r17m1 Preview 의 composite label `||` 접기가 사용자가 비운 `""` 를 stale Label 자식으로 되살림 (engine 은 undefined 만 skip) → shared `resolvePropagatedText` 를 10 reader (그룹 2 · SearchField/ProgressBar/Meter/ComboBox child-first 4 · Date* default-fallback 4) 가 위임 (원복 RED 6) · r17m2 형태 migration 4개가 진입점마다 복제·import 는 0 통과 → `applyCanonicalDocumentMigrations` 단일 체인 + 3 진입점 + 정적 결선 게이트 · r17m3 `renderDisclosure` 헤더 `children || title` → 계약 위임 · r17l1 sweep 20 가족 정확 집합 · r17l2 1607 → 1612. 차등 97/97 · parity 1033 · layout 415 · builder unit 1616 (+resolvers 96) · shared 935 · type-check 0. 상세: [evidence/923-phase3-differential.md](../evidence/923-phase3-differential.md) 수리 35~37. Phase 4 진입은 사용자 승인 후.
- [x] **Codex 판독 round 21 수리 (2026-09-02, `1155ae702`) — Phase 3 재닫힘 (Codex 재판독 대기)** — MEDIUM 2·LOW 1 확증·fixed + live 후속 1: r21m1 빈 내용 상자 sweep 이 5 종을 남김 → 수리 54 ToggleButtonGroup 자식 0 = 0×0 (종전 80×30) · 55 Tabs items 0 = tab bar 0 + panel 없음 (stale TabPanel 제외; `utils.ts` + `implicitStyles` 3 분기 동일 판정) · 56 TagGroup 슬롯 자식 가시성 = parent prop (`{label && <Label>}` — 빈 label 이 gap 4 를 만들었다) · 57 GridList/Tree `data-empty` 상태 padding 16/24 (base 규칙만 보던 layout, 인라인 우선) · 58 Table `min-height: 40px` layout 채널 · r21m2 Button min-content 하한이 최종 폭과 다른 padding 원천 (padding:20 → 84 vs 68 · padding:0+minWidth:0 → 44 vs 2) → 수리 60 `resolveLeafBoxEdges` 단일 원천 + 인라인 minWidth 는 엔진 적용 · r21l1 → 수리 61 ADR-157 주석 정정. **live 후속 수리 59**: 58 만으로는 Preview 가 auto 에서도 402 — `Table.binding.ts` accepts 에 `heightMode`/`height` 선언이 없어 cutover 렌더러가 미전달 (Inspector·AI writer dead, layout 만 소비) → 선언 추가로 3 모드 일치. 게이트: Chrome 실 CSS 오라클 +7 (하니스 `attrs`/`props`/`style` 축) · 단위 15 · binding 다리 2. 원복 RED 4 조합 · 차등 97/97 · parity 1041 · builder 1810 · layout 446 · shared 955 · specs 875 · type-check 0. Live: 빈 8 조합 Skia = Preview DOM + Table 3 모드 (auto 40 · fixed 402 · height 240 → 242).
- [x] **Codex 판독 round 22 수리 (2026-09-02, `6c2e1b388`) — Phase 3 재닫힘 (Codex 재판독 대기)** — MEDIUM 1 확증·fixed + 전수 sweep 2: r22m1 prop 부재 기본값 계약 — `toRacProps` 는 props 에 키가 없으면 `accepts[key].default` 를 채워 넘기므로 prop 없는 Table 의 Preview 는 fixed × 400 = 402 인데 layout 은 리터럴 300 → 302 였다 (round 21 이 binding 다리를 놓으면서 layout fallback 미정렬; factory 가 항상 `height: 400` 을 기록해 팔레트 경로가 가렸다) → 수리 62 shared `resolveBindingPropDefault(type, key)` 단일 조회. 같은 형태를 catalog 전 타입으로 전수 대조 → 수리 63 layout `DEFAULT_SIZE_BY_TAG` ↔ catalog `defaultSize` 불일치 2 (Badge catalog sm · 표 md / Select catalog md · 표 sm — 생성 CSS base 규칙이 `defaultSize` 값으로 emit 되므로 prop 없는 DOM 은 catalog 기본 size) → `resolveComponentRuleByTag(tag).defaultSize` 정본 · 수리 64 catalog `sizes[size].borderWidth` 가 L3 size 축 미러에 없어 top-level `containerStyles` 없는 타입은 border 가 캔버스에 도달할 경로 자체가 없었다 (Badge DOM 20 vs layout 18). 게이트: 전수 동치 2 (prop 부재 = binding default 명시 / catalog defaultSize·defaultVariant 명시) + 갈렸던 3 타입 고정 4 + shared 조회 계약 6 + Chrome 실 CSS 오라클 Badge 1. 원복 RED 3 조합 · 차등 97/97 · parity 1042 · builder 1810 · layout 452 · shared 961 · specs 875 · type-check 0. Live: factory 가 기록한 `height`/`heightMode` · `size` 를 store 에서 제거해 prop 부재 재현 — Table Skia 402 = Preview 402 · Badge Skia 22 = DOM 22 (sm).
- [x] **Codex 판독 round 23 수리 (2026-09-02, `5678b377c`) — Phase 3 재닫힘 (Codex 재판독 대기)** — MEDIUM 1 확증·fixed + 같은 형태 sweep 1: r23m1 round 22 의 전수 동치 게이트가 **자식 없는 단일 노드**만 재서 기본값을 자식·데이터가 있을 때만 소비하는 경로를 못 봤다 (GridList `selectionMode` `single → multiple` mutation 이 6 테스트를 그대로 통과; item 1개면 갈린다) → 수리 65 fixture 를 타입마다 3 변형 (`bare` / 자식 2개 / `items` 2개) 으로 확장 (같은 mutation 에 `GridList [items] 76 vs 98` RED). 그 축을 따라가 수리 66: `defaultSelectionMode` 를 네 소비처 (layout 카드 높이 · scene 카드 · virtualization stride · Skia Tree 체크박스) 가 각자 리터럴로 들고 있었다 — cutover Preview 는 `toRacProps` 가 채운 binding default 를 받으므로 (렌더러 기본값 미도달) 원천은 catalog binding 하나여야 한다 (GridList binding `single` vs 세 자리 `none`; 둘 다 `checkboxModes: ["multiple"]` 밖이라 시각은 같았다) → shared `resolveBindingSelectionMode(type, fallback)`. 게이트: 확장 전수 동치 2 · shared 계약 +2 · 정적 4 (리터럴 0). 원복 RED 3 조합 · 차등 97/97 · parity 1042 · builder 1812 (+2 는 다른 세션) · layout 456 · shared 963 · specs 875 · type-check 0. Live: GridList items 3 의 `selectionMode` 부재 164 = 164 · multiple 208 = 208 · single 164 = 164 (시각 무변경 계약 확인).
- [x] **Codex 판독 round 24 수리 (2026-09-02) — Phase 3 재닫힘 (Codex 재판독 대기)** — HIGH 1 + MEDIUM 2 전부 확증·fixed: r24m1 계약의 근거(`toRacProps` 가 binding default 를 채운다)가 **generic cutover 경로에서만** 성립했다 — delegating 렌더러 타입은 `toRacProps` 를 거치지 않아 Preview 가 렌더러 리터럴을 쓴다. GridList·ListBox binding `selectionMode` default `single` 은 어느 표면에도 없던 값(렌더러 `|| "none"` · RAC `none` · factory `"none"`)이고 Inspector 만 그것을 현재값으로 표시했다 → 수리 67 선언을 실제 렌더 값으로 정정 + delegating 렌더러 5 자리와 `selectionStyle` 소비처 4곳을 binding 경유로. 수리 68 게이트 축 2개(활성화 baseline · Preview 전수 동치 `renderToStaticMarkup`). r24m2 → 수리 69 정적 게이트를 기능 게이트로(scene/virtualization/Skia 각 production 진입점 + 대조군). r24h1 → CHANGELOG 엔트리. 원복 RED 3 조합 · 차등 97/97 · parity 1042 · layout 459 · builder 1814 · scene 153 · shared 965 · specs 875 · type-check PASS. Live: 부재 = 명시 `none` (Preview aria-selected 없음 / Skia 164) · 대조군 multiple (체크박스 / 208). 잔여: Preview 게이트가 처음 드러낸 축 5개 45건은 방향이 축마다 갈려 `KNOWN_DIFFS` baseline 고정 (다음 round 후보).
- [x] **Codex 판독 round 25 수리 (2026-09-02) — Phase 3 재닫힘 (Codex 재판독 대기)** — MEDIUM 2 + LOW 3 전부 확증·fixed: r25m1 기능 게이트가 양성 대조(multiple)와 기본값(none)만 보고 제외돼야 할 유효 enum `single` 을 음성 대조로 두지 않아 scene/virtualization `checkboxModes` 를 Tree 규칙으로 바꿔도 13/13 PASS → 수리 70 음성 대조 2 (`single` = none · `multiple`+`highlight` = none) 를 scene · virtualization · layout(신설) 에, Tree Skia 는 모드 집합 3 으로 (`checkboxModes` 4 자리 전수). r25m2 Preview ratchet 키가 속성 차이만 담아 그 뒤의 내용·구조 변경(ProgressBar `valueLabel` BROKEN)을 흡수 → 수리 71 서로 다른 속성을 걷어낸 나머지를 다시 비교해 갈리는 구간을 키에 포함 — 기존 Meter/ProgressBar 6건의 값 문구 차이(`0%` ↔ `75%`/`50%`)가 드러나 키 갱신(45건 그대로). LOW 3 → 수리 72 (45건 정정 · helper 주석 · CHANGELOG 영향 범위). 원복 RED 4 조합 · 차등 97/97 · parity 1042 · layout 460 · builder 1814 · scene 153 · shared 965 · specs 875 · type-check PASS. Live: `single`+checkbox = Skia 164 · Preview 체크박스 0 (none 과 같음) · `multiple`+checkbox 208 · 체크박스 3 · `multiple`+highlight 164. 잔여 5축 45건은 판독의 축별 정본 판정(ListBox 렌더러 · Menu binding · labelAlign 시각 무차이 · value 는 content · 나머지 비-load-bearing)을 evidence 에 기록, 후속 scope.
- [x] **Codex 판독 round 26 수리 (2026-09-02) — Phase 3 재닫힘 (Codex 재판독 대기)** — MEDIUM 1 + LOW 1 전부 확증·fixed: r26m1 기능 게이트는 현재 값의 boolean 동치만 봐서 GridList 규칙에서 none·single 이 같은 false 로 접히고, 정적 게이트는 helper 호출 여부만 봐 layout `defaultSelectionMode` 를 Tree binding 으로 오결선해도 14/14 + layout 460 통과 → 수리 73 **binding mutation 게이트** 3 (테스트 안에서 binding default 를 바꾸고 복구 — GridList 소비처 3 은 GridList binding 만 따라가고 Tree binding 에 불변, Skia Tree 는 반대) + 정적 게이트를 파일별 component 리터럴 고정으로. r26l1 → 수리 74 evidence 문구 (ListBox factory 는 `single`). 원복 RED 4 조합 (4 소비처 오결선 각 3 fail) · 차등 97/97 · parity 1042 · layout 463 · builder 1814 · scene 153 · shared 965 · specs 875 · type-check PASS. Live (프로덕션 코드 변경 0): GridList 명시 `none` 164 · 키 제거 부재 164. 판독: 수리 71 VERIFIED, `KNOWN_DIFFS` 후속 범위 판단 타당.
- [x] **Codex 판독 round 27 수리 (2026-09-02) — Phase 3 재닫힘 (Codex 재판독 대기)** — MEDIUM 1 확증·fixed: r27m1 round 26 의 binding mutation 은 `selectionMode` 에만 양성이라 `resolveBindingSelectionStyle` 이 GridList 대신 같은 현재값(`checkbox`)의 CardView 를 읽어도 17/17 · layout 463 · shared 965 통과, Tree-style 음성 대조는 mutation 안의 두 결과끼리 비교해 vacuous → 수리 75 parity +2 (GridList style → highlight 양성 + CardView·Tree 음성, Skia Tree 양성/음성, 기준은 mutation 밖에서) + shared `defaultContractLookup` +2 (helper 수준 source identity: style 은 CardView, mode 는 ListBox 가 같은 값 sibling). 원복 RED 4 조합 (helper style→CardView 2 · utils style→CardView 2 · helper mode→ListBox 2 · Skia style→GridList 3) · 차등 97/97 · parity 1042 · layout 465 · builder 1814 · scene 153 · shared 967 · specs 875 · type-check PASS. Live (프로덕션 코드 변경 0): GridList multiple + style 부재 208·체크박스, highlight 명시 164. 판독: 수리 74 VERIFIED · singleton 누출 0 · fallback 반전은 현 계약에서 동치.
- [x] **Codex 판독 round 28 (2026-09-02) — Phase 3 닫힘 · Phase 4 진입 가 (사용자 승인 대기)** — 수리 75 VERIFIED: selection mode/style × 소비처 4 × helper 2 source identity 누락 0, mutation 누출 0, 재실행 전부 일치 (layout 465 · shared 967 · builder 1814 · scene 153 · specs 875 · 차등 97/97 · parity 1042 · type-check · ADR validator 98/98), 원복 RED 4 조합 기대 일치. 이슈 HIGH 0 · MEDIUM 0 · LOW 1 (r28l1 README 집계 `수리 61` → 75, fixed — 코드 변경 0). 다른 기본값 축 (Table height · Badge/Select size) 의 일반화 mutation 보강은 후속 범위 판정 (Phase 4 차단 사유 아님).
- [x] **Phase 4 완료 (2026-09-02) — 준비 (동작 무변경), Codex round 29 판독 대기** — 사용자 착수 승인 2026-09-02. G5: 분류표 24 (AB 11 · B 6 · ? 7) + `INTRINSIC_MEASURE_TAGS` 분리, 분리 전 baseline 과 enrich 출력 diff 0 (124 entries) · G4 전반: 패널 Direction/Alignment `inline-flex` = flex (테스트 +4, live 확인) · `resolveDefaultDisplay` 신설·미배선 (spec 3종은 containerStyles 없음 → block, 사실 정정) · HC1 browser 게이트 (Button inline-block ↔ flex, `it.fails`) · DC-6 인벤토리 (도달 18 · cap 실동작 6 = SelectValue 4 + inline Button 2, Phase 5 제거 목록 등재). 검증 layout 472 · builder 1814 · scene+styles 589 · shared 967 · specs 875 · 차등 97/97 · parity 1045 · type-check PASS · 원복 RED 6 조합. 상세 evidence/923-phase4-preparation.md.
- [x] **Codex 판독 round 29 수리 (2026-09-02) — Phase 4 재닫힘** — MEDIUM 2 · LOW 3 전부 확증·fixed: r29m1 DC-6 입력이 수동 creator 44 + catalog leaf 조합이라 production 생성 형태 (complex 5 종 · reusable ref 4 종) 를 못 덮음 → 수리 76 팔레트 × creation facet 파생 + production origin seed/ref 해석 + facet 집합 고정 → 도달 19 · **cap 8** (ListBox/GridList 신규 발견) · r29m2 calendargrid `handDisplay: block` 이 현재값과 후보값 두 뜻 → 수리 77 `handDisplay` = 현재 값 / `domDisplay` = DOM 후보 분리 + CalendarGrid Q4 browser 게이트 3 · r29l1 수치 (472/7 → 실제 473/8, 현재 474/9) · r29l2 CHANGELOG 엔트리가 다른 세션 커밋 `a996ccb67` 에 휩쓸림 (기록 — 이번 라운드 코드도 `5b229437c` 에 휩쓸림) · r29l3 README 행 stale 꼬리 + 옛 영어 은어 1 정정. 검증 layout 474 · parity 1049 · type-check PASS · 원복 RED 5 조합.
- [x] **Codex 판독 round 30 통과 (2026-09-02) — Phase 4 닫힘 · Phase 5 착수 가 (사용자 승인 필요)** — 수리 76·77 VERIFIED, HIGH 0 · MEDIUM 0 · LOW 3 (전부 문서, fixed): r30l1 evidence 머리말 stale · r30l2 원복 RED (h) 기록이 두 타입을 묶음 — 재실측 19 → 18 행, ListBox 만 `> ref overflow:auto H=` 행으로 바뀌고 GridList 행은 소실 (ListBox factory ref parent 의 raw `overflow:"auto"` `SelectionComponents.ts:276` vs GridList `overflow:hidden` 은 `implicitStyles.ts:1502` gridlist 분기의 type-keyed 주입) · r30l3 Phase 5 ListBox/GridList 게이트에 flex §4.5 소비 문맥 명시 (`is_scroll_container` `tree.rs:4788` 은 §4.5 의 입력이지 대안이 아님). Codex 판정: CalendarGrid 는 추가 측정 없이 `block` 선택 근거 충분 (Phase 5 항목에 기본 경로로 기록). 검증 DC-6 2/2 · type-check PASS. evidence §11 · reviews Round 30.
- [x] **Codex 판독 round 31 수리 (2026-09-02) — Phase 5 열림 유지 (round 32 재판독 대기)** — cutover 코드·의도된 diff 18·게이트 반전·G1 후반·DC-6·G3 전부 VERIFIED, HC2 PARTIAL: r31m1 (MEDIUM) HC2 FieldError 행이 Canvas 기본 상태 (factory `display:none`) 와 DOM invalid 상태 (`span:block`) 를 비교해 "일치" — 같은 production 상태 짝 (기본 · invalid) 을 양쪽에서 각각 캡처하는 게이트 (`fieldErrorStates`) 로 교체 → 기본 `{none, (없음)}` · invalid `{none, span:block}` (Canvas 에 `isInvalid` 를 읽는 코드 0) → 새 범주 `투영필요(후속)`, 분포 2·6·13·2·4·5·1 을 7 범주 전체 단언, 원복 RED (e) 2 · (f) 1 · (g) 2 · r31l1 집계 정정 (focused 122 · full parity 1065) · r31l2 stale 주석 9곳 현재 계약으로 (실행 코드 변경 0, cargo PASS). 상태 투영 수리는 Phase 5 후속 항목. evidence/923-phase5-cutover.md §5·§11 · reviews Round 31.
- [x] **Codex 판독 round 32 통과 (2026-09-03) — Phase 5 닫힘 · Phase 6 진입 가 (사용자 확인 필요)** — r31m1 같은 상태 짝 VERIFIED (Inspector 와 같은 top-level props 축 · factory 기본값 = DOM 기본 캡처 · Canvas `isInvalid` 소비 0), 7 범주 분포 단언이 종전보다 강함, 후속 분리 타당 → HC2 를 Phase 5 종결 근거로 사용 가. 원복 (e) 2 · (f) 1 · (g) 2 재현. HIGH 0 · MEDIUM 0 · LOW 2 (전부 문서·주석, fixed): r32l1 요약·상태 표면 집계 drift (evidence §0/§1/§8 · breakdown · README — builder 1817 = 범위 밖 신규 3) · r32l2 이전 계약 서술 5곳 + sweep 6곳 (skills reference · component-registry · cross-check SKILL · probe 머리말 · 테스트 머리말 2). evidence §12 · reviews Round 32.
- [x] **Codex 판독 round 20 수리 (2026-09-02, `641763bfb`) — Phase 3 재닫힘 (Codex 재판독 대기)** — MEDIUM 2 확증·fixed + sweep 2 + live 발견 3: r20m1 layout 빈 정적 ListBox/GridList 의 sample fallback (3 행 110 / 4 카드 164; DOM rows [] · scene projection 없음) → 빈 집합 = padding + border · r20m2 Menu trigger 이름 경로 — 호출자 aria-\* 가 `MenuTrigger` (context provider) 로 가서 소실 → trigger Button 직접 + 둘 다 없고 글자도 없으면 i18n `menuTriggerLabel` (속성만) · sweep: `renderMenu` 가 factory `aria-label` 미전달 · legacy `renderButton` aria 미전달 · live 발견: Menu trigger 빈 글자 DOM 68×10 vs Skia 106×30 → `deriveSizeConfig` 가 catalog `sizes.minWidth` 를 버림 (글자 있는 버튼도 54 vs 68) + 빈 글자 `DEFAULT_WIDTH` 80 + 줄 상자 20 → button 가족 빈 글자 = minWidth × (padding + border), Chrome 실 CSS 오라클 `catalogComponentBox` Button 케이스. 원복 RED 18 · 차등 97/97 · parity 1034 · builder 1810 · layout 431 · shared 953 · specs 875 · type-check 0. Live: ListBox `items: []` → Skia = Preview 366×10 · Menu 빈 글자 → aria-label 3 경로 + Skia 68×10 = DOM.
- [x] **Codex 판독 round 19 수리 (2026-09-01, `c298cbe6b`) — Phase 3 재닫힘 (Codex 재판독 대기)** — MEDIUM 1·LOW 1 확증·fixed, 같은 형태 sweep 3: r19m1 ① legacy Preview `renderButton` "Button" 폴백 제거 ② layout Breadcrumbs 집계 — items projection 문서 (자식 = Rows) 에서 crumb 0 → 항상 192px phantom 주입이던 것을 crumb 원천 = Rows 아래 projection crumb / legacy 자식, 기본 crumb 없음, 빈 라벨도 separator 폭 · sweep: TagList `Tag N` → `toItemProjectionRow` · IllustratedMessage `||` (legacy 렌더러 + canonical 컴포넌트) → specs `resolveIllustratedMessageText` 단일 지점 + "" 줄 접기 3 표면 · Menu 컴포넌트 `|| "Menu"` (r18 렌더러 수리를 되돌리던 층) · r19l1 `.setDocument(` 직접 호출자 18. 원복 RED 12 · 차등 97/97 · parity 1033 · builder 1810 · layout 423 · shared 946 · specs 875 · type-check 0. Live: Breadcrumbs fit 190 (실제 crumb) · IllustratedMessage heading 비움 → 두 표면 "No content" 동일 (Inspector 는 "" 를 undefined 로 쓴다).
- [x] **Codex 판독 round 18 수리 (2026-09-01, `9bd23adf5`) — Phase 3 재닫힘 (Codex 재판독 대기)** — MEDIUM 3·LOW 1 전부 확증·fixed, 같은 형태 sweep 으로: r18m1 Preview 가 계약 resolver 의 빈 결과에 `|| "Section"` 류 기본 글자를 다시 붙여 사용자가 비운 헤더가 Preview 에만 남음 (Skia 는 기본 글자 0) → Preview 6 지점 (Disclosure 헤더 2 · standalone 헤더 · Column · TreeItem 2 · Menu) + 레이아웃 2 (DisclosureHeader `?? "Section"` · Breadcrumb `children ?? label ?? title`) 계약 그대로; 확증 중 Disclosure parent `title` 다리 부재 → registry `title → DisclosureHeader.children` (Card 동형) + Preview `resolvePropagatedText` · r18m2 시드 + 형태 migration 체인이 두 곳에 복제되고 전체 문서 교체 경계 (`applySnapshotDocument`: snapshot 복원 · undo/redo · 프로젝트 파일 가져오기) 는 0 통과 → `normalizeMainDocument` 단일 소유 + 3 경계 · r18m3 import adapter 변환 3 분기 + migration 단일 출구 (분기 누락 구조적 불가) + 분기별 기능 게이트 · r18l1 README 행 `|`. 원복 RED 12 · 차등 97/97 · parity 1033 · builder 1810 · layout 417 · shared 939 · type-check 0. Live: Disclosure Title 편집/비움 → Skia·Preview 동일.
- [x] **완료** — 1차 실행 14/23 → **9 fail 전부 엔진 결함 확정·수리** → 2차 23/23 ≤1px (TOL 1.0 무변경, 강등 없음). 수리 5건: ① valign-bottom 초과분의 baseline 밀림 (`line_metrics` — §10.8.1) ② atomic line-height 불관여 (§10.8 margin box — Phase 2 슬롯 18 의미론 반전, S4 예약) ③ 컨테이너 strut (`block_layout_with_strut`, asc=desc=lh/2 half-split — 실폰트 보정은 S4/Phase 5) ④ 폴백 baseline = bottom margin edge + overflow≠visible 강제 (§10.8.1 두 조항 — r7 관찰 확정) ⑤ atomic inline shrink-to-fit (`solve_block_child` §10.3.9 + wrap min-content = 최대 item — css-flexbox-1 §9.9). **프로덕션 실효 변화는 ⑤의 wrap flex min-content 측정 정정뿐** (2-b 실측 교체 경로 — parity 957 회귀 0, 나머지는 inline-\* 미송신으로 Phase 5 까지 휴면). cargo 346 · 차등 23/23 · parity 957 (기존 2 실패만) · layout unit 401 · type-check 0 · clippy 신규 0. Live: 실 빌더 TEST 프로젝트 Chrome MCP — wrap 카드 자연폭·줄바꿈 정상, 콘솔 에러 0.

### Phase 4 — 준비: 패널 · `INTRINSIC_MEASURE_TAGS` 분리 · HC1 테스트 (G4 전반 · G5) — **동작 무변경**

- [x] **완료 2026-09-02** — `isFlexDisplay` (`flex | inline-flex`) 를 `useFlexDirectionKeys` · `useFlexAlignmentKeys` 두 곳에, 테스트 +4 (양성 2 · 음성 2). live: inline-flex div → Direction column · Alignment centerTop, inline-block → block. (원문) 패널: `useLayoutAuxiliary.ts:71 useFlexDirectionKeys` + `:99 useFlexAlignmentKeys` — `display` 가 `flex | inline-flex` 면 flex (r2 l3). `useLayoutValues.ts:52` specPreset.display 소비처 동형. Direction·Alignment 가 `display:inline-flex` 요소를 flex-row 로 표시하는 테스트 (2026-06-27 회귀 방지). catalog 가 아직 `flex` 라 Button 표시 무변경 — 사용자 지정 `inline-flex` 만 바로잡힌다.
- [x] **완료 2026-09-02** — `utils.ts` `INLINE_BLOCK_TAG_CLASSIFICATION` 24 (role AB 11 · B 6 · ? 7 / 원천 catalog 17 · hand 7) → `INTRINSIC_MEASURE_TAGS` (명시 목록, 24 전부). `needsWidth` 만 소비. G5 diff: 분리 전 커밋에서 캡처한 baseline JSON (31 tag × width 4 = 124) 과 diff **0** (`adr923IntrinsicMeasureSplit.test.ts` 7, 정적 결선 게이트 포함 — 멤버십이 같아 기능 게이트로는 원복을 못 가른다). (원문) `INTRINSIC_MEASURE_TAGS` (Phase 0 분류표의 intrinsicMeasure 항목, **명시 목록 유지**) 신설 → `needsWidth` (`utils.ts:4570`) 만 이 목록을 소비. G5 diff: 분리 전후 `enrichWithIntrinsicSize` 출력이 대표 fixture 에서 diff 0.
- [x] **완료 2026-09-02** — `engines/defaultDisplay.ts` (utils 는 implicitStyles 와 순환이라 별도 모듈) — `resolveContainerStylesFallback` 그대로 → hand 7 → `block`. 잔존 spec 3종은 spec 에 `containerStyles` 자체가 없어 `block` (= 현 `getElementDisplay`, 사실 정정). 미배선 잠금: `getElementDisplay` 는 여전히 inline-block, adapter 소스에 참조 0 (정적). **round 29 r29m2**: calendargrid `handDisplay` 를 현재 값 `inline-block` 으로 정정 (hand 7 전부 현재 값 — `getElementDisplay` 오늘 값과 G5 가 대조) 하고 DOM 정합 후보 `block` 은 `domDisplay` + `domEvidence` 로 분리, Q4 browser 게이트 3 (`adr923CalendarGridQ4` — production 트리 inert · 자유 배치 엔진 직결에서만 갈림 · DOM table/flex, evidence §9). 배선 시 의도된 diff = catalog 17 목록 고정, hand 0. (원문) `resolveDefaultDisplay(type)` (catalog `containerStyles.display` 파생, 미등록 native 3종은 spec) 함수 + 단위 테스트 추가. **`getElementDisplay` 배선은 Phase 5** — 지금 배선하면 catalog `flex` 가 부모 판정으로 새어 `classifyChildDisplay`(`taffyDisplayAdapter.ts:334-347`) 가 block 으로 분류 → IFC 시뮬레이션 해제 → Button 세로 적층 (r2 m2). `INLINE_BLOCK_TAGS` 는 Phase 5 까지 유지.
- [x] **완료 2026-09-02** — `tests/parity/adr923Hc1ChildDisplay.browser.test.ts`: 부모 `childDisplays` (toTaffyDisplay mock) ↔ 자식 `buildTreeBatch` 도달 display. Button = inline-block ↔ flex → `it.fails` + 사실 고정, 대조군 (명시 block 자식) PASS. (원문) HC1 테스트: `fullTreeLayout` 단위 테스트에서 `childDisplays[i]` == 자식 노드에 전달된 `display` 단언 — 현재 Button 은 `inline-block` vs `flex` 로 **실패**(`it.fails` 고정) → Phase 5 에서 통과로 전환.
- [x] catalog 무변경 확인 (git diff 0). (원문) catalog 는 이 Phase 에서 **건드리지 않는다** (r1 의 "같은 commit" 은 Phase 5 로 이동).
- [x] **완료 2026-09-02 · round 29 r29m1 수리** — `tests/parity/adr923Dc6OverflowCapInventory.browser.test.ts`: 입력을 production 생성 SSOT — 팔레트 65 × creation facet (`adr923ProductionTrees.ts`: ref 5 는 `useElementCreator` 형태 instance, complex 41 은 `ComponentFactory.createComplexComponent`, none 19 는 `getDefaultProps`; 세 arm 전부 `normalizeMainDocument` origin seed 위에서 `resolveCanonicalRefTree` 해석; facet 집합 고정) + sub-part standalone 21 + inline 5 로 돌려 `PersistentTaffyTree.buildFull(batch)` 캡처 (JSON 은 elementId 사영 소실), availH/availW 8 ↔ 100000 대조. overflow 도달 **19** · cap 실동작 **8** = SelectValue 4 + **ListBox/GridList 2** (ref 해석 후 처음 드러남 — 종전 수동 44 는 `ListBox > ref H=`) + 사용자 inline Button 2. 종전에 빠졌던 complex 5 · ref 4 는 도달 0. catalog 에만 overflow 가 있는 타입은 enrich raw 에 닿지 않음. ratchet 고정 (arm 접두사 키), Phase 5 목록 등재 (아래). 상세 evidence/923-phase4-preparation.md §5. (원문) **DC-6 overflow cap 인벤토리** (Phase 3 round 9 후속 ②, 2026-09-01): `enrichWithIntrinsicSize` (`utils.ts:4531` `isOverflowClipped = overflow !== "visible"`, 소비 `:4753` height · `:4856` width) 가 주입 intrinsic 크기를 available 로 cap — 게이트 `needsHeight = !rawHeight` (`:4555`) 라 **height 미지정 요소 전부**가 대상. 엔진 flex §4.5 automatic minimum 의 TS 중복 구현 (엔진은 r9h1 로 scroll-container 술어 정합) 이면서 block 문맥에도 걸고 clip 을 hidden 과 같이 취급 (css-overflow-3: clip 은 scroll container 아님). 이 Phase 는 **동작 무변경** — Q4 소비 경로 캡처 (overflow 를 받는 needsHeight/needsWidth 요소가 프로덕션에 실재하는지 `buildTreeBatch` spy) 만 하고, **제거는 Phase 5 cutover 목록** (TS 시뮬레이션 제거) 에 추가 + 제거 시 Chrome 케이스 (block 문맥 auto-height + overflow hidden/clip 은 cap 되지 않는다 / flex 문맥은 엔진 §4.5 가 담당) 로 게이트. clip 만 빼는 부분 수정은 증상 수정이라 금지.

### Phase 5 — cutover: TS IFC 시뮬레이션 제거 (G1 후반 · G2 · G3) — **HIGH 귀속, 단일 commit** — **완료 2026-09-02 · Codex round 31 (MEDIUM 1 · LOW 2 fixed) · round 32 통과 2026-09-03 → 닫힘**

- [x] **DC-6 overflow cap 제거** (Phase 4 인벤토리 등재): `enrichWithIntrinsicSize` 의 `isOverflowClipped` 높이/폭 cap 삭제. 게이트 = Chrome 케이스: SelectValue 4 (Select · ComboBox · NumberField · SearchField trigger 값 — flex 문맥, 엔진 §4.5 담당) + ListBox/GridList 2 (production 형태의 컨테이너 — round 29; **round 30 r30l3 명시**: production 서브트리를 **main-axis 크기가 제한된 flex 부모의 item** 으로 두고 ListBox `overflow:auto` · GridList `overflow:hidden` 이 엔진 flex §4.5 automatic minimum **0** 으로 소비되는지 확인 — `is_scroll_container` (`tree.rs:4788`, `data[off + 18]`) 는 §4.5 의 입력이지 대안이 아니다; 현 인벤토리의 root availableHeight=8 대조는 TS cap 존재만 확인하고 §4.5 소비를 증명하지 않는다) + inline Button overflow hidden/clip (block 문맥 auto-height + overflow 는 cap 되지 않는다). DC-6 ratchet (`adr923Dc6OverflowCapInventory`) 은 제거 결과로 갱신 (cap 8 → 0). clip 만 빼는 부분 수정 금지. **완료 2026-09-02** — cap 2 곳 + `isOverflowClipped` 삭제, ratchet cap 8 → 0 (도달 19 유지), Chrome 게이트 11 (`adr923Dc6ChromeGate`: block auto-height Text 3 · production ListBox/GridList flex 80/400 §4.5 소비 · DOM 아날로그 4 · SelectValue 불변 · block Button overflow 불변 2). 관찰: 가상화 collection 의 min-content floor (evidence/923-phase5-cutover.md §4·§9).
- [x] **calendargrid 전환 판정** (round 29 r29m2, Q4 evidence §9): 분류표 `domDisplay: "block"` (DOM 정합 후보) 은 production Calendar 트리에서 inert (부모 flex 두 표면 · 엔진 경계 blockify) 이고 자유 배치 형태에서만 엔진 직결 결과가 갈린다 (DOM `<div>` block 과 정합). 전환하면 `handDisplay` 를 바꾸고 `getElementDisplay → resolveDefaultDisplay` 의도된 diff 목록에 calendargrid 를 추가; 전환하지 않으면 현재 값 유지. 어느 쪽이든 Q4 §9 를 근거로 명시 — 한 필드에 두 뜻 금지. **round 30 Codex 판정**: 추가 측정 없이 `block` 을 선택하고 의도된 diff 목록에 calendargrid 를 추가할 근거가 충분 (Q4 3 케이스 + Rust blockify 의 inner 보존 `display.rs:142` · `tree.rs:1271`) — Phase 5 기본 경로 = `handDisplay: "block"` 전환 + `domDisplay` 제거 (현재값과 같아짐) + G5 의도된 diff 목록 (catalog 17 + calendargrid 1 = 18) 갱신 + Q4 게이트의 현재값 검사 갱신. **완료 2026-09-02** — `handDisplay: "block"` 전환 (domDisplay 제거), 의도된 diff 18 에 등재, Q4 게이트 현재값 검사 갱신.
- [x] catalog: `componentRulesTable.ts:927` Button · `:13108` ToggleButton → `containerStyles.display` 를 DOM 과 같은 값(`inline-flex`) 으로 (확정 전환 2). Phase 0 §A 의 나머지 후보 11 은 **Q4 소비 경로 캡처**(factory style · `applyImplicitStyles` 후 `buildTreeBatch` 도달값) 로 전환 / 투영 예외 (대응 DOM box 명시) 를 분류한 뒤에만 전환 — Menu 는 `MenuTrigger > Button` inline-flex 대조로 값 무변경 (r4). `componentRulesTable.ts` 는 직접 편집 정본(헤더 `:1-9`, 생성기 없음). **완료 2026-09-02** — Button · ToggleButton `inline-flex`. 후보 11 은 HC2 판정표 (evidence §5) 로 분류: 일치/예외 6 · **전환필요(후속) 5** (Skeleton · Slot · Avatar · StatusLight · TailSwatch — 아래 후속 항목).
- [x] `getElementDisplay` → `resolveDefaultDisplay` 배선, `INLINE_BLOCK_TAGS` 삭제. `getElementDisplay` 출력이 Button 류에서 `inline-block`→`inline-flex` 로 바뀌는 것이 **의도된 diff** — 목록으로 고정. **완료 2026-09-02** — 의도된 diff 18 을 G5 가 값으로 고정 (evidence §2), 측정 소비처 4 는 `INTRINSIC_MEASURE_TAGS`.
- [x] S9 (r2 m1): `fullTreeLayout.ts:1060-1061` flex 분기와 `:1077-1084` grid 분기가 record 의 `display` 를 CSS 값 그대로(`inline-flex`/`inline-grid` 보존) 넣도록; `TaffyFlexEngine.ts:110-117` 의 `inline-flex → "flex"` 정규화 삭제 (flex 컨텍스트 기본값 `flex` 는 유지); `taffyDisplayAdapter.ts:510-512` inner-only 반환 삭제; (r6) `fullTreeLayout.ts` `toBatchDisplay` (post-order patch union 강제) 삭제 + `tests/parity/seamDisplayInvariant.browser.test.ts` 삭제/반전. outer/inner 해석은 Phase 1 의 `display.rs` 가 맡는다. **운반 타입 (r3m1)**: `wasm-bindings/layoutTypes.ts:11 TaffyDisplay` 를 CSS display union(`block | inline-block | flex | inline-flex | grid | inline-grid | inline | none`) 으로, `taffyDisplayAdapter.ts:124 TaffyDisplayConfig.taffyDisplay` 를 같은 union 으로 확장 (CSS 값 통과 계약). `TaffyBlockEngine.ts:117-118` 대입은 그대로. focused type-check 1건 포함. **완료 2026-09-02** — `TaffyDisplay` CSS 8 값 union · `TaffyFlexEngine` 보존 · grid 분기 `normalizeCssDisplay` · `toBatchDisplay` → `normalizeCssDisplay` (손실 없는 정규화) · seam 테스트 반전 (inline-flex 가 경계에 닿는다).
- [x] `toTaffyDisplay`: block 부모는 자식과 무관하게 `{ taffyDisplay: "block" }`. `INLINE_BLOCK_PARENT_CONFIG`, `INLINE_BLOCK_LEAF_CONFIG`, `resolveInlineBlockAlignItems`, `isInlineBlockSimulationParent`, `needsBlockChildFullWidth` 삭제. 자식 display 는 CSS 값 그대로 전달. **완료 2026-09-02** — 심볼 5 + `classifyChildDisplay` · `blockifyDisplay` · `displayToString` 삭제, `toTaffyDisplay(display, childDisplays)` = `{ taffyDisplay: normalizeCssDisplay(display) }` (childDisplays 는 HC1 관측 인자).
- [x] `fullTreeLayout.ts` §5.5 width:100% 보정 삭제, `:1154-1156` 주석 재작성, TS `blockifyDisplay` 호출(`:1796`) 은 Phase 1 의 Rust blockify 로 대체돼 삭제. **완료 2026-09-02** — §5.5 삭제 (주석 기록), block 경로 주석 재작성, TS blockify 블록 + `childDisplays` blockify 삭제.
- [x] Phase 3 케이스를 **프로덕션 경로**로 재실행 → 통과 (G1 후반). HC1 테스트 `it.fails` → pass 전환. G0 선행 검사 (a)(b)(c) pass 전환. **완료 2026-09-02** — ChromeDifferential 69 케이스 `pipelineLeg` 게이트 69/69 · HC1 `it` PASS · G0 (a)(b)(c) PASS.
- [x] G4 후반: HC2 미판정 0 (전환 2 + Q4 분류 결과가 전부 전환 또는 대응 box 명시, DOM 충돌 3·미선언 17 판정 포함) · 패널 테스트 재통과 · `blockInlineProbe` 로 block 부모 + Button 2 가 한 줄. **완료 2026-09-02** — HC2 33/33 판정 (`adr923Hc2DisplayJudgment`: 전환 2 · 일치 6 · 일치(outer) 13 · 예외 6 · 전환필요(후속) 5 · 투영필요(후속) 1 — round 31 r31m1 로 FieldError 를 같은 상태 짝 (기본 · invalid) 으로 양쪽 캡처해 재판정, 테스트가 7 범주 분포 전체를 단언) · 패널 테스트 재통과 (`specPresetResolver` Button preset inline-flex) · `blockInlineProbe` block+Button Δ0.
- [x] ADR-198: `crossLeg.browser.test.ts` `KNOWN_LAYERS["catalog-state-paint"]` 를 **결과로만** 갱신, `blockInlineProbe` 4 변형 두 leg 일치. 예산 무변경. **완료 2026-09-02** — 결과로 갱신 = 층 판정 무변경 (`L1:fail` 잔여는 Button 폭 Δ2.66/2.80 텍스트 측정; 위치 발산 0). probe 4 변형 두 leg 일치.
- [x] 성능 (G3, r4): cutover 직전 commit 과 cutover commit 을 교대로 fresh-browser N=3 쌍 측정 — `VITE_ADR923_BASELINE=1 VITE_ADR923_BASELINE_TAG=<before|after>-<sha>-<i>` 로 run 마다 고유 raw 보존 — arm F/B 각각 median(after_p95ᵢ / before_p95ᵢ) ≤ 1.05. N 은 결과 확인 전 고정, 이후 run 추가 금지. **완료 2026-09-02** — N=3 교대: median(after_p95/before_p95) F **1.049** · B **1.025** (≤ 1.05); p50 +5~7% 기록 (evidence §6).
- [x] 회귀: builder/shared/specs vitest + cargo test + `pnpm gate:visual-parity` full. **완료 2026-09-02** — layout 469 · builder 1817 (round 32 정정: 범위 밖 신규 3 포함) · scene+styles 589 · shared 967 · specs 875 · cargo 371+15+10+11+1 · smoke 81 · full parity 1065 (기존 2 · skipped 2 — round 31·32 집계 정정) · type-check PASS.
- [x] 런타임 플래그 금지. 실패 시 이 commit 만 revert. (단일 commit 계약 — 실제 경계는 evidence §10: 다른 세션 sweep `e2b657b0d`·`a1fd5e8fc` 에 코드·테스트가 실림. revert 는 그 두 커밋 + 본 문서 커밋.)

#### Phase 5 후속 (판정 완료 · 실행 별도 — D3 commit 각각 Skia 시각 확인 동반)

- [ ] **HC2 전환필요 5** (evidence/923-phase5-cutover.md §5): Skeleton (catalog structure inline-flex ↔ Skeleton.css block) · Slot (잔존 spec — spec containerStyles 에 display 명시 등재, generated Slot.css archetype inline-flex 와 Canvas block 이 갈림) · Avatar · StatusLight (catalog structure inline-flex ↔ live flex) · TailSwatch (catalog inline-flex ↔ UA block). 각각 catalog/spec 값을 DOM 과 맞추고 `INTENDED_DIFF`/HC2 표 갱신.
- [ ] **가상화 collection 의 min-content floor** (evidence §4): production ListBox/GridList 는 행이 layout 트리 자식이 아니라 엔진 content-size suggestion 이 0 — overflow visible/clip + 제약 flex 부모에서 DOM(164) 과 갈린다. 스칼라 채널 또는 행 투영 판정 (ADR-923 밖).
- [ ] **FieldError 상태 투영** (evidence §5 같은 상태 짝, round 31 r31m1): Canvas 에 `isInvalid`/`errorMessage` → FieldError 가시성·텍스트 투영이 없어 invalid 상태에서 Canvas `display:none` ↔ DOM `span:block` 이 갈린다 (field 가족 5 — `FIELD_VISIBLE_CHILD_TAGS`). r21m1 TagGroup 슬롯 자식 (`isTagGroupSlotChildVisible`) 과 같은 형태로 parent 상태가 자식 가시성을 정하게 하고 텍스트는 parent `errorMessage` 를 투영 (propagation 규칙 추가). 동작 변경 — Skia 시각 확인 + HC2 `fieldErrorStates` 반전 동반 (ADR-923 display 어휘 밖, 상태 투영).

### Phase 6 — 명명 정리 + capability matrix seed + 문서

- [x] Taffy 식별자 8 파일(코드 7 + 파일명 5 합집합) 개명 (`taffyDisplayAdapter.ts → displayAdapter.ts`, `TaffyBlockEngine.ts → blockStyleAdapter.ts`, `TaffyStyle → EngineStyle`, `toTaffyDisplay → toEngineDisplay` …). 동작 무변경 commit. **완료 2026-09-03 `7f1cf963d`** — 파일 5 (+test) · 심볼 30종 (`layoutTypes` 타입 17 · `EngineDisplayConfig.engineDisplay` · `PersistentLayoutTree` · `elementToEngineStyle/BlockStyle` · `applyCommonEngineStyle` · `engineStyleToRecord` · 지역/필드) · 41 파일 (builder src/tests 31 · scripts 1 · Rust doc 4 · .claude 5). 개명 지도 evidence/923-phase6-naming-capability-seed.md §1. 검증 type-check · layout 469 · builder 3772 · focused 122 · cargo.
- [x] 헤더 재작성: "시뮬레이션 규칙" 절 삭제 → "번역 규칙 + display 이원 계약(`display.rs`) 참조". **완료** — `displayAdapter.ts` 는 round 31 (`91c2c25e6`) 에서 운반 레이어 + 번역 규칙 3 으로, 개명 파일 4 머리말은 6a 에서 "style 어댑터 (값 변환만, 계산은 Rust 엔진)" 으로 재작성 (`blockStyleAdapter` 의 종전 IFC 시뮬레이션 서술 삭제, `layoutTypes.ts` "Taffy 접두 유지" 문구 정정).
- [x] capability matrix **seed** (`apps/builder/src/builder/workspace/canvas/layout/engines/layoutCapabilityMatrix.ts`): 행 = `{ property, value, engineSupport: "native"|"partial"|"none", policy: "pass"|"declared-substitution"|"ignored", oracle?: string }`. §2.2 의 S4/S7/S8 을 `ignored`/`declared-substitution` 으로 **선언만**, 각 항목의 현재 Chrome 격차 수치를 1 케이스씩 기록. 집행(게이트 테스트·정규화) 은 §8. **완료 2026-09-03 `b48978fc1`** — 3 행 선언 + `adr923CapabilityMatrixSeed.browser.test.ts` 실측 고정: S4 `display:inline` gap {41.9, 19, 259.2, 2} · S7 `float:left` ignored {0, 20, 0, 0} · S8 `grid-template-columns: subgrid` declared-substitution. **Codex round 33 정정 (2026-09-03)**: 첫 S8 fixture 의 `gridColumn: "span 2"` shorthand 는 production 이 운반하지 않아 (longhand `gridColumnStart/End` 만) 두 leg 이 다른 이유로 같아졌다 — dense "Δ0 = 구현" 은 그 우연이었고 §2.2 S8 "미구현" 이 맞다. longhand 격리 후 subgrid {100, 20, 100, 0} · dense oracle {100, 40, 0, 0} (S8 oracle 2) · S4 policy `declared-substitution` (값이 경계에 실려 엔진이 block-level 로 치환 — `ignored` 는 키 자체가 없는 float) · 범주를 경계 기준으로 정의 · 테스트는 행별 정확값 + `buildTreeBatch` 인자 경계 대조. 원복 RED (h)~(l) 5. evidence §2·§6.
- [x] `docs/CHANGELOG.md` (사용자-가시: block 컨테이너 안 inline-level 배치가 Chrome 과 일치, Button Direction 표시 정정), ADR-198 breakdown catalog 발산 종결 표기, ADR-916 완료 문서에 "명명 잔재 ADR-923 정리" 각주. **완료 2026-09-03** — CHANGELOG 2건은 Phase 5 엔트리 (`75d134e1f`) 에 이미 있음 (Phase 6 은 동작 무변경 면제, 승격 때 덧붙임) · ADR-198 breakdown `catalog-state-paint` L1 절 종결 표기 · ADR-916 각주.
- [ ] `### Live Exercise` → Implemented 승격. (6d — Codex round 33 통과 후: 개명 후 main 에서 G6 시나리오 Chrome MCP 재실행 → 본문 기재 → Implemented + closure 5단계.)

## 4. 파일 변경표

| 파일                                                                                   |    Phase    | 변경                                                                                                                                                                                                                                                               |
| -------------------------------------------------------------------------------------- | :---------: | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `apps/builder/tests/parity/displayContract.browser.test.ts` (신규)                     |    0·3·5    | 선행 검사 1케이스(it.fails → pass), 차등 케이스 ≥12                                                                                                                                                                                                                |
| `scripts/adr-923/scan-block-inline.mjs` (신규)                                         |      0      | BC 정량                                                                                                                                                                                                                                                            |
| `packages/composition-engine/src/tree.rs`                                              |     1·2     | 1: `display.rs` 배선(4곳)·blockify · 2(완료): `NodeLayout.baseline`+센티널, 입력 3종, solver baseline 전파, stride 5                                                                                                                                               |
| `packages/composition-engine/src/display.rs`                                           |      1      | `blockify(Display)` · `is_atomic_inline_level` 추가, `classify_child_display`/`ChildDisplayClass` 삭제, 모듈 doc 배선 사실 갱신 (r6l2 정정)                                                                                                                        |
| `packages/composition-engine/src/block.rs`                                             |      2      | (완료) 자식 baseline 소비 (센티널→border-box bottom 해소), trailing meta 3번째 `lastLineBaseline` 출력                                                                                                                                                             |
| `packages/composition-engine/src/{flex,grid}.rs`                                       |      2      | (완료 — **무변경**) 컨테이너 baseline 은 tree.rs placement 루프가 산출 (커널 계약 무변경, P2 편차)                                                                                                                                                                 |
| `packages/composition-engine/src/{cascade,style,wasm}.rs`                              |      2      | (완료) wasm.rs 만 변경 — stride 5·`getLayout` baseline. `NodeStyle` 3필드 선언은 tree.rs (P2 편차 — NodeStyle 실거주지)                                                                                                                                            |
| `apps/builder/src/builder/workspace/canvas/layout/engines/TaffyBlockEngine.ts`         |    2·5·6    | 2(완료 — **무변경**, 편차: 입력 통과는 `applyCommonTaffyStyle`+`taffyStyleToRecord` 3-브랜치 choke point); 5: leaf config 제거; 6: 개명                                                                                                                            |
| `packages/shared/src/catalog/generated/componentRulesTable.ts` (직접 편집 정본)        |      5      | Button `:927`·ToggleButton `:13108` 등 Canvas 전용 display override 제거 (DOM 값) — cutover 와 같은 commit                                                                                                                                                         |
| `apps/builder/src/builder/workspace/canvas/layout/engines/TaffyFlexEngine.ts`          |     5·6     | `:110-117` inline-flex 정규화 삭제 (S9); 개명                                                                                                                                                                                                                      |
| `apps/builder/src/builder/workspace/canvas/wasm-bindings/layoutTypes.ts`               |     5·6     | `:11 TaffyDisplay` union → CSS display 값 (S9, r3m1); 개명                                                                                                                                                                                                         |
| `apps/builder/src/builder/panels/styles/hooks/{useLayoutAuxiliary,useLayoutValues}.ts` |      4      | `:71`·`:99` inline-flex 를 flex 로 판정 (동작 무변경 — catalog 는 Phase 5)                                                                                                                                                                                         |
| `apps/builder/src/builder/workspace/canvas/layout/engines/utils.ts`                    |     4·5     | 4: `INLINE_BLOCK_TAG_CLASSIFICATION` + `INTRINSIC_MEASURE_TAGS` 추가, `needsWidth` 소비 교체 (완료) · 5: `getElementDisplay` 배선, `INLINE_BLOCK_TAGS` 삭제                                                                                                        |
| `apps/builder/src/builder/workspace/canvas/layout/engines/defaultDisplay.ts` (신규)    |     4·5     | 4: `resolveDefaultDisplay(type)` (미배선, utils 와의 순환 import 회피로 별도 모듈 — 완료) · 5: `getElementDisplay` 가 이것을 배선                                                                                                                                  |
| `…/engines/fullTreeLayout.ts`                                                          | 1(r6)·2·4·5 | 1(r6): `toBatchDisplay` post-order patch union 강제 (Phase 5 제거) · 2(완료): patch 루프 lineHeight px coerce (r6h1-재발 수리) + `taffyStyleToRecord` 3필드 통과 · 4: HC1 테스트 대상 · 5: `:1060,1084` 분기 CSS 값 보존, §5.5·blockify 호출·`toBatchDisplay` 삭제 |
| `apps/builder/tests/parity/adr923ProductionTrees.ts` (신규, r29)                       |     4·5     | production 생성 형태 (팔레트 × creation facet, origin seed + ref 해석, `createComplexComponent`) — DC-6 · Q4 입력 SSOT; Phase 5 Chrome 케이스도 이 트리로                                                                                                          |
| `apps/builder/tests/parity/adr923CalendarGridQ4.browser.test.ts` (신규, r29)           |     4·5     | calendargrid Q4 — production 트리 inert · 자유 배치 엔진 직결/현 어댑터 대조 · DOM table+flex (Phase 5 전환 판정 근거)                                                                                                                                             |
| `apps/builder/tests/parity/seamDisplayInvariant.browser.test.ts` (신규, r6)            |     1·5     | HC7 seam 불변식 — wasm 경계 display ∈ `TaffyDisplay` union (Phase 5 삭제/반전)                                                                                                                                                                                     |
| `apps/builder/tests/parity/seamBaselineContract.browser.test.ts` (신규, P2)            |      2      | baseline 입력/출력 경계 왕복 + lineHeight **두 writer**(buildTreeBatch·updateStyleRaw) 캡처 가드                                                                                                                                                                   |
| `…/engines/taffyDisplayAdapter.ts`                                                     |     5·6     | IFC 시뮬레이션 제거 + `:510-512` inner-only 반환 삭제, `:124` union 확장 → 번역만; 개명                                                                                                                                                                            |
| `…/engines/layoutCapabilityMatrix.ts` (신규)                                           |      6      | seed 선언                                                                                                                                                                                                                                                          |
| `apps/builder/tests/visual-parity/compare/{crossLeg,blockInlineProbe}.browser.test.ts` |      5      | ratchet 결과 갱신                                                                                                                                                                                                                                                  |
| `docs/CHANGELOG.md`, ADR-198 breakdown, ADR-916 완료 문서                              |      6      | 문서                                                                                                                                                                                                                                                               |

## 5. 검증 계획

| 검증                    | 도구                                                         | 기준                                                                        |
| ----------------------- | ------------------------------------------------------------ | --------------------------------------------------------------------------- |
| 전제 (G0)               | 선행 검사 1케이스, wasm 경계 캡처                            | 현재 실패 확증 → Phase 5 후 통과                                            |
| 엔진 기하 ≡ Chrome (G1) | ADR-156 하니스, `@vitest/browser` Chromium                   | ≥12 + baseline 케이스, ≤1px, 허용치 무변경, (전)/(후) 나란히                |
| 두 leg 픽셀 (G2)        | `pnpm gate:visual-parity` full                               | catalog-state-paint L1 pass, 예산 무변경                                    |
| 성능 (G3)               | `adr923LayoutBaseline` before/after 교대 N=3 쌍 (r4)         | arm F/B median(after_p95ᵢ / before_p95ᵢ) ≤ 1.05                             |
| catalog·패널 (G4)       | 패널 단위 테스트 + HC2 판정표 (Q4 캡처) + `blockInlineProbe` | (4) Direction·Alignment inline-flex 표시 · (5) HC2 미판정 0, Button 2 한 줄 |
| 분리 (G5)               | `enrichWithIntrinsicSize` diff, 분류표                       | diff 0, 24 항목 전부 분류                                                   |
| display 단일 표현 (HC1) | `fullTreeLayout` 단위 테스트                                 | `childDisplays[i]` == 전달 display                                          |
| 회귀                    | builder/shared/specs vitest + cargo                          | 전량 PASS, fixture 변경 금지                                                |
| live (G6)               | 실제 빌더 Chrome MCP                                         | block 컨테이너 + Button 2 + 폭 명시 div: Canvas·Preview·패널 일치           |

## 6. Phase 0 측정 결과 (착수 시 채움)

- 선행 검사 (a)/(b)/(c) 현재 값 (2026-08-31, 실행 Codex gpt-5.6-sol `07c724a37`, 독립 재실행·raw 판독 Claude): `buildTreeBatch` JSON 1회 호출, 노드 3. **(a) Button display = `"flex"`** (기대 `inline-flex` — 실패, 사실 4·10 확증) · **(b) parent display = `"flex"`** + `flexWrap:"wrap"` + `alignContent:"flex-start"` (= `INLINE_BLOCK_PARENT_CONFIG` 서명, 기대 `block` — 실패, 사실 3 확증) · **(c) Button inner=flex = true**. `it.fails` 로 고정, Phase 5 후 `it` 전환. raw: `apps/builder/tests/parity/.artifacts/adr-923-g0-capture.json` (untracked). Button 경계 style 에 `width:36px height:30px minWidth:68px padding 4/12 border 1 gap 8` — catalog 값이 도달함(경로 자체는 살아 있음, display 만 정규화됨).
- Canvas 전용 display override 표 (2026-08-31, Claude `e6d08237d` · Codex 판독 round 4 반영): [evidence/923-phase0-inventory.md](../evidence/923-phase0-inventory.md) §A — rule **123** 중 top-level display 16. **정확값 불일치 13** = (i) 2 Button `:927`·ToggleButton `:13108` (flex vs DOM inline-flex) · (iii) 1 Menu `:6775` (inline-flex vs DOM flex — ADR-151 B7 의도된 D1 차이, HC2 예외 판정 필요) · (ii-s) 2 structure≠DOM (FileTrigger `:5364` inline-block vs inline-flex · Skeleton `:10405` inline-flex vs `Skeleton.css:65` block) · (ii-g) 8 catalog display 없음 → Canvas 기본값≠DOM (ColorPicker·ColorSlider·ColorSwatchPicker·GridList·Label·Slot·Tag·TagList `contents`). **outer(line box 참여) 차이 6** (Button·ToggleButton·Menu·Skeleton·Label·Slot) / inner 만 7. 추가 판정 대기: DOM 충돌 3 (Checkbox·Radio·SliderOutput — live 로만 확정) · DOM 미선언 17 (generated 27 파일 dead 원인) · 둘 다 없음 30 · 일치 60 (Canvas 는 ADR-171 Phase 3 이후 structure 층을 읽으므로 top-level 불요 — `resolveDefaultDisplay` 는 `resolveContainerStylesFallback` 과 같은 precedence). catalog inline-\* 선언 30 (ADR 사실 7 그대로). 초판의 (ii-a)/(ii-b) 분류(outer 기준) 는 r4m1 로 정확값 기준으로 대체. **Codex 판정 (r4 개정, reviews round 5)**: 13 은 Phase 5 전환 목록이 아니라 **Q4 판정 후보** — production 경로는 factory style · `applyImplicitStyles` 가 catalog fallback 보다 우선 (`fullTreeLayout.ts:1658`, `implicitStyles.ts:335`): ColorPicker · ColorSwatchPicker 는 factory `display:"flex"` (`DateColorComponents.ts:587` · `:664`), GridList owner 는 `implicitStyles` gridlist 분기가 의도적으로 flex-column (`componentRulesTable.ts:5557` 주석), TagList 는 Canvas transparent projection shell 과 DOM `display:contents` 가 같은 box 가 아니다 (`TagList.binding.ts:14`). 확정 전환 **Button · ToggleButton 2** · Menu 는 `MenuTrigger > Button` inline-flex 대조 (popover `.react-aria-Menu` 제외, 값 예외 아님) · 나머지 10 은 Q4 캡처 후 전환 / 투영 예외 분류. DOM 충돌 3 · 미선언 17 은 G4 unresolved 0 전까지 통과로 세지 않는다.
- `INLINE_BLOCK_TAGS` 24 분류표: [evidence/923-phase0-inventory.md](../evidence/923-phase0-inventory.md) §B — 실제 24 (ADR 일치). **A 0 · B 6** (progresscircle·togglebuttongroup·toolbar·disclosureheader·calendarheader·calendargrid — DOM/catalog 가 block-level 인데 부모가 inline-block 으로 봄, 측정 목록에만 남겨야 함) · **AB 11** (button·togglebutton·badge·checkbox·radio·switch·statuslight·link·breadcrumb·icon·tab) · **? 7** (submitbutton·fancybutton·type·chip·linkbutton — catalog rule 없음·생성 참조 0 / menu — B7 / dateinput — catalog display 없음). R5 선례 = `073751610` (2026-06-08) needsWidth 게이트 `utils.ts:4570-4572` → `calculateContentWidth` 미호출 → width 0. G5: ? 5건은 손 목록 유지 + 사유 기록.
- 영향 % / 영향 문서 수 / factory 11곳 자식 분류: [evidence/923-phase0-inventory.md](../evidence/923-phase0-inventory.md) §C — factory 11곳 전부 leaf(자식 0) → "block 컨테이너 + inline-level 자식" 조합 **0**; 51 정의 전수 instantiate 도 0 (현행·C′ 규칙). 스캐너 `scripts/adr-923/scan-block-inline.mjs` (상수 추출 123/123 일치) fixture smoke: 2 노드 · block 컨테이너 1 · 영향 0. **실 문서 분포 미측정** — `apps/builder/tests/parity/.artifacts/projects/*.json` 사용자 export 후 같은 명령으로 채움. "평균 재직렬화 파일 수" 는 persisted 변경 0 (HC7) 이라 무의미 → 영향 컨테이너 %·영향 문서 수로 대체. 실제 조합은 factory 가 아니라 사용자가 body/frame 에 Button 류를 놓을 때 생긴다 (G0 케이스).
- p50/p95 baseline (flex 위주 / block arm): [evidence/923-phase0-inventory.md](../evidence/923-phase0-inventory.md) §D — `adr923LayoutBaseline.browser.test.ts` (기본 skip, `VITE_ADR923_BASELINE=1 pnpm --filter @composition/builder exec vitest run --config vitest.browser.config.ts tests/parity/adr923LayoutBaseline.browser.test.ts`). **arm F** (root flex-column → 8 row(wrap) → 8 column → 8 row(wrap) → 9 box 40×20, 5,193 노드) **p50 21.0 / p95 23.9 ms** · **arm B** (root block → 100 block → 49 catalog Button, 5,001 노드) **p50 90.2 / p95 97.2 ms**. HeadlessChrome 151 · Apple M4 Pro · DPR 1 · `resetPersistentTree` 후 full build, warm-up 5 + 30. G3 = 같은 규칙 재측정 p95 ≤ +5% (F 25.1 / B 102.1 ms). raw `.artifacts/adr-923-layout-baseline.json` (untracked). **run 편차 (r4l3)**: raw 는 실행마다 덮어써진다 — 3 run p95 F 23.9–25.3 · B 97.2–104.7 (+5.9% / +7.7%) 로 G3 의 +5% 보다 크다 → **판정 (r4, Codex round 5)**: 단일 run 폐기, G3 = 고정 N=3 before/after 교대 paired ratio 중앙값 ≤ 1.05. 3 run 단순 중앙값 F 24.9 / B 98.0 ms 는 실행 조건 (단독 실행 · `test:parity` 전체 실행 · Codex 환경) 이 섞여 공식 baseline 으로 쓰지 않는다 — Phase 0 값은 fixture·하니스 확정용 참고치. 하니스는 `VITE_ADR923_BASELINE_TAG` 로 run 마다 고유 raw 파일을 남긴다.
- pencil import display 경로: [evidence/923-phase0-inventory.md](../evidence/923-phase0-inventory.md) §E — (1) `pencil-adapter.types.ts:258-268 collectPencilProps` 가 pen 키를 props 로 그대로 복사, `.pen` 속성 → `style.display` 파생 규칙 0 (pen 에 `style.display` 가 있으면 검증 없이 통과). (2) `importPencilDocument` production caller 0; 실경로 `importPayloadAdapter.ts:32` ← `importRegistry.ts:144` fetch → `loaded` 캐시(`:99`, 비영속) → `storeBridge.ts:108` ref 해석 — hydration chain ✗, store ✗. builder 에 `.pen` 열기 UI 없음. (3) frame(catalog display 없음 → block) + `icon_font`→Icon / `metadata.compositionType` Button → 조합 유입 가능, 단 read-only ref 해석용.

## 7. 롤백

- Phase 5 단일 commit revert. Phase 1·2 는 seam 무변경(1) / 계약 추가(2) 라 revert 시 TS 통과 코드만 동반 revert. Phase 4 는 동작 무변경(패널 허용·목록 분리·미배선 함수·테스트) 이라 단독 revert 가능. catalog 전환·`getElementDisplay` 배선·S9 는 Phase 5 commit 안에 있어 함께 revert.
- persisted 문서 변경 0 → 데이터 롤백 불필요.

## 8. 별도 결정으로 넘기는 것 (B 갈래 — 사용자 결정 2026-08-31)

본 ADR 에서 제외. 후속 결정이 갖춰야 할 요건을 r1 실측으로 기록한다:

- **S4 순수 `display:inline` 요소** — 정규화하려면 (a) store 쓰기 경로(`updateSelectedStyle`/`updateElementProps`) 를 chokepoint 로 삼아 세션 중 유입을 막고, (b) hydration persist-back 은 **원값 보존**(예: `style.__authoredDisplay`) 없이는 금지, (c) 패널 `DISPLAY_OPTIONS` 노출 조정을 같이. 그 전까지는 capability matrix 에 `ignored` (block 격상) 로 선언.
- **S7 float/clear/writing-mode/다단** — 패널 노출 없음 확인, import strip 여부 결정. `ignored` 선언 + Chrome 격차 1 케이스.
- **S8 grid 미구현 4종** — `declared-substitution` + 차등 케이스로 수치 고정. 구현은 별도 ADR.
- **capability matrix 집행** — matrix 밖 property 무시가 발생하면 게이트 실패로 만드는 테스트. seed 는 본 ADR Phase 6.
- 완전한 IFC(대안 D), ADR-198 Phase 6 매트릭스 — 각각 별도.
