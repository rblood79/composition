---
adr: 161
title: GridList ref 기반 재사용 composite 전환
reviews:
  - round: 1
    ts: '2026-07-23T08:18:35.482Z'
    reviewer: claude
    source: live
    issues:
      - id: l1
        severity: LOW
        category: other
        summary: >-
          breakdown Phase 1 컨테이너 origin 기본값(selectionMode:single/columns
          생략/style 생략)이 현행 factory 기본값(selectionMode:none/columns:2/width:100%)과
          상이 — Phase 2 instance override 로 이전 필수
        evidence: >-
          design/161-...breakdown.md §2 Phase 1 vs
          SelectionComponents.ts:330-337
        root_cause: >-
          컨테이너 origin 리터럴을 ListBox createListBoxOrigin(:185-189
          selectionMode:single) 에서 복사 — GridList 현행
          default(none/2col/full-width)와 불일치. R4/G4 가 override 보존 검증하나 이 구체 인스턴스
          미명시
        outcome: deferred
      - id: l2
        severity: LOW
        category: other
        summary: 'breakdown §0 createListBoxOrigin 라인 인용 :178 -> 실제 :179 (off-by-one)'
        evidence: 'design/161-...breakdown.md:11 vs listBoxTemplateOrigins.ts:179'
        root_cause: 인용 라인 off-by-one
        outcome: fixed
        addressed_in: review round 1 breakdown edit
      - id: l3
        severity: LOW
        category: other
        summary: >-
          R5 심각도 LOW vs ADR-148 R9 MED — component-gridlist publish 미소비를 scoped
          subset 으로 LOW 처리
        evidence: 'ADR-161:96 R5 vs 148-reusable-slot-system-unification.md:113 R9'
        root_cause: >-
          R5 는 component-gridlist origin 한정 subset + ADR-148 R9 후속 명시 위임이라 상위
          R9(MED) 부분집합. 의도된 scoping
        outcome: rejected
---
# ADR-161 Review Log

## Round 1 — 2026-07-23 (reviewer: claude)

## ADR 리뷰 결과: ADR-161 GridList ref 기반 재사용 composite 전환

### 구조 검증

7/7 섹션 PASS(HIGH 품질). Context 4 hard constraint(BC 무손실/76px parity/projection 무회귀/60fps, :15-20), Alternatives 3개 4축(:29-57), Risk Threshold Check + Phase 분리 검토(:61-69), Decision 위험수용 3+기각 2(:71-86), 구현상세 breakdown 분리(:86), Risks R1-R5(:88-96), Gates G1-G4 R1-R4 1:1(:98-105).

### 코드 검증 — 11/11 VERIFIED

ListBox ref(SelectionComponents.ts:249-250), GridList standalone(:327-338), canvasSceneNode ref-master(:751-752), appendGridListRowProjection(:1308), GRIDLIST_SYSTEM_ORIGIN_IDS 1원소(gridListTemplateOrigins.ts:51), LISTBOX 3원소(:67-71), preview master slot(App.tsx:263), legacyListBoxTemplateMigration.ts 존재, 커밋 3개(2818c6bf0/c51e0d1d2/9c28eef6f) 일치, ADR-148 R9(148:113), sceneVersion signature ref 포함(buildSceneSnapshot.ts:63).

### 위험 평가 — 누락 0건

R1(HIGH,G1)/R2(HIGH,G2)/R3(MED,G3)/R4(MED,G4)/R5(LOW) 모두 기재. layoutVersion 5-심볼 체인은 layout prop 기존처리(9c28eef6f), ref 전환이 무효화 경로 변경 안 함(R4 override 보존 커버). sceneVersion signature ref 이미 추적 -> R1 일부 선완화.

### 종합 판정

- 구조: PASS (7/7 HIGH)
- 코드 정합: 11/11 VERIFIED
- 누락 위험: 0건
- 권고: HIGH 없음 / MED 없음 / LOW 3건(전부 종결: deferred override-default, fixed line-cite, rejected R5-severity)
- 결론: 승인 가능

HIGH/MED pending 0 -> 전제 확정(종결 계약). Phase 4(Skia)/Phase 5(migration) HIGH 는 execute-adr auto 에서도 surface.
