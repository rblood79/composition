---
adr: 172
title: 팬 경로 파생 비용 제거 — 카메라 이동과 파생 계층 분리
reviews:
  - round: 1
    ts: '2026-07-29T13:54:44.847Z'
    reviewer: claude
    source: live
    issues:
      - id: h1
        severity: HIGH
        category: evidence-missing
        summary: >-
          Phase 1 단독으로 layoutPublisherInputs identity 안정화 불가 — deps 의
          sceneSnapshot(:524)/visiblePages(:517) 가 카메라 결합 스냅샷(:478,:482 deps)
          산출물이라 Phase 3 전까지 팬마다 identity 파괴. breakdown §2 검증·§8 'Phase 1 → 2 순서만
          강제'·대안 A ① 서술이 코드와 불일치
        evidence: >-
          apps/builder/src/builder/workspace/canvas/BuilderCanvas.tsx:466-483,489,491-495,516-527
        root_cause: >-
          layoutPublisherInputs 의 deps 전수를 확인하지 않고 직접 카메라 deps(panOffset/zoom)
          2개만으로 identity 결합을 판정
        outcome: fixed
      - id: m2
        severity: MEDIUM
        category: other
        summary: >-
          §4-1 분리선이 visibility 산출물을 pageSnapshots[].isVisible 로 축소 서술 — 실제 카메라
          의존 표면은 document.visible* 4필드 + sceneVersion(visible* 입력, :228-238) +
          viewportVersion(:240-248).
          sceneVersion→projectionVersion(rendererInput.ts:74)→layoutInputKey(:95)
          체인과 visibility 산출 identity 안정성 요구 미기재
        evidence: >-
          apps/builder/src/builder/workspace/canvas/scene/buildSceneSnapshot.ts:126-134,188-203,222-224,228-248
        root_cause: 분리 설계 시 buildSceneStructureSnapshot 반환 구조 전체가 아니라 pageSnapshots 루프만 검토
        outcome: fixed
---
# ADR-172 Review Log

## Round 1 — 2026-07-29 (reviewer: claude)

## ADR 리뷰 결과: ADR-172 팬 경로 파생 비용 제거 (round 1)

### 구조 검증

| 섹션 | 판정 | 품질 | 인용 |
| --- | --- | --- | --- |
| Context (hard constraint 4) | PASS | HIGH | ADR:61-66 |
| Alternatives (4개) | PASS | HIGH | ADR:74-114 |
| 4축 위험 평가 | PASS | HIGH | ADR:80-84,90-94,100-104,110-114 |
| Risk Threshold Check | PASS | HIGH | ADR:116-125 (루프 판정 명시) |
| Decision 위험 수용 근거 + 기각 사유 | PASS | HIGH | ADR:127-143 |
| Risks (Decision 뒤/Gates 앞, ID 표 R1~R7) | PASS | HIGH | ADR:147-159 |
| Gates (G1~G5) | PASS | HIGH | ADR:161-169 |

동적 seed: #1 HIGH 위험 코드 경로 3곳+ 인용 PASS (useLayoutPublisher.ts:82-85/:82-97 + BuilderCanvas.tsx:516-527) / #2 Generator N/A (builder-system layer, ADR:11) / #3 BC N/A (마이그레이션 LOW, 저장 데이터 무관 ADR:84) / #4 Phase 분리 질문 PASS (ADR:159) / #5 gap-fork N/A.

### 코드 검증 — 16/16 VERIFIED

주요: rAF 배칭 useViewportControl.ts:346-360 / layoutInputKey 비-memo useLayoutPublisher.ts:86-97 + 2-commit 주석 :82-85 / panOffset·zoom 선언 rendererInput.ts:28,30 + new Map :67 + 소비 0건 (grep hooks/+renderers/) / commandChildrenMap 재구축 skiaFramePipeline.ts:259-274 + 반환 :335 + executeRenderCommands 재래스터 한정 :318-330 + selfSpans :327 / SkiaCanvas.tsx:664-678 무게이트 호출 / 5중 키 캐시 renderCommands.ts:346-359 / replay skipDepth :967-1013 / buildVisiblePageSet 카메라 소비 buildSceneSnapshot.ts:126-131 + isVisible :179 / panOffset 구독 BuilderCanvas.tsx:328 + snapshot deps :478,:482 / frame 경로 :585 / perfMarks.ts·useGPUProfiler·buildFrameRendererInput.test.ts 실존 / 2026-07-27 프레임 분해 인용은 메모리 정본과 일치.

### 발견 이슈 (리뷰 중 정정 완료)

1. **HIGH — Phase 전제/순서 서술 결함**: layoutPublisherInputs deps(BuilderCanvas.tsx:516-527)에 sceneSnapshot(:524)·visiblePages(:517)가 있고 둘 다 카메라 결합 스냅샷(:466-483 deps 에 panOffset:478/zoom:482) 산출물 → Phase 1 단독으로 identity 안정화 불가, breakdown §2 검증은 착수 즉시 FAIL 구조. **정정**: ADR 대안 A ①·R1 대응을 'Phase 1+3 결합' 으로, breakdown §2 검증·§3 전제·§8 순서를 'Phase 1·3 → 2 강제' 로 갱신.
2. **MEDIUM — visibility 산출물 표면 축소**: 카메라 의존 산출물이 isVisible 외에 document.{visiblePageIds,visiblePageFrames,visibleContentVersion,visiblePagePositionVersion}(buildSceneSnapshot.ts:126-134,188-203) + sceneVersion(:228-238, projectionVersion→layoutInputKey 체인) + viewportVersion(:240-248). **정정**: breakdown §4-1 에 표면 전수 + visibility 산출 identity 안정성 요구(CRITICAL) 명시.

### 종합 판정

- 구조: PASS (7/7 HIGH 품질)
- 코드 정합: 16/16 VERIFIED
- 누락 위험: 2건 (HIGH 1 / MED 1) — 리뷰 중 문서 정정으로 양건 종결 (outcome: fixed)
- 권고: HIGH 없음(정정 완료) / MEDIUM 없음(정정 완료) / LOW 없음
- 결론: **승인 가능** (정정 반영본 기준)
