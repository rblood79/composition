---
adr: 142
title: RAC primitive binding + canonical 문서 기반 컴포넌트 시스템 family 단위 cutover
reviews:
  - round: 1
    ts: '2026-05-19T03:50:28.585Z'
    reviewer: claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: other
        summary: >-
          catalog/ 를 packages/specs 에 배치 — canonical/theme 타입(shared)에 의존하나
          specs←shared boundary 상 의존 불가
        evidence: >-
          breakdown §2 line 35-40; packages/specs/package.json deps {colord};
          CanonicalNode/ResolvedNode in packages/shared/src/types/
        root_cause: §2 경계 규칙이 catalog↔legacy-specs 만 다루고 catalog↔shared 의존 방향을 미정의
        outcome: pending
      - id: l2
        severity: LOW
        category: adr-structure-violation
        summary: >-
          inspectorFields.test.ts 가 Phase 1 작업 #9 에 언급되나 §2 새 파일 표·Phase 1 검증
          커맨드에 미동기
        evidence: breakdown line 251 vs §2 line 32-44 / line 253
        root_cause: Inspector 보완 편집 시 신규 fixture 를 §2 표·검증 커맨드에 미반영
        outcome: pending
      - id: l3
        severity: LOW
        category: other
        summary: >-
          reusable propsSchema 저작 경로 — Phase 2 reusable 승격 tooling 이 propsSchema
          저작 포함 여부 미명시
        evidence: 'breakdown line 199, 262'
        root_cause: Inspector 편집 source 는 정의됐으나 reusable 측 저작 도구 서술 부재
        outcome: pending
      - id: l4
        severity: LOW
        category: other
        summary: ADR line 32 shared/components 51개 파일 — 실측 57
        evidence: >-
          ADR:32 vs grep packages/shared/src/components react-aria-components
          import = 57
        root_cause: imprecise/stale count (line 84 51개 이상 은 유효)
        outcome: pending
  - round: 2
    ts: '2026-05-19T10:52:27.276Z'
    reviewer: claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: other
        summary: generic 렌더러 Skia 렌더 성능 검증 게이트 부재 — Canvas 60fps hard constraint 미반영
        evidence: 'docs/adr/142-starter-spec-component-system-cutover.md:90,100,138-165'
        root_cause: >-
          대안 E 성능을 정의 수 감소(124->35 binding)로 LOW 단정. per-frame Skia 렌더
          비용(generic traversal + theme/data-* 해석이 specialized render.shapes()
          대체)은 별개. Risks 9건·Gate 8건·Hard Constraints 11개에 성능 항목 0건. Gate G2는
          DOM-Skia 시각 대칭만 보고 frame budget 미검증
        outcome: pending
      - id: m2
        severity: MEDIUM
        category: other
        summary: R8 status 재평가 목록에 ADR-140/141 누락
        evidence: 'docs/adr/142-starter-spec-component-system-cutover.md:147'
        root_cause: >-
          R8은 폐기되는 ADR-036 메커니즘의 재평가 대상으로 ADR-036/907/908만 명시.
          ADR-141(starter->Spec D3 반영+CSSGenerator
          compositionOwnsContainerBox)·ADR-140(press-scale
          states.pressed.scale+CSSGenerator emit)도 124
          ComponentSpec+CSSGenerator 응용 ADR — legacy 격리 시 재이관 대상인데 누락
        outcome: pending
      - id: l3
        severity: LOW
        category: adr-structure-violation
        summary: R5 BC 위험 수식화 부재 (adr-writing seed#3)
        evidence: 'docs/adr/142-starter-spec-component-system-cutover.md:144'
        root_cause: >-
          R5 legacy payload 깨짐이 BC 위험인데 X% 사용자 영향/Y 파일 재직렬화 수식 없이 정성 서술.
          import/export adapter로 영향 0 목표인 사실을 R5에 명시 권장
        outcome: pending
  - round: 3
    ts: '2026-05-19T13:59:39.808Z'
    reviewer: claude
    source: live
    issues:
      - id: l1
        severity: LOW
        category: other
        summary: >-
          ADR '55 컴포넌트'(.tsx 모듈) vs 변경된 design.md 부록 '컴포넌트 CSS (54)' — 둘 다 정확하나
          G1이 두 산출물을 한 Gate로 묶어 단위 차이 주석 부재
        evidence: >-
          ADR-142:34,160 / design.md 부록 / src 실측 55 .tsx + 54 component .css
          (ProgressCircle 무 CSS)
        root_cause: >-
          55는 컴포넌트 모듈 수, 54는 CSS 파일 수 — 서로 다른 단위. 어느 문서도 ProgressCircle만 CSS
          미보유라는 metric 차이를 명시하지 않음
        outcome: pending
      - id: l2
        severity: LOW
        category: other
        summary: >-
          변경된 design.md §7이 .claude/rules/css-tokens.md를 composition 시맨틱 정본으로
          격상했으나 ADR-142 본문은 미참조
        evidence: 'design.md:330-331 / ADR-142:34,116 (theme/tokens root collection만 언급)'
        root_cause: >-
          css-tokens.md(naming 규칙 layer)와 theme/tokens root collection(runtime 값
          SSOT)은 직교 — 모순은 아니나 ADR 단독 독자가 naming 권위를 추적 불가
        outcome: pending
---
# ADR-142 Review Log

## Round 1 — 2026-05-19 (reviewer: claude)

## ADR 리뷰 결과: ADR-142 RAC primitive binding + canonical 문서 기반 컴포넌트 시스템 family 단위 cutover

### 구조 검증

| 섹션                    | 판정 | 품질 | 인용                                                            |
| ----------------------- | ---- | ---- | --------------------------------------------------------------- |
| Context                 | PASS | HIGH | `ADR:39-51` — Hard Constraint 11개, 측정 가능                   |
| Alternatives (2개+)     | PASS | HIGH | `ADR:59-90` — 대안 A~E 5개                                      |
| 4축 위험 평가           | PASS | HIGH | `ADR:65,71,77,84,90` — 5개 전부 기술/성능/유지보수/마이그레이션 |
| Risk Threshold Check    | PASS | HIGH | `ADR:92-104` — 표 + HIGH+ 수용 근거                             |
| Decision 위험 수용 근거 | PASS | HIGH | `ADR:104,127-132` — 대안 A~D 기각 사유 명시                     |
| 구현 상세 분리          | PASS | HIGH | `ADR:134` — breakdown 포인터, 본문에 Phase/파일표 없음          |
| Risks                   | PASS | HIGH | `ADR:136-150` — R1~R9 ID 표, 잔존 HIGH=R1 1건                   |
| Gates                   | PASS | HIGH | `ADR:152-165` — G0~G7, G2 가 R1 1:1                             |

동적 seed: #1(HIGH+ 3곳 인용) — R1 이 4개 코드 경로 인용 ✓. #3(BC 수식화) — R5 가 "X% 사용자/Y 파일" 미수식 (LOW). #2/#4 — N/A.

### 코드 검증

| 주장                                                  | 판정     | 근거                                                                                   |
| ----------------------------------------------------- | -------- | -------------------------------------------------------------------------------------- |
| `resolveCanonicalDocument()` 존재                     | VERIFIED | `apps/builder/src/resolvers/canonical/index.ts:57`                                     |
| `CanonicalNodeRenderer.tsx` 존재                      | VERIFIED | 파일 존재                                                                              |
| `buildSpecNodeData.ts` render.shapes 경로             | VERIFIED | 파일 존재 + `.shapes(` 3건                                                             |
| react-aria-starter private / main·exports 없음        | VERIFIED | package.json: private:true / main:undefined / exports:none                             |
| starter 55 컴포넌트                                   | VERIFIED | `react-aria-starter/src/*.tsx` = 55                                                    |
| 124 ComponentSpec                                     | VERIFIED | `*.spec.ts` = 124                                                                      |
| `FieldDef` 11종 union                                 | VERIFIED | `spec.types.ts:448` — 11 member                                                        |
| properties 보유 83/124                                | VERIFIED | grep = 83. `GenericPropertyEditor`/`SpecField`/`ItemsManagerField` 실재                |
| App.tsx legacy+canonical 경로 공존                    | VERIFIED | `App.tsx:32,38,180,470` — `resolveCanonicalRefTree`(legacy)+`resolveCanonicalDocument` |
| shared/components react-aria-components import "51개" | PARTIAL  | 실측 57 — `ADR:32` "51개" 과소 (`ADR:84` "51개 이상" 은 유효)                          |

### 위험 평가

| 위험                                            | 증거                                                                                                                                                                   | 심각도                 | 상태     |
| ----------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------- | -------- |
| R1~R9 (ADR 나열)                                | `ADR:136-150`                                                                                                                                                          | HIGH×1 / MED×7 / LOW×1 | 기재됨   |
| catalog 패키지 배치 ↔ `specs ← shared` boundary | `packages/specs/package.json` deps `{colord}` 만 / `CanonicalNode`·`ResolvedNode` 는 `packages/shared/src/types/` / specs→shared import 0건 / shared→specs 역방향 존재 | MED                    | **누락** |

### 종합 판정

- 구조: PASS (8/8 섹션 HIGH 품질)
- 코드 정합: 10/11 VERIFIED, 1 PARTIAL (`ADR:32` count)
- 누락 위험: 1건 (MED 1)
- 권고:
  - **HIGH**: 없음
  - **MEDIUM**: catalog 패키지 배치 — breakdown §2 가 `catalog/`(types/bindings/outputs/componentCatalog/library)를 `packages/specs/src/catalog/` 에 둔다. 그러나 catalog 는 canonical document(`CompositionDocument`/`CanonicalNode`/`ResolvedNode`)와 theme 타입에 의존하고, 그 타입은 `packages/shared` 에 있다. `specs` 는 `@composition/shared` 를 의존하지 않으며 `specs ← shared` boundary 상 의존할 수 없다(역방향 shared→specs 만 존재). `toRacProps.ts`/`inspectorFields.ts`/`componentCatalog.ts`/`catalog/library/` 가 Phase 1 작업 #1 부터 이 경계에 부딪힌다. §2 경계 규칙은 catalog↔legacy-specs 만 다루고 catalog↔shared 는 미정의. → (a) catalog 를 `packages/shared` 또는 신규 패키지로 이전, (b) 전 catalog 파일을 구조적 타입으로 한정, (c) canonical 타입 위치 재배치 중 하나를 Phase 1 착수 전 §2 에 명시.
  - **LOW**: ① `inspectorFields.test.ts` 가 Phase 1 작업 #9 에 언급되나 §2 "새로 만들 파일" 표 + Phase 1 검증 커맨드(line 253)에 미동기. ② reusable `propsSchema` 저작 경로 — Phase 2 "reusable 승격 tooling" 이 `propsSchema` 저작을 포함하는지 미명시. ③ `ADR:32` "51개 파일" 실측 57.
  - **결론**: 구조·결정 **승인 가능** — Decision 14항 / Risk R1~R9 / Gate G0~G7 정합. Inspector 4개 보완(Decision #14 / PropContract / G2 / Phase 1 #8)도 본문↔breakdown 정합. 단 MEDIUM 1건(catalog 패키지 배치)은 Phase 1 착수 전 breakdown §2 에서 해소 권장 — Decision 변경 불요, 파일 경계 정정 수준.

## Round 2 — 2026-05-19 (reviewer: claude)

## ADR 리뷰 결과: ADR-142 (Round 2)

Round 1(2026-05-19) 정정 반영 후 재검증. Round 1 이슈 전건(MEDIUM catalog 패키지 배치 + LOW 3건) 해소 확인.

### 구조 검증
7/7 섹션 PASS HIGH. Context HC 11개(측정 가능), Alternatives A~E 5개 + 4축, Risk Threshold Check 표+루프, Decision 14항+기각 4건, Gates G0~G7(R1-G2 1:1). adr-writing seed#1(코드 경로 3곳) 충족.

### 코드 검증
8/9 VERIFIED + 1 PARTIAL. 124 ComponentSpec / 55 starter / shared RAC import 57(.ts+.tsx, 재측정 정확) / starter private 패키지 / 전역 import 0 / resolveCanonicalDocument·CanonicalNodeRenderer 존재 / 6 registry 실재 / FieldDef 11종 전부 VERIFIED. PARTIAL: Preview App.tsx가 canonical·legacy 경로 양쪽 사용 중 — 결정 영향 없음.

### 누락 위험 2건 (MED)
1. generic 렌더러 Skia 성능 — 60fps 프로젝트인데 렌더러 전면 교체의 성능 Risk·Gate·HC 0건.
2. R8 status 재평가 목록에 ADR-140/141 부재.

### 종합 판정
구조 PASS / 코드 8/9 VERIFIED / 누락 MED 2. HIGH 0. 결론: 수정 후 승인 가능 — Decision·구조 건전(재설계 불요), MEDIUM 2건은 Risks/Gate 보강 수준. Phase 0 착수 전 본문 반영 권장.

## Round 3 — 2026-05-19 (reviewer: claude)

## ADR 리뷰 결과: ADR-142 — RAC primitive binding + canonical 문서 기반 컴포넌트 시스템 cutover

검토 범위: packages/react-aria-starter/design.md 변경(281+/205-, uncommitted)을 트리거로 한 본문↔design.md 정합 재검토.

### 구조 검증

7개 섹션(Context/Alternatives/Risk Threshold/Decision/구현상세 분리/Risks/Gates) 전부 PASS·HIGH. design.md 변경은 구조 섹션 미변경.

### 코드 검증 (7/7 VERIFIED)

- ADR×3 'starter 55 컴포넌트' → src/*.tsx = 55개 VERIFIED
- breakdown×2 'starter/src 56 CSS' → src/*.css = 56개 VERIFIED
- design.md 부록 '56 CSS = 토큰/유틸 2 + 컴포넌트 54' → VERIFIED (ProgressCircle만 무 CSS)
- ADR:34 'private 패키지, main/exports 없음' → VERIFIED
- ADR:34 'repo 전역 starter import 0건' → path·패키지명 grep 양쪽 0건 VERIFIED
- breakdown:78 '컴포넌트별 design.md 금지(단일)' → repo 내 design.md 1개 VERIFIED
- breakdown:236 design.md §1~§7 섹션 열거 → 신 design.md 구조와 정확히 일치 VERIFIED

### design.md 변경 영향 (핵심)

- 잠재 모순 1건 해소: 구 design.md 'D3 = packages/specs' 헤더가 ADR-142(D3 SSOT를 theme/tokens root collection으로 이전)와 정면 충돌하던 stale 서술 → 신 design.md가 'ADR-142 runtime D3 SSOT = theme/tokens root collection / 본 문서는 저작 입력'으로 재서술하여 정렬.
- 신규 불일치 0건: design.md §1~§7+부록 구조가 breakdown:236 섹션 열거와 정확 일치. design.md가 'ADR-142 Phase 0 산출물'로 자기 규정.

### 위험 평가

누락 위험 없음. design.md §2.6 elevation 레시피는 R4의 Phase 0 inventory 입력으로 흡수.

### 종합 판정

- 구조: PASS (7/7 HIGH)
- 코드 정합: 7/7 VERIFIED
- 누락 위험: 0건
- 권고: HIGH 없음 / MEDIUM 없음 / LOW 2건(선택 개선 — 55 vs 54 metric 주석, css-tokens.md 참조 추가)
- 결론: 승인 가능 — design.md 변경은 ADR-142 정합을 깨지 않았고 stale D3 SSOT 모순 1건을 해소함.

참고: ADR 본문·breakdown·design.md 3개 파일 모두 uncommitted working-tree 상태.
