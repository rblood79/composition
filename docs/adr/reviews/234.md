---
adr: 234
title: 상태 변형 = origin 의 instance · 목록 = slot 을 채운 instance 자식
reviews:
  - round: 1
    ts: "2026-09-23T02:46:45.008Z"
    reviewer: codex
    source: live
    issues:
      - id: h1
        severity: HIGH
        category: other
        summary: 복제본−origin raw props diff는 부재 키를 보존하지 못해 시각 보존 이관을 깨뜨린다
        evidence: >-
          docs/adr/design/234-variant-instances-and-slot-filled-collections-breakdown.md:64;
          apps/builder/src/builder/components/originChildRefs.ts:69-93;
          apps/builder/src/adapters/canonical/instanceResolver.ts:35-46
        root_cause: >-
          차분은 복제본에 존재하는 키만 순회하고 resolver는 style을 병합한다. origin에만 존재하는 키의 삭제/카탈로그
          fallback을 표현하지 못한다.
        outcome: fixed
      - id: h2
        severity: HIGH
        category: other
        summary: 변형 자식 ID/path 이관 계약이 없어 기존 instance descendants 편집이 유실된다
        evidence: >-
          docs/adr/design/234-variant-instances-and-slot-filled-collections-breakdown.md:53,64;
          apps/builder/src/builder/components/stateVariantOrigins.ts:107-121;
          apps/builder/src/resolvers/canonical/index.ts:269-280
        root_cause: >-
          복제본 자식은 변형 전용 ID를 갖는다. ref로 전환하면 base 자식 ID를 상속하지만 기존 descendants 키의
          재매핑이나 alias 보존이 정의되지 않았다.
        outcome: fixed
      - id: m3
        severity: MEDIUM
        category: other
        summary: "enabled?: false만으로는 ref에서 상속된 숨김을 명시적으로 다시 표시할 수 없다"
        evidence: >-
          docs/adr/234-variant-instances-and-slot-filled-collections.md:91;
          docs/adr/design/234-variant-instances-and-slot-filled-collections-breakdown.md:54-55;
          apps/builder/src/adapters/canonical/canonicalRefResolution.ts:224-226
        root_cause: >-
          기존 master→ref 병합에서 부재는 상속이다. false 또는 부재 두 값으로는 상속/숨김/표시 세 의도를 구분할 수
          없다.
        outcome: fixed
      - id: m4
        severity: MEDIUM
        category: other
        summary: 새 상태 합성 순서에서 기존 instance 명시 override의 최종 우선순위가 빠졌다
        evidence: >-
          docs/adr/234-variant-instances-and-slot-filled-collections.md:88;
          apps/builder/src/builder/components/stateVariantResolution.ts:182-197;
          apps/builder/src/builder/components/__tests__/stateVariantResolution.test.ts:141-147
        root_cause: >-
          현행 instanceOwned 제외 및 DOM inline 우선 규칙을 삭제하는 교체 설계가 상태 간 순서만 규정하고
          instance와의 충돌 순서를 규정하지 않는다.
        outcome: fixed
      - id: l5
        severity: LOW
        category: evidence-missing
        summary: G2/G4에 상태 geometry의 layout 결과와 총 프레임 비용을 함께 관측할 보조 근거가 없다
        evidence: >-
          docs/adr/234-variant-instances-and-slot-filled-collections.md:116-118;
          apps/builder/src/builder/workspace/canvas/skia/buildSpecNodeData.ts:1831-1844,1883-1897;
          apps/builder/src/builder/workspace/canvas/scene/layoutCache.ts:136-168
        root_cause: >-
          paint-only 관리 키에서 geometry까지 확장하지만 gate는 scene/renderer 결과 및
          scene.build·재렌더 횟수 위주다. 신규 구현의 실제 회귀는 아직 재현하지 않았다.
        outcome: deferred
---

# ADR-234 Review Log

## Round 1 — 2026-09-23 (reviewer: codex)

판정: **설계 보완 필요 — HIGH 2 / MEDIUM 2 pending, LOW 1 deferred.** ADR은 Proposed 유지. 제품 코드 및 ADR 본문·breakdown 수정 없음.

기준 HEAD: `71cd0755053bee1d0b6a8a3b160b978236b59635` (2026-09-23). 본문/설계 상세와 canonical resolver, props merge/diff, 변형 seed/해소, Canvas 상태 적용·layout cache, Tabs wrapper/renderer, slot 정책을 대조했다. 시작 시 다른 작업의 `docs/adr/reviews/223.md` dirty를 보존했다.

### h1 — HIGH: raw diff로는 기존 모양을 보존할 수 없음

- 대상 결정: breakdown:64의 `(복제본 − origin) diff (F12)` 재사용과 본문:34의 이관 전후 Canvas 동일.
- 현재 seed는 default의 root style을 제거해 변형에 `style:{}`를 넣는다 (`stateVariantOrigins.ts:131-151`). `diffPropsAgainstOrigin`은 복제본에 존재하는 키만 비교한다 (`originChildRefs.ts:75-88`). resolver는 원본 style 위에 patch를 병합한다 (`instanceResolver.ts:35-46`).
- 최소 반례: origin `{style:{color:"red",padding:24}}`, 기존 복제본 `{style:{}}`. 지정한 diff 유틸 결과는 `{}`이고 병합 후에는 `color:red,padding:24`가 나타난다. 부재 보존 assertion **RED**. 이는 아직 없는 234 migration 구현에 대한 실패 판정이 아니라, 명시한 기존 유틸의 재사용만으로는 보존 계약을 만족하지 못한다는 반증이다.
- 영향: Components 변형의 기존 모양과 runtime 상태 모양이 달라질 수 있다. 기존에 무시되던 변형 padding을 새 모델에서 활성화하는 것 역시 무조건적인 이전 픽셀 동일과 충돌할 수 있다.
- 해결 방향: 저장된 raw 객체 차분과 기존 consumer가 실제 그린 상태 차분을 구분하고, 부재/상속/명시 제거(빈 fills 포함)의 뜻을 정한다. catalog fallback 복원, 상태별 유효값의 보존, 불가능한 가족의 원자적 보류 범위를 이관 계약에 넣는다. 단순히 빈 객체를 patch로 쓰지 않는다.

### h2 — HIGH: 변형 subtree 교체 시 기존 descendants 경로가 끊김

- 대상 결정: breakdown:53,64의 master 선해소 및 복제본→ref 전환. 본문:35는 사용자 저작 노드 Δ0도 요구한다.
- `cloneVariantChildren`은 `${variantId}__n`으로 자식 ID를 바꾼다 (`stateVariantOrigins.ts:107-121`). Tab origin도 default/selected에 서로 다른 `__label` ID를 만든다 (`tabsTemplateOrigins.ts:29-35,63,84`). Preview descendants는 ID path 또는 segment path에 정확히 매칭한다 (`resolvers/canonical/index.ts:269-280`).
- 최소 반례: 기존 variant의 `variant-label`을 instance의 `descendants`에서 `Custom`으로 편집한다. variant를 base의 ref로 바꾸고 Phase 1에서 제시한 대로 중간 master를 먼저 연 결과를 사용하면, 자식 ID는 `base-label`이 되고 같은 instance는 `Base`를 표시한다. 편집 보존 assertion **RED**. 실제 Phase 1 코드가 이미 구현되었다는 뜻은 아니며, 제안된 선해소 절차를 기존 resolver 두 호출로 구성한 반례다.
- 해결 방향: old child ID/경로→새 origin 경로 대응표를 만들고, 직접 ref·중첩 ref·descendants 안 ref 및 이름 기반 경로의 재매핑 범위를 명시한다. 사용자 ref를 재직렬화한다면 Δ0 수식도 수정한다. 사용자 저작을 그대로 보존해야 한다면 안정 ID/alias를 유지하는 대안을 정한다. root ref ID만 유지하는 것으로는 부족하다.

### m3 — MEDIUM: 상속한 enabled:false의 복구 표현이 없음

- 본문:91 및 breakdown:54는 `enabled?: false`, breakdown:55는 필드 부재=표시를 제안한다. 기존 ref 병합은 `{...master,...ref}`로 부재 필드를 상속한다 (`canonicalRefResolution.ts:224-226`, Preview `resolvers/canonical/index.ts:170-172`).
- 시나리오: reusable origin 또는 중간 변형이 자식을 숨긴 뒤, 마지막 instance에서 그 자식만 다시 표시한다. 마지막 patch에서 `enabled`를 지우면 upstream `false`를 상속하고, `true`는 제안 타입상 저장할 수 없다. 반대로 ref의 필드 부재를 무조건 표시로 읽으면 upstream 숨김 자체가 상속되지 않는다.
- 해결 방향: 필드명은 사용자 확정대로 유지하면서 `enabled?: boolean`의 부재=상속/최종 기본 true, false=숨김, true=표시로 정하거나, 별도의 명시 reset 규칙을 정한다. 조상 subtree 자체의 숨김과 자식 자신의 표시 override는 구분한다. 이 항목은 신규 UI 결함을 실측했다는 주장이 아니라 저장 계약의 표현력 문제다.

### m4 — MEDIUM: 상태 diff와 instance override의 충돌 순서가 미정

- 본문:88과 breakdown:61은 상태 간 우선순위만 규정한다. 현행 규칙은 `instance 명시 > disabled > selected > baseline`이며 Canvas는 `instanceOwned`를 제외한다 (`stateVariantResolution.ts:182-197`). 인접 테스트:141-147이 명시 color/opacity 보호를 확인한다.
- 시나리오: instance가 color를 파랑으로 편집하고 disabled 변형은 회색이다. ref를 먼저 해소한 뒤 RAC 활성 상태 diff를 얹는 순서면 회색이 최종값이다. 현행은 파랑을 유지한다. 두 순서 중 어떤 것을 새 계약으로 삼을지 현재 Decision만으로 결정할 수 없다.
- 해결 방향: base→선택/휴지 및 상호작용 상태→instance 명시 patch 등 최종 순서를 root와 descendants 모두에 선언한다. 상속으로 얻은 값과 instance가 직접 소유한 patch를 구별하고, 선택된 변형 ref 자체의 patch를 runtime 상태보다 우선할지까지 명시한다. 이는 기존 테스트를 더 늘리라는 요청이 아니라 230 대체 시 계승할 동작을 확정하라는 요청이다.

### l5 — LOW deferred: geometry 및 전체 비용 관측

- 현행 상태 overlay는 width/height 주입 뒤 paint 단계에 있다 (`buildSpecNodeData.ts:1831-1844,1883-1897`). layout cache는 별도로 props/style signature를 만든다 (`layoutCache.ts:136-168`). padding/자식 표시를 상태에 따라 바꾸려면 layout 전 입력과 캐시 무효화까지 이어져야 한다.
- G2에는 selected 변경 후 실제 형제 위치·hit 영역, G4에는 scene.build 외 layout.publish/render.frame 등 전체 비용과 Preview commit 시간의 A/B 관측을 함께 남기는 것이 적절하다. 재렌더 횟수만 같아도 비용은 늘 수 있다.
- 새 구현의 geometry/성능 회귀를 재현한 것은 아니므로 **커버리지 보완 의견, LOW deferred**다. `.agents/rules/review-loop-closure.md`에 따라 이 항목만으로 추가 판독 라운드를 요구하지 않는다.

### 확인한 방향과 검증 범위

- ref 체인, 정적 collection 자식, runtime 유효 상태를 분리하는 방향 자체는 타당하다. React Aria는 TabList의 static/dynamic collection 양쪽과 Tab/TabPanel id 대응을 제공한다. `items`를 데이터 바인딩에만 쓰는 것은 Composition의 제품 정책이며 RAC가 강제하는 제한은 아니다. [React Aria Tabs 공식 문서](https://react-aria.adobe.com/Tabs), [Collections 공식 문서](https://react-aria.adobe.com/collections).
- 실행: `pnpm -F @composition/builder exec vitest run`으로 기존 `stateVariantResolution.test.ts`와 `resolvers/canonical/__tests__/resolver.test.ts` **33/33 PASS**, 임시 이관 반증 **2/2 RED**. 실행 로그: `/private/tmp/adr234-review-probe.log`, 진단 소스: `/private/tmp/adr234-review-probe.test.ts`. 임시 repo 테스트 파일은 제거했다.
- 이 리뷰는 설계 판정이다. Pencil 원본 P1~P5의 독립 재실측, Builder live, Preview/Compare, 픽셀 동일성, 성능 A/B, 실제 IndexedDB 이관은 **UNVERIFIED**. 문서의 과거 실측을 이번 실측으로 집계하지 않는다. Preview/Compare는 열지 않았다.
- 완료 검증은 리뷰 파일의 writer/스키마/범위 포맷/guard/diff-check다. 제품 TS 변경이 없어 typecheck 및 전체 preflight는 실행하지 않는다. 전체 dirty 자동 포맷도 실행하지 않는다.

## Round 1 수리 — 2026-09-23 (executor: claude)

ADR 이 Proposed 라 수리는 본문 · breakdown 계약 개정이다 (제품 코드 변경 0). 네 건 모두 근거를 코드에서 재확인했다 — `diffPropsAgainstOrigin` 은 입력 객체에 있는 키만 순회하고 (`originChildRefs.ts:75-88`), `mergePropsWithStyleDeep` 은 style 을 얕게 spread 한다 (`instanceResolver.ts:35-46`).

| id  | 수리                                                                                                                                                                                                                                                                                                                                                                                                                                      | 위치                                                                                                                      |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| h1  | 이관 diff = **유효값 차분** (target = 이관 전 consumer 가 그린 유효값). origin 에만 있는 키 = `null` (병합 후 제거 → catalog 기본값), fills 부재 차이 = `[]`, 키 부재 = 상속. `null` 은 `mergePropsWithStyleDeep` 한 곳에서 걸러 두 leg 가 보지 않는다. 230 이 무시하던 관리 키 밖 raw 값은 target 에 없으므로 이관이 켜지 않는다. F12 재사용 철회.                                                                                       | 본문 Hard constraints · Decision 기반/이관 · R8 · G1 · G5 / breakdown Phase 0 (e)(h) · Phase 1 삭제 표기 · Phase 2 이관   |
| h2  | **사용자가 ref 하는 origin id 와 자식 id 를 고정** — 선택 가능한 가족은 기존 default origin 노드의 내용만 선택 상태로 다시 쓰고, 기존 default 모양은 새 `unselected` 변형이 갖는다 → 일반 instance 의 `descendants` Δ0. 변형을 직접 ref 한 노드만 옛 자식 id → origin 자식 id 대응표로 키를 옮기고 (root · 중첩 · descendants 안 ref), 대응 없는 경로가 있으면 가족 전체 보류. BC 수식의 "사용자 노드 Δ0" 에 이 예외와 Δbyte 식을 넣었다. | 본문 Hard constraints (사용자 편집 보존 · BC 수식) · Decision 이관 · G5 / breakdown Phase 0 (f)(g)(i) · Phase 2 이관 · §6 |
| m3  | `enabled?: boolean` — 부재 = 상속 (끝까지 부재면 표시) · `false` = 숨김 · `true` = 상속된 숨김 해제. 조상 숨김은 자식 `true` 로 못 푼다. 필드 이름은 사용자 확정 그대로.                                                                                                                                                                                                                                                                  | 본문 Decision 기반 · G1 / breakdown Phase 1                                                                               |
| m4  | 층 순서 선언 (root · descendants 같음): origin → 직접 ref 한 변형 patch (상속 층) → 실행 중 상태 patch → **instance 자기 patch (최종)**. "자기 patch" = instance 노드에 저장된 키 (체인 중간 상속값 제외) — 230 `instanceOwned` 를 이 정의로 교체. 작성자가 고른 변형은 실행 중 상태에 진다.                                                                                                                                              | 본문 Decision 상태 → 변형 적용 · G2 / breakdown Phase 2                                                                   |
| l5  | deferred 유지. 비용이 작아 G2 에 형제 위치 · hit 영역 (layout cache 무효화), G4 에 `layout.publish` · `render.frame` · Preview commit 보조 기록 (게이트 아님) 을 넣고 R9 로 적었다.                                                                                                                                                                                                                                                       | 본문 R9 · G2 · G4                                                                                                         |

반증 2건 (h · i) 은 Phase 0 진단 RED 로 옮겨 G0 에서 고정한다. 판독 루프 규칙 (`.claude/rules/review-loop-closure.md`) 에 따라 다음은 수리 검증 1회다.
