---
adr: 218
title: collection 런타임 데이터 영속 · 실행 정책
reviews:
  - round: 1
    ts: "2026-09-12T13:14:19.030Z"
    reviewer: codex
    source: live
    issues:
      - id: h1
        severity: HIGH
        category: other
        summary: 별도 캐시의 스키마·소스 변경 및 복원 유효성 계약 누락
        evidence: >-
          docs/adr/218-collection-runtime-data-persistence-execution-policy.md:45-53;
          docs/adr/design/218-collection-runtime-data-persistence-execution-policy-breakdown.md:39-51;
          apps/builder/src/builder/stores/utils/dataChange.ts:679-687
        root_cause: >-
          collectionId만으로 캐시를 식별하고 삭제만 정리한다. 현행 rename은 메모리 runtimeData도 변환하므로
          별도 캐시가 이를 따르지 않으면 reload 후 옛 key가 복원된다. endpoint 교체·mapping 변경도 같은 id
          아래 오래된 행을 남긴다.
        outcome: pending
      - id: h2
        severity: HIGH
        category: other
        summary: interval 요청 중첩·늦은 응답·정지 후 완료 처리 계약 누락
        evidence: >-
          docs/adr/design/218-collection-runtime-data-persistence-execution-policy-breakdown.md:50;
          docs/adr/218-collection-runtime-data-persistence-execution-policy.md:98-110;
          apps/builder/src/builder/stores/utils/dataActions.ts:543-561,680-693,722-745
        root_cause: >-
          현재 실행기는 loadingApis를 표시만 하고 중복 요청을 막지 않으며 응답 시점에 target을 찾아 무조건 쓴다.
          timer 해제는 진행 중 요청을 취소하거나 오래된 응답의 영속을 막지 못한다.
        outcome: pending
      - id: m3
        severity: MEDIUM
        category: other
        summary: 엔드포인트 picker의 저장 관계·교체·Undo 계약 누락
        evidence: >-
          docs/adr/design/218-collection-runtime-data-persistence-execution-policy-breakdown.md:39-46;
          apps/builder/src/builder/stores/utils/dataChange.ts:785-798;
          apps/builder/src/builder/main/BuilderCore.tsx:897-911;
          packages/shared/src/schemas/dataChange.ts:180-185
        root_cause: >-
          set_source.endpointId는 현재 명시적으로 throw하며 기존 실행은 targetCollectionId 역조회
          후 첫 endpoint를 선택한다. picker가 어느 관계를 정본으로 바꾸고 기존 연결을 어떻게 해제하는지 정의되지 않았다.
        outcome: fixed
        addressed_in: 28b2aca95; round 2 설계 해소 확인
      - id: m4
        severity: MEDIUM
        category: other
        summary: Preview 자동 실행의 소유자·정책 전달·기존 실행 경계 미정
        evidence: >-
          docs/adr/218-collection-runtime-data-persistence-execution-policy.md:12,16,47;
          docs/adr/design/218-collection-runtime-data-persistence-execution-policy-breakdown.md:50,60-65;
          packages/shared/src/collections/collectionSnapshot.ts:35-45,56-69;
          packages/shared/src/hooks/useCollectionData.tsx:517-530;
          apps/builder/src/services/agent/dataAgentCommands.ts:96
        root_cause: >-
          Send 외 실행 경로와 legacy API binding의 interval이 존재한다. 새 collection 정책과 별개인
          현행 경로를 구분하지 않고 실행 정책이 전혀 없다고 서술한다. Preview 열 때 auto를 요구하지만 host,
          policy projection, endpoint 전달 경로가 지정되지 않았다.
        outcome: pending
      - id: m5
        severity: MEDIUM
        category: evidence-missing
        summary: 대용량·번들 Gate의 표본·대조군·판정 조건 미정
        evidence: >-
          docs/adr/218-collection-runtime-data-persistence-execution-policy.md:107-110;
          docs/adr/design/218-collection-runtime-data-persistence-execution-policy-breakdown.md:28,35,69-71;
          .claude/rules/measurement-validity.md:13-25
        root_cause: >-
          대용량 응답·interval 누적과 증가 0만 제시하며 표본 크기, 비교 arm, 기기/visibility/측정 범위 및 허용
          비용이 없다. Q2/Q4 언급만으로 실제 통과 기준을 결정할 수 없다.
        outcome: fixed
        addressed_in: 28b2aca95; round 2 설계 해소 확인
    hate:
      assumption: collectionId 별 캐시가 현재 collection 의미에 유효하다
      root: schema/source 변경 후에도 같은 id 캐시를 복원한다
      axis: 소비 경로
      first_nail: 한 행 캐시 저장 -> field rename -> offline reload
      verdict: "issue:h1"
    prism:
      lenses:
        - lens: 정합성
          verdict: fail
          reason: 캐시 revision 및 응답 수용 규칙 없음
        - lens: 생명주기
          verdict: fail
          reason: timer 해제가 진행 중 요청을 막지 못함
      convergence: 일치
      question: 없음
  - round: 2
    ts: "2026-09-12T13:36:28.022Z"
    reviewer: codex
    source: live
    issues:
      - id: h1
        severity: HIGH
        category: other
        summary: sourceRev가 같은 endpoint의 요청 정의 변경을 구분하지 못한다
        evidence: >-
          docs/adr/218-collection-runtime-data-persistence-execution-policy.md:23,89;
          docs/adr/design/218-collection-runtime-data-persistence-execution-policy-breakdown.md:43;
          apps/builder/src/builder/stores/utils/dataChange.ts:440-479;
          apps/builder/src/builder/stores/utils/dataActions.ts:593-648
        root_cause: >-
          지문 입력을 endpoint id + dataPath + schema key/type으로 제한해
          URL/method/query/header/body 변경 후에도 같은 revision이 된다. 현재
          define_endpoint는 id를 유지하면서 이 값들을 바꾼다.
        outcome: pending
      - id: h2
        severity: HIGH
        category: other
        summary: 새 실행마다 abort하는 규칙에 느린 API의 interval 기아 방지 계약이 없다
        evidence: >-
          docs/adr/218-collection-runtime-data-persistence-execution-policy.md:24,90,123;
          docs/adr/design/218-collection-runtime-data-persistence-execution-policy-breakdown.md:54-55;
          apps/builder/src/builder/stores/utils/dataActions.ts:680-693;
          packages/shared/src/hooks/useCollectionData.tsx:521-524
        root_cause: >-
          고정 주기 tick마다 새 실행이 진행 요청을 취소하면 응답시간이 주기보다 긴 API는 계속 취소된다. latest-wins
          수용 안전성은 보강됐지만 완료 가능성을 보장하는 스케줄 규칙이 없다.
        outcome: pending
      - id: m3
        severity: MEDIUM
        category: other
        summary: Preview 응답 전용 projection과 export 정책 전용 projection이 공유 경로에서 충돌한다
        evidence: >-
          docs/adr/218-collection-runtime-data-persistence-execution-policy.md:49,92;
          docs/adr/design/218-collection-runtime-data-persistence-execution-policy-breakdown.md:54,57,69;
          apps/builder/src/builder/main/BuilderCore.tsx:1141-1159,1179-1184;
          packages/shared/src/collections/collectionSnapshot.ts:35-45
        root_cause: >-
          Preview/export가 같은 toRuntimeCollection을 호출한다. 응답을 싣고 정책을 빼도록 공용
          allowlist를 바꾸면 export가 응답 제외·정책 포함 계약을 위반한다. 채널별 projection 및 import
          보존 경로가 명시되지 않았다.
        outcome: pending
    hate:
      assumption: sourceRev 일치가 현재 요청 소스 유효성을 보장한다
      root: 동일 endpoint의 path/query 변경이 지문에서 빠짐
      axis: 소비 경로
      first_nail: id/dataPath/schema 고정 후 path만 변경하여 지문 입력 비교
      verdict: "issue:h1"
    prism:
      lenses:
        - lens: 캐시 정합
          verdict: fail
          reason: 요청 정의 변경을 지문에서 구별하지 못함
        - lens: 실행 생명주기
          verdict: fail
          reason: 느린 응답을 자동 tick이 반복 취소 가능
        - lens: 전달 경계
          verdict: fail
          reason: 반대 payload 정책에 공용 projection 지정
      convergence: 다른 이유로 일치
      question: 없음
---

# ADR-218 Review Log

## Round 1 — 2026-09-12 (reviewer: codex)

검토 기준: HEAD `723339712`, 작업 시작 시 clean worktree. 본문과 breakdown 전체 및 현재 실행·영속·동기화 소비 경로를 정적 대조했다. 결론은 **수정 후 재리뷰 — HIGH 2, MEDIUM 3**. ADR-152 후속으로 분리한 범위와 ADR-212 종결은 유지한다. 저장소 분리 대안 B 자체를 기각하는 판정은 아니다.

### 발견 사항

1. **[HIGH h1] 캐시가 현재 스키마·소스에 유효한지 판정할 계약이 없다.**
   본문 45–53행은 collectionId 키의 별도 store를 정하고 R1/G1(96/108행)은 삭제 시 정리만 요구한다. 하지만 `apps/builder/src/builder/stores/utils/dataChange.ts:679-687`의 필드 rename은 runtimeData 행 key도 변경한다. API 응답 캐시 저장 → 필드 rename → reload 시, 캐시가 같이 변환되지 않으면 현재 schema와 옛 key의 응답이 섞인다. Settings의 endpoint 교체나 responseMapping 변경 후 같은 collectionId로 이전 결과를 복원하는 경우도 빠져 있다. 캐시의 source/schema revision 또는 변환·무효화 규칙, hydration 적용 순서, Undo/Redo 시 처리까지 정하고 G1에 rename→reload / 소스 교체→offline reload를 넣어야 한다. 이는 새 기능 구현이 아직 없다는 지적이 아니라 분리한 두 저장소가 일치해야 하는 시점의 계약 누락이다.

2. **[HIGH h2] 타이머 정리만으로 늦은 응답의 덮어쓰기를 막을 수 없다.**
   breakdown 50행과 본문 98/110행은 interval 정리 후 timer 0만 요구한다. 현재 `apps/builder/src/builder/stores/utils/dataActions.ts:543-561`은 loadingApis를 조회한 뒤 중복 차단 없이 추가하며, 680–693행의 AbortController는 함수 내부 timeout 전용이다. 722–745행은 완료 시점에 collection을 찾아 응답을 쓴다. 요청 A가 느리고 이후 B가 먼저 성공하면 A가 최신 값을 덮어쓸 수 있고, 페이지 전환·정책 변경 후에도 이미 시작한 요청은 완료될 수 있다. 영속화를 붙이면 다음 세션까지 오래된 값이 남는다. single-flight 또는 세대별 응답 수용 규칙, 정지 시 취소/완료 무시, hydration과 새 응답의 우선순위를 정해야 한다. G3에는 A/B 역순 완료와 정지 후 완료 검사를 추가해야 한다.

3. **[MEDIUM m3] endpoint picker가 바꿀 정본 관계와 역연산이 없다.**
   breakdown 45–46행은 picker와 applyDataChange만 요구한다. 현재 `apps/builder/src/builder/stores/utils/dataChange.ts:785-798`은 endpointId가 들어오면 throw한다. `apps/builder/src/builder/main/BuilderCore.tsx:897-911`은 endpoint.targetCollectionId/이름을 역조회해 첫 항목을 실행한다. 따라서 선택 UI만 연결하면 오류가 나거나 이전 endpoint가 계속 선택될 수 있다. collection당 연결 cardinality, 기존 연결 해제, endpoint 재사용/삭제, Undo/Redo를 정의하고, `set_source` 확장인지 `define_endpoint` 묶음인지 지정해야 한다. 정책 쓰기 역시 DataOp 스키마·역연산·persistablePatch(동 파일 1020–1026행) 배선을 변경표에 명시하면 HC2를 검증할 수 있다.

4. **[MEDIUM m4] auto의 Preview 실행 소유자와 경계가 불명확하다.**
   본문 12행/인벤토리 23행의 “Send 클릭만, refreshInterval/setInterval 0”은 전체 코드 사실과 다르다. `apps/builder/src/services/agent/dataAgentCommands.ts:96`과 BuilderCore 911행도 실행하며, `packages/shared/src/hooks/useCollectionData.tsx:517-530`에는 legacy API binding용 interval이 있다. 이것이 collection-level executionPolicy가 이미 있다는 뜻은 아니다. 두 정책의 적용 범위를 구분해야 한다. breakdown 50행은 미리보기 열 때 auto를 요구하지만 본문 16행은 사용자 표면인 Settings UI에 Preview 대응물이 없다고 설명하고, 변경표에는 Preview 소비 경로가 없다. `packages/shared/src/collections/collectionSnapshot.ts:35-45`는 allowlist projection이고 56–69행은 Preview/Publish 공용 서비스다. 본문 47행의 정책 export를 충족하려면 이 전달 경로도 다뤄야 한다. Builder가 Preview 열기 전에 실행하는지 Preview가 직접 실행하는지, 편집기를 닫았을 때 interval이 계속되는지, 기존 binding 정책과 Publish의 적용 범위를 명시해야 한다. G3은 실제 헤더 Preview 진입과 endpoint 호출 수로 검증해야 한다. raw response postMessage는 `dataChange.ts:113-123`에 이미 존재하므로 export 제외와 postMessage 제외를 같은 정책으로 취급해서도 안 된다.

5. **[MEDIUM m5] 성능 Gate를 동일 조건으로 재현할 기준이 없다.**
   본문 G0/G3(107–110행), breakdown 28/35/69–71행은 크기 실측·대용량·interval 누적·initial 증가 0을 요구하지만 표본 수/행·byte 규모, 이전 arm, visible 상태, 측정 조건, latency/저장 비용 기준이 없다. `.claude/rules/measurement-validity.md` §1의 표본 대표성·불리 케이스·대조군·측정 조건 요구에 미달한다. Phase 0에서 freeze할 fixture/규모, 동일 build 설정의 기준 SHA와 initial 집계 단위, 반복 주기/실행 시간, IDB 저장 및 복원 비용 판정 기준을 정해야 한다. 수치 실측은 Phase 0에서 수행해도 되며 현재 리뷰에서 성능 FAIL을 주장하는 것은 아니다.

### 구조 및 코드 정합

- 구조: Context·대안 3개/4축·Risk Threshold·Decision·상세 분리·Risks·Gates가 모두 존재한다(본문 7/33/65/75/90/92/103행). 형식 PASS, 위험 및 Gate 내용은 위 이슈로 PARTIAL.
- VERIFIED: runtimeData 주요 set 경로는 메모리만 갱신한다(dataActions.ts:204-212,296-302,736-745). collections adapter는 전달된 객체를 저장한다(adapter.ts:564-590). 현행 publish projection은 응답 캐시를 제외한다(collectionSnapshot.ts:35-45).
- PARTIAL: “실행 정책 없음”은 collection-level 필드 부재에는 맞지만 “Send 클릭만/interval 0”으로 확대할 수 없다(m4).
- 대안의 외부 제품 관례(Airtable/NocoDB/Retool/Appsmith)는 공식 자료 미확인으로 UNVERIFIED이며 이번 판정의 근거로 사용하지 않았다.
- BC 정량화는 G0가 이미 담당한다. Proposed 단계의 미실측 자체를 별도 이슈로 중복 집계하지 않았다. Spec/Generator seed는 비해당.

### 근본 반론 (hate)

- 가정: 캐시를 collectionId 별 별도 store에 두면 현재 collection 의미에 맞는 마지막 성공 응답을 복원할 수 있다(본문 45–53/120행).
- root: ID가 같아도 schema/source 의미가 바뀌면 복원 결과가 유효하지 않다(h1).
- first nail: 한 collection, 한 행으로 API 응답 저장 → field rename → offline reload; 현재 field key로 값이 읽히는지 검사. 전체 4개 Phase 구현보다 작은 1개 storage/rename 통합 fixture다.
- 판정: G1 삭제/BC 검사와 G3 단순 reload는 이 입력 차원을 지정하지 않는다. h1 pending. 기존 fork lock-in 재질문은 필요 없다.

### 렌즈 대조 (prism)

| 렌즈          | 판정 | 핵심 이유                                                                                     |
| ------------- | ---- | --------------------------------------------------------------------------------------------- |
| 데이터 정합   | fail | 저장된 행의 유효 revision 및 늦은 응답 수용 규칙이 없어 마지막 성공 값 보장을 못 한다(h1/h2). |
| 실행 생명주기 | fail | 타이머 정리로는 정지 이후 응답 쓰기를 막을 수 없다(h2).                                       |
| 검증 무결성   | fail | G1/G3은 변경된 소스·역순 응답을 지정하지 않고 측정 조건도 미정이다(h1/h2/m5).                 |

수렴: 수정 필요로 일치. 공통 핵심은 타이머/ID 존재만 검사하면 실제로 복원·표시되는 응답의 정합성을 증명하지 못한다는 점이다. 불일치 없음.

### 검증 범위

제품 테스트·build·브라우저 실행은 수행하지 않았다. 미구현 설계의 현재 코드 경로 정적 검토이며 위 시나리오는 추가해야 할 회귀 검증이다. 설계 본문·제품 코드는 수정하지 않았고 리뷰 로그만 저장한다. commit/push 없음.

검증 결과: review schema 113/113 PASS, format/guard PASS, diff-check PASS. `codex:preflight`는 agent-catalog 단계에서 기존 지침의 `scripts/.auth-session.json` 경로 부재로 FAIL 1. TS 변경이 없어 typecheck/registration은 스킵됐고 이후 engine/text-axis gate는 실행되지 않았다. 해당 보호 파일 및 지침은 수정하지 않았다.

## Round 1 수정 반영 — 2026-09-12 (실행자: claude, 재리뷰 대기)

리뷰 5건을 ADR 본문·breakdown 에 계약으로 반영. 제품 코드 미변경 (Proposed 설계 보강). 재리뷰가 아래 매핑을 검증.

- **h1 (캐시 유효성)** → HC4 신설 (`sourceRev` 지문 + hydration 무효화 + rename 값 보존 변환 · endpoint/mapping/스키마 변경 무효화 · Undo revision 재판정). Decision 계약 절 + R1(HIGH, code 인용) + G1 에 rename→offline reload · 소스 교체→offline reload 시나리오. breakdown Phase 1 캐시 유효성 항목.
- **h2 (요청 경쟁)** → HC5 신설 (`runSeq` single-flight — 진행 중 abort · seq 일치 응답만 수용 · 정지 시 무시). R2(HIGH, `dataActions.ts:543-561`·`:680-693`·`:722-745` 인용) + G3 에 A/B 역순 완료·정지 후 완료 무시. breakdown Phase 3 요청 경쟁 항목.
- **m3 (endpoint 연결)** → Decision 계약 (cardinality 0..1 · `set_source{endpointId}` 승인 dispatcher 확장 · 역연산 · `dataChange.ts:785-798` throw 대체) + R3 + G2 연결 교체/Undo. breakdown Phase 2 + 파일표(dataChange schema·persistablePatch).
- **m4 (실행 경계)** → Context 정정 (Send 외 실행 경로 3종 명시, "interval 0" 철회) + Decision host=Builder 계약 (Preview snapshot 응답만·정책 미투영·postMessage≠export) + R4 + G3 헤더 Preview 진입 endpoint 호출 수. breakdown Phase 0 실행 경로 인벤토리 + Phase 3.
- **m5 (측정 기준)** → G0 측정 기준 freeze (fixture 3구간·대조군·측정 조건·판정선) + breakdown Phase 0 측정 항목 + §5 측정 절 (`measurement-validity` §1·Q2·Q4).

수정 커밋: (아래 commit 참조). preflight FAIL 은 `scripts/.auth-session.json` 부재(catalog 단계) — 보호 파일·지침 미수정, 본 수정과 무관.

## Round 2 — 2026-09-12 (reviewer: codex)

검토 기준: **HEAD `28b2aca95`**, 시작 worktree clean. 수정된 ADR 본문·breakdown 전체와 round 1 기록을 대조했다. 제품 코드는 이전 검토 이후 변경되지 않았으며 관련 실행/endpoint 수정/Preview/export 소비 경로를 현재 파일에서 재확인했다.

**결론: 수정 후 재리뷰 — HIGH 2, MEDIUM 1.** 기존 m3(endpoint 연결), m5(측정 기준)는 설계 수준에서 해소됐다. h1/h2/m4는 보강됐지만 아래 잔여 계약이 있어 아직 종결하지 않는다. ADR-152 후속/ADR-212 종결의 승인된 범위는 재논의하지 않는다.

### 발견 사항

1. **[HIGH h1] sourceRev가 요청의 실제 변경을 놓친다.**

   본문 89행과 breakdown 43행은 지문을 `endpoint id + responseMapping.dataPath + schema field key/type 집합`으로 확정했다. 하지만 `apps/builder/src/builder/stores/utils/dataChange.ts:440-479`의 define_endpoint는 **동일 id를 유지**하면서 baseUrl/path/method/headers/queryParams/body를 바꾼다. 실제 요청은 `dataActions.ts:593-648`에서 이 값들로 구성된다. 같은 endpoint의 `/tenant-a/users`를 `/tenant-b/users`로 바꿔도 dataPath와 schema가 같으면 지문 입력은 동일하다. 따라서 offline reload에서 이전 소스 행이 유효한 캐시로 복원된다. endpoint 교체·dataPath 변경 테스트(G1, 본문 121행)는 같은 id 내부의 URL/요청값 변경을 검증하지 않는다.

   요청 결과를 바꾸는 정의·실행 파라미터 변경을 revision/무효화 경계에 포함해야 한다. 원문 secret을 캐시 메타에 저장하지 않는 제약은 유지한다. G1에 **같은 endpoint id의 path 또는 query 수정 → offline reload**를 추가하고, 진행 중 요청의 원래 source revision도 완료 수용 시 대조하도록 정하면 source 변경 직전 응답이 새 캐시를 채우는 경우까지 막을 수 있다.

   확인: 문서에 지정된 세 입력을 직렬화하는 결정적 비교에서 path 변경 전후 `same_sourceRev_input=true`. 이는 제안 계약의 반례이며 미구현 코드에서 재현했다는 뜻은 아니다.

2. **[HIGH h2] interval에 latest-wins 취소만 적용하면 느린 요청이 끝나지 않을 수 있다.**

   본문 24/90행·breakdown 54–55행은 `interval(N초)`과 “새 실행은 진행 중 요청 abort”를 함께 정한다. 고정 tick마다 이 경로를 호출할 경우 주기 1초·응답 1.5초인 정상 API는 다음 tick마다 취소되어 성공 응답을 남기지 못한다. 현행 legacy interval도 `packages/shared/src/hooks/useCollectionData.tsx:521-524`에서 고정 setInterval을 사용하며, Builder fetch는 AbortController.signal을 실제 전달한다(`dataActions.ts:680-693`). legacy 자체를 새 정책으로 오인하는 판정이 아니라, 새 scheduler의 tick 처리 방식이 빠져 있어 이 실패 구현도 문서에 부합한다는 지적이다.

   자동 tick은 진행 중이면 skip/coalesce하거나 완료 후 N초를 기다리는 등 **반드시 완료 기회가 있는 스케줄 규칙**을 정해야 한다. 사용자의 명시적 재실행과 소스/정책 변경에 대한 취소는 구분할 수 있다. G3에 응답시간 > interval인 API를 일정 시간 실행했을 때 성공 응답이 실제 갱신되는 검사를 넣어야 한다. 현재 A 느림/B 빠름 테스트는 빠른 B가 있다는 조건이라 이 입력을 커버하지 않는다.

   확인: ADR의 고정 tick + 새 실행 abort 규칙을 10초간 결정적으로 모형화하면 시작 11회, 취소 10회, 성공 0회(주기 1000ms, 응답 1500ms). 모형 검증이며 제품 런타임 측정은 아니다.

3. **[MEDIUM m3; round 1 m4 잔여] Preview와 export가 같은 projection을 공유한다.**

   본문 49행은 **export=정책 포함/응답 제외**, 92행과 breakdown 54/69행은 **Preview=응답 포함/정책 제외**를 요구한다. 그런데 현재 `BuilderCore.tsx:1141-1144`(헤더 Preview)와 `:1179-1184`(JSON export)는 모두 `toRuntimeCollection`을 쓴다. 그 함수의 allowlist는 `packages/shared/src/collections/collectionSnapshot.ts:35-45`에 하나뿐이다. 이를 문서대로 “응답만 투영”하도록 공통 변경하면 export 응답 제외 계약이 깨지고 정책은 export에서 빠진다. 그대로 두면 Preview는 새 응답을 받지 못한다.

   두 채널의 projection을 구분하고, export→import에서 executionPolicy가 보존되는 경로와 Preview에서만 runtimeData가 포함되는 경로를 명시해야 한다. 실제 헤더 Preview는 `BuilderCore.tsx:1152-1159`의 sessionStorage→새 `/publish/` 탭이므로 iframe postMessage와 구별해야 한다. G3은 호출 수뿐 아니라 **같은 collection의 헤더 Preview payload/JSON export payload/import 결과**를 대조해야 한다. 현재 누출 발생을 주장하는 것은 아니며 공용 helper 변경 지시의 설계 충돌이다.

### 이전 이슈 해소 판정

| Round 1            | 판정              | 근거                                                                                                                                                           |
| ------------------ | ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| h1 캐시 유효성     | PARTIAL / pending | 본문 23/89/121행의 revision·rename·Undo·무효화는 반영. 같은 endpoint 요청 정의 변경이 빠져 round 2 h1 유지.                                                    |
| h2 요청 경쟁       | PARTIAL / pending | 본문 24/123행의 seq·abort·역순/정지 후 완료 검사는 stale 수용 문제를 보완. interval 완료 가능성이 빠져 round 2 h2 유지.                                        |
| m3 endpoint picker | FIXED (설계)      | 본문 91/122행·breakdown 49/67행이 cardinality 0..1, targetCollectionId 정본, 연결 교체/역연산, DataOp 확장을 명시. 구현 검증은 G2.                             |
| m4 실행 경계       | PARTIAL / pending | host=Builder 및 legacy 구분은 본문 12/92행에 반영. Preview/export projection 충돌은 round 2 m3. 인벤토리 23행의 Send-only 서술도 본문과 맞춰 정리해야 한다.    |
| m5 측정 기준       | FIXED (설계)      | 본문 120행·breakdown 36/78행이 표본 100/1000/5000, 대조군, 조건, Phase 0 통과선 freeze와 미확보 시 착수 금지를 명시. 실제 수치 확정/측정은 G0/G3에서 검증한다. |

### 구조 및 코드 정합

필수 구조는 PASS: Context(7행), Alternatives 3개·4축(35–75행), Decision(77행), 상세 링크(99행), Risks(101행), Gates(116행). R1/R2를 HIGH로 명시하고 HC/Gate에 연결한 점은 반영됐다. 내용 완결성은 위 세 이슈로 PARTIAL. 외부 제품 저장 관례는 새 검증을 하지 않았고 판정 근거에 사용하지 않았다. Spec/Generator seed는 비해당. 이미 G0에 배정된 BC/측정 확정을 Proposed 단계 미실측만으로 다시 이슈화하지 않았다.

### 근본 반론 (hate)

- load-bearing 가정: sourceRev 일치가 현재 요청 소스에 유효한 응답임을 보장한다(본문 23/89행).
- root: 요청을 바꾸는 path/query 등이 지문에 없으므로 동일 revision이 서로 다른 소스를 가리킨다(round 2 h1).
- first nail: endpoint id/dataPath/schema를 고정하고 path만 바꿔 지문 입력을 비교. 두 입력 비교로 동일함을 확인했다. 전체 4개 Phase보다 작다.
- 판정: G1은 endpoint 교체만 지정해 동일 id 수정 반례를 커버하지 않는다. h1 pending. 기존 fork 전제는 재질문하지 않는다.

### 렌즈 대조 (prism)

| 렌즈          | 판정 | 핵심 근거                                                |
| ------------- | ---- | -------------------------------------------------------- |
| 캐시 정합     | fail | 유효성 판정 입력이 실제 요청 입력을 구분하지 못한다(h1). |
| 실행 생명주기 | fail | abort 반복 시 정상 API의 완료 기회가 없다(h2).           |
| 전달 경계     | fail | 상반된 payload 계약에 같은 projection을 지정한다(m3).    |

수렴: 다른 이유로 수정 필요에 일치. 불일치 없음. 추가 승인 질문 없음.

### 검증 범위

현재 소스 정적 대조와 두 결정적 계약 반례 모형을 실행했다. 제품 테스트/build/live는 실행하지 않았다. 설계 본문·제품 코드 변경 없음. 리뷰 round 추가 및 기존 이슈의 해소 상태만 갱신한다. commit/push 없음.

Round 2 게이트: review schema 113/113 PASS, guard/format/diff-check PASS. `codex:preflight`는 기존 `scripts/.auth-session.json` 경로 부재로 agent-catalog FAIL 1. TS 변경이 없어 typecheck/registration 스킵, 이후 engine/text-axis는 미실행. 보호 파일/지침 수정 없음.

## Round 2 수정 반영 — 2026-09-12 (실행자: claude, 재리뷰 대기)

round 2 HIGH 2·MEDIUM 1 을 ADR 본문·breakdown 에 계약으로 반영. 제품 코드 미변경. round 2 판정에서 이미 FIXED 인 m3(endpoint 연결)·m5(측정 기준)는 유지.

- **round 2 h1 (sourceRev 입력 부족)** → HC4 지문을 endpoint id 만이 아니라 요청 결과를 바꾸는 정의·실행 파라미터 전부(`baseUrl+path+method+queryParams+bodyType/bodyTemplate+header 키`, 헤더 값은 secret 이라 제외)로 확장. define_endpoint 가 같은 id 로 path/query 변경(`dataChange.ts:440-479`→`dataActions.ts:593-648`) 시 지문 갈림. 진행 중 요청 시작 revision 을 완료 수용 시 대조. R1 갱신 + G1 에 "같은 id path/query 수정→offline reload". breakdown Phase 1 + §5 (a2).
- **round 2 h2 (느린 interval 반복 취소)** → HC6 신설: 자동 tick 은 abort 하지 않고 진행 중이면 skip/coalesce 또는 완료 후 N초 재예약. abort 는 사용자 재실행·소스/정책 변경에만(HC5). R2 갱신(②느린 API 성공 0) + G3 에 "응답시간 > 주기 API 여러 tick 실행 시 성공 응답 실제 갱신". breakdown Phase 3 + §5 (b2).
- **round 2 m3 (Preview/export projection 충돌)** → HC3 에 채널별 projection 분리 명시(`toRuntimeCollection` 공용 금지 — Preview=응답 포함·정책 제외, export=정책 포함·응답 제외, `BuilderCore.tsx:1141-1144`/`:1179-1184`/`collectionSnapshot.ts:35-45`). R5 를 projection 충돌로 재정의 + G3 에 "Preview payload/JSON export/import 대조". breakdown Phase 3 + §5 (d).

수정 커밋: (아래 commit 참조). preflight FAIL 은 `scripts/.auth-session.json` 부재(catalog 단계) — 보호 파일·지침 미수정, 본 수정과 무관.
