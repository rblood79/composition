---
adr: 211
title: 차트 표시 예산 — 픽셀 폭 기준 마크 수와 창·축약 계약
reviews:
  - round: 1
    ts: "2026-09-10T11:51:13.070Z"
    reviewer: codex
    source: live
    issues:
      - id: h1
        severity: HIGH
        category: other
        summary: 집계가 표시 포맷에 의존하고 others의 다중 시리즈·부호 의미가 미정
        evidence: >-
          docs/adr/design/211-chart-display-budget-pixel-fit-window-decimation-breakdown.md:40-48;
          packages/specs/src/chart/series.ts:102-107;
          packages/specs/src/chart/presentation.ts:476-511
        root_cause: 표시 형식과 집계 통계의 의미를 혼동
        outcome: fixed
        addressed_in: 8f47eded4; round 2 문서 수리 확인 (실증 아님)
      - id: h2
        severity: HIGH
        category: other
        summary: 공통 x 후보 두 방식은 시리즈별 극값 보존 Gate를 보장하지 못함
        evidence: >-
          docs/adr/design/211-chart-display-budget-pixel-fit-window-decimation-breakdown.md:41,79,104;
          docs/adr/211-chart-display-budget-pixel-fit-window-decimation.md:115
        root_cause: 대표 시리즈의 모양을 전체 시리즈의 모양으로 대체
        outcome: fixed
        addressed_in: 8f47eded4; round 2 문서 수리 확인 (실증 아님)
      - id: h3
        severity: HIGH
        category: other
        summary: fit 공식이 최소 크기와 총 마크 예산을 보장하지 않음
        evidence: >-
          docs/adr/design/211-chart-display-budget-pixel-fit-window-decimation-breakdown.md:16-23;
          packages/specs/src/chart/marks/pie.ts:264;
          packages/specs/src/chart/marks/radial.ts:135-140
        root_cause: 평균 슬롯 개수와 실제 마크 크기/다중 시리즈 비용을 동일시
        outcome: fixed
        addressed_in: round 2 수리 (실행자 종결, 2026-09-10) — 문서 수리, 실증은 G0~G2
      - id: m1
        severity: MEDIUM
        category: other
        summary: 숫자/날짜 자동 판정의 순서·정렬·간격 계약 누락
        evidence: >-
          docs/adr/design/211-chart-display-budget-pixel-fit-window-decimation-breakdown.md:27-30,40-41;
          packages/specs/src/chart/series.ts:42-49,112-118
        root_cause: 현재 범주 문자열/출현 순서에서 연속축 의미로 넘어가는 계약 부재
        outcome: fixed
        addressed_in: 8f47eded4; round 2 문서 수리 확인 (실증 아님)
      - id: m2
        severity: MEDIUM
        category: evidence-missing
        summary: displayBudget의 catalog 투영·Skia allowlist·저장 테스트 누락
        evidence: >-
          docs/adr/design/211-chart-display-budget-pixel-fit-window-decimation-breakdown.md:60-73;
          packages/shared/src/catalog/outputs/toRacProps.ts:88;
          packages/specs/src/renderers/skiaPrimitives.ts:3330
        root_cause: props 선언과 실제 생산/소비 결선을 동일시
        outcome: fixed
        addressed_in: 8f47eded4; round 2 문서 수리 확인 (실증 아님)
      - id: m3
        severity: MEDIUM
        category: other
        summary: 고정 창 크기와 Brush 키보드/데이터 공급·리셋 계약 불완전
        evidence: >-
          docs/adr/design/211-chart-display-budget-pixel-fit-window-decimation-breakdown.md:34-37,52-54;
          recharts@3.10.1/es6/cartesian/Brush.js:386-415
        root_cause: Brush의 범위 선택을 고정 폭 창 이동으로 간주
        outcome: fixed
        addressed_in: round 2 수리 (실행자 종결, 2026-09-10) — 문서 수리, 실증은 G0~G2
      - id: m4
        severity: MEDIUM
        category: migration-cost-unquantified
        summary: fit 이하 byte 동일 및 200행 초과만 영향이라는 호환 주장 불성립
        evidence: >-
          docs/adr/design/211-chart-display-budget-pixel-fit-window-decimation-breakdown.md:55,80,97-99;
          apps/builder/src/builder/workspace/canvas/scene/canvasSceneNode.ts:2585-2590
        root_cause: 행 수와 집계 후 범주 수를 혼동하고 안전 cap 경로를 분리
        outcome: fixed
        addressed_in: 8f47eded4; round 2 문서 수리 확인 (실증 아님)
      - id: m5
        severity: MEDIUM
        category: evidence-missing
        summary: Publish의 기타 번역 export 고정에 대한 기존 경로 인용이 사실과 다름
        evidence: >-
          docs/adr/design/211-chart-display-budget-pixel-fit-window-decimation-breakdown.md:48,65,70;
          packages/specs/src/chart/computeChartScene.ts:129;
          packages/shared/src/components/chart/RechartsChart.tsx:227
        root_cause: 고정 영문 상수 소비를 번역 문자열 export 주입으로 오인
        outcome: fixed
        addressed_in: 8f47eded4; round 2 문서 수리 확인 (실증 아님)
      - id: m6
        severity: MEDIUM
        category: adr-structure-violation
        summary: B 위험 평가와 Risks가 모순되고 HIGH 경로 근거가 빠짐
        evidence: >-
          docs/adr/211-chart-display-budget-pixel-fit-window-decimation.md:74-77,99-107;
          .agents/rules/adr-writing.md:248
        root_cause: 대안 B HIGH 0으로 선언하면서 같은 경로의 R1/R2를 HIGH로 평가
        outcome: fixed
        addressed_in: round 2 수리 (실행자 종결, 2026-09-10) — 문서 수리, 실증은 G0~G2
  - round: 2
    ts: "2026-09-10T12:21:50.227Z"
    reviewer: codex
    source: live
    issues:
      - id: h1
        severity: HIGH
        category: other
        summary: 극값 합집합을 모든 시리즈가 그리므로 실제 상한은 2·S²·B
        evidence: >-
          docs/adr/design/211-chart-display-budget-pixel-fit-window-decimation-breakdown.md:18,29,57
        root_cause: 선택 index 수와 시리즈 전체 렌더 점 수의 혼동
        outcome: fixed
        addressed_in: round 2 수리 (실행자 종결, 2026-09-10) — 문서 수리, 실증은 G0~G2
      - id: m1
        severity: MEDIUM
        category: other
        summary: pie 반지름 방향 링 수 제한을 범주 fit으로 적용
        evidence: >-
          docs/adr/design/211-chart-display-budget-pixel-fit-window-decimation-breakdown.md:30;
          packages/specs/src/chart/marks/pie.ts:237-252
        root_cause: 범주 조각 수를 줄여도 동심 링 수 S는 줄지 않음
        outcome: fixed
        addressed_in: round 2 수리 (실행자 종결, 2026-09-10) — 문서 수리, 실증은 G0~G2
      - id: m2
        severity: MEDIUM
        category: other
        summary: 집계 후 domain과 full model domain 계약 충돌
        evidence: >-
          docs/adr/design/211-chart-display-budget-pixel-fit-window-decimation-breakdown.md:58,62,90;
          packages/specs/src/chart/computeChartScene.ts:444-445
        root_cause: 원본 전체 범위와 변환된 값의 범위를 구분하지 않음
        outcome: fixed
        addressed_in: round 2 수리 (실행자 종결, 2026-09-10) — 문서 수리, 실증은 G0~G2
      - id: m3
        severity: MEDIUM
        category: other
        summary: min/max 선택이 M4 무손실 선례와 다르고 결측 gap 연결 정책 누락
        evidence: >-
          docs/adr/design/211-chart-display-budget-pixel-fit-window-decimation-breakdown.md:57;
          docs/adr/211-chart-display-budget-pixel-fit-window-decimation.md:78;
          packages/shared/src/components/chart/RechartsChart.tsx:555
        root_cause: 극값 보존과 선 연결/픽셀 모양 보존을 동일시
        outcome: fixed
        addressed_in: round 2 수리 (실행자 종결, 2026-09-10) — 문서 수리, 실증은 G0~G2
      - id: l1
        severity: LOW
        category: other
        summary: RAC Slider 기본 PageUp/Down 이동량은 fitEff가 아님
        evidence: >-
          docs/adr/design/211-chart-display-budget-pixel-fit-window-decimation-breakdown.md:65;
          react-stately@3.50.0/dist/private/slider/useSliderState.mjs:24-31
        root_cause: RAC 기본 page step을 창 길이로 가정
        outcome: deferred
---

# ADR-211 Review Log

## Round 1 — 2026-09-10 (reviewer: codex)

# ADR-211 리뷰 결과 — round 1

판정: **설계 수정 후 재리뷰**. 픽셀 예산 방향의 폐기를 요구하지 않는다. HIGH 3 / MEDIUM 6. P0 조사 자체로 확인할 내용은 있으나 현재 Decision을 구현 계약으로 승인할 수 없다.

기준 HEAD `8971013cd4ba6237740e6d34c819b7189a1982f6`. 시작 worktree clean. 설계 코드 기준 `2a9ab37fc`와 현재 HEAD의 chart spec/shared runtime/Canvas 주입 경로 diff 없음. 제품 테스트·build·live·P0는 실행하지 않았다. 코드 정독, 공식 문서 조회, 독립 Python 산술 반례만 사용했다. 본 로그와 로컬 공용 작업 상태 외에는 수정하지 않았다.

아래 `ADR`은 `docs/adr/211-chart-display-budget-pixel-fit-window-decimation.md`, `D`는 해당 design breakdown이다.

## 발견 사항

### h1 HIGH — 합계/평균 및 others의 의미

D:40은 값 필드가 퍼센트면 평균을 쓰지만 현행 `series.ts:102-107`은 범주별 sum이고 `presentation.ts:476-511`은 숫자 표시만 소유한다. 같은 원본 0.1/0.9가 decimal이면 집계 1, percent이면 0.5가 된다. 이를 valueFormat으로 판정하면 표시 옵션 변경으로 기하가 달라지고, 별도 메타데이터를 뜻한다면 그 입력 채널이 없다. 평균을 사용하면 G2의 일괄 '집계 합=원본 합'(ADR:115)도 성립하지 않는다. 중복 범주의 행 수가 다르면 '범주 평균'과 '행 평균'도 다르다.

D:47의 '값 내림차순'은 다중 시리즈 radar/pie/radial에서 어떤 값을 정렬할지 없다. 음수 pie는 현행 `marks/pie.ts:194-209`에서 절댓값으로 면적을 낸다. +10/-10을 others로 sum하면 면적이 사라질 수 있다. radial others는 합산 후 domain 재계산 여부가 없고 현행 `marks/radial.ts:145-153`은 domain 밖을 clamp한다.

최소 수정: 집계 연산을 표시 포맷에서 분리하고 sum/mean의 입력 단위·가중치·결측·음수 계약을 명시한다. 극좌표 종류별 ranking key, 시리즈별 합산, others synthetic identity/원본 '기타' 충돌, domain 재계산을 정한다. G2는 sum/mean/선택/others 별 독립 오라클로 나눈다. 자동 의미 추정 대신 명시 설정 또는 지원 제한을 선택할 수 있다.

### h2 HIGH — 공통 x LTTB로 모든 시리즈 극값을 보장할 수 없음

D:41/104는 첫 시리즈 또는 합산 시리즈로 공통 x를 선택하고 D:79/ADR:115는 극값 위치 오차 ≤1 슬롯을 요구한다. 첫 시리즈가 평평하면 다른 시리즈의 좁은 spike는 선택에 영향을 주지 않는다. 합산 방식도 B의 +spike와 C의 -spike가 상쇄되면 동일하다. 선택되지 않은 구간에 spike를 놓으면 중요한 극값이 사라지며 '위치 오차'를 계산할 대상도 없다. 이는 구현 버그 예측이 아니라 제안한 입력 축약의 정보 손실 반례다.

최소 수정: 시리즈별 오차/극값 보존 목표와 허용 손실을 정하고 multi-series 선택 규칙을 다시 설계한다. union이 fit을 초과하는 경우의 우선순위도 필요하다. P0에 평탄 기준 시리즈·서로 다른 위치 spike·상쇄 시리즈·결측 gap fixture를 넣고, 선택 알고리즘과 무관한 원본 극값/픽셀 오라클로 검증한다. 합계 접미는 원본 점 선택인 LTTB에 붙이지 않는다.

### h3 HIGH — 개수 fit이 가독성과 총비용 계약을 보장하지 않음

D:16-23은 종류별 fit만 정한다. 반지름60, minArc6이면 fit62지만 pie 값 [1000,1]의 작은 조각 호는 약0.377px다(`marks/pie.ts:264` 값 비율 sweep). 개수2≤fit이라 개입하지 않는다. 따라서 '최소 호 길이' 보장과 'fit 이하 무변경'은 동시에 주장할 수 없다. 여러 동심 pie 링은 안쪽 반지름과 링 두께도 제한해야 한다. stacked bar/line/area/radar는 fit에 시리즈 수가 없으므로 폭이 같아도 시리즈 수에 따라 점/마크 비용이 증가한다. '컬럼 수와 분리' 주장(ADR:124)을 구조로 증명하지 못한다.

작은 plot 폭4, minSlot8이면 fit0이다. D:40의 ceil(n/fit), D:47의 fit-1, LTTB의 0/1/2점 처리 모두 미정이다. Python 독립 산술로 fit0 및 pie 호0.3766을 확인했다(제품 테스트 아님).

최소 수정: 개수 예산과 최소 실제 크기를 구분한다. 최소 호 보장을 포기할지, 값 비중 기반 묶기를 할지 명시하고 HC를 맞춘다. 다중 시리즈/안쪽 링/부분 sweep/간격 예산과 fit0·1·2의 렌더/진단 정책을 넣는다. 총비용 보장은 시리즈 수를 포함한 제한 또는 측정 범위 한정으로 표현한다.

### m1 MEDIUM — 축 종류 자동 추론의 의미 전환

D:27-30은 파싱 가능한 모든 값을 순서/기간 축으로 취급한다. `series.ts:42-49,112-118`은 문자열화와 첫 출현 순서이며 시간 정렬은 하지 않는다. 숫자형 ID '001','002'도 기간/수량 축처럼 집계될 수 있고, 날짜가 역순이면 '인접 묶음'이 기간을 의미하지 않는다. LTTB x가 timestamp인지 categoryIndex인지, 불규칙 간격을 재현하는지도 없다.

최소 수정: raw 타입 보존, 정렬·동률·결측·엄격 ISO 판정과 x 거리 의미를 명시한다. 숫자형 ID를 자동 집계하지 않는 opt-in 또는 보수적 auto 규칙을 정하고 overflow×종류의 전체 지원표를 추가한다. 현재는 극좌표 window만 금지돼 bar others, line aggregate 등의 의미가 열려 있다.

### m2 MEDIUM — 새 prop 전달 경로 누락

D:60-73에 `Chart.binding.ts` accepts 등록이 없다. `toRacProps.ts:88`은 accepts만 투영한다. shared `Chart.tsx`의 props 추출/memo와 Skia의 `pickChartPresentationProps`(`skiaPrimitives.ts:3330`)도 명시 키 전달이다. types/validator만 추가하면 실제 화면에 전달되지 않는다. 중첩 displayBudget의 ref 전체 교체·reset·동일 값 재적용·Undo/reload 검사도 없다.

최소 수정: catalog kind/accepts/전용 editor mount, shared memo 및 Skia allowlist를 inventory에 넣고 실제 Properties→semantic→canonical→DB→reload→양 renderer→Export 경로를 Gate로 고정한다. helper에 직접 props를 넣는 테스트만으로 통과시키지 않는다.

### m3 MEDIUM — Brush는 고정 폭 창 이동을 자동 제공하지 않음

D:34는 창 크기=fit, D:36은 화살표로 창 이동을 선언한다. 설치된 Recharts3.10.1 `es6/cartesian/Brush.js:386-415`는 startX/endX 손잡이 각각의 index를 움직인다. 그대로 쓰면 창 크기가 변한다. D:54처럼 렌더 행을 창으로 잘라 넘기면 Brush가 전체 index를 어디에서 받는지도 필요하다. resize·source 변경·필터 감소·종류 전환 때 start clamp/reset 규칙과 resolver의 window start 입력도 없다(D:52).

최소 수정: full model/visible model/index 변환, controlled start/end의 고정 길이, 마지막 창, 데이터 교체 및 포커스 정책을 명시한다. Brush 공존 spike는 렌더 여부뿐 아니라 키보드 창 길이 불변·끝까지 접근을 검사한다. Brush가 plot 공간을 차지한다면 Canvas도 동일 공간을 예약하거나 외부 배치를 택해야 동일 size의 fit이 성립한다. 자체 스크롤 fallback은 Recharts accessibilityLayer가 자동 소유하지 않으므로 기존 접근성 primitive를 통한 계약이 필요하다.

### m4 MEDIUM — 호환 범위와 안전 cap

D:97은 fit 초과와 200행 초과를 동일시하지만 다르다. 폭이 좁은 50범주는 200행 미만이어도 바뀐다. 반대로 300행이 같은 범주에 합산돼 범주수1≤fit이면 기존 Canvas 앞200행 합계와 신규 전체300행 합계가 달라진다(`canvasSceneNode.ts:2585-2590`, `series.ts:102-118`). 'fit 이하 scene byte 동일'(D:80)은 그대로는 불가능하다.

D:55의 Canvas 전용 안전 cap은 도입 시 전체 runtime과 합계/domain을 다시 다르게 만든다. D:98의 '구버전 200행으로 복귀'는 Canvas에만 해당하며 runtime은 전체 행이다.

최소 수정: raw 행 수/집계 범주 수/표시 셀 수로 영향 집합을 분리하고 기존 slice 영향 문서는 byte 동일 예외를 명시한다. 안전 cap을 허용한다면 두 leg 의미·안내·동일성 조건을 재정의하고 자동 도입하지 않는다. 데이터 보존과 시각적 rollback 동일성도 분리한다.

### m5 MEDIUM — 번역 export 정본 경로 오인

D:48은 others 문자열을 export 시점에 고정하는 선례로 `CHART_INVALID_SETTINGS_TEXT`를 들지만 실제 `computeChartScene.ts:129`는 영문 상수이고 Recharts도 같은 상수를 직접 렌더한다(`RechartsChart.tsx:227`). 번역을 export에 실어 주는 경로의 근거가 아니다. displayBudget 외 저장 필드가 없다는 D:9와도 연결 방식이 미정이다.

최소 수정: 문서 locale/표시 문자열의 실제 전달 위치와 저장·export 계약을 정하고 Canvas/Preview/독립 Publish ko/en 및 reload 테스트를 추가한다. 기존 상수 동작을 번역 주입 선례로 쓰지 않는다.

### m6 MEDIUM — 위험 평가·Gate 정합

ADR:74-77은 B HIGH0으로 위험 회피 대안 검토를 끝내지만 같은 B의 행 cap 제거와 두 leg 불일치를 R1/R2 HIGH로 선언한다(ADR:99-107). HIGH 경로별 구체 인용도 없어 adr-writing seed(:248)를 충족하지 않는다. 대안 C의 독립 Canvas/DOM 표시 정책을 consumer-to-consumer 의존이라고 단정한 기각 이유도 과하다. 정책 비대칭과 실제 소비자 간 의존은 구분해야 한다.

최소 수정: B의 위험 축을 R1/R2와 맞추고 필요한 회피 대안 또는 위험 수용 근거를 제시한다. 각 HIGH에 코드 경로와 반증 Gate를 연결한다. G4는 engineering/policy를 분리하며 실제 5,000행 모델 시리즈 수·분포·표본수·warm/cold·창 이동 반복 조건을 고정한다. ±2ms 상대 비교만으로 두 arm 동반 악화를 통과시키지 않도록 before 대비 총비용도 기록한다. G1의 CHART_SAMPLE_ROWS 참조0은 역사 문서/리뷰가 아니라 제품 코드 범위로 한정한다.

## 구조·코드 검증

| 항목                           | 판정              | 근거                                                      |
| ------------------------------ | ----------------- | --------------------------------------------------------- |
| Context·3-domain·분리 질문     | PASS (MED)        | ADR:7-37, D:5-9. 표시 집계 의미는 h1 보완 필요            |
| Alternatives/4축·Threshold     | PARTIAL           | ADR:39-77, m6                                             |
| Decision·상세 분리             | PASS (MED)        | ADR:79-95, D 전체; 구조적 성능 보장은 h3                  |
| Risks·Gates                    | PARTIAL           | ADR:97-118, h1/h2/m6                                      |
| 200행 Canvas slice             | VERIFIED          | computeChartScene.ts:57, canvasSceneNode.ts:2585-2590     |
| runtime 전체 행                | VERIFIED          | Chart.tsx:336, runtimeData.ts:35-36                       |
| Skia에서 별도 slice 제거 필요  | PARTIAL           | skiaPrimitives.ts:3357은 행 수신 분기이며 자체 slice 없음 |
| 현재 범주 first-seen 및 sum    | VERIFIED          | series.ts:102-118                                         |
| 두 renderer shared layout 사용 | VERIFIED          | computeChartScene.ts:526, RechartsChart.tsx:162           |
| 새 overflow 실제 전달          | UNVERIFIED (제안) | accepts/allowlist 결선 누락, m2                           |
| Brush 화살표 고정 창 이동      | PARTIAL           | 설치본 Brush.js:386-415는 손잡이 이동, m3                 |
| 기타 문자열 export 선례        | 주장과 모순       | computeChartScene.ts:129, m5                              |

전체 주장 전수 검증을 했다는 수치로 요약하지 않는다. 미검증: P0 최소단위 가독성·LTTB 구현·Brush live·생산 로그인 network·최종 번들·실제 프로젝트 영향 건수·토큰 생성기 결선. 예산 승인은 실측과 별도 사용자 결정이다.

## 근본 반론 (hate)

- 가정: 종류별 fit 하나면 데이터 의미를 유지하면서 가독성과 총비용을 제한한다(ADR:85-89, D:16-23).
- root: fit은 평균 슬롯 수일 뿐 값 비중·시리즈 수·집계 통계의 의미를 포함하지 않는다(h1/h3).
- first nail: 반지름60의 [1000,1] pie를 손계산하면 fit62 안에서도 작은 호0.377px이다. 이미 독립 산술 확인. 단순 fit 표(현재 G1)만으로 최소 마크 가독성을 판정할 수 없다.
- 판정: h3; P0에 비균일 값·다중 시리즈·fit0/1/2를 포함해야 한다.

## 렌즈 대조 (prism)

| 렌즈             | 판정    | 이유                                                |
| ---------------- | ------- | --------------------------------------------------- |
| 데이터 의미      | fail    | format 의존 집계와 others 의미 미정(h1)             |
| 비용/가독성      | fail    | 평균 슬롯 수는 실제 최소 크기·총 마크 비용 아님(h3) |
| 측정 무결성      | fail    | 공통 x 극값 Gate가 반례를 포함하지 않음(h2)         |
| 생산/소비·접근성 | unclear | prop 및 고정 창 키보드 결선 미검증(m2/m3)           |
| 호환             | fail    | raw 행 수와 범주 수를 동일시(m4)                    |

수렴: 다른 실패 이유들이 설계 보완 필요로 수렴. 해결 질문: 자동 축약이 보장할 것은 값/합계 보존인가, 시각적 추세인가? 둘의 적용 범위와 사용자 선택을 종류별로 명시해야 한다.

## 다음 단계

- P0 전 계약 수정: h1/h2/h3 및 m1/m4/m5/m6의 문서 모순.
- P0 검증 항목: m2 전체 전달 inventory, m3 고정 창/plot 공간/키보드, 불리 시리즈 선택·극값 fixture.
- 설계 수정 후 해당 항목 수리 검증 1회. 이번 요청은 리뷰이므로 설계·제품 수정과 P0 실행은 하지 않는다.

공식 자료 확인: [Recharts Brush](https://recharts.github.io/en-US/api/Brush/)는 zoom/pan 범위 컨트롤이며 현재 온라인 API와 설치3.10.1은 구분했다. [Highcharts dataGrouping](https://api.highcharts.com/highstock/series.line.dataGrouping)은 픽셀 기준 집계와 approximation 확장 지점을 제공한다. 이 선례가 Composition의 포맷 기반 평균·모든 시리즈 극값 보존을 보증하지 않는다.

## Round 2 — 2026-09-10 (reviewer: codex)

# ADR-211 수리 검증 — round 2

**판정: 추가 설계 수리 필요.** 검증 기준 `8f47eded4`, 시작 worktree clean. round 1의 원문은 보존하고 확인된 항목의 frontmatter outcome만 갱신했다. 제품·설계 파일 변경 0, 테스트/build/live/P0 미실행. 독립 산술과 코드 정독, 새로 인용된 M4 논문 원문을 확인했다.

## round 1 수리 판정

| 기존 ID | 결과         | 근거/남은 조건                                                                                      |
| ------- | ------------ | --------------------------------------------------------------------------------------------------- |
| h1      | fixed (문서) | D §2.4: aggregate 명시, mean 범주 평균, ranking/부호 합산 의미 분리. 데이터 통계와 valueFormat 분리 |
| h2      | fixed (문서) | D:57: 모든 시리즈 min/max 선택으로 기존 첫 시리즈·상쇄 반례 해소. 새로운 마크 수 문제는 round 2 h1  |
| h3      | pending      | 최소 마크 크기 미보장/fit0 정의는 반영. 총 마크 보장은 round 2 h1, pie 링 축은 m1                   |
| m1      | fixed (문서) | D §2.3/2.4: 보수적 ISO/ordinal opt-in·index 순서·지원표                                             |
| m2      | fixed (문서) | D §2.6: accepts/allowlist/memo/객체 교체/실제 경로 Gate 추가. 실행 확인은 G0/G3                     |
| m3      | pending      | Brush 제거·clamp·양쪽 트랙 예약은 반영. Page 키 세부가 설치 RAC와 불일치 → round 2 l1, P0 deferred  |
| m4      | fixed (문서) | D §7: 행/범주/마크 영향 집합과 두 leg 동일 R, 사용자 결정 기록                                      |
| m5      | fixed (문서) | D §2.7: 영문 상수 선례로 정정, 저장 othersLabel 소비                                                |
| m6      | pending      | 대안 D·공학/정책 분리는 반영. 마크 상한이 아직 성립하지 않아 D HIGH0 근거 미충족                    |

'fixed'는 설계 수정 확인이며 제품 Gate PASS나 구현 완료가 아니다.

## 남은 발견

### h1 HIGH — extrema 마크 상한의 S 배 누락

D:57은 시리즈별 min/max 합집합의 모든 index에서 모든 시리즈 값을 그린다. 합집합 index 수 U≤2SB지만 실제 수치 점은 S×U≤2S²B다. D:18의 k=2 계산은 2SB만 센다.

독립 산술: S=4, M=2000, fit≥250이면 B=250. bucket마다 8개 이상의 원본 범주를 두고 각 시리즈 극값 index 2개가 서로 겹치지 않게 하면 U=2000, 렌더 점=8000이다. 추가 dot/값 라벨 비용은 제외한 수치다. fitEff=1도 1 bucket에서 최대 2S index를 선택하므로 'min/max 2점'(D:22)이 전 시리즈 합계가 아니다. 실제 renderer도 각 행을 시리즈마다 소비한다(`runtimeData.ts:52-58`, `RechartsChart.tsx:184-204`).

최소 수정: 선택 index/시리즈별 점/추가 장식의 계수를 분리해 B를 산정한다. 공통 x 유지 시 S² 최악값을 예산에 포함하거나 시리즈별 희소 표현으로 계약을 바꾼다. G1/G2는 선택 수뿐 아니라 실제 모든 시리즈 점/마크 수≤M을 검증해야 한다. M이 모든 마크 상한이라면 축/범례/트랙 및 dot/라벨도 포함할지 별도 비용으로 정의할지 고정한다. R=Infinity를 허용하는 경우도 '유한 상한으로 HIGH 해소'라고 단정하지 않는다.

### m1 MEDIUM — pie 링 제한의 축이 잘못됨

D:30은 fit=min(둘레/minArc, 반지름 폭/minRing)으로 **범주 수**를 제한한다. 그러나 현행 `marks/pie.ts:237-252`의 ringCount는 stack 모드의 시리즈 수다. 범주를 others로 줄여도 링 S는 변하지 않는다. 한 시리즈·반지름60·minRing4에서 범주가 62개 들어갈 수 있어도 fit15로 잘리고, S가 과도한 경우 링 두께는 여전히 해결되지 않는다.

최소 수정: 조각의 각도 방향 예산과 실제 그리는 ringCount의 반지름 방향 예산을 따로 계산한다. 과도한 S에 대한 진단/지원 정책을 정한다. 단순히 링 최소 두께 보장을 제외한다면 범주 fit에 반지름 제한을 넣지 않는다. §5의 pie fit62와 같은 조건의 min(62,15) 기대값도 함께 정합화한다.

### m2 MEDIUM — 집계 후 domain 정의 충돌

D:62의 full domain 고정은 window에는 타당하다. 하지만 D:90은 모든 visible 모델에도 'domain은 full model'이라고 한다. 원본 [60,60]을 sum bucket 하나로 만들면 값120이 원본 extent를 넘어간다. radial others 뒤 domain 재계산은 D:58에 예외로 있지만 bar/누적 area 집계도 같은 문제가 있다. 현행 extent는 입력 grid에서 만들어진다(`computeChartScene.ts:444-445`).

최소 수정: window=변환 전 전체 domain, aggregate/others=변환 후 전체 domain, extrema=원본 full domain 등 모드별 출처를 명시한다. full model이 '축약 후 전체 모델'을 뜻한다면 입력/변환/visible 3단계를 구분한다. expand 정규화 및 통계별 domain fixture를 추가한다. fitEff=1의 집계 '전체 합'(D:22)도 명시 mean/max/min을 유지하도록 고친다.

### m3 MEDIUM — M4 인용과 결측 연결

ADR:78은 M4를 min/max만으로 시각적 손실0인 근거로 인용한다. 실제 [M4 논문 §1·§4](https://www.vldb.org/pvldb/vol7/p797-jugel.pdf)는 픽셀 열의 min/max 외 first/last도 선택하며 정해진 rasterization 조건에 대한 결과다. 현재 제안은 min/max 선택이고 visible index를 다시 band 배치하므로 그 무손실 결론을 승계할 수 없다. **시리즈별 극값 값 보존** 목표 자체는 유효하며 M4 전체 도입을 요구하지 않는다.

원본 [1,null,2]의 null index가 선택되지 않으면 visible [1,2]가 연결된다. 현행 Recharts는 `connectNulls:false`(`RechartsChart.tsx:555`)이므로 '원본 결측은 현행대로 결측'(D:57)만으로 기존 gap 보존을 보장하지 못한다. 기존 G2 결측 fixture도 min/max index 포함만 확인한다.

최소 수정: M4에서 참고한 부분과 보장하지 않는 외관을 분리한다. gap sentinel/segment 분리 또는 gap 손실을 허용하는 명시 정책을 정하고 실제 line/area 연결 검사를 Gate에 추가한다. 추가 sentinel 비용은 h1 마크 예산에 포함한다.

### l1 LOW deferred → P0 — Slider Page 키

D:65는 PageUp/Down=fitEff를 선언하고 Slider 파일은 변경0 재사용이다. 설치된 `react-stately@3.50.0/dist/private/slider/useSliderState.mjs:24-31`은 pageSize=(max-min)/10을 step에 맞춰 정한다. max=n-fitEff이므로 n1000, fitEff50이면 기본 pageSize95다. `react-aria/.../useSliderThumb.mjs:63-70`이 이 값을 쓴다.

P0에서 요구를 기본 RAC 규칙으로 맞추거나 thumb 이벤트 계층의 최소 확장을 inventory로 확정한다. 기존 Slider 재사용만으로 fitEff 이동을 VERIFIED 처리하지 않는다. 기존 G0 Slider spike가 수리 장소이므로 독립 blocker로 올리지 않는다.

## 구조/근본 반론/렌즈

구조는 Context·4대안·분리 질문·Gates 모두 존재한다. 대안 D HIGH0/총비용 상한의 근거는 h1 때문에 PARTIAL이다. round 1 전체 리뷰를 반복하지 않고 변경된 계약과 직접 연결된 소비 경로만 확인했다.

- load-bearing 가정: 선택 index 수=총 렌더 점 수(D:57).
- root: 합집합 index를 S개 시리즈가 다시 그리는 비용이 빠졌다.
- first nail: S4/B250의 서로 겹치지 않는 극값 fixture를 산술로 세면 2000 대 8000. 제품 구현 없이 반증됐다.
- lens: 통계 의미 pass(h1 이전 수정), 총비용 fail(새 h1), 기하/축 unclear(m1/m2), 외부 근거/연결 fail(m3), 키보드 unclear(l1, P0).
- 수렴: 전건 종결 불가. 질문은 새로운 사용자 취향이 아니라 구현 계약 수정 사항이다. 추가 승인 질문 없이 설계자가 수식을 수리할 수 있다.

## 결론

round 1 9건 중 문서 해소6, 잔여3. 이번 수리 검증 발견은 HIGH1/MEDIUM3/LOW1(deferred). 설계와 제품은 수정하지 않았다. h1 및 m1–m3를 반영한 뒤 해당 수리만 확인한다. P0 실행 승인을 이 리뷰로 대체하지 않는다.

### round 2 수리 기록 (실행자, 2026-09-10)

판독 루프 상한 (phase 당 판독 1 + 수리 검증 1, review-loop-closure §1) 에 도달했고 사용자가 "초기 설계를 흔들지 않게 재리뷰 없이 한 번에 끝내자" 고 지시해 실행자가 종결한다. 초기 설계 (세 예산 · 창/집계/극값/others · RAC Slider · 두 leg 동일 계약) 는 유지하고 계약의 수식·경계만 고쳤다.

- h1: `M` 은 데이터 마크 (축·범례·트랙 제외) 의 **실제 점 수** 기준 — 극값 선택은 `S × |U| × k ≤ M` 을 적응 B (`fitEff` 에서 반감) 로 맞추고, `B = 1` 에서도 넘치면 `too-many-series` + bucket 집계 fallback. `R` 은 유한 (P0 는 값만 정한다). G1/G2 는 선택 수가 아니라 렌더 점 수를 검증. S 4 · B 250 반례를 fixture 로 고정 (D §2.1 · §2.4 · §5).
- m1: pie 는 각도 방향 범주 fit 만; 동심 링 = 시리즈 S 는 개수 예산 밖, `minRing` 미보장 (radial 전용), 비용은 `S × fitEff ≤ M`, 초과 시 `too-many-series` + 앞 `floor(M/fitEff)` 링. §5 pie fixture 정합 (D §2.2).
- m2: input → transformed → visible 세 층, domain 은 항상 transformed 전체 (창 = input, 집계 = 통계값 범위, others = 합산 포함, 극값 = 원본 극값), expand 는 transformed 위에서, `fitEff = 1` 집계는 명시 통계 (D §2.5 · §3 · §5).
- m3: M4 인용을 "픽셀 열 min/max 선택 규칙" 으로 한정, 무손실 외관 주장 삭제. 결측 run 이 있는 bucket 은 시리즈별 gap sentinel index 1 추가 → `connectNulls:false` 로 끊김 유지 (gap 존재 보존, 위치는 bucket 근사). `|U| ≤ 3·S·B`, sentinel 비용은 h1 예산에 포함. G2 에 연결 검사 추가 (D §2.4 · §5, ADR Gates).
- l1: PageUp/Down = fitEff 요구 철회 — RAC 기본 (범위 1/10, step 정렬) 을 계약으로, Slider 파일 변경 0 유지. G0 spike 항목은 "RAC 기본 확인" 으로 (D §2.5).

수리 commit: ADR·breakdown 같은 commit. 실증 (G0~G2) 은 착수 후.
