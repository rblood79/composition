---
adr: 174
title: Paragraph 수명 retained 전환 — 전역 LRU 폐지와 안전 폐기
reviews:
  - round: 1
    ts: '2026-07-30T15:04:46.469Z'
    reviewer: claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: other
        summary: 현행 content 키 dedup 소실 — Phase 0 중복 계수 실측 부재 + 공유 variant 분기 미기재
        evidence: 'nodeRendererText.ts:130'
        root_cause: 'retained 설계가 per-node 소유만 상정, 현행 캐시의 노드 간 공유 구조 미조사'
        outcome: fixed
        addressed_in: round 1 동일 커밋 (breakdown Phase 0 항목 5 + Phase 2 소유 모델 분기 + ADR G1/R2)
      - id: m2
        severity: MEDIUM
        category: other
        summary: >-
          프레임 중 delete 지점 전수 미목록화 (evict/replace/fontMgr-clear 3곳) + fontMgr
          invalidation 축 미기재
        evidence: 'nodeRendererText.ts:73-77,97-100'
        root_cause: 퇴거 경로만 상정 — same-key 교체와 fontMgr 일괄 clear 도 renderText 내부 즉시 delete
        outcome: fixed
        addressed_in: round 1 동일 커밋 (breakdown Phase 0 항목 6 + Phase 2 invalidation 축)
      - id: l3
        severity: LOW
        category: other
        summary: 문턱 변수 표현 부정밀 — draw 수가 아니라 고유 캐시 키 수
        evidence: 'nodeRendererText.ts:64-70'
        root_cause: content 키 히트는 draw 수와 무관하게 캐시 삽입을 유발하지 않음
        outcome: fixed
        addressed_in: 'round 1 동일 커밋 (ADR Context :9)'
---
# ADR-174 Review Log

## Round 1 — 2026-07-30 (reviewer: claude)

## ADR 리뷰 결과: ADR-174 Paragraph 수명 retained 전환 (round 1)

### 구조 검증

| 섹션 | 판정 | 품질 | 인용 |
| --- | --- | --- | --- |
| Context (HC 5개 측정 가능) | PASS | HIGH | ADR:9-23 |
| Alternatives 3개 + 4축 | PASS | HIGH | ADR:32-60 |
| Risk Threshold Check + 루프 판정 | PASS | HIGH | ADR:62-70 |
| Decision 수용 근거 4 + 기각 사유 2 | PASS | HIGH | ADR:72-86 |
| 구현 상세 분리 (breakdown 포인터) | PASS | HIGH | ADR:88 |
| Risks (Decision 뒤/Gates 앞, ID 표 + HIGH 없음 명시) | PASS | HIGH | ADR:90-100 |
| Gates G1~G5 | PASS | HIGH | ADR:102-110 |

동적 seed: 코드 경로 4곳 인용 (breakdown §선차단) / Generator·BC 해당 없음 명시 / M3 Phase 0 흡수 명시 — PASS.

### 코드 검증 (8/8 VERIFIED)

| 주장 | 판정 | 근거 |
| --- | --- | --- |
| 고정 상한 1,000 + env override | VERIFIED | nodeRendererState.ts:14-21 |
| 퇴거 시 프레임 중 즉시 delete | VERIFIED | nodeRendererText.ts:80-87 (85 oldest?.delete()) |
| registerSkiaNode data identity 변경 시 bump (R1 anchor) | VERIFIED | useSkiaNode.ts:41-44 + 47 invalidateNodePicture (per-node invalidation 선례) |
| unregister 경로 존재 | VERIFIED | useSkiaNode.ts:51-55 |
| SkiaRenderer.render + flush (drain 부착점) | VERIFIED | SkiaRenderer.ts:839 render, 176/435/584/826 flush |
| 49d71dbd3 폐기 지연 5종 테스트 계보 | VERIFIED | git show 49d71dbd3 (deferredDisposal.ts + nodePictureCache.deferredDisposal.test.ts) |
| canvas-rendering.md §3 문구 (정정 대상) | VERIFIED | .claude/rules/canvas-rendering.md §3 |
| Pen §3-4-1 / 기준선 §8 존재 | VERIFIED | commit c3e312338 |

### 발견 이슈 (round 1 에서 즉시 수정 — 전건 fixed)

- **M1 (MEDIUM)**: 현행 캐시는 content 키 (nodeRendererText.ts:130 — processedText+layoutMaxWidth+font, 노드 id 미포함) 로 노드 간 dedup. per-node retained 전환은 이 공유를 소실 — 중복 라벨 다수 페이지에서 배수 증가 가능한데 Phase 0 에 중복 계수 실측 항목 부재. → fix: breakdown Phase 0 항목 5 (중복 계수) + Phase 2 소유 모델 분기 (refcount 공유 variant) + ADR G1/R2 보강.
- **M2 (MEDIUM)**: 프레임 중 delete 지점이 퇴거 외 2곳 더 존재 — same-key 교체 existing.delete() (nodeRendererText.ts:73-77), fontMgr 변경 일괄 clear 가 renderText 내부 즉시 delete (97-100 → 46-52). retained invalidation 축에 fontMgr 미기재. → fix: breakdown Phase 0 항목 6 (delete 3지점 전수) + Phase 2 invalidation 축 2개 명시.
- **L1 (LOW)**: 문턱 변수 표현 — 엄밀히는 draw 수가 아니라 고유 캐시 키 수 (동일 키 draw 는 히트). → fix: ADR Context 정정 (:9).

### 누락 위험 탐색

렌더링 파리티 (D3 불변) / layoutVersion 5-심볼 체인 (store 키 무변) / 상태 동기화 / SSOT — 해당 경로 grep 결과 추가 위험 0건.

### 종합 판정

- 구조: PASS (7/7 HIGH)
- 코드 정합: 8/8 VERIFIED
- 이슈: MEDIUM 2 / LOW 1 — 전건 round 1 내 fixed (같은 커밋)
- 결론: **승인 가능** (Accepted 승격 조건 충족)
