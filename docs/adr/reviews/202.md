---
adr: 202
title: Builder AI compiler-first 명령 실행
reviews:
  - round: 1
    ts: "2026-09-15T14:08:54.766Z"
    reviewer: claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: other
        summary: >-
          Context·breakdown §2 의 'create_element type enum 은 수동 COMPONENT_TAGS'
          는 stale — 09-09 prompt audit 로 이미 catalog placeable 파생 + 정적 가드 (G1 '수동
          enum 재도입 RED' 부분 충족)
        evidence: >-
          apps/builder/src/services/ai/tools/definitions.ts:36-52 ·
          definitions.test.ts:31
        root_cause: >-
          2026-09-02 코드 read 이후 `01fbab325` (PROMPT_AUDIT D1~D6) 가 enum 파생을 먼저
          반영. Phase 0 재freeze 항목이지만 본문 Context 가 현재 사실과 다름
        outcome: fixed
        addressed_in: round 2; ADR-202 Context/G1; breakdown §2/§3-2/Phase 0-1
      - id: m2
        severity: MEDIUM
        category: ssot-violation
        summary: >-
          HC3·§3-2 command manifest 파생원이 ADR-196 COMMAND_META 하나로 적혀 있으나,
          ADR-213 Phase 5 이후 agent 명령 표는 COMMAND_META +
          DATA_COMMAND_META/DATA_AGENT_COMMANDS 두 개이고 listAgentCommands 가 병합한다 —
          manifest 는 병합 descriptor 를 읽어야 두 번째 정본을 만들지 않는다. 같은 이유로 HC5/HC8 '단일 실행
          표면·승인 우회 0' 에 dispatchDataProposal (AI 데이터 쓰기 유일 진입점 ·
          requestAgentCommandConfirmation 승인) 이 빠져 있고, tool registry 도 10 이 아니라
          20 executor
        evidence: >-
          apps/builder/src/services/agent/executeAgentCommand.ts:15,29-53,106-125
          · apps/builder/src/services/ai/data/dataProposalDispatcher.ts:100-120
          · apps/builder/src/services/ai/tools/index.ts:51-71
        root_cause: >-
          ADR-213 (Implemented 09-12) 가 202 초안 뒤에 executor 표면을 확장. 방향 (기존
          executor 소비, 재구현 0) 은 그대로이나 파생원·승인 불변식 목록이 좁다
        outcome: fixed
        addressed_in: round 2; ADR-202 HC3/HC5/HC8/G3; breakdown §3-2/§3-4/§6-3
      - id: m3
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          initial 번들 상한 gate 부재 — compiler core·manifest·alias/recipe registry 는
          client 모듈인데 G5 는 'provider SDK·Markdown index 포함 0' 만 본다. 프로젝트 정본은
          ADR-219 재승인 Builder ≤ 1,319,829 / Preview ≤ 675,021 B gzip (만료
          2026-10-14) + ADR 별 순증 한도 관행 (211: 7 KiB · 219: 3.5 KiB). AI
          패널/services 가 initial 청크인지도 Phase 0 실측 항목이어야 한다 (BuilderCore.tsx:303 은
          devAgentEntry 만 lazy)
        evidence: >-
          docs/adr/202-…md §Gates G5 · docs/adr/completed/219-*.md:33,104 ·
          apps/builder/src/builder/main/BuilderCore.tsx:303
        root_cause: 202 는 시간·호출 수 축만 측정 무결성에 넣고 번들 축을 빠뜨림. 6-2 정적 검증 매트릭스에도 번들 행 없음
        outcome: fixed
        addressed_in: round 2; ADR-202 HC12/R9/G5; breakdown §3-6/§6-5
      - id: l4
        severity: LOW
        category: other
        summary: >-
          R3/G3 의 composite 유실 축이 부정확 — composite early return 에서도 canonical 필드는
          applyCanonicalFields 로 적용된다. 유실은 props/styles/fills (finalProps 가 leaf
          경로 :158 에만 쓰임) 와 slot 뿐. G3 '…canonical/slot routing 손실 0' 의 canonical
          은 이미 충족 상태로 RED 재현 대상에서 제외
        evidence: "apps/builder/src/services/ai/tools/createElement.ts:89,117-150,158"
        root_cause: 09-02 read 이후 canonical patch 경로가 composite 에도 배선됨
        outcome: fixed
        addressed_in: round 2; ADR-202 R3/G3; breakdown §2/§6-2
      - id: l5
        severity: LOW
        category: evidence-missing
        summary: >-
          one-shot LLM IR (G4) 의 schema-constrained 출력은 AnthropicProvider
          responseSchema (output_config json_schema) 에만 있고
          OpenAI-compatible/Ollama 어댑터에는 없다 — Ollama 폐쇄망에서 one-shot IR 은
          prompt-only JSON. G4 '불안정 provider 는 Agent fallback' 조항이 이미 덮으므로 Phase
          0 기준선에 사실만 기록
        evidence: >-
          apps/builder/src/services/ai/providers/AnthropicProvider.ts:244-245 ·
          providers/LLMProvider.ts:117
        root_cause: >-
          PROMPT_AUDIT 구조화 출력이 anthropic 한정으로 반영 (메모리
          project-prompt-audit-2026-09-applied)
        outcome: fixed
        addressed_in: round 2; breakdown §2/Phase 0/§7
    hate:
      assumption: >-
        기존 tool/ADR-196 executor 가 정적 '단일 실행 표면' 이라 compiler 는
        routing/validation 만 얹으면 된다
      root: >-
        09-02 이후 executor 표면이 2배 (20 도구 · 데이터 승인 dispatcher · command 표 2개) 로 늘어
        '단일 표면' 의 경계 자체가 움직인다
      axis: manifest 파생원 완결성 (HC3) 과 승인 우회 0 (HC5/HC8)
      first_nail: >-
        listAgentCommands() 가 COMMAND_META 하나만 읽는가 → 아니다, DATA_COMMAND_META 병합
        (executeAgentCommand.ts:106-125)
      verdict: "issue:2"
  - round: 2
    ts: "2026-09-15T14:15:40.379Z"
    reviewer: codex
    source: live
    issues: []
---

# ADR-202 Review Log

## Round 1 — 2026-09-15 (reviewer: claude)

## ADR 리뷰 결과: ADR-202 Builder AI compiler-first 명령 실행 (round 1, 2026-09-15)

**판정: 방향 유지 · Accepted 승격은 MEDIUM 3 반영 후.** 대안 B 선택과 4질문 lock-in (breakdown §1: 134 base / D6~D8 specialization / 196 소비 / 조기 판독) 은 현재 코드와 정합한다. HIGH 0.

### 코드 사실 대조 (2026-09-02 read → HEAD 74eb5be22)

| 본문 주장                                                                         | HEAD                                                                                                                                                      | 판정      |
| --------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- |
| useAgentLoop 가 Agent-first, fallback 은 예외·Agent 부재 뒤 (`:99-130, :224-229`) | 동일                                                                                                                                                      | ✓         |
| runFallback 은 metadata-only, mutation 0 (`:73-83`)                               | 동일                                                                                                                                                      | ✓         |
| AgentService MAX_TURNS 10 (`:32,:73`)                                             | 동일                                                                                                                                                      | ✓         |
| IntentParser.extractStyles 빈 객체 (`:317-321`) · 소비처 useAgentLoop 1곳         | 동일                                                                                                                                                      | ✓         |
| create_element type enum 은 수동 COMPONENT_TAGS                                   | **catalog placeable 파생 + 정적 가드** (`definitions.ts:36`, `definitions.test.ts:31`)                                                                    | ✗ issue 1 |
| composite early return 이 props/styles/fills/canonical/slot 유실                  | canonical 은 `applyCanonicalFields` 적용 (`createElement.ts:127`), props/styles/fills 만 유실 (`finalProps` :158 leaf 전용)                               | △ issue 4 |
| executor 10개 + run_command lazy                                                  | **20개** (213/214 데이터 도구 추가) + lazy 유지 (`tools/index.ts:37-71`)                                                                                  | △ issue 2 |
| command manifest 파생원 = ADR-196 COMMAND_META                                    | COMMAND_META + **DATA_COMMAND_META/DATA_AGENT_COMMANDS** 병합 (`executeAgentCommand.ts:106-125`) · AI 데이터 쓰기는 `dispatchDataProposal` 승인 단일 진입 | ✗ issue 2 |
| batchDesign async transaction 한계                                                | 동일 (`batchDesign.ts:7-10,60,110`)                                                                                                                       | ✓         |
| ADR-134 base (Implemented) · 196 Implemented · apps/desktop 부재                  | 134 Implemented 08-29 · 196 Implemented 08-28 · apps/ = builder, publish                                                                                  | ✓         |
| 구조화 출력                                                                       | Anthropic 만 responseSchema (`AnthropicProvider.ts:244`)                                                                                                  | issue 5   |

### 구조 (adr-writing.md)

Context→Alternatives(5, 4축)→Threshold→Decision→포인터→Risks(R1~~R8)→Gates(G0~~G6)→Consequences ✓ · D1/D2/D3 위치 명시 ✓ · 기각 사유 ✓ · HIGH 0 이라 코드 경로 3곳 인용 의무 N/A · BC 는 runtime-only/migration 0 ✓ · fork confirm 은 09-02 사용자 초안 승인 기록 ✓.

### 측정 무결성

Q1~Q5 기재 ✓. 핵심 판정을 시간이 아니라 provider/agent/network **count = 0** + palette/human oracle 로 둔 것은 기기·모델 독립이라 타당. 빠진 축 = 번들 (issue 3).

### 이슈

1. **MEDIUM** Context 의 '수동 enum' stale — G1 의 '수동 enum 재도입 RED' 가 이미 부분 충족. Context·breakdown §2·Phase 1 범위를 '파생은 존재, contract map 으로 schema·executor·capability join 만 추가' 로 갱신.
2. **MEDIUM** manifest 파생원과 승인 불변식 확장 — HC3/§3-2 는 `listAgentCommands()` 병합 descriptor 를 파생원으로, HC5/HC8 은 `dispatchDataProposal` 승인을 우회 금지 목록에 추가. direct compiler 가 data.* / bind_element 를 다루면 반드시 이 dispatcher 경유. 6-3 sensitivity 에 'DATA_COMMAND_META 항목 confirm 변경 → direct schema 반영' 행 추가.
3. **MEDIUM** 번들 gate — G5 에 Builder initial 순증 상한 (KiB) + 절대 상한 ADR-219 재승인값 참조 + compiler/recipe registry lazy chunk 여부 판정 추가. Phase 0 에 AI 패널·services 가 initial 인지 실측.
4. **LOW** R3/G3 유실 축을 props/styles/fills/slot 으로 정정 (canonical 제외).
5. **LOW** Phase 0 기준선에 'responseSchema 는 anthropic 한정' 기록.

### 검증 범위·한계

- 읽은 것: ADR 본문 전체 · breakdown 전체 · adr-writing.md · 인용 코드 12 파일 (grep + 발췌).
- UNVERIFIED: AI 패널이 initial 청크인지 (번들 실측 안 함) · Ollama 에서 prompt-only JSON 의 실제 유효율 · 팔레트 canonical 과 createCompositeElement 결과의 실제 parity (G2 에서 측정).
- production 재현 없는 커버리지 지적은 없음 — 5건 전부 문서 사실 정정 또는 gate 누락.

## Round 2 — 2026-09-15 (reviewer: codex)

## Round 1 수리 검증 — 이슈 1~5 fixed

**판정: 방향 유지 · 문서 이슈 5건 해소 · 추가 이슈 0.** round 1의 outcome을 모두 `fixed`로 종결했다. 대안 B와 134 base / D6~D8 specialization / 196 소비는 유지한다. ADR 상태는 **Proposed**이며, Accepted 승격의 문서 수리 조건을 충족했다. 구현 gate 통과나 상태 승격으로 해석하지 않는다.

| 원 이슈 | 반영 및 검증 근거                                                                                                                                                                                                                                                                                                                                   |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| m1      | Context/G1 및 breakdown §2·§3-2·Phase 0/1: catalog placeable enum 파생과 `definitions.test.ts:31` 가드를 기존 구현으로 기록. 신규 범위는 schema·executor·capability join과 IR projection.                                                                                                                                                           |
| m2      | HC3/HC5/HC8/G3와 breakdown §3-2·§3-4·§6-3: `listAgentCommands()` 병합 descriptor 소비, `DATA_COMMAND_META` args/confirm sensitivity, `dispatchDataProposal` 승인 경유·거부 시 mutation 0. `data.runEndpoint`의 인자별 confirm과 `data.importPaste` dispatcher 위임을 구분한다.                                                                      |
| m3      | HC12/R9/G5와 breakdown §3-6·§6-5: Builder initial 순증 ≤3.5 KiB gzip·Preview 0 B를 ADR-202 제안 예산으로 추가. ADR-219 재승인 절대 상한 Builder 1,319,829 / Preview 675,021 B gzip·만료 2026-10-14를 함께 적용. production graph/network로 initial/lazy 전이 의존성을 판정하고 lazy 크기·첫 AI 로드 비용을 별도 기록한다. 초과/만료 자동 갱신 금지. |
| l4      | R3/G3와 breakdown §2·§6-2: props/styles/fills/slot은 유실 수리, 이미 적용되는 canonical fields는 보존 검사로 분리.                                                                                                                                                                                                                                  |
| l5      | breakdown §2·Phase 0·§7: `responseSchema` 전송은 현재 Anthropic 어댑터만 지원. OpenAI-compatible/Ollama 어댑터의 prompt-only JSON 경로와 유효율 측정 과제를 명시. `providers.test.ts:818`도 미전송 계약을 확인한다.                                                                                                                                 |

### 원 리뷰의 executor 수 정정

HEAD `dbb2cdb0d`에서 `tools/index.ts`의 `createToolRegistry()` 등록 배열을 직접 계수한 결과는 **19개**다 (`run_command` lazy adapter 포함). round 1의 **20개** 표기는 정정하며, 당시 본문은 이력 보존을 위해 수정하지 않는다. 09-02 초안의 10개 표기가 stale하다는 판단과 병합 descriptor/승인 경로 보강은 유효하다.

### 검증과 경계

- 변경한 ADR 본문·breakdown의 범위 지정 `codex:format`, `codex:guard`, `git diff --check` PASS.
- round 1 본문은 보존하고 frontmatter의 outcome/수리 참조만 갱신했다. 이 round는 문서 수리 검증 기록이다.
- 제품 코드·Properties 동시 변경·ADR 상태·commit/push는 변경하지 않았다. 문서 수정으로 TS/브라우저 검증은 해당 없음. 전체 dirty 파일을 포맷하는 preflight 대신 문서 범위 검증을 사용했다.
- UNVERIFIED 유지: AI 패널/services의 production initial/lazy 실측, Ollama prompt-only JSON 유효율, 팔레트 canonical parity. 각각 G0/G5, G0/G4, G2의 구현 단계 과제이며 이번 문서 검증의 PASS 대상이 아니다.
