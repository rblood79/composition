---
adr: 169
title: 컨테이너 intrinsic 크기 산출 (min/max-content)
reviews:
  - round: 1
    ts: '2026-07-27T01:13:47.418Z'
    reviewer: claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: other
        summary: >-
          TS minWidth 주입이 §4.5 automatic minimum 분기를 무력화해 Phase 2 fix 가
          width:fit-content 컨테이너에서 masking
        evidence: >-
          apps/builder/src/builder/workspace/canvas/layout/engines/utils.ts:4767-4769
          · packages/composition-engine/src/flex.rs:292 · utils.ts:4490,4479
        root_cause: >-
          needsWidth 가 leaf 태그군 외에 intrinsic width 키워드를 선언한 컨테이너까지 포함해
          minWidth=ceiledWidth 를 주입 → min_main == AUTO 가 거짓이 되어 auto-min 경로 자체가
          실행되지 않음. 방치 시 '상한을 하한으로' 패턴이 explicit min 채널로 재유입
        outcome: fixed
        addressed_in: e73f6091a
      - id: m2
        severity: MEDIUM
        category: alternative-strawman
        summary: 대안 A 의 정확도 근거와 R4 의 근사 인정이 충돌 — B 기각 논거가 'A 는 정확' 으로 읽힘
        evidence: 'docs/adr/169-container-intrinsic-sizing-propagation.md:35 vs :110'
        root_cause: >-
          A 도 착수 시점에는 CSS-FLEXBOX-1 §9.9.3 완전 구현이 아니라 근사인데, 기각 사유가 근사 여부로 서술돼 B
          를 실제보다 불리하게 대비시킴. 실제 갈림은 수렴 경로의 유무
        outcome: fixed
        addressed_in: e73f6091a
      - id: l3
        severity: LOW
        category: other
        summary: G4 통과 조건의 성능 상한이 미정이라 객관적 실패 판정 불가
        evidence: 'docs/adr/169-container-intrinsic-sizing-propagation.md:124'
        root_cause: >-
          baseline 실측 전이라 수치를 못 박을 수 없는 것은 타당하나, 상한 기록 시점과 미기록 시 차단 규칙이 없어 게이트가
          무력화될 수 있었음
        outcome: fixed
        addressed_in: e73f6091a
---
# ADR-169 Review Log

## Round 1 — 2026-07-27 (reviewer: claude)

## ADR 리뷰 결과: ADR-169 컨테이너 intrinsic 크기 산출 (min/max-content)

### 구조 검증

| 섹션 | 판정 | 품질 | 인용 |
| --- | --- | --- | --- |
| Context (측정 가능 hard constraint) | PASS | HIGH | `ADR:17-23` — HC2/HC3 이 실측 수치(발산 0 / 324·105·867) |
| Alternatives (2개+, 4축) | PASS | HIGH | `ADR:32-72` — A/B/C/D 4개, 각 4축 전부 평가 |
| 4축 위험 평가 | PASS | HIGH | `ADR:36-41,46-51,57-62,67-72` — missing axis 0 |
| Risk Threshold Check | PASS | HIGH | `ADR:73-82` — 표 + 루프 판정, B 가 루프 산물임 명시 |
| Decision 위험 수용 근거 | PASS | HIGH | `ADR:88-100` — 근거 4 + 기각 3건 |
| 구현 상세 분리 | PASS | HIGH | `ADR:102` design 포인터, Phase/파일표/체크리스트는 breakdown |
| Risks (Decision 뒤 / Gates 앞, ID 표) | PASS | HIGH | `ADR:104-115` R1~R8 |
| Gates | PASS | HIGH | `ADR:117-127` G1~G6 |

동적 seed: #1 코드 경로 인용 PASS (Context 7 앵커) · #2 Generator 확장 N/A (엔진 내부, `ADR:15` 스키마 무변경 선언) · #3 BC 수식화 — 스키마 변경 없음, 사용자 문서는 IndexedDB/Supabase 라 정적 계수 불가 → R7 이 live 표본으로 위임한 것이 타당 · #4 HIGH+ 0 이라 분리 질문 N/A · #5 추정 vs 실측 gap 없음 (Phase 0 실측 완료 후 착수).

### 코드 검증

| 주장 | 판정 | 근거 |
| --- | --- | --- |
| `tree.rs` 인용 7곳 (92/621/639/652/1930/2423/2445) | VERIFIED | sed verbatim 일치 |
| `flex.rs:99` FLEX_FIELD_COUNT=20 / `flex.rs:288-293` floor fallback | VERIFIED | `flex.rs:293` `if content_min_main > 0.0 { .. } else { content_main }` |
| "센티넬 도메인 확장이라 호출부 전수 변경 없음" (`ADR:34`) | VERIFIED | `INDEFINITE_AVAIL` 7 사용처 전부 **생산 측**, 등가 비교 0건. 소비 가드는 전부 `avail >= 0.0` (`tree.rs:963,970,1008,1010,1251,1400,1407,1575,1582`) → -2/-3 도 동일 indefinite 경로 |
| "off 19 는 텍스트 leaf 전용, 컨테이너는 항상 absent" (`ADR:11`) | VERIFIED | `utils.ts:4695-4696` `const suppliesIntrinsicScalars = TEXT_LEAF_TAGS.has(type); if (suppliesIntrinsicScalars && ...)` |
| baseline Rust 324 (`ADR:21`) | VERIFIED | `cargo test` 297+15+11+1 = 324 |
| baseline parity 13 files / 105 (`ADR:20`) | VERIFIED | `vitest --config vitest.browser.config.ts` → 13 passed / 105 passed |
| baseline builder 867 (`ADR:21`) | VERIFIED | `vitest run src/builder/workspace/canvas` → 867 passed (+4 skipped) |
| 프리셋 3종 flex row 형태 (`ADR:13`) | VERIFIED | `presetDefinitions.ts:194/229/267` |

8/8 VERIFIED.

### 위험 평가

| 위험 | 증거 | 심각도 | 상태 |
| --- | --- | --- | --- |
| R1~R7 (캐시 부재 지수 비용 / 부분 반영 악화 / 센티넬 오염 / §9.9.3 근사 / grid 이연 망각 / height 축 잠식 / 기존 문서 이중 보정) | `ADR:104-114` | MED·LOW | 기재됨 |
| **TS `minWidth` 주입이 §4.5 auto-min 무력화** | `utils.ts:4767-4769` + `flex.rs:292` + `utils.ts:4490,4479` | MED | **누락 → R8 신설(fixed)** |
| golden.rs fixture churn | `tests/golden.rs:26,96` — `flex_layout(&data,..)` 직접 호출이라 `solve_flex` 측정 변경과 무관 | — | 위험 아님 |
| layoutVersion 5-심볼 2계층 체인 | 신규 style/prop 키 0 (센티넬은 runtime available 값) | — | 해당 없음 |
| Skia↔CSS 파리티 | 엔진은 Builder 전용 소비자, Preview/Publish 는 실 CSS → 수정은 발산 축소 방향 | — | 위험 아님 |

### 종합 판정

- **구조**: PASS (8/8 섹션 HIGH)
- **코드 정합**: 8/8 VERIFIED
- **누락 위험**: 1건 (MED 1) → R8 로 반영 완료
- **권고**
  - HIGH: 없음
  - MEDIUM: (1) R8 신설 — TS minWidth 주입 masking. (2) 대안 A 정확도 위상 — A 도 착수 시점 §9.9.3 근사인데 B 기각 논거가 "A 는 정확" 으로 읽힘 → "수렴 경로 유무" 로 재서술. **둘 다 `e73f6091a` 에서 반영**
  - LOW: G4 상한 미정 → "Phase 1 종료 시 baseline·상한 기록, 미기록 시 Phase 2 진입 금지" 로 절차화 (`e73f6091a`)
  - **결론: 승인 가능**

### 자기 리뷰 편향 고지

본 ADR 은 리뷰어와 작성자가 동일하다. 그 편향을 상쇄하려고 (a) 본문 인용 전수를 `sed` verbatim 으로 재대조하고 (b) baseline 3종을 실제 실행해 확인했으며 (c) 대안 A 자신의 정확도 주장을 R4 와 교차 검증해 **A 에 불리한 이슈 1건(MEDIUM)** 을 스스로 제기했다. 그럼에도 대안 선정 자체의 편향은 완전히 배제되지 않으므로, Phase 1 G1(동작 무변경) 이 실패하면 대안 재평가로 즉시 복귀하는 것을 권한다.
