---
adr: 923
title: display 이원 계약 — TS IFC 시뮬레이션 제거·엔진 outer/inner 직결 (C′)
reviews:
  - round: 1
    ts: "2026-08-31T07:08:45.026Z"
    reviewer: codex
    source: live
    issues:
      - id: h1
        severity: HIGH
        category: evidence-missing
        summary: >-
          outer/inner display 미분리 — catalog Button 은 부모 판정에서 inline-block(손 목록),
          자식 style 로는 catalog flex 를 받고, 엔진은 문자열 "inline-block" 만 line item 으로
          봐서 r0 의 '단일 display 문자열 직결' 은 Button 을 block item 으로 만들어 catalog 발산을
          악화시킨다 (load-bearing 전제 실패)
        evidence: >-
          packages/shared/src/catalog/generated/componentRulesTable.ts:915
          (display:"flex") vs
          packages/shared/src/components/styles/Button.css:14 (inline-flex);
          apps/builder/src/builder/workspace/canvas/layout/engines/fullTreeLayout.ts:1795
          (getElementDisplay→INLINE_BLOCK_TAGS) vs :1031
          (resolveContainerStylesFallback);
          packages/composition-engine/src/tree.rs:4609 ("inline-block" 문자열 일치);
          display.rs parse_display 의 tree.rs 참조 0
        root_cause: >-
          r0 가 '엔진이 line box 를 구현했다' 는 사실만 보고 '엔진에 무엇이 도달하는가'(소비 경로) 를 wasm 경계에서
          확인하지 않았다. 2026-06-27 패널 결함을 catalog Canvas 전용 override 로 덮은 우회가
          display 를 세 소비자에서 다르게 만들었다.
        outcome: fixed
        addressed_in: 2fbd389da
      - id: h2
        severity: HIGH
        category: other
        summary: >-
          INLINE_BLOCK_TAGS 를 catalog display SSOT 로 대체하는 G4 는 다른 개념의 비교 — 이 집합은
          기본 display 목록이자 intrinsic width/height 측정을 켜는 escape 목록(self-render
          leaf 포함) 겸용. 손 목록 24 vs catalog inline-* 30
        evidence: >-
          apps/builder/src/builder/workspace/canvas/layout/engines/utils.ts:4400
          (목록), :4570-4572 (needsWidth 게이트)
        root_cause: >-
          이름이 display 처럼 보여 한 개념으로 읽음. default display · 부모 flow 참여 · intrinsic
          측정 capability 세 역할이 한 집합에 겹쳐 있었다.
        outcome: fixed
        addressed_in: 2fbd389da
      - id: h3
        severity: HIGH
        category: other
        summary: >-
          baseline 을 NodeStyle 입력으로 넣는 설계가 역방향 — baseline 은 자식 레이아웃·텍스트 측정이 끝난 뒤
          나오는 출력. 현재 tree.rs 가 0/AUTO 를 넣고 block.rs 가 그 값으로 줄 위치를 잡으며, TS
          calculateBaseline 은 production caller 0 이고 중첩 inline-flex 의 마지막
          in-flow baseline 을 표현 못 함
        evidence: >-
          packages/composition-engine/src/tree.rs:4590 (0/AUTO 전달);
          packages/composition-engine/src/block.rs:370-390 (baseline 소비);
          apps/builder/src/builder/workspace/canvas/layout/engines/utils.ts:5121
          (calculateBaseline, re-export 만)
        root_cause: >-
          block.rs 필드 계약(FIELD_COUNT=19) 을 입력 목록으로만 읽고, CSS 2.1 §10.8 / Flexbox
          §8.5 의 baseline 산출 방향(자식→부모) 을 확인하지 않았다.
        outcome: fixed
        addressed_in: 2fbd389da
      - id: m4
        severity: MEDIUM
        category: migration-cost-unquantified
        summary: >-
          hydration migration chain 은 단일 ingress 가 아니고(패널 updateSelectedStyle ·
          AI updateElementProps 가 store 직접 쓰기) persist-back 이 IndexedDB 를 덮어써
          원값이 사라진다 — S4 inline→inline-block 정규화는 세션 중 유입을 못 막고 롤백 불가
        evidence: >-
          apps/builder/src/builder/panels/styles/hooks/useStyleActions.ts:51;
          apps/builder/src/services/ai/tools/updateElement.ts:98;
          apps/builder/src/builder/hooks/usePageManager.ts:385
        root_cause: >-
          ADR-130/913 의 hydration migration 선례를 '단일 chokepoint' 로 일반화했으나 그 선례들은
          factory 잔재 strip(멱등·의미 무손실) 이었고, 값 정규화(의미 변경) 에는 원값 보존과 live 경로 커버가 따로
          필요하다.
        outcome: fixed
        addressed_in: 2fbd389da
      - id: m5
        severity: MEDIUM
        category: other
        summary: >-
          substitution registry 의 감시 단위(display class 변경) 가 너무 좁다 — S7
          float/writing-mode, S8 grid 의미는 class 변경이 아니라 속성 무시라 잡히지 않는다. property
          × value × engineSupport × policy × oracle capability matrix 필요
        evidence: ADR-923 r0 HC1 / G5 문구; taffyDisplayAdapter.ts 규칙 1-4 는 display 만 다룸
        root_cause: >-
          이번 발산이 display 치환에서 났기 때문에 감시 단위를 display 로 잡았다. silent ignore 는 다른
          부류.
        outcome: fixed
        addressed_in: 2fbd389da
      - id: l6
        severity: LOW
        category: other
        summary: >-
          G1 의 '2회 실패 후 선언된 치환으로 강등' 분기는 게이트를 vacuous 하게 만드는 탈출구 — 실패 = cutover
          차단으로 단순화
        evidence: ADR-923 r0 Gates G1 실패 시 대안
        outcome: fixed
        addressed_in: 2fbd389da
    hate:
      assumption: block.rs 가 inline-block line box 를 구현했으므로 TS 시뮬레이션만 걷어내면 엔진 경로에 직결된다
      root: >-
        catalog Button 은 엔진에 flex 로 도달하고 엔진은 문자열 "inline-block" 만 line item 으로
        본다 — 직결하면 Button 이 block item 이 되어 발산이 악화된다
      axis: >-
        소비 경로 (measurement-validity Q4) — '엔진에 무엇이 도달하는가' 를 wasm 경계에서 읽지 않고 TS 쪽
        사실로 추정
      first_nail: >-
        style 없는 catalog Button 1케이스: wasm 경계 display 값 · block 부모에서 line item
        여부 · subtree 가 flex solver 를 타는지 — 현재 (a)=flex, (b)=부모가 flex 로 바뀜
      verdict: "G0 커버 | issue:h1"
  - round: 2
    ts: "2026-08-31T07:47:42.606Z"
    reviewer: claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          Button(inline-flex) 이 실제로 타는 TS flex/grid 분기가 outer=inline 을 지우는 3
          사이트(TaffyFlexEngine inline-flex→flex, fullTreeLayout inline-grid→grid,
          toTaffyDisplay taffyDisplay:parsed.inner) 가 breakdown Phase 5 제거 목록·§4
          파일 변경표에 없다 — 계획대로 해도 엔진은 "flex" 를 받아 Button 이 block item (r0 실패 모드
          잔존). 현재도 사용자 지정 inline-flex Button 이 같은 경로로 Skia 세로 적층
        evidence: >-
          apps/builder/src/builder/workspace/canvas/layout/engines/TaffyFlexEngine.ts:110-117;
          apps/builder/src/builder/workspace/canvas/layout/engines/fullTreeLayout.ts:1060-1061,
          :1077-1084;
          apps/builder/src/builder/workspace/canvas/layout/engines/taffyDisplayAdapter.ts:510-512;
          breakdown:130-138 (Phase 5), :148-167 (§4 표); ADR:46 (HC1), :128 (비용
          서술)
        root_cause: >-
          r1 이 소비 경로(Q4) 를 toTaffyDisplay(block 계열 분기) 에서만 읽었다 —
          fullTreeLayout.ts:1060 이 flex/inline-flex 를 먼저 잡아 별도 분기로 보내므로 Button 은
          그 자리를 지나지 않는다. r1 h1 과 같은 부류의 누락이 flex 분기에서 반복
        outcome: fixed
        addressed_in: 820d8a0e2
      - id: m2
        severity: MEDIUM
        category: other
        summary: >-
          Phase 4 단독 commit 이 live 회귀를 만든다 —
          getElementDisplay(Button)=inline-flex 가 되면 TS classifyChildDisplay 가
          inline-flex 를 block 으로 분류해 부모 IFC 시뮬레이션이 해제되고, 자식은 m1 경로로 flex 로 도달 →
          block 부모 안 Button 들이 세로 적층 (오늘은 가로). G4/G5 에 레이아웃 검사 없음, G1 후반/G2 는
          Phase 5 — breakdown 의 "HIGH 귀속 Phase 는 5 하나" 와 충돌
        evidence: >-
          apps/builder/src/builder/workspace/canvas/layout/engines/taffyDisplayAdapter.ts:334-347
          (classifyChildDisplay inline-flex→block), :527-537 (IFC 시뮬레이션 게이트);
          breakdown:87, :122-128 (Phase 4), :127 (의도된 diff); ADR:163-167 (G1/G2
          시점 vs G4/G5 조건)
        root_cause: >-
          Phase 4 를 catalog·패널·목록 분리 작업으로만 보고, getElementDisplay 출력 변경이 어댑터의 부모
          판정(classifyChildDisplay) 을 통해 레이아웃 dispatch 를 바꾼다는 점을 순서 설계에 반영하지 않았다
        outcome: fixed
        addressed_in: 820d8a0e2
      - id: l3
        severity: LOW
        category: evidence-missing
        summary: >-
          패널의 두 번째 display !== "flex" 소비처 useFlexAlignmentKeys(:99) 가 R4/Phase 4
          에 없다 — inline-flex 전환 후 Alignment 9-grid 가 사라진다
        evidence: >-
          apps/builder/src/builder/panels/styles/hooks/useLayoutAuxiliary.ts:99;
          ADR:150 (R4), breakdown:125 (Phase 4 패널 항목); 참고 패턴
          apps/builder/src/builder/panels/styles/sections/TransformSection.tsx:475
        root_cause: "R4 경로 인용이 :69 한 함수에 그쳤다 — 같은 파일 grep 으로 잡히는 두 번째 소비처 미열거"
        outcome: fixed
        addressed_in: 820d8a0e2
      - id: l4
        severity: LOW
        category: other
        summary: >-
          Phase 1 의 순수 inline(outer=inline, inner=flow) 판정이 미명세 — :102
          "outer==Inline 이면 1" 은 순수 inline 을 line item 으로 만들고, :104 "현행대로 block
          격상 테스트 고정" 은 code 0 으로 읽힐 수 있다. 영향은 사용자 지정(DISPLAY_OPTIONS) 뿐
          (catalog·factory display:"inline" 0건)
        evidence: >-
          breakdown:102 vs :104;
          apps/builder/src/builder/panels/styles/constants/styleOptions.ts:70;
          packages/shared/src/catalog/generated/componentRulesTable.ts
          (display:"inline" 0건)
        root_cause: >-
          "block 격상" 이 inner solver(Block) 를 뜻하는지 outer display code 0 을 뜻하는지
          용어를 고정하지 않았다
        outcome: fixed
        addressed_in: 820d8a0e2
    hate:
      assumption: >-
        Phase 5 에서 toTaffyDisplay 의 시뮬레이션만 걷어내면 Button 의 display 는 엔진 경계에 CSS 값
        1개(inline-flex) 로 도달한다 (ADR:46 HC1 / ADR:128 / breakdown:132)
      root: >-
        Button 은 toTaffyDisplay 를 타지 않는다 — fullTreeLayout.ts:1060 이
        flex/inline-flex 를 먼저 잡아 TaffyFlexEngine.ts:110-112 가 "flex" 로 지운다
        (inline-grid 는 fullTreeLayout.ts:1084). 계획의 제거 목록을 전부 수행해도 엔진은 "flex" 를
        받아 Phase 1 규칙(outer==Inline) 에서 block item 이 된다 — r0 실패 모드 잔존
      axis: 소비 경로 (measurement-validity Q4) — r1 h1 과 같은 부류의 누락이 flex 분기에서 반복
      first_nail: >-
        grep -n "result.display = \"flex\"" TaffyFlexEngine.ts 1회 + G0 (a) wasm
        경계 캡처를 Phase 5 후 상태에 적용 — 수 분 / 케이스 1 vs 7 Phase · 7 Gate
      verdict: >-
        G0(a)·HC1·G1 후반 커버 (검출) | issue:m1 (계획 누락) · issue:m2 (Phase 4 중간 상태는
        Gate 부재)
    prism:
      lenses:
        - lens: 정합성/D3 SSOT
          verdict: pass
          reason: >-
            Skia·패널이 같은 top-level containerStyles 를 읽어 override 1곳 수정으로 두
            consumer 가 DOM 값을 받는다 (componentRulesTable.ts:918-921,
            useLayoutValues.ts:52, implicitStyles.ts:291, Button.css:14)
        - lens: 소비 경로/배선 (Q4)
          verdict: fail
          reason: >-
            Button 이 실제로 타는 fullTreeLayout.ts:1060 → TaffyFlexEngine.ts:112 가
            outer 를 지우며 계획에 없다 (m1)
        - lens: 되돌림/rollout 순서
          verdict: fail
          reason: >-
            Phase 4 commit 이 단독으로 live 회귀 (taffyDisplayAdapter.ts:334-347 + m1)
            이고 레이아웃 Gate 는 Phase 5 (m2)
        - lens: 측정 무결성/oracle
          verdict: pass
          reason: >-
            G1 oracle 은 실 Chrome (harness.ts:12,99-101), 어댑터 우회 engineLeg 존재
            (:113-135), 5-질문 명시 (ADR:170-176), G0 가 전제를 실패 테스트로 고정
            (breakdown:91)
        - lens: 비용/성능
          verdict: unclear
          reason: >-
            block 경로 비용 미측정 (ADR:99, HC6) — G3 가 block arm 을 별도 지정하나 수치는 Phase 0
            이후
      convergence: 불일치 — 정합성·측정 렌즈 pass vs 소비 경로·rollout 렌즈 fail (두 fail 은 같은 원인)
      question: >-
        Phase 4 의 catalog inline-flex 전환·getElementDisplay catalog 파생을 Phase 5
        cutover 와 같은 commit 으로 합치고, Phase 5 제거 목록에 flex/grid 분기의 display 정규화 3
        사이트 (TaffyFlexEngine.ts:110-117 / fullTreeLayout.ts:1084 /
        taffyDisplayAdapter.ts:510-512) 를 추가하면 소비 경로·rollout 렌즈가 pass 로 바뀌는가? —
        owner: breakdown Phase 4/5 + ADR:128 1줄
  - round: 3
    ts: "2026-08-31T08:31:48.420Z"
    reviewer: codex
    source: live
    issues:
      - id: r3m1
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          Phase 5 가 inline-flex/inline-grid/inline-block 보존을 요구하지만 TS 운반 타입
          TaffyDisplay(layoutTypes.ts:11)·TaffyDisplayConfig.taffyDisplay(taffyDisplayAdapter.ts:124)
          가 block-level 4 값만 허용 — S9 실행 분기만 고치면 타입 오류 또는 강제 변환 위험
        evidence: >-
          apps/builder/src/builder/workspace/canvas/wasm-bindings/layoutTypes.ts:11,89-91;
          layout/engines/taffyDisplayAdapter.ts:122-124;
          TaffyFlexEngine.ts:110-117; TaffyBlockEngine.ts:117-118; breakdown
          Phase 5 S9 · §4 표
        root_cause: >-
          r2 는 값을 바꾸는 실행 분기는 추가했지만 그 값을 운반하는 TS 타입 계약과 파일 변경표까지 소비 경로를 이어 확인하지
          않았다. 반영: ADR 사실 11 · R11 대응 · breakdown Phase 5 S9 union 확장 · §4
          layoutTypes.ts 행
        outcome: fixed
        addressed_in: 1b3d2e14b
      - id: r3l1
        severity: LOW
        category: evidence-missing
        summary: >-
          G0 (a) 가 build_tree_batch 입력 캡처를 요구하지만 기존 harness 두 진입점(engineLeg 는
          프로덕션 변환 우회, pipelineLeg 는 payload 미노출) 중 어느 것도 캡처 지점을 제공하지 않음
        evidence: >-
          apps/builder/tests/parity/harness.ts:114-135,223-270;
          layout/engines/fullTreeLayout.ts:2376-2384,2464-2472;
          persistentTaffyTree.ts:183-188;
          wasm-bindings/compositionEngine.ts:167-170
        root_cause: >-
          직접 엔진 leg 와 프로덕션 pipeline leg 의 역할은 구분했지만 pipeline 내부 경계 인자를 관측할 spy
          지점을 적지 않았다. 반영: G0 (a) 캡처 지점 = pipelineLeg 중
          CompositionEngineLayout.prototype.buildTreeBatch JSON 인자 spy (ADR G0 행
          · breakdown Phase 0)
        outcome: fixed
        addressed_in: 1b3d2e14b
    hate:
      assumption: >-
        Phase 5 후 Button 의 CSS display 가 "inline-flex" 그대로 엔진 경계에 도달한다 (ADR G1
        행)
      root: >-
        TS 운반 타입 두 union(layoutTypes.ts:11 / taffyDisplayAdapter.ts:124) 이 그 값을
        차단한다 — 실행 분기 수정만으로는 도달 불가
      axis: 소비 경로 (Q4) — 값의 운반 타입 계층
      first_nail: Phase 5 S9 + §4 표에 union 확장 2곳과 focused type-check 1건 추가 (수 분)
      verdict: "issue:r3m1 → fixed 1b3d2e14b. Decision 재개 조건(사실 1–10 반증) 미충족"
  - round: 4
    ts: "2026-08-31T14:19:54.000Z"
    reviewer: codex
    source: live
    issues:
      - id: r4m1
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          (classification) evidence §A 가 Phase 5 후보를 8 로 한정하고 structure 공급 17 을 정합으로 판정했으나
          HC2 의 정확한 display 값 비교로는 ColorPicker·ColorSlider·ColorSwatchPicker·FileTrigger·GridList·
          Skeleton·Tag·TagList 8 이 추가 불일치
        evidence: >-
          docs/adr/evidence/923-phase0-inventory.md:42,:72;
          componentRulesTable.ts:5364 ↔ generated/FileTrigger.css:24;
          componentRulesTable.ts:10405 ↔ Skeleton.css:65; TagGroup.css:32
        root_cause: >-
          초판이 (ii) 를 outer(inline-*) 기준으로만 세고, base 셀렉터 매칭이 compound 변형
          (.react-aria-Skeleton.button) 까지 base 로 잡았다. 반영: §A 정확값 재집계 — 불일치 13
          ((i) 2 · (iii) 1 · structure≠DOM 2 · catalog 없음 8; outer 차이 6 · inner 7), DOM 충돌 3,
          미선언 17, 일치 60. breakdown §6 · ADR Status 로그 · README 로그 동기
        outcome: fixed
        addressed_in: f8ab25a45
      - id: r4l1
        severity: LOW
        category: other
        summary: >-
          (evidence-wrong) catalog inline-* 선언을 29 로 집계 — 독립 재귀 import 는 30
          (structure 25 + composition 1 + delegation 3 + top-level Menu 1); 잘못된 29 가 ADR 사실 7 ·
          breakdown §6 에 반영됨
        evidence: "componentRulesTable.ts:6775 (Menu top-level inline-flex); ADR:25; breakdown:195"
        root_cause: >-
          초판 집계가 top-level containerStyles 경로를 nested 집계에서 제외했다. 반영: ADR 사실 7 원문(30) 복원,
          evidence A-4 · breakdown §6 · README 30 으로 정정
        outcome: fixed
        addressed_in: f8ab25a45
      - id: r4l2
        severity: LOW
        category: other
        summary: >-
          (citation-drift) evidence 가 "ADR 사실 4 의 121 을 123 으로 정정" 이라 적었으나 ADR 본문(현재·5b28b9a0b^)
          에 121 주장이 없다 — 121 은 승인 프롬프트의 수
        evidence: "docs/adr/923-layout-vocabulary-closure.md:22 (grep 121 → 0건)"
        root_cause: >-
          Claude 가 자기 프롬프트의 수를 ADR 본문 인용으로 착각. 반영: evidence 헤더·§0·관찰 1, breakdown §6 A 문구 정정
          (123 실측만 기록)
        outcome: fixed
        addressed_in: f8ab25a45
      - id: r4l3
        severity: LOW
        category: other
        summary: >-
          (citation-drift) evidence §D 가 지목한 raw JSON(10:18:58Z, F 21.0/23.9 · B 90.2/97.2) 이 경로에 없음 —
          이후 test:parity 전체 실행(10:22:40Z, F 22.0/24.9 · B 94.5/104.7) 이 덮어썼다
        evidence: >-
          apps/builder/tests/parity/.artifacts/adr-923-layout-baseline.claude.json (Codex 보존본) ·
          adr-923-layout-baseline.json (Codex 재실행 F 20.1/25.3 · B 92.2/98.0)
        root_cause: >-
          테스트가 고정 경로에 덮어쓰고 문서는 첫 실행 수치를 인용. 반영: evidence §D 에 3 run 전부 기록 + "raw 는 실행마다
          덮어써진다" 명시, run 편차(p95 +5.9%/+7.7%) 가 G3 +5% 보다 크다는 측정 설계 관찰 추가 (Gate 문구는 사용자 판단)
        outcome: fixed
        addressed_in: f8ab25a45
      - id: r4l4
        severity: LOW
        category: other
        summary: >-
          (doc-drift) README 진행 로그는 G0 완료를 기록하는데 README:513 표 행은 r1 상태 · "착수 금지 — Phase 0 선행
          검사·inventory 후" 문구를 유지
        evidence: "docs/adr/README.md:7 vs :513; ADR:5"
        root_cause: >-
          5b28b9a0b 가 로그 줄만 추가하고 표 행을 갱신하지 않았다. 반영: 표 행 상태 "r1~r3 개정 · G0 완료 2026-08-31",
          진입 조건 "Phase 1 진입은 사용자 승인 + 실행 모델 결정 후"
        outcome: fixed
        addressed_in: f8ab25a45
  - round: 5
    ts: "2026-08-31T14:56:02.000Z"
    reviewer: codex
    source: live
    issues:
      - id: r5m1
        severity: MEDIUM
        category: other
        summary: >-
          (HC2 정의) HC2 "catalog display ≠ DOM display rule 수 0" 은 정확값이 맞지만 (C′ 는 inner 로 solver 를
          고르므로 outer 6건만 검사하면 계약을 놓친다), evidence 의 불일치 13 은 catalog fallback 기준 후보이지
          production 전환 목록이 아니다 — factory style 과 applyImplicitStyles 가 catalog 보다 우선한다
        evidence: >-
          ADR:35; display.rs:70; fullTreeLayout.ts:1658; implicitStyles.ts:335; DateColorComponents.ts:587
          (ColorPicker factory display flex); componentRulesTable.ts:5557 (GridList owner 의도적 flex-column);
          TagList.binding.ts:14 (projection shell ↔ DOM contents)
        root_cause: >-
          HC2 원문이 비교 대상 box 와 소비 경로를 지정하지 않아 catalog fallback 표를 그대로 전환 목록으로 읽게
          했다. 반영: HC2 r4 정의 — 같은 의미의 실제 렌더 box 에 대해 production 소비 경로의 exact display 가
          DOM computed display 와 일치, 투영 구조가 다른 box 는 대응 DOM box 명시, 미판정 0. 확정 전환
          Button·ToggleButton 2, 나머지 10 은 Q4 캡처 후 전환/투영 예외 분류, DOM 충돌 3·미선언 17 은 G4
          unresolved 0 전까지 통과 아님. G4 후반·Q4·breakdown Phase 5·§6·evidence 갱신
        outcome: fixed
        addressed_in: 3c2d89e5e
      - id: r5m2
        severity: MEDIUM
        category: other
        summary: >-
          (G3 판정 절차) 같은 코드의 run 편차 (F +5.9% / B +7.7%) 가 Gate 예산 +5% 보다 커 단일 run 판정은 같은
          코드에도 실패한다. N≥3 처럼 열어 두지 말고 결과 확인 전 N=3 고정
        evidence: "evidence/923-phase0-inventory.md §D (3 run p95 F 23.9–25.3 · B 97.2–104.7)"
        root_cause: >-
          G3/HC6 가 단일 baseline 값 대비 +5% 로 쓰여 있었다. 반영: 같은 기기·Chromium 에서 baseline commit 과
          candidate commit 을 교대로 fresh-browser N=3 쌍 측정, run 마다 warm-up 5 + measured 30 p95, arm F/B 각각
          median(after_p95ᵢ / before_p95ᵢ) ≤ 1.05, raw 는 고유 파일 보존, 결과 확인 후 run 추가 금지. 3 run 단순
          중앙값 (F 24.9 / B 98.0) 은 실행 조건 혼합이라 공식 baseline 아님. 하니스에 VITE_ADR923_BASELINE_TAG
          (고유 raw 파일명) 추가
        outcome: fixed
        addressed_in: 3c2d89e5e
      - id: r5l1
        severity: LOW
        category: other
        summary: >-
          (Menu B7) Menu top-level inline-flex 는 HC2 값 예외가 아니라 비교 대상 DOM box 지정 — Canvas Menu 는
          trigger Button 을 그린다는 사용자 결정이 확정·재확인됨
        evidence: "151-breakdown:183 (07-16 결정 · 07-18 재확인); implicitStyles.ts:339 (top-level 대체 = 트리거 박스)"
        root_cause: >-
          evidence 가 "HC2 예외 명시 필요" 로 적어 값 불일치 허용으로 읽혔다. 반영: 비교 대상 = DOM MenuTrigger >
          Button 의 inline-flex, popover 내부 .react-aria-Menu 의 flex 는 비교 제외 — "동일 역할의 box 를 비교"
        outcome: fixed
        addressed_in: 3c2d89e5e
  - round: 6
    ts: "2026-08-31T15:53:37.000Z"
    reviewer: codex
    source: live
    issues:
      - id: r6h1
        severity: HIGH
        category: phase-split-late
        summary: >-
          patchBatchStyleFromImplicit (post-order implicit patch · 2-pass 재-enrich 공유) 가 display 를 raw 복사해
          buildNodeStyle 의 S9 정규화를 덮음 — Label 공통 주입(flexShrink) 이 style 을 clone 하면 inline-flex 가
          wasm 경계에 도달, Phase 1 이후 엔진이 line item 으로 읽어 프로덕션 배치가 phase 경계 밖에서 바뀜
        evidence: >-
          fullTreeLayout.ts:839-877,1964-2012,3019; implicitStyles.ts:3088-3104; persistentTaffyTree.ts:173-188;
          tree.rs write_block_item; 재현 seamDisplayInvariant RED — batch display ["inline-flex","block","block"]
        root_cause: >-
          HC7/seam 검증이 1차 writer(buildNodeStyle) 만 보고 batch 를 다시 쓰는 post-order patch 소비 경로를
          누락했다. 수리: toBatchDisplay (blockify → flex/grid/none 외 block = 1차 writer 규칙) 로 운반 union
          TaffyDisplay 강제 — 수리 전 동작(inline-flex→flex, code 0) 과 등가. HC7 가드
          tests/parity/seamDisplayInvariant.browser.test.ts (Phase 1~4 불변식, Phase 5 에서 S9 와 함께 삭제/반전).
          S9 4번째 사이트로 breakdown §2.1·Phase 5·ADR R11·Decision 등재 + rules/layout-engine.md 금지 패턴
        outcome: fixed
        addressed_in: ef608decf
      - id: r6l1
        severity: LOW
        category: other
        summary: >-
          (evidence-count-drift) breakdown 의 "tree.rs adr923_* 6" 은 실제 7개와 불일치 — 전체 순증가 +8 과 파일별
          테스트 개수를 혼동
        evidence: "breakdown Phase 1 cargo test 항목; tree.rs adr923_* 7개"
        root_cause: "순증가 +8 = 신규 9 (tree 7 + display 2) − 삭제 classify_child 1. 반영: 7 로 정정 + 산식 병기"
        outcome: fixed
        addressed_in: ef608decf
      - id: r6l2
        severity: LOW
        category: other
        summary: >-
          (doc-drift) "호출부 3곳 무변경" (solve_node 는 effective_display 경유로 변경됨) · display.rs 변경을
          "주석 갱신" 으로만 기재 · 모듈 머리말이 삭제된 자식 classifier 를 유지
        evidence: "breakdown Phase 1 항목 2·§4 파일표; display.rs:3-6"
        root_cause: >-
          신규 배선 절만 추가하고 기존 요약·파일표를 함께 갱신하지 않았다. 반영: 호출부 서술 정정, 파일표
          display.rs/fullTreeLayout.ts 행 갱신 + seamDisplayInvariant 행 추가, 모듈 머리말 정정
        outcome: fixed
        addressed_in: ef608decf
---

# ADR-923 Review Log

## Round 1 — 2026-08-31 (reviewer: codex)

## ADR 리뷰 결과: ADR-923 (r0 '레이아웃 어휘 닫기' → r1 'display 이원 계약')

- **경로**: Codex 설계 리뷰 (사용자 전달 2026-08-31). Claude 가 인용된 줄을 전부 코드에서 재확인 — 5건 전부 성립. 추가 발견: `display.rs` 의 `{outer, inner}` 모델이 `tree.rs` 어디에서도 호출되지 않음 (참조 0).
- **판정**:
  - **HIGH h1**: outer/inner 미분리 + display 이중 표현 → 선택안 C 기각, **C′ display 이원 계약** 으로 개정 (**fixed `2fbd389da`**)
  - **HIGH h2**: `INLINE_BLOCK_TAGS` 겸용 → default-display resolver + intrinsic-measurement capability 분리, G5 를 분류표 + `enrichWithIntrinsicSize` diff 0 으로 (**fixed**)
  - **HIGH h3**: baseline 역방향 → `NodeLayout.baseline` 출력 계약, 입력은 `vertical-align`/`line-height` + leaf 텍스트 측정값만 (**fixed**)
  - **MEDIUM m4**: hydration 비단일·비가역 → B 갈래(S4/S7/S8·persisted migration) 를 ADR 에서 **분리** (사용자 결정), breakdown §8 에 비파괴 요건 기록 (**fixed**)
  - **MEDIUM m5**: registry 감시 단위 → capability matrix 로 재정의, 본 ADR 은 seed 선언까지 (**fixed**)
  - **LOW l6**: G1 강등 분기 삭제 → 실패 = cutover 차단 (**fixed**)
- **원문 분류 (category: other)**: h2 = 게이트 조건 실효성(다른 개념 비교) · h3 = 계약 방향 오류(출력을 입력으로) · m5 = 게이트 조건 실효성(감시 단위 협소) · l6 = 게이트 탈출구
- **결론**: 잔존 `pending` 0건. **착수 금지 유지** — G0 선행 검사 1케이스(현재 실패 확증) 가 Phase 1 진입 조건. 사용자 승인 후 Phase 0.

## Round 2 — 2026-08-31 (reviewer: claude)

## ADR 리뷰 결과: ADR-923 display 이원 계약 — TS IFC 시뮬레이션 제거·엔진 outer/inner 직결 (C′) — round 2 (r1 본문 리뷰, reviewer: claude)

> round 1 (codex) 은 r0 를 리뷰해 6건을 r1 커밋 `2fbd389da` 로 종결했다. 본 round 는 **r1 본문·breakdown 자체**를 코드에 대조한 결과다 — round 1 결론을 승계하지 않고 인용된 줄을 전부 다시 열었다.

### 구조 검증

| 섹션                                | 판정 | 품질 | 인용                                                                                                                   |
| ----------------------------------- | ---- | ---- | ---------------------------------------------------------------------------------------------------------------------- |
| Context (D3 선언 + hard constraint) | PASS | HIGH | `ADR:11` D3 선언 · `ADR:44-52` HC 1-7 (측정 가능: override 0 / 차등 ≥12 ≤1px / p95 +5%) · 실측 사실 9건 `ADR:17-27`    |
| Alternatives (5개)                  | PASS | HIGH | A `ADR:63-71` · B `:73-81` · C `:83-91` · C′ `:93-101` · D `:103-107` — 외부 참조 (CSS Display 3 / Dropflow / Blink)   |
| 4축 위험 평가                       | PASS | HIGH | A `:67-71` · B `:77-81` · C `:87-91` · C′ `:97-101` · D `:107` — 누락 축 없음                                          |
| Risk Threshold Check                | PASS | HIGH | `ADR:109-119` 표 + 루프 판정 (C HIGH 2 → C′ 추가, 루프 1회)                                                            |
| Decision 위험 수용 근거 + 기각 사유 | PASS | HIGH | 근거 4 `ADR:125-130` · 기각 A/B/C/D `ADR:132-137` · 분리 결정 (사용자 2026-08-31) `ADR:139`                            |
| 구현 상세 분리                      | PASS | HIGH | `ADR:141` 포인터 → breakdown 205줄 (Phase 0-6 / 파일 변경표 / 검증 계획 / 롤백 / §8 B 갈래)                            |
| Risks (Decision 뒤 / Gates 앞)      | PASS | HIGH | `ADR:143-156` R1-R10, HIGH R1↔G0/G1 · R2↔G2 1:1                                                                        |
| Gates + 측정 조건 5-질문            | PASS | HIGH | `ADR:158-168` G0-G6 · `ADR:170-176` Q1-Q5 명시 (measurement-validity §1)                                               |
| Live Exercise placeholder           | PASS | —    | `ADR:178-180`                                                                                                          |
| adr-writing 동적 seed               | PASS | HIGH | HIGH 코드 경로 3곳+ (R1 `:147` 5경로, R2 `:148` 4경로) · Generator 선언 `:11` · BC 수식화 → Phase 0 `breakdown:94,188` |
| No-fork 4 질문 lock-in              | PASS | HIGH | `breakdown:27-32` + 사용자 결정 기록 `ADR:139`                                                                         |

### 코드 검증 (핵심 주장)

| #   | 주장                                                                                                                       | 판정                  | 근거                                                                                                                                                                                                                                                                                                     |
| --- | -------------------------------------------------------------------------------------------------------------------------- | --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Taffy 완전 제거 (`Cargo.toml:5`) — `ADR:19`                                                                                | VERIFIED              | `packages/composition-engine/Cargo.toml:5` 주석 "taffy dependency 부재 — 추가 금지", `:14-18` 의존성에 taffy 없음                                                                                                                                                                                        |
| 2   | `block.rs:141-215` line box / `:370-390` baseline 정렬 — `ADR:20`                                                          | VERIFIED              | `block.rs:141-215` (`line_items`, `DISPLAY_INLINE_BLOCK` 분기, wrap `:180-185`, `flush_line_box`), `:370-390` (`line_baseline` → `start_y + line_baseline - item.baseline`)                                                                                                                              |
| 3   | TS 치환 체인 `fullTreeLayout.ts:1156 → TaffyBlockEngine.ts:118 display="flex" → tree.rs:1185/3598 Flex` — `ADR:21`         | VERIFIED              | `fullTreeLayout.ts:1156` `toTaffyDisplay` 호출 · `taffyDisplayAdapter.ts:235-241` `INLINE_BLOCK_PARENT_CONFIG.taffyDisplay="flex"` · `:527-537` 분기 · `TaffyBlockEngine.ts:118` · `tree.rs:1185`, `:3598-3605`                                                                                          |
| 4   | Button DOM `inline-flex` (`Button.css:14`) vs catalog `flex` (`componentRulesTable.ts:915`) — `ADR:22`                     | PARTIAL (라인)        | `Button.css:14` VERIFIED · `componentRulesTable.ts:915` 는 rule 헤더, `display: "flex"` 는 `:927`, 2026-06-27 사유 주석 `:918-925`. **ToggleButton `:13101-13109` 도 같은 override** (HC2 전수는 Phase 0 — ADR 스스로 "현재 Button 확인" 명시)                                                           |
| 5   | 부모 판정 `getElementDisplay`(`fullTreeLayout.ts:1795`) → `inline-block` / 자식 자신 `:1031` fallback → `flex` — `ADR:22`  | PARTIAL (라인)        | 호출은 `fullTreeLayout.ts:1797` (정의 `taffyDisplayAdapter.ts:395-408`: `style.display` 없으면 `INLINE_BLOCK_TAGS` → `"inline-block"`) · 자식 자신 `:1031` merge 후 `:1057` `getElementDisplay(enriched)` → `flex`. 이중 표현 성립                                                                       |
| 6   | `tree.rs:4609` `"inline-block"` 문자열 일치만 code 1 — `ADR:23`                                                            | VERIFIED              | `tree.rs:4609-4612` `Some("inline-block") => 1.0, _ => 0.0`                                                                                                                                                                                                                                              |
| 7   | `display.rs` outer/inner 모델이 `tree.rs` 어디서도 미호출 (참조 0) — `ADR:23`, `breakdown:45`                              | VERIFIED              | `display.rs:24-58` (Outer/Inner/Display), `:70/:138/:158/:170` pub fn · grep `display::\|parse_display\|blockify_display\|classify_child_display` — `display.rs` 밖 0건, `lib.rs:125` `pub mod display;` 만                                                                                              |
| 8   | `tree.rs:4590` baseline 0 / AUTO 고정 전달 — `ADR:24`, `breakdown:62`                                                      | PARTIAL (라인)        | 주석 `tree.rs:4590-4592`, 실제 기록은 `:4672-4674` (`data[off+16]=0.0; [17]=0.0; [18]=-1.0`) — 주장 성립, 라인 갱신 권고                                                                                                                                                                                 |
| 9   | `NodeLayout`(`tree.rs:296`) = `{x,y,width,height}` / `pub display: Option<String>` `:185`                                  | VERIFIED              | `tree.rs:296-301`, `:185`                                                                                                                                                                                                                                                                                |
| 10  | `calculateBaseline`(`utils.ts:5121`) production caller 0, re-export 만 — `ADR:24`                                          | VERIFIED              | `utils.ts:5121` 정의 · 참조는 `layout/index.ts:40`, `engines/index.ts:39` (re-export) 뿐                                                                                                                                                                                                                 |
| 11  | `INLINE_BLOCK_TAGS` 24 항목 / `needsWidth` 겸용 (`utils.ts:4400-4432`, `:4570-4572`) — `ADR:25`                            | VERIFIED              | `utils.ts:4400-4451` 24 entries (닫힘 `:4451`) · `:4570-4573` `needsWidth` 게이트. catalog `display: "inline-*"` 30건 grep 일치                                                                                                                                                                          |
| 12  | ingress 비단일: `useStyleActions.ts:51` / `updateElement.ts:98` store 직접, chain 은 `usePageManager.ts:385` 만 — `ADR:26` | VERIFIED              | `useStyleActions.ts:51` `updateSelectedStyle` · `updateElement.ts:98` `updateElementProps` · `usePageManager.ts:385-395` chain → `:402-407` persist-back(`hydration-migrate-back`). 같은 chain 이 `adapters/canonical/index.ts:331-343`(`legacyToCanonical`) 에도 있으나 프로덕션 호출자 0               |
| 13  | 패널 `useLayoutAuxiliary.ts:69` `display !== "flex"` → block — `ADR:27`, R4                                                | PARTIAL               | 정의 `:68`, 판정 `:71`. **같은 파일 `:99` `useFlexAlignmentKeys` 도 `display !== "flex"` → `[]`** — R4/Phase 4 미언급 (→ l3). `useLayoutValues.ts:52` `specPreset.display` VERIFIED                                                                                                                      |
| 14  | factory `display:"block"` 11곳 3파일 / `styleOptions.ts:68` — R2                                                           | VERIFIED              | DateColor `:540,:612,:622` · Form `:204,:217,:313` · Overlay `:54,:67,:132,:146,:203` = 11 · `styleOptions.ts:67-75` `DISPLAY_OPTIONS` (inline/inline-block/inline-flex 포함)                                                                                                                            |
| 15  | `tree.rs` 문자열 매칭 4곳 `:1185/:1373/:2552/:4609` — `ADR:128`                                                            | VERIFIED              | 4곳 모두 정확                                                                                                                                                                                                                                                                                            |
| 16  | `taffyDisplayAdapter.ts:20` 헤더 / `:521` 주석 — `ADR:19`                                                                  | VERIFIED              | `:20` "=== Taffy 시뮬레이션 규칙 ===", `:521` "Taffy는 inline formatting context를 지원하지 않으므로"                                                                                                                                                                                                    |
| 17  | breakdown §2.2 S1/S2/S3/S4 라인 (`:526-536`, `:436-440`+`fullTreeLayout.ts:2386`, `:250-254`, `:521-523`)                  | PARTIAL (라인)        | S1 `:527-537` ✓ · S2 `:436-446` + `fullTreeLayout.ts:2386` ✓ · **S3 `INLINE_BLOCK_LEAF_CONFIG` 는 `:244-248`** (250-254 아님) · S4 `:520-524` ✓                                                                                                                                                          |
| 18  | ADR-198 `blockInlineProbe` 4 변형 / `KNOWN_LAYERS["catalog-state-paint"]` L1 fail — G1/G2                                  | VERIFIED              | `blockInlineProbe.browser.test.ts:202/:224/:253/:266` (4 it) · `crossLeg.browser.test.ts:74-78` `"catalog-state-paint": "... L1:fail ..."`                                                                                                                                                               |
| 19  | ADR-156 차등 하니스 (실 Chrome) + "어댑터 우회 + 엔진 직접 호출" 진입점 — G1, `breakdown:97`                               | VERIFIED              | `tests/parity/harness.ts:12-13` 두 leg, `:99-101` `getBoundingClientRect`, `:113-135` `engineLeg` = `buildTreeBatch → computeLayout` 직접 (어댑터 우회 **이미 존재**), `:223` `pipelineLeg` = 프로덕션 진입점. Phase 0 항목 "존재 여부 확인" 답: 존재                                                    |
| 20  | `display.rs` 는 ADR-916 2-A 이식 — `ADR:96`                                                                                | VERIFIED              | `display.rs:1-6`                                                                                                                                                                                                                                                                                         |
| 21  | 커밋 `2fbd389da` / `dd5a6e403` / `0aa52b68a`, ADR-198 Accepted · 156/916 Implemented                                       | VERIFIED              | `git log` 3건 존재, `2fbd389da` = docs 3파일 (HEAD ancestor) · `README:503/:433/:448`                                                                                                                                                                                                                    |
| 22  | 비테스트 **14 파일** Taffy 식별자 — `ADR:19`, `breakdown:83`, R10                                                          | PARTIAL (수치)        | 식별자 코드 라인 기준 6~7 파일 (`fullTreeLayout/persistentTaffyTree/TaffyBlockEngine/taffyDisplayAdapter/TaffyFlexEngine/utils/layoutTypes`), 주석 포함 언급 58 파일 — 14 는 어느 기준으로도 재현 안 됨. Phase 6 inventory 재집계 (LOW, R10 범주)                                                        |
| 23  | breakdown Phase 4 "생성 테이블이면 소스에서 고치고 재생성" / §4 "`catalog/** → generated/`"                                | PARTIAL               | `componentRulesTable.ts:1-9` 헤더: **직접 편집 정본** (generate:rules 삭제됨). 조건문이라 실행엔 무해, §4 표기만 낡음                                                                                                                                                                                    |
| 24  | Button 이 타는 TS 경로에서 엔진 경계 display 가 CSS 값 1개가 되는가 (HC1 `ADR:46`, `breakdown:132` "CSS 값 그대로")        | **UNVERIFIED → 반증** | Button(`inline-flex`) 은 `toTaffyDisplay` 를 타지 않는다 — `fullTreeLayout.ts:1060` 가 flex/inline-flex 를 먼저 잡아 `TaffyFlexEngine.ts:110-112` 가 `"flex"` 로 정규화, inline-grid 는 `fullTreeLayout.ts:1077-1084` `{display:"grid"}`. 두 자리 모두 breakdown §3 Phase 5·§4 파일 변경표에 없음 (→ m1) |

### 위험 평가

| 위험                                                                     | 증거                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 심각도  | 상태     |
| ------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | -------- |
| R1 line box live 미검증                                                  | `block.rs:141-215`, `tree.rs:4609-4612` 로 live 도달 불가 확인 (#3, #6)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | HIGH    | 기재됨   |
| R2 배치 이동 (factory 11곳 / DISPLAY_OPTIONS)                            | #14                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | HIGH    | 기재됨   |
| R3 block 경로 비용                                                       | 미측정 — G3 baseline arm (`ADR:165`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | MED     | 기재됨   |
| R4 패널 Direction 회귀                                                   | `useLayoutAuxiliary.ts:71` (#13), catalog 주석 `componentRulesTable.ts:923-925`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | MED     | 기재됨   |
| R5 intrinsic 측정 누락                                                   | `utils.ts:4570-4573` (#11), DisclosureHeader 선례 주석 `utils.ts:4429-4433`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | MED     | 기재됨   |
| R6 컨테이너 baseline 출력                                                | `flex.rs`/`grid.rs` 존재, `NodeLayout` 출력 없음 (#9)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | MED     | 기재됨   |
| R7 HC1 재발                                                              | `fullTreeLayout.ts:1796-1802` vs `:1057` (#5)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | MED     | 기재됨   |
| R8/R9/R10                                                                | breakdown §8 · HC5 · #22                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | LOW     | 기재됨   |
| **m1** TS flex/grid 분기가 outer=inline 을 지우는 3 사이트가 계획에 없음 | `TaffyFlexEngine.ts:110-117` (`inline-flex → "flex"`), `fullTreeLayout.ts:1060-1061` (flex 분기 진입), `:1077-1084` (`{display:"grid"}`), `taffyDisplayAdapter.ts:510-512` (`taffyDisplay: parsed.inner`). breakdown `:130-138` Phase 5 목록·`:148-167` §4 표에 `TaffyFlexEngine.ts` 부재 (`:83` 개명 언급뿐). **현재도** 사용자 지정 `inline-flex` Button 은 이 경로로 엔진에 `"flex"` 로 도달 → `tree.rs:4609` code 0 → Skia 세로 적층 (기존 발산)                                                                                                            | **MED** | **누락** |
| **m2** Phase 4 단독 commit 의 중간 상태가 live 회귀                      | Phase 4 후 `getElementDisplay(Button)="inline-flex"` (`breakdown:127` "의도된 diff") → `taffyDisplayAdapter.ts:334-347` `classifyChildDisplay` 가 inline-flex 를 `"block"` 으로 → `:527` IFC 시뮬레이션 해제 → 부모 block → 자식은 m1 경로로 `"flex"` → 엔진 block item → **block 부모 안 Button 들이 세로 적층** (오늘은 flex-row-wrap 시뮬레이션으로 가로). G4/G5 (`ADR:166-167`) 는 override 0·패널 테스트·`enrichWithIntrinsicSize` diff 만 — 레이아웃 검사 없음, G1 후반/G2 는 Phase 5 (`ADR:163-164`). `breakdown:87` "HIGH 귀속 Phase 는 5 하나" 와 충돌 | **MED** | **누락** |
| **l3** 패널 두 번째 `display !== "flex"` 소비처                          | `useLayoutAuxiliary.ts:99` `useFlexAlignmentKeys` → `[]` (Alignment 9-grid 소실). R4 `ADR:150`·`breakdown:125` 는 `:69`+`useLayoutValues` 만. 참고 패턴: `TransformSection.tsx:475` 는 이미 `flex \|\| inline-flex`                                                                                                                                                                                                                                                                                                                                             | LOW     | **누락** |
| **l4** Phase 1 순수 inline 판정 미명세                                   | `breakdown:102` "outer==Inline 이면 1" (순수 inline 포함) vs `:104` "순수 inline 은 현행대로 **block 격상** 테스트 고정" — code 0/1 어느 쪽인지 불명. 영향 범위: catalog·factory `display:"inline"` 0건, `styleOptions.ts:70` 사용자 지정만                                                                                                                                                                                                                                                                                                                     | LOW     | **누락** |

관찰 (이슈 승격 안 함): Direction 토글 write-back 은 `display:"flex"` 를 style 에 기록한다 (`useStyleActions.ts:74,90,137,142,178,186,202,212` · `useResetStyles.ts:305,405`) — catalog `inline-flex` 전환 후에도 토글 1회로 DOM·Canvas 모두 block-level flex 가 되지만 두 consumer 가 같은 값을 받아 대칭은 유지되며 기존 동작과 동일. breakdown §8(a) store 쓰기 chokepoint 요건이 흡수.

### 근본 반론 (hate)

- **load-bearing 가정**: "Phase 5 에서 `toTaffyDisplay` 의 시뮬레이션만 걷어내면 Button 의 display 는 엔진 경계에 CSS 값 1개(`inline-flex`) 로 도달한다" (`ADR:46` HC1 · `ADR:128` "비용은 tree.rs 4곳 + baseline 필드 하나" · `breakdown:132` "자식 display 는 CSS 값 그대로 전달")
- **root**: Button 은 `toTaffyDisplay` 를 타지 않는다. `fullTreeLayout.ts:1060` 이 flex/inline-flex 를 먼저 잡아 `TaffyFlexEngine.ts:110-112` 가 `"flex"` 로 지운다 (inline-grid 는 `fullTreeLayout.ts:1084`). 계획의 제거 목록을 전부 수행해도 엔진은 `"flex"` 를 받고, Phase 1 규칙(`outer==Inline`)에서 block item 이 되어 r0 의 실패 모드가 그대로 남는다 — 공격 축: **소비 경로 (measurement-validity Q4)**, r1 h1 과 같은 부류의 누락이 flex 분기에서 반복됐다.
- **first nail**: `grep -n 'result.display = "flex"' TaffyFlexEngine.ts` 1회 + G0 (a) wasm 경계 캡처를 Phase 5 후 상태에 적용 — 비용 수 분 / 케이스 1 vs 프로그램 7 Phase · 7 Gate.
- **판정**: 검출은 G0(a)·HC1 테스트·G1 후반이 커버 (cutover 차단) — 계획 누락은 issue:m1 (MEDIUM). Phase 4 중간 상태(m2) 는 어느 Gate 도 덮지 않음.

### 렌즈 대조 (prism) — HIGH 2 · phase 7 → 실행

| 렌즈 (failure mode)   | 판정    | load-bearing 이유                                                                                                                                                                                                       |
| --------------------- | ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 정합성 / D3 SSOT      | pass    | Skia·패널이 같은 top-level `containerStyles` 를 읽는다 (`componentRulesTable.ts:918-921` 주석, `useLayoutValues.ts:52`, `implicitStyles.ts:291`) — override 1곳 수정으로 두 consumer 가 DOM 값(`Button.css:14`) 을 받음 |
| 소비 경로 / 배선 (Q4) | fail    | Button 이 실제로 타는 `fullTreeLayout.ts:1060 → TaffyFlexEngine.ts:112` 가 outer 를 지우며 계획에 없음 (m1)                                                                                                             |
| 되돌림 / rollout 순서 | fail    | Phase 4 commit 이 단독으로 live 회귀 (`taffyDisplayAdapter.ts:334-347` + m1) 이고 레이아웃 Gate 는 Phase 5 (m2)                                                                                                         |
| 측정 무결성 / oracle  | pass    | G1 oracle 은 실 Chrome (`harness.ts:12,99-101`), 어댑터 우회 `engineLeg` 존재 (`:113-135`), 5-질문 명시 (`ADR:170-176`), G0 가 전제를 실패 테스트로 고정 (`breakdown:91`)                                               |
| 비용 / 성능           | unclear | block 경로 비용 미측정 (`ADR:99`, HC6) — G3 가 block arm 을 별도 지정하나 수치는 Phase 0 이후                                                                                                                           |

- **수렴**: 불일치 — 정합성·측정 렌즈 pass vs 소비 경로·rollout 렌즈 fail (두 fail 은 같은 원인).
- **해결 질문 1**: "Phase 4 의 catalog `inline-flex` 전환·`getElementDisplay` catalog 파생을 Phase 5 cutover 와 **같은 commit** 으로 합치고, Phase 5 제거 목록에 flex/grid 분기의 display 정규화 3 사이트 (`TaffyFlexEngine.ts:110-117` / `fullTreeLayout.ts:1084` / `taffyDisplayAdapter.ts:510-512`) 를 추가하면 소비 경로·rollout 렌즈가 pass 로 바뀌는가?" — owner: breakdown Phase 4/5 + `ADR:128` 비용 서술 1줄.

### 종합 판정

- **구조**: PASS (11/11 HIGH — Risk-First 순서·4축·루프·기각 사유·분리 포인터·Risks 위치·Gates+5-질문·seed·no-fork 전부 충족)
- **코드 정합**: 24 주장 중 VERIFIED 16 / PARTIAL 7 (라인 drift 5 · 수치 1 · 표기 1 — 주장 자체는 성립) / UNVERIFIED→반증 1 (#24)
- **누락 위험**: 4건 (HIGH 0 / MED 2 / LOW 2)
- **권고**:
  - **HIGH**: 없음
  - **MEDIUM**:
    - m1 — breakdown Phase 5 제거 목록·§4 파일 변경표에 `TaffyFlexEngine.ts:110-117` (inline-flex→flex 정규화) · `fullTreeLayout.ts:1077-1084` (inline-grid→grid) · `taffyDisplayAdapter.ts:510-512` (`taffyDisplay: parsed.inner`) 추가, `ADR:128` "비용" 서술에 TS flex/grid 분기 1줄 반영. G0 (a) 를 "Phase 5 후 통과" 로 재실행하는 조건은 이미 있으므로 Gate 신설 불요.
    - m2 — Phase 4 의 catalog display 전환 + `getElementDisplay` catalog 파생을 Phase 5 와 같은 commit 으로 (패널·`INTRINSIC_MEASURE_TAGS` 분리는 Phase 4 에 남겨도 무해), 또는 G4 에 "block 부모 + Button 2 가 한 줄" 레이아웃 단언 추가. `breakdown:87` 의 "HIGH 귀속 Phase 5 하나" 문장과 정합시킬 것.
  - **LOW**:
    - l3 — R4/Phase 4 에 `useLayoutAuxiliary.ts:99 useFlexAlignmentKeys` 추가 (Direction 테스트에 Alignment 9-grid 표시 단언 병기).
    - l4 — `breakdown:104` 의 "block 격상" 이 inner solver(Block) 를 뜻하는지 outer code 0 을 뜻하는지 1줄 명시 (`:102` 규칙과 정합).
    - 라인 갱신 (선택): `ADR:22` `:1795→:1797`, `ADR:24` `:4590→:4672-4674` 병기, `breakdown:53` `:250-254→:244-248`, `ADR:19/breakdown:83` "14 파일" 재집계.
  - **결론**: **수정 후 재리뷰** — Decision(C′)·Gate 골격은 유지 가능하며 수정 대상은 breakdown Phase 4/5 범위 + `ADR:128` 1줄. round 1 이 세운 "착수 금지 — G0 선행 검사가 Phase 1 진입 조건" 은 그대로 유효.

## Round 3 — 2026-08-31 (reviewer: codex)

## ADR-923 round 3 (reviewer: codex, 대상: r2 820d8a0e2) — 범위 한정 리뷰

> Decision C′·B 갈래 분리·No-fork lock-in 은 확정 상태로 재검토 제외. 재개 조건 = 사실 1–10 중 하나를 file:line 으로 반증할 때만 — **미충족 확인**.

### 과제 1 — Phase 5 제거 목록 완전성

확인 사이트 20곳 (componentRulesTable.ts:927,13108 · taffyDisplayAdapter.ts:395-408,274-302,334-347,497-540,122-124 · utils.ts:4400-4451,4570-4784 · fullTreeLayout.ts:1031-1057,1060-1074,1077-1084,1153-1161,1742-1802,892-902,2376-2384,2464-2472 · TaffyFlexEngine.ts:110-117 · TaffyGridEngine.ts:15-61(display 쓰기 없음) · TaffyBlockEngine.ts:117-118 · layoutTypes.ts:11,89-91 · persistentTaffyTree.ts:173-188 · wasm.rs:87-90(binary 비활성) · tree.rs:181-185,675-705,1185,1373,2552,3598-3604,4607-4612). 동작 경로만 보면 S9 적용 후 Button 은 fullTreeLayout.ts:1060-1074 → taffyStyleToRecord:900-902 → JSON 경계에 `inline-flex` 로 도달 가능. 그러나 운반 타입 union 2곳이 목록·파일 변경표에 없음 → **r3m1**.

### 과제 2 — Phase 4 동작 무변경: PASS

INTRINSIC_MEASURE_TAGS 가 needsWidth 멤버십을 보존하면 출력 무변화, 다른 INLINE_BLOCK_TAGS 소비는 Phase 5 까지 유지 (utils.ts:4570-4609,4644-4652,4677-4689,4763-4784). 패널 hook 은 useMemo 읽기뿐 (useLayoutAuxiliary.ts:20-62,68-74,95-111); store 쓰기는 사용자 이벤트 → useStyleActions callback 에서만 (LayoutSection.tsx:271-319; useStyleActions.ts:66-93,110-146,151-195).

### 과제 3 — Phase 1 code 규칙: PASS (조건부)

parse_display 는 inline-flex→{Inline,Flex}, inline-grid→{Inline,Grid}, inline-block→{Inline,FlowRoot}, inline→{Inline,Flow} (display.rs:70-90). **classify_child_display 는 inline-flex/inline-grid 를 Block 으로 분류** (display.rs:134-149) — Phase 1 은 parse_display 직접 사용. block.rs:144-170 은 code==1 만 line item, :211-218 이 code 2.

### 과제 4 — G0 (a) 캡처 지점: LOW → r3l1

engineLeg(harness.ts:114-135) 는 fixture style 로 batch 직접 생성(프로덕션 변환 우회), pipelineLeg(harness.ts:223-270) 는 payload 미반환. 정확한 지점: buildNodeStyle 반환값이 batch 에 들어간 뒤 PersistentTaffyTree.buildFull → CompositionEngineLayout.buildTreeBatch 인자 (persistentTaffyTree.ts:183-188; compositionEngine.ts:167-170) 를 spy.

### 과제 5 — r2 라인 갱신: PASS

fullTreeLayout.ts:1797 · componentRulesTable.ts:927,13108 · tree.rs:4672-4674 · Taffy 식별자 7 파일 + 파일명 5 = 합집합 8 일치.

### 종합

- 코드 정합: 3/5 VERIFIED (과제 1·4 이슈)
- 이슈: HIGH 0 / MEDIUM 1 / LOW 1 → 전부 `1b3d2e14b` 로 fixed
- 결론: 수정 후 재리뷰 → r3 개정 반영 완료. note: r2 의 m1/m2/l3/l4 방향은 코드와 일치, 수정 범위는 Phase 5 타입 계약 + G0 캡처 지점으로 한정.

**main 확증 (claude)**: layoutTypes.ts:11 `TaffyDisplay = "flex"|"grid"|"block"|"none"` · :91 · taffyDisplayAdapter.ts:122-124 · display.rs:134-149 · harness.ts:223-232,262-270 · persistentTaffyTree.ts:181-188 · compositionEngine.ts:167-170 — 전부 원문과 일치.

## Round 4 — 2026-08-31 (reviewer: codex, evidence-verification)

## ADR-923 round 4 (reviewer: codex, evidence-verification of Phase 0 (b) — e6d08237d · 5b28b9a0b)

> 대상은 설계(C′) 가 아니라 Phase 0 (b) evidence 의 사실 주장 — 실행자(Claude) ≠ 판독자(Codex). Decision 재개 조건 해당 없음.

- 재실행: scan exit 0/0 · baseline exit 0 — arm F 20.1/25.3 ms (Claude 대비 p50 −4.3% / p95 +5.9%), arm B 92.2/98.0 ms (p50 +2.2% / p95 +0.8%) · skip exit 0 (2 skipped) · type-check exit 0 · catalog 직접 diff 0건 (123/123) · `test:parity -- <file>` 확인 exit 1 (35 files 실행, baseline 통과, 기존 실패 2건).
- 과제별 판정: 1 PASS (재현) · 2 **FAIL** (정확값 비교로 전환 후보 밖 불일치 8 추가) · 3 PASS (`INLINE_BLOCK_TAGS` 24 순서·B 6 경로·? 7·intrinsic 표본·R5) · 4 PASS (factory leaf 11 · 51 정의 · 스캐너 규칙 = 프로덕션 분기, C′ 열은 근사로 명시) · 5 PASS (skip 조건·fixture·타이머·reset·노드 수 검사; arm B 는 공통 비용이 민감도를 낮추지만 G3 가 무의미해지지는 않음) · 6 PASS (pencil 키 복사·타입 매핑·caller 0·prefetch·비영속 Map) · 7 **FAIL** (inline-\* 수치 · 존재하지 않는 과거 수치 인용 · baseline raw · README 표).
- 이슈: r4m1 MEDIUM classification · r4l1 LOW evidence-wrong · r4l2 LOW citation-drift · r4l3 LOW citation-drift · r4l4 LOW doc-drift (frontmatter 참조).
- 판독 결론: G0 (b) **열림** — 과제 2·7 FAIL, MEDIUM 1 · LOW 4. Decision 재개 조건 해당 없음 (evidence 분류·인용·문서 상태 문제).
- 관찰: 두 baseline 재실행 값은 문서 값의 ±20% 안 · `test:parity -- <file>` 은 실제로 35 파일 실행 · 저장소 JSON 에 사용자 export 없음 (synthetic·minimal·invalid 만) · 소스·ADR·README 무수정, commit·push 없음, baseline 두 파일 untracked.

**main 확증 (claude)**: r4m1 — 정확값·엄격 base 셀렉터로 123 rule 재집계: Codex 의 8 전부 성립 (FileTrigger `:5364` inline-block vs `generated/FileTrigger.css:24` inline-flex · Skeleton `:10405` inline-flex vs `Skeleton.css:65` block 은 structure≠DOM, 나머지 6 은 catalog display 없음) + 초판의 Button·ToggleButton·Menu·Label·Slot = **13**; outer 차이 6 · inner 7; 초판 (ii-a) 17 중 13 은 일치, Checkbox·Radio·SliderOutput 은 live 파일 간 충돌로 재분류. r4l1 — `jq` 재집계 nested 29 + top-level Menu 1 = 30, ADR 사실 7 원문 복원. r4l2 — ADR 본문 `grep 121` 0건 확인 (승인 프롬프트의 수). r4l3 — `.claude.json` 보존본 10:22:40Z 확인, 3 run 기록. r4l4 — README:513 갱신. 전부 **fixed `f8ab25a45`**. → **G0 (b) 닫힘** (Claude 판정 — (a) `07c724a37` + (b) `e6d08237d`/`f8ab25a45`). Phase 1 진입은 사용자 승인 + 실행 모델 결정 후.

## Round 5 — 2026-08-31 (reviewer: codex, decision-judgment on 3 pending questions)

> 대상은 round 4 종결 보고가 사용자 판단으로 넘긴 3건 (① HC2 정확값 vs outer ② Menu B7 예외 ③ G3 run 편차). Codex 판정 → Claude 가 인용 9건을 코드로 확인한 뒤 ADR r4 개정으로 반영. Decision 재개 조건 해당 없음 (Gate·계약 문구 정밀화, scope 무변경).

- ① HC2: **정확값 유지, outer 기준 기각, 13건 일괄 전환도 기각**. C′ 는 inner 로 solver 를 고르므로 (`ADR:35`, `display.rs:70`) outer 6건만 검사하면 계약을 놓친다. 13 은 catalog fallback 기준 후보 — production 경로는 factory style·`applyImplicitStyles` 가 우선 (`fullTreeLayout.ts:1658`): ColorPicker·ColorSwatchPicker factory `display:"flex"` (`DateColorComponents.ts:587`), GridList owner 의도적 flex-column (`componentRulesTable.ts:5557`), TagList projection shell ≠ DOM `contents` (`TagList.binding.ts:14`). 확정 전환 Button·ToggleButton 2 · Menu 비교 대상 재지정 · 나머지 10 은 Q4 캡처 후 전환/투영 예외 분류 · DOM 충돌 3·미선언 17 은 G4 unresolved 0 전까지 통과 아님. G0 (b) 는 "인벤토리 발견" 의미로 닫힌 상태 유지.
- ② Menu: 값 예외가 아니라 **비교 대상 DOM box 지정** — Canvas Menu `inline-flex` ↔ DOM `MenuTrigger > Button` inline-flex, popover `.react-aria-Menu` flex 는 비교 제외 (`151-breakdown:183` 사용자 결정, `implicitStyles.ts:339` resolver 도 두 층을 의도적으로 분리).
- ③ G3: **단일 run 폐기, 고정 N=3 paired ratio 중앙값**. 같은 기기·Chromium 에서 baseline/candidate commit 을 교대로 fresh-browser N=3 쌍, run 마다 warm-up 5 + measured 30 p95, arm F/B 각각 median(after_p95ᵢ / before_p95ᵢ) ≤ 1.05, raw 고유 파일 보존, 결과 확인 후 run 추가 금지. 3 run 단순 중앙값 F 24.9 / B 98.0 ms 는 실행 조건 혼합이라 공식 baseline 아님.

**main 확증 (claude)**: 인용 9건 전부 코드 일치 — `ADR:35` outer/inner 표 · `display.rs:70 parse_display` · `fullTreeLayout.ts:1658 applyImplicitStyles` · `implicitStyles.ts:335` "사용자/factory 편집 우선" · `DateColorComponents.ts:587`/`:664` ColorPicker·ColorSwatchPicker `display:"flex"` · `componentRulesTable.ts:5557` GridList 주석 · `TagList.binding.ts:14` · `151-breakdown:183` · `implicitStyles.ts:339` · evidence §D 편차 (실제 행 275). 반영 **`3c2d89e5e`**: ADR HC2·HC6·G3·G4·Q3·Q4·Status, breakdown 헤더·Phase 5·§5 표·§6, evidence §0·A-2·관찰 2/3·§D, README 로그·표 행, 하니스 `VITE_ADR923_BASELINE_TAG` (고유 raw), `.gitignore` `.artifacts/`. type-check 0 · baseline skip path exit 0. 전부 **fixed**. 부수 발견: `.artifacts/` JSON 3개가 이미 tracked (`dbb4489ea` stats 자동 커밋 · `ea3ae5f3e` 이 쓸어 담음, Codex r4 의 "untracked" 는 그 시점 기준) — 정리는 사용자 결정. Phase 1 진입은 사용자 승인 + 실행 모델 결정 후.

## Round 6 — 2026-09-01 (reviewer: codex, implementation-verification of Phase 1)

> 대상 `5822f2496` (Rust) · `e7ee4395d` (docs). 실행자(Claude) ≠ 판독자(Codex). 재실행: cargo 333/333 · adr923 7/7 · RED 재현 3 FAIL · clippy 신규 0 · wasm+parity 930 pass · layout unit 393 — 전부 재현. 과제 판정 1·2·4·7 PASS / 3·5·6 FAIL. 판독 결론: Phase 1 **열림** (r6h1 이 프로덕션 무변경 phase 경계를 깸). Decision 재개 조건 해당 없음 (C′ 반전·scope 변경 아닌 Phase 분리 구현 결함).

- 과제 3 (shrink-to-fit): 고정 크기 케이스는 맞지만 일반 CSS shrink-to-fit 과 동치 아님 — block 부모의 definite available 로 1회 solve 한 `content_w` 를 그대로 사용, flex 재-solve 는 indefinite available 일 때만. **Phase 1 결함이 아니라 Phase 3 차등·수리 대상** (wrap: 폭 100 부모 아래 80px×2 → CSS 100 vs one-pass 80 · percentage descendant 재해소). → breakdown Phase 3 케이스 목록에 추가.
- 과제 5 (r6h1): seam probe 로 Label(inline-flex, flexShrink 미지정) 이 JSON 경계에 raw 도달 확인 → HIGH.
- 과제 6: seam 무변경 주장 반증 + 테스트 수·호출부·파일표 LOW 정합 (r6l1·r6l2).

**main 확증·수리 (claude, `ef608decf`)**: r6h1 — `seamDisplayInvariant.browser.test.ts` RED 로 재현 (batch display `["inline-flex","block","block"]`) → `toBatchDisplay` (운반 union 강제, 1차 writer 규칙과 동일 — 수리 전 동작 등가) → GREEN 1/1 + `displayContract` it.fails 유지 · type-check 0 · parity 36 files 931 pass (catalogComponentBox 2 기존) · layout unit 393 · cargo 333/333. r6l1·r6l2 문서 정정. S9 4번째 사이트 등재 (Phase 5 제거 목록 포함) + `rules/layout-engine.md` 금지 패턴 + 메모리 `feedback-seam-invariant-verify-all-batch-writers`. 전부 **fixed** → **Phase 1 닫힘** (Claude 판정). Phase 2 진입은 사용자 승인 후.

## Round 7 — 2026-09-01 (reviewer: codex, implementation-verification of Phase 2)

## ADR-923 round 7 (reviewer: codex, implementation-verification of Phase 2 — ee05fbdfb · 062a02797)

> 실행자(Claude) ≠ 판독자(Codex). 재실행 전부 재현: cargo 340 전량 · adr923 12/12 · 슬롯 원복 RED(16/17/18 — baseline·line-height FAIL, vertical_align_top 은 PASS → r7l2) · 전파 제거 RED · lineHeight coerce 제거 RED(`"20px"` serde + null 재현) · clippy 신규 0 · parity 934 (기존 2) · seam 4/4 · layout unit 399 · type-check 0. 과제 판정 1·3·4·5·6·8 PASS / 2·7·9 FAIL. 판독 결론: Phase 2 열림 (r7m1·r7m2). Decision 재개 조건 해당 없음.

- r7m1 MEDIUM — grid 컨테이너 baseline 이 명시 placement row-major 첫 원천 대신 children source 순서 첫 원천 선택 (역순 probe 70, 기대 10). root_cause: placement 결과를 후보 정렬에 쓰지 않고 `children.iter()` 순서로 즉시 확정.
- r7m2 MEDIUM — unitless lineHeight 가 상속 computed fontSize 를 무시하고 기본 16 환산 (fontSize 20 × 1.5 기대 30, 실제 24; `elementToTaffyStyle` 이 `_computedStyle` 을 받고도 미사용). + 세 번째 writer `createNodeRaw` seam 미캡처.
- r7l1 LOW — seam "모든 writer" 주장에 `createNodeRaw` 누락 (writer inventory 가 initial batch + update writer 에서 끝남).
- r7l2 LOW — 슬롯 16 을 0 으로 되돌려도 `adr923_p2_vertical_align_top_pins_item` 통과 주장 (fixture 가 단절을 구별 못 함).
- r7l3 LOW — `NODESTYLE_FIELD_COUNT` 인접 주석이 49 유지 (실제 54).
- 관찰 (Phase 3 이관): border-box bottom vs CSS margin-edge 폴백 차이 · block 자식 text `leafBaseline` 미주입 · top/bottom-only line box 의 baseline 0 · column flex 첫-item 근사.

**main 확증·수리 (claude, `9a51479a5`)**:

- r7m1 **확증·fixed** — RED `adr923_p2_grid_container_baseline_row_major_not_source_order` (명시 `gridRowStart` 역순: 실측 42 vs 기대 4) → `solve_grid` placement 루프의 후보 정렬 키 = 셀 (y, x) (offset 전 좌표 — 같은 row 는 같은 트랙 y 라 f32 동등 비교 안전) 로 row-major 첫 원천 선택 → GREEN. flex 는 document order 가 CSS 계약(§8.5 order-modified)이라 무변경.
- r7m2 **확증·fixed** — `applyCommonTaffyStyle` 4번째 param `computedFontSize` (inline fontSize 우선, 부재 시 상속 computed — 기본 16 폴백 제거) + `elementToTaffyStyle` `_computedStyle` → 실사용 + `elementToTaffyBlockStyle` param + fullTreeLayout grid 분기 + `patchBatchStyleFromImplicit` 2 call site (implicit patch 는 부모 computedStyle 기반 `resolveStyle(modStyle, computedStyle).fontSize`, 2-pass 재-enrich 는 기존 `childComputed.fontSize` 재사용 — 후자는 root 기반 근사 잔존, 해당 경로 기존 계약). RED→GREEN 단위 2건 (1.5 × computed 20 → 30 · inline 우선).
- r7l1 **fixed** — seam 에 `createNodeRaw` spy 추가 (3 writer 캡처 — 이 시나리오에선 0회지만 inventory 닫힘). 메모리 `feedback-seam-invariant-verify-all-batch-writers` 3-writer 로 보강.
- r7l2 **기각 (실측 미재현)** — slot16 **단독** 원복 실측 = 해당 테스트 **FAIL** (line baseline 32 로 이동해 a.y 0→16 감지, 판별 성립). 관찰된 PASS 는 슬롯 16+**17** 동시 원복 시나리오 — slot17=0 이 전 item baseline 을 0 으로 눌러 이 테스트만 마스킹되며, 그 경우에도 `adr923_p2_inline_flex_containers_align_by_text_baseline` 이 FAIL 로 suite RED 유지 (실측 2회: 단독 FAIL · 동시 4 pass/1 FAIL). per-slot 판별이 suite 수준에서 성립하므로 fixture 무변경.
- r7l3 **확증·fixed** — 주석 4곳 49→54 (선언 doc 헤더 · 미소비 목록 · 가드 산술 doc · 구조분해 주석), 이력(ADR-165 +2 · ADR-923 P2 +3) 병기.
- 관찰 4건 → breakdown Phase 3 케이스 목록 등재.

검증: cargo **341**(+1 grid RED→GREEN) · golden 15 · layout_trace 10 · tree_golden 11 · clippy 신규 0 (`doc_lazy_continuation` 6건은 tree.rs:6408-6411·6701 — 이번 diff hunk 밖 기존 위치) · wasm 재빌드 · seam 4/4 · parity **934 pass** (기존 GridListItem/Tooltip 2 실패만 — 파일 단독 재실행으로 동일성 확인) · layout unit 49 files/**401**(+2) · type-check 0. → **Phase 2 닫힘** (Claude 판정). Phase 3 진입은 사용자 승인 후.

## Round 8 — 2026-09-01 (reviewer: codex, implementation-verification of Phase 3)

## ADR-923 round 8 (reviewer: codex, implementation-verification of Phase 3 — 288b1c465)

> 실행자(Claude) ≠ 판독자(Codex). 재실행 전부 재현: cargo 346 전량 · adr923 18/18 · clippy 신규 0 · wasm 재빌드 + 차등 23/23 · full parity 957 (기존 catalog 2) · layout unit 401 · type-check 0 · **재생성 artifact 대조군 발산 15/정합 8 (문서 16/7 과 불일치)** · 원복 RED 4종 실측 (baseline 식 → valign-bottom 1 FAIL / margin-edge → ib-baseline-margin-bottom 1 FAIL, 예상 문구의 overflow 케이스는 추가 실패 없음 / shrink 게이트 → 3 FAIL / strut → browser 2 + Rust 1 FAIL). 과제 판정 2·3·4·7·9 PASS / 1·5·6·8 FAIL. 판독 결론: Phase 3 열림 (차단 r8h1·r8h2). Decision 재개 조건 해당 없음 (block solver 내부 line-box correctness — C′ 방향·범위 무관).

- r8h1 HIGH correctness — `line_metrics` 의 vertical-align:middle 이 baseline 제약 없이 line box 중앙 배치 (CSS 는 middle box 중심 = 부모 baseline + x-height/2). 기존 20+40 케이스는 중심이 우연히 일치해 통과; 60px 반례 실측 dom a.y=10 vs eng 0. "fontSize 0 에서 동치" 관찰은 일반적으로 거짓.
- r8h2 HIGH correctness — 마지막 line box 의 flush 높이가 진행 높이/metadata 에 반영되지 않아 자식 bounding box 만으로 block auto-height 산출 — tail 없는 strut 반례 root 40 vs 20.
- r8m1 MEDIUM ssot-violation — 대조군 합계 16/7 이 재생성 artifact 15/8 과 불일치 (evidence :13-14 · breakdown :125 · ADR :5 · README :7/:521 — artifact 표 갱신 후 합계 수동 재집계 누락).
- r8m2 MEDIUM correctness — baseline 억제의 non-visible 판정을 `overflow_creates_bfc` 가 공유해 overflow:clip 까지 BFC 로 분류 — css-overflow-3 §valdef-overflow-clip 위반 (clip 은 새 FC 를 만들지 않는다). baseline 쪽 clip 포함은 타당 판정.
- r8l1 LOW doc-drift — slot 17 폴백을 border-box bottom 으로 적은 주석 + slot 18 dormant/S4 예약을 소비로 적은 field-contract 주석 stale (block.rs:28-29 · tree.rs:263-270/:4826-4829/:4920-4923/:5216-5219 · breakdown :116).
- r8l2 LOW evidence-missing — 프로덕션 pipeline 의 wrap intrinsic-min 전용 fixture 부재 (신규 검증이 raw engine leg 에 집중 — `min_wrap_measure` 는 이미 활성인데 production coverage 공백).
- 관찰: 실폰트 strut·lineHeight normal 공급의 S4/Phase 5 이관은 적정 / middle 반례·마지막 line box 는 현 Phase 3 correctness 결함 (→ r8h1/r8h2) / fit 재-solve 성능은 Phase 5 G3 paired 측정으로 충분 (지금 추가 측정 불요).

**main 확증·수리 (claude, `efb56a888`)**:

- r8h1 **확증·fixed** — RED 신규 차등 케이스 `valign-middle-tall` (ib20+ib60(middle)) FAIL 재현 → `line_metrics` 에서 middle 을 별도 max 버킷이 아니라 **baseline 참여자**로: asc/desc 를 margin box 절반씩 밀고, `flush_line_box` 는 `baseline − mbox/2 + margin_top` 배치 (x-height/2 항은 fontSize 0 채널에서 0 — 실폰트 공급은 S4) → GREEN (dom a.y 10 · m.y 0, 기존 valign-middle 40px 케이스도 동치 유지). Rust 단위 `adr923_p3_valign_middle_anchors_to_baseline`(block) + `adr923_p3_valign_middle_tall_pushes_baseline`(tree).
- r8h2 **확증·fixed** — RED `strut-last-line` (tail 없는 ib20+strut40 → dom root.h 40 vs 20) 재현. 원인 = 마지막 flush 만 `current_y` 미전진 + tree.rs 컨테이너 auto-height 가 자식 rect bbox 만 소비 (기존 strut 케이스는 전부 tail block 보유라 중간 flush 로 관측 밖). 수리 = 마지막 flush 도 전진 + trailing meta **4번째 `inFlowBottom`** 신설 (out 길이 +1, 기존 소비 오프셋 무이동) + `container_h = max(bbox, inFlowBottom − escapedTop)` → GREEN. Rust 단위 `adr923_p3_last_line_strut_in_flow_bottom`(meta) + `adr923_p3_last_line_strut_extends_auto_height`(tree).
- r8m1 **확증·fixed** — 재생성 artifact 직접 재집계로 15/23 발산·8/23 정합 확증 (오집계 원인: valign-top 을 발산으로 오전기). 수리 후 27 케이스 기준 **발산 18 / 정합 9** 로 evidence·breakdown·ADR·README 일괄 정정 (개별 행 값은 원래 artifact 와 일치 — 합계만 오류).
- r8m2 **확증·fixed + 확장** — BFC 쪽 확증: `overflow_creates_bfc` 에서 clip 제외 (RED `clip-no-bfc` margin 관통 → GREEN; css-overflow-3). **단 "baseline 은 clip 포함 타당" 판정은 오라클 케이스로 반증** — `ib-overflow-clip-baseline` 실측 dom a.y=20 (clip 도 visible 처럼 last line box baseline 유지) vs 종전 엔진 10 (margin-edge 강제). baseline 억제 술어에서도 clip 제외로 확장 수리 → GREEN. Rust 단위 `adr923_p3_overflow_clip_no_bfc_margin_escapes` + `adr923_p3_overflow_clip_keeps_line_box_baseline`. flex §4.5 auto-min 의 overflow 판정(`overflow_main`)은 spec 이 "visible 아님"이라 무변경.
- r8l1 **확증·fixed** — 주석 6곳 정정: block.rs 필드표 slot 17(margin edge)/slot 18(S4 예약) + 센티널 테스트 doc + tree.rs `write_block_item` doc·슬롯 write 2곳 + NodeStyle `line_height` 필드 doc(소비 = 컨테이너 strut) + 산술 가드 주석(슬롯 16/17 + strut). breakdown Phase 2 절은 이력 기록이라 원문 유지, Phase 3 절에 반전 명시.
- r8l2 **확증·fixed** — `adr923ChromeDifferential` 에 pipelineLeg **게이트** describe 추가: display:flex row 60px 안 wrap flex item (80px×2) 이 min-content 최대 item 80 으로 바닥 — 프로덕션 운반 어휘 그대로 `min_wrap_measure` 소비를 고정 (첫 실행부터 GREEN — 수리 5 가 이미 프로덕션 반영임을 실증).

검증: cargo **352**(+6) · golden 15 · layout_trace 10 · tree_golden 11 · clippy 신규 0 (경고 7 = tree.rs:2034/6496-6499/6789-6790 기존 위치) · wasm 재빌드 · 차등 **27/27** · full parity **962 pass** (기존 GridListItem/Tooltip 2 실패만) · layout unit 49 files/401 · type-check 0 · Live: 실 빌더 TEST 프로젝트 재로드 — Skia 전 페이지 정상·Page 2 wrap 카드 불변·콘솔 0. → **Phase 3 재닫힘** (Claude 판정 — Codex 재판독 대기). Phase 4 진입은 사용자 승인 후.

## Round 9 — 2026-09-01 (reviewer: codex, implementation-verification of round 8 repairs — `efb56a888` · `06358b4f4`)

> 실행자(Claude) ≠ 판독자(Codex). 재실행 전부 재현: cargo 352 전량 · adr923 24/24 · clippy 신규 0 · wasm 재빌드 + focused 차등 28/28 · full parity 962 (기존 catalog 2) · layout unit 401 · type-check 0 · 재생성 artifact 27행·대조군 18/9·종전 23행 불변 · 원복 RED 4종 + `min_wrap_measure=false` 전부 판별 성립 (browser 1/Rust 1 씩, hidden 대조군·strut-short/tall PASS 유지). 과제 판정 1·4·5·6·7 PASS / 2·3·8·9 FAIL. 판독 결론: Phase 3 열림 (차단 r9h1; r9m2 는 Phase 3 케이스 포함 또는 범위 판정 요구). Decision 재개 조건 해당 없음 (flex/block solver correctness + evidence — C′ 방향·범위 무관). Phase 4 진입 보류.

- r9h1 HIGH correctness — flex item 의 overflow:clip 이 auto-min content floor 를 잃어 Chrome 80px 대비 pipeline 60px (`tree.rs` 슬롯 18 write "visible 이외 = clipped" · `flex.rs` §4.5 · `styleOptions.ts:115` Clip 노출). 구 non-visible=clipped 조건을 현 css-flexbox-1 §4.5 non-scrollable 규칙으로 갱신하지 않음.
- r9m1 MEDIUM ssot-violation — "clip UI 미노출·실효 0" 공시(evidence :95)가 실제 Style Panel(`styleOptions.ts:107`/`AppearanceSection.tsx:338`)·shorthand 분배(`utils.ts:5307`)·AI styles(`definitions.ts:138`/`styleAdapter.ts:39`)·엔진 경로와 반대. inFlowBottom 은 block-only 입력에서도 실행되지만 표본에서 bbox 와 동일 — r8 휴면 주장은 line-item 효과에 한해 성립.
- r9m2 MEDIUM correctness — 마지막 empty collapsed margin(solid h10+mb10 → empty mt20/mb30: Chrome root 10 / 엔진 30)과 padding 으로 포함돼야 하는 마지막 bottom margin(h10+mb20 + pb1: Chrome 31 / 엔진 11)에서 block auto-height 발산 (inFlowBottom 소비 제거 전후 동일 → r8 회귀 아님, production block 경로 미검출 결함). root_cause: bbox/current_y 모델이 최종 margin 의 탈출·포함 상태를 구분하지 못함.
- r9l1 LOW evidence-missing — clip baseline 은 Chrome oracle 로 확증됐지만 CSS2.2 §10.8.1 "overflow other than visible" 문면과의 충돌을 해소하는 규범 근거 없음 (evidence 가 Chrome 결과를 css-overflow-3 규칙으로 서술).
- r9l2 LOW doc-drift — slot 18 을 S4 예약으로 고쳤지만 상위 요약(`tree.rs:263`/`:4836`, `block.rs:193`)은 여전히 "미소비 해소".
- 과제 부수 판정: ① middle 수식 PASS — 비대칭 mt10/mb30/h60 반례에서 Chrome·엔진 m.y 10·root 100 일치 (margin box 중심 50 ≠ border box 40). ③ clip 술어 3곳 중 BFC·block baseline 은 정합, flex 만 오류. ⑧ 수치 정합·관찰 4→2 정리 맞음, 프롬프트의 `.claude/stats` 변경 언급은 실제 diff 에 없음 (코드 3 + docs 5 = 8 파일). ⑨ 표준 Pencil clip 은 canonical boolean 이라 CSS overflow 와 다르지만 임의 style 필드는 props 로 보존 가능.
- 관찰: 실폰트 x-height/strut·lineHeight normal 은 S4/Phase 5 유지 / fit 재-solve 성능은 Phase 5 G3 paired 측정 / full parity 962 는 clip flex·최종 block margin 차원을 쓰지 않아 두 반례를 덮지 않는다.

**main 확증·수리 (claude)**:

- r9h1 **확증·fixed** — RED 신규 차등 `flex-item-clip-auto-min` (root flex 60 > f flex wrap overflowX clip > 80×2: dom f.w 80 / eng 60) + pipeline 게이트 (shorthand `overflow: "clip"` — 패널 경로) 둘 다 FAIL 재현. 수리 = overflow 판정 3곳(BFC · block baseline · flex §4.5)을 **단일 술어 `is_scrollable_overflow`/`is_scroll_container`** 로 통합 (scroll/auto/hidden 만 scroll container — css-overflow-3 §3.1; css-flexbox-1 §4.5 "computed overflow value is non-scrollable → content-based minimum"). 확장: 한 축 scrollable 이면 다른 축 visible 은 auto 로 계산되므로 **양축 판정** — Chrome `flex-item-cross-hidden-auto-min` (overflowY hidden 만 → f.w 60) 이 확증 (종전 main 축 단독 판정은 80). `flex.rs` 슬롯 18 의미론 "clipped" → "scroll container" 개명. Rust `adr923_p3_r9_flex_item_clip_keeps_auto_min_floor` (clip/visible 80 · hidden/cross-hidden 60).
- r9m1 **확증·fixed** — `styleOptions.ts` Clip 노출·`utils.ts` shorthand 분배 grep 으로 확인. evidence §프로덕션 영향 전면 재작성: 실효 = 수리 5·9·10~~12 (block 경로 어휘 전부 운반 union 안), 휴면 = line item 본체(1~~4·6~8)만. `.claude/stats` 언급은 프롬프트 오기 (diff 8 파일 맞음).
- r9m2 **확증·fixed (root cause 2겹)** — RED `trailing-empty-block-escape` (root 30 vs 10) · `trailing-margin-contained` (11 vs 31) · `trailing-empty-block-contained` (31 vs 41) 재현. root cause ① **self-collapsing box 를 intake 가 분류하지 않았다** — `write_block_item` 주석은 "코드 2 는 block.rs 사전 분류" 라 했지만 발행 주체가 없어 `DISPLAY_EMPTY_BLOCK` 경로는 cargo 단위에서만 살아 있었고 프로덕션·차등에선 empty block 이 일반 block 으로 흘러 margin 이 관통하지 않았다 (`empty-first-child-padded` solid.y 51 vs Chrome 31 로 별도 확증) → §8.3.1 조건(block-level·상하 pad/border 0·height auto·min-height 0·content_h 0·BFC 아님)으로 코드 2 발행. ② **꼬리 margin chain 의 포함/탈출 미구분** — `can_collapse_bottom=false` 면 chain 을 inFlowBottom 에 포함 (§10.6.3), true 면 탈출·제외; 컨테이너 auto height 는 자식 bbox max 를 버리고 **inFlowBottom 단독** (bbox 는 꼬리 self-collapsing box 의 as-if-border 자리 rect 와 음수 bottom margin 을 부풀린다). 위치 = "non-zero bottom border 가 있었다면" 자리, 부모 top 과 collapse 하는 선두 chain 은 부모 top border edge (`empty-first-chain-through-wrap` empty/solid/wrap y 40 — 종전 30/60/30). Rust block 3 + tree 3.
- **인접 수리 (같은 §8.3.1 모델 — Chrome 실측 7 케이스 중 5 발산)** — `bfc_flag` 가 BFC 자식의 **자기** top margin 을 합산(`prev + m_top`)하고 bottom 탈출을 0 으로 막았다: §8.3.1 BFC 조항은 "자기 in-flow 자식과 collapse 안 함" 뿐이고 그 차단은 tree.rs 가 자식 solve `can_collapse_*=false` 로 이미 한다 → block.rs 이중 차단 제거 (`bfc-sibling-top-collapse` b.y 40→30 · `bfc-last-child-margin-escape` sib.y 10→30 · `bfc-first-child-top-escape` wrap.h 30→10). line box 는 margin 을 collapse 하지 않는다 — 새 line box 시작 시 pending chain 을 놓고 마지막이 line box 면 탈출 0 (`block-margin-then-line-box` b.y 10→20; 종전 inline 분기가 `prev_margin_bottom` 을 버렸다). 기존 단위 `test_bfc_blocks_collapse`(150)·`test_bfc_child_no_bottom_collapse_to_parent`(0) 는 Chrome 반증으로 반전 (130 · 35). 범위 판정: Phase 3 = 엔진 block/inline 경로의 Chrome 차등 증명이고 수리 1~8 과 같은 절차 (RED→root cause→GREEN), scope 변경 아님.
- r9l1 **확증·fixed** — 규범 귀속: css-align-3 §9.1 "a block-level or inline-level block container that is a **block-axis scroll container** always has a last baseline set, whose baselines all correspond to its block-end margin edge" — CSS 2.1 §10.8.1 "overflow other than visible" 문면을 scroll container 로 갱신한 조항이고 clip 은 scroll container 가 아니다 (css-overflow-3 §3.1). Chrome 결과와 규범 일치 — css-overflow-3 자체에 baseline 조항이 없다는 Codex 지적은 맞음 (evidence·tree.rs 주석에 인용 교체).
- r9l2 **확증·fixed** — `tree.rs` NodeStyle baseline 계약 주석 · `write_block_item` doc · `block.rs` 모듈 doc (margin collapse 모델 + 슬롯 7/18 미소비 명시) 정정.

검증: cargo **362** (+10) · adr923 34/34 · golden 15 · layout_trace 10 · tree_golden 11 · clippy 신규 0 (경고 7 기존 위치) · wasm 재빌드 · 차등 **41/41** (39 케이스 + 게이트 2 — 신규 12 중 RED 10 → GREEN) · 대조군 **발산 18 / 정합 21** · full parity **975 pass** (기존 GridListItem/Tooltip 2 실패만) · layout unit 49 files/401 · type-check 0. · Live: 실 빌더(localhost:5173) TEST 프로젝트 재로드 — Skia 전 페이지 정상 렌더, Page 2 wrap 카드 배치 불변(Desert Sunset·Hiking Trail 한 줄 + Mountain Sunrise 줄바꿈) 확대 확인, Home Button/avatar 스택 정상, 콘솔 에러 0 (로드 시점 포함). r9h1 프로덕션 경로(패널 shorthand overflow:clip → 어댑터 → wasm)는 pipelineLeg 게이트가 고정. → **Phase 3 재닫힘** (Claude 판정 — Codex 재판독 대기). Phase 4 진입은 사용자 승인 후.

**round 9 후속 (claude, 사용자 승인 "권장대로 진행해")** — 관찰 2건 처리:

- ① `height: 0` 명시 self-collapsing **fixed** — RED `height-zero-self-collapsing` (b.y 60 vs Chrome 40, root 70 vs 50) 재현; 대조군 `height-zero-with-content-not-self-collapsing` (b.y 60) · `self-collapsing-wrapper-of-empty` (wrap.y 40 — as-if-border 집합에 e 의 mt·mb 가 함께 adjoining) 는 첫 실행 PASS. 수리 = 자식 `solve_block` 이 §8.3.1 재귀 정의로 `self_collapsing` 플래그 판정 (in-flow 하단 0 · line box 없음 · 상하 pad/border 0 · height auto/0 · min-height 0 · BFC 아님) + leaf(solve_block 미경유) 는 intake 가 explicit 0 + content_h 0 로 판정. 내용 있는 height:0 은 플래그 false 로 제외. Rust `adr923_p3_r9_height_zero_self_collapsing` (양쪽). 차등 **44/44** (42 케이스 + 게이트 2) · cargo 363 · parity 978 (기존 2) · layout unit 401 · type-check 0 · live 재로드 무회귀.
- ② TS DC-6 overflow cap **수리 안 함 — Phase 4 인벤토리 등재**: `enrichWithIntrinsicSize` (`utils.ts:4531`, 게이트 `needsHeight = !rawHeight` 라 height 미지정 요소 전부 대상) 가 `overflow !== "visible"` 이면 주입 intrinsic 크기를 available 로 cap — 엔진 flex §4.5 의 TS 중복이고 block 문맥에도 걸며 clip 을 hidden 과 같이 취급. clip 만 빼면 증상 수정이라 Phase 5 cutover 제거 목록(TS 시뮬레이션 제거)으로 — breakdown Phase 4 항목 + Q4 소비 경로 메모.

## Round 10 — 2026-09-01 (reviewer: codex, implementation-verification of round 9 repairs + 후속 ①② — `74ce66563` · `c8337266d` · `3eab3cd79` · `166be9b11`)

> 실행자(Claude) ≠ 판독자(Codex). 재실행 전부 재현: cargo 363 · adr923 35/35 · clippy 신규 0 · wasm 재빌드 + focused 차등 44/44 · parity 978 (기존 2) · layout unit 401 · type-check 0 · 원복 RED (a)~(h) 8종 전부 해당 케이스만 RED (단, (c) 는 지정 4건 외 self-collapsing-wrapper-of-empty 도 RED = 5건 — r10l1) · 재집계 42행·18/24·기존 39행 불변. 과제 10 중 PASS 1·4·6·8·10 / FAIL 2·3·5·7·9. Phase 3 **열림**.

- r10h1 HIGH correctness — height:0 Text leaf 를 self-collapsing 으로 잘못 분류 (`tree.rs` leaf predicate 가 line box/leafBaseline 을 보지 않고 구조·높이만 사용; harness pipeline 이 Text 경로를 성립시켜 evidence "빌더 성립 경로 없음" 반증 — Chrome 다음 형제 60 / pipeline 40).
- r10m1 MEDIUM correctness — absolute-only 자식의 height:0 컨테이너가 self-collapse 하지 않음 (in-flow 필터 후 leaf 경로가 플래그 false 반환, intake 는 원본 `children.is_empty()` 기준 — Chrome 40 / 엔진 60).
- r10m2 MEDIUM correctness — 3개 이상 혼합 부호 margin chain 이 결합 순서에 따라 오계산 (`collapse_margins` 이항 누적이 전체 adjoining 집합의 양/음 extrema 를 보존하지 않음 — Chrome 30 / 엔진 35).
- r10m3 MEDIUM correctness — 음수 margin auto-height 가 음수 NodeLayout 높이 생성 (inFlowBottom 에 비음수 바닥 없음 — Chrome 2/1 / 엔진 −8/−9).
- r10l1 LOW evidence-missing — 원복 (c) RED 영향이 4건이 아니라 5건.
- r10l2 LOW doc-drift — breakdown 대조군 수치가 39·18/21 로 남음.
- r10l3 LOW doc-drift — `bfc_flag` 필드 설명이 실제 미소비 계약과 모순 (block.rs 표 · tree.rs helper 주석).
- 과제 부수 판정: ① scroll-container 술어 PASS (css-overflow-3 §3.1 computed value · flexbox §4.5) · ④ BFC 이중 차단 제거 PASS (overflow:hidden block 자식 반례도 Chrome 일치, 슬롯 7 소비처 없음) · ⑥ 반전 단위 2건 PASS (Chrome 형상 동일, 원복 시 150/0 으로 RED) · ⑧ 프로덕션 소비 경로 PASS (generic block > block `buildTreeBatch` payload 양쪽 block 유지 캡처 → 수리 10~~13 활성; DC-6 file:line 정확) · ⑩ `729b1d5e5` 무영향.
- 관찰: 정상 44행은 §8.3.1 구조적 경계 전체를 덮지 않는다 / 임시 probe·artifact 삭제, 원복 복구 / git status 는 범위 밖 2파일 (`.claude/stats` · `translations.ts`) 로 clean 아님.

**main 확증·수리 (claude)** — Chrome 실측 RED 원천: 신규 케이스 9 (8 RED) + pipelineLeg 게이트 3 (2 RED) — 수치 전부 Codex 판정과 일치 (r10m2 는 별도 fixture 로 20 / 35):

- r10h1 **확증·fixed (root cause 2겹)** — RED `text-leaf-height-zero-has-line-box` (engine leg `engineStyle.leafBaseline` — b.y 60 vs 40) + 게이트 `height:0 Text leaf` · `width 명시 height:0 Text leaf` (pipeline 40). ① 엔진: leaf 경로 self-collapsing 판정에 `leaf_baseline.is_none()` — 텍스트 leaf 는 이 스칼라가 line box 존재 신호. ② TS: `enrichWithIntrinsicSize` 의 early return 2곳 (`!needsHeight && !needsWidth` · `contentHeight <= 0`) 이 height 명시 텍스트 leaf 를 공급 전에 반환 + 텍스트 분기가 `needsWidth`(flex/grid 자식 한정) 에 묶여 **block 부모 텍스트 leaf 전부**가 leafBaseline 을 못 받았다 (Phase 2 계약 gap) → 텍스트 있으면 공급, 빈 텍스트는 미공급 (대조군 게이트 40 PASS). evidence 관찰 "빌더 성립 경로 없음" 취소선 + 수리 14. 부수효과 공시: block 부모 텍스트 leaf baseline 이 bottom 폴백 → 첫 줄 baseline (Chrome 방향, parity 회귀 0).
- r10m1 **확증·fixed** — RED `abs-only-height-zero-self-collapsing` (b.y 60 vs 40; auto 대조군 44 는 첫 실행 PASS). 판정을 `solve_node` leaf 경로로 이관 (pad/border 0 · height auto/0 · min-height 0 · BFC 아님 · leafBaseline 없음) — intake 는 플래그만 읽고 `write_block_item` 의 auto-height 코드 2 규칙 제거 (**단일 원천**; r9m2 이중 층 교훈 메모리 적용). 수리 15.
- r10m2 **확증·fixed (3층)** — RED `mixed-sign-chain-three-empties` (b.y 35 vs 20) · `mixed-sign-chain-hoisted-through-wrapper` (g.y 35 vs 20 — 손자 −20 이 wrapper 25·형제 10·c 30 과 한 집합) · `mixed-sign-chain-self-collapsing-wrapper` (35 vs 20). root cause 는 Codex 지적 (이항 누적) 이 형제 chain 뿐 아니라 self-collapsing 관통 · 부모-자식 hoist 경계(`escaped_mt/mb` 스칼라, 슬롯 3/5 이항 collapse) 까지 3층 — `block.rs` `MarginSet {pos, neg}` (최대 양수 + 최소 음수) 로 커널 상태 3종 + 경계 (Node `escaped_*`, 슬롯 19/20 음수 성분, meta 4/5) 를 집합으로 운반. `FIELD_COUNT` 19 → 21, golden `block_child`/contract guard 21 (기대값 무변경 — Chrome 값 원칙). 수리 16.
- r10m3 **확증·fixed + 인접 1** — RED `negative-top-margin-padded-auto-height-clamped` (root.h −8 vs 2; fixture 의 `padding` shorthand 는 엔진 raw 계약 밖이라 longhand 로 — 대조군 pipeline 은 정합) · `negative-bottom-margin-contained-clamped` (−9 vs 1) → `container_h = flow_bottom.max(0)`. 인접 RED `negative-flow-bottom-not-self-collapsing` (b.y 40 vs 60): 종전 self-collapsing 판정이 `in-flow bottom ≤ 0` 을 근거로 써 음수 margin 컨테이너를 빈 상자로 오판 → 자식 코드 전수(코드 2) 로 교체. 수리 17·18.
- r10l1 **확증·fixed** — evidence 검증 절에 5건 정정 (round 10 이후 코드 2 원천은 플래그 하나라 원복 단위도 하나).
- r10l2 **확증·fixed** — breakdown :125 → 51 케이스 18/33 (42 18/24 · 39 18/21 · 27 18/9 이력 병기).
- r10l3 **확증·fixed** — `block.rs` 필드표 슬롯 7 "미소비" · `tree.rs node_establishes_bfc` doc 소비처 2 (self-collapsing 제외 · 슬롯 7 inert) 정정.

검증: cargo **368** (+5: tree 4 · block 1 — adr923 40/40) · golden 15 · layout_trace 10 · tree_golden 11 · doc 1 · clippy 신규 0 (경고 7 기존) · wasm 재빌드 · 차등 **56/56** (51 케이스 + 게이트 5) · 대조군 발산 18 / 정합 33 · full parity **990** (기존 catalogComponentBox 2 · 1 expected fail · 2 skipped) · layout unit 401 · type-check 0. Live: 실 빌더 TEST 프로젝트 재로드 — Skia 전 페이지 정상, Page 2 wrap 카드 불변, Components 텍스트 leaf 배치 불변, 콘솔 에러 0. 범위 밖 dirty 2파일은 그대로 (커밋 제외). → **Phase 3 재닫힘** (Claude 판정 — Codex 재판독 대기). Phase 4 진입은 사용자 승인 후.

## Round 11 — 2026-09-01 (reviewer: codex, implementation-verification of round 10 repairs — `18a494216` · `a31d4f9b8`)

> 실행자(Claude) ≠ 판독자(Codex). 재실행 전부 재현: cargo 368 · adr923 40/40 · clippy 신규 0 · wasm 재빌드 + focused 차등 56/56 · parity 990 (기존 2) · layout unit 401 · type-check 0 · 원복 RED (a)~(h) 8종 전부 해당 케이스만 RED (단, (b) 는 hoisted 1건만 · (g) 는 지정 2 + negative-flow-bottom 3건 — r11l1) · 재집계 51행·18/33·기존 42행 불변·문서 5곳 일치. 과제 10 중 PASS 4·5·6·7·8 / FAIL 1·2·3·9·10. Phase 3 **열림**.

- r11h1 HIGH correctness — normal 공백-only Text 가 line box 로 오분류되어 margin 이 20px 덜 접힘 (신호가 CSS whitespace 처리 후 내용이 아니라 raw nonempty — Chrome `" "` 40 / engine·pipeline 60; pre 60/60 · fontSize:0 "x" 60/60 · display:none 20/20 은 일치).
- r11m1 MEDIUM correctness — explicit height/min-height 부모에서 마지막 자식 margin 이 부모 밖으로 잘못 탈출 (`can_collapse_bottom` 이 height:auto 와 min-height:0 을 검사하지 않음 — Chrome height:50px 50/60 vs 70/80 · min-height:10px 55/65 vs 80/90).
- r11m2 MEDIUM evidence-missing — block Text 의 즉시 flex/inline-block baseline 위치 변화 공시가 현 프로덕션 소비 경로와 다름 (payload 에 leafBaseline 은 있으나 어댑터가 inline-block → block leaf · block 부모 → flex row 시뮬레이션 alignItems:center, `parse_align_items` 는 baseline → stretch).
- r11l1 LOW evidence-missing — 원복 (b)/(g) 예상 실패 집합이 실측과 다름.
- r11l2 LOW doc-drift — FIELD_COUNT/meta 확장 후 주석 4곳이 19필드/후행 2·4값.
- 과제 부수 판정: ① MarginSet 산술 정합 (pos/neg 채널 분리 · can_collapse_top=false 시 meta 0) · ④ 코드 2 발행 intake 1곳 · 플래그 원천 leaf/solve_block · cache HIT 은 own style 변경이 dirty → 재계산 시 리셋 (block→flex→block probe PASS) · ⑤ 0 하한의 raw inFlowBottom 소비 1곳 (`child_h + m_bottom` 은 baseline 폴백) · ⑥ padding longhand 는 raw NodeStyle 계약 (shorthand 없음, engineLeg 미분배) — 유리한 fixture 변경 아님 · ⑧ 슬롯 19/20=0 이면 종전 signed 와 동치, golden 기대값 무변경 · ⑩ 미커버: root margin · clearance · float/clear · anonymous block · inline↔block 전환 · percentage/min/max-height × bottom collapse · writing-mode · multicol · 생성 콘텐츠.
- 관찰: HEAD 895f33bec 의 후속 커밋은 대상 파일 무변경 / 범위 밖 dirty 8 (Transform 2 · Skia 5 · CHANGELOG) 보존 / task-state 열림 갱신.

**main 확증·수리 (claude)** — Chrome 실측 RED 원천: 신규 케이스 10 (3 RED + Blink 바인딩 경계 3 은 1차 수리안에서 RED) + pipelineLeg 게이트 9 (4 RED):

- r11h1 **확증·fixed** — RED 게이트 normal `" "` · `" \t\n "` · nowrap `" "` · pre-line `" \t "` (pipeline 60 vs Chrome 40); 대조군 pre-line `"\n"` · pre · pre-wrap · nbsp · fontSize:0 `"x"` 는 60 첫 실행 PASS. root cause 는 Codex 지적 그대로 (raw nonempty) → `textLeafRendersContent(text, whiteSpace)` (CSS Text 3 §4.1.1: normal/nowrap 은 space·tab·segment break 전부 collapsible, pre-line 은 segment break 보존, pre 계열 전부 보존) 를 leafBaseline **과 폭 스칼라** 의 공통 신호로 (collapse 로 사라진 내용의 측정폭은 공백 폭 — Chrome max-content 0). 엔진 무변경. 수리 19.
- r11m1 **확증 절반 · 절반은 Chrome 이 정정 · fixed (2건)** — height:auto 조건 확증: RED `parent-explicit-height-bottom-margin-contained` (70 vs 50) · 인접 `parent-height-zero-bottom-margin-contained` (20 vs 0 — height:0 은 auto 가 아니다, `container_h` 도 content 0) · `parent-height-zero-min-height-used` (30 vs 10). **`min-height: 0` 조건은 §8.3.1 adjoining 정의에 없다** (self-collapsing 조건) — Chrome 미바인딩 min-height:10 은 접힘 유지 (b.y 40; Codex 의 55 는 fixture 미공개 — 1차 수리안 `min_h > 0` 일괄 포함이 정확히 55 로 발산해 Chrome 이 기각). 대신 **바인딩** 경계에서 Blink 모델 확정: min-height:100 → 115 (탈출 120 반증) · min-height:30 → p.h 30 · 45 (포함-후-clamp 40·55 반증) · max-height:10 → 25 → flow_bottom 확정 뒤 `min_h > content || max_h < content` (content-box) 면 strut 미전파 (`escaped_bottom_set = ZERO`, 사용 높이는 부모 intake `clamp_size`). top 조건 무관 대조군 (p.y 30) · min-height:0 · max-height:100 첫 실행 PASS. 수리 20·21. Rust `adr923_p3_r11_parent_bottom_collapse_auto_height_and_unbound_min_max` 7 변형.
- r11m2 **확증·fixed** — evidence 프로덕션 영향 round 10 bullet 정정 (payload 공급 ≠ baseline 소비 — 어댑터 :514/:526 + `parse_align_items` stretch 폴백, 즉시 가시 변화 0, Phase 5 + S8 이후 조건; `measureFontMetrics` 비용 공시). 코드 무변경.
- r11l1 **확증·fixed** — evidence 검증 절에 (b) 1건 / (g) 3건 실측 기록 + "원복 RED 는 실측 후 기록" 규약.
- r11l2 **확증·fixed (+5)** — Codex 4곳 (block.rs:4 · :116 · tree.rs:2739 · golden.rs:301) + 추가 5곳 (tree.rs:41 OUT `4n+2` · :2655 · :4895 · :4897 "코드 2 는 intake 가 내지 않음, block.rs 사전 분류" (r10m1 이후 오류) · golden.rs:329 후행 2값).

검증: cargo **369** (+1 — adr923 41/41) · golden 15 · layout_trace 10 · tree_golden 11 · doc 1 · clippy 신규 0 (경고 7 기존, 행 이동) · wasm 재빌드 · 차등 **75/75** (61 케이스 + 게이트 14) · 대조군 발산 18 / 정합 43 · full parity **1009** (기존 catalogComponentBox 2 · 1 expected fail · 2 skipped) · layout unit 401 · type-check 0. Live: 실 빌더(localhost:5173) TEST 프로젝트 재로드 (Chrome MCP) — Skia 전 페이지 정상, Page 2 wrap 카드 불변 (Desert Sunset·Hiking Trail 한 줄 + Mountain Sunrise 줄바꿈), Components 페이지 텍스트 leaf (메뉴 항목·폼 label·리스트 행 — 수리 19 신호 함수 경유) 배치 불변 확대 확인, 콘솔 에러 0 (로드 시점 포함). → **Phase 3 재닫힘** (Claude 판정 — Codex 재판독 대기). Phase 4 진입은 사용자 승인 후.
