---
adr: 206
title: 엔진 늘어난 크기 definite 전파 + grid 암묵 트랙 준수
reviews:
  - round: 1
    ts: '2026-09-07T09:07:26.397Z'
    reviewer: claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          Context 의 '①은 빌더의 기본 패턴 (height:100% 자식) 에 직접 닿는다' 는 근거 없음 —
          factory·preset 은 height:% 를 생성하지 않고 (grep 0건) catalog 도달은
          Meter·ProgressBar Value 2곳뿐
        evidence: >-
          grep -rn 'height: "100%"' apps/builder/src/builder/factories → 0건;
          packages/shared/src/catalog/generated/componentRulesTable.ts:7058 ·
          8339 (Meter/ProgressBar Value, template 없는 Track grid area 안);
          presetDefinitions.ts 는 width:100% 만 / ADR:19
        root_cause: 대조 문서 §2 의 fixture (F5) 가 production 패턴이라는 추정을 grep 없이 승계
        outcome: fixed
        addressed_in: >-
          round 1 반영 — ADR:19 재서술 (catalog Value 2곳 + Styles 패널 입력, 규모는 Phase 0
          N/M 으로만) · breakdown F19 신규 · F14 live 판정에 Value 높이 추가
      - id: m2
        severity: MEDIUM
        category: other
        summary: >-
          G1 fixture 가 단일 라인만 — Chrome 은 multi-line (flex-wrap: wrap) stretch
          item 도 확정으로 본다 (실측 W3 inner 50 · W4 57.5, 엔진 0). 채널이 컨테이너 cross 를 기준
          삼으면 W4 가 RED 인 채 게이트를 통과한다
        evidence: >-
          리뷰 round 1 Chrome 프로브 (runParityCase, 2026-09-07): W2 wrap 1라인 100/0 ·
          W3 wrap 2라인 50/0 · W4 align-content stretch 분배 라인 115 → 57.5/0 · W5
          column wrap width 50% 일치 / ADR:126 G1 · breakdown Phase 1 fixture 목록
          (F5·B1d·B6c 전부 단일 라인) / CSS-FLEXBOX-1 §9.8 'single-line' 한정
        root_cause: >-
          §9.8 을 spec 문장 그대로 인용하면서 multi-line 케이스를 측정하지 않음 — oracle 은 Chrome 이라
          spec 한정과 다른 동작이 fixture 없이는 안 보인다
        outcome: fixed
        addressed_in: >-
          round 1 반영 — ADR:21 Chrome multi-line 실측 기록 · G1 에 W3·W4 추가 (원복 RED
          5건) · breakdown Phase 1 fixture·원복·파일 변경표 갱신
      - id: l1
        severity: LOW
        category: evidence-missing
        summary: >-
          F7 '10,000 가드 4곳' 은 5곳 (grid.rs:788 first_free_row 누락) · F8 '폴백 100
          1곳' 은 3곳 (:685 높이 · :1187 column-flow) — Phase 2 변경표가 그 줄 번호만 들면 수정 누락
        evidence: >-
          grep -n 10_000 packages/composition-engine/src/grid.rs →
          788·1008·1014·1038·1044; grep -n 'unwrap_or(100.0)' grid.rs →
          682·685·1187 / breakdown F7·F8·Phase 2 표
        root_cause: 확인 명령의 출력 건수를 표에 적지 않아 부분 인용이 전수처럼 읽힘
        outcome: fixed
        addressed_in: >-
          round 1 반영 — breakdown F7 (5곳) · F8 (3곳) · Phase 2 변경표 2행 · 확인 명령에 건수
          병기
    hate:
      assumption: >-
        ① (stretch/grid area/aspect 확정값이 손자 % 의 base) 이 production 문서에 실제로 도달한다
        — 그래야 R1 의 '배치 변화' 와 Decision 의 대칭 편익이 성립 (ADR:19)
      root: >-
        factory·preset 은 height:% 를 만들지 않고 catalog 도달은 Track 안 Value 2곳뿐이다.
        Track 이 implicitStyles 로 가려져 있으면 (R5) ① 의 catalog 도달도 같이 가려지고, 남는 것은
        사용자가 손으로 넣은 % 높이뿐 — 편익은 남지만 규모가 '기본 패턴' 이 아니다
      axis: >-
        load-bearing 사실 거짓 (Context 서술 vs grep) — 결정을 뒤집지는 않는다 (D3 대칭 위반은 도달 규모와
        무관하게 결함)
      first_nail: >-
        G0 의 F14 live 판정 1회에 Value 높이 읽기를 추가 (같은 시드, 추가 비용 0) + BC 인벤토리 N/M —
        Phase 1~3 전체 (엔진 2파일 + fixture 2 + 문서 5) 보다 싸다
      verdict: >-
        G0 커버 (round 1 에서 F14 live 판정 항목에 Value 높이를 추가해 nail 을 게이트에 넣음) —
        issue:m1 은 서술 정정으로 fixed
    prism:
      lenses:
        - lens: 정합성/SSOT (D3 대칭 · ADR-164 경계)
          verdict: pass
          reason: >-
            변경이 엔진 Rust 2파일에 갇히고 TS 보정 재도입이 Hard Constraint 4 로 금지 —
            layout-engine.md:217 규칙이 살아 있음
        - lens: 측정 무결성 (oracle · 대조군 · 원복)
          verdict: pass
          reason: >-
            모든 게이트 값이 Chrome getBoundingClientRect 차등 (measurement-validity §2
            #3 회피) 이고 원복 RED · 대조군 GREEN 이 짝으로 요구된다 — round 1 이 multi-line 을 추가해
            fixture 차원 (Q5) 을 넓혔다
        - lens: 비용/성능 (solve pass 증가)
          verdict: pass
          reason: >-
            재-solve 가 stretch + definite 라인 조건부이고 G3 가 ADR-169 G4 와 같은 bench +
            600 요소 frame lane 두 층으로 상한 — 실패 시 대안 (자손 % 존재 게이트) 이 명시됨
        - lens: 되돌림/rollout (기존 문서 배치 변화)
          verdict: unclear
          reason: >-
            변화 규모가 Phase 0 N/M 에 달려 있고 아직 0 — Positive 방향이라도 사용자에게는 '달라짐' 이다
            (R1). G0 이 수치 없이는 착수를 막으므로 설계 결함은 아니다
      convergence: 다른 이유로 일치 (3 pass + 1 unclear 는 G0 이 해소)
      question: 없음
---
# ADR-206 Review Log

## Round 1 — 2026-09-07 (reviewer: claude)

## ADR 리뷰 결과: ADR-206 엔진 늘어난 크기 definite 전파 + grid 암묵 트랙 준수

### 구조 검증

| 섹션 | 판정 | 품질 | 인용 |
| --- | --- | --- | --- |
| Context (D3 선언 · hard 5 · soft 3) | PASS | HIGH | `ADR:9` D3 선언 · `ADR:23-29` hard 5 (Δ≤1px · 회귀 0 · bench +5% · NodeStyle 55 · 10,000 clamp) |
| Alternatives (4개) | PASS | HIGH | `ADR:39-77` A/B/C/D, 각 외부 근거 (Taffy #1003/#1123 · Blink ConstraintSpace · Yoga · stretch crate) |
| 4축 위험 평가 | PASS | HIGH | `ADR:43-47 · 53-57 · 63-67 · 73-77` 전 대안 4축 |
| Risk Threshold Check | PASS | HIGH | `ADR:81-88` 표 + 루프 판정 (A·C HIGH 0, D CRITICAL 기록만) |
| Decision 위험 수용 근거 + 기각 사유 | PASS | HIGH | `ADR:92-104` 근거 3 · 기각 B/C/D 각 사유 |
| 구현 상세 분리 | PASS | HIGH | `ADR:106` 포인터 · breakdown 119행 |
| Risks (Decision 뒤 / Gates 앞, ID 표) | PASS | HIGH | `ADR:108-119` R1~R6 + "잔존 HIGH 위험 없음" |
| Gates | PASS | HIGH | `ADR:121-130` G0~G5, 실패 시 대안 전부 기재 · Live Exercise 자리 `ADR:132` |
| adr-writing seed (HIGH+ 3경로 · BC 수식화 · M3) | PASS | — | HIGH 없음 (N/A) · BC 는 `ADR:33` N/M + G0 · M3 인용 `ADR:125` |
| Fork 4 질문 + 사용자 confirm | PASS | — | breakdown §1 (완전 신규 주제, 2026-09-07 AskUserQuestion 기록) |

### 코드 검증 (breakdown F1~F18 전수 + 본문 주장)

| 주장 | 판정 | 근거 |
| --- | --- | --- |
| F1 flex `child_containing_h = if explicit_h > 0.0` | VERIFIED | `tree.rs:2131` |
| F2 3.5 재-solve `solve_node(c, used_main, cs_h)` main 만 | VERIFIED | `tree.rs:2576` (`cs_h` 는 1차 `child_solves[i]`) |
| F3 block 동일 게이트 | VERIFIED | `tree.rs:2977` |
| F4 grid 셀 `solve_node(c, w, h)` available 만 | VERIFIED | `tree.rs:3753` |
| F5 `aspect_h_floor = Some(transferred)` · explicit_h 유지 | VERIFIED | `tree.rs:1768` (`floor_applies = !children.is_empty() && …`) |
| F6 `block_fits` `c0 + cs - 1 > cols` → false | VERIFIED | `grid.rs:754-757` |
| F7 10,000 가드 "4곳" | **PARTIAL → 5곳** | `grid.rs:788` (`first_free_row`) 누락 — l1 |
| F8 폴백 100 "`:682`" | **PARTIAL → 3곳** | `grid.rs:685` (높이) · `:1187` (column-flow) — l1 |
| F9 `parse_implicit_track_size` 첫 토큰 px 만 | VERIFIED | `grid.rs:1094-1102` |
| F10 암묵 열 생성 column-flow 한정 | VERIFIED | `grid.rs:1183-1186` (`flow_column && child_count > 0`) |
| F11 암묵 열 합성이 `inline_intrinsic` 분기 안 | VERIFIED | `tree.rs:3390` |
| F12 `SizingFn::Definite` 통째 | VERIFIED | `tree.rs:4446 · 4489` |
| F13 `auto_row_tokens` 순환 | VERIFIED | `tree.rs:3469 · 3496-3497` (`% auto_row_tokens.len()`) |
| F14 catalog template 없는 grid 6 규칙 | VERIFIED | `componentRulesTable.ts` `display: "grid"` 10곳 중 7011·8289·10430·10490 은 `gridTemplateColumns` 보유, 나머지 6 (7261·7357·8543·8603·8645·10703) 무 |
| F15 ledger "판정은 `explicit_h > 0.0` 하나" | VERIFIED | `layout-css-parity-ledger.md:396` |
| F16 격자 "중첩 2단 이상" 사각 | VERIFIED | `layout-engine.md:194` |
| F17 Chrome 실측 7건 | VERIFIED | 대조 문서 §2 행 F5·B1d·B6c·G4·G12·G11·G10 + §6 fixture |
| F18 A 묶음 `0b1cecb4a` | VERIFIED | `git show 0b1cecb4a --stat` · ledger §24 (`:640`) |
| Hard 2 "parity 1,122 · 기존 실패 2건" | VERIFIED | round 1 전량 실행: 1,122 passed · 2 failed (GridListItem · Tooltip) · 2 skipped = 1,126 (`ADR:77` 의 1,126 과 정합) |
| Hard 3 `cargo bench tree_solve` · ADR-169 G4 | VERIFIED | `Cargo.toml:31` · `benches/tree_solve.rs` · ADR-169 `:124 · :152` |
| Hard 4 `NODESTYLE_FIELD_COUNT` 55 · `FLEX_FIELD_COUNT` 21 | VERIFIED | `tree.rs:289` · `flex.rs:119` |
| 대안 B 기각 근거 — Step 4.5 1회 계약 · 폭 재보정 금지 | VERIFIED | `layout-engine.md:213 · :217` · ADR-165 `:81 · :124` |
| Context "①은 빌더의 기본 패턴" | **UNVERIFIED → 정정** | factory·preset grep 0건, catalog Value 2곳 (`:7058 · :8339`) — m1 |
| Phase 1 규범 "flexbox §9.8 stretch 확정" 의 multi-line | **PARTIAL → 실측 보강** | Chrome W3 50 · W4 57.5 (엔진 0) — m2 |
| preset 주석 `presetDefinitions.ts:21` | VERIFIED | `:21-22` "`repeat()` / `minmax()` 는 쓰지 않는다 — 2026-07-25 실측" |

### 위험 평가

| 위험 | 증거 | 심각도 | 상태 |
| --- | --- | --- | --- |
| R1 기존 문서 배치 변화 | `ADR:33` N/M 미측정 (G0 차단) | MED | 기재됨 |
| R2 `%` 4번째 누수 경로 | ledger `:408-427` 3 경로 이력 · `tree.rs:2131 · :2977` 두 게이트 | MED | 기재됨 — round 1 이 multi-line 대조 (W3·W4 는 positive) 를 G1 에 추가 |
| R3 암묵 트랙 vs 커서·`col_min` | `grid.rs:1183-1190` column-flow 분기 · `tree.rs:3390` 합성 | MED | 기재됨 |
| R4 solve pass 증가 | ADR-169 `:152` (3.5 재-solve 매 레벨 → 2^d 사고 이력) | MED | 기재됨 — G3 |
| R5 Track implicitStyles 마스킹 | F14 6 규칙 · 인벤토리 함정 메모리 `feedback-boundary-inventory-counts-canvas-injection-as-production-fact` | LOW | 기재됨 — round 1 이 Value 높이 (① 도달) 를 같은 live 에 묶음 |
| R6 ledger drift | `layout-css-parity-ledger.md:396` | LOW | 기재됨 |
| 레이아웃 부작용 (layoutVersion 체인) | 변경이 엔진 내부 해석뿐 — `NodeStyle` 필드·TS 키 무변경이라 `LAYOUT_STYLE_KEYS` 갱신 불요 | — | 누락 아님 |
| 렌더링 파리티 (Skia/CSS) | 엔진 값은 Skia 유일 소비, Preview 는 Chrome — 변경이 곧 대칭 복구 | — | 누락 아님 |

누락 위험: **HIGH 0**. m1·m2·l1 은 위험 표가 아니라 Context 사실 · 게이트 fixture 차원 · 인벤토리 건수의 결손이며 round 1 에서 반영했다.

### 근본 반론 (hate)

- **load-bearing 가정**: ① 이 production 문서에 실제로 도달한다 — R1 의 규모와 Decision 의 대칭 편익이 그 위에 선다 (`ADR:19`).
- **root**: factory·preset 은 `height:%` 를 만들지 않고 (grep 0건) catalog 도달은 Track 안 Value 2곳뿐이다. Track 이 implicitStyles 에 가려지면 (R5) ① 의 catalog 도달도 같이 가려지고, 남는 것은 사용자가 손으로 넣은 `%` 뿐 — 편익은 남지만 '기본 패턴' 이 아니다. 공격 축: load-bearing 사실 (Context 서술 vs grep). Decision 은 뒤집히지 않는다 — D3 대칭 위반은 도달 규모와 무관한 결함이고 Chrome 실측이 있다.
- **first nail**: G0 의 F14 live 1회에 Value 높이 읽기를 추가 (같은 시드, 추가 비용 0) + BC 인벤토리 N/M — Phase 1~3 (엔진 2파일 + fixture 2 + 문서 5) 보다 싸다.
- **판정**: **G0 커버** — round 1 에서 nail 을 breakdown Phase 0 항목에 넣었고 m1 은 서술 정정으로 fixed.

### 렌즈 대조 (prism) — breakdown phase 3 이라 실행

| 렌즈 (failure mode) | 판정 | load-bearing 이유 |
| --- | --- | --- |
| 정합성/SSOT (D3 대칭 · ADR-164 경계) | pass | 변경이 엔진 Rust 2파일에 갇히고 TS 보정 재도입은 Hard 4 로 금지 (`layout-engine.md:217`) |
| 측정 무결성 (oracle · 대조군 · 원복) | pass | 모든 게이트 값이 Chrome 차등 (measurement-validity §2 #3 회피), 원복 RED · 대조군 GREEN 짝 — round 1 이 multi-line 차원 (Q5) 을 넓힘 |
| 비용/성능 (solve pass) | pass | 조건부 재-solve + G3 두 층 (bench · frame lane) + 실패 시 대안 명시 |
| 되돌림/rollout (기존 문서 변화) | unclear | 규모가 Phase 0 N/M 에 달려 있고 아직 0 — G0 이 수치 없이 착수를 막으므로 설계 결함 아님 |

- **수렴**: 다른 이유로 일치 (3 pass, 1 unclear 는 G0 이 해소)
- **해결 질문 1**: `불일치 없음`

### round 1 에서 직접 반영한 것 (동작 변경 0 — 문서만)

- ADR `:19` ① 도달 경로 재서술 (catalog Value 2곳 + Styles 패널) · `:21` Chrome multi-line 실측 (W3 50 · W4 57.5) · G1 에 W3·W4 + 원복 RED 5건.
- breakdown F7 5곳 · F8 3곳 · **F19 신규** (factory/preset grep 0 · catalog Value 2) · F14 live 에 Value 높이 · Phase 1 fixture/원복/파일표 · Phase 2 변경표 2행 · Phase 0 전수 대조 체크 완료.
- Chrome 프로브 파일은 삭제 (fixture 는 Phase 1 에서 `percentSize.browser.test.ts` 에 정식 추가).

### 종합 판정

- **구조**: PASS (8/8 섹션 HIGH · seed · fork 4 질문 confirm 기록)
- **코드 정합**: 23/26 VERIFIED · 3 PARTIAL/UNVERIFIED (F7 · F8 · Context ① 도달 · §9.8 multi-line) → 전부 round 1 반영으로 fixed
- **누락 위험**: 0건 (HIGH 0 / MED 0 / LOW 0) — 이슈 3건은 사실·fixture·인벤토리 결손 (MED 2 · LOW 1), 모두 fixed
- **권고**:
  - **HIGH**: 없음
  - **MEDIUM**: 없음 (m1 · m2 반영 완료)
  - **LOW**: 없음 (l1 반영 완료)
  - **결론**: **승인 가능** — 전제 확정. 착수 조건은 G0 (F14+Value live · BC N/M · bench baseline) 그대로.
