---
adr: 171
title: catalog 레이아웃 값의 소비자 비대칭 해소
reviews:
  - round: 1
    ts: '2026-07-28T15:04:29.465Z'
    reviewer: claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: other
        summary: >-
          누락 위험 — factory 인라인 제거가 Inspector dirty/reset baseline 을 stale 로 만든다
          (ADR·design 전체에 panel/dirty/reset 언급 0건)
        evidence: >-
          apps/builder/src/builder/panels/styles/StylesPanel.tsx:96 ·
          apps/builder/src/builder/panels/styles/hooks/useResetStyles.ts:292
        root_cause: >-
          useResetStyles.ts 가 factory 인라인을 손으로 미러한 baseline 테이블을 보유. Phase 4 가 7
          파일의 layout 인라인을 제거하면 그 항목들이 stale 이 되어 '수정 N' 뱃지·reset 목적지가 어긋난다. 반대
          방향(인라인만 추가, baseline 누락) 회귀 이력이 useResetStyles.ts:292 에 기록되어 있어 결합이
          실증됨. ADR 은 Phase 4 종료 조건을 Skia box 불변 단독으로 뒀다.
        outcome: fixed
        addressed_in: >-
          round 1 — R7 신설 + Gate G4 통과 조건에 패널 live exercise 추가 + design §3 Phase
          4 / §5 파일 변경 예상 등재
      - id: m2
        severity: MEDIUM
        category: migration-cost-unquantified
        summary: >-
          기존 계약 테스트 47 케이스(resolveContainerStylesFallback.test.ts, ADR-080 G1)가
          Phase 3 로 필연 RED 인데 파일 변경 예상에 미등재
        evidence: >-
          apps/builder/src/builder/workspace/canvas/layout/engines/resolveContainerStylesFallback.test.ts:102-110
          (listboxitem/gridlistitem toEqual({}) lock)
        root_cause: >-
          Hard Constraint 6 이 '검증 오라클 0건' 을 end-to-end parity 축 기준으로 서술했는데,
          resolver 단위 계약 lock 은 이미 존재한다. Phase 3 의 단일 판정 통합은 이 47 케이스 반환값을 바꾸므로
          갱신 비용과 갱신 근거(새 기대값 = 실효 CSS)를 미리 선언해야 blind 갱신·skip 을 막는다.
        outcome: fixed
        addressed_in: >-
          round 1 — R8 신설 + Gate G2 통과 조건에 편입 + design §3 Phase 3 / §5 등재(의도된 갱신
          대상 명시)
      - id: l3
        severity: LOW
        category: evidence-missing
        summary: >-
          Hard Constraint 4 의 catalog 키 총계·미보유 수치가 재측정과 불일치 (총 121→123, 미보유
          72→74)
        evidence: >-
          docs/adr/completed/171-catalog-layout-delivery-unification.md:20 ·
          packages/shared/src/catalog/generated/componentRulesTable.ts (brace 매칭
          재스캔)
        root_cause: >-
          A/B 개수(24/25)는 소문자 키(body/frame) 포함으로 셌는데 총계는 PascalCase 만 세어 2 어긋남.
          방향(대다수가 어느 경로도 아님)은 불변이나 Hard Constraint 수치는 정확해야 한다.
        outcome: fixed
        addressed_in: >-
          round 1 — 본문 Hard Constraint 4 + design §4 실측 근거 표 정정, 집계 규칙(경로 A 우선
          return) 명시
      - id: l4
        severity: LOW
        category: adr-structure-violation
        summary: Gate 표 행 순서(G3=Phase 4 → G4=Phase 5)가 결정의 핵심인 실행 순서(3→5→4)와 반대로 읽힌다
        evidence: >-
          docs/adr/completed/171-catalog-layout-delivery-unification.md:130-136 (수정 전) vs
          같은 문서 Decision 근거 3 · design breakdown §3 Phase 5
        root_cause: >-
          Gate 를 phase 번호순으로 나열해, 'fixture 를 인라인 제거 앞에 둔다' 는 결정과 표의 읽기 순서가 충돌.
          집행자가 표만 보고 Phase 4→5 순으로 진행할 여지가 있었다.
        outcome: fixed
        addressed_in: >-
          round 1 — Gate 표를 실행 순서로 재배열(G3=Phase 5 도입, G4=Phase 4 종료) + 표 상단에 순서
          명시 + R2/R7 의 Gate 참조 갱신
---
# ADR-171 Review Log

## Round 1 — 2026-07-28 (reviewer: claude)

## ADR 리뷰 결과: ADR-171 catalog 레이아웃 값의 소비자 비대칭 해소

### 구조 검증

| 섹션 | 판정 | 품질 | 인용 |
| --- | --- | --- | --- |
| Context (hard constraint) | PASS | HIGH | `ADR:15-23` — 7개 전부 측정치 동반 |
| Alternatives (2개+) | PASS | HIGH | `ADR:41-79` — A/B/C/D 4개 |
| 4축 위험 평가 | PASS | HIGH | `ADR:45-49,55-59,65-69,75-79` — 누락 축 0 |
| Risk Threshold Check | PASS | HIGH | `ADR:83-90` — 표 + 루프 판정 |
| Decision 위험 수용 근거 | PASS | HIGH | `ADR:98-109` — 근거 4 + 기각 사유 3 |
| 구현 상세 분리 | PASS | HIGH | `ADR:111` 포인터, 본문에 Phase/파일표 없음 |
| Risks (Decision 뒤/Gates 앞) | PASS | HIGH | `ADR:113-126` — R1~R8 + "잔존 HIGH 위험 없음" |
| Gates | PASS | HIGH | `ADR:128-138` — G1~G5, 실행 순서 배열 |

동적 seed 5항목: #1 코드 경로 인용 PASS(8심볼) · #2 Generator 지원 선언 PASS(`ADR:25`) · #3 BC 수식화 PASS(`ADR:27-31`) · #4 Phase 분리 검토 PASS(`ADR:92`) · #5 추정↔실측 gap PASS(R3).

### 코드 검증

| 주장 | 판정 | 근거 |
| --- | --- | --- |
| `parentStyle[key] !== undefined` 면 catalog skip | VERIFIED | `implicitStyles.ts:294,327` |
| 경로 B 가 `structure.composition` 게이트 뒤 | VERIFIED | `implicitStyles.ts:321` (이연 주석 `:318`) |
| parity 노드 type 이 generic `box` | VERIFIED | `harness.ts:221` 원문 일치 |
| 생성 CSS 94개 중 62개 import | VERIFIED | `ls generated/*.css`=94 · unique `@import`=62 |
| 경로 A 24 · 경로 B 25 | VERIFIED | brace 매칭 재스캔 (A 우선 return 반영) |
| 총 121 · 미보유 72 | UNVERIFIED→fixed | 실제 총 123 · 미보유 74 (소문자 키 `body`/`frame`) |
| factory 인라인 7 파일 | VERIFIED | layout 키 보유 파일 정확히 7 |
| 인라인 약 260 선언 | PARTIAL | layout 축 141 + width 89 + height 15 — grep 방식 차로 키별 ±6 |
| ADR-170/142 링크 경로 | VERIFIED | `docs/adr/completed/` 양쪽 존재 |
| `packages/specs/src/runtime/containerStylesFallback.ts` | VERIFIED | 파일 존재 |

### 위험 평가

| 위험 | 증거 | 심각도 | 상태 |
| --- | --- | --- | --- |
| R1~R6 (ADR 기재분) | 본문 실측 인용 | MED/LOW | 기재됨 |
| Inspector dirty/reset baseline 이 factory 인라인과 결합 | `StylesPanel.tsx:96` · `useResetStyles.ts:292` | MEDIUM | **누락 → R7 로 편입** |
| 기존 47-케이스 resolver 계약 lock 이 Phase 3 로 RED | `resolveContainerStylesFallback.test.ts:102-110` | MEDIUM | **누락 → R8 로 편입** |
| publish 앱 CSS 경로 분기 | `apps/publish/src/styles/index.css:11` → `components/index.css:2` → 같은 `styles/index.css` | — | 위험 아님(동일 번들 확인) |
| layoutVersion 5-심볼 체인 | catalog 값은 빌드 정적 + `size`/`children` 이미 등재 | — | 위험 아님 |

### 종합 판정

- **구조**: PASS (7/7 섹션 HIGH 품질 + seed 5/5)
- **코드 정합**: 8/10 VERIFIED · 1 PARTIAL · 1 UNVERIFIED(수치, round 1 에서 정정)
- **누락 위험**: 2건 (MEDIUM 2 / HIGH 0) — round 1 에서 R7·R8 로 편입
- **권고**:
  - HIGH: 없음
  - MEDIUM: Inspector baseline(R7) · 기존 계약 테스트(R8) — 본 round 에서 반영 완료
  - LOW: Hard Constraint 4 수치 · Gate 표 순서 — 본 round 에서 반영 완료
  - **결론: 승인 가능** (Proposed → Accepted 승격 가능)
