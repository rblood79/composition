---
adr: 226
title: 페이지 헤더 DOM 층 줌 LOD — 제스처 중 프레임 집합 동결 + 헤더 폭 티어
reviews:
  - round: 1
    ts: '2026-09-19T05:12:43.572Z'
    reviewer: codex
    source: live
    issues:
      - id: h1
        severity: HIGH
        category: evidence-missing
        summary: G2 recorder가 wheel idle 150ms 전에 종료되어 settle spike를 측정하지 못함
        evidence: >-
          ADR-226 G2; useViewportControl.ts:304-310;
          perf-baseline.mjs:1586-1609,1773-1787
        root_cause: gesture driver 수명과 camera gate 수명을 같은 측정 창으로 가정
        outcome: fixed
        addressed_in: >-
          Hard constraints/G2와 breakdown F12/Phase 3 — gate-off+2 rAF, identity
          A/B 3회
      - id: m2
        severity: MEDIUM
        category: evidence-missing
        summary: gate-off 즉시 hidden 제거가 최신 React frames commit보다 앞설 수 있음
        evidence: 'usePageHeaderPlacement.ts:182-186; ADR-226 Decision 1'
        root_cause: store 구독과 React commit 순서를 암묵적으로 신뢰
        outcome: fixed
        addressed_in: Decision 1/R5/G1 — commit-before-reveal 및 원복 RED
      - id: m3
        severity: MEDIUM
        category: generator-extension-gap
        summary: pointer pan cleanup이 onInteractionEnd를 호출하지 않아 gate가 true로 남을 수 있음
        evidence: 'useViewportControl.ts:275-285; ADR-226 R2/G3'
        root_cause: effect cleanup을 종료 경로 인벤토리에서 누락
        outcome: fixed
        addressed_in: F12/Phase 1/R2/G3 — cleanup 종료 신호와 interrupt 4경로
      - id: m4
        severity: MEDIUM
        category: evidence-missing
        summary: '타이틀 12px 700 보존 주장이 현행 일반 600, 편집 input 700과 불일치'
        evidence: 'PageHeaderLayer.css:44-57,141-150'
        root_cause: ADR-221 목표값을 현재 구현값으로 재확인하지 않음
        outcome: fixed
        addressed_in: 'F6/Hard constraints/Decision 2/G4 — 일반 600, 편집 input 700'
      - id: l5
        severity: LOW
        category: adr-structure-violation
        summary: 편집 중 pan 유지와 viewport 밖 settle 종료 조건이 모호함
        evidence: breakdown Phase 1/G3
        root_cause: 가시 마진 안/밖을 구분하지 않음
        outcome: fixed
        addressed_in: 'G3 ⑤ — 가시 마진 안 유지, 밖 settle 종료'
      - id: l6
        severity: LOW
        category: evidence-missing
        summary: 외부 원칙의 적용 추론을 React Flow/tldraw 공식 계약처럼 인용
        evidence: ADR-226 Alternative A; research §0/§3/§4-2
        root_cause: 공식 구현 사실과 Composition 적용 추론을 혼합
        outcome: fixed
        addressed_in: Alternative A와 research의 출처·표현 정정
      - id: l7
        severity: LOW
        category: adr-structure-violation
        summary: README ADR-226 공지 집계가 현황 표와 불일치
        evidence: 'docs/adr/README.md:18,30-43'
        root_cause: 같은 날 상태 전이 뒤 집계를 재대조하지 않음
        outcome: fixed
        addressed_in: '열림 7, Proposed 5, Accepted 1, 부분 1, 합계 253'
    hate:
      assumption: 동결은 제스처 비용을 제거하면서 settle 한 프레임에 허용 가능한 비용만 옮긴다
      root: 기존 recorder는 settle 전에 종료되어 이 가정을 측정하지 않았다
      axis: 측정 leakage와 reveal 순서
      first_nail: cameraGestureActive=false부터 2 rAF까지 settle max를 분리
      verdict: 'G1/G2 설계 수리로 커버, 구현은 UNVERIFIED'
    prism:
      lenses:
        - lens: runtime ordering
          verdict: pass
          reason: 최신 frames commit→placeAll→hidden 제거 순서 명시
        - lens: measurement validity
          verdict: pass
          reason: identity A/B와 gesture/settle 분리
        - lens: interrupt completeness
          verdict: pass
          reason: pointercancel/blur/visibility/unmount-remount 포함
        - lens: source accuracy
          verdict: pass
          reason: 공식 사실과 적용 추론 분리
      convergence: 일치
      question: 없음
  - round: 2
    ts: '2026-09-19T05:30:18.722Z'
    reviewer: claude
    source: live
    issues:
      - id: l1
        severity: LOW
        category: other
        summary: >-
          settle 순서: 카메라 미러 commit (store zoom) 이 gate-off 보다 먼저 와서 티어 판정이 gate
          안 render 1회를 더 만든다
        evidence: >-
          useViewportControl.ts:288-301 (finishWheelInteraction: session.finish
          → commitMirror → onInteractionEnd) ·
          viewportInteractionSession.ts:157-163,222-232 · breakdown Phase 2
          (zoom 구독)
        root_cause: 티어 입력 zoom 을 frames 와 별개로 구독 — 두 store 갱신이 uSES 동기 render 2회로 갈라진다
        outcome: deferred
        addressed_in: >-
          구현 시 useSettledFrames 가 frames 와 zoom 을 같은 gate 로 동결 (settled snapshot
          하나) — Phase 2 착수 시 반영, 별도 round 불요
    hate:
      assumption: gate-off 한 커밋에 최신 frames 배치 + reveal 이 닫힌다
      root: 카메라 미러 commit 이 gate-off 보다 먼저 store 를 바꿔 커밋이 2회로 갈라질 수 있다
      axis: store 갱신 순서
      first_nail: >-
        finishWheelInteraction 에서 setCameraGestureActive(false) 직전 render 횟수를 세는
        단위 테스트
      verdict: 'issue:l1 (LOW deferred, Phase 2 흡수)'
    prism:
      lenses:
        - lens: round 1 fix verification
          verdict: pass
          reason: 7/7 코드 라인 재확인
        - lens: measurement validity
          verdict: pass
          reason: settle 창 · 대조군 · 고정 입력 · A/B 명시
        - lens: runtime ordering
          verdict: unclear
          reason: mirror commit 선행으로 render 2회 가능 — LOW
      convergence: 일치
      question: 없음
---

# ADR-226 Review Log

## Round 1 — 2026-09-19 (reviewer: codex)

판정: **수리 후 승인 가능, Proposed 유지.** B+C 방향과 A 기각/D 유보를 유지한다. HIGH 1 · MEDIUM 3 · LOW 3을 ADR 본문, breakdown, research, README에 반영했다. 제품 구현과 상태 승격은 하지 않았다.

G2는 gesture/settle 창 분리와 gate-off+2 rAF, identity A/B 3회로 바꿨다. 최신 frames commit→배치→reveal 순서, pointer cleanup interrupt, 일반 타이틀 600/편집 input 700, 편집 페이지 가시 마진 계약을 gate와 원복 RED에 연결했다. 외부 사례와 README 집계도 정정했다.

구현 G0~G4와 live/performance 결과는 아직 UNVERIFIED다.

## Round 2 — 2026-09-19 (reviewer: claude)

## round 2 — 수리 검증 (2026-09-19, HEAD `30e3bfbd8`)

### round 1 7건 대조 (코드 실측)

| id | 판정 | 근거 |
| --- | --- | --- |
| h1 G2 settle 창 | **VERIFIED FIXED** | wheel gate-off = 마지막 입력 +150 ms (`useViewportControl.ts:304-310`) · `wheelBurst` 는 마지막 wheel 직후 반환 (`perf-baseline.mjs:1586-1609`) · recorder 즉시 정지 (`:1773-1787`) — ADR Hard constraints/G2 가 gate-off + 2 rAF · identity A/B 3회 · refresh/DPR/visibility 기록으로 재정의, breakdown F12 · Phase 3 · 파일표 · 원복표 반영 |
| m2 commit-before-reveal | **VERIFIED FIXED** | `usePageHeaderPlacement.ts:181-186` `applyGate(false)` 가 구독 콜백에서 즉시 `removeAttribute("data-hidden")` + `placeAll` — Decision 1 · R5 · G1 · breakdown Phase 1 이 reveal 을 최신 커밋 layoutEffect 단독 소유로 옮김 |
| m3 pointer cleanup | **VERIFIED FIXED** | pointer pan effect cleanup 은 `finish("interrupted")` 만 (`:275-285`), wheel cleanup 은 `finishWheelInteraction("interrupted")` → `onInteractionEnd` (`:401-404`), blur/visibility 는 `interruptViewportInteraction` (`:420-440`) — pointer 경로만 누락이 맞고 F12 · Phase 1 · R2 · G3 ② 가 그 하나를 겨눈다 |
| m4 600/700 | **VERIFIED FIXED** | `.page-header` `font-weight: 600` (`PageHeaderLayer.css:44-57`) · 편집 input 700 (`:141-150`) — 현행 보존으로 F6 · Hard constraints · Decision 2 · G4 정정 |
| l5 편집 중 pan | VERIFIED FIXED | G3 ⑤ · breakdown Phase 1 — 200 px 가시 마진 안/밖 구분 |
| l6 외부 출처 | VERIFIED FIXED | 대안 A 근거 · 연구 문서 §3/§4-2/§5 — 공식 사실 (tldraw `display:none` 컬링 · React Flow 는 저줌 미마운트 미규정) 과 적용 추론 분리 |
| l7 README | VERIFIED FIXED | 공지 · 현황 표 열림 7 (Proposed 5 · Accepted 1 · 부분 1) · 합계 253 = `docs/adr/[0-9]*.md` 실측 7 |

### 신규 (LOW 1)

- **settle 순서 (deferred → Phase 2 구현 시 흡수)**: `finishWheelInteraction` 은 `viewportSession.finish()` 가 `commitMirror` 로 store zoom/panOffset 을 먼저 쓰고 (`viewportInteractionSession.ts:157-163 · 222-232`) 그 다음 `onInteractionEnd` → `setCameraGestureActive(false)` 를 호출한다 (`useViewportControl.ts:288-301`). breakdown Phase 2 대로 층이 `zoom` 을 별도 구독하면 gate 가 아직 true 인 render 가 한 번 더 생겨 (uSES 동기 render) `data-lod` attribute 가 hidden 상태에서 먼저 쓰인다 — 시각 영향 0 · G1 (childList) 무영향이나 "settle 커밋 1회" 서술과 어긋난다. 수리 방향: `useSettledFrames` 가 frames 와 zoom 을 한 snapshot 으로 같은 gate 에 동결. production 재현 시나리오 없음 → LOW deferred, 라운드 재개 사유 아님.

### 전제 · 대안 · 측정 규칙 대조

- 전제 (base 221 → 응용 226, 신호 읽기만) 는 코드와 일치 — `cameraGestureActive` 소비자 1 (`viewportSync.ts:17,87-93`).
- 대안 A 기각 근거 (줌 1 churn 4/4) 는 evidence `probe-p50-z1.json` 실측. D 유보 조건 명시. B 의 잔존 위험은 G2 로 1:1.
- measurement-validity: G2 가 대조군 (22p A/B) · 고정 입력 · 같은 세션 · 환경 기록 · A/B 원복을 갖췄다. 원복은 편집 역적용으로 (git checkout 금지 — 메모리 `feedback-red-check-revert-via-git-checkout-destroys-parallel-wip`).
- 구조 체크리스트 8항 통과. 병행 커밋 `b82d0a814` 는 docs + probe 만 (src 0) — F 표 라인 drift 없음.

### 판정

**승인 가능 — Accepted 승격 가능 (사용자 결정).** HIGH 0 · MEDIUM 0 · LOW 1 (deferred). review-loop-closure §1 에 따라 phase 판독 1 + 수리 검증 1 로 라운드 종결.

검증 범위: ADR 본문 · breakdown · 인용 코드 6 파일 라인 재확인 · evidence JSON 3. 한계: 구현 코드 0 이라 G1~G4 는 UNVERIFIED (설계 검증만).
