---
adr: 159
title: 'Collection 필드 템플릿 바인딩 — {field} 보간 + dataTable 단일 소스'
reviews:
  - round: 1
    ts: '2026-07-21T00:37:42.837Z'
    reviewer: claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: other
        summary: >-
          템플릿 소스 precedence 미명세 — item props.children({name}) vs slot-label
          text({num}) 공존 + flat item 커버리지 불명
        evidence: >-
          breakdown:40,77 (slot text 만 언급) + 라이브 seed
          component-listbox-item-default (props.children={name} + __label slot
          text={num} 동시 보유)
        root_cause: >-
          breakdown §2-3/Phase 2 가 slot 자식 text 캡처만 명시하고 두 template 소스의
          precedence + slot 없는 flat item 의 item-level template 보간 여부를 확정하지 않음 →
          소스 분열 버그 재발 소지
        outcome: fixed
        addressed_in: >-
          breakdown §2-3-1 precedence 계약(slot text > item children, flat item 은
          item children fallback) + resolveRowTemplateSource 단일 헬퍼 + 양쪽 커버리지 P2
          vitest lock-in
      - id: l2
        severity: LOW
        category: other
        summary: G3 BC 계약이 slot 단위 독립 fallback(label 휴리스틱 + description 보간 혼합) 미명시
        evidence: 'breakdown:71 (토큰 없음 → 휴리스틱 동일 만 명시)'
        root_cause: role 별 template 유무가 독립 판정되는지 vitest 계약 부재
        outcome: fixed
        addressed_in: breakdown Phase 1 slot 단위 독립 fallback vitest 추가 + G3 문구에 slot 단위 독립 포함
      - id: l3
        severity: LOW
        category: other
        summary: R5 가 scene rebuild 별 재compile 미언급 (대형 scroll window A2 가상화)
        evidence: 'ADR:104 (행별 재compile 만 경계)'
        root_cause: >-
          appendListBoxRowProjection 이 rebuild 마다 호출 → slot 템플릿 compile 이
          rebuild 마다 반복될 수 있음, 캐시 대응 부재
        outcome: fixed
        addressed_in: R5 대응에 template string 키 캐시(WeakMap/Map by text) 추가
  - round: 2
    ts: '2026-07-23T17:09:40.677Z'
    reviewer: claude
    source: live
    issues:
      - id: l1
        severity: LOW
        category: evidence-missing
        summary: >-
          canvasSceneNode.ts:987-989 인용 stale — ADR-154 후속(07-21)/161(07-23)
          편집으로 라인 이동, 실질 근거는 :1186(ListBox)/:1476(GridList) rowProps children:
          row.label
        evidence: >-
          canvasSceneNode.ts:985-992 (현재 = ADR-154 responsive 주석) vs :1186/:1476
          (실제 덮어쓰기)
        root_cause: round 1(07-21) 이후 3일간 동일 파일 다수 커밋 — 라인 인용 미갱신
        outcome: fixed
        addressed_in: 'round 2 리뷰 중 본문 Context 인용 갱신 (:1186/:1476 + 이동 사유 병기)'
      - id: l2
        severity: LOW
        category: evidence-missing
        summary: 'breakdown §2-3 slotRoles.ts:74-81 인용 stale — SlotChildConfig 현재 :79-85'
        evidence: 'slotRoles.ts:79-85 (SlotChildConfig), :88-92 (SlotComposition)'
        root_cause: 동일 — slotRoles.ts 의 size/fills fold 추가(07-21)로 라인 이동
        outcome: fixed
        addressed_in: 'round 2 리뷰 중 breakdown §2-3 + 본문 :79-92 갱신'
      - id: l3
        severity: LOW
        category: other
        summary: >-
          [boundary-sync] ADR-162 신규 base 소비자 미반영 — 162 가 P1 resolver(임의 자식
          string prop 보간)/P4 오소링을 소비, P1 API 의 string 일반성 유지 의무 미기록
        evidence: >-
          docs/adr/162-gridlist-template-subtree-projection.md:16 (2026-07-24
          사용자 confirm '159 base 의존 재획정')
        root_cause: 162 작성(07-24)이 159 round 1(07-21) 이후 — 역방향 포인터 부재
        outcome: fixed
        addressed_in: >-
          round 2 리뷰 중 본문 Context ADR-162 소비 관계 문단 + breakdown §1-6 추가 (계약 무변,
          slot 텍스트 특정 가정 금지 명시)
---
# ADR-159 Review Log

## Round 1 — 2026-07-21 (reviewer: claude)

## ADR-159 리뷰 (round 1)

구조 PASS 7/7 HIGH. 코드 10/10 VERIFIED (getItemLabel:263-282 / canvasSceneNode:987-989 / slotRoles:75-81,95,143 / ListBox:442 / CollectionRenderers:245-268 / PropertyDataBinding:108-112 / FieldType:45-53 / 핵심 feasibility: CollectionProjectionRow.item:147-160 존재 → interpolate(compiled,row.item) 가능). 누락 위험 3건(MED 1 / LOW 2) 전건 round 내 fixed — 템플릿 소스 precedence(breakdown §2-3-1), slot 단위 독립 fallback(Phase 1 vitest), rebuild compile 캐시(R5). Decision 무변. 결론: 승인 가능.

## Round 2 — 2026-07-23 (reviewer: claude)

## ADR-159 리뷰 (round 2 — 사용자 재제기, stale 재검증)

round 1(2026-07-21 승인·전건 fixed) 이후 코드 변동(ADR-160 07-22 / ADR-161 07-23 / ADR-154 개정 07-23) 재검증.

### 구조

7/7 PASS (HIGH) + round 1 수정분 3건 반영 확인 (breakdown §2-3-1 precedence / Phase 1 slot 독립 vitest / R5 rebuild compile 캐시).

### 코드 정합

10/10 VERIFIED — getItemLabel:263-282 / row.label 덮어쓰기 (현재 :1186·:1476, 구 :987-989 stale→정정) / slotRoles text 부재 :79-92 / ListBox.tsx:440-442 / CollectionRenderers:245-268 / PropertyDataBinding:108-113 / FieldType:45 / useCollectionData api :331·411·436·522 / row.item :156 / fieldTemplate.ts 미구현(Proposed 정합).

ADR-161 ref-composite 의 P2 영향: 없음 (slot text 캡처 메커니즘 중립).

### 위험

신규 LOW 3건 — 전건 round 내 fixed: stale 인용 2건 갱신 + ADR-162 소비 관계(본문 Context + breakdown §1-6, P1 string 일반성 유지 의무) 추가.

### 종합

- 구조 PASS / 코드 10/10 / 누락 위험 LOW 3 → 전건 fixed
- 결론: **승인 가능** (round 1 승인 유지)
