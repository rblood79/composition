---
adr: 163
title: 빌더 패널 표준 구조화 — 레퍼런스 기반 전 패널 DOM/클래스/CSS 정본 통일
reviews:
  - round: 1
    ts: '2026-07-24T08:03:44.308Z'
    reviewer: claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: other
        summary: >-
          Phase 2/3 시각 변화 판정 기준 미정의 — G4 가 라이브 확인만 요구, 구조 정렬의 의도 변화 vs 회귀 구분 불가
          (원문 분류: gate-criterion-gap)
        evidence: 'docs/adr/163-builder-panel-structure-standardization.md:108 (수정 전)'
        root_cause: Hard constraint 1 의 diff 0 이 Phase 1 한정임이 G4 에 미반영
        outcome: fixed
      - id: m2
        severity: MEDIUM
        category: migration-cost-unquantified
        summary: >-
          root 클래스 제거 시 co-located CSS 선택자 파손 표면 미열거 — .datatable-editor-panel
          4건 / .themes-panel 2건 / .monitor-panel 2건 / .ai-panel 1건 실측
        evidence: 'grep .{root-class} apps/builder/src --include=*.css 실측'
        root_cause: 'design §3-1 이 ''강등 또는 제거'' 양자택일만 제시, CSS 참조 동시 수정 조건 누락'
        outcome: fixed
      - id: l3
        severity: LOW
        category: evidence-missing
        summary: >-
          events CSS 규모 수치 불일치 — 기재 4파일 2,225줄, 실측 5파일 2,845줄 (block-editor.css
          620줄 누락)
        evidence: wc -l apps/builder/src/builder/panels/events/*.css = 2845
        root_cause: inventory 집계 시 파일 1개 탈락
        outcome: fixed
      - id: l4
        severity: LOW
        category: evidence-missing
        summary: 'properties-aria 참조 수 과대 기재 — 기재 30+, 실측 26곳 (tsx 25 test 포함, css 1)'
        evidence: grep -rn properties-aria --include=*.tsx | wc -l = 25
        root_cause: head 절삭된 grep 출력 기반 추정치를 실측 없이 기재
        outcome: fixed
---
# ADR-163 Review Log

## Round 1 — 2026-07-24 (reviewer: claude)

## ADR 리뷰 결과: ADR-163 (round 1)

### 구조: PASS 7/7 HIGH
Context ADR:19-24 (diff 0 게이트/test/BC 0%) · Alternatives ADR:33-61 (3개×4축) · Threshold ADR:63-71 (B HIGH+ 0) · Decision ADR:73-86 · Risks ADR:90-99 (R1-R4, 잔존 HIGH 없음) · Gates ADR:101-108 (G1-G4). 동적 seed 5항목 전부 통과 (Generator 비해당 ADR:17, BC 수식화 ADR:24, Phase 분리 비해당 ADR:71).

### 코드 정합: 10/10 VERIFIED
dead 블록 39/360/480/528 (awk 재실측) · dead 무매칭 (라이브 probe display:block) · panel-tabs 4파일 · iconButton 5파일 · empty-state 2파일 · inspector-layout.css:249 · PanelContainer.tsx:156 · 패널 13종 (panelConfigs) · historyActions.static.test.ts 존재 · import 순서 index.css:1-4.

### 이슈: MED 2 + LOW 2 — 전건 round 내 수정 (fixed)
M1 G4 기준 보강 (ADR:108 — 의도 변화 커밋 항목화 + 그 외 0) / M2 root 클래스 .panel 병기 기본 (design §3-1/§4) / L1 events 5파일 2,845줄 정정 / L2 properties-aria 26곳 정정.

### 종합: 승인 가능 — pending 0건
렌더링 파리티/성능/레이아웃 체인/상태 동기화/SSOT 축 비해당 확인 (builder 시스템 UI).
