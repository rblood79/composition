---
adr: 201
title: 대용량 파일 업로드 — 독립 전송 엔진 + FileUpload 컴포넌트 + Spring 서버 계약
reviews:
  - round: 1
    ts: "2026-09-16T18:48:45.482Z"
    reviewer: claude
    source: live
    issues:
      - id: h1
        severity: HIGH
        category: other
        summary: >-
          G2·G5(JSP) 가 이 환경 (JDK/Maven/tusd/docker 0, CI Java job 0) 에서 실행 불가하고
          사용자 confirm 대체 경로가 없어 Implemented 도달 불가
        evidence: >-
          docs/adr/completed/201-large-file-upload-engine-component-server-contract.md:145;
          docs/adr/completed/201-large-file-upload-engine-component-server-contract.md:148;
          docs/adr/design/201-large-file-upload-engine-component-server-contract-breakdown.md:107;
          .github/workflows/deploy.yml:30
        root_cause: HC5 의 유일한 외부 oracle (tusd) 과 참조 서버 툴체인을 실행 계획·선행 조건으로 두지 않았다.
        outcome: fixed
      - id: h2
        severity: HIGH
        category: other
        summary: >-
          번들 계약이 ADR-202 정본 (Preview Δ ≤ 0 · 절대 592,000 · 만료) 과 충돌하고 G3 'builder
          초기 chunk Δ 0' 은 정적 등록 구조상 달성 불가, HC1 인용 2건 stale
        evidence: >-
          docs/adr/completed/201-large-file-upload-engine-component-server-contract.md:25;
          docs/adr/completed/201-large-file-upload-engine-component-server-contract.md:146;
          apps/builder/scripts/adr202-bundle-gate.mjs:56-59;
          .claude/skills/component-design/SKILL.md:19;
          packages/shared/src/catalog/componentCatalog.ts:18
        root_cause: >-
          HC1 이 저장소의 per-ADR initial 상한 재승인 절차 대신 폐기된 500KB 예시를 인용했고 gate 대상 앱
          (Preview) 을 publish 로 잘못 잡았다.
        outcome: fixed
      - id: h3
        severity: HIGH
        category: ssot-violation
        summary: >-
          D2 참조 원천 선언에 prop 별 판정 기록이 없고 autoUpload·showPreview·protocol 3건은 선언한
          원천 (RAC/tus-js-client/Uppy) 어디에도 없다
        evidence: >-
          docs/adr/completed/201-large-file-upload-engine-component-server-contract.md:11;
          docs/adr/design/201-large-file-upload-engine-component-server-contract-breakdown.md:94;
          docs/adr/completed/194-chart-component-headless-geometry.md:125;
          packages/shared/src/catalog/bindings/FileTrigger.binding.ts:48-71
        root_cause: ADR-194 가 세운 '외부 원천 선언 + prop 별 D2 판정 기록 + 이탈 근거 주석' 선례를 원천 선언까지만 따랐다.
        outcome: fixed
      - id: m1
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          R2 'G1 테스트가 차단' 은 사실이 아니고 (events 는 명명 규약만 검사) 'ADR-158
          internal-source 예외 절차' 는 존재하지 않는다 — 커스텀 이벤트 등재 시 무음 no-op 규칙
        evidence: >-
          packages/shared/src/interactions/__tests__/capabilityRegistry.test.ts:31-35;
          packages/shared/src/interactions/__tests__/capabilityRegistry.test.ts:61-92;
          docs/adr/completed/201-large-file-upload-engine-component-server-contract.md:131;
          docs/adr/design/201-large-file-upload-engine-component-server-contract-breakdown.md:166
        root_cause: >-
          G1 테스트 범위 (capabilities.racRef) 를 events 축까지 확장해 읽었고 후속 절차를 실측 없이
          인용했다.
        outcome: fixed
      - id: m2
        severity: MEDIUM
        category: other
        summary: >-
          Phase 1·2·3 병렬 착수에서 에러 코드 표 (클라이언트·계약 문서·mock 서버) 3자 대조 gate 가 없고
          /react entry API 시그니처가 미정의라 Phase 3 이 추정에 의존
        evidence: >-
          docs/adr/design/201-large-file-upload-engine-component-server-contract-breakdown.md:132;
          docs/adr/design/201-large-file-upload-engine-component-server-contract-breakdown.md:55;
          docs/adr/completed/201-large-file-upload-engine-component-server-contract.md:130
        root_cause: >-
          순차 전제로 쓰인 breakdown 을 병렬로 바꾸면서 산출물 간 공유 계약 (에러 표 · hook API) 의 단일 소스를
          정하지 않았다.
        outcome: fixed
      - id: m3
        severity: MEDIUM
        category: other
        summary: >-
          R3/G4 '문서/ApiEndpoint 정적 비밀 0' 게이트가 ADR-212 vault ({{secret.*}}) ·
          ADR-213 redactor 와의 관계, 기존 평문 endpoint 처리, preview/publish 런타임의
          placeholder 처리를 정의하지 않는다
        evidence: >-
          docs/adr/completed/201-large-file-upload-engine-component-server-contract.md:132;
          apps/builder/src/services/ai/security/redactEndpointAuth.ts:1-22;
          docs/adr/completed/212-data-panel-editor-redesign.md:29;
          packages/shared/src/types/collection.types.ts:104
        root_cause: endpoint 비밀 정책이 이미 212/213 으로 확정됐는데 201 이 이를 소비하지 않고 별도 게이트를 신설했다.
        outcome: fixed
      - id: l1
        severity: LOW
        category: evidence-missing
        summary: >-
          breakdown §2 라인 인용 drift 12건 (FormRenderers 1047/1106 · paletteItems
          244-245 · catalog 205/995 · InteractionAction 4종 · cloud SDK 행 소거 등) —
          결론 불변
        evidence: >-
          packages/shared/src/renderers/FormRenderers.tsx:1047;
          packages/shared/src/renderers/FormRenderers.tsx:1106;
          apps/builder/src/builder/panels/components/paletteItems.ts:244-245;
          packages/shared/src/interactions/interactionRule.types.ts:64
        root_cause: >-
          b06ddd643 기준 인용이 HEAD d013b5658 까지의 변경으로 밀렸다 — Phase 0 inventory
          freeze 대상.
        outcome: fixed
      - id: l2
        severity: LOW
        category: other
        summary: >-
          cloud.ts 어댑터 (6MB clamp · x-upsert) 는 대상 서비스 SDK·이름이 09-12 저장소에서 소거돼
          검증 oracle 0 — §7 비스코프 이동 권고
        evidence: >-
          docs/adr/design/201-large-file-upload-engine-component-server-contract-breakdown.md:53
        root_cause: ADR 초안 (09-02) 이후 인증·스토리지 방침 변경 (09-12) 이 breakdown 에 반영되지 않았다.
        outcome: fixed
      - id: l3
        severity: LOW
        category: alternative-strawman
        summary: >-
          ADR-194 '의존 0' 선례 인용은 ADR-209 부분 대체 (Recharts) 로 정정 필요; 힙 측정
          performance.memory 는 ADR-218 힙 스냅샷 Δ 방식으로
        evidence: >-
          docs/adr/completed/201-large-file-upload-engine-component-server-contract.md:20;
          docs/adr/completed/194-chart-component-headless-geometry.md:3;
          docs/adr/design/201-large-file-upload-engine-component-server-contract-breakdown.md:138
        root_cause: 선례·측정법 인용이 후속 ADR (209 · 218) 종결 이전 상태다.
        outcome: fixed
      - id: l4
        severity: LOW
        category: adr-structure-violation
        summary: >-
          작업 트리 Status In Progress 전환에 docs/adr/README.md:52 (Proposed) 동시 갱신 누락
          — 실행자 첫 커밋에서
        evidence: >-
          docs/adr/README.md:52;
          docs/adr/completed/201-large-file-upload-engine-component-server-contract.md:5
        root_cause: Status 전이와 README 갱신이 같은 commit 에 묶이지 않았다 (아직 미커밋).
        outcome: fixed
    hate:
      assumption: TUS 1.0 표준 채택 + tusd 대조군이 참조 서버가 방언이 되는 것을 막는다 (HC5)
      root: >-
        그 보증 장치 (tusd) 와 참조 서버 툴체인 (JDK/Maven/Tomcat) 이 실행 환경·CI 어디에도 없어 외부
        oracle 이 실행 계획에 존재하지 않는다
      axis: 측정 leakage — measurement-validity Q5 oracle 독립성
      first_nail: >-
        command -v tusd mvn java (2026-09-17 이 머신) → tusd/mvn 없음, java 는 'Unable
        to locate a Java Runtime' stub
      verdict: "issue:h1"
    prism:
      lenses:
        - lens: oracle 독립성 (tusd · 참조 서버)
          verdict: fail
          reason: 도구 0 + CI Java job 0 + 대체 경로 없음 (h1)
        - lens: 번들 비용 계약
          verdict: fail
          reason: "202 판정기 previewDelta ≤ 0 · 절대 592,000 과 충돌, Δ 0 달성 불가 (h2)"
        - lens: SSOT 경계 D2
          verdict: fail
          reason: prop 3건 원천 없음 · 판정 기록 부재 (h3)
        - lens: 병렬 실행 정합
          verdict: unclear
          reason: "프로토콜 축은 표준+G2 로 안전, 에러 표·hook API 축은 gate 없음 (m2)"
        - lens: 보안 계약 정합 (212/213)
          verdict: unclear
          reason: placeholder·평문 처리 미정의 (m3)
      convergence: "다른 이유로 일치 — 결정 A 는 유지, gate 가 현재 정본·환경에서 통과 가능하게 재작성돼야 한다"
      question: >-
        G2 서버 e2e 를 사용자 머신 실행 + confirm 증거 (G2b) 로 대체할지, JDK/Maven/tusd 를 이 머신에
        먼저 설치할지
---

# ADR-201 Review Log

## Round 1 — 2026-09-16 (reviewer: claude)

## Round 1 — 2026-09-17 (reviewer: claude)

판정: **수정 요청 — HIGH 3 / MEDIUM 3 / LOW 4**. 대안 A (TS sans-I/O 코어 + TUS + 독립 package + Spring 참조 서버) 의 선택 자체와 B~E 기각 근거는 현재 코드와 정합하고, 전제 4건 lock-in 도 유지된다. 결함은 결정이 아니라 **gate 가 현재 저장소 정본·실행 환경에서 통과 가능한가**에 몰려 있다 — 번들 계약 (h2) · 서버 oracle 실행 불가 (h1) · D2 근거 (h3). 셋 다 문서 수정으로 닫히며 코드 재설계는 필요 없다. Status 는 실행자 소관 (In Progress 유지).

검토 기준: main HEAD `d013b5658` (구현 worktree 3개의 base `5b2346c12` 와 인용 대상 `packages/shared` · `apps/publish` · `packages/specs` diff 0 — 판정은 양쪽에 같이 적용). ADR 본문은 작업 트리에서 Status 만 In Progress 로 바뀐 상태를 읽었다. 코드·ADR 본문·breakdown·README 수정 없음. 리뷰 프롬프트 계약: production 재현 시나리오가 없는 커버리지 지적은 LOW deferred 로 두고 라운드 재개 사유로 삼지 않는다 — phase·후속 항목의 닫힘 판정은 실행자가 한다.

### h1 — HIGH: G2 · G5(JSP) 가 이 환경에서 실행 불가능하고 ADR 에 대체 경로가 없다

근거: 본문 G2 (`:145` — Spring 참조 서버 e2e · tusd 대조군 · `mvn -q test` CI job green) · G5 (`:148` — JSP 예제 IIFE) · breakdown §3-5 (`:107` — Maven · JDK 8 · Tomcat 9 · 별도 CI job). 실측 (2026-09-17, 이 머신): `java` 는 macOS stub ("Unable to locate a Java Runtime") · `mvn` · `go` · `tusd` · `docker` 전부 없음. CI 는 `.github/workflows/deploy.yml` 하나 (Visual Parity Smoke + deploy) — Java job 0, 그리고 ADR-198 잔여 기록대로 Linux smoke 0/N · deploy 09-04 이후 실패 상태다.

문제: HC5 의 핵심 보증 ("계약이 참조 서버 방언이 되는 것을 tusd 가 차단") 과 R1·R3·R6 의 대응 gate 가 전부 이 도구들 위에 있다. measurement-validity Q5 (oracle 독립성) 로 보면 tusd 가 유일한 외부 oracle 인데 실행 계획에 없다. G5 는 preview 축만 "Chrome MCP 또는 사용자 confirm" 을 두었고 G2 에는 사용자 confirm 대체가 없다. 이대로면 Phase 2 산출물 (Java) 은 검증 불가 → Implemented 도달 불가.

수정 방향 (문서만):

1. breakdown §3-5 에 **선행 조건 표** — Temurin JDK 8 (또는 8 target 으로 컴파일하는 17) + Maven 3 + Tomcat 9 zip + **tusd release 바이너리** (Go 불필요 — GitHub release 단일 실행 파일). docker 는 요구하지 않는다.
2. G2 를 **G2a** (mock 서버 + tusd 바이너리 — 로컬 Node 로 실행 가능) / **G2b** (Spring 참조 서버 e2e — 사용자 머신 실행 + 로그 · confirm 을 `docs/adr/evidence/201-g2b-*.md` 로 증거화) 로 분리. G2b 없이 Implemented 불가를 본문에 명시.
3. "`mvn -q test` CI job green" 은 workflow 파일 신설이 산출물이고 러너 상태는 ADR-198 잔여라 "로컬 `mvn -q test` PASS 로그 + workflow 파일 존재" 로 통과 조건을 재정의 (green 을 요구하면 198 과 결합된다).
4. Phase 순서와 무관하게 h1 의 도구 확보는 **Phase 2 착수 전** 사용자 결정 사항 — 아래 §해결 질문.

### h2 — HIGH: 번들 계약이 저장소 정본 (ADR-202 절대 상한 + `adr202-bundle-gate.mjs`) 과 충돌하고, G3 "builder 초기 chunk Δ 0" 은 등록 8지점 구조상 달성 불가

근거: 본문 HC1 (`:25`) 은 "`CLAUDE.md` 초기 번들 <500KB · `component-design/SKILL.md` 외부 라이브러리 금지" 를 인용하고 publish 초기 Δ ≤ +2KB 만 둔다. 그러나 (a) `.claude/skills/component-design/SKILL.md:19` 는 정확히 그 인용을 금지한다 — "오래된 500KB 예시를 최신 승인 상한이나 만료 기록 대신 사용하지 않습니다" (외부 라이브러리 "금지" 문구도 없음 — "bundle 계약과 승인된 예외를 확인" 이다). (b) 현재 정본은 ADR-202 재승인 **Builder ≤ 1,319,829 / Preview ≤ 592,000 B gzip, 만료 2026-10-16** (`docs/adr/README.md:182`, `apps/builder/scripts/adr202-bundle-gate.mjs:56-57`) 이고 판정기는 `previewDelta: delta.preview <= 0` (`:59`) — Preview 는 **1 byte 도 못 는다**. (c) ADR-201 이 말하는 "publish" 는 gate 밖 (link-only 방침) 이고, gate 가 재는 것은 builder 의 **Preview** arm 이다. `renderFileUpload` 는 shared `rendererMap` (`packages/shared/src/renderers/index.ts:39-40` 과 같은 표) 에 실리므로 Preview initial 에 반드시 들어간다. (d) catalog entry · binding · rule · factory · palette 는 정적 import (`componentCatalog.ts:18 getPrimitiveBinding` → `bindings/index`, `paletteItems.ts:76 from "@composition/shared"`) 라 Builder initial 도 0 이 아니라 +수 KB 다. (e) 메모리 `project-adr202-compiler-first-promotion-2026-09`: HEAD Builder 는 이미 상한 +2,116 B 초과 (미해결) — 201 의 baseline 자체가 상한 밖이라 Q3 대조군 정의가 없으면 Δ 만 보고 절대 상한을 놓친다.

production 결과: ADR-215~219 · 202 · 220 은 전부 "initial 상한 재승인" 절 + 전용 판정기 (`adr2NN-bundle-gate.mjs`) 를 뒀다. 201 만 이 절차 밖이면 Phase 3 커밋이 정본 gate 를 조용히 건너뛰거나 (판정기 미실행) 다음 ADR 의 before arm 을 오염시킨다.

수정 방향: HC1 을 202 정본 형식으로 재작성 — ① Builder / Preview initial **절대 상한 재승인 수치 + 만료일** (Δ 허용치: Builder ≤ +N KiB 등록 8지점 실측, Preview ≤ +M KiB 렌더러 shell — 수치는 Phase 3 전 detached checkout baseline 로 확정) ② `adr201-bundle-gate.mjs` (202 판정기 동형 + `initialFiles.has(uploadEngineChunk) === false` 로 엔진 lazy 검사) ③ "publish Δ" 는 gate 밖 참고치로 강등 ④ G3 문구 "builder 초기 chunk Δ 0" → "엔진 chunk 가 initial closure 밖 + 등록 Δ ≤ N". `CLAUDE.md` <500KB 와 SKILL.md 인용은 제거.

### h3 — HIGH: D2 참조 원천 선언이 prop 별 근거 없이 3건을 임의 도입한다 (`autoUpload` · `showPreview` · `protocol`)

근거: 본문 Context (`:11`) 는 D2 = "RAC prop 명 + tus-js-client·Uppy 옵션 명 참조 (`endpoint`/`chunkSize`/`retryDelays`/`parallelUploads`)" 로 선언한다. breakdown §3-4 binding 행 (`:94`) 의 accepts 12 개를 대조하면 — RAC `FileTriggerProps` 실존 (`acceptedFileTypes` · `allowsMultiple` · `acceptDirectory` · `isDisabled`, 기존 `FileTrigger.binding.ts:48-71` 과 동일) 4 · tus-js-client 옵션 실존 4 · Uppy `restrictions.maxFileSize` 1 · **출처 없음 3**: `autoUpload` (Uppy 는 `autoProceed`, FilePond 는 `instantUpload`) · `showPreview` (어느 참조 라이브러리에도 이 이름 없음) · `protocol` (어댑터 선택자 — 업계 동등 옵션 없음).

판정: RSP·S2 에 FileUpload 가 없을 때 외부 클라이언트를 참조 원천으로 두는 것 자체는 ADR-194 선례 (`completed/194-*.md:13` — RSP 에 chart 가 없어 RSC 를 원천으로 선언, `:125` **D2 판정 기록** + 평탄화 근거를 binding 주석에 기록, R5) 로 허용된다. 그러나 `ssot-hierarchy.md` §3 "RSP 미규정 prop 임의 도입 → 코드 리뷰 거부" 는 **prop 단위**로 적용되며, 201 은 194 가 남긴 판정 기록 (prop → 원천 매핑 + 이탈 근거) 이 없고 3건은 원천 자체가 없다. prop 명은 문서에 영속되므로 뒤에 정렬하면 재직렬화 마이그레이션이 생긴다 — 지금이 가장 싸다.

수정 방향: Context 또는 breakdown §3-4 에 **D2 판정 기록 표** (prop → RAC `FileTriggerProps` / RAC `DropZoneProps` / tus-js-client / Uppy / custom + 근거). 원천이 있는 것은 이름을 맞춘다 (`autoUpload` → `autoProceed` 또는 custom 근거 1줄). `showPreview` · `protocol` 은 custom 으로 선언하되 194 R5 동형으로 binding 주석에 근거를 남기고, `protocol` 은 `endpoint` 로부터 파생 (어댑터 자동 선택) 가능하면 D2 표면에서 제거를 우선 검토. 이 표가 G3 통과 조건에 들어가야 한다 (등록 8지점 검사는 prop 출처를 보지 않는다).

### m1 — MEDIUM: R2 "G1 테스트가 차단" 은 사실이 아니고 "ADR-158 internal-source 예외 절차" 는 존재하지 않는다

근거: `packages/shared/src/interactions/__tests__/capabilityRegistry.test.ts:31-35` G1 은 **capabilities 의 `racRef`** 만 검사한다. events 는 `:61-68` `^on[A-Z]` 정규식 + `:70-92` 은퇴 DOM 별칭 13종 제외뿐 — `onUploadComplete` 는 그대로 통과한다. "internal-source 예외 절차" 는 ADR-158 본문·breakdown 어디에도 없고 (`rg internal-source docs/adr` hit 은 201 자신 2곳뿐), 201 §7 이 존재하지 않는 절차에 후속을 걸어 두었다.

production 시나리오: 커스텀 이벤트를 events 에 넣으면 Events 패널에 노출 → 규칙 저장 → dispatcher 가 받을 RAC callback 이 없어 **무음 no-op 규칙**. 그래서 LOW deferred 가 아니라 MEDIUM 이다.

RAC 실존 확인 (R2 대응 자체는 맞다): `react-aria-components@1.21.0` `dist/types/src/FileTrigger.d.ts:19` `onSelect?: (files: FileList | null) => void` · `DropZone.d.ts:37` `DropZoneProps extends Omit<DropOptions, …>` → `@react-types/shared` `dnd.d.ts:259` `onDrop?`. catalog DropZone 은 `kind:"internal"` 래퍼 (`DropZone.binding.ts:13-16`) 지만 `renderDropZone` 이 `onDrop` 을 `createEventHandlerMap` 으로 RAC 에 넘기므로 (`FormRenderers.tsx:1106`) 등재 가능.

수정 방향: R2 문구를 "정책 (registry 주석) 이 차단, 테스트는 명명 규약만" 으로 정정하고 G3 에 정적 검사 1줄 추가 — 예: `const _fileTriggerEvents = ["onSelect"] as const satisfies readonly (keyof FileTriggerProps)[]` (DropZone 동형) 로 컴파일 시 RAC 실존을 증명. §7 후속은 "ADR-158 개정 또는 별도 ADR" 로 고친다.

### m2 — MEDIUM: Phase 1·2·3 병렬 착수 (사용자 결정) 에서 계약 SSOT 가 세 산출물에 분산되고 `/react` API 가 미정의다

배경: breakdown §4 (`:132`) 는 "1 → 2 순차, 3 은 1 완료 후" 인데 worktree 3개가 Phase 0~3 을 동시에 진행 중이다. 계약이 TUS 1.0 표준이라 **프로토콜 축**의 병렬은 안전하다 — 양쪽이 같은 외부 문서 (tus.io 1.0.0) 를 구현하고 G2 (같은 클라이언트 스위트를 mock · tusd · 참조 서버에 돌림) 가 발산을 잡는다. 안전하지 않은 축 둘:

1. **에러 코드 표** — R1 의 `E_PATCH_BLOCKED` / `E_PROXY_TIMEOUT` / `E_OFFSET_MISMATCH` … 는 클라이언트 (Phase 1) 가 정의하고 `server-contract.md` (Phase 2) 가 서술하며 mock 서버 (Phase 0) 가 재현한다. 세 곳을 대조하는 gate 가 없다 — G2 는 프로토콜만 본다. 불일치는 고객 통합 시점에 드러난다.
2. **`/react` entry** — breakdown §3-1 (`:55`) 은 `useUploadQueue · useUploadItem` 이름만 두고 시그니처가 없다. Phase 3 `renderFileUpload` 는 이 API 를 추정해 작성하게 되고 Phase 1 merge 시 충돌한다.

수정 방향: (a) breakdown §3-3 표를 "계약 v1.0 — 병렬 착수 commit 에서 freeze, 변경 시 3 worktree 동시 반영" 으로 선언 (b) 에러 코드 표를 `packages/upload-engine/src/errors.ts` (또는 JSON) 단일 소스로 두고 mock 서버 · 계약 문서 §Errors · 클라이언트가 같은 표를 import/생성 — G1 에 "계약 문서 에러 표 == 코드 enum" 정적 대조 1건 추가 (c) §3-2 에 `/react` 시그니처 (`useUploadQueue(opts): { items, add, pause, resume, cancel, retry }` 수준) 를 추가하고 Phase 3 는 그 타입 stub 에 대해 작성.

### m3 — MEDIUM: R3/G4 "문서/`ApiEndpoint` 정적 비밀 0" 게이트가 ADR-212 vault · ADR-213 redactor 계약과의 관계를 정의하지 않는다

근거: ADR-212 (Implemented 09-12) HC6 — auth 값은 프로젝트 로컬 vault, 문서에는 `{{secret.NAME}}` placeholder 만, 기존 평문은 Phase 4 로드 시 lazy 변환. ADR-213 HC5 — `redactEndpointSecrets` (`apps/builder/src/services/ai/security/redactEndpointAuth.ts`, AUTH_HEADER_KEYS 13종) 는 **AI provider 경계에서만** 가리고 IndexedDB 평문은 남는다 (파일 주석 3). 201 R3 (`:132`) 는 둘을 인용하지 않고 별도 "정적 게이트" 를 신설한다.

미정의 2가지: ① FileUpload 가 참조하는 endpoint 에 기존 평문 `Authorization` 이 있으면 어느 층이 어떤 결과를 내는가 (게이트 실패? 런타임 제거? `/project.json` 으로 서빙?) ② preview/publish 런타임은 vault 가 없다 — `{{secret.X}}` 가 헤더 값으로 그대로 전송되는지, `getHeaders()` provider 가 대체하는지. 또 인용 타입명: shared 정의는 `ApiEndpointDefinition` (`collection.types.ts:104`, b06ddd643 에서도 `:66` 같은 이름 · `id` 포함 · `headers?: ApiEndpointHeader[] | Record`) 이고 `ApiEndpoint {name, baseUrl, path, method?, headers?}` 는 builder `data.types.ts:210` 과 섞인 표기다.

수정 방향: R3 를 ADR-212 vault 계약 소비로 재작성 — 정적 게이트 = "FileUpload 참조 endpoint headers 에 placeholder 가 아닌 auth 값 0" (AUTH_HEADER_KEYS 재사용) · 런타임은 placeholder 헤더를 **제거**하고 쿠키 세션 / `getHeaders()` 에 위임 — breakdown §3-3 인증 행과 §3-6 클라이언트 행 갱신, 타입명 `ApiEndpointDefinition` 로 통일.

### l1 — LOW (deferred → Phase 0 inventory freeze): breakdown §2 라인 인용 drift

HEAD `d013b5658` 실측: `renderFileTrigger` 1037 → **1047** (selectedFiles write `:1089`) · `renderDropZone` 1078 → **1106** · `paletteItems.ts` 231-232 → **244-245** · `paletteOracle.ts` 292/298 → **286/292** · `componentCatalog.ts` FileTrigger 204 → **205** · DropZone 984 → **995** (ProgressBar 613) · `interactionRule.types.ts:47` → **:64** 이고 union 은 3종 → **4종** (`SetStateAction` 추가 — "업로드/HTTP 액션 없음" 결론은 유지) · `App.tsx:359` → **377** · `InteractionRuntime.tsx:126` → **102/132** · "cloud SDK `apps/builder/package.json:33`" → 09-12 소거로 **0건 (행 삭제)** · publish registry 는 `apps/publish/src/registry/ComponentRegistry.tsx` · ratchet 은 `apps/builder/src/builder/factories/__tests__/componentRegistrationContract.test.ts:131`. `selectedFiles` 소비처 0 · `XMLHttpRequest|upload.onprogress` 0 · capabilityRegistry 에 DropZone/FileTrigger 0 은 그대로다.

### l2 — LOW (deferred): `cloud.ts` 어댑터는 검증 oracle 이 0

breakdown §3-1 (`:53`) 의 6MB clamp + `x-upsert` 어댑터가 겨냥하는 서비스는 SDK·이름이 저장소에서 09-12 소거됐다 (메모리 `project-license-auth-closed-network-2026-09`). 대조 서버가 없고 HC1 6KB 예산도 core+tus 만이다 — §7 비스코프로 이동을 권고.

### l3 — LOW (deferred): 선례·측정법 인용 정정 2건

- "ADR-194 가 같은 상황에서 의존 0 자체 구현으로 간 선례" — 194 는 Implemented 이나 ADR-209 가 Preview/Publish 런타임에 Recharts 를 도입해 부분 대체했다 (`completed/194-*.md:3`). B 기각은 크기 (100KB+) + D3 이중화로 자립하므로 선례 문구만 정정.
- 힙 측정 `performance.memory` (breakdown §5 `:138`) — ADR-218 종결에서 raw usedSize 는 JIT+계측 버퍼라 **힙 스냅샷 JS 데이터 Δ** 로 판정하기로 했다 (메모리 `project-adr218-runtime-cache-closure-and-bundle-ceiling`). HC2 64MB 도 같은 방식으로.

### l4 — LOW (pending, 실행자 커밋 시): README Status 동기화

작업 트리 ADR Status 는 In Progress 인데 `docs/adr/README.md:52` 는 "Proposed — 2026-09-02" — adr-writing §Status 전이 규칙 (README 동시 갱신). 실행자 첫 커밋에서 처리.

### 요청 항목 판정 요약

| #   | 요청                              | 판정                                                                                                 |
| --- | --------------------------------- | ---------------------------------------------------------------------------------------------------- |
| 1   | breakdown §2 라인 drift           | l1 — 전부 LOW, Phase 0 freeze 갱신. 결론 (런타임 0 · 잘못된 채널 · 미등재) 은 유지                   |
| 2   | D2 참조 원천 정합                 | h3 — 원천 선언 방식은 194 선례로 허용, prop 3건 근거 없음 + 판정 기록 부재가 결함                    |
| 3   | 번들 상한 충돌                    | h2 — 202 정본과 충돌 (Preview Δ ≤ 0 · 절대 592,000) + "Δ 0" 달성 불가 + stale 인용 2건               |
| 4   | R2 ↔ capabilityRegistry           | m1 — `onSelect`/`onDrop` RAC 실존 ✓, "테스트가 차단" ✗, "예외 절차" 부재                             |
| 5   | Phase 1→2 병렬                    | m2 — 프로토콜 축은 안전 (TUS 표준 + G2), 에러 코드 표 · `/react` API 축은 gate 없음                  |
| 6   | 환경 (JDK/Maven/Go/tusd/docker 0) | h1 — G2 · G5(JSP) 실행 불가, 사용자 confirm 대체 경로 없음 → Implemented 도달 불가. 분리·증거화 필요 |

### 해결 질문 (사용자)

G2 서버 e2e 를 **사용자 머신 실행 + 로그·confirm 증거 (G2b)** 로 대체하는 것을 승인하는가, 아니면 이 머신에 **JDK + Maven + tusd 바이너리** 를 먼저 설치하는가? (docker · Go 는 어느 쪽에도 필요 없다.)

### 검증 범위와 한계

ADR 본문 · breakdown · 기존 리뷰 형식 (220) · `ssot-hierarchy.md` · `adr-writing.md` · `measurement-validity.md` · `review-loop-closure.md` 와 HEAD 코드 (FormRenderers · capabilityRegistry + 테스트 · bindings · componentCatalog · paletteItems/Oracle · interactionRule.types · collection.types · App.tsx · InteractionRuntime · adr202-bundle-gate · component-design SKILL · deploy.yml · RAC 1.21.0 타입 · ADR-158/194/202/212/213) 를 대조했다. 실행 환경은 `command -v` · `java -version` 실측. 구현 worktree 3개의 내용은 읽지 않았다 (리뷰는 구현과 독립). 번들 실측 · 힙 측정 · TUS 동작 · Spring 빌드는 수행하지 않았고 G0~G5 통과를 주장하지 않는다. 리뷰 로그만 저장, commit/push 없음.

## round 1 반영 기록 (실행자, 2026-09-17)

| ID  | outcome | 반영 지점                                                                                                                                                                                                                           |
| --- | ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| h1  | fixed   | ADR HC5 선행 조건 + G2 → G2a (mock + tusd 바이너리, 로컬 — **8/8 PASS 실측**) / G2b (Spring e2e, 사용자 머신 + 증거, 없으면 Implemented 불가) 분리. breakdown §3-5 선행 조건 표                                                     |
| h2  | fixed   | ADR HC1 을 202 정본 형식으로 재작성 · G3 "Δ 0" 폐기 · `apps/builder/scripts/adr201-bundle-gate.mjs` (self-test 5) · initial 상한 재승인 절 (실측 Builder +3,875 / Preview +3,507 — **승인은 사용자 결정, 판정기 `APPROVED=false`**) |
| h3  | fixed   | ADR Context D2 판정 표 (accepts 11 · `autoProceed` · `protocol` 제거) = `FileUpload.binding.ts` 머리말                                                                                                                              |
| m1  | fixed   | R2 문구 정정 · `capabilityRegistry.ts` events `as const satisfies readonly (keyof FileTriggerProps)[]` · §7 "ADR-158 개정 또는 별도 ADR"                                                                                            |
| m2  | fixed   | `packages/upload-engine/src/errors.ts` + `errors.json` 단일 소스 · `server-contract.md` §9 11개 3자 대조 (921a404df) · `/react` 시그니처 freeze (breakdown §3-2)                                                                    |
| m3  | fixed   | R3/G4 를 ADR-212 vault · ADR-213 `AUTH_HEADER_KEYS` 소비로 재작성 · `plaintextTokenGate.ts` (placeholder 통과) · 런타임 placeholder 헤더 제거                                                                                       |
| l1  | fixed   | Phase 0 inventory freeze (breakdown §2 재grep, 440aeb9dc)                                                                                                                                                                           |
| l2  | fixed   | `cloud.ts` 미작성 — §7 비스코프                                                                                                                                                                                                     |
| l3  | fixed   | ADR 대안 A 선례 문구 + HC2 힙 판정 방식 (스냅샷 JS 데이터 Δ)                                                                                                                                                                        |
| l4  | fixed   | README 201 행 In Progress                                                                                                                                                                                                           |

production 재현 시나리오가 없는 커버리지 지적은 없었다. round 2 는 열지 않는다 (review-loop-closure §1: phase 당 판독 1 + 수리 검증 1 — 수리 검증은 통합 세션 게이트 실행으로 대체: type-check 5/5 · 엔진 79 · shared 541 · builder 211 · publish 11 · live 20/20 · bundle gate 10/11, 남은 1 = 사용자 재승인).
