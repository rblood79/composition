---
adr: 205
title: 텍스트 시각 축 computed 단일 seam — letterSpacing 결선
reviews:
  - round: 1
    ts: '2026-09-04T18:44:14.744Z'
    reviewer: claude
    source: live
    issues:
      - id: h1
        severity: HIGH
        category: evidence-missing
        summary: >-
          F9/Context 의 '레이아웃 letterSpacing 유일 유입 =
          inlineSpecStyle?.letterSpacing' 이 거짓 — width leg 은 이미 결선돼 있고 실제 결손은
          wrap/height leg
        evidence: >-
          apps/builder/src/builder/workspace/canvas/layout/engines/utils.ts:2210-2212
          (parseNumericValue(style?.letterSpacing) ??
          computedStyle?.letterSpacing ?? 0), 진입 utils.ts:5210·5219
          (_computedStyle 전달) / 결손 utils.ts:5469 measureTextWithWhiteSpace ·
          apps/builder/src/builder/workspace/canvas/utils/textMeasure.ts:531
          measureWrappedTextHeight (letterSpacing 인자 없음) / ADR:23-24, breakdown
          F9
        root_cause: >-
          evidence/051-letterspacing-canvas2d.md §5 의 round-1 결론을 재확인 없이 승계.
          breakdown §3 소비자 (a) 가 이미 결선된 width 경로를 가리키고 live 증상(줄 수)을 만드는 wrap
          경로를 놓친다
        outcome: fixed
        addressed_in: 'round 1 반영 — ADR 본문/breakdown/evidence 051 §5-1/README (2026-09-05)'
      - id: h2
        severity: HIGH
        category: evidence-missing
        summary: >-
          Skia scene build 경로에 ComputedStyle 이 존재하지 않아 소비자 (b) 배선 규모와 R2 완화가
          성립하지 않는다
        evidence: >-
          grep -rn "ComputedStyle|resolveStyle("
          apps/builder/src/builder/workspace/canvas/skia/ → 0건;
          specShapeConverter.ts:162 specShapesToSkia(shapes, theme,
          containerWidth, containerHeight, elementId); resolveStyle 결과는
          fullTreeLayout.ts:1872 재귀 지역 변수로만 존재 / ADR:111-112, 153-154, breakdown
          §3(b)
        root_cause: >-
          '_computedStyle 은 이미 같은 호출에 전달돼 있다' 는 사실이 레이아웃 소비자에만 참인데 두 소비자 모두에 적용된
          것으로 서술
        outcome: fixed
        addressed_in: 'round 1 반영 — ADR 본문/breakdown/evidence 051 §5-1/README (2026-09-05)'
      - id: m3
        severity: MEDIUM
        category: alternative-strawman
        summary: >-
          대안 A 를 '호출 지점마다 수작업' 으로 기술했으나 Skia 쪽 실제 형태는 ADR-057 의 13필드 일반화 블록에 1행
          추가
        evidence: >-
          apps/builder/src/builder/workspace/canvas/skia/buildSpecNodeData.ts:2050-2165
          (whiteSpace·wordBreak·overflowWrap·lineHeight·textIndent·clipText·textDecoration(+style/color)·textOverflow·wordSpacing·fontVariant·fontStretch·textShadow·verticalAlign
          — letterSpacing 만 부재) / ADR:74-92 대안 A, breakdown F10
        root_cause: >-
          F10 이 specShapeConverter.ts:891 만 인용하고 buildSpecNodeData 의 inline
          style → child.text 투영 블록을 지나침 → 유지보수 H 판정 근거가 실제보다 커짐
        outcome: fixed
        addressed_in: 'round 1 반영 — ADR 본문/breakdown/evidence 051 §5-1/README (2026-09-05)'
      - id: m4
        severity: MEDIUM
        category: other
        summary: G2·G3 의 fixture 가 letterSpacing≠0 요소를 포함하지 않아 새 경로를 한 번도 실행하지 않는다
        evidence: >-
          ADR:49-53 (factory/import/theme letterSpacing 0건 — 기존 fixture 에 ls≠0 이
          있을 수 없음) vs ADR:189-190 (G2 600요소 p95 회귀 0 · G3 maxByte 0).
          measurement-validity.md §1 Q2 · §2 패턴 6·8
        root_cause: >-
          불리 케이스(ls≠0 다수 → ctx.letterSpacing set + fontKey 분기 캐시 미스)를 Gate 명시
          항목으로 두지 않아 G2 는 vacuous, G3 의 maxByte 0 은 '변경 미반영' 과 구별 불가
        outcome: fixed
        addressed_in: 'round 1 반영 — ADR 본문/breakdown/evidence 051 §5-1/README (2026-09-05)'
      - id: m5
        severity: MEDIUM
        category: other
        summary: >-
          G4 집합 출처(INHERITABLE_PROPERTIES)가 'DOM 이 소비하는 텍스트 CSS 속성 집합' 의 대리로
          부정확하고, 집합 누락은 잡지만 표면 미도달은 못 잡는다
        evidence: >-
          apps/builder/src/builder/workspace/canvas/layout/engines/cssResolver.ts:34-52
          (visibility 포함 / textDecoration·textShadow·textOverflow·verticalAlign
          제외 — 이 4종은 buildSpecNodeData.ts:2112-2165 가 실제로 소비) / ADR:179 R4,
          ADR:191 G4
        root_cause: >-
          상속 속성 목록과 DOM 소비 텍스트 속성 집합을 동일시. 또 게이트 판정 축이 '필드 존재' 라 F15 형태(필드는 있으나
          표면 미도달)를 검출하지 못한다
        outcome: fixed
        addressed_in: 'round 1 반영 — ADR 본문/breakdown/evidence 051 §5-1/README (2026-09-05)'
      - id: l6
        severity: LOW
        category: evidence-missing
        summary: letterSpacing 재조립 4번째 지점(paragraph 캐시 키) 미기재
        evidence: >-
          apps/builder/src/builder/workspace/canvas/skia/textParagraphKey.ts:44
          (node.text.letterSpacing ?? 0) / breakdown F11 은
          nodeRendererText.ts:463·532·648 만 나열
        root_cause: 캐시 키 미갱신 시 seam 결선 후에도 ls 변경이 stale paragraph 로 무시될 수 있다
        outcome: fixed
        addressed_in: 'round 1 반영 — ADR 본문/breakdown/evidence 051 §5-1/README (2026-09-05)'
      - id: l7
        severity: LOW
        category: other
        summary: docs/adr/README.md 현황 줄에 ADR-205 미등재 (201·202·203 Proposed 는 등재)
        evidence: 'docs/adr/README.md:7 (ADR-201/202/203 Proposed 항목 존재, 205 없음)'
        root_cause: Proposed 작성 시 README 대시보드 동시 갱신 누락
        outcome: fixed
        addressed_in: 'round 1 반영 — ADR 본문/breakdown/evidence 051 §5-1/README (2026-09-05)'
      - id: l8
        severity: LOW
        category: other
        summary: Hard 3 · G3 이 자기 게이트를 'visual-parity G2' 로 지칭 (참조 오기)
        evidence: 'ADR:61 · ADR:190'
        root_cause: 게이트 번호 교차 참조 오기
        outcome: fixed
        addressed_in: 'round 1 반영 — ADR 본문/breakdown/evidence 051 §5-1/README (2026-09-05)'
    hate:
      assumption: >-
        seam 은 _computedStyle 을 읽기만 하면 되므로 추가 조회가 아니고, computed 를 읽으므로 CSS 상속이
        자동으로 맞는다 (ADR:111-112, 153-154)
      root: >-
        그 가정은 레이아웃 소비자에만 참이다 — Skia scene build 경로에는 ComputedStyle 이 존재하지 않으며
        resolveStyle 결과는 fullTreeLayout.ts:1872 의 재귀 지역 변수로만 산다. 소비자 (b) 결선은
        cascade 를 scene build 로 새로 끌어오는 작업이고, 그러면 R2 완화와 '기술 M — letterSpacing 한
        축' 완화가 동시에 무효
      axis: load-bearing 사실이 거짓
      first_nail: >-
        grep -rn "ComputedStyle|resolveStyle("
        apps/builder/src/builder/workspace/canvas/skia/ — 1 명령, 초 단위 (결과 0건).
        Phase 1 전체보다 압도적으로 싸다
      verdict: 'issue:2'
    prism:
      lenses:
        - lens: SSOT/D3 정합
          verdict: pass
          reason: 'D3 대칭 위반 분류가 정확하고 catalog 채널(F14)과 인라인 채널을 분리 선언, D2 신설 prop 0'
        - lens: 측정 무결성
          verdict: fail
          reason: G2·G3 fixture 에 ls≠0 요소가 있을 수 없어 새 경로를 한 번도 실행하지 않는다
        - lens: 비용/범위
          verdict: fail
          reason: 'Phase 1 소비자 (a) 는 이미 결선된 width leg, (b) 는 computed 없는 곳을 가리킨다'
        - lens: 되돌림/BC
          verdict: pass
          reason: 영향 범위를 grep 3축 0건으로 수식화하고 R5 로 사용자-가시 변화를 명시
      convergence: 불일치
      question: >-
        Phase 1 의 Skia leg 은 (a) ComputedStyle 을 scene build 로 배선해 상속까지 이번 phase
        에서 맞추는가, 아니면 (b) buildSpecNodeData.ts:2050-2165 (ADR-057 블록) 에 인라인
        letterSpacing 한 행을 더해 상속은 후속 phase 로 미루는가?
---
# ADR-205 Review Log

## Round 1 — 2026-09-04 (reviewer: claude)

## ADR 리뷰 결과: ADR-205 텍스트 시각 축 computed 단일 seam

### 구조 검증

| 섹션 | 판정 | 품질 | 인용 |
| --- | --- | --- | --- |
| Context (hard constraint 5개) | PASS | HIGH | `ADR:55-64` |
| Alternatives 3개 | PASS | HIGH | `ADR:74-128` |
| 4축 위험 평가 | PASS | HIGH | `ADR:80-92, 108-115, 123-128` |
| Risk Threshold Check | PASS | HIGH | `ADR:130-139` |
| Decision 위험 수용 + 기각 사유 | PASS | HIGH | `ADR:143-168` |
| 구현 상세 분리 | PASS | HIGH | `ADR:170` |
| Risks (Decision 뒤/Gates 앞) | PASS | MED | `ADR:172-181` |
| Gates | PARTIAL | MED | `ADR:183-192` |
| seed #1~#4 | PASS | HIGH | `ADR:43-53, 84-91, 156-160` |

### 코드 검증 (F1~F17)

| 주장 | 판정 | 근거 |
| --- | --- | --- |
| F1~F4 cssResolver 상속 17종·초기값·타입·resolveStyle | VERIFIED | `cssResolver.ts:34-52, 78, 242, 274, 581` |
| F5 `enrichWithIntrinsicSize(_computedStyle)` | VERIFIED | `utils.ts:4912-4917` |
| F6 "computed 소비 텍스트 속성 = whiteSpace 하나뿐" | UNVERIFIED | `utils.ts:5051, 5240, 5247, 5250` 이 fontSize/fontFamily/fontWeight 도 computed 로 fallback |
| F7·F8 measureTextWidth · fontSize 21곳 | VERIFIED | `utils.ts:1341`, grep -c = 21 |
| F9 "레이아웃 letterSpacing 유일 유입" | UNVERIFIED | `utils.ts:2210-2212` 가 인라인+computed 소비 |
| F10·F11·F12 | VERIFIED | `specShapeConverter.ts:891`, `nodeRendererText.ts:463/532/648`, catalog grep 0 |
| F13 62곳 · F14 Generator emit | VERIFIED | grep 62, `CSSGenerator.ts:969-971` |
| F17 측정기 결선 준비 | VERIFIED | `canvas2dSegmentCache.ts:527-534`, `8b6c1bd22` |
| BC factory·theme 0건 · 패널 노출 | VERIFIED | grep 0/0, `TypographySection.tsx:276-280` |

### 위험 평가 — 누락 위험 2건

| 위험 | 증거 | 심각도 | 상태 |
| --- | --- | --- | --- |
| Skia scene build 에 ComputedStyle 자체가 없다 | `canvas/skia/**` grep 0건; `specShapeConverter.ts:162` | HIGH | 누락 |
| 레이아웃 wrap/height 경로가 letterSpacing 미수용 | `utils.ts:5469` · `textMeasure.ts:531` | HIGH | 누락 |

### 근본 반론 (hate)

- **load-bearing 가정**: seam 은 `_computedStyle` 을 읽기만 하면 되고 그래서 상속이 자동으로 맞는다 (`ADR:111-112, 153-154`).
- **root**: 그 가정은 레이아웃 소비자에만 참이다. Skia 텍스트 경로에는 computed 가 없다 — `resolveStyle` 결과는 `fullTreeLayout.ts:1872` 재귀 지역 변수이고 skia 디렉터리 참조 0건. 공격 축: load-bearing 사실이 거짓.
- **first nail**: `grep -rn "ComputedStyle|resolveStyle(" .../canvas/skia/` — 1 명령. 결과 0건 → root 성립.
- **판정**: 어느 Gate 도 커버 안 함 (G0 은 속성×표면 도달 축이지 표면이 resolver 입력을 가졌는지 축이 아님) → 이슈 #2 (HIGH).

### 렌즈 대조 (prism)

| 렌즈 | 판정 | load-bearing 이유 |
| --- | --- | --- |
| SSOT/D3 정합 | pass | D3 대칭 위반 분류 정확, catalog↔인라인 채널 분리 선언, D2 신설 prop 0 |
| 측정 무결성 | fail | G2·G3 fixture 가 새 경로를 실행하지 않는다 (BC 절 스스로 ls 기본값 0건) |
| 비용/범위 | fail | Phase 1 두 소비자 위치가 모두 실제 결손 지점과 다름 |
| 되돌림/BC | pass | 영향 범위 grep 3축 0건 수식화 + R5 + phase 단위 원복 |

- **수렴**: 불일치 (SSOT·BC pass vs 측정·범위 fail)
- **해결 질문 1**: Phase 1 의 Skia leg 은 (a) ComputedStyle 을 scene build 로 배선해 상속까지 이번 phase 에 맞추는가, (b) `buildSpecNodeData.ts:2050-2165` 에 인라인 letterSpacing 1행을 더하고 상속은 후속 phase 로 미루는가?

### 종합 판정

- 구조: PASS (Gates PARTIAL)
- 코드 정합: 15/17 VERIFIED
- 누락 위험: 2건 (HIGH 2)
- 권고: HIGH 2 (F9/Context 정정 + wrap leg 재지정 · Skia computed 부재로 인한 Phase 1 규모 재평가) / MEDIUM 3 (대안 A 재기술 · G2·G3 불리 케이스 · G4 집합 출처) / LOW 3
- 결론: **수정 후 재리뷰**

## Round 1 반영 기록 (2026-09-05)

8건 전부 반영. 사용자 판정: **"인라인만 Phase 1, 상속은 조건부 Phase"** (결정 지점 ④ scope 확정).

| 이슈 | 반영 내용 |
| --- | --- |
| h1 (wrap leg 오지정) | ADR Context 를 "결손 3자리" 로 재기술 · breakdown F9/F9b/F9c 정정 + F19 신규 · Phase 1 소비자를 폭 leg(값 변화 0 리팩터) / **wrap leg(실제 결손)** / Skia 3곳으로 재지정 |
| h2 (Skia ComputedStyle 부재) | breakdown F20 신규 (`canvas/skia/**` 참조 0건 · `specShapesToSkia` 시그니처 · `fullTreeLayout.ts:1872` 지역 변수) · seam 의 `computed` 를 선택 인자로 · R2 완화를 레이아웃 소비자 한정으로 · **Phase 5 (Skia cascade 배선) 신설** · Decision Phase 분리 문단에 표면 최소화 근거 |
| m1 (대안 A strawman) | breakdown F18 신규 (ADR-057 Phase A/B 블록 13종, letterSpacing 만 부재) · 대안 A 를 "속성별 배선 / 축당 3파일 5지점" 으로 재기술하고 Skia 비용이 1행임을 명시 · 기각 사유 재작성 (H 유지, 근거 교체) |
| m2 (G2·G3 leakage) | G2 에 불리 케이스 필수 (텍스트 leaf 절반 이상 ls≠0 arm + 대조군 + 측정 조건) · G3 를 "기존 fixture maxByte 0 **와** 신규 ls fixture maxByte > 0" 2조건으로 (변경 미반영과 구별) |
| m3 (G4 집합 출처) | R4 의 집합 출처를 F1(텍스트 항목, `visibility` 제외) ∪ F18 합집합으로 · G4 를 집합 대조 + **도달 검사** 2축으로 |
| l1 (캐시 키) | breakdown F21 신규 (`textParagraphKey.ts:44`) · Phase 1 (c) 에 동반 갱신 명시 |
| l2 (README) | 표 행은 이미 있었고(리뷰의 부분 오판) **현황 요약 줄 누락**이 실제 결손 — 요약 줄에 ADR-205 추가 + 표 행 요약을 정정본으로 교체. 개수 표(214/4/14/232)는 Proposed 커밋이 이미 갱신했으므로 불변 |
| l3 (G3 참조 오기) | Hard 3 · G3 의 "visual-parity G2" → G3 |

**신규 위험**: R7 (Phase 1 이 인라인만 닫으므로 상속 ls 에서 layout↔paint 발산 잔존 — 현행 상태의 명시이자 Phase 5 착수 판정 입력).

**미반영**: 없음. 후속 조건부 phase 2개 (Phase 4 fontSize 수렴 · Phase 5 Skia cascade) 는 본 ADR 종결 조건이 아니다.

**잔존 doc-drift (본 ADR 범위 밖)**: `docs/adr/README.md` 현황 요약 줄의 개수(210/4/16/230)가 개수 표(214/4/14/232)와 어긋나 있다 — 본 리뷰 이전부터의 drift.
