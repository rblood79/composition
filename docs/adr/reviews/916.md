---
adr: 916
title: 자체 단일 Rust 엔진 통합
reviews:
  - round: 1
    ts: '2026-07-03T11:57:50.763Z'
    reviewer: claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          Context 병목 서술 '노드당 WASM 경계 ~5회
          (createNode/updateStyle/setChildren/computeLayout/getLayout)' 가 활성 경로와
          불일치 — 활성 persistentTaffyTree 는 buildTreeBatch 1회 batch + 증분
          updateStyleRaw + 문서 단위 computeLayout/getLayoutsBatch. per-node 5-call
          은 비활성 composition-layout API
        evidence: >-
          docs/adr/916-unified-rust-engine.md:14 vs
          persistentTaffyTree.ts:16,109,139 +
          composition-layout/src/lib.rs:91,146,183,338,365
        root_cause: 감사 agent 의 비활성 엔진 API 서술을 활성 경로 병목으로 승계
        outcome: pending
      - id: m2
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          대안 B / Phase 0-B 'LAYOUT_WORKER=true → main thread 해방' 과대 서술 — worker
          는 BLOCK_LAYOUT/GRID_LAYOUT 가속기만 처리, Taffy full-tree solve 는 main
          thread 잔류
        evidence: >-
          docs/adr/916-unified-rust-engine.md:53 + breakdown 0-B vs
          layoutWorker.ts:33-38
        root_cause: worker 인프라 '완성' 서술을 전체 layout offload 로 확대 해석
        outcome: pending
      - id: m3
        severity: MEDIUM
        category: other
        summary: >-
          breakdown Phase 2-C (scene.rs) 가 ADR-136 sceneVersion projection
          content signature 계약 승계 미언급 — 2-D 는 §8 scrollOffset 계약 명시했으나 2-C 는 §9
          누락 (분류: contract-inheritance-gap)
        evidence: >-
          design/916-unified-rust-engine-breakdown.md:122-125 vs
          scene/buildSceneSnapshot.ts:91,206 + canvas-rendering.md §9
        root_cause: scene graph 이관 시 dirty detection 만 명시하고 version signature 계약 미포함
        outcome: pending
  - round: 2
    ts: '2026-07-03T12:52:42.828Z'
    reviewer: codex
    source: live
    issues:
      - id: h1
        severity: HIGH
        category: evidence-missing
        summary: >-
          Phase 0-A USE_RUST_LAYOUT_ENGINE=true 는 flag 단독 전환 무효 — layoutBridge 는
          flag true 시 경고 후 TaffyLayout fallback (wiring 주석 처리),
          persistentTaffyTree 는 factory 미경유 직접 new TaffyLayout(). batch API vs
          per-node API 비호환까지 확인 (claude 확증)
        evidence: >-
          persistentTaffyTree.ts:27,84 + layoutBridge.ts:36-44 +
          composition-layout/src/lib.rs:91-365
        root_cause: 인프라 '기구현' 을 '배선 완료' 로 오독한 Phase 0 전제 — flag flip 으로는 동작 경로 자체가 없음
        outcome: fixed
      - id: h2
        severity: HIGH
        category: evidence-missing
        summary: >-
          Phase 0-B LAYOUT_WORKER=true 는 worker 초기화만 수행 — getLayoutScheduler
          caller 0건 (전체 grep 공집합), block/grid 계산을 scheduler 로 보내는 소비 경로 미배선.
          flag 전환 효과 0
        evidence: 'init.ts:41-48 + wasm-worker/index.ts:32 + layoutWorker.ts:33-38'
        root_cause: SWR 캐시 '구현 존재' claim 이 소비자 부재를 은폐 — 인프라 코드 존재 ≠ 가동 경로 존재
        outcome: fixed
      - id: m3
        severity: MEDIUM
        category: adr-structure-violation
        summary: >-
          HC3 내부 충돌 — dual-run ≤ 1px tolerance 와 시각 diff 0 동시 요구 (1px layout
          shift 는 그 자체가 시각 diff). G2 는 ≤ 1px 만 게이트
        evidence: 'docs/adr/916-unified-rust-engine.md:28,126'
        root_cause: 수치 tolerance 와 device pixel 라운딩 기준 미분리
        outcome: fixed
      - id: m4
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          R6 'Paragraph 객체 캐싱 금지' 포괄 서술이 render 경로 현실과 충돌 —
          nodeRendererText.ts:36 은 관리형 Paragraph 객체 캐시 실존 (delete 수명 관리).
          canvas-rendering.md §3 규칙은 측정 경로 대상
        evidence: 'nodeRendererText.ts:36 vs canvaskitTextMeasurer.ts:122-127'
        root_cause: 측정 경로 규칙을 render 경로까지 확대 적용한 scope 미분리
        outcome: fixed
---
# ADR-916 Review Log

## Round 1 — 2026-07-03 (reviewer: claude)

## ADR-916 리뷰 (round 1)

- 구조: PASS 8/8 (HIGH 품질) — hard constraint 5개(ADR:24-30), 대안 4개 4축(ADR:40-78), Threshold 루프(ADR:80-89), Decision 수용/기각(ADR:91-105), Risks R1~R7(ADR:109-119), Gates G1~G5(ADR:121-129), 선차단 seed 4항목 통과
- 코드 정합: 11/13 VERIFIED (JSON.stringify dirty persistentTaffyTree.ts:139,158,213,293 / 3x solve fullTreeLayout.ts:2343,2602,2718 / z-sort renderCommands.ts:538-588 / detectChangedIds StoreRenderBridge.ts:226,259 / FNV-1a lib.rs:12-15 / 줄수 19건 wc 전수 일치 / parseGridTemplate TaffyGridEngine.ts:51 / buildCatalogShapes buildSpecNodeData.ts:22,945)
- UNVERIFIED 1: ADR:14 노드당 ~5회 WASM call — 활성 경로는 batch API
- PARTIAL 1: ADR:53 worker main thread 해방 — block/grid 가속기 한정
- 누락 위험 2 (MED): 2-C sceneVersion signature 계약 미언급 / 0-B worker 커버리지 갭
- 결론: 문구 정정 3곳 즉시 적용 가능, 구조 건전

## Round 2 — 2026-07-03 (reviewer: codex)

## ADR-916 리뷰 (round 2 — codex 교차 리뷰, claude 4/4 CONFIRMED 후 정정)

- HIGH 2건: Phase 0-A/0-B 'flag Quick Win' 전제 파기 — 배선(wiring) 작업으로 재작성 (breakdown Phase 0 전면 개정: 0-A 엔진 주입 + batch API 정합 3작업 / 0-B scheduler 소비 배선). ADR 대안 B/D 설명 + Decision 근거 2 동기 정정
- MED 2건: HC3 2단 기준 신설 (수치 ≤1px + 1x zoom device pixel 스크린샷 diff 0, (b) 우선) + G1/G2 통과 조건 정합 / R6·2-E 측정 경로 한정 명시 (render paragraphCache 비대상·현행 유지)
- 판정 절차: 사용자 지시 '수정전 판정부터' — 4건 전부 코드 실증 후 정정 (persistentTaffyTree.ts:27,84 / layoutBridge.ts:36-44 / init.ts:41-48 / index.ts:32 caller 0건 / nodeRendererText.ts:36)
- round 1 pending 3건 중 sceneVersion 계약은 이미 보강 확인 (codex 언급)
