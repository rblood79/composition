---
adr: 144
title: Composite RAC resolved-tree 정합성 및 slot-editable component contract
reviews:
  - round: 1
    ts: '2026-05-21T08:49:37.968Z'
    reviewer: claude
    source: live
    issues:
      - id: l1
        severity: LOW
        category: other
        summary: >-
          shadcn-design-system fixture reusable count claim (87) 이 jq `reusable
          == true` 측정값 (174) 과 불일치 — query 정의 명시 또는 본문 정정 권장
        evidence: 'docs/adr/design/144-composite-rac-resolved-tree-parity-breakdown.md:85'
        root_cause: >-
          breakdown inventory 표기 시 어떤 query 기준으로 카운트했는지 미명시. contract 결정에는 영향 없음
          (slot host 22 / refs 119 핵심 evidence 는 정확)
        outcome: fixed
        resolution: >-
          Codex rechecked the query definitions. Top-level `.reusableComponents
          | length` is 87, while deep `.. | objects | select(.reusable? ==
          true)` is 174 because it also counts reusable nodes under the selected
          exported tree. Breakdown line was clarified to state both counts.
  - round: 2
    ts: '2026-05-21T08:54:46.206Z'
    reviewer: claude
    source: live
    issues: []
  - round: 3
    ts: '2026-05-21T10:53:02.517Z'
    reviewer: claude
    source: live
    issues: []
  - round: 4
    ts: '2026-05-21T13:13:07.108Z'
    reviewer: claude
    source: live
    issues:
      - id: h1
        severity: HIGH
        category: ssot-violation
        summary: >-
          C3-a (1) / C3-b 의 `CompositionDocument.collections` (canonical root
          collection) 표현이 ADR-131 Phase 8 framing revert 와 정합 안 함
        evidence: 'packages/shared/src/types/composition-document.types.ts:439-440'
        root_cause: >-
          ADR-131 Phase 8 에서 사용자 framing 으로 `data` root field 추가 미수행,
          `collections` 는 IndexedDB store 로만 존재. C3-a/C3-b 가
          `CompositionDocument.collections` root field 인 듯 표현했으나 실제는 store. data
          SSOT 정의 정정 필요
        outcome: fixed
        addressed_in: 7aa7d16b3
      - id: m2
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          Phase 5 task 7 의 'Phase 7 family expansion 의 다른 7 family 에 동일 패턴 확장
          후보' framing 이 land 상태 미반영 — ADR-076 getCustomPreEditor 패턴은 이미 4 family
          (ListBox/Menu/TagGroup/GridList) land 완료
        evidence: 'apps/builder/src/builder/inspector/editors/registry.ts:38-60'
        root_cause: >-
          보강 작성 시 registry.ts grep 미수행. 4 family land 상태 + Tabs 의
          `getHybridAfterSections` 별 패턴 인지 부재. land 상태 반영하여 '미land 5 family +
          Tabs pattern mismatch' 로 정정 필요
        outcome: fixed
        addressed_in: 7aa7d16b3
      - id: l3
        severity: LOW
        category: evidence-missing
        summary: >-
          C3-a/b/Phase 5 task 7 인용이 모두 symbolic (ADR-132 / ADR-076) — file:line
          부재. 구체 path 추가 권장
        evidence: >-
          docs/adr/design/144-composite-rac-resolved-tree-parity-breakdown.md:144-392
        root_cause: >-
          review-adr Anti-Hallucination 규칙 1 (증거 없으면 판정 없음) 정합 차원. ADR-076 패턴
          인용에 `apps/builder/src/builder/inspector/editors/registry.ts:38` 추가 권장
        outcome: fixed
        addressed_in: 7aa7d16b3
      - id: l4
        severity: LOW
        category: other
        summary: >-
          Phase 2 task 5 의 '0 ref vs 1 sample ref' 미결정 잔존 — Phase 5 task 7 slot
          추가/삭제 UI 와의 cross-link 권장
        evidence: >-
          docs/adr/design/144-composite-rac-resolved-tree-parity-breakdown.md:298
        root_cause: >-
          사용자 framing 의 'Properties UI 로 항목 추가' 기본 entry point 가 Phase 5 task 7
          slot UI 로 cover 되는데, Phase 2 task 5 의 default child set 결정이 G2/G6
          evidence 단계로 미뤄짐. 두 task 간 cross-link 부재로 design 결정 추적성 낮음
        outcome: fixed
        addressed_in: cross-link follow-up commit (same session)
  - round: 5
    ts: '2026-05-21T14:00:56.292Z'
    reviewer: codex
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          C3-a (2) 의 'API 데이터가 collections IndexedDB store runtime entry 로 저장'
          표현이 코드와 불일치 — runtimeData 는 메모리만, DB 미저장
        evidence: >-
          apps/builder/src/types/builder/data.types.ts:68 +
          apps/builder/src/builder/stores/utils/dataActions.ts:645-660
        root_cause: >-
          runtimeData 타입 주석 명시: '메모리에만 존재, DB에 저장 안함'. executeApiEndpoint 도
          set({ collections }) + canvas sync 만, DB update 호출 없음. 'IndexedDB
          store 에 저장' 표현 → 'Zustand collections.runtimeData runtime sink +
          Preview postMessage 동기화' 로 정정 필요
        outcome: fixed
        addressed_in: follow-up commit (same session)
      - id: m2
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          C3-a (2) 의 'API source 가 항상 targetCollection -> runtimeData sink' 표현이
          Preview/Canvas 경로 누락 — Canvas branch 는 /api/proxy direct fetch
          fallback 잔존
        evidence: >-
          apps/builder/src/builder/hooks/useCollectionData.ts:340-355 +
          docs/adr/design/132-usecollectiondata-useasynclist-alignment-breakdown.md:241
        root_cause: >-
          Builder hook 의 isCanvasContext branch 는 executeApiEndpoint 가 아니라
          /api/proxy 직접 fetch + list.items 반환. ADR-132 Canvas 측 sink 정합 완성은 후속
          ADR lock-in. C3-a 에 두 경로 (Builder execute path vs Preview/Canvas
          direct proxy fallback) 명시 + ADR-144 G3/G6 에서 row identity 검증 추가 필요
        outcome: fixed
        addressed_in: follow-up commit (same session)
      - id: m3
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          C3-b 의 '한 instance 는 둘 중 하나 (external vs children/ref)' 가 C3-a 3축 모델과
          충돌 — Properties inline props.items[] 축이 authoring-local 인지 legacy
          fallback 인지 흐려짐
        evidence: >-
          docs/adr/design/144-composite-rac-resolved-tree-parity-breakdown.md:146,158
        root_cause: >-
          C3-a 는 3축 선언인데 C3-b 는 dataBinding 설정 여부만으로 2 state 로 축소. instance
          input mode 를 3개 분리 명시 필요: (1) external dataBinding (2) new
          resolved-tree local data (3) legacy props.items[] adapter fallback
        outcome: fixed
        addressed_in: follow-up commit (same session)
---

# ADR-144 Review Log

## Round 1 — 2026-05-21 (reviewer: claude)

## ADR 리뷰 결과: ADR-144 Composite RAC resolved-tree 정합성 및 slot-editable component contract

### 구조 검증

| 섹션                           | 판정 | 품질 | 인용        |
| ------------------------------ | ---- | ---- | ----------- |
| Context (hard constraints 7개) | PASS | HIGH | ADR:7-64    |
| Alternatives (4개 + 4축 위험)  | PASS | HIGH | ADR:66-123  |
| Risk Threshold Check           | PASS | HIGH | ADR:125-136 |
| Decision                       | PASS | HIGH | ADR:138-174 |
| 구현 상세 분리                 | PASS | HIGH | ADR:176     |
| Risks (Decision 뒤, Gates 앞)  | PASS | HIGH | ADR:178-189 |
| Gates                          | PASS | HIGH | ADR:191-202 |

fork checkpoint 4 질문: `Amends ADR-142` 명시 + base/응용 분류 + framing reverse 검증 정합.

### 코드 검증 (핵심 주장)

| 주장                                                                    | 판정     | 근거                                                                                              |
| ----------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------- |
| Tabs catalog `defaultProps.items[]` + `skiaPrimitive: { kind: "tabs" }` | VERIFIED | packages/shared/src/catalog/primitives/tabs.ts:119-141                                            |
| Preview `<Tabs {...tabsProps}>` 직접 렌더                               | VERIFIED | apps/builder/src/preview/components/CanonicalNodeRenderer.tsx:742-747                             |
| Skia `buildGenericTabsNode()` synthetic `${node.id}:tab:*`/`:panel:*`   | VERIFIED | apps/builder/src/builder/workspace/canvas/skia/buildSpecNodeData.ts:4985,5059,5072,5098,5117,5134 |
| canonical `reusable`/`ref`/`descendants`/`slot` primitive 존재          | VERIFIED | packages/shared/src/types/composition-document.types.ts:262-278,325-331                           |
| ADR-142 Implemented                                                     | VERIFIED | docs/adr/completed/142-...:5                                                                      |
| ADR-910 Proposed (parity 이후 perf layer 정합)                          | VERIFIED | docs/adr/910-...:5                                                                                |
| RAC-showcase reusable 67 / refs 263                                     | VERIFIED | jq 측정 일치                                                                                      |
| shadcn refs 119 / slot hosts 22                                         | VERIFIED | jq 측정 일치                                                                                      |
| shadcn reusable 87 (breakdown 주장)                                     | PARTIAL  | jq deep count 174 — query 방식 차이 추정                                                          |

### 위험 평가

6개 Risk (R1 HIGH / R2-R5 MED / R6 LOW) 모두 Gate 와 1:1 대응. 누락 위험 0건.

### 종합 판정

- 구조: PASS (7/7 HIGH)
- 코드 정합: 8/9 VERIFIED, 1 PARTIAL
- 누락 위험: 0건
- 권고: HIGH/MED 없음, LOW 1건 (fixture 카운트 정합화)
- 결론: **승인 가능**

## Codex follow-up — 2026-05-21

- LOW `l1` resolved.
- 재측정:
  - top-level `.reusableComponents | length` = 87
  - deep `.. | objects | select(.reusable? == true)` = 174
  - refs = 119
  - slot hosts = 22
- 조치: `docs/adr/design/144-composite-rac-resolved-tree-parity-breakdown.md` 의
  shadcn fixture inventory 에 top-level count 와 deep count 정의를 모두 명시했다.

## Round 2 — 2026-05-21 (reviewer: claude)

## ADR 리뷰 결과 (Round 2): ADR-144

### 변경 범위

| 파일                                       | 변경                                                                                                                                                                   |
| ------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| docs/adr/144-...md                         | 변경 없음                                                                                                                                                              |
| docs/adr/design/144-...-breakdown.md:83-88 | shadcn fixture inventory 표기 정정 — `reusable components: 87` → `top-level reusableComponents entries: 87` + `deep reusable:true objects: 174` 병기 + query 기준 명시 |

Round 1 단 1건 LOW 권고 (shadcn reusable 카운트 query 정의 미명시) 를 직접 정정.

### 구조 검증

ADR 본문 변경 없음 → Round 1 판정 유효 (7/7 PASS HIGH).

### 코드 검증 (변경된 주장)

| 주장                                               | 판정     | 근거                                                              |
| -------------------------------------------------- | -------- | ----------------------------------------------------------------- |
| shadcn top-level `reusableComponents` entries = 87 | VERIFIED | jq `.reusableComponents \| length` → 87                           |
| shadcn deep `reusable:true` objects = 174          | VERIFIED | jq `[.. \| objects \| select(.reusable == true)] \| length` → 174 |
| contract coverage uses top-level entries (87) 기준 | VERIFIED | breakdown:85-87                                                   |

Round 1 PARTIAL → VERIFIED.

### 위험 평가

변경 없음. 6 Risk 모두 Gate 1:1 대응, 누락 위험 0건.

### 종합 판정

- 구조: PASS (7/7 HIGH)
- 코드 정합: 9/9 VERIFIED
- 누락 위험: 0건
- 권고: HIGH/MED/LOW 모두 없음
- 결론: **승인 가능 (이슈 없음)**

## Round 3 — 2026-05-21 (reviewer: claude)

## ADR 리뷰 결과 (Round 3): 리스크 보완 후 재검토

### 변경 범위

| 파일                                | 변경                                                                   |
| ----------------------------------- | ---------------------------------------------------------------------- |
| 144-...md:189-202                   | R4 대응 강화 + G7 fail gate 승격                                       |
| design/144-...-breakdown.md:354-362 | Phase 5 Task 6 신규 — layoutVersion 명시 invalidation                  |
| design/144-...-breakdown.md:425-454 | Phase 8 blocking budget (p95 +25% / 60fps) + 1000+ scale family stress |
| design/144-...-breakdown.md:458-460 | Acceptance checklist 동기 갱신                                         |
| design/144-...-breakdown.md:476     | stale `adr912` → `adr144` 정정                                         |

### Round 2 피드백 대응 정합

3 위험 모두 직접 cover:

- R4 ADR-910 ordering 노출 → blocking perf gate (p95 ≤ +25% / 60fps) + family hold prerequisite
- layoutVersion 3-심볼 체인 누락 → Phase 5 Task 6 `LAYOUT_AFFECTING_PROP_KEYS` / `NON_LAYOUT_PROPS_UPDATE` / `INHERITED_LAYOUT_PROPS_UPDATE` 명시 점검
- Enterprise 1000+ stress → Phase 8 Task 3 `1000+ tabs/items` + `1000+ rows/cells` + nested slot-filled stress

### 코드 검증 (Phase 5 Task 6 신규 주장)

| 주장                                     | 판정     | 근거                                                          |
| ---------------------------------------- | -------- | ------------------------------------------------------------- |
| LAYOUT_AFFECTING_PROP_KEYS 심볼          | VERIFIED | apps/builder/src/builder/stores/utils/layoutInvalidation.ts:6 |
| NON_LAYOUT_PROPS_UPDATE 심볼             | VERIFIED | apps/builder/src/builder/stores/utils/elementUpdate.ts:42     |
| INHERITED_LAYOUT_PROPS_UPDATE 심볼       | VERIFIED | apps/builder/src/builder/stores/utils/elementUpdate.ts:96     |
| layoutVersion 증가 + dirty mark 메커니즘 | VERIFIED | inspectorActions.ts:586                                       |

부가: `.claude/rules/layout-engine.md` 의 `LAYOUT_AFFECTING_PROPS — 현재 코드 없음` 표기는 stale (`PROP_KEYS` 형태로 실재). ADR-144 scope 외 rules 정정 후보.

### G7 승격 평가

측정 only → fail gate (p95 ≤ +25%, 60fps 유지, family hold + ADR-910 선행). failure mode 가 측정 가능 + binary decision.

### 위험 평가

- R1 HIGH 변경 없음 (G3/G4 dependency 유효)
- R4 MED 유지 (심각도 동일) but mitigation 강도 상향 — 실효 노출 구간 축소
- 누락 위험 0건 (Round 2 3건 모두 cover)

### 종합 판정

- 구조: PASS (7/7 HIGH)
- 코드 정합: 4/4 VERIFIED
- 누락 위험: 0건
- 권고: HIGH/MED/LOW 모두 없음
- 결론: **승인 가능 (이슈 0건, 보완 적절)**

## Round 4 — 2026-05-21 (reviewer: claude)

## Round 4 — 2026-05-21 (reviewer: claude)

### Scope

breakdown 보강 `c418bd23d` commit (C3-a/b 데이터 source 3축 + Phase 2 task 5 composite template default child set + Phase 5 task 7 Inspector Properties UI integration) 에 대한 review. ADR 본문 자체는 Round 1-3 PASS 유지.

### 구조 검증

| 섹션                     | 판정    | 품질 | 인용                |
| ------------------------ | ------- | ---- | ------------------- |
| C3 보강 (C3-a/b)         | PASS    | MED  | `breakdown:144-159` |
| Phase 2 task 5           | PASS    | HIGH | `breakdown:295-299` |
| Phase 5 task 7           | PARTIAL | MED  | `breakdown:386-392` |
| Acceptance Checklist 3건 | PASS    | HIGH | `breakdown:490-492` |

### 코드 검증

| 주장                                                | 판정       | 근거                                                                       |
| --------------------------------------------------- | ---------- | -------------------------------------------------------------------------- |
| `useCollectionData({ datatableId \| dataBinding })` | VERIFIED   | `useCollectionData.tsx:220-228`                                            |
| `defaultProps.items` inline 박힌 데이터 9 family    | VERIFIED   | `listBox.ts:168-177`                                                       |
| `CompositionDocument.collections` root collection   | UNVERIFIED | `composition-document.types.ts:439-440` — IndexedDB store, root field 아님 |
| ADR-076 패턴 Phase 7 family expansion 후보 7 family | PARTIAL    | `registry.ts:38-60` — 이미 4 family land                                   |

### 위험 평가

- HIGH 1: `CompositionDocument.collections` root field 표현 (ADR-131 P8 framing revert 와 정합 안 함)
- MEDIUM 1: Phase 5 task 7 land 상태 미반영 (4 family 이미 land)
- LOW 2: file:line 부재 + Phase 2 task 5 ↔ Phase 5 task 7 cross-link 부재

### 권고

- HIGH: C3-a (1) / C3-b 의 root collection 표현을 'ADR-132 `collections` IndexedDB store' 로 정정
- MEDIUM: Phase 5 task 7 family expansion framing 을 4 family land + 5 family 미land + Tabs pattern mismatch 로 정정
- LOW: 구체 file:line 추가 + Phase 2 ↔ Phase 5 cross-link

### 결론

**수정 후 재리뷰** (HIGH 1건 정정 후 PASS 가능)

## Round 5 — 2026-05-21 (reviewer: codex)

## Round 5 — 2026-05-21 (reviewer: codex, external)

### Scope

Round 4 정정 (`7aa7d16b3`) 후 외부 codex review 가 발견한 3 MEDIUM 이슈. 모두 C3-a/b 의 runtime sink persistence + Canvas path + 3 input mode 관련.

### 코드 검증

| 인용 | 확인 |
| --- | --- |
| `data.types.ts:52-67` (DataTable + runtimeData) | VERIFIED — schema/mockData/runtimeData/useMockData |
| `data.types.ts:68` (runtimeData 메모리만) | VERIFIED — "메모리에만 존재, DB에 저장 안함" |
| `data.types.ts:205-215` (api_endpoints targetCollection) | VERIFIED |
| `dataActions.ts:645-660` (executeApiEndpoint sink) | VERIFIED — set({collections}) + canvas sync, DB update 없음 |
| `useCollectionData.ts:340-355` (Canvas direct proxy) | VERIFIED — isCanvasContext branch /api/proxy direct fetch |
| `ItemsManager.tsx:342` (props[itemsKey] read/write) | VERIFIED |

### 정정 방향

- C3-a (1): persisted IndexedDB 표현 유지
- C3-a (2): persisted config + Zustand runtime sink + Canvas direct proxy fallback 분리 명시. ADR-132 후속 영역 lock-in 인용 추가
- C3-a (3): element props[itemsKey] 직접 read/write, IndexedDB 미경유 명시
- C3-b: data SSOT 분해 (persisted source / runtime sink / RAC read entry point) + 3 input mode (external dataBinding / new resolved-tree local data / legacy props.items[] fallback) 명시
- Phase 5 task 7: 3 mode 별 Editor UI (dataBinding picker / slot UI / ItemsManager) 분기 + mode 감지 순서 명시
- Acceptance Checklist 3건 동시 갱신

### 결론

**수정 후 PASS** (3 MEDIUM 정정 동시 follow-up commit)
