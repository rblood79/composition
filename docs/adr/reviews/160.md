---
adr: 160
title: collection projection 행 텍스트 측정 SSOT 단일화
reviews:
  - round: 1
    ts: "2026-07-22T12:53:11.612Z"
    reviewer: claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: other
        summary: >-
          문제 진단(M1/M2/M3 세 곳 각자 측정)·SSOT 프레이밍이 코드 실측과 부분 불일치 — M1/M2 는 이미 동일 함수
          공유
        evidence: >-
          utils.ts:351,2475 / collectionVirtualization.ts:38,274 /
          skiaPrimitives.ts:515 / ADR:9-13,90,96
        root_cause: >-
          resolveListBoxItemRowHeightFromStyle 은 이미 utils.ts 단일 정의로
          M1(§1.55b-2)+M2(가상화 stride)가 공유. 실제 이중화는 layout-util(M1/M2) vs escape
          별도함수 measureSpecWrappedTextHeight(M3) 2개 소스. 'layout 으로 단일화'는 부정확 — 측정
          로직 SSOT 는 이미 존재, D 의 실제 기여는 escape(패키지 경계로 apps/builder 함수 직접 호출 불가)의
          재측정 제거. '측정 2→1'은 §1.55b-2 소비 전환 명시 없이는 미성립
        outcome: fixed
        addressed_in: round 2 (본문 Context/Decision/Risks/Gates + breakdown 정정)
      - id: m2
        severity: MEDIUM
        category: other
        summary: >-
          측정 주입 시점/주체 미확정 — scene projection 시점엔 layout 계산 폭 미정이라 wrap metric
          부정확 위험
        evidence: >-
          canvasSceneNode.ts:1433(width=calc/100%) /
          buildSpecNodeData.ts:1514(layout w→style.width, layout 이후) /
          ADR:67-68,90 / breakdown Phase 2
        root_cause: >-
          escape(packages/specs)는 apps/builder import 불가(경계
          specs←shared←builder) → metric 을 props 주입으로만 수신. breakdown Phase 2 는
          appendListBoxRowProjection(scene, layout 前)이 metric 계산·주입 지정하나, 정확한
          wrap 폭(px)은 layout 이 컨테이너 기준 계산해 buildSpecNodeData(layout 後)가
          style.width 로 주입. scene 시점 폭 미정이면 wrap metric 부정확. R3 가 '타이밍'을 MED 로
          언급하나 이 근본 제약 미해결 — 측정 주체를 buildSpecNodeData(layout 後)로 둘지 확정 필요
        outcome: fixed
        addressed_in: round 2 (본문 Context/Decision/Risks/Gates + breakdown 정정)
      - id: l3
        severity: LOW
        category: adr-structure-violation
        summary: Gate G1 'escape 자체 측정 호출 0건' 이 R1 fallback(부재 시 자체 측정)과 모순
        evidence: "ADR:121(G1 0건) vs ADR:111(R1 _slotMetrics 부재 시 자체 측정 fallback)"
        root_cause: >-
          BC fallback 유지하려면 escape 에 measureSpecWrappedTextHeight 가 남아야
          함(fallback 분기). G1 '0건' 은 fallback 을 부정. 'slotMetrics 존재 시 측정 skip' 또는
          'fallback 분기 외 0건' 으로 통과 조건 정정 필요
        outcome: fixed
        addressed_in: round 2 (본문 Context/Decision/Risks/Gates + breakdown 정정)
  - round: 2
    ts: "2026-07-22T13:00:46.236Z"
    reviewer: claude
    source: live
    issues: []
---

# ADR-160 Review Log

## Round 1 — 2026-07-22 (reviewer: claude)

## ADR 리뷰 결과: ADR-160 collection projection 행 텍스트 측정 SSOT 단일화

### 구조 검증

| 섹션                              | 판정    | 품질 | 인용                             |
| --------------------------------- | ------- | ---- | -------------------------------- |
| Context                           | PASS    | HIGH | ADR:21-26 (hard constraint 4)    |
| Alternatives (4개)                | PASS    | HIGH | ADR:35-73                        |
| 4축 위험 평가                     | PASS    | HIGH | ADR:39-43,49-53,59-63,69-73      |
| Risk Threshold Check              | PASS    | HIGH | ADR:77-84                        |
| Decision 위험수용+기각            | PASS    | HIGH | ADR:93-103                       |
| Risks (Decision 뒤/Gates 앞 ID표) | PASS    | MED  | ADR:107-115                      |
| Gates                             | PARTIAL | MED  | ADR:119-123 (G1 내부 모순 LOW-1) |

### 코드 검증

| 주장                                                        | 판정     | 근거                                                              |
| ----------------------------------------------------------- | -------- | ----------------------------------------------------------------- |
| M1/M2/M3 세 곳 각자 재측정                                  | PARTIAL  | M1/M2 이미 공유 (collectionVirtualization.ts:274 → utils.ts 함수) |
| resolveListBoxItemRowHeightFromStyle = layout util 단일정의 | VERIFIED | utils.ts:351                                                      |
| escape 별도 measureSpecWrappedTextHeight 재측정             | VERIFIED | skiaPrimitives.ts:515                                             |
| \_slots/\_projectedRowsContentHeight 주입 선례              | VERIFIED | canvasSceneNode.ts:1004,1074                                      |
| buildSpecNodeData width injection (layout w→style.width)    | VERIFIED | buildSpecNodeData.ts:1514                                         |
| layout(fullTreeLayout) props write 안 함                    | VERIFIED | grep 0건                                                          |
| ADR 참조 907/157/156/150                                    | VERIFIED | 각 파일 실재                                                      |

### 위험 평가

| 위험                           | 증거                                               | 심각도 | 상태                     |
| ------------------------------ | -------------------------------------------------- | ------ | ------------------------ |
| 측정 주입 시점 폭 미정 (MED-2) | canvasSceneNode.ts:1433, buildSpecNodeData.ts:1514 | MED    | R3 부분언급, 근본 미해결 |

### 종합 판정

- 구조: PASS (Gates PARTIAL — G1 모순)
- 코드 정합: 6/7 VERIFIED, 1 PARTIAL (문제 규모 과장)
- 이슈: MED 2 / LOW 1
- 결론: **수정 후 재리뷰** — 방향(escape 재측정 제거)은 유효하나 breakdown 에 측정 주체·타이밍·데이터 흐름 확정 + 본문 프레이밍(세 곳→2 소스, 측정 2→1 조건) 정정 필요. ADR 방향 자체는 재설계 불요.

## Round 2 — 2026-07-22 (reviewer: claude)

## ADR 리뷰 결과: ADR-160 (round 2 — round 1 정정 확인)

round 1 이슈 3건(MED 2 / LOW 1) 전부 정정 반영 확인. 재검 결과 신규 이슈 0건.

### round 1 이슈 해소 확인

| 이슈                                        | 정정 위치                                                                                                                                                            | 확인  |
| ------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- |
| MED-1 (문제 진단·SSOT 프레이밍 코드 불일치) | Context(2개 소스=layout-util M1/M2 ↔ escape M3, 패키지 경계 명시) + Decision(측정 로직 SSOT 기존, D=escape 재측정 제거) + 성능(2→1 에 M1·escape 소비 전환 조건 명시) | fixed |
| MED-2 (측정 주입 시점 폭 미정)              | 대안 D 설명 + R3 + breakdown Phase 2/§4 (측정 주체 = buildSpecNodeData, layout 이후 style.width 확정 시점; scene projection 시점 폭 미정 명시)                       | fixed |
| LOW-1 (G1 ↔ R1 fallback 모순)               | Gate G1 ('\_slotMetrics 존재 시 skip, measureSpecWrappedTextHeight 는 fallback 분기에만 잔존')                                                                       | fixed |

### 구조 재검

- 구조: PASS (Gates G1 모순 해소 → PARTIAL→PASS)
- 코드 정합: 문제 진단이 코드 실측(M1/M2 공유, escape 별도 함수)과 정합
- 측정 주체·타이밍·데이터 흐름(buildSpecNodeData 산출 → M1·escape 소비) breakdown 확정

### 종합 판정

- 이슈: 0건
- 결론: **승인 가능** — round 1 3건 전부 fixed, HIGH/CRITICAL 0. 전제 확정 종결 계약 성립. Phase 실행은 사용자 승인 후.
