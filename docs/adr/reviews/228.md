---
adr: 228
title: 팔레트 전 항목 reusable origin
reviews:
  - round: 1
    ts: "2026-09-20T15:32:30.510Z"
    reviewer: codex
    source: live
    issues:
      - id: h1
        severity: HIGH
        category: other
        summary: 기존 ref 생성 경로는 initialProps를 버려 Chart 종류별 팔레트 진입점이 동일 origin 기본값으로 합쳐진다
        evidence: >-
          docs/adr/228-palette-wide-reusable-origins.md:81;
          packages/shared/src/catalog/componentCatalog.ts:457-462;
          apps/builder/src/builder/panels/components/paletteItems.ts:304-313;
          apps/builder/src/builder/hooks/useElementCreator.ts:242-275,355-361
        root_cause: >-
          ref 분기는 props:{}만 저장한다. initialProps를 적용하는 것은 plain/complex 분기뿐이다.
          reusable 등록 시 creationVariants도 보존해야 한다.
        outcome: fixed
        addressed_in: 2026-09-21 design repair; round 2
      - id: m2
        severity: MEDIUM
        category: other
        summary: PropContract 기본값만으로 origin seed를 만들면 factory의 builder-local 기본값이 누락된다
        evidence: >-
          docs/adr/design/228-palette-wide-reusable-origins-breakdown.md:37-41;
          packages/shared/src/catalog/bindings/Button.binding.ts:56;
          apps/builder/src/types/builder/defaultPropsDerivation.ts:8-16,41-66
        root_cause: >-
          현행 factory 기본값은 catalog defaults와 FACTORY_LOCAL_DEFAULTS의 합성이다. 계약만 받는
          defaultPropsFrom(contract)는 기존 합성 함수와 동치가 아니다.
        outcome: fixed
        addressed_in: 2026-09-21 design repair; round 2
      - id: m3
        severity: MEDIUM
        category: evidence-missing
        summary: 제외 정책상 대상은 57종인데 G1/G3는 58 origin을 강제하고 전체 노드 수와 root 수도 혼동한다
        evidence: >-
          docs/adr/228-palette-wide-reusable-origins.md:79,108-110;
          docs/adr/design/228-palette-wide-reusable-origins-breakdown.md:54,88;
          apps/builder/src/builder/panels/components/paletteItems.ts:190-277;
          apps/builder/src/builder/components/iconbutton/iconButtonTemplateOrigins.ts:101-136
        root_cause: >-
          66개 type에서 primitive 8종과 별도 제외 IllustratedMessage 1종을 빼면 57종이다. 자식 구조형
          seed와 기존 origin을 무시한 고정 root/문서 증가량은 실제 gate와 맞지 않는다.
        outcome: fixed
        addressed_in: 2026-09-21 design repair; round 2
  - round: 2
    ts: "2026-09-20T15:43:24.672Z"
    reviewer: codex
    source: live
    issues: []
  - round: 3
    ts: "2026-09-21T01:21:41.043Z"
    reviewer: codex
    source: live
    issues:
      - id: h1
        severity: HIGH
        category: other
        summary: 선택된 Button ref 안에 새 Button ref가 중첩되어 생성 nesting 규칙이 우회된다
        evidence: >-
          apps/builder/src/builder/hooks/useElementCreator.ts:52;
          apps/builder/src/adapters/canonical/canonicalMutations.ts:242
        root_cause: >-
          preflight는 현재 페이지 elements에서 Components origin을 찾으므로 부모 타입이 ref로 남는다.
          canonical 삽입 guard도 새 자식의 element.type(ref)를 직접 판정하여 opaque 통과한다.
        outcome: fixed
        addressed_in: 2026-09-21 round 3 수리 — preflight 문서 유효 타입 · canonical guard 배치 유효 타입 (RED→GREEN 3+2) · live h1 PASS
      - id: h2
        severity: HIGH
        category: other
        summary: 재hydration ensurer가 사용자가 정렬한 origin 순서를 고정 seed 순서로 되돌린다
        evidence: >-
          apps/builder/src/builder/components/ensureTemplateOrigins.ts:68;
          apps/builder/src/builder/components/catalogOrigins.ts:305
        root_cause: >-
          기존 origin을 모두 strip한 뒤 type/family 고정 순서로 body 끝에 다시 붙인다. 편집 보존 테스트는
          실제 body 재정렬 없이 초기 순서와 비교한다.
        outcome: fixed
        addressed_in: 2026-09-21 round 3 수리 — ensureTemplateOrigins 제자리 보정 + 누락만 보충 (RED→GREEN) · live h2 reload PASS
      - id: m3
        severity: MEDIUM
        category: other
        summary: AI가 명시한 initialProps도 origin과 값이 같으면 override에서 제거된다
        evidence: >-
          apps/builder/src/builder/hooks/useElementCreator.ts:189;
          apps/builder/src/services/ai/tools/compositeCreation.ts:134;
          apps/builder/src/adapters/canonical/canonicalMutations.ts:984
        root_cause: >-
          팔레트에서 유래한 기본값과 명시적 AI 요청의 출처를 구별하지 않고 값의 동일성으로 제거한다. chartType만 명시
          보존하는 예외라 다른 명시 값은 origin 편집에 종속된다.
        outcome: deferred
        addressed_in: 2026-09-21 round 3 판정 — 가설 + 반증 1 (아래 §수리 검증), LOW deferred (canonical ref 소유권 모델 확장 = 별도 ADR)
      - id: m4
        severity: MEDIUM
        category: evidence-missing
        summary: "G2 PASS 근거는 V 전수 및 Canvas 픽셀, Chart 7종 왕복을 충족하지 않는다"
        evidence: >-
          docs/adr/completed/228-palette-wide-reusable-origins.md:109;
          apps/builder/scripts/adr228-instance-parity-live.mjs:325;
          apps/builder/scripts/adr228-instance-parity-live.mjs:351;
          apps/builder/scripts/adr228-instance-parity-live.mjs:381;
          apps/builder/scripts/adr228-reusable-origins-live.mjs:333
        root_cause: >-
          generic 50 type의 root/자손 rect와 DOM 캡처를 비교하며 Chart 생성은 bar 한 진입점이다. 두
          arm DOM 부재도 allowlist나 trigger 조작 없이 성공 처리한다. 51/51은 50타입+오류 검사이지 V63
          전수가 아니다.
        outcome: fixed
        addressed_in: 2026-09-21 round 3 — parity 하니스 V manifest 56 pair (Chart ×7) + Skia 캔버스 픽셀 leg 57/57 · origins live Chart 7 왕복 38/38
      - id: m5
        severity: MEDIUM
        category: evidence-missing
        summary: G4 완료 근거에 선언된 불리 조건의 스크롤·다중 선택 및 scene build 측정이 없다
        evidence: >-
          docs/adr/completed/228-palette-wide-reusable-origins.md:31;
          docs/adr/completed/228-palette-wide-reusable-origins.md:113;
          docs/adr/evidence/228-palette-wide-reusable-origins/g4b-button-refs-headed-1.log:3
        root_cause: >-
          headed A/B 로그의 classes는 select,edit,page-switch,zoom이며 요약은 callback
          gap p95 평균이다. 가시 집합 변경 스크롤+다중 선택 조건과 scene build p95의 직접 측정 또는 proxy
          타당성 근거를 제시하지 않았다.
        outcome: fixed
        addressed_in: 2026-09-21 round 3 — scene.build 직접 계측 + pan · multi-select-edit class 추가 · headed 3회 A/B (g4e) + 패널 닫은 대조 (g4g) + Skia bridge 선형 탐색 수리
---

# ADR-228 Review Log

## Round 1 — 2026-09-20 (reviewer: codex)

판정: **설계 보완 필요 — HIGH 1 / MEDIUM 2**. Proposed 유지. 리뷰 기준 HEAD `a7c85b70f`; 제품 코드 변경 없음.

### H1 — Chart 생성 preset이 ref 전환에서 사라진다

위치: `docs/adr/228-palette-wide-reusable-origins.md:81`.

현재 Chart는 canonical type 하나에 여러 생성 진입점을 둔다. `packages/shared/src/catalog/componentCatalog.ts:457-462`의 creationVariants가 chartType별 initialProps를 만들고 `apps/builder/src/builder/panels/components/paletteItems.ts:304-313`가 이를 UI 항목에 전달한다.

그런데 ADR이 그대로 재사용한다는 `useElementCreator.ts:242-275`의 ref 분기는 `props: {}`만 저장한다(:266). initialProps를 적용하는 코드는 plain 생성의 `composeCreationProps`(:355-361)와 complex 호출에만 있다. Chart를 reusable 등록하면 서로 다른 chart 메뉴를 골라도 모두 origin 기본 차트가 된다. reusable panel 메타를 옮길 때 creationVariants 자체가 빠져도 여러 진입점이 사라진다.

해결: reusable entry에도 creationVariants를 보존하고, 선택한 진입점의 initialProps만 instance override로 기록한다. origin 기본값 전체를 instance에 복사하면 origin 편집 전파를 막으므로 두 값의 소유권을 구분해야 한다. G1/G2에 Chart 생성 진입점 각각의 생성→저장→새로고침→origin 변경 시나리오를 넣는다. 직접 호출 시 initialProps를 전달하는 AI 경로도 같은 계약을 사용해야 한다.

### M2 — catalog default와 factory default는 동일하지 않다

위치: `docs/adr/design/228-palette-wide-reusable-origins-breakdown.md:37-41`.

`defaultPropsFrom(contract)`를 기존 factory와 같은 함수라고 적었으나, 실제 기본값은 `apps/builder/src/types/builder/defaultPropsDerivation.ts:8-16`의 catalog base + builder-local overlay다. 예를 들어 `Button.binding.ts:56`의 children에는 default가 없고 Button 텍스트는 `defaultPropsDerivation.ts:44`의 overlay에서 온다. Badge/Link/ToggleButton도 같은 패턴이다. contract만 읽으면 빈 origin이나 링크 초기값 차이를 만든다.

해결: 새 유사 함수를 정의하지 말고 실제 `getDefaultProps(type)`/기존 합성 진입점을 재사용한다고 명시한다. origin이 가진 root props와 기존 신규 plain 노드의 초기 유효 props를 비교한다. 자식 구조형도 기존 definition과 순수 seed 생성 경계를 확인한 뒤 재사용 대상을 적어야 한다.

### M3 — 58종/58노드 gate가 제외 정책과 맞지 않는다

위치: `docs/adr/228-palette-wide-reusable-origins.md:79,108-110`, breakdown :88.

현재 PALETTE_ORDER를 read-only로 집계하면 66 type이고, 명시한 primitive 8종에 IllustratedMessage까지 제외하면 **57 type**이다. Toast는 이 66에 없다. 따라서 현 정책을 지키는 구현은 origin 58을 요구하는 G1/G3를 통과할 수 없다. Chart의 여러 표시 항목과 underlying type도 구분해야 한다.

또한 자식 구조형을 origin으로 옮기면서 문서 증가량을 “origin 58 노드뿐”으로 제한할 수 없다. 기존 IconButton도 root에 Icon/Text 자식이 있으며(`iconButtonTemplateOrigins.ts:101-136`), 새 복합 origin은 더 많은 자식을 가진다. 기존 5 reusable 및 template origin은 이미 존재하므로 추가 root 수와 최종 root 수도 다르다.

해결: Phase 0의 확정 집합을 gate의 입력으로 삼아 eligible type 수, palette entry 수, 최종 origin root 수, 신규 root 수, descendant 포함 총노드·직렬화 byte 증가를 분리한다. 사용자 결정 ②③의 범위를 바꿀 필요는 없고 집계/게이트 정정으로 해결할 수 있다.

### 검증 범위와 한계

- VERIFIED: 66 type 및 제외 9 type → eligible 57 집계, Chart의 initialProps 전달/누락, factory-local defaults, 기존 origin subtree 구조.
- UNVERIFIED: 신규 ref 전수의 live 편집·drag·복제·Undo, Canvas/Preview 픽셀, 600요소 +1ms 성능 예산은 구현 후 측정 대상이다. 필드 목록의 정적 일치는 실제 편집 쓰기 경로의 동등성을 대신하지 않는다.
- Publish가 ref를 전혀 못 읽는다는 주장은 하지 않는다. `packages/shared/src/utils/export.utils.ts:297-313`에 origin lookup/props merge 경로가 있다. ADR-148 R9의 slot provider 미소비는 별도의 기존 한계로, 이 리뷰에서 새 회귀로 확정하지 않았다.
- ADR-148 → 228과 227/228의 직교 관계, 기존 plain 공존 방향 자체에는 이견 없다.

## Round 2 — 2026-09-20 (reviewer: codex)

수리 검증 판정: **기존 3건 fixed, pending 0 — 설계 보완 완료**. 사용자 요청에 따라 문서만 수정했다. Proposed 유지, 구현 G0~G4는 UNVERIFIED.

| 기존 이슈 | 반영 위치                                          | 확인 내용                                                                                                                                |
| --------- | -------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| H1        | breakdown §3.2, Phase 1/2/4, 본문 Decision 3/G1/G2 | creationVariants 보존, 명시 initialProps만 instance override, 공통 기본값 origin 상속, Chart 전수 생성/저장/reload/origin 변경/Undo 검증 |
| M2        | breakdown §3.1, Phase 1, 본문 Decision 2/R4/G1     | getDefaultProps/composeCreationProps와 builder-local overlay 재사용, 자식 구조형 root props+children definition 동치                     |
| M3        | breakdown §3.3, Phase 0~~4, 본문 Decision 1/G0~~G4 | 66−8−1=57 type, V entry/R root/신규 root/descendant/schema/shell/byte 집계 분리 및 재hydration Δ0                                        |

Props kind/값·기존 plain 공존 및 227과의 직교 관계를 유지했다. 초기 구현 예시의 존재하지 않는 범용 createDefaultChildren 가정 대신 현재 createSelectDefinition/createComboBoxDefinition 경계를 적었고, factory의 DB/store side effect를 hydration에 호출하지 않도록 명시했다. 운영 R1 HIGH는 G1/G2로 관리하며 설계 수리와 구현 gate를 구분했다.

## Round 3 — 2026-09-21 (reviewer: codex)

## 완료 검증 — 보완 필요 (HIGH 2 · MEDIUM 3)

검증 HEAD: `8015825b9`. 대상 구현 커밋 `25866715f`, `8015825b9` 및 현재 ADR·breakdown·하니스. 이전 round 1/2의 설계 수리 fixed는 유지하며, 이번 이슈는 구현 완료 검증에서 새로 발견한 것이다. 제품 코드와 Implemented 상태는 변경하지 않았다.

### h1 — 실제 팔레트 생성에서 Button 안 Button (HIGH)

headed Builder 1440×900, document.visibilityState=visible. 팔레트 Button 클릭 → 선택을 유지한 채 같은 Button을 다시 클릭했다. 첫 instance의 부모는 body였으나 둘째 instance의 parent_id는 첫 instance였다. 기존 live helper는 생성 전 매번 선택을 비워 이 조건을 검사하지 않는다.

- 재현 프로젝트: `53846155-089a-4130-849e-fd6c8e381789`.
- 첫 instance: `1964e818-c2a8-4dde-90b8-effd9447e8fa`; 둘째: `92fbc2d4-0571-4e22-a8e6-b26cf7d435d9`.
- 원본: `/private/tmp/adr228-completion-audit-probe/findings.json`, `second-button.png`.
- 원인: useElementCreator의 page-scoped origin lookup 실패 + canonical 신규 childType을 ref 그대로 판정.
- 수리 방향: canonical 문서 기반 유효 타입을 생성 preflight와 batch 신규 자식/조상 판정에 일관 적용. 실제 선택 유지 생성과 Card action routing도 인접 회귀 검증한다.

### h2 — origin 재정렬 보존 실패 (HIGH)

production `ensureReusableCompositeOrigins`로 빈 문서 seed → Components body.children 역순 정렬 → 같은 ensurer 재실행 진단 테스트가 실패했다. 60개 root 수는 같지만 순서가 바뀐다. 기대 첫 4개는 gridlist, gridlist-item-default, listbox, listbox-item-selected, 실제는 toolbar, form, iconbutton, inline-alert다.

이는 사용자 편집/배치 보존 G3 위반이다. 현재 catalogOrigins 보존 테스트와 live reload 검사는 사용자가 순서를 바꾼 상태를 만들지 않아 통과한다. 기존 origin의 현 위치/상대 순서를 유지하며 누락 origin만 보충해야 한다. helper의 기존 구현이지만 이번 generic origin 전수 확장이 이를 소비한다.

### m3 — 명시 initialProps와 파생 기본값 혼동 (MEDIUM)

`buildReusableInstanceProps({isDisabled:false,children:"Button"}, {isDisabled:false,children:"Button",size:"md"})`의 실제 반환은 `{}`이다. 명시 patch 보존 진단 assertion이 실패했다. AI compositeCreation도 이 함수를 그대로 호출한다. 따라서 초기 화면은 같지만 이후 origin이 바뀌면 해당 요청값을 보존할 patch가 없다. 이 후속 값은 merge 의미에서 도출한 영향이며 실제 AI 요청→DB→reload 전체 왕복을 추가 실행한 것은 아니다.

팔레트 Chart의 파생 기본값을 상속시키는 정책 자체가 문제가 아니다. 명시 요청과 파생 기본값을 분리하고, canonical diff에서도 해당 소유권을 보존해야 한다. chartType만 예외로 두는 현재 정책은 일반 명시 props 계약을 충족하지 못한다.

### m4 — G2 전수/픽셀/왕복 근거 부족 (MEDIUM)

현재 51/51은 generic 50 type + page error 검사다. E57/V63 전수가 아니다. Canvas에서는 raster pixel이 아닌 layout rect 열만 비교한다. Preview DOM은 45타입 캡처, 5타입은 둘 다 없음으로 통과한다. Chart live 생성은 bar 한 종류이고 7종 각각의 저장→reload→origin 변경→Undo/Redo를 검사하지 않는다.

V manifest 기반 전수, Canvas 실픽셀, overlay/trigger 실제 출력 상태, Chart7 왕복을 추가해야 한다. 현재 통과한 50타입 결과를 부정하는 것이 아니라 누락 영역은 UNVERIFIED이며 전체 G2 PASS를 뒷받침하지 못한다는 판정이다.

### m5 — G4 불리 조건/측정량 근거 부족 (MEDIUM)

문서 Q2는 가시 집합이 바뀌는 스크롤 + 다중 선택 + 편집을 요구한다. 완료 근거 headed 3회 로그는 `--classes select,edit,page-switch,zoom`만 실행했고 select driver는 external-props다. scene build p95와 callback gap/page-switch proxy의 대응 근거도 없다. 같은 조건의 ref0/ref100 arm에서 누락된 측정을 보충해야 한다.

zoom +4.5ms는 문서에서 별도 유보한 잔여 항목이므로 새 실패로 중복 집계하지 않았다. 이번 턴은 기존 성능 로그/코드를 검토했으며 성능 A/B 자체는 재실행하지 않았다.

### 이번 검증 결과와 한계

- 인접 Vitest 6파일 70/70 PASS: catalogOrigins, reusableInstanceProps, systemComponentsPage, componentRegistrationContract, entryUniverseContract, compositeCreation.
- 기존 `adr228-reusable-origins-live.mjs --headed` 재실행 24/24 PASS, page errors 0. 현재 HEAD 산출물: `/private/tmp/adr228-audit-current-live/findings.json`.
- 추가 진단 Vitest 2개는 위 h2/m3 조건에서 2/2 FAIL. 실패를 숨기지 않고 보완 근거로 기록한다.
- 추가 headed 팔레트 UI 검사에서 h1 실제 재현, page errors 0. 저장/새로고침까지의 중첩 재현은 별도 미검증.
- 기존 G2 51/51은 저장된 evidence 및 하니스 구현을 검토했으며 이번 턴 전수 재실행하지 않았다. Canvas pixel·전체 Chart·G4 추가 조건은 UNVERIFIED.
- 임시 진단 소스는 `/private/tmp/adr228-completion-audit-probe/`에 보관하고 저장소에서 제거했다. 테스트 프로젝트 2개는 진단 산출물로 남겼으며 사용자 프로젝트는 수정하지 않았다.
- 판정: 등록/seed/기본 생성·상속의 상당 부분은 확인되었으나 위 5건 때문에 누락 없는 구현 완료로 승인할 수 없다.

## Round 3 수리 검증 — 2026-09-21 (executor: claude)

수리 커밋 대상 HEAD `8015825b9` + 작업 트리. 판독 루프 종결 규칙 (`review-loop-closure.md` §1~§2) 에 따라 phase 당 판독 1 + 수리 검증 1 — 이 절이 그 수리 검증이며 HIGH 0 으로 실행자가 닫는다.

### h1 — fixed

- 원인 확정: (a) `useElementCreator.resolveNestedCreationParent` 의 `effectiveType` 이 page-scoped `elements` 만 봐서 Components 페이지 origin 을 못 찾아 "ref" 유지 → `resolveNestingAwareTarget` 이 opaque 통과. (b) `canonicalMutations.partitionMergeByNesting` 이 새 자식·배치 안 조상을 `element.type === "ref"` 그대로 판정.
- 수리: (a) `createCanonicalNestingIndex(doc)` + `findCanonicalNodeType` 로 문서에서 origin 타입 해석 (`resolveCreationParentForType` 을 export 해 테스트). (b) `effectiveBatchType` — 배치 요소의 `ref` 를 같은 배치 또는 문서 index 에서 origin 타입으로.
- RED→GREEN: `useElementCreator.test.ts` +2 (문서 조회 한 줄 원복 시 RED 확인) · `canonicalMutations.nestingGuard.test.ts` +3 (새 instance 아래 instance 거부 · 배치 안 조상 유효 타입 · frame 아래 통과, 수리 전 2 FAIL).
- live: `adr228-reusable-origins-live.mjs` h1 — Button instance 를 **선택한 채** 팔레트 Button 클릭 → 둘째 instance 의 parent = body (`round3-origins-live-findings.json`).

### h2 — fixed

- 원인 확정: `ensureTemplateOrigins` 가 origin 을 전부 떼어 seed 순서로 body 끝에 다시 붙였다 (ADR-148 helper — 228 이 generic 50 으로 소비 범위를 넓혀 사용자 재정렬이 매 hydration 되돌아감).
- 수리: Components body **안** 의 origin 은 제자리에서 보정본으로 치환 (frame 안 위치 · 사용자 순서 유지, 중복은 첫 것), body **밖** 의 origin 만 떼어 누락분과 함께 body 끝에 보충. 기존 계약 (`ensureTemplateOrigins.test.ts` 8 family — 밖에 있는 중복 origin 을 body 로 복구) 유지.
- RED→GREEN: `catalogOrigins.test.ts` +1 (역순 + frame 안 이동 + 1개 제거 → 순서·위치 보존 + 누락 1 보충 + 멱등; 수리 전 RED "expected [] to equal [component-gridlist]").
- live: h2 — `moveElementToSiblingEdge(component-badge, "front")` → IndexedDB `node:page-components-body` 마지막 child 확인 → reload → 순서 동일. (첫 root `component-listbox-item-default` 는 ListBoxItem ⊄ body 라 nesting guard 가 이동 자체를 거부 — 하니스는 Badge 로.)

### m3 — deferred (LOW)

- 가설: canonical ref instance 의 소유권 모델 (ADR-148 `diffRefPropsAgainstMaster`) 은 **값이 master 와 다른 키 = override** 다. Properties 편집은 legacy element 에 병합된 전체 props 를 실어 오므로 이 diff 가 override 를 뽑는 유일한 경로이고, 생성 시 "명시 키" 를 남겨도 첫 편집에서 같은 diff 가 지운다 (chartType 예외만 남는 이유). 따라서 "명시 요청 = origin 과 같아도 소유" 는 생성 함수 하나로 못 만들고 **ref 노드에 명시 키 집합 (예: `metadata.explicitProps`) 을 두고 diff 가 그 키를 보존**하는 모델 확장이 필요하다 — 결정 지점 (canonical 스키마) 이라 별도 ADR.
- 반증 1: 명시 값이 origin 과 같은 키 (`{isDisabled:false, children:"Button"}`) 로 만든 instance 는 override 0 — 렌더·저장·reload 결과는 명시하지 않은 instance 와 동일하고, origin 편집을 따라간다. 이는 미override prop 의 문서화된 instance 동작 (G2 「비override 상속」) 이지 깨진 렌더/저장이 아니다 → production 재현 시나리오 없음.
- 기록: ADR Consequences 에 「명시 initialProps = origin 유효값과 다른 키 (+chartType)」 로 계약 문구를 정확히 하고 후속 후보를 남긴다.

### m4 — fixed (근거 보강)

- `adr228-instance-parity-live.mjs` — V manifest 56 pair: generic 50 에서 Chart 를 팔레트 진입점 7 로 펼침 (chartType 별 `createChartInitialProps` → 팔레트와 같은 patch 규칙 · plain 은 origin props ⊕ patch). 한 arm 씩 같은 자리에 두고 **Skia 캔버스 실픽셀** (pixelmatch threshold 0, 상단 100px 크롬 제외) + layout rect 열 + Preview DOM 픽셀·computed digest. **57/57** (`round3-instance-parity-findings.json`). 손 seed 5 (ADR-148 template binding origin) 는 plain 대응이 없어 이 하니스 밖 — 헤더 주석에 명시.
- overlay 5 (Menu/FileTrigger/Modal/Popover/Tooltip) 는 Skia rect + Skia 픽셀 두 leg 로 판정, DOM 은 「둘 다 미출력」 이 대칭. 열린 상태의 DOM 출력 비교는 Preview 상호작용 (trigger) 축이라 이 ADR 범위 밖 — LOW.
- Chart 7 왕복: origins live 에 7 진입점 전부 생성 → origin width 320→440 → 7 instance 440 · chartType 7 보존 → Undo 320 · Redo 440 → IndexedDB persist → reload 후 chartType 7 보존. **38/38**.

### m5 — fixed (근거 보강) · 수리 1

- 계측: `PERF_LABEL.SCENE_BUILD` (`BuilderCanvas` 의 `buildCanonicalSceneModel` 을 `observe`) 를 하니스 표에 실었다 (p50/p95/max/n). Q2 class 추가: `pan` (가시 집합 변경 스크롤, 기존) · `multi-select-edit` (20개 `setSelectedElements` 유지한 채 5회/s 편집).
- 수리 1: 프로파일에서 ref arm 에만 `resolveCanonicalRefMaster`+`resolveReference` 1.9% — `StoreRenderBridge` 가 노드마다 `resolveCanonicalRefElement(element, elementsMap.values())` 로 master 를 **선형 탐색** (600 × 850). map 조회를 `knownMaster` 로 넘긴다 (`resolveCanonicalRefTree` 와 같은 수리). 동작 변경 0 (bridge/resolver 테스트 125 PASS).
- 결과 (headed 1440×900 · 50 Hz 세션 · 3회 평균 p95, plain → refs, `g4e-*`): pan gap 21.1→21.5 (+0.4) · render.frame 1.57→1.57 (0) / select gap 34.8→35.9 (+1.1, 20 ms cadence 양자화) / edit gap 161.5→163.4 (+1.9) · **scene.build 3.43→3.97 (+0.5)** · render.frame 4.0→4.6 (+0.6) / multi-select-edit gap 160.7→177.0 (+16.2) · **scene.build 4.0→5.2 (+1.2)** · render.frame 4.67→5.30 (+0.6) / page-switch gap 76.0→69.1 (−6.9) / zoom gap 41.2→43.5 (+2.3) · render.frame 14.7→14.7 (0).
- multi-select-edit +16 의 원인 분리: Navigator·Properties 를 **닫고** 같은 A/B (`g4g-*`, 2회) → gap 130.6→127.9 (−2.7), scene.build 3.1→5.6 (+2.5, n≈10 이라 p95≈max). 즉 +16 은 캔버스가 아니라 **DOM 패널 (Layers/Properties) 이 선택된 instance 20개를 그리는 비용**이고, 160 ms 짜리 편집 longtask 자체는 두 arm 공통 (React dev 빌드 · 228 밖).
- 판정: 캔버스 축 (render.frame · record.content) 은 전 class ≤ +0.7. **scene.build 만 +0.5~+2.5 ms** (600 instance 의 실체화 — instance 당 merge 3회: scene 층 ADR-228 merge · `resolveCanonicalRefTree` · Skia bridge) 로 게이트 문구 「+1 ms」 를 다중 선택 편집에서 넘는다. 게이트 표에 그대로 기록하고, 수리 후보 (세 merge 를 하나로 · 또는 resolved instance 의 identity 캐시 — zoom 유보 항목과 같은 가설) 를 후속으로 남긴다 (LOW — 사용자 체감 프레임은 render.frame 이 지배).
- 참고: 이번 세션은 디스플레이가 50 Hz (어제 G4 는 119 Hz) 라 callback gap 절대값은 어제와 비교 불가 — A/B 는 같은 세션 안에서만 읽는다.

### 종결

HIGH 2 fixed · MEDIUM 2 fixed (근거) · MEDIUM 1 LOW deferred. 회귀: builder 스위트 6837 PASS (기존 실패 2 — `g5LegacyFieldGrepGate` publish · `instanceActions.static` 삭제된 overlay 참조 — 이 작업 밖) · `pnpm type-check` PASS · live 38/38 + 57/57.
