---
adr: 139
title: 컴포넌트 등록·대칭 build-time gate
reviews:
  - round: 1
    ts: "2026-05-16T08:05:46.466Z"
    reviewer: claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          Context 레지스트리 표의 getDefaultProps 위치가 factories/constants.ts 로 오기 — 실제
          apps/builder/src/types/builder/unified.types.ts:2066. 리뷰 중 표를 6행으로 분리
          수정.
        evidence: >-
          docs/adr/139-component-registration-symmetry-gate.md Context 표 /
          unified.types.ts:2066 (export function getDefaultProps)
        root_cause: >-
          초안이 COMPLEX_COMPONENT_TAGS 와 getDefaultProps 를 한 행에 묶어 constants.ts 단일
          위치로 인용. getDefaultProps 는 별도 모듈에 위치.
        outcome: fixed
      - id: l2
        severity: LOW
        category: other
        summary: >-
          apps/builder 에 packages/specs 와 동명의 별도 TAG_SPEC_MAP 존재 — breakdown
          §2-2 레지스트리 inventory 에 별도 항목으로 포함 권장.
        evidence: >-
          packages/specs/src/runtime/tagToElement.ts:18 주석 ("apps/builder의
          TAG_SPEC_MAP과 유사하나, packages/specs의 내부 concern")
        root_cause: >-
          ADR 본문은 packages/specs TAG_SPEC_MAP 만 인용. 동명의 apps/builder 레지스트리는
          Phase 0 inventory 에서 포착될 예정이나 명시되지 않음.
        outcome: deferred
  - round: 2
    ts: "2026-05-16T08:38:49.289Z"
    reviewer: codex
    source: codex-cli
    issues:
      - id: h1
        severity: HIGH
        category: evidence-missing
        summary: >-
          apps/builder 별도 merged TAG_SPEC_MAP(BUILDER_ALIAS_MAP 8 alias +
          @composition/specs 정본)이 gate 대상 레지스트리 목록에서 누락 — Skia/Builder 소비 경로
          drift 미감지 가능
        evidence: >-
          apps/builder/src/builder/workspace/canvas/sprites/tagSpecMap.ts:31
          (export const TAG_SPEC_MAP) / ADR 본문 Context 표(packages/specs 쪽만 인용) /
          breakdown §2-2
        root_cause: >-
          ADR Context 표와 breakdown §2-2 inventory 가 packages/specs
          tagToElement.ts 의 BASE_TAG_SPEC_MAP/TAG_SPEC_MAP 만 열거. apps/builder 는
          BUILDER_ALIAS_MAP 을 병합한 별도 merged map 을
          getSpecForTag()/StoreRenderBridge 가 직접 소비하나 누락.
        outcome: fixed
      - id: h2
        severity: HIGH
        category: evidence-missing
        summary: >-
          Canonical 목록 SSOT 후보 (c) ComponentTag union 의 'build 시 enumerate 가능'
          전제가 부정확 — type alias union 은 런타임 값이 없어 Object.keys enumerate 불가
        evidence: >-
          packages/shared/src/types/composition-vocabulary.ts:22 (export type
          ComponentTag = ...) / breakdown §2-1:38
        root_cause: >-
          breakdown §2-1 이 (c) ComponentTag 를 잠정 권장하며 'build 시 enumerate 가능' 으로
          기술. 실제로는 ts-morph/TS compiler API 또는 spec 파일 glob 또는 신규 런타임
          COMPONENT_TAGS 배열 중 택해야 contract test matrix 구성 가능.
        outcome: fixed
      - id: m3
        severity: MEDIUM
        category: other
        summary: >-
          baseline ratchet 가 '감소 강제' 아닌 '갱신 권장 안내' 라 R1(baseline 정체) 대응이 실효 차단력
          부족
        evidence: >-
          ADR R1 대응 칸(append 금지 + 감소-전용 ratchet 선언) / breakdown §5 Phase 3:99 +
          Gate G3:103 (안내 출력만)
        root_cause: >-
          R1 은 ratchet 으로 현상 동결을 막겠다 선언하나 breakdown Phase 3 은 currentMissing <
          baseline 시 안내만 출력. baseline 파일 미갱신 상태로 통과 가능 → ratchet 미작동.
        outcome: fixed
      - id: m4
        severity: MEDIUM
        category: other
        summary: >-
          CSS registry 검증을 styles/index.css @import 줄 파싱으로만 정의 —
          generated/manual CSS 혼재 환경에서 false positive/negative 위험
        evidence: >-
          ADR Context 표 styles/index.css 행 / breakdown §2-2:56 /
          packages/shared/src/components/styles/index.css:5 (Leaf=generated,
          Container/Composite=manual 혼재)
        root_cause: >-
          index.css 는 spec generated CSS 와 수동 CSS 가 혼재. @import 존재 여부만으로는
          skipCSSGeneration/manual-only/generated-but-unmatched 케이스 구분 불가.
        outcome: fixed
      - id: m5
        severity: MEDIUM
        category: adr-structure-violation
        summary: ADR 본문 Gates 섹션이 'Gate 테이블 불요' 처리 — 실질 gate(G0~G3)는 breakdown 에만 존재
        evidence: ADR Gates 섹션('잔존 HIGH 위험 없음 — Gate 테이블 불요') / breakdown §3~5 G0~G3
        root_cause: >-
          adr-writing.md 상 '잔존 HIGH 위험 없음' 시 Gate 테이블 생략은 허용이나, 본문만 보면 구현 전 차단
          조건이 비어 보임. breakdown G0~G3 존재로 완전 누락은 아님.
        outcome: fixed
      - id: l6
        severity: LOW
        category: alternative-strawman
        summary: >-
          대안 C(ESLint) 기각 사유 '파일 단위 모델이라 cross-file 검사 부적합' 이 과도하게 단정적 — ESLint
          도 cross-file 검사가 불가능하지는 않음
        evidence: ADR 대안 C 위험 평가(기술 HIGH — cross-file 검사 부적합)
        root_cause: >-
          ESLint custom rule 도 파일시스템/타입 정보 접근으로 cross-file 검사 가능. '부적합' 보다 '이
          레포의 Record/Set/CSS import/runtime alias 까지 다루기엔 구현비가 Vitest contract
          보다 높다' 가 정확.
        outcome: fixed
---

# ADR-139 Review Log

## Round 1 — 2026-05-16 (reviewer: claude)

## ADR 리뷰 결과: ADR-139 컴포넌트 등록·대칭 build-time gate

### 구조 검증

| 섹션                    | 판정 | 품질 | 인용                                                                   |
| ----------------------- | ---- | ---- | ---------------------------------------------------------------------- |
| Context                 | PASS | HIGH | ADR:7-43 (hard constraint 3종 + 3-domain 분류 infrastructure)          |
| Alternatives (4개)      | PASS | HIGH | ADR:45-81 (A codegen / B contract test / C ESLint / D runtime warning) |
| 4축 위험 평가           | PASS | HIGH | 각 대안 기술/성능/유지보수/마이그레이션 4축 명시                       |
| Risk Threshold Check    | PASS | HIGH | ADR:83-92 (B만 HIGH 0, 루프 불필요)                                    |
| Decision 위험 수용 근거 | PASS | HIGH | ADR:94-112 (수용 근거 + A/C/D 기각 사유)                               |
| Gates                   | PASS | MED  | ADR:126-129 ("잔존 HIGH 위험 없음" 명시)                               |
| 구현 상세 분리          | PASS | HIGH | ADR:114 breakdown 링크                                                 |

### 코드 검증

| 주장                                         | 판정             | 근거                                                        |
| -------------------------------------------- | ---------------- | ----------------------------------------------------------- |
| rendererMap                                  | VERIFIED         | packages/shared/src/renderers/index.ts:19                   |
| BASE_TAG_SPEC_MAP/TAG_SPEC_MAP               | VERIFIED         | packages/specs/src/runtime/tagToElement.ts:129              |
| COMPLEX_COMPONENT_TAGS                       | VERIFIED         | apps/builder/src/builder/factories/constants.ts:13          |
| getDefaultProps 위치                         | UNVERIFIED→FIXED | constants.ts 아님 → unified.types.ts:2066, ADR 표 수정 완료 |
| ComponentFactory creators                    | VERIFIED         | apps/builder/src/builder/factories/ComponentFactory.ts      |
| rendererStyleContract 선례 (ADR-907 Layer C) | VERIFIED         | rendererStyleContract.test.ts:41,90                         |
| ssot-hierarchy §4-1 build-time 미완성        | VERIFIED         | .claude/rules/ssot-hierarchy.md:111                         |
| sweep 64/119 drift                           | VERIFIED         | sweep-2026-05-16.json (pass 55 / with_issues 64 / 119)      |

### 위험 평가

ADR 나열 R1(baseline 정체)/R2(예외 stale)/R3(gate 유지보수) 전부 기재, 증거 적정. 누락 HIGH 위험 0건. LOW 관찰: apps/builder 별도 TAG_SPEC_MAP — breakdown §2-2 inventory 에 포함 권장.

### 종합 판정

- 구조: PASS (7/7 섹션)
- 코드 정합: 8/8 VERIFIED (getDefaultProps 1건 리뷰 중 수정)
- 누락 위험: 0 HIGH (LOW 1)
- 권고: HIGH 없음 / MEDIUM 없음 (getDefaultProps 위치 오기 리뷰 중 수정 완료) / LOW — breakdown §2-2 에 apps/builder TAG_SPEC_MAP 별도 포함, R1 "장기 정체" 표현은 "감소 더딤" 이 더 정확
- 결론: 승인 가능

## Round 2 — 2026-05-16 (reviewer: codex)

## ADR-139 Codex 리뷰 결과 (round 2)

리뷰어: Codex CLI 0.130.0 (`codex exec`, read-only sandbox). 판정: **GO-WITH-FIXES** — 구조는 Risk-First 순서를 따르나 gate 대상 레지스트리 inventory 와 baseline ratchet 설계에 실효 차단력 결함.

### HIGH (2)

1. **apps/builder 별도 merged TAG_SPEC_MAP 누락** — `apps/builder/src/builder/workspace/canvas/sprites/tagSpecMap.ts:31` 이 `BUILDER_ALIAS_MAP`(8 alias) + `@composition/specs` 정본을 병합한 별도 merged map 을 export, `getSpecForTag()`/StoreRenderBridge/Skia 경로가 직접 소비. ADR Context 표·breakdown §2-2 는 packages/specs 쪽만 인용 → Phase 0 inventory 범위 누락. (claude round 1 의 LOW 관찰을 codex 가 HIGH 로 격상 — 두 리뷰어 수렴)
2. **ComponentTag enumerate 전제 부정확** — breakdown §2-1 이 canonical 목록 SSOT 후보 (c) `ComponentTag` 를 '타입 차원 SSOT, build 시 enumerate 가능' 으로 잠정 권장. 실제 `composition-vocabulary.ts:22` 의 `ComponentTag` 는 순수 type alias union → `Object.keys` enumerate 불가. 같은 파일 주석이 '실측 `*.spec.ts` 파일명 기준 118개' 라 후보 (b) glob 이 자연스러운 런타임-enumerable SSOT.

### MEDIUM (3)

1. **ratchet 실효성** — R1 대응은 'append 금지 + 감소-전용 ratchet' 선언이나 breakdown Phase 3/G3 는 `currentMissing < baseline` 시 '갱신 권장 안내' 만 출력 → baseline 미갱신 통과 가능. 최소 fail 또는 ratchet update command 강제 필요.
2. **CSS @import 파싱 한계** — index.css 는 generated/manual CSS 혼재(`index.css:5`). @import 존재 여부만으로는 skipCSSGeneration/manual-only 케이스 구분 불가 → false +/-.
3. **본문 Gates 위임** — ADR 본문 Gates 는 'Gate 테이블 불요', 실질 G0~G3 는 breakdown 에만. 템플릿상 허용이나 본문만 보면 차단 조건 공백으로 보임.

### LOW (1)

1. **대안 C 기각 사유 과단정** — ESLint cross-file 검사가 '불가능' 은 아님. '부적합' → '구현비가 Vitest contract 보다 높다' 가 정확.

### 코드 정합성

6개 인용 레지스트리(rendererMap / BASE_TAG_SPEC_MAP·TAG_SPEC_MAP / COMPLEX_COMPONENT_TAGS / getDefaultProps / ComponentFactory creators / styles/index.css) 전부 VERIFIED. 대안 B 선택 타당 — `rendererStyleContract.test.ts:41` 11 renderer matrix 선례 확인.

### claude 교차 확인

HIGH 2건 모두 `file:line` 직접 확인 완료 — codex 지적 정확. HIGH-1 은 round 1 claude 리뷰가 LOW(deferred)로 둔 항목을 codex 가 독립 격상 → 두 리뷰어 수렴이라 ADR/breakdown 본문 반영 권장.

### 결론

- 코드 정합: 6/6 VERIFIED
- HIGH 2 (둘 다 문서 정확성 — Proposed ADR 단계, 코드 결함 아님)
- 권고: HIGH-1/HIGH-2 → ADR Context 표 + breakdown §2-1/§2-2 정정. MEDIUM-1 → breakdown Phase 3 ratchet 을 fail 조건으로 강화. MEDIUM-2 → breakdown §2-2 에 CSS 검증 한계 명시. MEDIUM-3/LOW-1 → 선택 보강.
