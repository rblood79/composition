---
adr: 138
title: 컴포넌트 패널 복합 컴포넌트 reusable origin-instance 부착 — dynamic 검증 + 진입점/fork UX
reviews:
  - round: 1
    ts: '2026-05-15T18:24:23.880Z'
    reviewer: claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: other
        summary: >-
          breakdown §4 Card 시나리오가 slot:string[] schema 의미를 오독 — 추천 component-ID
          목록을 명명 영역(header/body/footer)으로 해석. 실제 영역별 override는
          descendants[CardHeader] 경로
        evidence: >-
          design/138-...-breakdown.md §4 vs
          packages/shared/src/types/composition-document.types.ts:263-272,
          apps/builder/src/builder/factories/definitions/LayoutComponents.ts:102-187
        root_cause: >-
          slot 필드(추천 삽입 component ID 배열)와 descendants[path] 키(자식 stable id 경로)
          혼동. Card 실제 하위는 CardPreview/CardHeader/CardContent/CardFooter
        outcome: pending
      - id: m2
        severity: MEDIUM
        category: other
        summary: >-
          breakdown §4 시나리오 매트릭스가 Tabs items[].id <-> TabPanel.customId 페어링 정합성을
          assert하지 않음 — pilot이 잡으려던 복합 컴포넌트 edge case가 미검증 통과
        evidence: >-
          design/138-...-breakdown.md §4 vs
          packages/specs/src/components/Tabs.spec.ts:38,
          apps/builder/src/builder/factories/definitions/LayoutComponents.ts:47-59
        root_cause: >-
          instance가 props.items에 탭 추가(shallow fork)하면 새 item에 페어링된 TabPanel 자식
          element가 없음. 사용자 framing(dynamic 검증)이 pilot으로 잡으려던 케이스
        outcome: pending
      - id: m3
        severity: MEDIUM
        category: other
        summary: >-
          hasItemsOverride 헬퍼의 타입 도메인(canonical RefNode vs legacy Element)과 배치
          모듈이 breakdown §3/§4/§5 간 불일치
        evidence: >-
          design/138-...-breakdown.md §3/§5 vs
          apps/builder/src/builder/stores/utils/instanceActions.ts:638-650
        root_cause: >-
          instanceActions.ts 기존 함수는 legacy Element 모델, breakdown §5는 canonical
          타입으로 헬퍼 선언
        outcome: pending
      - id: l4
        severity: LOW
        category: evidence-missing
        summary: >-
          breakdown §5 helper 시그니처의 CanonicalRefNode 타입명 미존재 (실제 RefNode) +
          deepEqual import 누락
        evidence: >-
          design/138-...-breakdown.md §5 vs
          packages/shared/src/types/composition-document.types.ts:324
        root_cause: canonical 타입명 오기 — RefNode가 정본
        outcome: pending
---
# ADR-138 Review Log

## Round 1 — 2026-05-15 (reviewer: claude)

## ADR 리뷰 결과: ADR-138 컴포넌트 패널 복합 컴포넌트 reusable origin-instance 부착

### 구조 검증

구조 6/6 PASS HIGH — Risk-First 템플릿 완전 준수. Context 측정 가능 hard constraint 5개(ADR:26-32), 대안 3개 4축 위험(ADR:56-72), Risk Threshold Check(ADR:74-82), Decision 기각 사유(ADR:94-97), Risks R1-R4(ADR:101-110), Gates G1-G4(ADR:112-121). adr-writing.md M3/M4 추정-실측 gap을 G4 게이트로 흡수 — fork 사유 아닌 절차 게이트로 올바르게 처리.

### 코드 검증

6/8 VERIFIED. instanceActions createInstance/detachInstance/resetInstanceOverrideField(638/695/846), toggleComponentOrigin(752), mergePropsWithStyleDeep/resolveCanonicalRefProps(instanceResolver.ts:38/71), canonical schema(RefNode/DescendantOverride/slot/reusable), items shallow override=배열 전체 교체(instanceResolver.ts:42 주석), ADR-066 Tabs.props.items 직렬화 — 전부 VERIFIED. CanonicalRefNode 타입명 UNVERIFIED(실제 RefNode), ComponentSemanticsSection line 110 PARTIAL.

### 누락 위험 3건 (MED)

1. Card slot 시나리오 오독 — slot:string[]은 추천 component-ID 목록(schema 주석), 명명 영역 아님. 영역별 override는 descendants[childId].
2. Tabs items <-> TabPanel 페어링 무검증 — 시나리오 매트릭스가 resolved items 개수만 assert. fork된 instance의 미페어링 탭 미검증.
3. hasItemsOverride 헬퍼 레이어 불일치 — canonical 타입 선언 vs legacy Element 모듈 배치.

### 종합 판정

구조 PASS(6/6 HIGH). 코드 정합 6/8 VERIFIED. 누락 위험 3 MED(전부 breakdown §4/§5 영역, ADR 본문 무결). 결론: ADR 본문 승인 가능 / breakdown §4·§5 MEDIUM 3건 Phase 0 진입 전 수정 필요. 재리뷰는 breakdown 한정.
