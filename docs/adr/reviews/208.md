---
adr: 208
title: radar/radial 제어 prop 과 차트 종류별 조건부 노출
reviews:
  - round: 1
    ts: '2026-09-08T14:08:33.190Z'
    reviewer: claude
    source: live
    issues:
      - id: h1
        severity: HIGH
        category: evidence-missing
        summary: >-
          Decision 의 load-bearing 사실이 거짓 — visibleWhen 이 live Properties 패널에 결선돼
          있지 않다 (인용한 CatalogInspectorFields 는 죽은 경로)
        evidence: >-
          packages/shared/src/catalog/resolvers/resolveEditContract.ts:58-79,335-349
          (ResolvedField 가 visibleWhen 미운반) ·
          apps/builder/src/builder/panels/properties/PropertiesPanel.tsx:138,240,261
          (live 경로) ·
          apps/builder/src/builder/panels/properties/hooks/useOwnerCollectionColumns.ts:26
          (CatalogInspectorFields = 레거시) · GenericPropertyEditor import 0건
        root_cause: >-
          인프라 존재를 소비 경로로 착각 (feedback-infra-exists-vs-wired-consumption-path).
          파일 단위 인용(CatalogInspectorFields.tsx:255-257)이 그 파일의 live 여부를 가렸다
        outcome: fixed
        addressed_in: eeb998d73
      - id: m2
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          조건 판정을 원시 props[key] 로 하면 기본값 미저장 노드에서 oneOf 가 전부 거짓이 되어 유효 필드가 통째로
          사라진다
        evidence: >-
          apps/builder/src/builder/panels/properties/generic/evaluateVisibility.ts:21,32
          (currentProps[key] 직독) ·
          packages/shared/src/catalog/resolvers/resolveEditContract.ts:343
          (currentValue = override ?? default) ·
          apps/builder/src/types/builder/unified.types.ts:2200
        root_cause: >-
          판정 입력의 기본값 해소 계약이 ADR 에 명시되지 않았다. pencil import·수기 canonical 문서는
          chartType 저장을 보장하지 않는다
        outcome: fixed
        addressed_in: eeb998d73
      - id: m3
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          G0 이 vacuous — breakdown §2 사실 표가 죽은 경로를 인용하고 있어 '사실과 일치' 판정이 결선 부재를
          통과시킨다
        evidence: >-
          docs/adr/208-radar-radial-grid-controls.md G0 행 (초판) · breakdown §2 초판
          4행이 CatalogInspectorFields/inspectorFields 인용
        root_cause: >-
          measurement-validity §2 패턴 #8 — 사실을 파일 단위로 적으면 그 파일의 live 여부라는 축이 측정에서
          빠진다
        outcome: fixed
        addressed_in: eeb998d73
      - id: l4
        severity: LOW
        category: other
        summary: >-
          VisibilityCondition 이 catalog(4필드)와 specs(notEquals/parentTag 포함) 두 곳에
          있고 ADR 은 하나인 것처럼 서술
        evidence: >-
          packages/shared/src/catalog/types.ts:184-189 (4필드) vs
          apps/builder/.../evaluateVisibility.ts:1 이 @composition/specs 에서
          import, 같은 파일 :28,43,47 이 notEquals/parentTag/parentTagNot 사용
        root_cause: >-
          binding 작성자가 보는 타입(catalog)과 평가기가 쓰는 타입(specs)이 구조적 부분집합 관계라 컴파일은
          통과하지만 문서는 하나로 읽힌다
        outcome: deferred
      - id: l5
        severity: LOW
        category: evidence-missing
        summary: 대안 B 의 외부 근거(Nivo gridShape 단일 enum)는 오프라인에서 확인 불가
        evidence: 'ADR:55 — 저장소 내 대조 대상 없음'
        root_cause: 외부 라이브러리 주장. 기각된 대안의 근거라 결정에 영향 없음
        outcome: rejected
    hate:
      assumption: 'visibleWhen 이 Chart 의 live 패널에서 동작한다 (ADR:15, ADR:58 초판)'
      root: >-
        그 배관은 live 패널에 닿지 않는다 — ResolvedField 가 visibleWhen 을 운반하지 않고,
        evaluateVisibility 호출부 2곳이 둘 다 죽은 경로다. 따라서 '선언만 추가' 라는 비용 추정 전체가 무너진다
      axis: 소비 경로 부재 (3-grep 미통과 — 타입·평가기는 존재하나 live caller 0)
      first_nail: >-
        grep -n visibleWhen resolveEditContract.ts → 결과 0 (정적, 초 단위). 확증은 Card
        요소 하나로 isSelectable=false 에서 isSelected 가 사라지는지 보기 (1분) vs 프로그램 6 phase
      verdict: 'issue:1 (HIGH, fixed in eeb998d73 — G2 ③ 으로 게이트화)'
    prism:
      lenses:
        - lens: 소비 경로/결선
          verdict: fail
          reason: 타입·평가기·선언은 있으나 live caller 가 0 이라 '이미 있는 메커니즘' 이라는 비용 추정이 틀렸다
        - lens: 측정 무결성
          verdict: fail
          reason: 'G0 이 파일 단위 사실과 대조해 결선 부재를 통과시킨다 (leakage #8)'
        - lens: 비용/성능
          verdict: pass
          reason: 번들·frame 예산이 되돌린 구현의 실측에서 왔고 한도 안
        - lens: 되돌림/rollout
          verdict: pass
          reason: 신규 필드 전부 선택적 + 기본값 무변경이라 revert 가 단일 방향
        - lens: SSOT 정합
          verdict: pass
          reason: 'D3 catalog 무변경, visibleWhen 은 D2/패널 표시 축이라 경계 교차 없음'
      convergence: 불일치 — 소비 경로·측정 무결성 fail vs 비용·되돌림·SSOT pass
      question: >-
        P1 의 범위는 '선언 추가' 인가 '결선 + 선언 추가' 인가? — 답이 결선이면 대안 C 기술축이 LOW→MED 이고 P1 이
        3파일 작업이 된다 (수리에서 후자로 확정)
---
# ADR-208 Review Log

## Round 1 — 2026-09-08 (reviewer: claude)

리뷰 본문은 세션 응답 참조 (구조 7/7 PASS · 코드 정합 14/16 VERIFIED · HIGH 1 + MED 2 + LOW 2, HIGH/MED 전부 eeb998d73 에서 수리).

핵심: 초판 Decision 의 load-bearing 사실 — "generic Inspector 가 이미 visibleWhen 을 평가한다" — 이 거짓이었다. 인용한 CatalogInspectorFields.tsx:256 은 주석이 스스로 "레거시 Inspector" 라 부르는 죽은 경로이고, live 는 PropertiesPanel → useEditContract → resolveEditContract → GenericFieldRenderer 인데 ResolvedField 가 visibleWhen 을 아예 운반하지 않는다. Card.binding.ts:110 의 선언도 현재 화면에서 무동작. 수리 후 대안 C 기술 위험 LOW→MED, R6/R7 신설, G0/G2 강화, P1 을 결선+소급으로 재정의.
