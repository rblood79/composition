---
adr: 168
title: Frame Preset 반응형 재구성 — 프리셋 카탈로그 · 반응형 계약 · 패널 재설계
reviews:
  - round: 1
    ts: '2026-07-26T05:47:48.820Z'
    reviewer: claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: ssot-violation
        summary: >-
          UNITLESS_PROPS 에 grid line longhand 4키 미등재 → publish @media 에서 숫자 값에
          px 부착으로 선언 무효화, DOM 은 auto-placement / Skia 는 정상 배치 = 배포 산출물 DOM↔Skia
          발산
        evidence: >-
          packages/shared/src/utils/responsiveCss.ts:45-56 (UNITLESS_PROPS 에
          gridColumn/gridRow shorthand 만 존재), :67-71 (formatCssValue 가 숫자에 px
          부착)
        root_cause: >-
          ADR-168 이 gridColumnStart/End·gridRowStart/End 를 eligible 로 만들면서
          @media emit 경로가 열리는데, UNITLESS_PROPS 는 shorthand 만 담고 있어 longhand 숫자
          authoring 이 1px 로 변환된다. gridSlot() 이 String(...) 으로 문자열화해 현행은 우연히 안전하나
          CSSProperties 는 string|number 를 허용하고 base inline(React auto-unit)은 숫자도
          정상 처리하므로 base↔override 비대칭. ADR-156 R6 의 overflow/overflowX 와 동형.
        outcome: fixed
        addressed_in: >-
          round 1 즉시 반영 — ADR Risks R7 신설 + Gate G6 조건 보강(숫자 authoring 유효 CSS +
          DOM↔Skia 배치 일치) + breakdown §3-5 신설 + 파일 인벤토리에 responsiveCss.ts
          추가(16→17)
      - id: m2
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          grid item placement 단독 변경은 full rebuild 를 발동시키지 못해 조용히 무반영 — 설계가 우연한
          결합(dashboard-widgets tablet 이 컨테이너 템플릿도 함께 변경)에 의존하는데 미문서화
        evidence: >-
          apps/builder/src/builder/workspace/canvas/layout/engines/fullTreeLayout.ts:503-524
          (GRID_REBUILD_TRIGGER_KEYS 가 컨테이너 키 20개만 포함), :2437 (검사가
          isGridDisplay(curDisplay) 게이트 안), :2433-2436 (updateStyleRaw 의 grid
          placement 캐시 무효화 실패 명시)
        root_cause: >-
          grid item 은 자신이 grid 컨테이너가 아니므로 isGridDisplay 게이트를 통과하지 못하고
          updateStyleRaw 증분 경로로 처리된다. ADR-168 설계에서 item placement 를 바꾸는 유일한 프리셋이
          마침 컨테이너 템플릿도 바꿔 살아나지만, 이 결합이 계약으로 고정돼 있지 않아 item placement 만 바꾸는 프리셋이
          추가되면 무반영된다.
        outcome: fixed
        addressed_in: >-
          round 1 즉시 반영 — ADR Risks R8 신설 + Gate G8 신설(정적 테스트로 계약 확증) +
          breakdown §5-2 에 'item placement override 는 컨테이너 템플릿 override 를 동반' 계약
          + Phase 5 검증 항목 추가
      - id: l3
        severity: LOW
        category: other
        summary: >-
          gridArea shorthand 를 eligible 집합에 포함하면 같은 BP 에서 longhand 와 동시 override
          시 emit source order 가 승자를 정해 shorthand 가 longhand 를 리셋
        evidence: >-
          packages/shared/src/utils/responsiveCss.ts:106 (Object.keys 순서 emit),
          :121 (모든 선언에 !important 부착 → 동일 특정도)
        root_cause: >-
          초안이 '대칭·future-proofing' 사유로 gridArea 를 집합에 포함했으나, 현행 설계에 shorthand
          override 실사용이 없어 도달 가능한 결함 경로만 여는 선택이었다.
        outcome: fixed
        addressed_in: >-
          round 1 즉시 반영 — gridArea 를 집합에서 제외(8키→7키), ADR Decision·breakdown §3-1
          에 제외 근거 명시, breakdown §9 미지원 경계에 재개 조건(shorthand 실사용 + 순서 계약) 등재.
          LAYOUT_STYLE_KEYS 도 4키→3키 정합화
      - id: l4
        severity: LOW
        category: evidence-missing
        summary: >-
          ADR:21 '정상 3개는 셋 다 flex column' 서술이 사실과 다름 — fullscreen 은
          flexDirection 미선언이라 기본 row
        evidence: >-
          apps/builder/src/builder/panels/properties/editors/LayoutPresetSelector/presetDefinitions.ts:78-81
          (containerStyle 이 display:flex + minHeight 만, flexDirection 없음)
        root_cause: >-
          결론(3개가 3 BP 정상)은 유효하나 근거 서술이 부정확. fullscreen 이 정상인 실제 이유는 flex column
          이라서가 아니라 슬롯이 1개라 주축 방향과 무관하기 때문.
        outcome: fixed
        addressed_in: >-
          round 1 즉시 반영 — ADR:21 을 'vertical-2/-3 는 flex column, fullscreen 은 슬롯
          1개라 방향 무관' 으로 정정
---
# ADR-168 Review Log

## Round 1 — 2026-07-26 (reviewer: claude)

## ADR 리뷰 결과: ADR-168 Frame Preset 반응형 재구성

> 자기 리뷰(작성자 = 리뷰어). 자기 승인 편향을 줄이기 위해 구조 판정보다 코드 반증(Phase 2)과 누락 위험 탐색(Phase 3)에 비중을 뒀다.

### 구조 검증

| 섹션 | 판정 | 품질 | 인용 |
| --- | --- | --- | --- |
| Context (hard constraint ≥1) | PASS | HIGH | `ADR:39-48` — HC1–HC6 6개, 측정 방법 병기 |
| Alternatives (2개+) | PASS | HIGH | `ADR:69-105` — A/B/C/D 4개 |
| 4축 위험 평가 | PASS | HIGH | `ADR:73-76,82-85,91-94,101-104` — 4대안 × 4축 전수, 누락 축 0 |
| Risk Threshold Check | PASS | HIGH | `ADR:107-116` |
| Decision 위험 수용 근거 | PASS | HIGH | `ADR:124-129` + `ADR:144-148` 기각 사유 표 |
| 구현 상세 분리 | PASS | HIGH | `ADR:122` 포인터만 |
| Risks (Decision 뒤 / Gates 앞) | PASS | HIGH | `ADR:167-178` (Decision `:118` 뒤, Gates `:180` 앞) |
| Gates | PASS | HIGH | `ADR:180-190`, 전부 실패 시 대안 병기 |

동적 seed: HIGH+ 코드 경로 인용 PASS · Generator 확장 여부 선언 PASS(`ADR:27`) · BC 수식화 PASS(`ADR:163`) · 추정 vs 실측 gap PASS(breakdown §8) · HIGH+ 분리 질문 N/A(채택안 HIGH+ 0건).

### 코드 검증

| 주장 | 판정 | 근거 |
| --- | --- | --- |
| publish 가 eligible 필터로 @media 생성 | VERIFIED | `responsiveCss.ts:106-121` |
| picker 가 eligible 전수 미순회 | VERIFIED | `ResponsiveSection.tsx:43-46` |
| usePresetApply 가 responsive 미기록 | VERIFIED | `usePresetApply.ts:408-411` |
| 슬롯이 body 직계 평면 | VERIFIED | `usePresetApply.ts:377` |
| LAYOUT_STYLE_KEYS 4키 미등재 | VERIFIED | `layoutCache.ts:49-127` |
| display 변경 → full rebuild | VERIFIED | `fullTreeLayout.ts:2429` |
| BP 전환이 엔진 재계산에 도달 | VERIFIED | `layoutCache.ts:346-364` → `fullTreeLayout.ts:2195`(DFS 전수 batch) → `:2429` |
| 원시 토큰 15곳 | VERIFIED | PresetPreview 10 + styles.css 5 |
| "정상 3개 모두 flex column" | UNVERIFIED(반증) | `presetDefinitions.ts:78-81` |
| minmax 비정상 실측 | PARTIAL | 2026-07-25 실측이나 fixture 미보존 |

### 위험 평가 — 누락 4건 (전부 round 1 내 반영)

| ID | 위험 | 심각도 | 반영 |
| --- | --- | --- | --- |
| M1 | UNITLESS_PROPS grid longhand 미등재 → publish DOM↔Skia 발산 | MED | R7 + G6 보강 + breakdown §3-5 |
| M2 | grid item placement 단독 변경이 full rebuild 미발동 | MED | R8 + G8 신설 + breakdown §5-2 계약 |
| M3 | gridArea shorthand emit 순서 미규정 | LOW | 집합에서 제외(8→7키) + §9 재개 조건 |
| L1 | ADR:21 사실 오류 | LOW | 서술 정정 |

### 종합 판정

- 구조: PASS (7/7 + 동적 seed 해당 항목 전부)
- 코드 정합: 8/10 VERIFIED, 1 UNVERIFIED(반증), 1 PARTIAL
- 누락 위험: 4건 발견 → **4건 전부 round 1 내 반영 완료**
- 권고: HIGH 없음 / MEDIUM 없음(반영 완료) / LOW 없음(반영 완료)
- 결론: **승인 가능** — 발견된 4건이 Decision·대안 비교·Threshold 판정을 바꾸지 않았고(전부 Phase 1·Gate 흡수 범위), 반영 후 잔존 이슈 0건. 잔존 HIGH 위험은 R1 1건으로 G2 가 관리한다.

### 잔여 관찰 (이슈 아님)

- `minmax` 비정상 실측이 2026-07-25 세션 기록에만 있고 재현 fixture 가 없다. ADR 은 이를 회피(트랙 명시 나열)로 처리했으므로 결정에 영향 없으나, 향후 엔진 grid 함수 표현을 도입하려면 fixture 부터 필요하다 — breakdown §9 에 재개 조건으로 등재돼 있다.
