---
adr: 196
title: command registry 의 agent 호출 표면 — metadata 분리 · allowlist · 승인 · 기록
reviews:
  - round: 1
    ts: "2026-08-28T00:58:35.000Z"
    reviewer: claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          HC5/R3 가 'history group 표면 유무' 를 Phase 0 미지수로 뒀지만 표면은 존재하며 동기 창 전용 —
          canvasActions 9 함수 전부 async 라 transaction 으로 못 묶는다. 실측 대상은 '액션별 entry
          수'
        evidence: >-
          apps/builder/src/builder/stores/history.ts:453-459,495-506 ·
          workspace/canvas/actions/canvasActions.ts:143-398
        root_cause: "Proposed 단계 코드 경로 확증 부재 (선차단 #1) — grep 1회로 답이 나오는 사실을 미지수로 남김"
        outcome: fixed
        addressed_in: round 1 (HC5 · R3 · G0 · G2 · breakdown §2/§3-4/§4 재작성)
      - id: m2
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          G1 parity oracle 이 자기 확인 — 'adapter 실행 결과 = 같은 canvasActions 를 직접 부른
          결과' 는 adapter 의 callee 를 oracle 로 쓴 tautology (measurement-validity §2
          #3/#4). 결과 parity 의 oracle 은 handler 경로여야 한다
        evidence: >-
          ADR-196 round 0 G1 'jsdom parity allowlist 전부 PASS' · breakdown §4
          Phase 1 · measurement-validity.md §2 #3/#4
        root_cause: Verifier = designer — 설계자가 adapter 정의를 oracle 로 재사용
        outcome: fixed
        addressed_in: >-
          round 1 (G1 = 정적 심볼 대조 + spy, G3 = 키보드 경로 store 상태 대조, Decision 5 · R1
          재작성)
      - id: m3
        severity: MEDIUM
        category: other
        summary: >-
          줌 adapter 대상이 `setZoom` 으로 적혀 있었으나 handler 경로는
          `zoomViewportAtContainerCenter` (useViewportSyncStore) —
          `canvasStore.setZoom` 은 소비자 12곳의 다른 store 라 split-brain 위험
        evidence: >-
          apps/builder/src/builder/hooks/useGlobalKeyboardShortcuts.ts:89-95 ·
          stores/canvasStore.ts:41
        root_cause: 이름이 같은 함수를 실측 없이 인용 (store 미러 split-brain 패턴)
        outcome: fixed
        addressed_in: >-
          round 1 (Context · Soft Constraint '심볼 그대로' 원칙 · 대안 A · breakdown
          §2/§3-3)
      - id: l4
        severity: LOW
        category: evidence-missing
        summary: >-
          `requestEditingSemanticsDetachConfirmation` 은 이미 `Promise<boolean>` —
          'Promise 화 필요/판정' 문구가 낡음
        evidence: >-
          apps/builder/src/builder/utils/editingSemanticsImpactConfirmation.ts:69-71
        root_cause: 시그니처 미확인
        outcome: fixed
        addressed_in: round 1
      - id: l5
        severity: LOW
        category: adr-structure-violation
        summary: >-
          등록 hook 인용 라인 `:293-352` 는 195 시점 값 — 현재 effect 317-382,
          `registerCommand` 368
        evidence: >-
          apps/builder/src/builder/hooks/useKeyboardShortcutsRegistry.ts:317,352,368,382
        root_cause: 선행 ADR 라인 번호 자동 승계
        outcome: fixed
        addressed_in: round 1
      - id: l6
        severity: LOW
        category: evidence-missing
        summary: >-
          z-order/패널 adapter 대상이 'Phase 0 확정' 으로 비어 있었으나 grep 으로 확정 가능 —
          `moveElementToSiblingEdge`,
          `activatePanelWorkspacePanelV3`+`setPanelWorkspaceLayout`
        evidence: "useGlobalKeyboardShortcuts.ts:335-342 · hooks/usePanelLayout.ts:59-63"
        root_cause: "선차단 #1"
        outcome: fixed
        addressed_in: round 1 (breakdown §3-3 표)
    hate:
      assumption: "adapter 가 handler 와 같은 결과를 낸다 (ADR:100 '의미 drift 구조적으로 0', ADR:103)"
      root: >-
        결과 동일성의 oracle 이 adapter 자신의 callee 였다 (round 0 G1) — 절반 (canvasActions)
        은 같은 함수라 vacuous, 나머지 절반 은 handler 부가 동작 (:335-342 targetId 결정) 이 정확히
        미검증 영역. 게다가 HC5 의 1 entry 는 transaction 이 async 액션을 못 묶어
        (history.ts:453-459) 액션 자체 사실에 달려 있다
      axis: "측정 leakage #3/#4 + load-bearing 사실 미확인"
      first_nail: >-
        Chrome MCP 에서 같은 문서에 키보드 경로 → agent 경로 순으로
        alignLeft·bringToFront·zoomIn·toggleNodes·duplicate 5건 실행해 store 상태 diff
        + history entry 수 diff (≈30분) — Phase 1~3 보다 싸다
      verdict: issue m1·m2 (fixed) → G0 entry 수 실측 + G3 handler 경로 oracle 로 커버
    prism:
      lenses:
        - lens: 정합성/SSOT (195 registry·키보드 무변경)
          verdict: pass
          reason: >-
            metadata 는 registry 밖 별도 표, adapter 는 handler 를 부르지 않음 — HC1 diff 0
            이 구조적
        - lens: 되돌림/rollout (undo 1 entry)
          verdict: unclear
          reason: >-
            runInTransaction 동기 창 전용 + async 액션 — 1 entry 는 액션별 실측 결과에 달려 있다 (G0
            로 이동, 결과 전까지 unclear)
        - lens: 측정 무결성 (parity oracle)
          verdict: pass
          reason: round 1 수정 후 oracle = handler 경로 live 대조 + 정적 심볼 대조; 자기 확인 테스트 제거
        - lens: 안전/적대 (agent 가 파괴를 숨김)
          verdict: pass
          reason: >-
            기본 거부 + external 금지 + 원소별 승인 + 정적 게이트 5조항 — 우회 경로가 export 표면 grep 으로
            잠김
        - lens: 비용/범위
          verdict: pass
          reason: "전부 additive, Phase 1 되돌리기 1 commit, 번들 +3KB 상한"
      convergence: "다른 이유로 일치 (unclear 1 = G0 실측 대기, 불일치 아님)"
      question: 없음 — Phase 0 entry 수 실측이 unclear 를 pass/fail 로 가른다
---

# ADR-196 Review Log

## Round 1 — 2026-08-28 (reviewer: claude)

## ADR 리뷰 결과: ADR-196 command registry 의 agent 호출 표면

### 구조 검증

| 섹션                                | 판정 | 품질 | 인용                        |
| ----------------------------------- | ---- | ---- | --------------------------- |
| Context (HC 6)                      | PASS | HIGH | ADR:24-31                   |
| Alternatives 4                      | PASS | HIGH | ADR:42-80                   |
| 4축 위험 평가                       | PASS | HIGH | ADR:46-50,56-60,66-70,76-80 |
| Risk Threshold Check                | PASS | HIGH | ADR:82-91                   |
| Decision 위험 수용·기각 사유        | PASS | HIGH | ADR:93-109                  |
| Risks (Decision 뒤/Gates 앞, R1~R7) | PASS | HIGH | ADR:113-125                 |
| Gates G0~G4 + Live Exercise 자리    | PASS | HIGH | ADR:127-139                 |
| 3-domain 명시                       | PASS | —    | ADR:11                      |

### 코드 검증 (핵심 주장)

| 주장                                                   | 판정                  | 근거                                                                                            |
| ------------------------------------------------------ | --------------------- | ----------------------------------------------------------------------------------------------- |
| registry entry = (id → handler), metadata 없음         | VERIFIED              | commandRegistry.ts:25-33,86                                                                     |
| handler 는 effect 클로저, registerCommand 는 effect 안 | VERIFIED (라인 정정)  | useKeyboardShortcutsRegistry.ts:317-382, :368                                                   |
| AI 도구 7종, 승인 0, delete 는 body 보호뿐             | VERIFIED              | services/ai/tools/index.ts:8-14 · deleteElement.ts:44-45 · confirm grep 0                       |
| canvasActions 액션 층 공유                             | VERIFIED (전부 async) | canvasActions.ts:143-398                                                                        |
| history group 표면 '유무 미지수'                       | UNVERIFIED → 정정     | history.ts:461,495-506,453-459 (존재, 동기 전용)                                                |
| 줌 adapter = setZoom                                   | UNVERIFIED → 정정     | useGlobalKeyboardShortcuts.ts:89-95 zoomViewportAtContainerCenter; canvasStore.ts:41 별도 store |
| detach 확인 흐름 Promise 필요                          | UNVERIFIED → 정정     | editingSemanticsImpactConfirmation.ts:69-71 이미 Promise<boolean>                               |
| StylesPanel 언마운트 = EmptyState                      | VERIFIED              | StylesPanel.tsx:168                                                                             |
| 패널 토글 store 경로                                   | VERIFIED              | usePanelLayout.ts:59-63 activatePanelWorkspacePanelV3 + setPanelWorkspaceLayout                 |
| z-order store 경로                                     | VERIFIED              | useGlobalKeyboardShortcuts.ts:335-342 moveElementToSiblingEdge                                  |
| 134 D11 MCP 도구 표면                                  | VERIFIED              | docs/adr/134-…:239                                                                              |

### 위험 평가

| 위험                                                    | 증거                                          | 심각도 | 상태                                             |
| ------------------------------------------------------- | --------------------------------------------- | ------ | ------------------------------------------------ |
| R1 adapter↔handler 부가 동작 drift                      | :335-342                                      | MED    | 기재 (round 1 구체화)                            |
| R3 history 1 entry — transaction 동기 전용 + async 액션 | history.ts:453-459 · canvasActions.ts:143-398 | MED    | 기재 (round 1 재작성)                            |
| split-brain 줌 store                                    | canvasStore.ts:41 (소비자 12)                 | MED    | **누락 → fixed** (Soft Constraint '심볼 그대로') |

### 근본 반론 (hate)

- load-bearing 가정: adapter = handler 결과 (ADR:100,103)
- root: round 0 의 parity oracle 이 adapter 의 callee (자기 확인, measurement-validity #3/#4) + HC5 1 entry 가 transaction 으로 성립 불가 (async)
- first nail: Chrome MCP 키보드→agent 5건 store/entry diff (≈30분)
- 판정: m1·m2 fixed → G0 entry 실측 + G3 handler 경로 oracle 커버

### 렌즈 대조 (prism)

정합성 pass · 되돌림 unclear (G0 실측 대기) · 측정 무결성 pass (수정 후) · 안전 pass · 비용 pass → 다른 이유로 일치, 해결 질문 없음.

### 종합 판정

- 구조: PASS (7/7 HIGH)
- 코드 정합: 8/11 VERIFIED, 3 UNVERIFIED → 본문 정정 (fixed)
- 누락 위험: 1건 (MED, fixed)
- 권고: HIGH 없음 · MEDIUM 3 (전부 fixed) · LOW 3 (전부 fixed)
- 결론: **승인 가능** (pending 0)
