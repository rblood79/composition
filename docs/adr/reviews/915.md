---
adr: 915
title: catalog binding.accepts prop parity 복원 (ADR-912 후속)
reviews:
  - round: 1
    ts: "2026-07-14T01:01:27.052Z"
    reviewer: claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: other
        summary: >-
          breakdown Phase 0/1 완료 마커 부재로 잔여 scope 과소 표기 — 실측 잔여는 P1.5-a/d 외에 P1-b
          전체(maxLength/minLength/pattern 0건)·P1-d 전체(defaultSelected 3 binding
          0건)·P1-a TextArea·P1-e/P1-g 다수·P0 0-2
        evidence: >-
          TextField.binding.ts maxLength grep 0 ;
          Checkbox/Switch/ToggleButton.binding.ts defaultSelected grep 0 ;
          breakdown:17-66 완료 마커 0개 vs :75-76 (P1.5-b ✅/P1.5-c ⛔ 만 존재)
        root_cause: 부분 이행 커밋(db2cf46bc) 후 진행 상태를 P1.5 섹션에만 역기입 — P0/P1 이행분·미이행분이 문서상 구분 불가
        outcome: fixed
      - id: m2
        severity: MEDIUM
        category: other
        summary: >-
          P1.5-a contextualHelp 는 dead-restoration 동형(삭제 spec render 소비 0)인데
          P1.5-c(icon)와 달리 잔여 scope 로 유지 — 설계 비대칭 + RAC 1.18.0 에 prop 부재(wiring
          전제 오류) + Skia 경로 설계 0줄 → scope 재판정 필요 (결정 지점 ④, 사용자 대상)
        evidence: >-
          react-aria-components dist contextualHelp grep 0건 (설치 1.18.0) ;
          breakdown:74 vs :79-91 (P1.5-c dead 판정 논리) ; 삭제 spec render 블록 소비 0
          (git show 91c2be0dd~1)
        root_cause: >-
          §9 감사가 '전멸' 검출 후 spec-era 렌더 소비 여부(dead 판정)를 P1.5-c 에만 적용 — 동일 기준을
          P1.5-a 에 미적용
        outcome: fixed
      - id: l3
        severity: LOW
        category: other
        summary: >-
          P1.5-d fillOffset/isFilled 는 전 src 0건 + 삭제 spec 소비 0건의 dead 인데 '일부
          live' 로 뭉뚱그림 — live 는 showValueLabel 1개(layout utils.ts:2585-2591 소비,
          DOM renderSlider 미forward) 한정
        evidence: >-
          utils.ts:2585-2591 ; SelectionRenderers.tsx:1486-1533 ;
          fillOffset/isFilled 전 src grep 0
        root_cause: live consumer grep 이 showValueLabel 결과를 행 전체로 일반화
        outcome: fixed
      - id: l4
        severity: LOW
        category: other
        summary: >-
          P1.5-b 완료 마커가 accepts 커밋(2f19e3b0e)만 인용 — renderer wiring
          커밋(0173de4a1, resolveInputHintProps) 누락
        evidence: "breakdown:75 ; git log 0173e4a1/ae47ee258 순서"
        root_cause: 마커 기입 시점이 wiring 커밋보다 선행
        outcome: fixed
      - id: l5
        severity: LOW
        category: other
        summary: >-
          Layer 0 리뷰 기록 부재 — 전제 확정은 경로 2(breakdown §1 lock-in + 사용자 confirm 기록)로
          성립하나 scope 재판정의 기준 기록이 없었음. 본 round 기록 생성으로 해소
        evidence: "docs/adr/reviews/915.md 부재(리뷰 시점) ; 915:5-7,90"
        root_cause: Accepted 승격(2026-06-25)이 review-adr Phase 4.5 영속화 미경유
        outcome: fixed
---

# ADR-915 Review Log

## Round 1 — 2026-07-14 (reviewer: claude)

## ADR-915 리뷰 round 1 (잔여 phase 성립성 + 실행분 문서 정합, 2026-07-14)

- 구조: 7/7 PASS (HC :30-34 / 대안 3+4축 :40-68 / Threshold :70-78 / 기각 :92-95 / 분리 :97 / Risks R1~R5 :99-109 / Gates G1~G4 :111-118, R4 HIGH↔G1 1:1)
- 코드 정합(실행분): P0 0-6 VERIFIED / P0 0-1 VERIFIED(단 ADR-912 커밋 46e0470e2 산물) / P0 0-2 미이행 / P1-a PARTIAL(TextArea 0건) / P1-b 미이행 / P1-c 미이행(G3 보류 정합) / P1-d 미이행 / P1-e PARTIAL / P1-f PARTIAL(TimeField min·max 제외 미기록) / P1-g PARTIAL / P1.5-b VERIFIED(accepts+renderer wiring)
- 잔여 성립성: P1.5-a 재판정 필요(dead-restoration 동형+RAC 1.18.0 prop 부재+Skia 설계 부재) / P1.5-d 조건부(showValueLabel 만 live)
- ADR-148 충돌: 없음 (accepts vs ref instance 분기 직교, 0-7 dedup 은 보류 가능 문서화)
- 신규 이슈: MEDIUM 2 (잔여 scope 과소 표기 / P1.5-a scope 재판정) + LOW 3
- 결론: 승인 유지(Accepted 지속) + 문서 정합 수정 필요 + P1.5-a scope 재판정 필요(사용자 결정 지점 ④)
- 주 검증: main 이 P1-b/P1-d 미이행 grep + RAC contextualHelp 0건 직접 재확인 완료
