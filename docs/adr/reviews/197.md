---
adr: 197
title: Builder chrome 상태 아이콘 morph — morphicons core vendoring + StateIcon 레지스트리
reviews:
  - round: 1
    ts: '2026-08-29T16:06:20.935Z'
    reviewer: claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          Gate G1/G2 에 측정 조건 (대조군·측정물·visibilityState·DPR) 부재 —
          measurement-validity §1 명시 의무
        evidence: 'docs/adr/197-builder-chrome-state-icon-morph.md:131-136 (수정 전)'
        root_cause: Gate 를 수치만으로 기술
        outcome: fixed
        addressed_in: round 1 in-session (ADR G0/G1/G2 셀 갱신)
      - id: l2
        severity: LOW
        category: other
        summary: >-
          사실 오류 — data-selected wash 를 --accent-subtle 로 기술 (실제
          ActionIconButton.css:29 color-mix), 라인 drift 4건 (SKILL.md:81→75,
          SkiaCanvas:626→627, actionIcons:37→34, lucideIcons:37→41)
        evidence: >-
          apps/builder/src/builder/components/ui/ActionIconButton.css:29;
          .claude/skills/component-design/SKILL.md:75
        root_cause: 초안 작성 시 grep 출력의 인접 라인 인용
        outcome: fixed
        addressed_in: round 1 in-session
      - id: l3
        severity: LOW
        category: other
        summary: >-
          누락 위험 — Prettier PostToolUse 가 vendoring 파일을 재포맷하므로 upstream 바이트 hash
          비교 절차가 무의미
        evidence: '.claude/hooks/auto-format.sh (ignore 로직 0건), .prettierignore 부재'
        root_cause: 갱신 절차를 hash 기준으로 기술
        outcome: fixed
        addressed_in: 'round 1 in-session (breakdown §3-5: 복사 → Prettier → 테스트)'
      - id: l4
        severity: LOW
        category: other
        summary: >-
          누락 위험 — React StrictMode 이중 mount 시 driver 2개 생성 가능성, 자체 binding 의
          cleanup 계약 테스트 부재
        evidence: >-
          scratchpad morphicons/src/react/index.tsx dead/destroy 계약; breakdown
          Phase 1 테스트 표 (수정 전)
        root_cause: upstream React binding 축소판 작성 시 lifecycle 테스트 누락
        outcome: fixed
        addressed_in: round 1 in-session (G1 + Phase 1 테스트 항목)
    hate:
      assumption: >-
        upstream core+dom 을 그대로 복사하면 composition strict tsconfig·eslint·Prettier
        에서 무수정 컴파일·통과한다 (ADR:54,57)
      root: 거짓이면 대안 B 의 기술 L·부분 patch 금지·통째 교체 절차가 전부 무너진다
      axis: load-bearing 사실 거짓
      first_nail: >-
        upstream 9 파일을 apps/builder/tsconfig.app.json 으로 tsc — 1분; 결과 오류 0
        (2026-08-30)
      verdict: G0 커버 (probe 결과 기록)
    prism:
      lenses:
        - lens: 정합성/SSOT
          verdict: pass
          reason: 'canvas·Preview 미접촉, getIconData 읽기 전용 (ADR:11)'
        - lens: 비용/성능
          verdict: pass
          reason: >-
            아이콘 데이터 이미 초기 chunk (specShapeConverter.ts:9) → Δ = core 7.1KB 상한,
            rAF singleton
        - lens: 측정 무결성
          verdict: pass
          reason: G1 대조군·G2 visibilityState/DPR 조건 명시 (수정 후; 수정 전 fail)
        - lens: 되돌림/rollout
          verdict: pass
          reason: '신규 모듈 + 삼항 복귀 롤백 (ADR:60), ContextualActionBar 마지막 순서'
        - lens: 접근성
          verdict: pass
          reason: >-
            upstream 기본 never (dom/index.ts:166) 를 wrapper user 로 override + 테스트
            고정
      convergence: 다른 이유로 일치
      question: 없음
  - round: 2
    ts: '2026-08-30T00:38:01.906Z'
    reviewer: claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          아이콘 정본이 2개로 늘고 판정 규칙이 명문화됐는데 ADR 이 세 번째 정본(ICON_STATE_PAIRS)의 경계·집행
          정합을 다루지 않음. 실측 겹침: propertyFieldIcons.ts:153,158,161 이 Lock/EyeOff/Eye
          를 필드 식별자로 사용
        evidence: >-
          apps/builder/src/builder/config/propertyFieldIcons.ts:153,158,161;
          apps/builder/src/builder/config/propertyFieldIcons.static.test.ts:70-131;
          .claude/rules/panel-structure.md:113-147
        root_cause: >-
          초안 기준선(2026-08-29) 이후 60a4dab37 이 필드 아이콘 정본을 신설하고 9f8b3089b 이 판정 기준을
          규칙으로 기재
        outcome: fixed
        addressed_in: >-
          round 2 in-session — ADR HC7 신설 + R8 추가 + G2/G3 조건, breakdown §3-5 정본
          경계 규칙 + §2-3 겹침 기록
      - id: m2
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          확장 후보 11건 중 5건(monitor/run/agent/online/filter)이 토글이 아님 — 탭 아이콘·1회 실행
          액션·중단 전용·서로 다른 요소·헤더 타이틀
        evidence: >-
          MonitorPanel.tsx:188(Realtime 탭); ApiEndpointEditor.tsx:821;
          AgentControls.tsx:24; ConnectionStatus.tsx:31,70;
          SelectionFilter.tsx:137
        root_cause: 초안이 '아이콘 심볼 + 상태 있음' 으로 후보를 뽑고 컨트롤 단위 확인을 Phase 3 으로 미룸
        outcome: fixed
        addressed_in: >-
          round 2 — breakdown §2-3 재판정표(유지 6 / 제외 5 + 근거), ADR
          Context·R5(LOW→MED)·G3, Phase 3 표 재구성
      - id: l3
        severity: LOW
        category: other
        summary: >-
          기준선 drift — 라인 6건(ItemsManager 72/216→79/231, AppearanceSection
          312→317, LayoutSection 481/516→483/518, MonitorPanel 175→188,
          BuilderHeader 308→312, dashboard 801→802), lucide import 100→108 파일,
          uiConstants 경로(builder/ 하위 아님), TypographySection 아이콘 범위
        evidence: >-
          apps/builder/src/builder/panels/properties/generic/ItemsManager.tsx:79,231;
          apps/builder/src/utils/ui/uiConstants.ts;
          apps/builder/src/builder/panels/styles/sections/TypographySection.tsx:257,384,391,419
        root_cause: 08-30 아이콘 감사·패널 표준화 commit 6개가 초안 이후 반영
        outcome: fixed
        addressed_in: round 2 — ADR Context 및 breakdown §2-1/§2-2 갱신
      - id: l4
        severity: LOW
        category: other
        summary: R7(ADR-192 동시 편집 충돌)과 '마지막 순서' 제약이 stale — ADR-192 는 completed
        evidence: docs/adr/completed/192-contextual-action-bar.md
        root_cause: 초안 시점에는 진행 중이었음
        outcome: fixed
        addressed_in: 'round 2 — R7 해소 표기, breakdown Phase 2 표 순서 제약 해제'
    hate:
      assumption: chrome 아이콘 선택이 자유롭고 새 정본 추가에 제약이 없다
      root: >-
        chrome 아이콘은 판정 규칙(panel-structure §아이콘) + static test 3개로 집행되는 영역이 됐고,
        필드 정본이 lock/eye 그림을 이미 점유한다
      axis: load-bearing 사실이 거짓
      first_nail: propertyFieldIcons.ts 를 Lock|Eye 로 grep 1회 (초 단위) — 3건 겹침 확인
      verdict: 'issue:r2-1'
    prism:
      lenses:
        - lens: 정합성 (아이콘 정본 경계)
          verdict: fail
          reason: 필드 정본이 lock/eye 를 정적 식별자로 이미 사용
        - lens: 기준선 신선도
          verdict: fail
          reason: 하루 만에 라인 6건·후보 5건·정본 1개 이동
        - lens: 사용자 의도 충족
          verdict: pass
          reason: 제외 5건은 애초에 형태 전환 지점이 아님 — 의도 손실 0
        - lens: 되돌림
          verdict: pass
          reason: '롤백은 삼항 복귀, 기존 정본 2개 무변경'
      convergence: 다른 이유로 일치
      question: 없음
  - round: 3
    ts: '2026-08-30T01:13:29.222Z'
    reviewer: claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          IconInput 계약 확장의 실패 경로 미규정 — upstream normalize.ts 가 미지원 태그를 throw 하고
          dom/index.ts 에 catch 가 없어 render 중 예외가 패널 트리를 죽인다
        evidence: >-
          morphicons@1.7.1 src/core/normalize.ts:302 (default: throw new
          Error(`morphicons: unsupported tag <${tag}>`)); src/dom/index.ts
          (throw/catch 0건, canonicalD:123 · createMorph:160 모두 전파)
        root_cause: >-
          resolveIconInput 이 배열을 '그대로 통과' 로만 규정 — 이름 경로의 null 처리에 대응하는 검증이 직접 전달
          경로에 없음
        outcome: fixed
        addressed_in: >-
          round 3 in-session — ADR R9 신설 + G1 조건, breakdown §3-1/§3-5(입력
          검증)/Phase 1 표·테스트
      - id: l2
        severity: LOW
        category: other
        summary: 계약 확장 반영 누락 — 대안 B 설명이 '이름 기반 MorphIcon' 으로 남아 HC4(IconInput) 와 불일치
        evidence: 'docs/adr/197-builder-chrome-state-icon-morph.md:55 (수정 전) vs :28'
        root_cause: HC4 만 갱신하고 Alternatives 본문 미갱신
        outcome: fixed
        addressed_in: round 3 in-session
      - id: l3
        severity: LOW
        category: other
        summary: Gates 도입문이 'MED 위험 (R1~R4)' 로 stale — round 2 에서 R5 상향·R8 신설로 MED 는 6건
        evidence: >-
          docs/adr/197-builder-chrome-state-icon-morph.md:131 (수정 전), :122 R5
          MED, :125 R8 MED
        root_cause: 위험 표만 갱신하고 Gates 도입문 미동기화
        outcome: fixed
        addressed_in: round 3 — 'R1~R5 · R8 · R9' 로 정정
      - id: l4
        severity: LOW
        category: other
        summary: >-
          vendoring 파일 인벤토리 오류 — upstream src/core 는 8 파일이고 index.ts 가 없는데
          breakdown 이 core/index.ts 를 나열하고 Phase 0 표가 (9) 로 기재
        evidence: >-
          morphicons src/core/ =
          parse·normalize·resample·plan·interpolate·serialize·spring·types 8 파일
          (ls); breakdown §3-1 · Phase 0 표 (수정 전)
        root_cause: src/index.ts(패키지 엔트리) 를 core 내부 index 로 오인
        outcome: fixed
        addressed_in: 'round 3 — core/* (8), index 없음 명기. ADR:53 ''8 + 1 파일'' 은 원래 정확'
      - id: l5
        severity: LOW
        category: other
        summary: '§3-4 custom 아이콘 예시의 원소가 실제와 불일치 (SquareOff 는 path 3개, line 없음)'
        evidence: 'apps/builder/src/builder/components/icons/SquareOff.tsx:39-41'
        root_cause: 예시를 실제 파일 확인 없이 작성
        outcome: fixed
        addressed_in: round 3 — 실제 path 데이터로 교체 + LayoutFreeform rect rx 지원 주석
    hate:
      assumption: icon 을 IconInput 으로 넓히면 아이콘 종류와 무관하게 같은 경로를 탄다
      root: >-
        core 는 지원 태그 밖 입력에 예외로 답하는데 (normalize.ts:302) driver 에 catch 가 없어 직접 전달
        경로에는 검증 지점이 없다 — <g> 하나로 render 가 죽는다
      axis: load-bearing 사실이 거짓 (계약이 total 이 아님)
      first_nail: >-
        rg 'throw' src/core/normalize.ts src/dom/index.ts 1회 — core throw /
        driver catch 0건
      verdict: 'issue:r3-1'
    prism:
      lenses:
        - lens: 계약 안전성 (total function)
          verdict: fail
          reason: 확장된 입력 집합의 실패 경로 미규정
        - lens: 정합성 (아이콘 정본 경계)
          verdict: pass
          reason: HC7 + §3-5 로 액션/필드/상태쌍 3축 분리 유지
        - lens: 비용·번들
          verdict: pass
          reason: '타입 1개 + 분기 1개, core 크기 무변'
        - lens: 되돌림
          verdict: pass
          reason: 직접 전달 미사용 시 round 2 상태와 동일
      convergence: 다른 이유로 일치
      question: 없음
---
# ADR-197 Review Log

## Round 1 — 2026-08-29 (reviewer: claude)

## ADR 리뷰 결과: ADR-197 Builder chrome 상태 아이콘 morph — morphicons core vendoring + StateIcon 레지스트리

### 구조 검증

| 섹션 | 판정 | 품질 | 인용 |
| --- | --- | --- | --- |
| Context (hard constraint) | PASS | HIGH | `ADR:23-31` HC 7개 (번들 Δ≤+10KB gz, reduced-motion 프레임 0, rAF singleton 등 측정 가능) · SSOT 위치 "해당 없음" 명시 `ADR:11` |
| Alternatives (2개+) | PASS | HIGH | `ADR:42-81` 대안 4개, 각각 외부 근거 (upstream binding suite / Radix·shadcn / Lordicon) |
| 4축 위험 평가 | PASS | HIGH | `ADR:46-50, 56-60, 66-70, 77-81` 4축 전부 |
| Risk Threshold Check | PASS | HIGH | `ADR:85-92` 표 + 루프 판정 (HIGH 0 대안 3개 → 추가 불필요) |
| Decision 위험 수용 근거 | PASS | HIGH | `ADR:98-103` 수용 근거 4 · `ADR:105-109` 기각 사유 3 · `ADR:111` breakdown 포인터 |
| Risks (Decision 뒤/Gates 앞, ID 표) | PASS | HIGH | `ADR:113-125` R1~R7 + "잔존 HIGH 위험 없음" |
| Gates | PASS (수정 후) | HIGH | `ADR:127-136` G0~G3 — 리뷰 전 측정 조건 부재 (이슈 #1) → 대조군·측정물·visibilityState·DPR 명시로 수정 |

adr-writing.md 선차단 seed: #1 HIGH+ 없음 (N/A) · #2 Generator 확장 아님 (N/A) · #3 사용자 문서 BC 0 (`ADR:60` 롤백 = 삼항 복귀) · #4/#5 N/A.

### 코드 검증 (핵심 주장만)

| 주장 | 판정 | 근거 |
| --- | --- | --- |
| 삼항 교체 11곳 인용 라인 | VERIFIED | `RuleRow.tsx:130` `expanded ? <ChevronDown/> : <ChevronRight/>` · `ItemsManager.tsx:72,216` · `ContextualActionBar.tsx:165` `const PinIcon = pinned ? PinOff : Pin` · `TransformSection.tsx:673` `<Lock` · `ResponsiveVisibilityEditor.tsx:142` `<Eye` |
| 고정 아이콘 후보 인용 라인 | VERIFIED | `AppearanceSection.tsx:312` `<SwatchIconToggleButton` · `AgentControls.tsx:24` `<Square` · `ConnectionStatus.tsx:70` `<Wifi` · `PanelToggleGroup.tsx:91` `<Icon` |
| data-selected 가 `--accent-subtle` wash | PARTIAL → 수정 | `ActionIconButton.css:29` 는 `color-mix(var(--fg) 10%)`, `--accent-subtle` 은 `SwatchIconButton.css:31` `[data-pressed]` — 본문 `ADR:18` 정정 (이슈 #2) |
| lucide-react `__iconNode` 메인 index 미노출 | VERIFIED | `node_modules/lucide-react/dist/esm/lucide-react.mjs` grep `__iconNode` 0건 |
| 아이콘 데이터가 이미 초기 chunk (Δ 는 core 만) | VERIFIED | `skia/specShapeConverter.ts:9` · `components/ui/SearchField.tsx:19-20` eager `getIconData` (lazy `import(` 0건) — HC1 에 추가 기재 |
| 외부 라이브러리 추가 금지 규칙 | VERIFIED (라인 정정) | `component-design/SKILL.md:75` (`:81` 은 빈 줄 → 정정) |
| Skia rAF 루프 · ActionIcon · getIconData 라인 | VERIFIED (라인 정정) | `SkiaCanvas.tsx:627` · `actionIcons.ts:34` · `lucideIcons.ts:41` (`:626/:37/:37` → 정정) |
| upstream: MIT 1.7.1 · WeakMap 캐시 · settle → canonicalD · reducedMotion 기본 never | VERIFIED | `morphicons/package.json:3,5` · `src/dom/index.ts:105,107` · `:211` · `:166` (R4 근거로 인용 추가) |
| 테스트 123 케이스 · core 6.60 KB / dom 7.12 KB gz | VERIFIED | `test/*.ts` `test(` 합 123 · `README.md:486-487` |
| `circle` 입력 지원 (LucideIconData.circles 무손실) | VERIFIED | `src/core/normalize.ts:279` `case "circle"` |
| `SquareOff` 가 lucide 인지 custom 인지 (breakdown Phase 0 항목) | VERIFIED → custom | `AppearanceSection.tsx:30` `import { SquareOff } from "../../../components/icons"` · `components/icons/index.ts` — breakdown §2-1/§2-3 확정 기재 |

### 위험 평가

| 위험 | 증거 | 심각도 | 상태 |
| --- | --- | --- | --- |
| R1 vendoring drift | upstream 활발 (`38d2a72` 2026-08-28) | MED | 기재됨 |
| R2 IconNode 참조 → plan 캐시 | `dom/index.ts:105,107` WeakMap 키 = IconNode 참조 | MED | 기재됨 |
| R3 정지 시 canonical 복귀 | `dom/index.ts:211` settle | MED | 기재됨 |
| R4 reduced-motion 기본 | `dom/index.ts:166` `?? "never"` — wrapper override 필수 | MED | 기재됨 (근거 인용 보강) |
| 툴체인 무수정 컴파일 (암묵 가정) | tsc probe: upstream core 8 + dom 1 을 `apps/builder/tsconfig.app.json` 으로 → 9 파일 · 오류 0 (2026-08-30) | — | 반증 시도 통과 → G0 에 기록 |
| Prettier 재포맷으로 upstream 바이트 hash 비교 무의미 | `.claude/hooks/auto-format.sh` ignore 로직 0건, `.prettierignore` 없음 | LOW | **누락** → breakdown §3-5 갱신 절차 정정 (이슈 #3) |
| React StrictMode 이중 mount 시 driver 2개 | 자체 binding 이 upstream cleanup 계약을 유지해야 함 (`src/react/index.tsx` `dead`/`destroy`) | LOW | **누락** → G1·Phase 1 테스트 추가 (이슈 #4) |
| 렌더링 파리티 / layoutVersion / 상태 동기화 / SSOT `@sync` | chrome 전용, canvas·Preview 미접촉 (`ADR:11`) | — | 해당 없음 |

### 근본 반론 (hate)

- **load-bearing 가정**: "upstream core+dom 을 그대로 복사하면 composition 툴체인 (strict tsconfig · eslint · Prettier hook) 에서 무수정으로 컴파일·통과한다" (`ADR:54` "그대로 복사", `ADR:57` 기술 L)
- **root**: 이것이 거짓이면 대안 B 의 "기술 L / 부분 patch 금지 / 디렉토리 통째 교체" 가 전부 무너진다 — 공격 축: load-bearing 사실 거짓
- **first nail**: upstream 9 파일을 `tsconfig.app.json` 으로 tsc — 비용 1분 vs 프로그램 Phase 4 / Gate 4. 실행 결과 오류 0 (`--listFilesOnly` morphicons 9). Prettier 재포맷은 바이트만 바꾸고 테스트로 동치 확인 (§3-5 정정)
- **판정**: first nail 통과 · G0 커버 (probe 결과를 G0 통과 조건에 기록)

### 렌즈 대조 (prism) — breakdown Phase 0~3 (≥3) 조건 충족

| 렌즈 (failure mode) | 판정 | load-bearing 이유 |
| --- | --- | --- |
| 정합성/SSOT | pass | canvas·Preview 미접촉, `getIconData` 읽기 전용 (`ADR:11`) |
| 비용/성능 | pass | 아이콘 데이터 이미 초기 chunk (`specShapeConverter.ts:9`) → Δ = core 7.1 KB 상한, rAF singleton (`dom/index.ts:77`) |
| 측정 무결성 | pass (수정 후) | G1 대조군 (같은 HEAD 전/후 build) · G2 visibilityState/DPR 조건 명시 — 수정 전 fail (이슈 #1) |
| 되돌림/rollout | pass | 신규 모듈 + 삼항 복귀 롤백 (`ADR:60`), ContextualActionBar 마지막 순서 (`ADR:38`) |
| 접근성 (사용자 적대) | pass | upstream 기본 `never` (`dom/index.ts:166`) 를 wrapper `user` 로 override + 테스트 고정 (G1) |

- **수렴**: 다른 이유로 일치 (측정 무결성은 수정 후 일치)
- **해결 질문 1**: 불일치 없음

### 종합 판정

- **구조**: PASS (7/7 HIGH — Gates 는 수정 후)
- **코드 정합**: 11/11 VERIFIED (라인 drift 4건·토큰명 1건 정정)
- **누락 위험**: 2건 (LOW 2) — 모두 본 round 에서 수정
- **권고**:
  - **HIGH**: 없음
  - **MEDIUM**: #1 Gate 측정 조건 (대조군·측정물·visibilityState·DPR) — fixed
  - **LOW**: #2 사실 오류 (`--accent-subtle` 토큰명 + 라인 drift 4) — fixed · #3 Prettier 재포맷 vs upstream hash — fixed (§3-5) · #4 StrictMode 이중 mount 테스트 — fixed (G1·Phase 1)
  - **결론**: **승인 가능** (round 1 이슈 4건 전부 fixed, pending 0)

## Round 2 — 2026-08-30 (reviewer: claude)

## ADR 리뷰 결과 (round 2 — 기준선 재검토): ADR-197

재검토 사유: 초안(2026-08-29) 이후 아이콘 전수조사 `9f8b3089b`(17건 교체) · Monitor 패널 재구성 `cb42ead69` · 속성 필드 아이콘 정본 신설 `60a4dab37` · 버튼 통일 `1227dd303` 이 반영돼 §2 기준선이 이동.

### 구조 검증

round 1 대비 섹션 구조 변경 없음 — Context/Alternatives/Threshold/Decision/Risks/Gates 7/7 PASS 유지. Risks 표에 R8 추가, Hard Constraint 8개(HC7 신설).

### 코드 검증 (재측정)

| 주장 | 판정 | 근거 |
| --- | --- | --- |
| 삼항 교체 11곳 존속 | VERIFIED (라인 3건 이동) | ItemsManager.tsx:79,231 · LayoutSection.tsx:483,518 · 나머지 9곳 라인 불변 |
| 상태 있음·고정 아이콘 12곳 | PARTIAL → 6곳 | MonitorPanel:188 탭 · ApiEndpointEditor:821 1회 실행 · AgentControls:24 중단 전용 · ConnectionStatus:31/70 별개 요소 · SelectionFilter:137 타이틀 |
| data-selected wash 인용 | VERIFIED | ActionIconButton.css:29 color-mix(var(--fg) 10%) · SwatchIconButton.css:31 --accent-subtle (버튼 통일 후에도 동일) |
| ActionIcon 타입 / getIconData | VERIFIED | actionIcons.ts:34 · lucideIcons.ts:41 |
| lucide-react 100 파일 | UNVERIFIED → 108 | rg -l 'from "lucide-react"' apps/builder/src |
| uiConstants 경로 | UNVERIFIED → 정정 | apps/builder/src/utils/ui/uiConstants.ts (builder/ 하위 아님) |
| SquareOff custom | VERIFIED | components/icons/SquareOff.tsx · AppearanceSection.tsx:30,317 |
| __iconNode 메인 index 미노출 | VERIFIED | lucide-react.mjs 0건 |

### 누락 위험 (신규 2건)

| 위험 | 증거 | 심각도 | 상태 |
| --- | --- | --- | --- |
| 아이콘 정본 3중화 — 같은 그림이 액션/필드/상태쌍에서 다른 뜻 | propertyFieldIcons.ts:153,158,161 (Lock/EyeOff/Eye) · panel-structure.md:113-147 | MED | 누락 → R8 로 추가 |
| 억지 짝 등록 (R5) 이 실제로 초안에 5건 존재 | §2-3 제외표 | MED | R5 LOW→MED 상향 |

### 근본 반론 (hate)

- load-bearing 가정: chrome 의 아이콘 선택이 자유롭고, ICON_STATE_PAIRS 를 새 정본으로 두는 데 제약이 없다 (ADR:31 prop 계약 호환만 기재)
- root: 08-16/08-30 사이에 chrome 아이콘은 **판정 규칙 + static test 로 집행되는 영역**이 됐다 (actionIcons.static / propertyFieldIcons.static / sectionHeaderIcon.static). 세 번째 정본을 그 위에 얹으면 같은 화면에서 그림 하나가 두 뜻을 갖는다 — 공격 축: load-bearing 사실이 거짓
- first nail: `rg 'Lock|Eye' config/propertyFieldIcons.ts` 1회 (초 단위) → 실제로 3건 겹침 확인 → 이슈화
- 판정: 이슈 r2-1 (MED, fixed — HC7 + R8 + G2/G3 조건)

### 렌즈 대조 (prism)

| 렌즈 | 판정 | load-bearing 이유 |
| --- | --- | --- |
| 정합성 (아이콘 정본 경계) | fail → fixed | 필드 정본이 lock/eye 를 이미 점유 |
| 기준선 신선도 | fail → fixed | 하루 만에 라인 6건·후보 5건·정본 1개 이동 |
| 사용자 의도 충족 | pass | 유지 6 + 삼항 11 로도 lock/eye/sun/pin/chevron 전부 포함 |
| 되돌림 | pass | 롤백 = 삼항 복귀, 정본 2개 무변경 |

- 수렴: 다른 이유로 일치 (설계는 유효, 기준선·경계 기재가 결손)
- 해결 질문: 없음

### 종합 판정

- 구조 PASS 7/7 · 코드 재측정 8건 중 6 VERIFIED · 2 정정
- 이슈: MED 2 · LOW 2 — 전부 in-session fixed
- 결론: **승인 유지** (Accepted). 착수 시 Phase 0 재grep 에 static 게이트 baseline 명령이 추가됨

## Round 3 — 2026-08-30 (reviewer: claude)

## ADR 리뷰 결과 (round 3 — IconInput 계약 확장 후): ADR-197

### 구조 검증

| 섹션 | 판정 | 품질 | 인용 |
| --- | --- | --- | --- |
| Context (HC 8개) | PASS | HIGH | ADR:23-32 — HC4 계약 확장, HC7 정본 경계 |
| Alternatives (4개, 4축) | PASS (문구 1건 stale → fixed) | HIGH | ADR:43-82, :55 |
| Risk Threshold Check | PASS | HIGH | ADR:86-93 |
| Decision | PASS | HIGH | ADR:97-110 |
| 구현 상세 분리 | PASS | HIGH | ADR:112 포인터 |
| Risks (ID 표) | PASS (R9 추가) | HIGH | ADR:117-126 |
| Gates | PASS (도입문 stale → fixed) | HIGH | ADR:133-138 |

### 코드 검증

| 주장 | 판정 | 근거 |
| --- | --- | --- |
| IconInput = IconNode \| string (upstream 계약) | VERIFIED | morphicons src/core/types.ts:13 |
| createMorph / canonicalD 가 dom/index.ts 에 있고 core 만 의존 | VERIFIED | src/dom/index.ts:7-13 import, :123 canonicalD, :160 createMorph — dom/controller.ts 는 binding 용이라 미필요 |
| core = 8 파일 (+dom 1) | VERIFIED → breakdown 정정 | ls src/core (index.ts 없음), ADR:53 '8 + 1' 은 정확 |
| custom 아이콘도 같은 경로 (HC4) | VERIFIED | SquareOff.tsx:39-41 path×3 · LayoutFreeform.tsx:37-39 rect rx=1, normalize.ts:294 rect → rectPath:221-243 이 rx/ry SVG 규칙대로 처리 |
| 미지원 태그 처리 | UNVERIFIED → R9 | normalize.ts:302 throw, dom/index.ts catch 0건 |
| 아이콘 데이터가 초기 chunk (HC1) | VERIFIED | specShapeConverter.ts:9 · SearchField.tsx:19 (모듈 최상위 getIconData("search")) |
| 외부 라이브러리 추가 금지 | VERIFIED | .claude/skills/component-design/SKILL.md:75 |
| Skia rAF 루프 | VERIFIED | workspace/canvas/skia/SkiaCanvas.tsx:627 requestAnimationFrame |

### 위험 평가

| 위험 | 증거 | 심각도 | 상태 |
| --- | --- | --- | --- |
| IconNode 직접 전달의 render-time throw | normalize.ts:302 / dom/index.ts | MED | **누락 → R9 추가** |
| R1~R8 | round 1·2 에서 확인 | MED/LOW | 기재됨 |

### 근본 반론 (hate)

- load-bearing 가정: `icon` 을 `IconInput` 으로 넓히면 아이콘 종류와 무관하게 같은 경로를 탄다 (ADR:28, :151)
- root: core 는 지원 태그 밖 입력에 **예외로 답한다** — 이름 경로는 `getIconData` 가 없으면 `null` 이라 안전하지만, 직접 전달 경로는 검증 지점이 없어 `<g>` 하나로 패널이 죽는다. 공격 축: load-bearing 사실이 거짓 (계약이 total 이 아님)
- first nail: `rg 'throw' src/core/normalize.ts src/dom/index.ts` 1회 (초 단위) — core 는 throw, driver 는 catch 0건 확인
- 판정: 이슈 r3-1 (MED, fixed — R9 + `resolveIconInput` 화이트리스트 + G1 케이스)

### 렌즈 대조 (prism)

| 렌즈 | 판정 | load-bearing 이유 |
| --- | --- | --- |
| 계약 안전성 (total function) | fail → fixed | 확장된 입력 집합의 실패 경로가 미규정 |
| 정합성 (아이콘 정본 경계) | pass | HC7 + §3-5 정본 경계로 3축 분리 유지 |
| 비용·번들 | pass | 확장은 타입 1개 + 분기 1개, core 크기 무변 |
| 되돌림 | pass | 직접 전달을 안 쓰면 이름 경로만 남아 round 2 상태와 동일 |

- 수렴: 다른 이유로 일치 (설계 유효, 확장의 실패 경로만 결손)
- 해결 질문: 없음

### 종합 판정

- 구조 PASS 7/7 · 코드 정합 7/8 VERIFIED (1건이 R9 로 전환)
- 이슈: MED 1 · LOW 4 — 전부 in-session fixed
- 결론: **승인 유지 (Accepted)**. 착수 시 Phase 1 이 `resolveIconInput` 검증 + G1 throw-free 케이스를 포함해야 한다
