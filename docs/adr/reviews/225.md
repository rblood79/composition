---
adr: 225
title: 재사용 레이아웃 어휘 정렬 — Frames 기능 표면을 Layouts로 통일
reviews:
  - round: 1
    ts: '2026-09-18T19:44:52.884Z'
    reviewer: claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: generator-extension-gap
        summary: >-
          HC6/G4 접힘 상태 승계 술어 (새 id 없음 + 구 id 있음 → 승계) 가 구 id 를 지우지 않으면 매 부팅
          재적용된다 — 사용자가 승계 후 펼친 섹션이 새로고침마다 다시 접힘. G4 의
          old-only/new-only/both/none 4-케이스는 이 순서 (승계 → 펼침 → reload) 를 못 잡는다
        evidence: >-
          apps/builder/src/builder/panels/styles/hooks/useSectionCollapse.ts:182-206
          (merge 가 stored.collapsedSections 를 그대로 재구성, 삭제 없음) · breakdown §4
          persisted state 승계
        root_cause: >-
          1회성 승계를 술어로만 정의하고 구 id 제거 (또는 defaultsApplied 류 마커) 를 계약에 넣지 않음.
          activeFocusSection 의 구 id 도 미언급
        outcome: fixed
        addressed_in: >-
          ADR-225 HC6/G4 + breakdown §4 — 구 id 제거·activeFocusSection
          치환·승계→펼침→reload test 계약
      - id: m2
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          G1 (ko/en 사용자 문구 Frame 0건) 은 grep 으로 통과하지만 PageLayoutSelector 의 6개 문구
          중 3개 (`No Frame` · `Using "…" frame` · `Select a reusable frame for
          this page`) 는 현재 i18n 경로가 없어 ko 사용자에게 영어로 남는다. `Using "{name}" layout`
          은 param 메시지라 formattedMessages 함수 등록이 필요
        evidence: >-
          apps/builder/src/builder/panels/properties/editors/PageLayoutSelector.tsx:55,106-107
          · apps/builder/src/i18n/labels.ts:353-357 (Frame/Apply Frame/Remove
          frame from this page/Remove Frame 4개만) · translations.ts:4035-4038
        root_cause: >-
          Phase 1 이 key rename 만 다루고 미번역 3건의 신규 key 추가 여부를 scope 로 명시하지 않음. G1
          증거는 ko-KR 부팅 probe (`_i18n-residue-probe.mjs` 패턴) 가 필요
        outcome: fixed
        addressed_in: >-
          ADR-225 G1 + breakdown §3 — 신규 static key 2개·formatted
          properties.usingLayout·ko-KR 부팅 probe
      - id: m3
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          기준선 정규식이 Phase 2 rename 표의 식별자 (`ReusableFrameLayoutSummary` ·
          `create/delete/update/selectReusableFrame` · `getNextFrameName` ·
          `useCanonicalFrameSelectionStore` · 파일명 `canonicalFrameStore`) 를 포함하지
          않는다 — 같은 정규식으로 G5 ratchet 을 만들면 rename 잔여가 있어도 통과한다. 확장 정규식 실측 49
          (prod 27) 로 1.5× 미만이라 scope 재확인 대상은 아님
        evidence: >-
          breakdown §2 기준선 정규식 vs §4 rename 표 · 재측정: 기준 46 (26/20) 재현, 확장 49 —
          추가 3 = adapters/canonical/index.ts (createReusableFrameNode, canonical
          보존) · BuilderCore.static.test.ts ·
          useCanvasElementSelectionHandlers.static.test.ts
        root_cause: >-
          기준선 정규식이 path/컴포넌트명 중심으로 작성되어 store/action 공개 API 이름이 빠짐. G0 동결 시 정규식을
          rename 표와 같은 집합으로 재동결해야 G5 가 실제 잔여를 잰다
        outcome: fixed
        addressed_in: >-
          ADR-225 Context/G0/G5 + breakdown §2 — 확장 정규식 49 (production 27, test
          22) authoritative 기준선
      - id: l4
        severity: LOW
        category: adr-structure-violation
        summary: >-
          3-Domain 귀속이 부정확 — Builder 패널 라벨/aria 와 `.frame-tree` 는 RAC 출력 DOM
          (D1) 도 catalog 시각 SSOT (D3) 도 아닌 Builder chrome 이라 3-domain 밖이다. 현재
          서술은 D1/D3 를 '변경' 한다고 읽혀 SSOT 경계 침범으로 오해될 수 있다
        evidence: >-
          docs/adr/225 §Context 'Domain과 SSOT 경계' ·
          .claude/rules/ssot-hierarchy.md §1 (D1 = RAC 출력 DOM, D3 =
          Canvas/Preview 대칭 시각)
        root_cause: >-
          adr-writing 의 domain 명시 의무를 '어느 domain 을 바꾸는가' 로 읽음. 정확한 진술은 'Builder
          chrome — D1/D2/D3 변경 0, canonical FrameNode 는 D2 밖 저장 계약'
        outcome: fixed
        addressed_in: >-
          ADR-225 Context Domain과 SSOT 경계 — Builder chrome은 3-domain 밖, D1/D2/D3
          변경 0으로 정정
      - id: l5
        severity: LOW
        category: evidence-missing
        summary: >-
          allowlist 범주가 `docs/adr/design/*` (완료 ADR breakdown 25+ 파일) ·
          `.claude/rules/state-management.md:79` (상시 로드 규칙, FramesTab 언급) ·
          `docs/explanation/research/BUILDER_PERF_BASELINE_2026-09.md` ·
          `apps/builder/scripts/.tmp-panel-cap/*` 를 열거하지 않는다. history 범주에
          design/ 를 넣고 현행 규칙·research 문서는 Phase 3 갱신 대상으로 분류해야 G5 '과거 기록 무관 변경
          0' 판정이 가능
        evidence: >-
          grep 실측 (apps/builder/src 밖): docs/adr/design 25 파일 ·
          .claude/rules/state-management.md · BUILDER_PERF_BASELINE_2026-09.md
        root_cause: Phase 0 변경 금지 목록이 completed/evidence 두 디렉터리만 열거
        outcome: fixed
        addressed_in: >-
          ADR-225 HC7/G5 + breakdown §1/§2/§5 — pre-225 design history, 현행
          rule/research 갱신, local probe 제외
    hate:
      assumption: >-
        제품 facade 와 canonical Frame 의 경계를 '단어' 가 아닌 '소유' 로 판정하면 rename 이 저장·렌더
        계약을 건드리지 않는다
      root: >-
        projection hook (useCanonicalReusableFrameLayouts) 는 Canvas/iframe
        messenger/BuilderCore 가 소비하는 cross-cutting 읽기라 'Navigator feature 소유' 가
        아니다 — 경계가 흐리면 rename 범위가 Canvas 로 번진다
      axis: 소유 경계 판정의 일관성
      first_nail: wire payload 키 (`layouts`) 와 소비처 6곳 grep — 전부 이름만 바뀌고 값·키 불변이면 계약 무변경
      verdict: 'G3 커버 (payload 키 이미 layouts, 렌더 결과 불변 확인 가능)'
  - round: 2
    ts: '2026-09-19T02:55:29.391Z'
    reviewer: codex
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          G5 ratchet가 generic UI-owned comment 어휘를 검사하지 않아 현행 소스의 Frames
          tab/Navigator Frames/Frame Preset 7건이 남아 있어도 green이다
        evidence: >-
          docs/adr/completed/225-reusable-layout-vocabulary-alignment.md:199;
          apps/builder/src/builder/panels/navigator/__tests__/adr225VocabularyRatchet.static.test.ts:18-31,45-56;
          NavigatorPanel.tsx:103; panel-system.css:64; PropertiesPanel.tsx:978;
          useElementHoverInteraction.ts:58,495; BuilderCanvas.tsx:419;
          workflowEdges.ts:378; DataTableCreator.css:46
        root_cause: >-
          authoritative regex를 rename symbol/path 중심으로 고정해 기능 소유 일반 문구와 주석 패턴을
          inventory 및 ratchet에 포함하지 않았다
        outcome: pending
      - id: m2
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          G0의 49-file 분류 정본이 gitignored 로컬 파일뿐이라 clean checkout에서 ADR/CHANGELOG
          링크와 분류 근거를 재현할 수 없다
        evidence: >-
          .gitignore:82-88;
          docs/adr/completed/225-reusable-layout-vocabulary-alignment.md:9-10,194;
          docs/CHANGELOG.md:25; git ls-files
          docs/adr/evidence/225-phase0-vocabulary-inventory.md = empty
        root_cause: docs/adr/evidence 전체가 커밋 금지인데 구현 커밋과 문서는 해당 로컬 파일을 영속 evidence처럼 참조했다
        outcome: pending
      - id: m3
        severity: MEDIUM
        category: adr-structure-violation
        summary: >-
          Phase 4 foreground 계획의 이름 변경·내부 tree 상호작용·split
          resize·Undo·Canvas/Preview geometry/slot 확인이 live harness에 없고 console
          warning도 수집하지 않아 G0~G6 전부 PASS 및 Implemented 주장이 과도하다
        evidence: >-
          docs/adr/design/225-reusable-layout-vocabulary-alignment-breakdown.md:218-233;
          apps/builder/scripts/adr225-layouts-live.mjs:127-132,179-303,354-357;
          docs/adr/completed/225-reusable-layout-vocabulary-alignment.md:200,204-228;
          LayoutList.tsx:39-50,150-195; reusableLayoutActions.ts:233-240 (update
          API production caller 0)
        root_cause: >-
          실행 harness가 좁은 생성→적용→해제→삭제 흐름으로 축소됐지만 breakdown/checklist를 그 범위에 맞게
          정정하지 않았고, 실행 후 Undo만 사후 제외했다
        outcome: pending
    hate:
      assumption: rename symbol/path 정규식이 재사용 레이아웃 기능 소유 어휘 전체를 대표한다
      root: generic UI 주석은 symbol 정규식 밖이므로 현재 7건이 남아도 gate가 green이다
      axis: gate completeness
      first_nail: >-
        rg '\bFrames tab\b|Navigator (Pages/)?Frames|Frame Preset|Pages/Frames'
        apps/builder/src
      verdict: 'issue:m1'
    prism:
      lenses:
        - lens: runtime behavior
          verdict: pass
          reason: 현재 HEAD의 핵심 생성·적용·해제·삭제·refresh와 ko-KR 흐름 13/13 PASS
        - lens: gate integrity
          verdict: fail
          reason: G5 구 UI 주석 7건을 놓치고도 focused gate가 PASS
        - lens: reproducibility
          verdict: fail
          reason: G0 분류 evidence가 gitignored라 clean checkout에서 링크와 49-file 분류가 사라짐
        - lens: status closure
          verdict: fail
          reason: breakdown의 foreground 범위와 실제 live harness가 불일치
      convergence: 다른 이유로 불일치
      question: m1~m3를 수리·재검증한 뒤 현재 Implemented 상태를 유지할 것인가
---

# ADR-225 Review Log

## Round 1 — 2026-09-18 (reviewer: claude)

## ADR-225 리뷰 round 1 (2026-09-19)

**판정**: 구조·전제·대안·gate 는 규칙 정합. CRITICAL/HIGH 0. MEDIUM 3 은 본문/breakdown 문장 보강으로 닫히는 항목 — 수정 후 Accepted 가능.

### 검증한 것

- 기준선 46 (prod 26 / test 20) 정규식 재실행으로 **재현**. 확장 정규식 (rename 표 식별자 포함) 49 — 1.5× 임계 밖.
- 코드 근거 표 7행 전부 확인: canonicalFrameStore.ts 에 selection store + projection 동거 (export 8), frameActions.ts 공개 API (create/delete/update/select + getNextFrameName 이 이미 `Layout N`), navigatorSectionIds.ts (`navigator-frames`/`navigator-frame-layers`, split key `navigator-split:layouts`), PageLayoutSelector 6 문구, `.frame-tree` 소비 4곳 (CSS :is 목록 + FrameList/FrameElementTree), editMode `"layout"`, Pencil fixture 5개, NavigatorPanelTabs 이미 Layouts.
- 접힘 persist = zustand persist `styles-panel-collapse` (localStorage) merge 훅 — 승계 지점은 있다.
- iframe/BuilderCore 의 wire payload 키는 이미 `layouts` — projection hook rename 이 preview 프로토콜을 건드리지 않는다.
- `Parent Frame`/`Top Frame` (utils/ui/labels.ts:308) 은 link target (browser frame) — platform 범주.
- fork 4 질문 lock-in + 사용자 명시 요청 기록 있음. ADR-111 Implemented 와의 의존 방향 (FrameNode → facade) 코드와 일치.

### 이슈

1. MEDIUM — HC6/G4 승계 술어가 1회성을 보장하지 않음 (구 id 삭제 필요, activeFocusSection 포함).
2. MEDIUM — G1 은 미번역 3 문구를 못 잡음; ko probe 증거 + param 메시지 함수 등록 요구.
3. MEDIUM — G0/G5 정규식을 rename 표 식별자 집합으로 재동결.
4. LOW — 3-Domain 귀속 문장 정정 (Builder chrome, D1~D3 변경 0).
5. LOW — allowlist 에 docs/adr/design · 현행 규칙/research 문서 범주 추가.

### UNVERIFIED

- Pencil/Figma 외부 문서 링크 내용 (결정에 비필수).

### 한계

- 정적 대조만. live 는 Proposed 단계라 대상 아님.

## Round 1 수정 종결 — 2026-09-19

- m1 fixed: hydration에서 구 section id를 제거하고 `activeFocusSection`도 치환한다.
  old/new/both/none에 `승계 → 펼침 → persist/reload → 펼침 유지` 순서 검증을 추가했다.
- m2 fixed: `properties.noLayout`, `properties.selectReusableLayout`, formatted
  `properties.usingLayout`의 ko/en 등록과 ko-KR 부팅 residue probe를 명시했다.
- m3 fixed: 공개 action/store API와 `SelectedReusableFrameId` 대소문자 변형을 포함한
  authoritative 정규식으로 재동결해 49개(프로덕션 27, 테스트 22)를 재현했다.
- l4 fixed: 대상은 Builder chrome이며 D1/D2/D3 계약 변경은 0이라고 정정했다.
- l5 fixed: pre-225 `docs/adr/design/**`은 역사 기록, 현행 rule/research는 Phase 3 갱신,
  `.tmp-panel-cap/**`은 untracked local probe로 분류했다.

**종합 판정**: round 1 pending 0. 사용자의 “수정 후 Accepted 가능” 조건을 충족해
ADR-225를 Accepted로 승격했다. 이는 설계 승인이고 제품 구현·live 검증은 미착수다.

## Round 2 — 2026-09-19 (reviewer: codex)

**판정**: 계획대로 완전 종결되지 않았다. 핵심 사용자 흐름과 canonical 보존은 확인됐지만 G5가 실제로 실패하고 G0/G6 증거가 완료 계약을 충족하지 못하므로 Implemented 종결 근거를 보강해야 한다.

### 확인한 것

- HEAD/origin/main `a5ffb3f9b`; ADR-225 보호 경계(`packages/shared`, `apps/publish`, Pencil adapter, `pageFrameBinding.ts`) 구현 diff 0.
- focused Vitest 11파일 74/74 PASS, Builder type-check PASS. Pencil/page binding 6파일은 27/28 PASS; 실패 1은 ADR 본문이 열거한 선재 `pageFrameBinding` assertion이며 ADR-225가 해당 production/test 파일을 변경하지 않았다.
- 현재 HEAD에서 headed `adr225-layouts-live.mjs` 13/13 PASS. 별도 새 프로젝트 foreground 부팅·Add Layout 뒤 console warning/error 0.
- `rg`로 현재 Builder feature-owned 구 UI 주석 7건을 재현했고 ratchet는 74/74 중 green이었다.

### UNVERIFIED

- breakdown가 요구한 layout 이름 변경 UI는 production caller가 없어 실제 Builder에서 실행할 수 없었다.
- 내부 Layers tree 선택/접기, section split resize, Undo, Canvas/Preview geometry/slot parity는 live script가 측정하지 않는다.
- 이번 판독에서 전체 builder suite와 `codex:preflight`는 반복 실행하지 않았다; focused gate와 type-check, live만 재실행했다.

### 권고

1. generic UI-owned comment 패턴과 7개 잔여를 inventory/ratchet에 넣고 수리한다.
2. 49-file 분류 근거를 추적 가능한 문서/게이트 fixture로 옮겨 dead link를 없앤다.
3. Phase 4 실제 Builder 계약을 구현 범위에 맞게 정정하거나 누락 시나리오를 live harness로 실행한 뒤 Implemented 근거를 다시 종결한다.
