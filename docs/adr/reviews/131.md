---
adr: 131
title: events/data/actions 일급 컴포넌트 루트 컬렉션
reviews:
  - round: 1
    ts: '2026-05-12T14:53:52.253Z'
    reviewer: claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: other
        summary: >-
          useEventsForTarget / useDocumentData selector cache 위험 미명시 (Zustand
          v5)
        evidence: >-
          design/131-events-data-actions-first-class-collections-breakdown.md:127
        root_cause: >-
          Phase 3 bridge hooks (useEventsForTarget(nodeId) / useDocumentData() /
          useDocumentActions()) 가 useStore selector 안에서 새 배열/객체 반환 시 Zustand v5
          selector cache miss → 무한 루프. memory/feedback-zustand-selector-cache.md
          + ADR-122 회귀 사례. Phase 3 산출물에 selector cache pattern (useMemo +
          getState() inside, canonicalElementsView.ts:filter 패턴 재사용) 명시 권장.
        outcome: pending
      - id: l2
        severity: LOW
        category: other
        summary: 'Hard Constraint #1 (Pencil round-trip) vs R6 (scope 외) 긴장'
        evidence: '131-events-data-actions-first-class-collections.md:23 vs :123'
        root_cause: >-
          Context Hard Constraint #1 line 23 'Pencil round-trip drop/sidecar
          가능해야 함' 으로 강 선언, R6 mitigation 은 'scope 외 — 향후 Pencil import/export
          ADR' 위임. Constraint 를 'Pencil consumer 가 unknown root key 무시 가능한 위치'
          한정 또는 R6 의 drop vs sidecar 결정 Decision 영역에 명시 권장.
        outcome: pending
      - id: l3
        severity: LOW
        category: other
        summary: 'SerializedEvent/Data/Action 에 [k: string]: unknown index signature 누락'
        evidence: >-
          design/131-events-data-actions-first-class-collections-breakdown.md:75-97
          vs composition-document.types.ts:522-548
        root_cause: >-
          ADR-116 의 SerializedEventHandler / SerializedAction /
          SerializedDataBinding 는 [k: string]: unknown 을 포함, 본 ADR design §3
          Phase 1 신규 schema 정의에는 누락. forward-compat + ADR-116 migration helper
          호환 위해 추가 권장.
        outcome: pending
      - id: l4
        severity: LOW
        category: adr-structure-violation
        summary: docs/adr/README.md 현황 요약 및 ADR row 미반영
        evidence: 'docs/adr/README.md:180-182'
        root_cause: >-
          ADR-131 Proposed 발의 시점에 README.md '미구현/진행 (Proposed/In Progress) 6'
          카운트와 ADR row 미갱신. adr-writing.md 'Status 변경 시 README.md 테이블도 동시 갱신' 조항
          적용. ADR-130 도 동일 영역.
        outcome: pending
  - round: 2
    ts: '2026-05-12T14:57:39.837Z'
    reviewer: claude
    source: live
    issues:
      - id: m1
        severity: MEDIUM
        category: other
        summary: useEventsForTarget / useDocumentData selector cache 위험 명시 보강 land
        evidence: >-
          design/131-events-data-actions-first-class-collections-breakdown.md:Phase
          3 §3 + 131.md:R7 + G5
        root_cause: >-
          Phase 3 bridge hooks 의 Zustand v5 selector cache 위반 위험 (selector 안 새
          배열/객체 반환 시 cache miss → 무한 루프, ADR-122 회귀 사례). 보강 내용: (1) ADR 본문
          §Risks 에 R7 (MED) 추가 + §Gates G5 추가, (2) design §3 Phase 3 산출물에
          selector cache pattern 의무 + canonicalElementsView.ts:333-339 reference
          + 예시 코드 + 정적 grep gate + render 100 회 회귀 test, (3) §7 verification
          checklist + §9 잔존 의문에 selector cache pattern 항목 추가.
        outcome: fixed
  - round: 3
    ts: '2026-05-12T15:37:00.514Z'
    reviewer: claude+codex
    source: live
    issues:
      - id: h1
        severity: HIGH
        category: ssot-violation
        summary: root collection 참조 무결성 cascade gate 보강 land (R8 + G6)
        evidence: >-
          131.md:R8 + G6 + breakdown 참조 무결성 cascade 산출물 +
          canonicalDocumentStore.ts:545 + compositionDocumentOrder.ts:448-460
        root_cause: >-
          codex round 3 HIGH 1건: SerializedEvent.target / props.onPress
          (actionRef) / props.dataSource / actions.next[] dangling reference 위험.
          기존 canonical removeNode 는 removeCanonicalChild (children tree only)
          호출, root collection cleanup 없음. 보강: (1) ADR 본문 R8 (HIGH) + G6 (Phase 3
          + Phase 6) 추가, (2) design Phase 3 산출물에 removeNodeWithRefCascade /
          duplicateNode remap / renameNodeId cascade / auditOrphanReferences
          utility 4 카테고리 정책 명시, (3) verification checklist 4 카테고리 unit test +
          Phase 6 orphan ref 0 evidence.
        outcome: fixed
      - id: m2
        severity: MEDIUM
        category: alternative-strawman
        summary: 대안 C 기각 근거 격하 + 대안 B 본질 한계 3축 정확화
        evidence: '131.md:Alternatives B/C + Risk Threshold Check table + Decision 기각 사유'
        root_cause: >-
          codex round 3 MED #2: 대안 C 기각 'schema 일관성 약함' 만으론 strawman. 보강: (1) 대안
          B 본질 한계를 data 만 → 3축 (data cross-node 공유 / actions cross-event 공유 /
          events page-level mental) 정확화, (2) 대안 C 기각 사유를 '본질 한계 아님 — schema 일관성
          + 사용자 framing flat 평등 정합 부족' 으로 격하, (3) Risk Threshold table 의 '본질 한계'
          열을 '본질 한계 / 기각 사유' 로 확장.
        outcome: fixed
      - id: m3
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          Hard Constraint #1 ↔ R6 긴장 해소 — Pencil export mode 분기 명시 (LOW → MED
          격상)
        evidence: >-
          131.md:Hard Constraint #1 mode 분기 + R6 MED + breakdown §6 export mode
          + Phase 2 산출물 exportCanonicalDocument mode flag
        root_cause: >-
          codex round 3 MED #3: format-model.md:6-14 evidence 는 'Pencil consumer
          가 unknown root key 무시' 까지 입증 불가. 본 ADR LOW 분류 약함. 보강: (1) Hard
          Constraint #1 을 mode 분기 (pencil-compatible drop / composition-native
          보존 / sidecar 후속 ADR) 로 명시, (2) R6 LOW → MED 격상 + mitigation 본 ADR
          scope 안 mode flag 도입, (3) breakdown §6 약점 인정 명시 (pencil-compatible
          drop default 가 안전), (4) Phase 2 산출물에 exportCanonicalDocument mode flag
          시그니처.
        outcome: fixed
  - round: 4
    ts: '2026-05-12T15:56:40.969Z'
    reviewer: codex-resume-round2
    source: live
    issues:
      - id: h1
        severity: HIGH
        category: ssot-violation
        summary: 'props.onPress 참조 대상 모순 — events[].id vs actions[].id'
        evidence: >-
          design breakdown:232 (events.id 정의) vs ADR:133 + breakdown:145
          (actions.id 검사)
        root_cause: >-
          ADR §5 예시 (props.onPress: 'ev1' → events[].id 참조) 와 R8/audit
          (props.onPress → actions[].id 참조) 가 같은 prop key 의 참조 대상을 모순 정의. G6 통과
          시 event-id orphan 누락 또는 정상 event ref 를 action orphan 으로 오판 가능. 수정: 참조
          그래프 `props[eventKey] → events[].id → events[].actionRef → actions[].id
          → actions.next[]` 로 고정 (props 는 직접 actions 참조 안 함, events 경유).
        outcome: pending
      - id: h2
        severity: HIGH
        category: ssot-violation
        summary: remove cascade 가 subtree descendant target event 누락
        evidence: >-
          breakdown:136 (doc.events.filter(e => e.target !== nodeId)) +
          compositionDocumentOrder.ts:448 (subtree 전체 제거)
        root_cause: >-
          현재 removeNodeWithRefCascade 는 단일 nodeId 만 events.target 매칭 제거. 실제
          removeCanonicalChild 는 children tree 전체 제거 → 삭제 subtree 의 descendant 를
          target 으로 가진 event 가 root collection 에 dangling 잔존. 수정:
          `removedSubtreeIds = collectIds(result.removed)` 후 `events.filter(e =>
          !removedSubtreeIds.has(e.target))` 처리.
        outcome: pending
      - id: h3
        severity: HIGH
        category: evidence-missing
        summary: audit 범위 onPress/dataSource 하드코딩 — EVENT_REGISTRY / event-id prop 미커버
        evidence: >-
          events.registry.ts:107
          (onSelectionChange/onAction/onOpenChange/onChangeEnd/onExpandedChange/onRemove)
          + Menu.spec.ts:340 (onActionId event-id prop) vs ADR:133 audit 4 카테고리
        root_cause: >-
          R8 audit utility 가 onPress/dataSource 만 명시. 실제 EVENT_REGISTRY 6+ 종
          event prop + spec item schema 의 onActionId 류 event-id type prop 미커버.
          G6 통과해도 다수 event-id orphan 누락. 수정: audit 가 EVENT_REGISTRY + spec
          editor fields (type: 'event-id') 를 derive 하여 모든 event-id prop key 를 동적
          검사.
        outcome: pending
      - id: m4
        severity: MEDIUM
        category: alternative-strawman
        summary: duplicate clone/share 정책 기준 부재
        evidence: >-
          breakdown:140 (events 가 새 actionRef 가지면 clone, 아니면 공유) vs ADR:71
          (action library 패턴 root collection 본질 이점)
        root_cause: >-
          duplicate 정책이 'events 가 새 actionRef 가지면 clone, 아니면 공유' 만 명시 —
          clone/share 결정 기준 없음. action library semantics (cross-event 공유) 와
          internal target dangling 위험 양쪽 미해소. 권장 기준: (1) action 이 pure shared
          library 면 공유, (2) action config 가 duplicated subtree 내부 node id 를
          target 하면 clone+remap, (3) 단일 event 전용 action 이면 clone.
        outcome: pending
      - id: m5
        severity: MEDIUM
        category: alternative-strawman
        summary: events page-level 논증 잔존 약점 + 대안 C 격하 표현 과함
        evidence: >-
          breakdown:75 (SerializedEvent.target: string 필수 UI node id) + ADR:87
          (대안 C '본질 한계 없음')
        root_cause: >-
          SerializedEvent.target 가 필수 UI node id → 'page-level event mental
          model' 은 root collection 만으로 자연 해소 안 됨. 수정: targetScope:
          'node'|'page'|'document' 또는 nullable/page target 도입. 대안 C 격하 '본질 한계
          없음' → 'data 한계 해소, events/actions partial residual limitation' 으로 정확화
          (codex round 1 MED #2 부분 잔존).
        outcome: pending
      - id: m6
        severity: MEDIUM
        category: migration-cost-unquantified
        summary: Phase 3 +0.5d 추정 부족 — duplicateNode/renameNodeId 모두 신설
        evidence: >-
          composition-document-actions.types.ts:57,155 (removeNode 존재,
          duplicateNode/renameNodeId 미존재)
        root_cause: >-
          Phase 3 추정 +0.5d 는 (mutation API 2개 신설 + remove cascade wrapper +
          audit utility + event-id key derivation + clone/share policy tests)
          포함하면 부족. 현실적 추정: +1.0~1.5d. Phase 2 +0.2d 도 mode flag 자체는 가능하나 project
          export UI/경고 포함 시 부족.
        outcome: pending
  - round: 5
    ts: '2026-05-12T16:09:06.736Z'
    reviewer: claude+codex-round4-reframing
    source: live
    issues:
      - id: h1
        severity: HIGH
        category: ssot-violation
        summary: >-
          참조 그래프 단일 source lock-in (5-A) — props → events 만 직접, actions 는 events
          경유
        evidence: >-
          ADR §Decision 5-A + R9 + G7 + design Phase 1 brand type
          (EventId/ActionId/DataId)
        root_cause: >-
          Round 3 R8 가 props.onPress 의 참조 대상을 events.id 와 actions.id 양쪽으로 모순 정의
          (codex round 4 HIGH-1). Round 5 보강: ADR §Decision 5-A 에 참조 그래프 단일
          source lock-in (props[eventKey] → events.id → events.actionRef →
          actions.id → actions.next[] / props[dataKey] → data.id 직교). brand type
          으로 compile-time 차단 (props → actions 직접 참조 type error).
        outcome: fixed
      - id: h2
        severity: HIGH
        category: ssot-violation
        summary: >-
          lifecycle cascade subtree id set lock-in (5-B) — removedSubtreeIds
          membership
        evidence: >-
          ADR §Decision 5-B + R8 갱신 + G6 갱신 + design Phase 3
          removeNodeWithRefCascade pseudo-code
        root_cause: >-
          Round 3 R8 cascade 가 단일 nodeId 만 events.target 매칭 (codex round 4
          HIGH-2). Round 5 보강: ADR §Decision 5-B 에 `removedSubtreeIds =
          collectIds(removedSubtree)` 후 `!removedSubtreeIds.has(e.target)` set
          membership 정의. descendant.target event 누락 방지.
        outcome: fixed
      - id: h3
        severity: HIGH
        category: evidence-missing
        summary: >-
          registry-driven audit derivation lock-in (5-C) — EVENT_REGISTRY + spec
          event-id field
        evidence: >-
          ADR §Decision 5-C + R10 + G8 + design Phase 3 collectEventIdPropKeys()
          pseudo-code
        root_cause: >-
          Round 3 R8 audit 가 onPress/dataSource 만 하드코딩, EVENT_REGISTRY 6+ event
          prop + spec onActionId 미커버 (codex round 4 HIGH-3). Round 5 보강: ADR
          §Decision 5-C 에 EVENT_REGISTRY + spec editor field (type: 'event-id' /
          'data-id') 동적 derivation lock-in. 신규 prop 추가 시 audit 자동 detect
          regression test (G8).
        outcome: fixed
      - id: m4
        severity: MEDIUM
        category: alternative-strawman
        summary: >-
          action library 3 분기 lock-in (5-D) — classifyAction 의 pure-library /
          internal-target / single-event
        evidence: >-
          ADR §Decision 5-D + R11 + G9 + design Phase 3 classifyAction()
          pseudo-code
        root_cause: >-
          Round 3 duplicate 정책 'events 가 새 actionRef 가지면 clone, 아니면 공유' 기준 부재
          (codex round 4 MED-4). Round 5 보강: ADR §Decision 5-D 에 3 분기 lock-in
          (pure-library 공유 / internal-target clone+remap / single-event clone).
          action.config 외부 자원 / subtree-internal target / single-event 사용 기준으로
          derive.
        outcome: fixed
      - id: m5
        severity: MEDIUM
        category: alternative-strawman
        summary: targetScope schema lock-in (5-E) + 대안 C 격하 partial residual 정확화
        evidence: >-
          ADR §Decision 5-E + R12 + 대안 C Risk Threshold 표 'partial residual' +
          design Phase 1 EventTargetScope type
        root_cause: >-
          Round 3 SerializedEvent.target 필수 UI node id → page-level event mental
          미해소 (codex round 4 MED-5). 대안 C 격하 '본질 한계 없음' 과함. Round 5 보강: ADR
          §Decision 5-E targetScope schema (node | page | document) + target
          scope-conditional 의미. 대안 C 격하 표현 'partial residual limitation' 으로 정확화
          (data 한계 해소, events/actions 한계 잔존).
        outcome: fixed
      - id: m6
        severity: MEDIUM
        category: migration-cost-unquantified
        summary: >-
          Phase 3 추정 보정 — +0.5d → ~2.5d (duplicateNode/renameNodeId 신설 + 5 영역
          cascade)
        evidence: >-
          design Phase 3 추정 갱신 + composition-document-actions.types.ts:57,155
          (duplicateNode/renameNodeId 미존재 확증)
        root_cause: >-
          Round 3 Phase 3 추정 +0.5d (1d→1.5d) 가 mutation 2 신설 + cascade wrapper +
          audit + clone/share tests 미반영 (codex round 4 MED-6). Round 5 보강: ~2.5d
          로 갱신 (brand type / targetScope / 5-D 분기 추가 +0.5d 포함).
        outcome: fixed
  - round: 6
    ts: '2026-05-12T16:28:22.986Z'
    reviewer: claude (사용자 lock-in)
    source: live
    issues:
      - id: l1
        severity: LOW
        category: other
        summary: Round 2~5 전체 revert — ADR-131 초기 설계 (Proposed 2026-05-12) 복귀
        evidence: >-
          131.md + design breakdown 본문 — Round 1/3/5 추가분 (R7~R12 + G5~G9 + 5 영역
          lock-in + Pencil mode 분기 + brand types) 모두 제거
        root_cause: >-
          사용자 명시 lock-in (2026-05-13): 'ADR-131 초기 설계 대로 진행. 추가로 132까지 할필요는
          없지않나' + '굳이 adr 안해서 문제될게 뭐가 있지?'. 사용자 framing question 5회 연속 → claude
          framing 평가 오류 인정 (memory feedback-execute-adr-surface-minimization 위반
          — 후속 ADR 분리 옵션 surface 자체가 부적절). Round 2~5 추가분 모두 codex review 사이클로
          발견된 영역이나, codex round 4 결함들은 모두 implementation 시점 ad-hoc 결정 가능 (commit
          message + design doc + 사용자 framing 안에서 충분). ADR 형식 lock-in 불필요. 사용자
          framing ('Pencil 에 events/dataBinding 없음' + 'token/theme 처럼 별도' +
          'flat node + Props string id 참조') 가 ADR-131 의 본래 framing scope 전체
          cover.
        outcome: rejected
---
# ADR-131 Review Log

## Round 1 — 2026-05-12 (reviewer: claude)

## ADR 리뷰 결과: ADR-131 events/data/actions 일급 컴포넌트 루트 컬렉션

### 구조 검증

| 섹션 | 판정 | 품질 | 인용 |
| ---- | ---- | ---- | ---- |
| Context | PASS | HIGH | ADR-131:7-39 (3-domain 분류 + Hard Constraints 5건 + baseline grep) |
| Alternatives (3개) | PASS | HIGH | ADR-131:41-77 |
| 4축 위험 평가 | PASS | HIGH | ADR-131:51-55, 61-66, 72-77 |
| Risk Threshold Check | PASS | HIGH | ADR-131:79-87 |
| Decision 위험 수용 근거 | PASS | HIGH | ADR-131:89-110 |
| 구현 상세 분리 | PASS | HIGH | ADR-131:112 포인터 + design 존재 |
| Risks | PASS | HIGH | ADR-131:114-123 Decision 뒤/Gates 앞, ID 표 R1-R6 |
| Gates | PASS | HIGH | ADR-131:125-132 G1-G4 |
| framing 4 질문 lock-in | PASS | HIGH | breakdown:8-14 + 18-25 사용자 explicit confirm |

### 코드 검증

핵심 8 주장 중 7 VERIFIED + 1 PARTIAL (Phase 0 baseline freeze 미시점). Pencil format-model.md / composition-document.types.ts / EventsPanel.tsx / createElement.ts / IndexedDB adapter 전수 확인.

### 위험 평가

- ADR 기재 R1-R6: 모두 mitigation 경로 명확
- 누락 위험 3건 (MED 1: zustand selector cache / LOW 2: Hard Constraint vs R6 긴장 + schema index signature) + 구조 LOW 1 (README 미반영)

### 종합 판정

- 구조: PASS (9/9 HIGH)
- 코드 정합: 7/8 VERIFIED + 1/8 PARTIAL
- 누락 위험: 3건 (HIGH 0 / MED 1 / LOW 2) + 구조 LOW 1
- 결론: **승인 가능** (MEDIUM 1건 + LOW 3건 보강 권장, framing 4 질문 + 사용자 explicit confirm 완료)

## Round 2 — 2026-05-12 (reviewer: claude)

## Round 2 — MEDIUM 1건 보강 land

### 보강 land 영역

1. **ADR 본문 §Risks R7 추가** (line 124, MED) — Phase 3 bridge hooks Zustand v5 selector cache 위반 위험 명시 + mitigation 경로 (selector cache pattern + reference + grep gate + 회귀 test)
2. **ADR 본문 §Gates G5 추가** (Phase 3 시점) — bridge hooks `useMemo([doc])` 파생 패턴 준수 + render 100 회 reference 동일성 회귀 test PASS + 정적 grep gate 0건 (boundary 외)
3. **design §3 Phase 3 산출물 보강** — selector cache pattern 의무 sub-bullet 추가:
   - reference 패턴: `canonicalElementsView.ts:333-339` (useActiveCanonicalDocument + useMemo([doc]))
   - 구현 예시 code block (useEventsForTarget(nodeId))
   - 정적 grep gate pattern (`useStore\(\s*\([^)]*\)\s*=>\s*[^.]+\.\s*(filter|map|reduce|slice)`)
   - render 100 회 reference 동일성 회귀 test Gate
   - memory `feedback-zustand-selector-cache` 인용 (ADR-122 회귀 사례)
4. **design §7 검증 evidence 체크리스트** — Phase 3 selector cache pattern 회귀 test 항목 추가
5. **design §9 잔존 의문** — selector cache pattern 차단 설계 항목 추가

### Round 1 → Round 2 변경

- MEDIUM 1건: `pending` → **`fixed`**
- LOW 3건 (Hard Constraint vs R6 긴장 / schema index signature / README 미반영): 사용자 미요청 — 변경 없음

### 결론

MEDIUM 1건 보강 완료. ADR 본문 + design + reviews 3 영역 동기 갱신.

## Round 3 — 2026-05-12 (reviewer: claude+codex)

## Round 3 — codex 리뷰 반영 (HIGH 1 + MED 2 보강 land)

### Codex round 3 결과

- 판정: GO-WITH-FIXES (HIGH 1 / MED 3 / LOW 2)
- 본 round 에서 land: HIGH 1 + MED 2 (codex MED #2/#3, MED #4 G5 verification 구체화는 Phase 3 진입 직전 정밀화)
- LOW 2건 (schema index signature / README 카운트 stale): 변경 없음

### Land 영역

1. **HIGH (R8 + G6)** — 참조 무결성 cascade
   - ADR 본문: R8 (HIGH) + G6 (Phase 3 + Phase 6) 추가
   - design Phase 3 산출물: `removeNodeWithRefCascade` / `duplicateNode` remap / `renameNodeId` cascade / `auditOrphanReferences` utility (4 카테고리: events.target / props.onPress / props.dataSource / next[])
   - design verification checklist: 참조 무결성 cascade unit test + Phase 6 orphan ref 0 evidence
   - 코드 증거: `canonicalDocumentStore.ts:545` + `compositionDocumentOrder.ts:448-460` (children tree only, root collection cleanup 없음 확증)

2. **MED #2 (대안 C 기각 격하 + B 본질 한계 정확화)**
   - 대안 B 본질 한계 3축 (data / actions / events page-level)
   - 대안 C 기각 사유 격하 ('본질 한계' 표현 제거, schema 일관성 + framing 정합 부족 으로 정확화)
   - Risk Threshold table 의 본질 한계 열 확장

3. **MED #3 (Pencil export mode 명시)**
   - Hard Constraint #1 mode 분기 명시 (`pencil-compatible` drop / `composition-native` 보존 / `sidecar` 후속 ADR)
   - R6 LOW → MED 격상 + mitigation 본 ADR scope 안 흡수
   - design §6 약점 인정 (format-model.md evidence 의 보장 한계)
   - Phase 2 산출물에 `exportCanonicalDocument(doc, {mode})` 시그니처 도입

### 잔존 (변경 없음)

- codex MED #4 (G5 verification 절차 구체화 — renderHook + rerender 100회 + StrictMode warning 0): Phase 3 진입 직전 정밀화 가능. 사용자 미요청.
- codex LOW #5 (schema index signature): 사용자 미요청.
- codex LOW #6 (README 카운트 stale): 사용자 미요청.

### 결론

HIGH 1 + MED 2 보강 완료. 구현 착수 전 권고 영역 모두 ADR + design 본문 반영. R6 격상 + G6 추가로 framing layer 정합 강화.

## Round 4 — 2026-05-12 (reviewer: codex-resume-round2)

## Round 4 — codex resume re-review 결과 (GO-WITH-FIXES 유지)

### Codex 재판정

- **GO-WITH-FIXES 유지** — Round 3 R8 본질 미도달
- Round 3 의 R6 mode 분기 + 대안 격하는 실질 진전
- 남은 핵심 결함: **참조 모델 정확도** (HIGH 3 + MED 3)

### HIGH 3건 (R8 본질 결함)

1. **참조 그래프 모순** — ADR §5 (props.onPress → events.id) vs R8/audit (props.onPress → actions.id). 같은 prop key 가 다른 참조 대상으로 정의. 그래프 고정 필요: `props[eventKey] → events[].id → events[].actionRef → actions[].id → actions.next[]`

2. **remove cascade subtree 누락** — `doc.events.filter(e => e.target !== nodeId)` 단일 nodeId 만. `removeCanonicalChild` subtree 전체 제거 → descendant.target event 잔존. 수정: `removedSubtreeIds = collectIds(result.removed)` 후 set membership 검사

3. **audit 범위 하드코딩** — EVENT_REGISTRY 6+ event prop + Menu.spec.ts:340 onActionId 류 event-id prop 미커버. registry/spec derivation 필요

### MED 3건

4. **duplicate clone/share 기준 부재** — pure shared library / internal target / 단일 event 전용 3 분기 명시 필요

5. **events page-level 논증 약함** — `SerializedEvent.target` 필수 UI node id → page-level mental 해소 안 됨. `targetScope: 'node'|'page'|'document'` 또는 nullable. 대안 C 격하 '본질 한계 없음' → 'partial residual' 정확화

6. **Phase 3 +0.5d 부족** — duplicateNode/renameNodeId 모두 미존재 신설. mutation 2 + cascade + audit + derivation + tests 포함 시 +1.0~1.5d 현실적

### Round 3 확증 영역 (긍정)

- R6 MED 격상 적합 (pencil-compatible lossy + sidecar 후속)
- 대안 C 격하 방향 정확 (표현만 과함)
- Phase 2 mode flag 가능 (pencilExport.ts:54 중앙화), UI/경고 +α

### 결론

Round 3 의 HIGH 1 + MED 2 outcome 을 `fixed` 로 처리한 것은 **본질 미도달**. Round 4 land 필요 영역: HIGH 3 (참조 그래프 고정 + subtree cascade + registry derivation) + MED 3 (duplicate 정책 / targetScope / 추정 보정). R8 outcome → `deferred` 재분류 권장 (round 3 보강이 방향은 맞으나 실질 미충족).

## Round 5 — 2026-05-12 (reviewer: claude+codex-round4-reframing)

## Round 5 — 본문 재설계 land (5 영역 본질 결정 lock-in)

### Framing 위반 해소

Round 4 codex 재판정 (R8 본질 미도달) 후 사용자 lock-in (옵션 A: ADR 본문 재설계). 메모리 정합:
- `framing 통과 ≠ 형식 통과` (CLAUDE.md): Round 3 의 storage location framing 만 통과, 실제 결정 5 영역 (참조 그래프 / lifecycle / registry / 공유 / target scope) 미통과
- `feedback-adr-essence-priority-over-formal-pass`: storage location 결정의 본질 cascade 영역
- `feedback-codex-not-framing-layer`: codex round 4 가 본문 정합 layer 결함을 정확히 detect, framing layer 는 사용자 + claude 평가

### 5 영역 본질 결정 lock-in (ADR §Decision 신설 섹션)

- **5-A 참조 그래프 단일 source**: `props[eventKey] → events.id → events.actionRef → actions.id → actions.next[]` / `props[dataKey] → data.id` 직교. brand type 으로 compile-time 차단
- **5-B lifecycle cascade 의미론**: removedSubtreeIds set membership + props scan + actions chain cleanup
- **5-C registry-driven audit**: EVENT_REGISTRY + spec editor field (type: 'event-id' / 'data-id') 동적 derivation
- **5-D 공유 의미론 (action library 3 분기)**: pure-library 공유 / internal-target clone+remap / single-event clone
- **5-E target scope schema**: targetScope: 'node' | 'page' | 'document' + scope-conditional target

### Risks / Gates 갱신

- R8 refined (cascade subtree 의미론)
- R9 (참조 그래프) / R10 (registry-driven audit) / R11 (공유 의미론) / R12 (target scope) 신설
- G6 refined / G7 / G8 / G9 신설 (각 R 와 1:1 매핑)

### Round 4 모든 결함 fixed

HIGH 3건 + MED 3건 모두 본문/design 본질 결정 lock-in 으로 해소. Round 3 의 `pending → fixed` 처리가 본질 미도달이었던 점 재인정.

### 사용자 explicit confirm 의무 (M2)

ADR 본문 §Decision 5 영역 lock-in 까지 land 됐다. 사용자 explicit confirm (참조 그래프 / cascade / registry / 공유 / target scope 5 영역 본질 결정 정합) 받기 전 commit / Accepted 승격 차단.

### 결론

Round 5 본문 재설계 완료. codex round 5 재진입으로 본문 정합 layer 확증 권장 (framing layer 는 사용자 confirm).

## Round 6 — 2026-05-12 (reviewer: claude (사용자 lock-in))

## Round 6 — Round 2~5 전체 revert (사용자 lock-in)

### 사용자 본질 question 패턴

Round 2~5 진행 중 사용자 framing question 5회 연속:
1. '현재 뭐에서 문제가 생겨서 이러는거지'
2. '후속은 왜 또나오는거야'
3. '굳이 adr 안해서 문제될게 뭐가 있지?'
4. 'adr 131 초기 설계 대로 진행 하면 되지않나? 추가로 132까지 할필요는 없지않나'
5. '왜 131 후속 adr 이 필요한 명확한 근거는 뭐지?'

각 question 이 claude framing 평가 오류를 정확히 지목. 메모리 `feedback-execute-adr-surface-minimization` + `feedback-no-derived-adr-mid-execution` + `feedback-adr-consolidation-burden-not-essence` 위반 누적.

### claude framing 평가 오류

- Round 3 R8 cascade 추가: storage location 결정의 cascade 영역이라 정당화했으나, implementation 시점 ad-hoc 결정 가능 영역
- Round 4 codex 결함 6건: 모두 implementation 영역 (참조 그래프 / cascade / registry / 공유 / scope 모두 ADR 형식 lock-in 불필요)
- Round 5 5 영역 lock-in: ADR 본문에 흡수 동기가 consolidation burden (codex review fatigue 회피) — 본질 framing 아님
- '후속 ADR' surface: codex review fatigue 회피 + ADR 본문 재설계 부담 회피 동기. 본질 근거 없음

### Revert 영역

**ADR 본문 (131.md)**:
- Status: Accepted 2026-05-13 → **Proposed 2026-05-12** (원본 복귀)
- Context Hard Constraint #1: Pencil mode 분기 명시 → '**drop 또는 sidecar 처리 가능**' (원본)
- Context Hard Constraint #6 (Round 5): 제거
- 대안 B 본질 한계: 3 카테고리 → **data 만 cross-node 공유 mental** (원본)
- 대안 C 격하: 'partial residual' → **'본질 한계 — schema 일관성 약함'** (원본)
- Risk Threshold 표: 본질 한계 컬럼 원본 복귀
- §Decision 의 '5 영역 본질 결정 lock-in' 섹션 (Round 5): **전체 제거**
- Decision 기각 사유: 원본 (단순 형식) 복귀
- R6 격상 (Round 3 MED): → **LOW** (원본)
- R7~R12 (Round 2~5 추가): **모두 제거**
- G5~G9 (Round 2~5 추가): **모두 제거** — G1~G4 만 유지

**design breakdown**:
- Phase 1 schema: brand types (EventId/ActionId/DataId) + targetScope + index signature → **원본 단순 interface** 복귀
- Phase 2 mode flag: 제거 → 원본 산출물 4 항목
- Phase 3 cascade 5-A~5-E + selector cache pattern: **전체 제거** → 원본 산출물 (canonicalDocumentStore 액션 + bridge hooks + clone-on-write)
- §6 Pencil mode 분기: → 원본 ('향후 ADR 결정 영역')
- §7 verification checklist: Round 3+5 추가 5 항목 제거 → 원본 5 항목
- §9 잔존 의문: Round 3+5 추가 3 항목 제거 → 원본 3 항목

**README**: 카운트 원본 (121 / 6 / 134) 복귀

### 결론

ADR-131 = 2026-05-12 Proposed 시점 (Round 1 직전) 으로 완전 복귀. codex round 4 결함 6건은 모두 implementation 시점 ad-hoc 결정 영역으로 미루기. 사용자 framing ('flat node + Props string id 참조') 충분.

메모리 적재 권장 (선택): 본 세션 패턴 (codex review fatigue → consolidation burden → 본문 재설계 → 사용자 framing question 5회 → 전체 revert) 재발 차단용.
