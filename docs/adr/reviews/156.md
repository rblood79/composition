---
adr: 156
title: composition-engine CSS 정합 복구 — 정렬·마진 미구현 5종 + Chrome 차등 oracle 도입
reviews:
  - round: 1
    ts: "2026-07-17T12:58:58.963Z"
    reviewer: claude
    source: live
    issues:
      - id: h1
        severity: HIGH
        category: other
        summary: >-
          누락 위험 — justifySelf/justifyItems 가 LAYOUT_STYLE_KEYS 에 없어 엔진을 고쳐도 편집
          무반영. 9칸 피커 leftTop→centerTop 은 justifySelf 만 바뀌어 style 시그니처 불변 → 캐시 히트
          → '세로만 되고 가로는 안 되는' 상태로 출하. R6 신설 + G2 에 캐시 키 등재/가로 전용 live 검증 편입
        evidence: >-
          layoutCache.ts:49-110 (LAYOUT_STYLE_KEYS, alignSelf=:84 존재 / grep -c
          '"justifySelf"' → 0) + layoutCache.ts:185-190
          createElementLayoutSignature + TransformSection.tsx:70-82
          SELF_ALIGN_POSITION_MAP
        root_cause: >-
          엔진이 justifySelf 를 애초에 소비하지 않아 캐시 키 누락이 드러난 적이 없음. ADR 작성 시
          layout-engine.md 3-심볼 체인 점검을 alignSelf 만 확인하고 justifySelf 로 확장하지 않음
        outcome: fixed
      - id: m2
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          핵심 동기 주장 '결함이 flex 단위 테스트 36개를 통과' 부정확 — 결함 존재 시점 flex.rs 는 34개. 36 은
          그 결함을 잡으려 추가한 RED 테스트 2개를 포함한 값이라 자기모순(2개는 통과하지 않았음). 34 로 정정 + 실측 근거
          명기
        evidence: >-
          git show bb6ab7e40~1:packages/composition-engine/src/flex.rs | grep -c
          '#\[test\]' → 34 (현재 36)
        root_cause: 리뷰 시점(수정 후) 카운트를 결함 시점 카운트로 소급 인용
        outcome: fixed
      - id: m3
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          E1 증거 사슬의 심볼 오귀속 — 'LAYOUT_PROP_KEYS(layoutCache.ts:84)가 캐시를 무효화' 는
          틀림. :84 는 LAYOUT_STYLE_KEYS 소속이고 LAYOUT_PROP_KEYS(:112)는 props
          배열(children/text/size…)이라 alignSelf 를 담지 않음. LAYOUT_STYLE_KEYS 로 정정 +
          두 배열 분리 명시
        evidence: >-
          layoutCache.ts:49 LAYOUT_STYLE_KEYS 선언 / :84 alignSelf / :112
          LAYOUT_PROP_KEYS(props) / :185-190 두 축 결합
        root_cause: >-
          layout-engine.md 규칙이 'LAYOUT_PROP_KEYS = 캐시 시그니처' 로 요약해 style 축 배열명을
          가림 — 규칙 표현을 원문 확인 없이 승계
        outcome: fixed
      - id: m4
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          'Chrome 실측 독립 oracle 은 tree_golden 6 fixture' 부정확 — N6 는 헤더가 스스로 손계산이라
          밝힘(padding≠0 케이스). 실측 fixture 는 N1~N5 5개. 5 로 정정하고, 손계산
          영역(N6=padding)에서 정확히 E5 가 발견된 사실을 순환 oracle 실증으로 보강
        evidence: >-
          packages/composition-engine/tests/tree_golden.rs 헤더 'N6 는 Chrome 실측이
          아니라 … CSS 산술로 손계산 고정한 padding≠0 케이스'
        root_cause: fixture 개수를 파일 단위로 셈하며 N6 예외 주석을 반영하지 않음
        outcome: fixed
      - id: l5
        severity: LOW
        category: evidence-missing
        summary: >-
          R3 충돌지점 ① 인용 정밀도 — DFS 가 조정하는 것은 자식 width 가 아니라 available
          width(trackWidth)이고, width 주입은 enrichWithIntrinsicSize 단계.
          layout-engine.md 요약 표현을 그대로 인용해 기전이 흐려짐. 실코드 라인으로 정정
        evidence: >-
          fullTreeLayout.ts:1516-1528 (childAvail.width = trackWidth) +
          utils.ts:4380-4381 (!growsInFlex → injectedStyle.width = ceiledWidth,
          needsWidth 게이트 · isFlexChild 전용 아님)
        root_cause: 규칙 문서 요약을 코드 확인 없이 인용
        outcome: fixed
  - round: 2
    ts: "2026-07-17T13:33:33.859Z"
    reviewer: claude
    source: live
    issues:
      - id: h1
        severity: HIGH
        category: other
        summary: >-
          R6 이 계열 결함의 일부만 커버 — justifySelf/justifyItems 외에
          gridAutoColumns/gridAutoRows/gridColumnStart/gridRowStart/overflowX/overflowY/order
          도 LAYOUT_STYLE_KEYS 미등재. E13/E14/E17 을 Phase 에 편입하는 순간 '엔진 고쳐도 편집
          무반영'이 동일 기전으로 재현. R6 을 계열 위험으로 재작성하고 각 Phase 착수 조건에 해당 키 등재를 편입할 것
        evidence: >-
          layoutCache.ts:49-110 LAYOUT_STYLE_KEYS 전체 60키 열거 —
          gridAutoColumns/gridAutoRows/gridColumnStart/gridRowStart/overflowX/overflowY/order
          grep 전부 0. gridColumn/gridRow shorthand 만 존재하나 layout-engine.md 는
          factory 에 gridColumnStart/End 숫자 line 병기를 요구. 캐시 히트 판정은
          layoutCache.ts:187-190 createElementLayoutSignature
        root_cause: >-
          round 1 이 R6 을 'justifySelf 단건'으로 기록해 동일 구멍의 계열성(발산 필드 다수가 캐시 키에 미등재)을
          점검하지 않음. E6~E17 이 Phase 미매핑이라 확장 점검 필요성이 드러나지 않음
        outcome: fixed
      - id: h2
        severity: HIGH
        category: other
        summary: >-
          breakdown §3 이 round 1 m3 fix 를 미전파 + R6 작업 누락 — breakdown:140 이 여전히
          'LAYOUT_PROP_KEYS 포함 (layoutCache.ts:84) — 캐시 무효화는 정상'이라 본문 R6 과 정면
          모순. Phase 2 실행자가 breakdown 을 근거로 작업하면 justifySelf 캐시 키 등재를 누락해 '세로만 되고
          가로는 안 되는' 상태로 출하
        evidence: >-
          breakdown:140 (LAYOUT_PROP_KEYS 포함 … 캐시 무효화는 정상) vs ADR 본문:124 R6 대응 /
          layoutCache.ts:49 LAYOUT_STYLE_KEYS 선언 · :112 LAYOUT_PROP_KEYS 선언 ·
          :84 alignSelf. breakdown §3 작업 1/2/3 (148-151) 에 LAYOUT_STYLE_KEYS 편집
          항목 0건
        root_cause: >-
          round 1 이 본문 5곳만 정정하고 design breakdown 을 동반 sweep 하지 않음
          (feedback-adr-body-design-dual-grep-sweep 패턴 재발)
        outcome: fixed
      - id: h3
        severity: HIGH
        category: other
        summary: >-
          E6~E17 12군이 Phase/Gate/Risk 어디에도 매핑되지 않음. 본문:49 가 '리뷰 시 확정'으로 위임했으나
          Gates G0~G4 는 E1~E5 만 커버 — 발견 발산 17군 중 30%만 게이트가 존재. E6(도달 가능·근원
          확정·본문이 E1 동급 후보로 명시)·E8·E13·E14·E15 가 Gate 0. adr-writing.md 'HIGH+ 위험
          ↔ Gate 1:1 대응' 미충족
        evidence: >-
          ADR:39-49 (2차 sweep 절 — E6~E17 서술) vs ADR:115-124 (Risks R1~R6 에
          E6~E17 대응 0건) vs ADR:128-134 (Gates G0~G4 가 E1~E5 만). breakdown
          §1-1-b:25-38 에 12군 표는 존재하나 §3~§6 Phase 매핑에 미반영
        root_cause: >-
          2차 sweep 결과를 G0 계약대로 inventory 표에 흡수했으나 Phase/Gate 재편은 '리뷰 시 확정'으로 유보
          — 유보 상태가 그대로 리뷰 대상이 됨
        outcome: fixed
      - id: h4
        severity: HIGH
        category: other
        summary: >-
          E17 잠복 결합이 G4 통과 조건 밖 — 본문:46/breakdown:38 이 'Phase 4 에서 E3 구현 시
          overflow BFC 상쇄 차단을 함께 구현하지 않으면 새 발산이 생긴다'고 명시했으나 G4 조건은 'E3/E4/E5 발산
          0 + 상쇄 차단 케이스·672 조합 무회귀'로 overflow 항목이 0. 스스로 식별한 회귀 유발 조건이 게이트로 관리되지
          않음
        evidence: >-
          ADR:46 (잠복 결합 1건 서술) + breakdown:38 (E17 행 'Phase 4 (E3) 구현 시 BFC 차단
          조건 결합 의무') vs ADR:134 G4 통과 조건. overflow_x/y 는 tree.rs:108-109 선언 +
          fullTreeLayout.ts:645-646 송신 + 소비 0 (grep)
        root_cause: >-
          E17 을 '현재 diff 0 (잠복)'으로 분류해 Gate 편입 대상에서 제외 — 잠복의 조건이 Phase 4 자신이라는
          점이 게이트 설계에 반영되지 않음
        outcome: fixed
      - id: m5
        severity: MEDIUM
        category: evidence-missing
        summary: >-
          breakdown:98 'tree_golden.rs(11 test / 6 fixture)' 오기 — round 1 m4 가
          본문을 6→5 로 정정했으나 breakdown 미전파. 실제 fixture 는 N1~N10 = 10개이며 그중 Chrome
          실측은 N1~N5. '6'은 실측 수도 전체 수도 아님
        evidence: >-
          tests/tree_golden.rs:129,159,188,213,236,267,301,330,362,392 (N1~N10)
          + :402 field_contract_guard = 11 test. 헤더 'fixture 원본:
          dualRunLive.test.ts C-2b N1~N5' + 'N6 는 Chrome 실측이 아니라 … 손계산'
        root_cause: round 1 fix 가 본문에만 적용됨 (H2 와 동일 근원)
        outcome: fixed
      - id: m6
        severity: MEDIUM
        category: other
        summary: >-
          G1 통과 조건이 'tree_golden N1~N6 재현'이라 N7~N10 이 회귀 기준선에서 이탈. 공교롭게 N7~N10 이
          담당하는 영역(auto column flexGrow / block fit-content / display:none /
          percent width)은 2차 sweep 이 정합 확인한 영역과 겹치므로 기준선 편입이 자연스러움
        evidence: >-
          ADR:131 G1 조건 vs tests/tree_golden.rs:301 (N7 auto-height column +
          flexGrow) · :330 (N8 block fit-content) · :362 (N9 display:none) ·
          :392 (N10 flex-start column percent width)
        root_cause: ADR 작성 시 tree_golden fixture 를 6종으로 오인 (M5 와 동일 근원)
        outcome: fixed
      - id: m7
        severity: MEDIUM
        category: other
        summary: >-
          회귀 기준선 수치가 '672 조합'에 고정 — 2차 sweep 49 케이스가 확인한 정합 영역(grid 트랙 산술 4종 /
          중첩 컨테이너 자기 크기 4형상 / fit-content / percent padding·margin / flex
          min·max 재분배 등)이 breakdown §1-2 표에만 추가되고 Hard constraint·G2·G3·G4 통과
          조건에는 미편입. 표는 갱신됐으나 게이트는 갱신되지 않음
        evidence: >-
          ADR:54 (정합 기준선 무회귀 — 위 672 조합 + Rust 256) · ADR:132-134 (G2/G3/G4 전부
          '672 조합') vs breakdown:51-57 (2차 sweep 정합 확인 행 7종 추가)
        root_cause: 2차 sweep 흡수가 inventory(§1-2) 표 갱신에 그치고 게이트 조건 문장으로 전파되지 않음
        outcome: fixed
      - id: m8
        severity: MEDIUM
        category: other
        summary: >-
          docs/adr/README.md:430 drift — 'R1~R5'(R6 신설 미반영) · 발산 5종만 서술(E6~E17
          미반영) · 본문:17 이 명시적으로 '근거로 쓸 수 없다'고 배격한 '36 flex unit' 수치를 그대로 사용. ADR
          현황 대시보드가 본문과 불일치
        evidence: >-
          docs/adr/README.md:430 ('R1~R5 (R3 HIGH …)' / '36 flex unit + 15
          golden 전부 통과') vs ADR:17 ('현재 36개는 이 결함을 잡으려 추가한 RED 테스트 2개를 포함한 값이라
          근거로 쓸 수 없다') + ADR:124 (R6)
        root_cause: "round 1 의 본문 정정(36→34, R6 신설)이 README 요약으로 전파되지 않음"
        outcome: fixed
      - id: m9
        severity: MEDIUM
        category: other
        summary: >-
          Consequences:143 '3축 교차표가
          feedback-engine-declared-but-unread-style-fields 함정의 상시 점검 자산이 된다' 주장에
          상시성 메커니즘이 없음. G0 산출물은 1회성 문서 표이며, NodeStyle 필드 추가 시 교차표 갱신을 강제하는 가드가
          0건. 기존 자산(golden.rs:368 FLEX_FIELD_COUNT assert / tree_golden.rs:402
          field_contract_guard)은 필드 수·노드 수만 검사해 '선언 O 소비 X' 를 잡지 못함
        evidence: >-
          ADR:143 vs tests/tree_golden.rs:401-412 (field_contract_guard =
          N*_EXPECTED.len() assert 뿐) / tests/golden.rs:368 (FLEX_FIELD_COUNT ==
          17 assert). NodeStyle 미소비 9필드가 현재 어떤 가드에도 걸리지 않음
        root_cause: >-
          교차표를 문서 산출물로만 설계 — 프로젝트에 이미 존재하는 정적 계약 가드 패턴(FLEX_FIELD_COUNT assert)을
          적용 대상으로 고려하지 않음
        outcome: fixed
      - id: l10
        severity: LOW
        category: evidence-missing
        summary: >-
          미소비 필드 수 표기 혼용 — 본문:43 '8개'는 overflow_x/_y 를 1행으로 센 표 행 수이고 실제 필드는 9개.
          breakdown:78 '나머지 39필드는 소비 존재'는 49−9=40 과 어긋남. G0 통과 조건이 '전수 열거'이므로
          산술이 맞아야 확증 가능
        evidence: >-
          tree.rs NodeStyle pub 필드 카운트 = 49. 미소비 = align_self:123 /
          justify_self:124 / justify_items:115 / overflow_x:108 / overflow_y:109
          / grid_auto_flow:129 / grid_auto_columns:130 / grid_auto_rows:131 /
          aspect_ratio:172 = 9필드 (grep 전부 0 hit, 대조군 .align_items → 1)
        root_cause: 표 행(overflow_x/_y 합침) 기준 카운트와 필드 기준 카운트를 혼용
        outcome: fixed
      - id: l11
        severity: LOW
        category: evidence-missing
        summary: >-
          handleSelfAlignment 라인 인용 — 본문:37 및 breakdown:137 이
          'TransformSection.tsx:239'로 인용하나 함수 정의는 :235 이고 :239 는 함수 내부의 초기화
          분기(updateStylesImmediate({ alignSelf: '', justifySelf: '' })). round 1
          코드검증표는 :235-249 로 정확히 기록했으나 본문에 반영되지 않음
        evidence: >-
          TransformSection.tsx:235 const handleSelfAlignment = useCallback( /
          :239 updateStylesImmediate({ alignSelf: '', justifySelf: '' }) / :462
          onSelectionChange={handleSelfAlignment}
        root_cause: round 1 검증 결과가 본문 인용으로 역전파되지 않음 (H2/M5 와 동일 근원)
        outcome: fixed
---

# ADR-156 Review Log

## Round 1 — 2026-07-17 (reviewer: claude)

## ADR 리뷰 결과: ADR-156 composition-engine CSS 정합 복구

작성자 self-review — 확증 편향 차단을 위해 승인 근거가 아니라 결함 탐색 방향으로 수행.

### 구조 검증

| 섹션                                  | 판정 | 품질 | 인용                                                 |
| ------------------------------------- | ---- | ---- | ---------------------------------------------------- |
| Context (측정 가능 hard constraint)   | PASS | HIGH | ADR:39-44 (1px / 672 조합 / 60fps / live 1회)        |
| Alternatives (2개+)                   | PASS | HIGH | ADR:54-76 (A/B/C/D 4개)                              |
| 4축 위험 평가                         | PASS | HIGH | ADR:57,63,69,75 (4/4 대안 전부 4축)                  |
| Risk Threshold Check                  | PASS | MED  | ADR:80-87 (테이블 + 루프 판정)                       |
| Decision 위험 수용 근거 + 기각 사유   | PASS | HIGH | ADR:93-99                                            |
| 구현 상세 분리                        | PASS | HIGH | ADR:101 → design/156-\*-breakdown.md                 |
| Risks (Decision 뒤 / Gates 앞, ID 표) | PASS | HIGH | ADR:103-112 (R1~R6)                                  |
| Gates                                 | PASS | HIGH | ADR:114-122 (G0~G4, HIGH 2건 1:1 대응 R3→G3 / R6→G2) |

### 코드 검증 (핵심 주장)

| 주장                                             | 판정              | 근거                                                                        |
| ------------------------------------------------ | ----------------- | --------------------------------------------------------------------------- | --------------------------- |
| golden.rs 기대값이 Chrome 실측 아닌 명세 손계산  | VERIFIED          | tests/golden.rs 헤더 자인 + `grep -c '#[test]'` → 15                        |
| tree_golden 이 Chrome 실측 독립 oracle           | PARTIAL           | N1~N5 만 실측, N6 는 손계산 → **6→5 정정**                                  |
| 결함이 flex 단위 34개 통과                       | VERIFIED(정정 후) | `git show bb6ab7e40~1` → 34 (ADR 원안 36 은 RED 2개 포함 오류)              |
| alignSelf 가 엔진에 송신되나 소비 0곳            | VERIFIED          | fullTreeLayout.ts:664 송신 / tree.rs:123 선언 / `grep style.align_self` → 0 |
| justify_items·justify_self 소비 0곳              | VERIFIED          | `grep style.justify_items                                                   | style.justify_self` → 0 hit |
| grid.rs 정렬 코드 0줄                            | VERIFIED          | `grep 'justify                                                              | align' grid.rs` → 0 hit     |
| R3 JS DFS 사전 보정 실재                         | VERIFIED          | fullTreeLayout.ts:1516-1528 + utils.ts:4380-4381                            |
| playwright.config 부재 / vitest jsdom            | VERIFIED          | find → 0건 / apps/builder/vitest.config.ts:48                               |
| Inspector 정렬 피커가 alignSelf·justifySelf 저장 | VERIFIED          | TransformSection.tsx:235-249 handleSelfAlignment                            |

합계 **9 검증 / 7 VERIFIED · 1 PARTIAL(정정) · 1 정정 후 VERIFIED**.

### 위험 평가

| 위험                                        | 증거                                                                             | 심각도   | 상태               |
| ------------------------------------------- | -------------------------------------------------------------------------------- | -------- | ------------------ |
| R1~R5 (ADR 기재)                            | 각 행 인용                                                                       | MED~HIGH | 기재됨             |
| **justifySelf/justifyItems 캐시 키 누락**   | layoutCache.ts:49-110 / :185-190 / TransformSection.tsx:70-82                    | **HIGH** | **누락 → R6 신설** |
| layoutVersion blacklist 오등재              | elementUpdate.ts NON_LAYOUT_PROPS_UPDATE 에 alignSelf/justifySelf 없음           | —        | 위험 아님 (확인)   |
| 값 어휘 불일치 (Inspector start/end ↔ 엔진) | tree.rs:1477-1485 parse_align_items 가 start/end + flex-start/flex-end 양쪽 수용 | —        | 위험 아님 (확인)   |

### 종합 판정

- **구조**: PASS (7/7 섹션, FAIL 0)
- **코드 정합**: 9 검증 중 4건에서 인용 오류 발견 → 전부 정정
- **누락 위험**: 1건 (HIGH 1 / MED 0 / LOW 0) → R6 신설 + G2 편입
- **권고**:
  - HIGH: R6 (캐시 키 누락) — **동일 세션 반영 완료**
  - MEDIUM: 테스트 수 36→34 / 심볼 LAYOUT_PROP_KEYS→LAYOUT_STYLE_KEYS / fixture 6→5 — **전부 반영 완료**
  - LOW: R3 ① 기전 정밀화 — **반영 완료**
  - **결론**: 승인 가능 (round 1 이슈 5건 전부 fixed, 잔존 pending 0)

### 리뷰가 드러낸 메타 패턴

발견 4건 중 3건이 **규칙 문서·요약 표현을 원문 확인 없이 승계**한 데서 나왔다 (layout-engine.md 의 'LAYOUT_PROP_KEYS = 캐시 시그니처' 요약 → 실제 style 축은 LAYOUT_STYLE_KEYS / '자식 width 사전 조정' 요약 → 실제는 available width). 순환 oracle 을 지적하는 ADR 자신이 같은 유형(2차 출처 신뢰)의 오류를 담고 있었다는 점에서, 본 ADR 의 문제의식이 문서 층에도 동일하게 적용된다.

## Round 2 — 2026-07-17 (reviewer: claude)

## ADR 리뷰 결과: ADR-156 composition-engine CSS 정합 복구 (round 2)

round 1(5건 fixed 종결) 이후 상태 재검증. 초점: ① round 1 fix 의 design breakdown 전파 여부 ② 2차 정밀 sweep(E6~E17) 의 Phase/Gate 편입 상태.

### 구조 검증

| 섹션                                  | 판정        | 품질 | 인용                                                  |
| ------------------------------------- | ----------- | ---- | ----------------------------------------------------- |
| Context (측정 가능 hard constraint)   | PASS        | HIGH | ADR:51-56 (1px / 672 조합 / 60fps / live 1회)         |
| Alternatives (2개+)                   | PASS        | HIGH | ADR:66,72,78,84 (A/B/C/D)                             |
| 4축 위험 평가                         | PASS        | HIGH | ADR:69,75,81,87 (4/4 전부 4축)                        |
| Risk Threshold Check                  | PASS        | HIGH | ADR:90-99                                             |
| Decision 위험 수용 근거 + 기각 사유   | PASS        | HIGH | ADR:103-111                                           |
| 구현 상세 분리                        | PASS        | HIGH | ADR:113 → design/156-\*-breakdown.md                  |
| Risks (Decision 뒤 / Gates 앞, ID 표) | PASS        | MED  | ADR:115-124 (R1~R6) — E6~E17 대응 위험 0건            |
| Gates                                 | **PARTIAL** | MED  | ADR:128-134 (G0~G4) — 발견 발산 17군 중 E1~E5 만 커버 |

### 코드 검증 (핵심 주장 11건)

| 주장                                                | 판정           | 근거                                                                                                                                        |
| --------------------------------------------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| align_self/justify_self/justify_items 선언 O·소비 0 | VERIFIED       | tree.rs:115,123,124 선언 / `.align_self` 등 grep → 0 (대조군 `.align_items` → 1)                                                            |
| 미소비 8행(9필드) + order 미선언                    | VERIFIED       | overflow*x/y:108-109, grid_auto*\*:129-131, aspect_ratio:172 grep 0 / NodeStyle 내 `pub order:` → 0                                         |
| NodeStyle 49필드                                    | VERIFIED       | struct 범위 pub 필드 카운트 → 49                                                                                                            |
| grid.rs 정렬 0줄                                    | VERIFIED       | grep -c "justify\|align" grid.rs → 0                                                                                                        |
| E6 근원 = 단일 폭 ctx                               | VERIFIED       | tree.rs:1350-1358 ctx_for → container_size: Some(avail) 단일축 / :1066 write_block_item(…,&ctx) / :905 resolve_dimension_opt(main_raw,&ctx) |
| Rust 256 tests                                      | VERIFIED       | cargo test → lib 256 passed (+golden 15 +tree_golden 11 = 283). `#[test]` 총합 282 와 혼동 소지 있으나 본문 수치 정확                       |
| R6 (justifySelf/justifyItems 캐시 키 누락)          | VERIFIED       | layoutCache.ts:84 = "alignSelf" (LAYOUT_STYLE_KEYS :49-110) / "justifySelf" grep → 0                                                        |
| R3 JS DFS 사전 보정                                 | VERIFIED       | fullTreeLayout.ts:1516-1528 childAvail.width = trackWidth / utils.ts:4381 injectedStyle.width = ceiledWidth                                 |
| E8 reverse UI 노출                                  | VERIFIED       | styleOptions.ts:80-81 row-reverse/column-reverse                                                                                            |
| playwright.config 부재                              | VERIFIED       | find → 0건 / package.json:31 "test:e2e": "playwright test"                                                                                  |
| tree_golden = 6 fixture (breakdown:98)              | **UNVERIFIED** | 실제 N1~N10 = 10 fixture (tree_golden.rs:129~392) + field_contract_guard = 11 test                                                          |

**부수 확인 (위험 아님으로 판명)**: 정렬 토큰 어휘 불일치 의심 → tree.rs:1477-1485 parse_align_items 가 `Some("flex-start") | Some("start")` 양쪽 수용하므로 Inspector 의 start/center/end 를 이미 인식. NON_LAYOUT_PROPS_UPDATE 오등재 0건 (alignSelf/justifySelf/justifyItems/aspectRatio/gridAutoFlow/order 전부 미등재 = layout 영향으로 정상 판정).

### 위험 평가 — 누락 11건 (HIGH 4 / MED 5 / LOW 2)

프론트매터 issues 참조. HIGH 4건 요약:

1. **R6 이 계열 결함의 일부만 커버** — LAYOUT_STYLE_KEYS(60키) 에 gridAutoColumns/gridAutoRows/gridColumnStart/gridRowStart/overflowX/overflowY/order 도 미등재. E13/E14/E17 편입 시 동일 기전 재현.
2. **breakdown §3 이 round 1 m3 fix 미전파** — breakdown:140 이 여전히 LAYOUT_PROP_KEYS 라 서술하고 "캐시 무효화는 정상"이라 단언 → 본문:124 R6 과 정면 모순. Phase 2 실행자가 R6 을 누락.
3. **E6~E17 12군 Phase/Gate/Risk 미매핑** — 본문:49 가 "리뷰 시 확정"으로 위임한 상태 그대로.
4. **E17 잠복 결합이 G4 조건 밖** — 본문:46/breakdown:38 이 명시한 회귀 유발 조건이 게이트 미편입.

### H3 확정 권고 (본문:49 가 리뷰에 위임한 항목)

G0 계약(ADR:130 "신규 ADR fork 금지 — 본 ADR 표 갱신으로 흡수") + R3 의 기존 판단("분리 시 oracle·회귀 기준선 공유 불가로 중복 비용") 이 E6~E17 에도 동일 적용 → **분리 없이 Phase 재편** 권고:

| 발산                        | 권고 배치                            | 근거                                                                                                           |
| --------------------------- | ------------------------------------ | -------------------------------------------------------------------------------------------------------------- |
| E6 percent height           | **Phase 2 승격** (E1 동급)           | 도달 가능 + 근원 확정(ctx_for 단일축). CssValueContext 세로축 추가는 flex/block 공통 구조 변경이라 선행이 유리 |
| E8 reverse / E7 음수 margin | Phase 4 (flow)                       | 국소, 도달 가능                                                                                                |
| E12·E13·E14 grid            | Phase 3 흡수                         | E2 와 동일 근원 (grid.rs 정렬 0줄)                                                                             |
| E10·E11 relative/absolute   | 신규 Phase 4.5                       | 좌표계 축 독립                                                                                                 |
| E15 aspect-ratio            | Phase 5                              |                                                                                                                |
| E17 overflow                | **Phase 4 필수 동반** (G4 조건 편입) | E3 가 가려주는 잠복                                                                                            |
| E16 order                   | §Residual                            | UI 없음                                                                                                        |

### 종합 판정

- **구조**: PARTIAL (Gates 가 발견 발산의 30%만 커버 — FAIL 섹션 0)
- **코드 정합**: 11 검증 / 10 VERIFIED · 1 UNVERIFIED (breakdown fixture 수)
- **누락 위험**: 11건 (HIGH 4 / MED 5 / LOW 2)
- **권고**:
  - HIGH: R6 계열화 / breakdown §3 정정+R6 작업 편입 / E6~E17 Phase·Gate 매핑 / E17 을 G4 조건에 편입
  - MEDIUM: breakdown fixture 6→10(실측 5) / G1 에 N7~N10 편입 / 회귀 기준선에 2차 sweep 영역 편입 / README:430 갱신 / 교차표 상시 가드 메커니즘
  - LOW: 미소비 필드 수 표기 통일 / handleSelfAlignment :239→:235
  - **결론**: 수정 후 재리뷰

### 리뷰가 드러낸 메타 패턴

round 1 이 남긴 교훈("2차 출처를 원문 확인 없이 승계")이 round 1 자신의 fix 에서 재발했다 — 본문 5곳은 정정했으나 같은 오류가 살아있는 design breakdown 을 동반 sweep 하지 않았다(feedback-adr-body-design-dual-grep-sweep 패턴). 또한 R6 을 "justifySelf 캐시 키 누락" 단건으로 좁혀 기록한 것이 계열 점검(같은 구멍에 빠진 필드 7개)을 막았다. 순환 oracle 을 지적하는 ADR 이 문서 층에서 같은 구조(단일 지점 확인 → 계열 미확장)를 반복한 셈이다.
