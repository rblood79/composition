# ADR-916: 자체 단일 Rust 엔진 통합

## Status

Implemented — 2026-07-06

- Proposed — 2026-07-03
- Accepted — 2026-07-03 (Risk Threshold Check 대안 D 선정 + Gate G1~G5 완비 + 자체리뷰/codex 2 라운드 정정 반영 후 사용자 명시 confirm)
- Implemented — 2026-07-06 (**layout 엔진 Taffy 대체 live 완료** = ADR 본 목표 달성. 사용자 결정 "layout 완료 = 승격, 잔존은 endgame/후속". 실측 판정: 자체 Rust 엔진이 live builder 에서 layout batch 계산 중 — flag `USE_RUST_LAYOUT_ENGINE`+`UNIFIED_ENGINE` true, C-2a 런타임 배선 flip, dualRunLive 12/12 diff 0, tree_golden Chrome 실측 6/6, Gate G1/G2/G5 통과. **live behavior exercise**: builder 진입 → `[ADR-916] composition-engine WASM initialized` + 콘솔 에러 0 → Canvas(Skia 자체 엔진 layout) ↔ CSS Preview 시각 정합(Text/Badge/Progress 50%/InlineAlert/Button 양쪽 동일 배치). 검증: cargo test 233 PASS(lib 211+golden 15+tree_golden 6+doc 1) / type-check baseline 69 신규 0. **명시 잔존(승격 후 관리, 미완 아님)**: (a) R4 폴백 로직 이중화 HIGH — endgame(Taffy 물리 삭제)까지 dual-run CI 상시 관리, (b) 2-CAT propagation(P2-PROP) WASM 주입 배선 = 성능 최적화 후속 단위(layout 정확성 무관), (c) Phase 2 렌더 계층 2-C/2-D/2-E = "이관 대상 제외" 종결(구조적 차단, 미완 아님). endgame kill criteria ③ 충족 → **3/3 도달**(Taffy 물리 삭제 착수 가능, 별도 승인).)
- Phase 0 — 종료 2026-07-03 (**0-A seam 만 land**, flag 보류). 0-A = 레이아웃 엔진 주입 seam (`createLayoutEngine` factory 경유, 동작 무변). 0-B(LayoutScheduler worker offload)는 Phase 2-B 로 이연 — block/grid 가속기 dead + Phase 1/2 재편 대상이라 flip 앞선 dormant 배선 회피. 다음 진입점 = Phase 1 (Taffy 제거, HIGH 위험 — 별도 사용자 승인 필요)
- Phase 1 진입 (사용자 승인 2026-07-03, "1-D 하네스 먼저") — **1-D dual-run diff 하네스 비교 엔진 land** (`dualRunHarness.ts`, HC3 2단 판정, 계약 test 5/5). breakdown 순서를 1-A(flex.rs) 먼저에서 **1-D(검증 기반) 먼저**로 재배열 — 하네스가 flex.rs 의 유일한 검증 경로(R1 대응)이므로 산출물보다 선행. fixture golden 생성은 candidate 엔진(flex.rs) 착수 시점 이연. 다음 진입점 = 1-A `flex.rs` 신규 (HIGH 위험, ~2,000줄 CSS Flexbox 명세 — 별도 세션·사용자 승인)
- 1-A 착수 (사용자 승인 2026-07-03, "1-A 착수" + 첫 단위 = crate scaffold + 단일축 기본) — **`composition-engine` crate(taffy 의존 없음) + `flex.rs` 단일 라인 flex land** (`flex_layout_single_line`: justify 6종 / align 4종 / row·column / gap, `FLEX_FIELD_COUNT=16` 계약 = block_layout.rs flat f32 패턴 승계). M4(sub-group N≥3 confirm) 발동 → 대안 C big-bang 회피 위해 최소 검증 가능 단위부터. cargo test 8/8 PASS. **미구현**: grow/shrink 분배(§9.7)·wrap(§9.3)·align-content·flex-basis content — 다음 세션 (dual-run FAIL 이 fixture). WASM seam 배선은 flex/grid/block 완성 후 이연. 다음 진입점 = 1-A 잔여(grow/shrink/wrap) 또는 1-B grid (HIGH — 별도 세션·승인)
- 1-A 잔여 land (사용자 승인 2026-07-04, "flex-grow/shrink 분배와 wrap 구현… 착수 승인") — **`flex.rs` §9.7 flex-grow/shrink 분배 + §9.3 flex-wrap multi-line + align-content land**. `flex_layout` 신규 진입점(wrap/align-content 파라미터 추가), `flex_layout_single_line` 은 nowrap 특수 케이스로 위임(하위 호환). §9.7 = 반복 동결(freeze) 알고리즘: grow/shrink 방향 결정 → inflexible 동결 → min/max clamp violation 부호 합산으로 재분배 루프. §9.3 = outer main-size 누적 초과 직전 라인 분할(라인당 최소 1개). `FLEX_FIELD_COUNT` 16→17 (packed grow_shrink → 별도 flex_grow/flex_shrink 필드 분리). cargo test **21/21 PASS** (기존 8 회귀 + grow 4 / shrink 3 / wrap 4 / align-content 2), clippy 0. **미구현**: flex-basis:content intrinsic 자동측정·aspect-ratio·align-self·auto margin 흡수·nested BFC. WASM seam 배선은 여전히 flex/grid/block 완성 후 이연 (dormant 회피). 다음 진입점 = 1-B grid.rs (track sizing §11, HIGH — 별도 세션·승인)
- 1-C block.rs land (사용자 승인 2026-07-04, "1-C block.rs 먼저") — **기존 `block_layout.rs`(625줄, test 17) 를 `composition-engine/src/block.rs` 로 승계 이식 + 명세상 명확한 잔여 케이스 보강**. 이식 = 입력 계약(`FIELD_COUNT=19`) 그대로 (계약 통일은 Phase 2-B tree.rs 로 이연 — 지금 통일 시 dormant). **승계 vs 재작성 판정**: block 은 flex 와 달리 검증된 커널 존재 → 재작성은 Soft Constraint(WPT-파생 검증 자산 상실) 위배 → design freeze("block_layout.rs 승계") 정합으로 이식 선택 (사용자 관점 의문 아님). **잔여 케이스 (명세상 명확한 것만)**: (1) empty block **through-collapse chain** — 원본 커널이 `prev_margin_bottom = collapsed_self` 로 덮어써 앞선 sibling margin 유실 → CSS 2.1 §8.3.1 위반 발견 → `collapse(prev, self)` 누적으로 수정 (3연속 empty 통과 test), (2) 부모-자식 bottom collapse metadata 전파, (3) BFC 자식 bottom collapse 차단. clippy 수정 1건(child_w fit-content/explicit 동일 분기 병합). cargo test **40/40 PASS** (flex 21 + block 19: 승계 16 + 잔여 3), clippy 0. **미구현**: float/clear·writing-mode·BFC 내부 다단. 원본 block_layout.rs 무변(승계 후 개선은 새 crate 만). 다음 진입점 = 1-B grid.rs (track sizing §11, HIGH — 별도 세션·승인)
- 1-B grid.rs land (사용자 승인 2026-07-04, "1-B grid.rs (§11 track sizing) 착수… 승인") — **두 검증 자산을 `composition-engine/src/grid.rs` 로 통합 승계**: (a) `grid_layout.rs`(279줄, test 11) 의 px/fr/%/auto track 산술 + row-major cell positions 커널, (b) `GridLayout.utils.ts` 의 더 완전한 알고리즘 — `repeat(auto-fill/auto-fit, ...)` 동적 track 수 / `minmax(min, max)` (fr sentinel) / named grid-template-areas / `gridColumn·gridRow` span 배치. **승계 판정**: grid_layout.rs 는 auto=1fr 근사 + repeat/minmax/areas 미지원이었으나, GridLayout.utils.ts 에 실동작 검증된 JS 구현이 존재 → 재작성 아닌 두 자산 통합 이관 (block.rs 와 동일 패턴, design freeze "grid_layout.rs 승계 확장" 정합). **완결 공개 엔트리** `grid_layout(template_cols, template_rows, template_areas, placement_spec, ...)` 신규 = flex_layout/block_layout 과 대칭 (문자열 template → 최종 자식 bounds flat 배열). 이관 중 `resolve_grid_tracks` fr 분배를 원본 계약(`frSize * frVal`)대로 정정 + `parseFloat||1` 폴백(0fr→1fr) 재현. cargo test **64/64 PASS** (flex 21 + block 19 + grid 24: 승계 11 + repeat/minmax 5 + areas/span/place 5 + 완결 엔트리 3), clippy 0. **미구현**: subgrid·intrinsic track(min/max-content→0 폴백)·dense 역채움·baseline 정렬·`fit-content()` 함수. 원본 grid_layout.rs / GridLayout.utils.ts 무변. WASM seam 배선은 여전히 flex/grid/block dual-run 통과 후 이연(dormant 회피). **Phase 1 self-impl 3종(flex/block/grid) 완료** — 다음 진입점 = 1-D fixture golden 생성 + WASM batch 엔트리(`LayoutEngineAPI`) + seam 배선 + 1-E Taffy 제거 (HIGH — 별도 세션·승인)
- 1-D fixture golden land (사용자 승인 2026-07-04, "1-D fixture golden 생성 착수 승인, 배선까지 한 번에") — **세 완결 엔트리(flex_layout/grid_layout/block_layout) 전체 파이프라인을 CSS 명세 유래 기대값으로 회귀 고정** (`tests/golden.rs`, 현재 15 케이스). golden 방식 재정의: breakdown §1-D 의 "Chrome 실측 → 자동 생성" 은 자체 엔진의 **WASM 트리 배선(buildTreeBatch)** 을 전제 (dualRunHarness 가 트리 batch 계약 소비) → 트리 오케스트레이션은 Phase 2-B tree.rs 범위(G5 confirm 필수)라, Phase 1 scope 유지를 위해 **단일 컨테이너 단위 golden** (명세 정확 계산값, 정수 좌표 위주)으로 확보. golden 이 grid cell x/y leading gap 누락 승계 버그를 발견했고, 후속 처리에서 원본 JS live helper와 Rust 후보 엔진을 동시에 수정해 CSS↔Skia 분기를 피했다. block.rs test identity_op clippy 3건 정리. 후속 grid gap 처리 후 cargo test **79/79 PASS** (lib 64 + golden 15, ignored 0), clippy --tests 0. **배선(WASM 트리 batch + createLayoutEngine)은 미착수** — dualRunHarness 트리 계약이 tree.rs(2-B, HIGH·G5 confirm) 선행을 요구한다. 다음 진입점 = G5 scope confirm 후 Phase 2-A/2-B 진입 (HIGH — 사용자 승인)
- **Phase 1 부분 마감** (사용자 결정 2026-07-04, "(C) 여기서 멈추고 Phase 1 마감") — **self-impl 알고리즘 계층 완료, 배선/Taffy 제거 미착수**. land: 1-D dual-run 하네스 비교 엔진 + 1-A flex.rs(§9.7 grow/shrink·§9.3 wrap·align-content) + 1-C block.rs(승계 이식 + through-collapse) + 1-B grid.rs(track sizing §7·placement §8) + 1-D 단일 컨테이너 golden. **미착수 (배선 후 가능)**: WASM 트리 batch 엔트리(`buildTreeBatch`) + `createLayoutEngine` 배선 = tree.rs(2-B) 트리 오케스트레이션 선행 요구 → 사실상 Phase 2 진입, 1-E(Taffy dependency 제거)는 배선·dual-run 안정 후에만 가능. **ADR Status = Accepted 유지** (Phase 1 전체 미완 → Implemented 승격 아님). 자체 엔진은 seam 미배선 순수 crate 로 존재 — live builder 영향 0, 현행 Taffy 경로 그대로 가동. **다음 진입점 = Phase 2 진입 (G5 scope confirm 필수, HIGH — 별도 세션·사용자 승인)**. 배선·1-E 는 Phase 2-A(style.rs)/2-B(tree.rs) 완료 후 트리 계약이 생겨야 착수 가능.
- Grid gap 승계 버그 처리 완료 (사용자 승인 2026-07-04, "Phase 2 진입 가능하게 grid gap JS + grid.rs 동시 수정") — `GridLayout.utils.ts` 와 `composition-engine/src/grid.rs` 의 leading gap offset 조건을 동시에 `colStart-1`/`rowStart-1` 로 정정. `tests/golden.rs` 의 ignored fixture 해제 + row/column leading gap fixture 추가(15 golden), JS live helper 단위 테스트 추가. 검증: composition-engine cargo test **79/79 PASS** (lib 64 + golden 15, ignored 0), clippy --tests 0, `GridLayout.utils.test.ts` PASS, type-check PASS. **G5 confirm 착수 조건 충족**: grid gap 차단은 해소됐고, Phase 2 code 진입은 여전히 G5 사용자 scope confirm 이 필요.
- **Phase 2 진입 (G5 confirm)** (사용자 결정 2026-07-04, "본 ADR 내 2-A/2-B 선착수") — Phase 2 scope 를 **본 ADR 내 2-A/2-B 선착수**로 확정 (후속 ADR fork 아님, breakdown 안전 기본값). 2-B tree.rs 완료 실측 후 2-C/2-D/2-E 재판정. G5 gate 통과.
- **2-A style.rs 첫 단위 land** (사용자 승인 2026-07-04, "세 실측 결정 승인, 첫 단위 착수") — **CSS 값 산술 파서 커널 이식** (`composition-engine/src/style.rs` 신규). 착수 전 실사로 breakdown 대비 실측 3건 확정·정정: (1) **style.rs 위치** — 원안 "composition-layout/style.rs 기반" 은 stale(Taffy 0.10 종속 폐기 예정 crate) → `composition-engine` 신규가 정합(Phase 1 crate 결정 자연 승계, fork 4질문 대상 아님). (2) **이관 경계** — `cssValueParser.ts` 의 DOM 의존(`getComputedStyle(documentElement)`, var()/토큰) 은 JS 잔류, 순수 산술만 Rust — var() 선치환 후 산술을 Rust 에 전달(flat f32 철학). (3) **첫 단위** — 4,700줄 통이관 불가(M4) → 값 산술 커널만. 이식 함수: `resolve_css_size_value`(진입점) / `resolve_unit_value`(px/rem/em/vw/vh/vmin/vmax/in/cm/mm/pc/pt/ch/ex/%) / `resolve_calc`(재귀 하강 + tokenize) / clamp·min·max / env(safe-area 4종→0) / split_css_function_args. 계약: `number|undefined`→`Option<f32>`, `CSSValueContext`→`CssValueContext`(스칼라, variableScope 제외), intrinsic→센티넬 f32(FIT/MIN/MAX_CONTENT), `parse_leading_f32` 로 JS parseFloat 근사. rem-before-em / %는 container 필요 / calc 0-division None 등 edge case 승계. cargo test **106 PASS** (lib 90=기존 64+style 26 / golden 15 / doc-test 1), clippy --tests 0, 원본 JS 산술 계약 값 대조 일치. seam 미배선 순수 함수 → live 영향 0(dual-run/cross-check 는 트리 배선 2-B 이후). 당시 **미이식(후속 2-A 단위)**: font/border shorthand → cssResolver → taffyDisplayAdapter → implicitStyles(2,440줄). font/border shorthand 는 아래 단위에서 처리 완료.
- **2-A style.rs shorthand 단위 land** (사용자 승인 2026-07-04, "1") — **font/border shorthand 분해 이식**. `parse_font_shorthand` 는 JS `parseFontShorthand` 계약을 승계해 `fontStyle/fontWeight/fontSize/lineHeight/fontFamily` 를 optional string 으로 분해하고 `normal` 은 결과에서 제외, quoted family/comma family 를 보존한다. `parse_border_shorthand` 는 JS `parseBorderShorthand` 계약을 승계해 순서 무관 width/style/color 를 분해하고 width 는 JS `parseFloat` 근사(`1.5rem`→`1.5`), 기본값은 `{width:0, style:"none", color:"#000000"}`. seam 미배선 순수 함수 → live 영향 0. 검증: shorthand RED→GREEN, cargo test **112 PASS** (lib 96 / golden 15 / doc-test 1), clippy --tests 0. **남은 2-A 단위**: `cssResolver.ts` 캐스케이드 → `taffyDisplayAdapter.ts` display 매핑 → `implicitStyles.ts` 데이터 주도 매핑. 다음 진입점은 `cssResolver.ts` 캐스케이드 최소 계약 또는 보류 (HIGH — 별도 승인).
- **2-A cascade.rs 단위 land** (사용자 승인 2026-07-04, "승인") — **`cssResolver.ts` 자기완결 순수 계층 이식** (`composition-engine/src/cascade.rs` 신규). 착수 전 실사로 store/DOM 의존이 `getRootComputedStyle()`(`useThemeConfigStore.getState()`) 한 곳에만 격리됨을 확인 → 순수 로직/데이터 테이블만 이식. 이식: `is_inheritable_property`(상속 19종) / `css_initial_value`(초기값 맵) / `resolve_cascade_keyword`(inherit/initial/unset/revert → `CascadeResult::Inherit|Value`) / `resolve_current_color`(currentColor 단어 경계 치환, `\bcurrentColor\b/gi` 재현) / `resolve_font_variant_features`+`DEFAULT_FONT_FEATURES`(font-variant→OpenType) / `resolve_logical_properties`(논리→물리, LTR, shorthand 2값 분리 + 물리 우선). 계약: `Record<string, string|number>`→`BTreeMap<String, CssValue>`(`CssValue::Str|Num` enum), INHERIT_SENTINEL→`CascadeResult::Inherit`, 논리 속성 반복 순서는 정의 순서 상수로 재현. **미이식**: `getRootComputedStyle`/`ROOT_COMPUTED_STYLE`(store 의존 JS 잔류), `resolveFontStretchWidth`(`@composition/specs` FONT_STRETCH_KEYWORD_MAP = ADR-091 spec SSOT 의존 → Rust crate 의 spec 참조 계약 미정으로 제외, 이중화 회피), `resolveStyle` 본체(조립 단위). 검증: cargo test **130 PASS** (lib 114=96+cascade 18 / golden 15 / doc-test 1), clippy --tests 0, 원본 JS 로직 대조(currentColor/cascade/font-variant) 전 케이스 값 일치. seam 미배선 순수 함수 → live 영향 0. **남은 2-A 단위**: `resolveStyle` 본체 조립 + `resolveFontStretchWidth`(spec 참조 계약 확정 후) → `taffyDisplayAdapter.ts` → `implicitStyles.ts`(2,440줄). 다음 진입점 = 위 잔여 단위 또는 2-B tree.rs (HIGH — 별도 승인).
- **2-A display.rs 단위 land** (사용자 승인 2026-07-04, "승인") — **`taffyDisplayAdapter.ts` 자기완결 순수 display 문자열 계층 이식** (`composition-engine/src/display.rs` 신규). 착수 전 실사로 tag/node 의존 함수(`getElementDisplay`=`INLINE_BLOCK_TAGS`, `toTaffyDisplay`/`needsBlockChildFullWidth` childElements, `VERTICAL_ALIGN_MIDDLE_TAGS`)와 순수 문자열 함수를 분리 — cascade `resolveFontStretchWidth`(spec SSOT) 제외와 동일 패턴으로 tag 도메인 의존 제외. 이식: `parse_display`(CSS Display Level 3 이원 구조 9종 매핑 + block 폴백) / `display_to_string`(역변환) / `classify_child_display`(block/inline/none, inline-flex/grid→block) / `blockify_display`(CSS L3 blockification, outer:inline→block inner 유지) / `is_inline_level`. `Display{outer, inner}` → `OuterDisplay`/`InnerDisplay` enum. 검증: cargo test **137 PASS** (lib 121=114+display 7 / golden 15 / doc-test 1), clippy --tests 0, 원본 JS 로직 대조(blockify 8 / classify 7 / roundtrip 9) 전 케이스 값 일치. seam 미배선 순수 함수 → live 영향 0. **미이식**: tag/node 의존 함수 → tree.rs(2-B) 노드 계약 또는 spec 참조 계약 확정 후. 다음 진입점 = `implicitStyles.ts`(2,440줄) 또는 2-B tree.rs (HIGH — 별도 승인).
- **2-B 착수 전 실사** (사용자 결정 2026-07-04, "B — 2-B tree.rs 진입") — breakdown "6-step DFS 일체화" 서술 대비 실측 gap 확정·기록(commit `0dbef44ae`). DFS 상단 3-step(`resolveStyle`=`getRootComputedStyle()` store 의존 + `applyImplicitStyles` tag/spec + `enrichWithIntrinsicSize` @composition/specs·propagationRegistry)은 **JS 잔류**(2-A 순수 계층 격리 이유). Rust 이관 표면은 `PersistentTaffyTree` 가 `LayoutEngineAPI` 를 호출하는 **하단 batch 계약**(`buildTreeBatch`→`computeLayout`→`getLayoutsBatch`)뿐 — payload `node.style` 은 상단이 이미 순수화한 TaffyStyle 레코드. gap 은 Phase 0 inventory 정밀화(서술 정정)로 흡수, 새 ADR fork 아님(M3). scope 정의 fork 판단은 AskUserQuestion 으로 surface(전제·관점 layer). **미착수** — 사용자 confirm 대기.
- **2-B tree.rs 단위 1 land** (사용자 승인 2026-07-04, "실측 하단만 착수" + "층별 점진" 기본값 채택) — **트리 오케스트레이션 계층 skeleton** (`composition-engine/src/tree.rs` 신규, taffy_bridge.rs `TaffyLayoutEngine` batch 계약 대응). 착수 전 실사로 **아키텍처 gap 확정**: flex/block/grid.rs 는 모두 "단일 컨테이너 + 자식 flat f32 → 자식 위치" 1-depth 커널인데 batch 계약은 N-depth 트리 상호의존 해결 필요 → 그 사이 오케스트레이션(handle 관리 + intrinsic + placement + dispatch)을 층별 점진(2-A 최소 검증 단위 패턴)으로 분할. **단위 1 scope**: `LayoutTree`(handle `nodes: Vec<Option<TreeNode>>` + `free_list` 재활용, taffy_bridge `alloc_handle`/`resolve` 대응) + `build_tree_batch`(post-order JSON 파싱·저장·handle 배열, child index 범위 검증 = forward-reference 거부) + `get_layouts_batch`(flat `[x,y,w,h,...]`, 무효 handle `[0,0,0,0]`) + 증분 API(create/update_style/set_children/remove_node/mark_dirty/clear/node_count). `compute_layout` 은 **leaf-only** — 각 노드 자기 크기(width/height, style.rs `resolve_css_size_value` 재사용)만 해결, 자식 배치·좌표는 (0,0). `NodeStyle` = StyleInput 전체 스키마(camelCase 계약 정합). **자식 배치는 단위 2(post-order intrinsic)/3(top-down placement+display dispatch)/4(dirty 추적)로 명시 분리**. 검증: cargo test **152 PASS** (lib 136=121+tree 15 / golden 15 / doc-test 1), clippy --tests 0, taffy_bridge.rs batch 계약 대조(child index 치환·flat shape·handle 재활용) 일치. seam(`createLayoutEngine`) 미배선 순수 Rust → live 영향 0. **다음 진입점** = 단위 2 post-order intrinsic 측정 또는 단위 3 placement+dispatch (HIGH — 별도 승인).
- **2-B tree.rs 단위 2 land** (사용자 승인 2026-07-04, "승인") — **post-order flex solve** (`tree.rs` `compute_layout` 확장). 착수 전 실사로 **단위 경계 재정의**: flex/block/grid.rs 는 컨테이너 자기 크기를 반환하지 않으므로(자식별 `[x,y,w,h]` 만) height:auto 부모 intrinsic 은 자식을 먼저 배치(커널 호출)해 bounding box 를 봐야 나옴 → **intrinsic ↔ placement 물리적 분리 불가**. 원안 "단위 2=intrinsic / 단위 3=placement" 를 "post-order 트리 solve" 한 단위로 병합, 내부를 display 별(flex → block/grid) 최소 검증층으로 재분할(승인된 옵션 A 내부 단위 경계 조정 — fork 아님). **단위 2 scope**: `compute_layout` = post-order `solve_node`(leaf/비-flex 는 자기 크기, flex 는 `solve_flex`) — 자식 재귀 solve → `write_flex_item`(NodeStyle → flex flat f32: direction 별 width↔main·height↔cross 매핑, padding/border 축 합산, min/max 논리축, content_main/cross=자식 solve 결과) → `flex::flex_layout` → 자식 좌표 반영 + bounding box 로 컨테이너 content 크기(height:auto sentinel) 도출. CSS 키워드 → flex.rs u8 매핑(`parse_flex_direction`/`parse_justify_content`/`parse_align_items`/`parse_align_content`/`parse_flex_wrap`, flex.rs 상수 리터럴 대조). **발견(flex.rs 알려진 제약, scope 밖)**: `flex.rs` `ALIGN_STRETCH` 가 자식 명시 cross size 를 무시하고 컨테이너 cross 로 stretch(CSS 명세 위반 — Phase 1 flex.rs 버그). tree.rs 는 건드리지 않고 테스트를 `align-items:flex-start` 로 우회, flex.rs 수정은 Phase 1 후속. **미포함(다음 단위)**: block/grid dispatch(단위 3 — 현재 비-flex 컨테이너는 자기 크기만·자식 미방문), row-reverse/column-reverse, flex-basis:content/px, 증분 dirty(단위 4). 컨테이너 크기 = bounding box 근사 — Taffy formatting context 정확값 정합은 seam 배선 후 dual-run 에서 검증. 검증: cargo test **158 PASS** (lib 142=136+flex solve 6 / golden 15 / doc-test 1), clippy --tests 0. seam 미배선 → live 영향 0. **다음 진입점** = 단위 3 block/grid dispatch (HIGH — 별도 승인).
- **2-B tree.rs 단위 3-a land** (사용자 승인 2026-07-04, "단위 3 (block/grid dispatch) — 승인") — **block dispatch** (`tree.rs` `solve_block` 추가). 착수 전 실사로 **단위 3 재분할**: flex/block/grid.rs 세 커널 계약 비대칭 확인 — flex(17필드,논리축)·block(19필드,물리축)은 "자식 flat f32 → 위치" 커널이라 solve 패턴 동일하나 grid 는 근본적으로 다름(자식 flat 없음, `template_cols/rows/areas`+`placement_spec` 문자열만 받아 트랙 산술). 따라서 block(3-a, flex 와 계약 근사) 먼저 + grid(3-b, 문자열 어댑터) 분리(승인된 옵션 A 내부 경계 조정 — fork 아님). **단위 3-a scope**: `solve_node` 의 `ContainerDisplay::Block` 분기 → `solve_block`(자식 재귀 solve → `write_block_item`= block flat f32 19필드 물리축: display code(block/inline-block)/margin 4-way/pad*border v·h 축 합/min·max/content_w·h → `block::block_layout(data, w, h, false, false, 0)` → 자식 좌표 반영 + bounding box → 컨테이너 크기). `classify_container_display` 확장: flex/inline-flex→Flex, grid/inline-grid→Other(3-b), 그 외(block/inline-block/**미설정**)→Block(`_hasChildren` 컨테이너는 상단 blockify → 미설정=block 기본). margin collapse/auto-width stretch/fit-content 는 block.rs 내부 처리. **미포함(다음 단위)**: grid dispatch(단위 3-b — grid 컨테이너 현재 자기 크기만·자식 미방문), 부모-자식 margin collapse 전파(block.rs OUT trailing metadata 2필드 미소비, `can_collapse*_=false`BFC 격리 가정 — tree.rs 레벨 배선은 별도 단위), inline-block baseline 전달, BFC 감지(bfc_flag=0 고정), 증분 dirty(단위 4). **참조 자산 대조**: taffy_bridge.rs 는 Taffy 내장 solver(TaffyTree) 사용 — 자체`block*layout`미호출 →`can_collapse*_=false` 가정은 batch 계약과 무관(정합 위반 없음). 검증: cargo test **164 PASS** (lib 148=142+block dispatch 6 / golden 15 / doc-test 1), clippy --tests 0. 검증층 = block vertical stack / 자식 margin collapse / auto-width stretch / explicit px+padding border-box / height:auto intrinsic=stacking 합 / display 미설정→block / grid 자기 크기만(3-b 전). seam 미배선 → live 영향 0. **다음 진입점** = 단위 3-b grid dispatch (HIGH — 별도 승인).
- **2-B tree.rs 단위 3-b land** (사용자 승인 2026-07-04, "승인") — **grid dispatch** (`tree.rs` `solve_grid` 추가). flex/block/grid dispatch 완성. grid 는 계약이 근본적으로 다름 — 자식 flat 을 안 받고 `template_cols/rows/areas`+`placement_spec` **문자열**만 받아 트랙 산술로 셀 배치(자식 크기 = 트랙 크기, intrinsic track 미측정). **tree.rs 어댑터 3요소**: (1) `join_tracks`(NodeStyle `grid_template_columns: Vec<String>` track array → space-join `"1fr auto"`, grid.rs `tokenize_template` 재분해 무손실), (2) `combine_grid_line`(NodeStyle 은 taffy_bridge 처럼 `gridColumnStart`+`End` 분리 값 보유 → grid.rs `parse_grid_line` 결합 형식 `"{start} / {end}"` 재조립, `normalize_grid_line_part` 로 auto/미설정은 None), (3) `build_grid_placement_spec`(자식들을 `area_name|grid_column|grid_row` 파이프+개행 직렬화, 전부 auto 면 빈 문자열). `solve_grid` = 어댑터 → `grid::grid_layout` → 셀 bounds → 각 자식 셀 크기로 재귀 solve(셀 안 flex/block 컨테이너 배치) → 셀 좌표 반영 + bounding box → 컨테이너 크기. `classify_container_display` grid/inline-grid→Grid. **발견(grid.rs 알려진 제약, scope 밖)**: intrinsic track 미측정(min/max-content→0), 음수 line index 0 clamp(미지원), dense/subgrid/baseline 미구현 — grid.rs Phase 1-B scope. **미포함(다음 단위)**: `justify-self`/`align-self` 셀 내 정렬(현재 자식 셀 stretch), `grid-template-areas` named area(NodeStyle 필드 없음 — Skia 숫자 line), grid-auto-flow column/dense, 증분 dirty(단위 4). **참조 자산 대조**: fullTreeLayout payload 직렬화(`gridColumnStart`=`String(...)` 개별 필드 / `gridTemplateColumns`=`coerceGridTrack`→track array)가 NodeStyle 계약과 1:1 일치 — 어댑터 상류 정합. 검증: cargo test **169 PASS** (lib 153=148+grid dispatch 6−삭제 1 / golden 15 / doc-test 1), clippy --tests 0. 검증층 = 2열 auto-placement / gap 셀 좌표 / gridColumn span / fr 분배 / grid 셀 안 flex 재귀 solve / height:auto intrinsic=셀 bounding box. seam 미배선 → live 영향 0. **다음 진입점** = 단위 4 증분 dirty 추적 (HIGH — 별도 승인). 단위 4 land 시 `LayoutEngineAPI` batch 계약 완비 → seam 배선(dual-run Taffy self-diff 0) 단계.
- **2-B tree.rs 단위 4 land** (사용자 승인 2026-07-04, "승인") — **증분 dirty 추적** (`tree.rs` `compute_layout`/증분 API 확장). taffy 의 "dirty 조상 자동 전파" 계약(taffy_bridge.rs:890-897 주석: "set_style/set_children 이 mark_dirty 내부 호출 + dirty 를 조상까지 자동 전파") 이식. **이관 4요소**: (1) `TreeNode` 에 `parent: Option<usize>` 추가 + `set_children`/`build_tree_batch` 가 자식 parent 배선(조상 전파 경로), (2) `update_style`/`set_children`/`mark_dirty` → `propagate_dirty(handle)` 로 변경 노드부터 root 까지 조상 dirty 마킹(이미 dirty 노드 만나면 조기 종료 — 그 조상은 이미 dirty 이므로 누락 없음), (3) `solve_node` 진입 시 `subtree_has_dirty(handle)` false 면 저장된 `layout.width/height` 를 반환값으로 재사용하고 재귀 생략(dirty 서브트리만 재계산 — clean sibling 은 skip 되나 저장 크기가 부모 flex/block/grid 배치 입력으로 정확 반영), (4) `LayoutTree.last_compute: Option<(root,avail_w,avail_h)>` — available 이 직전과 다르면 `mark_subtree_dirty(root)` 로 skip 전면 무효화(%/auto stale 방지, taffy layout cache 대비 캐시 없는 보수적 갈음), `clear`/`remove_node` 는 `last_compute=None` 무효화(handle 재발급 stale skip 차단). **참조 자산 대조**: taffy 의 3 incremental 테스트(test_mark_dirty_incremental=값 반영+값 보존, test_mark_dirty_add_remove_child=add/remove reflow) 관찰 계약 동형 커버 — taffy 는 column y좌표로 검증하나 자체는 flex column+height:auto(-1 sentinel) 미해결 영역(flex.rs main available 음수 미처리, Phase 1 후속) 우회 위해 row x좌표로 동형 검증(관찰 계약=최종 layout 정확성 동일). **미포함(다음 단계)**: taffy 수준 노드별 available-space 캐시(현재 root-level 비교 보수적 무효화), flex column height:auto intrinsic(flex.rs Phase 1 후속). 검증: cargo test **177 PASS** (lib 161=153+단위 4 신규 8 / golden 15 / doc-test 1), clippy --tests 0. 검증층 = update_style 조상 전파+반영(100→200) / explicit mark_dirty 값 보존 / set_children add·remove reflow / clean sibling skip+크기 재사용 / available 변경 % 재계산(width 200→400) / 동일 avail 재호출 값 불변 / clear 후 stale skip 없음. seam 미배선 → live 영향 0. **tree.rs 오케스트레이션 4 단위(1/2/3-a/3-b/4) 완료 → `LayoutEngineAPI` batch 계약 완비.** **다음 진입점** = seam 배선(`createLayoutEngine` flag 전환) + dual-run(Taffy self-diff 0) 검증 (HIGH — 1-E Taffy 제거 전제, 별도 승인).
- **2-B seam 배선 (A) WASM 바인딩 land** (사용자 승인 2026-07-04, "승인") — **자체 crate WASM wrapper** (`composition-engine/src/wasm.rs` 신규). 착수 전 실사로 **"seam 배선"이 3 sub-scope 로 분해됨을 확정**: (A) WASM 바인딩(live 0, 선행 필수) → (B) dual-run self-diff 측정(live 0, 검증) → (C) `createLayoutEngine` flag 전환(**live 엔진 교체**, (B) self-diff 0 통과가 전제). (C)는 (B)라는 경험적 gate 에 의존하므로 별도 게이트 — [[feedback-no-dormant-foundation-ahead-of-flip]] 차단 우선 평가 후 최소 표면 (A)만 착수(사용자 AskUserQuestion 무응답 → 차단 메모리 근거 안전 기본값). **(A) scope**: `LayoutEngine` wrapper struct(`#[wasm_bindgen]`)가 내부 `tree::LayoutTree` 를 감싸 JS `LayoutEngineAPI`(layoutBridge.ts) 16 메서드 노출 — `taffy_bridge.rs::TaffyLayoutEngine` 과 동일 시그니처라 같은 seam 에 교체 가능하게 꽂힘. 설계: (1) `#[cfg(target_arch = "wasm32")]` 게이트(lib.rs) → native cargo test 무영향(JsValue non-wasm32 panic 회피, taffy*bridge.rs:1209-1210 동일 이유), (2) wrapper 얇게(레이아웃 로직은 `LayoutTree`, wrapper 는 JSON→NodeStyle 역직렬화 + flat f32 직렬화 + 에러→JsValue), (3) binary protocol 미구현(`hasBinaryProtocol`=false → JS JSON 경로 fallback, `buildTreeBatchBinary` Err stub). **참조 자산 대조**: taffy_bridge.rs wasm-bindgen 패턴(constructor/js_name/`Box<[f32]>` flat/`Result<*,JsValue>`) 승계 + `getLayoutsBatch` flat = JS 가 handle 순서 슬라이스해 Map 재구성(persistentTaffyTree.ts). **검증**: native cargo test **177 PASS**(lib 161 무회귀 / golden 15 / doc-test 1), native clippy --tests 0(wasm 게이트로 회귀 0), **wasm32 컴파일 성공**(`cargo check --target wasm32-unknown-unknown`— wrapper 가 실제 wasm 타겟에서`#[wasm_bindgen]` 포함 에러 없이 컴파일 = (A) 핵심 exercise 증거), wasm32 clippy 0. **seam 미배선 유지** — wrapper 존재 ≠ flag 전환(`createLayoutEngine` flag true 경로 여전히 fallback). live builder 영향 0. **다음 진입점** = (B) wasm-pack 산출물 생성(`pkg/`.wasm + 바인딩) →`dualRunHarness.runDualLayout`(candidate=본 wrapper vs reference=Taffy) self-diff 측정 (HIGH — 별도 승인). (B) 결과가 (C) flag 전환 가능 여부 결정(flex column height:auto 등 미해결 영역이 실제 배치 diff 를 낼 수 있음).
- **2-B seam 배선 (B1) wasm-pack 빌드 검증 land** (사용자 승인 2026-07-04, "승인") — **wasm-pack 산출물 생성 + export 검증**. 착수 전 실사로 **(B) "dual-run 측정"이 다시 (B1)/(B2) 로 분해됨을 확정**: `dualRunHarness.test.ts` 는 현재 **실제 WASM 엔진을 로드하지 않고** mock(`makeEngine` + 주입 `layoutFn`)으로 diff 산술만 검증 — 자체 엔진 vs 실제 Taffy 비교 경로는 미구축([[feedback-infra-exists-vs-wired-consumption-path]]: 하네스 코드 존재 ≠ 실제 엔진 소비 경로 존재). breakdown 의 (B) 정의("wasm-pack 산출물 → runDualLayout")는 JS↔WASM 통합 테스트 인프라(두 엔진 실제 로드)를 전제하나 그게 미확립. 사용자 AskUserQuestion(scope 질문) 무응답 → [[feedback-infra-exists-vs-wired-consumption-path]] + execute-adr-surface-minimization 근거 최소 표면 **(B1) wasm-pack 빌드만** 착수(통합 인프라 (B2)는 분리, native Rust dual-run 은 Cargo.toml "taffy dependency 추가 금지" 충돌로 배제). **(B1) scope**: `wasm-pack build packages/composition-engine --target bundler --out-dir pkg` → `pkg/composition_engine.{js,d.ts}` + `composition_engine_bg.wasm`(220KB) 생성. **export 검증**: `.d.ts` 가 `export class LayoutEngine` + 16 메서드(`buildTreeBatch(nodes_json:string):Uint32Array` / `computeLayout` / `getLayoutsBatch(handles:Uint32Array):Float32Array` / `isAvailable():boolean` 등, camelCase js_name 변환 정상) 노출 = (A) wrapper 가 실제 JS consumable 산출물을 냄을 확증. raw 반환(`Uint32Array`/`Float32Array`)은 taffy_bridge 동일 형식 → persistentTaffyTree.ts 어댑터가 `LayoutEngineAPI`(number[]/Map) 로 흡수. flex/block/grid free function(`flex_layout`/`block_layout`/`grid_layout`)도 함께 export(golden 소비). **pkg 커밋 제외**: `.gitignore` 에 `pkg/` 사전 등록(taffy `wasm-bindings/pkg` 관례 동일 — 빌드 산출물, Vite 번들 시 재생성). (B1)은 코드 변경 0(문서만) — 빌드 검증 단계. **seam 미배선 유지**. live builder 영향 0. **다음 진입점** = (B2) JS↔WASM 통합 테스트 인프라(vitest/node 에서 자체 pkg + 실제 Taffy 동시 로드 경로 신규 — taffy 는 `--target bundler`라 vitest 직접 로드 미확립) + 실제 batch self-diff 측정 (HIGH — 별도 승인). (B2) self-diff 결과가 (C) flag 전환 가능 여부 결정.
- **2-B seam 배선 (B2) JS↔WASM 통합 인프라 + 실제 self-diff land** (사용자 승인 2026-07-04, "승인") — **두 실제 WASM 엔진 dual-load + self-diff 측정**. 로딩 전략 AskUserQuestion 무응답 → [[feedback-no-dormant-foundation-ahead-of-flip]] + surface-minimization 근거 **옵션 1(vitest 에 vite-plugin-wasm)** 선택: 런타임 앱과 **동일 `--target bundler` pkg** 를 그대로 로드 → self-diff 가 실배선 산출물 측정(최고 신뢰도 + 최소 표면). nodejs 타겟 별도 빌드(옵션 2)는 런타임과 다른 산출물 → 신뢰도 갭 + 빌드 파이프라인 부수 표면으로 배제, 순수 커널만(옵션 3)은 "두 엔진 실제 비교"라는 (B2) 본질 미달로 배제. **실사 확정**: `dualRunHarness.ts::runDualLayout` 은 실제 `LayoutEngineAPI` 주입 시 그대로 동작(mock 인 것은 `.test.ts` 뿐), 두 pkg 모두 `--target bundler`(loader `import * as wasm from "./..._bg.wasm"`)라 vitest 직접 로드 불가였고 기존 테스트 실제 WASM 로드 0건 → (B2)가 미구축 경로. **최소 스파이크 실증**(infra-exists≠wired 를 wasm 로딩에 적용): `vite-plugin-wasm`(apps/builder 이미 설치)만으로 자체 pkg 로드+leaf compute 성공(top-level-await 불필요 — 자체 loader 동기). **구현**: (1) `dualRunEngines.ts` 신규 — raw wasm-bindgen → `LayoutEngineAPI` 어댑터 2개(`adaptSelfEngine` camelCase+raw 변환 / `adaptTaffyEngine` snake→camel+raw 재구성, 런타임 taffyLayout.ts 는 전역 getRustWasm 의존이라 vitest 미가용 → fixture 전용 별도), (2) `dualRunLive.test.ts` 신규 — 두 실제 WASM 을 `runDualLayout` 에 주입, (3) `vitest.config.ts` 에 `vite-plugin-wasm` 등록(`.wasm` import 없는 기존 테스트엔 no-op). **검증**: dualRunLive **4/4 PASS** — 자체 self-diff diff 0(실 WASM 하네스 정확성) / 자체 leaf 실측 30×20·40×20·flex row x=0/30 / Taffy 동일 batch 로드 / **자체 vs Taffy 실전 비교 HC3 통과**(flex row fixed 두 엔진 동일 = live exercise 증거). dualRunHarness.test.ts(mock) 5/5 무회귀. `tokenConsumerDrift.test.ts` 2 snapshot 실패는 **plugin 없는 baseline 동일 확증**(ADR-081 인프라, 무관 사전 실패 — plugin 회귀 아님). type-check PASS(baseline 69, 새 violation 0). **seam 미배선 유지** — 어댑터는 테스트 fixture 전용, createLayoutEngine 배선 안 함. live builder 영향 0. **다음 진입점** = (C) `createLayoutEngine` flag 전환 (live 엔진 교체, HIGH — 별도 승인). 전제: (B2) self-diff 가 flex row fixed 통과했으나 자체 미해결 영역(flex column height:auto / grid intrinsic track)이 실 catalog 배치 diff 를 낼 수 있음 → (C) 착수 시 넓은 실전 fixture 로 dual-run 확장 후 diff 0 확인이 flag 전환 전 선결.
- **2-B seam 배선 (C-1) 실전 catalog dual-run 진단 land — flag 전환 차단 확정** (사용자 승인 2026-07-04, "(C) 승인"; batch 소스 질문 → "수작 대표 fixture batch 확장" 선택) — **실사 결과 (C)가 (C-1) 실전 dual-run diff 측정 → (C-2) diff 0 통과 시만 flag 전환 으로 분해됨을 확정**(승인된 (C) 내부 순서 — fork 아님). 실전 batch 는 store→spec→factory→DFS 런타임 생성이라 캐처 fixture 없음 → catalog containerStyles 실사(flexDirection:column 다수 / grid 6 / block)로 **대표 3 패턴 + height:auto** fixture 수작 확장. **자체 crate 는 apps/builder dependency 미등록**(런타임 import 경로 없음 — flag 전환 시 자체 pkg 로드 배선도 (C-2) 선결). **측정 결과 (dualRunLive.test.ts (C-1) describe)**: block height:auto → **diff 0**(flag 준비됨) / flex column height:auto → **h=0 붕괴**(자체가 avail_h=-1 sentinel 을 flex main available 로 받아 자식 shrink → 자식 h 0·y 겹침, root h 0 vs Taffy 70) / grid height:auto → **셀 h +50**(자체 grid.rs 셀 높이를 intrinsic 대신 available 로 채움 → 100 vs Taffy 50, intrinsic track 미측정). **결론: (C-2) flag 전환 불가** — [[feedback-no-dormant-foundation-ahead-of-flip]] 게이트상 실전 catalog 다수 패턴(flex column/grid height:auto)이 diff 를 내므로 flag 를 켜면 live 회귀. **flex.rs §9.7 main available 음수 처리 + grid.rs intrinsic track 측정이 flag 전환 전 선결 과제로 확정**. block 경로는 이미 정합. **검증**: dualRunLive **7/7 PASS**(기존 4 + C-1 3: block diff 0 통과 / flex column diff 발생 확증 height 필드 / grid diff 발생 확증 height 필드), type-check PASS(baseline 69, 새 violation 0). 진단 테스트가 선결 경계를 못박음 — flex.rs/grid.rs 수정 후 `toBe(false)` 가 `toBe(true)` 로 뒤집히면 flag 전환 준비 신호. `createLayoutEngine` flag 경로는 fallback 그대로 유지(코드 변경 0). **seam 미배선 유지**. live builder 영향 0. **다음 진입점** = flex.rs §9.7 main available 음수(height:auto column intrinsic) + grid.rs intrinsic track 측정 (Phase 1 flex.rs / Phase 1-B grid.rs 후속 — HIGH, 별도 승인). 두 선결 완료 후 (C-1) 진단 diff 0 전환 확인 → (C-2) flag 전환 재평가.
- **flag 전환 선결 #1 — flex.rs main-negative + ALIGN_STRETCH 정정 land** (사용자 승인 2026-07-04, "승인"; 순서 질문 무응답 → flex.rs 먼저: catalog flex column 다수 + h=0 완전 붕괴 심각도 + flex.rs:278 단일 진입점 표면 집중). **근원 3곳 (TDD RED→GREEN)**: (1) `resolve_flexible_lengths:278` `growing = hypothetical_sum < available_main` 가 available=-1(height:auto sentinel)을 항상 shrink 로 오판 → remaining 큰 음수 → 자식 main 축 0 붕괴. 수정: `available_main < 0` 이면 grow/shrink 분배 skip, 각 아이템 `target_main = clamp(flex_basis)` 로 확정 후 조기 반환(intrinsic sizing = max-content). (2) `collect_lines` WRAP_WRAP + sentinel 이면 `outer > -1` 항상 초과 판정 → 각 아이템 별도 라인 오분할. 수정: `available_main < 0` 도 WRAP_NOWRAP 처럼 한 라인. (3) `ALIGN_STRETCH` 가 자식 명시 cross size(column 자식 width:100px)를 무시하고 컨테이너 cross 로 stretch(단위 2 발견 우회 버그) → FlexItem 에 `cross_is_auto` 필드 추가, `ALIGN_STRETCH if cross_is_auto` 가드로 명시 cross 는 start 배치+크기 유지. **검증**: cargo test **183 PASS** (lib 167 = 161 + sentinel 4[main basis 유지/column stack/wrap single-line/양수 grow 회귀] + stretch 2[명시 cross 유지/auto stretch 회귀] / golden 15 / doc-test 1), clippy 0. **live exercise (실 WASM dual-run)**: pkg 재빌드 → `dualRunLive.test.ts` (C-1) flex column height:auto **diff 0 전환 확인**(이전 h=0 붕괴 + width stretch → 이제 Taffy 완전 일치), 7/7 PASS. C-1 flex column 기대를 `toBe(false)`(diff)→`toBe(true)`(diff 0)로 갱신 = 선결 경계 뒤집힘 기록. dualRunHarness mock 5/5 + type-check PASS 무회귀. **seam 미배선 유지** — flag 경로 fallback. live builder 영향 0. **다음 진입점** = flag 전환 선결 #2 grid.rs intrinsic track 측정(현재 셀 높이를 available 로 채움 → C-1 grid diff 잔존, Phase 1-B grid.rs 후속 — HIGH, 별도 승인). #2 완료 후 C-1 grid diff 0 전환 확인 → (C-2) flag 전환 재평가.
- **flag 전환 선결 #2 — grid.rs implicit auto row intrinsic land — C-1 전면 diff 0 달성** (사용자 승인 2026-07-04, "승인") **근원(TDD RED→GREEN)**: C-1 GRID_AUTO 는 `gridTemplateRows` 미명시(implicit auto row). grid.rs 단독은 자식 flat 을 안 받아 intrinsic 을 모르므로 rows 미명시 시 `place_children` 이 셀 높이를 하드코딩 fallback(`grid.rs:588` `unwrap_or(100.0)`)으로 채움 → 자체 셀 h=100 vs Taffy 자식 intrinsic 50, +50 diff. **수정 위치 = tree.rs `solve_grid`**(grid.rs 아님 — 자식 intrinsic → row 크기 도출은 트리 레벨 책임): `template_rows` 미명시 + 전부 auto-placement(`placement_spec.is_empty()`)이면 (1) 자식을 먼저 `solve_node(avail_w, avail_h)` 로 intrinsic content height 확보, (2) row-major(row = i / col_count, col_count = `parse_tracks` 트랙 수) 별 max intrinsic 계산, (3) row 별 max 를 `"{h}px"` 트랙 문자열로 `template_rows` 주입 후 grid.rs 호출. 명시 placement 있으면 row-major 가정 깨지므로 grid.rs fallback 경로 유지(안전). **검증**: cargo test **185 PASS**(lib 169 = 167 + grid implicit 2[단일 행 자식 intrinsic 50 / 다중 행 row 별 max 30·50→50, 40] / golden 15 / doc-test 1), clippy 0. **live exercise (실 WASM dual-run)**: pkg 재빌드 → `dualRunLive.test.ts` (C-1) grid height:auto **diff 0 전환 확인**(이전 셀 h +50 → 이제 Taffy 완전 일치), C-1 grid 기대 `toBe(false)`→`toBe(true)` 갱신. **dualRunLive 7/7 PASS — C-1 진단 3 대표 패턴(block/flex column/grid) 전면 diff 0 달성** = 두 flag 전환 선결(#1 flex.rs + #2 grid.rs) 완료로 catalog 대표 패턴에서 자체 엔진 ↔ Taffy 완전 일치. dualRunHarness mock 5/5 + type-check PASS 무회귀. **seam 미배선 유지** — flag 경로 fallback, live builder 영향 0. **다음 진입점** = (C-2) flag 전환 재평가 — 자체 pkg 를 apps/builder dependency 등록 + 런타임 로드 배선(현재 미등록) + `createLayoutEngine` flag true 경로에 자체 엔진 주입 + **넓은 실전 fixture(실제 catalog 컨테이너 트리)로 dual-run 확장 후 diff 0 재확인**(C-1 3 패턴은 대표 샘플 — 실전 트리의 중첩/혼합/edge 는 미검증). live 엔진 교체라 HIGH — 별도 승인 필수. C-1 대표 diff 0 는 (C-2) 청신호이나 실전 전면 검증이 flag flip 전 선결.
- **(C-2b) 실전 중첩/혼합 dual-run 전면 diff 0 proof land** (사용자 승인 2026-07-04, "승인"; 순서 무응답 → 차단 메모리 default: proof 를 먼저·배선은 proof 통과 후). **관점 검증**: (C-2) 를 flag flip 까지 한 단위로 밀지 않고 배선/flip 을 분리 — [[feedback-no-dormant-foundation-ahead-of-flip]](flip 앞선 dormant 배선 회피) + [[feedback-infra-exists-vs-wired-consumption-path]](인프라 존재≠가동) + [[feedback-execute-adr-surface-minimization]](최소 surface). 런타임 배선 코드를 flag flip 없이 넣는 것 자체가 dormant foundation → **배선 없이 fixture 만으로 proof 확보 우선**. **왜 C-1 로 불충분**: C-1 은 단일 레벨 3패턴(block/flex column/grid) — 실전 catalog 컨테이너는 컨테이너를 **중첩**(catalog 실측 componentRulesTable: flex 88[flex 62+inline-flex 26] > grid 10 > block 6, flexDirection column 23 > row 10). 단일 레벨은 부모 available 전파 / cross-axis stretch 가 자식 컨테이너 intrinsic 계산에 미치는 상호작용을 못 잡음. **fixture(catalog 근거 5종)**: N1 flex-in-flex(column root auto → row 컨테이너, Card 헤더/바디) / N2 flex-in-grid(grid auto row 셀 안 flex column) / N3 grid-in-flex(flex column 안 고정 2열 grid) / N4 gap 혼합(rowGap/columnGap 명시) / N5 dimension 혼재(고정+auto 자식 flex row). **검증 (live exercise = 실 WASM dual-run)**: pkg 재빌드 → `dualRunLive.test.ts` (C-2b) describe 5 fixture **전면 diff 0**. **dualRunLive 12/12 PASS**(B2 4 + C-1 3 + C-2b 5) = 실전 대표 8형상(단일 3 + 중첩/혼합 5) 자체 vs Taffy 시각 동일. sanity(leaf 폭 30 vs 60 실제 산출 확인 — 조용한 통과 아님) 통과. type-check 무회귀. **seam 미배선 유지** — flag false 유지, 런타임 로드 배선 미도입 → live builder 영향 0. **다음 진입점** = (C-2a) 런타임 배선 + flag 전환 — 자체 pkg apps/builder 런타임 로드 배선(taffy `rustWasm.ts`/`layoutEngine.ts` 패턴, 현재 미배선) + `createLayoutEngine` flag true 경로 자체 엔진 주입 + `USE_RUST_LAYOUT_ENGINE` flip + Chrome MCP live exercise. **배선/flip 은 (C-2b) proof 확보 후 별도 승인 단위** — live 엔진 교체라 HIGH.
- **(C-2a) 런타임 배선 + flag 전환 land — 자체 엔진 live builder 전환 완료** (사용자 승인 2026-07-04, "(C-2a) 승인"). **배선(taffy 패턴 미러링)**: (1) `compositionEngineWasm.ts`(전역 비동기 로드 + 동기 캐시 read, taffy `rustWasm.ts` 대응) + `compositionEngine.ts`(동기 wrapper `CompositionEngineLayout`, taffy `TaffyLayout` 대응 — 자체 pkg `LayoutEngine` 이 이미 camelCase 16-메서드 = `LayoutEngineAPI` 이름 일치라 매핑 없이 raw `Uint32Array`/`Float32Array` → `number[]`/`Map` 변환만; `adaptSelfEngine` 로직의 런타임 승격). (2) `layoutBridge.ts::createLayoutEngine` flag true 경로에 `new CompositionEngineLayout()` 주입 — `isAvailable()` false(WASM 미준비)면 TaffyLayout 안전 폴백(회귀 시 flag 조정으로 rollback). (3) `init.ts` `initAllWasm` 에 flag 활성 시 `initCompositionEngineWasm()` 추가(startup 로드, createLayoutEngine 동기 호출 전 캐시 준비). (4) `featureFlags.ts` `USE_RUST_LAYOUT_ENGINE: false→true`. **경로 정정 (근본 이슈)**: 자체 pkg 는 monorepo `packages/composition-engine/pkg/`(vite dev 서버 root=apps/builder **밖**)이라 절대 URL `/packages/...` 동적 import 가 `Failed to fetch` (Chrome MCP 진단으로 특정). taffy 는 pkg 를 apps/builder 내부(`wasm-bindings/pkg/`)에 두고 상대 로드하는 선례 → 자체도 wasm-pack out-dir 을 `wasm-bindings/composition-engine-pkg/`(dev 서버 root 안, gitignore)로 지정 + 상대 경로 import(`compositionEngineWasm.ts`). `package.json wasm:build:engine` 스크립트로 CI 재현. **핵심 발견 (전제 정정)**: `UNIFIED_ENGINE: true`(featureFlags.ts:55) global override 로 `isUnifiedFlag("USE_RUST_LAYOUT_ENGINE")` 는 개별 flag 무관 이미 true(featureFlags.ts:61) → 배선 커밋 = 즉시 live 전환(배선/flip 분리 불가 구조). AskUserQuestion 무응답 → 사용자 (C-2a) 승인 범위(=flag 전환 포함) + proof(12/12) 확보로 옵션 1(배선=즉시 flip 인정) 진행, `isUnifiedFlag` 재설계(옵션 2)는 다른 flag 영향 scope 확장이라 회피(execute-adr-surface-minimization). **Chrome MCP live exercise (test/type-check 단독 종결 금지 이행)**: builder 진입 → `[ADR-916] composition-engine WASM initialized` 로그 확인 → `createLayoutEngine()` 이 **`CompositionEngineLayout` 반환**(이전 `TaffyLayout` → 전환 확증) + flex row 실계산(leaf-a x=0/w30, leaf-b x=30/w40 = dualRunLive 값 일치) + nodeCount 3 + hasBinaryProtocol false(자체 특성) + Canvas/Skia 렌더 무붕괴. **infra-exists-vs-wired-consumption-path 검증이 초기 폴백(compositionEngineReady:false → TaffyLayout 반환)을 정확히 잡아 경로 정정 유도** — live exercise 없이 커밋했으면 "전환 완료" 오보고할 뻔. type-check PASS(baseline 69) + dualRunLive 12 + harness 5 + seam 3 = 20/20 무회귀. **다음 진입점** = 1-E Taffy 제거 재평가(자체 엔진 live 안정화 후 Taffy WASM crate/pkg/wrapper 제거) → 2-C scene.rs / 2-D commands.rs / 2-E text.rs. HIGH — 별도 승인.
- **(1-E) Taffy dead code 제거 land** (사용자 승인 2026-07-05, "1-E Taffy 제거 승인"; scope AskUserQuestion = "폴백 유지 + Taffy 사용 참조만 제거" + "dead 코드 삭제만"). **자체 엔진 live 전환(C-2a) 후 Taffy 엔진 클래스 4종이 dead** (인스턴스화 0건 / `.calculate()` 호출 0건 — router `selectEngine()` 자체가 부재, live 경로는 `createLayoutEngine()` seam → `CompositionEngineLayout`). **삭제(파일 전체)**: `BaseTaffyEngine.ts`(abstract 클래스, `new TaffyLayout()` line 57 dead) + `layoutAccelerator.ts`(`wasmBlockLayout`/`wasmParseTracks`/`wasmGridCellPositions` export, importer 0). **클래스만 제거(파일 유지)**: `TaffyFlexEngine`/`TaffyGridEngine`/`TaffyBlockEngine` 클래스 + `isTaffy*Available` 삭제하되 **순수 style helper 3종 보존** — `elementToTaffyStyle`(TaffyFlexEngine.ts) / `parseGridTemplate`(TaffyGridEngine.ts) / `elementToTaffyBlockStyle`(TaffyBlockEngine.ts) 는 여전히 `fullTreeLayout.ts:41-43` DFS 가 소비(Taffy 인스턴스 비의존 pure fn). **폴백 유지(사용자 지시)**: `layoutBridge.ts:84` Taffy fallback = WASM 미가용 안전망으로 보존 — crate/pkg 물리 삭제 아님, dead 코드만 삭제. **검증**: 삭제 심볼(`BaseTaffyEngine`/`elementToTaffyGridStyle`/`isTaffy*Available`/`wasmBlockLayout`) 잔존 참조 0건, `TaffyFlexEngine`/`Grid`/`Block` 참조 9/6/2건은 파일명 import 경로(3) + 주석뿐(클래스 참조 0). type-check PASS(baseline 69) 무회귀. commit f2ac4860c(-1252줄, 5 file). **다음 진입점** = 2-C scene.rs / 2-D commands.rs / 2-E text.rs 재평가(fullTreeLayout DFS → 자체 엔진 편입 후속). HIGH — 별도 승인.
- **2-C scene.rs 재평가 — 벤치로 breakdown 전제 반증, 착수 보류** (사용자 결정 2026-07-05, "2-C 재평가 진행" + AskUserQuestion "현 ADR 유지 + 벤치 선행"). **관점 검증**: 2-C 는 fork성 판정(본 ADR 착수 vs 후속 분리 vs scope 재정의)이라 표면 착수 회피, 실측 후 판단. [[feedback-no-derived-adr-mid-execution]](다음 순위 자동 surface 금지) + [[feedback-execute-adr-surface-minimization]] + [[feedback-no-dormant-foundation-ahead-of-flip]](scene 은 cutover 즉시 삭제 원칙 — 잘못 배선 시 dormant) 차단 우선 평가 후 AskUserQuestion 으로 surface. **실측 (scene 경로 성격)**: 2-C 이관 대상 = `StoreRenderBridge.detectChangedIds`(Map 참조 비교 ~30줄) + `buildSceneStructureSnapshot`(depthMap/pageDataMap/pageFrames/projection signature, 265줄) + scene/ 디렉토리 ~3,747줄. Phase 2-B 가 이관한 순수 flat f32 레이아웃(입력=style 레코드, 출력=좌표 배열)과 **성격이 근본 상이** — JS 객체 그래프(`CanvasSceneNode` 참조) 직접 조작 + React useMemo(`BuilderCanvas.tsx:352`, deps 에 panOffset/zoom → pan/zoom 중 매 프레임 재계산)/Skia 렌더/selection/projection(ADR-135/136) 통합. **벤치 (신규 `sceneDirtyDetection.bench.ts`, 검증 매트릭스 '성능 벤치' gate)**: 500/1000/3000 노드 프레임당 재계산 비용 측정. **결정적 발견 — breakdown 2-C 전제 반증**: (1) `detectChangedIds`(breakdown 이 O(N)→O(1) 이관 대상으로 지목) 는 3000 노드 **0.052ms**(60fps 예산 16.7ms 의 0.3%) — 병목 아님, O(1) 이관 효과 사실상 0. (2) 실제 병목은 `buildSceneStructureSnapshot`, 그 중 `createResolvedProjectionSignature`(전체 elements `stableSerialize` + `hashString`) — 3000 노드 **19.25ms(예산 초과)** / 1000 노드 **5.23ms(예산 31%)** / 500 노드 2.01ms. detectChangedIds 대비 287×(500)/334×(1000)/372×(3000) 느림. **결론**: breakdown 원안(detectChangedIds 이관)은 **잘못된 대상 최적화** — scope 를 signature 계산(sceneVersion 서명 경로) 으로 재정의해야 유효. 사용자 선택("벤치 선행") 이행 완료 → **2-C 착수 보류**, scope 재정의(signature 최적화 vs detectChangedIds vs 전체 scene.rs) 는 별도 사용자 승인 단위. **seam/코드 이관 0** — 벤치 파일만 추가(live 영향 0). type-check PASS(baseline 69) 무회귀. **다음 진입점** = 2-C scope 재정의 판정(signature 경로 우선) 또는 2-D commands.rs / 2-E text.rs 재평가. HIGH — 별도 승인.
- **2-C 안 A land — projection signature useMemo 분리, 병목 예산 내 해소 (2-C 종료)** (사용자 결정 2026-07-05, "signature 경로 설계/최소 수정안 판정 … breakdown 2-C 제목/목표를 'projection signature recomputation 제거/증분화'로" + AskUserQuestion "안 A 착수(useMemo 분리, 최소)"). **관점 검증**: 어느 수정 표면(안 A JS useMemo / 안 B JS 증분 / 안 C Rust scene.rs)을 택할지 fork성 결정 — [[feedback-execute-adr-surface-minimization]](최소 표면) + [[feedback-no-dormant-foundation-ahead-of-flip]](scene.rs Rust 이관은 cutover 즉시 삭제 원칙상 dormant 위험) 차단 우선 평가 → 두 차단 모두 Rust 이관보다 JS 증분화 우선 지지. AskUserQuestion 으로 안 확정. **breakdown 재정의 (사용자 명시 지시)**: 2-C 제목 "Scene graph dirty detection O(N)→O(1)" → "Projection signature recomputation 제거/증분화". 폐기 원안은 `<details>` 로 보존. **병목 실체 (재벤치 signature 단독 측정)**: `createResolvedProjectionSignature` 단독 = `buildSceneStructureSnapshot` 비용의 ~100%(500 2.23ms / 1000 5.99ms / 3000 18.15ms) — depthMap/pageDataMap/pageFrames 무시 가능. **핵심 성질**: signature 입력 = `elements`(sceneNodes) + `pageSnapshots` 의 node 참조(`bodyElement`/`pageElements`) 뿐, `isVisible` 등 viewport 필드 미참조 → **pan/zoom/containerSize 완전 독립**. 그런데 전체 snapshot 이 단일 useMemo(deps 에 panOffset/zoom)에 묶여 pan/zoom 중 매 프레임 전체 재직렬화. **안 A 구현**: (1) `createResolvedProjectionSignature` export + 입력 타입 `pageSnapshots: Map<string, ScenePageData>` 로 확장(`ScenePageSnapshot extends ScenePageData` → 기존 호출 호환, 호출측이 가벼운 `pageDataMap` 으로 계산 가능). (2) `buildSceneStructureSnapshot` 에 `precomputedProjectionSignature?: number` 파라미터 — 주입 시 내부 `createResolvedProjectionSignature` skip(`??` fallback = 미주입 하위 호환). (3) `BuilderCanvas.tsx` 가 signature 를 별도 useMemo(deps: `sceneNodes`/`sceneNodesMap`/`layoutVersion`/`pages`/`scenePageIndex`/`isFrameEditMode` — **panOffset/zoom/containerSize 제외**)로 계산해 주입, signature 입력 `pageDataMap` 은 `buildPageDataMap` 직접 호출로 확보. **재벤치 — 병목 예산 내 해소**: pan/zoom 프레임(signature 주입) 3000 노드 **21.44ms→0.49ms(44×, 예산 3%)** / 1000 6.35→0.15ms(42×) / 500 2.11→0.072ms(29×). → **안 B(증분)/안 C(Rust scene.rs) 불필요, 2-C 종료. scene.rs 신설·element registry Rust 관리 미착수(불필요 확정).** **검증**: 정합성 test 3(주입==내부 동일 sceneVersion / pan-zoom-only signature 불변 / 잘못된 주입 소비 확증), buildSceneSnapshot.test.ts 6/6 PASS(기존 3 + 안 A 3). type-check PASS(baseline 69) 무회귀. **live exercise (test/type-check 단독 종결 금지 이행)**: builder 진입 → CSS(Preview DOM)+Canvas(Skia) 양쪽 정상 렌더 + 요소 선택/hit-test/자식 경계선 정확 + signature 관련 콘솔 에러 0(유일 에러 `inlinealert` 태그 = Preview DOM 사전 이슈, 무관 — stash 로 사전 실패 2건과 함께 검증). **다음 진입점** = 2-D commands.rs / 2-E text.rs — **ADR-916 통합 엔진의 이관 후보로 이미 전제**(scene/commands/text 는 layout 과 같은 Rust 경계, JS↔WASM 경계 최소화 목표의 구조적 대상). 벤치의 역할은 이관 여부 go/no-go 게이트가 **아니라** 이관의 before/after 비용·이득을 정량화하는 **정당화** 도구. "병목 아님" 발견 시에도 이관 취소가 아니라 다른 정당화 축(단일 엔진 일관성 / SSOT 단일화 / JS main-thread 제거)으로 진행 판단. 2-C 는 예외 — signature 가 순수 함수라 useMemo 분리만으로 예산 내 해소돼 Rust 이관이 불필요했던 특수 케이스(이 결과를 2-D/2-E 전제로 자동 승계 금지). HIGH — 별도 승인. [[feedback-rust-migration-candidate-bench-justifies-not-gates]]
- **2-D 재평가 — 벤치로 command stream 병목 아님 확인, P3(layout→SpatialIndex 직결) 계약/oracle 설계 (구현 보류)** (사용자 결정 2026-07-05, "2-D commands.rs 먼저" → AskUserQuestion 3연: "단계별 분리 계측" + "SpatialIndex 경계 횡단만 먼저(부분 이관)" + "먼저 계약/oracle만 설계, 구현 보류"). **관점 검증**: 2-D 착수 표면/정당화 축 결정이라 [[feedback-rust-migration-candidate-bench-justifies-not-gates]](이관 후보 전제, 벤치=정당화 도구) 적용 — 병목 아님이 이관 취소가 아니라 정당화 축 전환 신호. [[feedback-execute-adr-surface-minimization]](최소 표면) + [[feedback-no-dormant-foundation-ahead-of-flip]](cutover 즉시 삭제) 차단 우선 평가 → command stream 전체 이관(표면 과대) 배제, SpatialIndex 경계 횡단만. **벤치 (신규 `renderCommandStream.bench.ts`, 비용 분리 계측)**: buildRenderCommandStream 전체 / z-sort 기여분(zIndex 유·무 build 차이) / syncSpatialIndex JS 재직렬화 / commandChildrenMap 재구성 4단계 분리, 500/1000/3000 노드. **결정적 발견 — 원안 대상 반증 (2-C detectChangedIds 와 동형)**: (1) command stream 전체 3000 노드 **1.57ms(예산 9.4%)** — 현재 병목 아님. (2) breakdown 2-D 가 지목한 대상 모두 효과 미미: z-sort **0.145ms(예산 0.9%)** / syncSpatialIndex JS 재직렬화 **0.099ms(예산 0.6%)** / childrenMap 재구성 0.041ms(0.2%). detectChangedIds(0.3%)처럼 잘못 짚은 대상. (3) 실제 비용 축 = **DFS `visitElement` 순회 자체**(~1.39ms, getSkiaNode+layout 조회+sticky+boundsMap.set+command push 노드당 합). **정당화 축 (병목 아님 전제)**: 비용 아닌 **경계 횡단 구조** — `syncSpatialIndex`(renderCommands.ts:350)가 매 build 마다 JS boundsMap → Float32Array → WASM batchUpdate 로 경계 1회 횡단. 원안 "복사 제거"가 노린 실체는 이 구조 이득이지 재직렬화 0.099ms 아님(표현이 비용 잘못 짚음, 방향은 유효). **breakdown 재정의**: 2-D 제목 "commands.rs — Render command stream 전체 이관" → "layout → SpatialIndex 직결 (부분 이관 — 경계 횡단 제거)". **P3 계약**: 이미 Rust 인 layout solve(2-B) 결과에서 Rust 가 절대좌표 bounds 자체 계산 → SpatialIndex 내부 갱신 (JS boundsMap→WASM 횡단 소멸). command stream(JS)·boundsMap(TextOverlay/AI 소비자) 유지, SpatialIndex 갱신 경로만 layout 직결. **mutation 단일 진입점 확인**: SpatialIndex mutation = `renderCommands.ts:369 batchUpdate` 1곳뿐(elementRegistry 구 동기화 위임 완료), 쿼리 소비자 5(useViewportCulling/HoverManager/useDragBridge/useCentralCanvasPointerHandlers/BuilderCanvas)는 read-only → 쿼리 계약 미변경. **diff oracle**: 동일 layout 입력 → JS boundsMap batchUpdate vs Rust 직결의 `query_viewport`/`query_point`/`query_rect` 3종 쿼리 동일 id 집합(diff 0), fixture=중첩 flex/grid+scrollOffset+다중 페이지+sticky/fixed. cutover 시 syncSpatialIndex+batchUpdate JS 경로 즉시 삭제(no-dormant). **⏸️ 구현 보류 (사용자 "먼저 계약/oracle만 설계")**: 재정의 + P3 계약 + diff oracle 설계만 문서 추가, Rust 구현(layout→spatial 직결 + JS 경로 삭제)은 별도 승인 단위. **seam/코드 이관 0** — 벤치 파일 + 문서만(live 영향 0). type-check PASS(baseline 69) 무회귀. **다음 진입점** = 2-D P3 Rust 구현(승인 시) 또는 2-E text.rs 재평가(벤치 선행). HIGH — 별도 승인.
- **2-E 재평가 — 텍스트 측정 이관 대상 제외 (CSS 정합 제약, 근본 차단)** (사용자 결정 2026-07-05, "2-E text.rs 재평가" → AskUserQuestion "이관 대상 제외 — 근거 기록 후 종료"). **관점 검증 (2-C/2-D 와 질적으로 다름)**: 2-C/2-D 는 "잘못 짚은 대상"(성능 병목이 다른 곳 → 정당화 축 전환으로 이관 유효)이었으나, 2-E 는 **아키텍처 제약(CSS 정합=브라우저 폰트 엔진 필수)이 이관 자체를 막는** 경우 → [[feedback-rust-migration-candidate-bench-justifies-not-gates]] 의 "병목 아님 시 정당화 축 전환" 프레임의 **예외**(2-C 가 순수 함수 예외였던 것과 다른 종류의 예외). 차단 메모리 우선 평가: [[feedback-no-derived-adr-mid-execution]](새 ADR fork 아닌 현 ADR 내 breakdown 재정의 → 통과) + [[feedback-execute-adr-surface-minimization]](text.rs 신설 차단 = 최소 표면 정합). **정합성 확증 (Explore 에이전트 독립 검증 + 코드 실사)**: (1) live 측정 경로 = CanvasKit Paragraph 아니라 **Canvas 2D `measureText`**(`canvas2dSegmentCache.ts:33` `USE_CANVAS2D_MEASURE=true`, ADR-051). CanvasKit 은 `needsFallback()` true(~10%)에서만. 폭·줄 수·줄바꿈 전부 Canvas 2D 결정, CanvasKit 은 `\n` hard break 받아 shaping/렌더만(`nodeRendererText.ts:449-460`). (2) **근본 차단**: Canvas 2D = 브라우저 폰트 엔진(Blink/ICU) = CSS Preview 와 동일 엔진 = CSS↔Skia 줄바꿈 정합의 구조적 근거(ADR-051 `completed/051:28,50,109,118`, canvas-rendering.md §3 "Layout=Canvas 2D=CSS 정합 원칙"). 측정 계산을 Rust 로 옮기면 제3 폰트 엔진 → sub-pixel·줄바꿈 발산 → D3 시각 대칭 파괴. ADR-051 이 정확히 이 이유로 CanvasKit 측정 기각(`completed/051:118`). (3) **결정적 제약**: Rust/WASM 은 `document.fonts`·시스템 폰트 접근 불가 → 브라우저 measureText 폭 원리적 재현 불가. **원안 두 목표 동시 성립 불가**: "Rust batch 측정"(계산 이관)=정합 파괴 근본 차단 / "Rust LRU 캐시만"=정합 유지되나 캐시 조회·저장마다 JS↔WASM 경계 왕복 증가 → ADR-916 경계 최소화 목표(§0-4)와 역행, 실익 없음. **벤치 (신규 `textMeasure.bench.ts`, 비용도 병목 아님)**: Canvas 2D 3-Tier 파이프라인(짧은 라벨) 0.0013ms / tokenize 0.0059ms / segment 캐시 hit 0.0084ms — JS 오버헤드 전부 sub-0.01ms(예산 <0.1%). 조상 체인 font 상속(④ 3000노드 0.416ms)은 text 측정과 무관한 순수 트리 순회 → **2-B tree.rs 흡수**(2-E 별도 대상 아님). **breakdown 재정의**: 2-E 제목 "text.rs — 텍스트 측정 캐시" → "텍스트 측정 — 이관 대상 제외 (CSS 정합 제약)". 측정=Canvas 2D(브라우저 엔진) 전담 유지, `text.rs` 를 crate 목표 다이어그램에서 제거, 검증 매트릭스 2-E CSS↔Skia 대칭 게이트 제거. 폐기 원안은 `<details>` 보존. (측정 원칙 자체를 뒤집으려면 ADR-051/900 supersede 하는 별도 상위 D3 재정의 ADR 선행 — 본 breakdown 범위 밖.) **seam/코드 이관 0** — 벤치 파일 + 문서만(live 영향 0). type-check PASS(baseline 69) 무회귀. **Phase 2 재평가 종결**: 2-C(종료)/2-D(P3, 구현 보류)/2-E(이관 제외) 3 모듈 재평가 완료. 남은 실제 Rust 이관 진입점 = 2-A style.rs 잔여 + 2-B tree.rs 배선 + 2-D P3 구현(승인 시). HIGH — 별도 승인.
- **2-CAT Catalog 정적 참조 계약 — 설계 v2 확정 (조상 체인 propagation 이관 선결, 구현 미착수)** (사용자 결정 2026-07-05, "다음 진입점 정리" → "catalog 참조 계약 먼저 설계" → 반대심문 후 "설계 v2 재작성" → "v2 문서화"). **framing**: 2-B tree.rs 조상 체인 흡수(2-E 재평가에서 이관됨)의 의미 있는 단위가 catalog(`resolveComponentRule`)+token(`resolveToken`) 정적 참조를 Rust 측에 선결 요구 — 2-A 잔여와 공유하는 벽. [[feedback-rust-migration-candidate-bench-justifies-not-gates]](이관 후보 전제) + [[feedback-no-derived-adr-mid-execution]](새 ADR fork 아닌 breakdown 내부 계약 → 통과) + [[feedback-execute-adr-surface-minimization]](최소 표면 = 조상 체인에 필요한 정적 층만, color/radius 런타임 주입 이연) + [[feedback-no-dormant-foundation-ahead-of-flip]](WASM 주입 배선을 소비 phase 동시). **설계 프로세스 (Fable 5 위임)**: 실사 2건(2-A 잔여 + 조상 체인 입출력) → catalog/token SSOT 실사 → 설계 v1 → **적대적 반대심문 3건 병렬**(S3 주입 / color 이연 범위 / oracle·gate — 각 "설계 무너뜨려라") → **CRITICAL 3 + HIGH 6** 확정 → v2 재작성 봉합. **CRITICAL 3**: C1 전파 필드에 `height:"auto"`+nested `indicator{}` 실데이터(`Number("auto")=NaN`, `Object.is(NaN,NaN)` 침묵 통과) → **사영을 key 단위 allowlist(fontSize/lineHeight/iconSize)로 축소해 범위에서 제거** + strict resolve(`parsePxValue` 경유, isFinite assert). C2 defaultSize fallback 의미론 정반대(live 3경로 `sizes[size] ?? sizes[defaultSize]` vs v1 "미존재 size→None") → **1급 조항 Rust 이식**, Negative="미존재 type→None"만. C3 oracle 순환(양변 동일 resolveToken, 재현 이력 2건: ADR-913 `{radius.xs}` silent undefined / live `spacing.md=16` vs CSS 12px) → **L0 primitive↔CSS parity 독립 leg**(도입 즉시 spacing RED=oracle 작동 증거, 발산 ledger 등록)+손검증 golden 앵커. **HIGH 6**: H1 폴백 로직 이중화(Taffy 폴백에 propagation 없음 → "무이중화" 철회, "값 이중화 0+로직 이중화 endgame 까지 HIGH 잔존 R4") / H2 주입 대상·ready(thread_local static+init promise 원자 주입+isAvailable 2조건+fail-loud) / H3 doc override(`resolveStaticComponentRule` 타입 분리=compile error, grep 보조 강등 — live 호출 6건 기존재) / H4 scope 붕괴(`resolveButtonChildColor` color 주입 실존 → 이관 scope 4심볼 고정+parent-read 주입군 7종 명시 제외) / H5 radius 가 variants 아닌 sizes 안(key allowlist 로 자동 배제) / H6 L3 소유 공백(완료 phase 2-B 오지정 → 실존 후속 P2-PROP 재지정). **S3 핵심 방향 생존**: JS 선해석 숫자 스냅샷 1회 주입, TokenRef 가 WASM 경계 미횡단 → 값 사본 repo 미보관. **Phase 순서 재정의**: "2-A→2-B 순서 필수" 정밀화 → "2-A 순수 계층(완료)+2-CAT → 2-B 조상 체인 개방"(조상 체인이 소비하는 것은 catalog sizes+토큰 정적 층뿐, resolveStyle 본체·implicitStyles 아님). **P2-CAT 은 L0/L1/L1.5/L2(cargo fixture)까지만 land, WASM 주입 배선+L3 dual-run 은 소비 phase P2-PROP 동시**(dormant 회피). **잔존 위험 정직**: R4(폴백 이중화 HIGH — Taffy 제거까지)·R10(spacing 발산 MED — L0 이 표면화한 실채무, Skia/DOM 동시 수정). **문서/코드 이관 0** — breakdown 2-CAT 절 + 순서 의존성 정밀화 + crate 다이어그램 catalog.rs 추가만(live 영향 0). type-check 무관(문서만). **다음 = P2-CAT ① 구현**(resolveStaticComponentRule + buildCatalogStaticSnapshot + strict 파이프라인) **또는 2-A style.rs 잔여**. HIGH — 별도 승인.
- **P2-CAT ① 구현 — resolve/snapshot 순수 계층 land (배선 없음)** (사용자 결정 2026-07-05, "다음은 P2-CAT 구현입니다" → 명시 착수 지시로 [[feedback-no-derived-adr-mid-execution]] 자동 착수 금지 해제). **실사 선행**: 착수 대상 3파일 실코드 + 반대심문이 잡은 다형성 실데이터 재확증(`height:"auto"` componentRulesTable:1473 / nested `indicator{}` :9556 / sizes 내 `borderRadius:"{radius.full}"` :38 / allowlist 3키 실값 분포 — fontSize 는 TokenRef 대부분+`{typography.text-md}` 1건도 typography.ts:19 정의됨 resolve 가능 / `parsePxValue` 는 TokenRef parseFloat 실패 → fallback 이라 resolveToken 선행 필수 / `resolveToken` 미정의 시 throw 아닌 원문 반환 → isFinite assert 필요 확증 / live 소비자 3경로 `sizes[size] ?? sizes[defaultSize]` fallback 실코드 대조). **TDD RED-GREEN** (test-driven-development skill): strict 파이프라인 RED(import 실패) → GREEN / resolveStaticComponentRule RED(3 fail) → GREEN. **산출물 4**: (1) `resolveStaticMetric`(조항 C3 — resolveToken→parsePxValue(fallback=NaN)→isFinite assert, Number() 미사용, "auto"/미정의/비수치 throw) (2) `buildCatalogStaticSnapshot`(조항 1 — METRIC_KEYS allowlist fontSize/lineHeight/iconSize projectMetric 만, 조항 2 — defaultSize 값 보존 사전 전개 금지) (3) `lookupCatalogMetric`(조항 2 — sizes[size]??sizes[defaultSize], defaultSize optional 반영: 미정의 시 fallback leg 없음, Negative 미존재 type→undefined) — 이상 `apps/builder/src/builder/utils/catalogStaticSnapshot.ts`(builder=shared+specs 동시 import 유일 계층) (4) `resolveStaticComponentRule(type)`(조항 4 — doc 파라미터 없는 타입 분리, `packages/shared/.../resolveComponentRule.ts`). **조항 4 봉합 실증**: 테스트에 `@ts-expect-error` + `resolveStaticComponentRule("Avatar", {componentRules:{}})` 라인 → type-check 이 doc 유입을 compile error 로 확인(시그니처에 doc 추가 시 "unused directive" 로 회귀 감지). **검증 exercise**: builder 13 test PASS(strict 6+snapshot 7) / shared 4 test PASS(+@ts-expect-error 봉합) / 전체 type-check PASS(baseline 69 신규 0 — 초기 `defaultSize:string` vs optional 불일치 1건은 `string|undefined` + fallback 가드로 정정). **live behavior exercise 면제 근거**: 배선 0줄(소비자 grep 0건, `initCompositionEngineWasm` 미연결) = 순수 계층, 사용자-가시 동작 미발생 → CLAUDE.md "0 live code change" 케이스. CHANGELOG 면제(ADR-916 Accepted 유지). commit `1a48ec78a`(4 files +315). **미포함(다음 단위)**: P2-CAT ②(L0 primitive↔CSS parity — 도입 시 spacing 발산 RED 예상 R10 + L1 구조 정합 + L1.5 golden 앵커) / P2-CAT ③(Rust catalog.rs + inject + cargo fixture = 완료선) / P2-PROP(WASM 주입 배선 + tree.rs propagation 이관 + L3 dual-run). **다음 = P2-CAT ② 착수(L0 parity — R10 spacing 발산 표면화 예상) 또는 2-A style.rs 잔여**. HIGH — 별도 승인.
- **P2-CAT ② L0 — primitive↔CSS typography parity leg land (순수 검증)** (사용자 결정 2026-07-05, "② L0 parity 진행(발산 ledger 등록)" → 후속 반대심문 응답 "정합 확인 + negative fixture 로 oracle 증명" + "ledger 등록(KNOWN_DIVERGENCES) + 그 외 RED"). **oracle 순환(C3) 봉합 실증**: catalog 스냅샷은 `resolveToken`(primitive typography.ts)으로 fontSize/lineHeight 숫자화 → 양변 동일 resolveToken 이면 CSS↔Skia 발산을 shared-fault 로 못 잡음. L0 = `shared-tokens.css` **텍스트 직접 파싱**(resolveToken 미경유, 독립 권위) → `--text-*:0.875rem` → 14px / `--text-*--line-height:calc(a/b)` × fontSize_px 배율 → px. **산출물 2**: `typographyCssParity.ts`(`cssFontSizeToPx` rem 만 허용 throw / `cssLineHeightToPx` calc(a/b) throw / `parseCssTypographyTokens` fontSize+line-height pairing / `KNOWN_TYPOGRAPHY_DIVERGENCES` ledger) + 9 test(`packages/specs/src/renderers/utils/`). **실측 정정(M3 — 추정 vs 실측 gap = 절차 정밀화, fork 아님)**: v2 초안 "도입 즉시 spacing RED 예상" 은 **오예측 폐기** — spacing 은 사영 allowlist(fontSize/lineHeight/iconSize) 밖(조항 1)이자 primitive `spacing.md=16` 과 CSS `--spacing-md:12px` 는 spacing.ts 주석이 명시한 **의도된 별개 계열**(발산 벡터 아님). L0 이 실제 검출한 발산 = **`text-xl--line-height` 1건**(primitive 30 = 주석 "20×1.5" vs CSS `calc(1.75/1.25)×20px=28`) → `KNOWN_TYPOGRAPHY_DIVERGENCES` 등록, 그 외 typography 전수 정합. **oracle 작동 3방식 증명**: (a) positive 전수 정합 통과 + (b) ledger 재확인(stale 방지 — 등록 발산이 여전히 primitive≠CSS 임을 양값 assert) + (c) negative fixture(변조 CSS 를 실제 RED 로 검출). **잔존 실채무 R10**: text-xl line-height 정본 판정 + Skia/DOM 동시 정렬 = 별도 D3 symmetric 단위(본 leg 는 "발산 존재+양값"만 기록). **검증 exercise**: 9 test PASS / type-check PASS(문서·순수 파서, baseline 신규 0). **live behavior exercise 면제**: CSS 텍스트 파싱 순수 검증, 배선 0(live 소비자 grep 0건) → CLAUDE.md "0 live code change". CHANGELOG 면제(ADR-916 Accepted). commit `4bcb611fa`(2 files) + 문서 정정 커밋 예정. **미포함(다음 단위, 별도 승인)**: L1 구조 정합 + L1.5 golden 앵커(P2-CAT ③ 착수 시) / P2-CAT ③(Rust catalog.rs + inject + cargo fixture = 완료선) / P2-PROP(WASM 배선 + tree.rs propagation + L3 dual-run) / text-xl line-height 실수정(R10 D3 symmetric). **다음 = P2-CAT ③ 또는 2-A style.rs 잔여**. HIGH — 별도 승인.
- **P2-CAT ③ 완료선 — Rust catalog.rs + JS 직렬화(L1/L1.5) + wasm seam land** (사용자 결정 2026-07-05 "P2-CAT ③ 진행해" → 명시 착수, [[feedback-no-derived-adr-mid-execution]] 해제. WASM seam 범위 AskUserQuestion "seam 정의 포함(wasm.rs 관습)" confirm). **dormant 경계 사용자 confirm**: catalog.rs 조회 로직은 L2 cargo fixture 소비자라 dead 아님 / `injectCatalogSnapshot` WASM export 는 wasm.rs seam 관습(wrapper 존재 ≠ flag 전환, seam 미배선 유지)과 동형 — JS init 실배선은 P2-PROP 소유([[feedback-no-dormant-foundation-ahead-of-flip]] 통과). **산출물**: (JS) `serializeCatalogSnapshot`(Map→JSON, defaultSize 미정의 시 키 생략 = Rust Option<String> None) + L1 구조 정합 5 test(WASM 경계=string 계약/roundtrip 동형/allowlist key 만/유한 숫자) + L1.5 golden 앵커 5 test(Button 전 size 손검증 fontSize·lineHeight·iconSize 회귀 고정: xs 10/16/14, sm 12/16/16, md 14/20/18, lg 16/24/24, xl 18/28/28). (Rust) `packages/composition-engine/src/catalog.rs` — `CatalogMetric`/`CatalogTypeEntry`(serde `rename_all="camelCase"` — JS camelCase JSON key 수신) + `thread_local! CATALOG: RefCell<Option<..>>`(조항 5 per-instance 아님) + `inject_catalog_snapshot`(no-silent-drop Err) / `lookup_catalog_metric`(조항 2 sizes[size]??sizes[defaultSize], 미주입/미존재 type→None) / `is_catalog_injected`(조항 5, JS isAvailable 2조건 후자) / `clear_catalog`. (wasm.rs) `injectCatalogSnapshot`/`isCatalogInjected`/`lookupCatalogMetric`(NaN sentinel flat f32) free function seam — thread_local static 대상이라 LayoutEngine 메서드 아님, `#[cfg(target_arch="wasm32")]` 게이트. (lib.rs) `pub mod catalog` + 모듈 doc. **L2 cargo fixture(11 test)**: JS `serializeCatalogSnapshot` 출력 형식 대표 샘플(Button 5 size = JS L1.5 golden 동일 손검증값 + Avatar fontSize-만 partial) 로 조항 1(camelCase 역직렬화/partial None)·2(fallback/미존재 type None/default 없음 None)·5(주입 상태/미주입 None fail-loud)·no-silent-drop(invalid JSON Err) 확증 → **두 언어 lookup 동형 고정**. **검증 exercise**: cargo lib 180(기존 169+catalog 11) + golden 15 + doc 1 = 196 PASS / clippy 0(native + `--target wasm32-unknown-unknown`) / JS builder 23(기존 13+L1 5+L1.5 5) + shared 4 PASS / type-check baseline 69 신규 0. **live behavior exercise 면제**: `injectCatalogSnapshot`/`serializeCatalogSnapshot`/`buildCatalogStaticSnapshot` JS 호출 grep 0건(테스트 외) = 순수 계층 + seam 정의만, 사용자-가시 동작 미발생 → CLAUDE.md "0 live code change". CHANGELOG 면제(ADR-916 Accepted 유지). **미착수(P2-PROP 소유, 별도 승인)**: JS init 실배선(`initCompositionEngineWasm`→injectCatalogSnapshot) + isAvailable 2조건 결합 + fail-loud 발동 + tree.rs propagation 이관 + L3 dual-run / text-xl line-height 실수정(R10 D3 symmetric) / 2-A style.rs 잔여. **다음 = P2-PROP(조상 체인 이관) 또는 2-A/2-C/2-D/2-E 재평가**. HIGH — 별도 승인.
- **P2-PROP 착수 실사 — 전제 정정(M3) + 코드 착수 보류** (사용자 결정 2026-07-05 "P2-PROP 진행해" → 실측 gap surface → AskUserQuestion 2회: "서술 정정(M3) — propagationRegistry 배선" + "P2-PROP 보류 — 서술만 M3 정정 후 중단"). **깊은 사고 진입 후 전제·관점 layer 의문 처리(CLAUDE.md literal instruction following)**: P2-PROP 규정("tree.rs propagation 이관")이 실측과 어긋나 무비판 진행 차단. **실측 3**: (1) propagation 4심볼(`getPropagationAncestors`/`applyParentPropagationProps`)은 tree.rs(순수 레이아웃)가 아니라 **Skia 렌더 경로**(`skia/buildSpecNodeData.ts:286-406`)에 있고, tree.rs grep propagation/catalog 0건. **2-B 실사(2026-07-04 사용자 confirm)로 "DFS 상단 enrich=JS 잔류, tree.rs=batch 계약만"** 확정됨 → propagation 은 enrich 안 JS 잔류 영역. §2-CAT "tree.rs propagation 이관"은 그 확정 이전 전제 자동 승계([[feedback-adr-dependency-direction-stale-baseline]]). (2) catalog 실소비처는 propagation 이 아니라 **다수 동기 hot path**: propagationRegistry `buttonTextMetrics`/`buttonIconPx` transform(:345,:361) + buildSpecNodeData sizeSpec(:1123) + resolveSkiaVisualRule(:68) + implicitStyles(:284) + specPresetResolver(:139) + specCatalogBacked(:45). propagation 오케스트레이션 자체는 부모 props→자식 복사이지 catalog 미소비([[feedback-infra-exists-vs-wired-consumption-path]]). (3) **Rust catalog lookup 의 live Rust 소비자 부재** — tree.rs 가 propagation 을 안 가져가면(2-B 확정) Rust catalog 동기 호출 코드 0 = L2 fixture 만. JS 소비처를 WASM 으로 바꾸면 모든 hot path 경계 왕복(60fps 회귀) + allowlist 3 key 만이라 padding/borderRadius 소비처 이관 불가, 안 바꾸면 dormant([[feedback-no-dormant-foundation-ahead-of-flip]]). **결정**: P2-PROP 코드 착수 **보류**. Rust catalog live 소비는 2-B DFS 상단 enrich 이관 시 발생 → P2-PROP 배선은 그 이관과 묶어 착수. P2-CAT ①②③ 산출은 그때까지 seam 유지(L2 소비자, dead 아님). **문서 정정만(코드 0)**: breakdown §2-CAT 하단 M3 정정 블록 + Phase 테이블 P2-PROP 행 ⏸️ 보류 + 순서 재정의 정정 + R11(Rust catalog live 소비자 부재 MED) 추가. type-check 무관(문서만). **다음 = 2-B DFS 상단 enrich 이관(P2-PROP 동반) 또는 2-A style.rs 잔여 / 2-C·2-D·2-E 재평가**. HIGH — 별도 승인.
- **R10 실수정 — text-xl line-height CSS↔Skia 대칭 복원 land** (사용자 결정 2026-07-05 추천 순서 "R10 → ledger 제거 → targeted test → 이후 2-D/2-B 재협상"). **정본 판정(깊은 사고)**: 요약 기록 "primitive 30 vs CSS 28" 을 추정으로 두지 않고 코드 직접 확인 — CSS 정본 `shared-tokens.css --text-xl--line-height: calc(1.75/1.25)` = 20×1.4 = **28**, primitive `typography.ts` 주석 `20×1.5=30` 은 나머지 9개 토큰(text-2xs~5xl)이 전부 따르는 CSS calc 배율 패턴 미준수 = **primitive 30 이 버그**(text-xl 만 유일 발산, 나머지 9 전수 CSS 정합). **D3 symmetric 복원**: Skia(`getLabelLineHeight`/`resolveToken` → primitive 직접 소비)와 CSS(`var(--text-xl--line-height)` → shared-tokens.css)가 **같은 primitive SSOT** 를 소비 → primitive 30→28 정정으로 양변 28 수렴(CSS 정본 이미 28 이라 CSS 파일 무변경, primitive 만 정렬). **TDD RED-GREEN**(test-driven-development skill): ledger 제거 상태 RED(전수 정합 test 가 primitive 30≠CSS 28 검출) → primitive 28 GREEN / 신규 `typography.test.ts` 회귀 앵커 = golden 10 토큰 + `getLabelLineHeight(20)===28` Skia 소비 계약(임시 30 회귀로 RED 3건 확인 후 28 GREEN 4건). **산출**: `typography.ts` 30→28 + 주석 정정 / `token.types.ts` 주석 30→28 / `typographyCssParity.ts` ledger `KNOWN_TYPOGRAPHY_DIVERGENCES` 비움(정합화 → 유지 시 stale) / `typography.test.ts` 신규 / `tokenSnapshot` 갱신. **baseline 대조(feedback-git-stash-pop-baseline-measure-hazard)**: builder 67 failed 를 R10 귀속하기 전 HEAD(text-xl=30) 로 원복 후 재측정 → **67 failed 동일**(전부 pre-existing propagation rule 구조 등, line-height 무관) = R10 회귀 0 증명. **검증 exercise**: specs 534 test PASS(신규 typography 4 포함) / type-check baseline 69 신규 0 / cross-check(Chrome MCP) 면제 근거 = 값 수렴(좌표 변경 아님) + L0 독립 권위 leg(CSS 텍스트 파싱)·Skia 소비 계약·CSS var 경로 3방향 정합 증명으로 대칭 확증. **breakdown §2-CAT L0 R10 실수정 Land 반영 + R10 잔존 위험 해소 표시**. CHANGELOG Bug Fixes 반영(사용자-가시 text-xl 텍스트 줄 높이 정합). **다음 = 2-D P3 또는 2-B DFS 상단 이관 재협상**. HIGH — 별도 승인.
- **2-B "enrich 이관" 재협상 — P2-PROP 구성 폐기, propagation JS 잔류 정본화** (사용자 선택 2026-07-05 "2-B enrich 이관 재협상", 깊은 사고 진입, 코드 이관 0). **재협상 대상 실측(추정 아닌 실코드)**: P2-PROP 실사가 "catalog live 소비=2-B enrich 이관 시 발생" 이라 했으나, "enrich" 가 CanvasKit 측정 의존 여부 정반대인 **두 경로**를 혼용했음을 확인. **실측 3**: (1) `enrichWithIntrinsicSize`(`layout/engines/utils.ts:3953`)는 `min/max-content` 를 **CanvasKit Paragraph API 로 텍스트 측정**(`calculateMin/MaxContentWidth`) → 폰트 측정 의존이라 Rust 엔진 이관 **불가**(2-B "JS 잔류" 확정의 근본 이유). 반면 `applyParentPropagationProps`(`skia/buildSpecNodeData.ts:361`)는 조상 props→transform→자식 props 의 **순수 함수**(CanvasKit/getComputedStyle 호출 0), catalog 소비(`buttonTextMetrics`/`buttonIconPx` transform)는 이 순수 경로 안. (2) 따라서 catalog Rust live 소비를 위해 enrich 전체 이관 불필요 — propagation 순수 계층만 옮기면 됨. P2-PROP 실사의 "enrich 이관과 묶어야" 는 **과대 스코프**(CanvasKit blocker 를 propagation 이관 전제로 잘못 승계). (3) 그러나 propagation 은 [[feedback-rust-migration-candidate-bench-justifies-not-gates]] 의 "이관 후보로 전제된 계층" 이 **아님** — 실체가 순수 JS(조상 Map 순회+rule 조회+transform)로 이미 빠르고, Rust 이관 시 매 Skia 렌더 노드마다 props JS→WASM→JS 왕복이 순수 JS 실행보다 느릴 개연 → 성능·SSOT·main-thread 어느 축으로도 미정당화. **결론**: P2-PROP 의 "2-B enrich 이관과 묶는" 구성 **폐기**. propagation 은 **JS 잔류가 정본**(성능상 적합 계층). catalog Rust seam(P2-CAT ③)은 R11 대로 seam 유지하되, **live 소비는 2-C/2-D(scene.rs/commands.rs — 이관 후보로 전제된 렌더 계층)가 Rust 화하며 catalog metric 읽는지를 그 phase 착수 시 재판정**. breakdown §2-CAT 재협상 블록 + R11 관리 문구 갱신([[project-adr916-catalog-not-propagation-prereq]] 정밀화). **코드 이관 0** — 문서만, type-check 무관, live 영향 0. **다음 = 2-C/2-D/2-E 재평가(catalog 소비 여부 포함) 또는 2-A style.rs 잔여**. HIGH — 별도 승인.
- **2-C/2-D/2-E 재평가 — catalog Rust 소비 경로 부재 확정 + P2-CAT ③ dormant 제거** (사용자 "2-C/2-D/2-E 재평가 진행" → 실측 후 AskUserQuestion "catalog.rs+seam 제거 검토" 선택, 깊은 사고 진입). **재평가 실측(추정 승계 금지, 실코드)**: 2-B 재협상이 "catalog live 소비는 2-C/2-D 렌더 Rust 화 시 재판정" 으로 열어둔 질문을 세 phase 실측으로 닫음 — **세 렌더 phase 어느 것도 catalog metric 미소비**. (1) 2-C(scene.rs): 안 A(signature useMemo) 60fps 내 해소(21.44→0.49ms), scene.rs 미생성, catalog 소비 0. (2) 2-D(commands.rs→SpatialIndex 직결 P3): command stream JS 유지, `renderCommands.ts` catalog import 0(line 179 fontSize=이미 계산된 `child.text.fontSize`, `resolveComponentRule` 미호출), P3 는 bounds 좌표만 → catalog 무관. (3) 2-E(text.rs): Canvas 2D 측정(브라우저 폰트 엔진=CSS Preview 동일) D3 대칭 제약으로 이관 제외, text.rs 미생성, catalog 소비 0. (4) tree.rs(2-B): catalog 참조 grep **0**(layout 좌표 ≠ metric). → catalog 실소비처는 전부 `buildSpecNodeData`/propagationRegistry/implicitStyles/specPresetResolver = **DFS 상단·노드 빌드 JS 잔류 계층**, 렌더 계층 아님 → **catalog Rust live 소비 경로가 구조적으로 없음**(R11 이 미래 해소 아닌 영구 상태). **결정(사용자 "catalog.rs+seam 제거 검토")**: P2-CAT ③ Rust 산출은 flip 경로 없는 dormant foundation([[feedback-no-dormant-foundation-ahead-of-flip]] 정면) → **제거**. Rust `catalog.rs` 삭제 + `wasm.rs` seam 3 export + `lib.rs` mod 제거(cargo lib 180→169) + JS 경계 전용 `serializeCatalogSnapshot` + L1(WASM 경계) test 제거(조항 1/C3 검증은 스냅샷 Map 직접으로 보존). **유지(사용자 결정 "JS ①② 유지")**: catalogStaticSnapshot 순수 조회(build/lookup/resolveStaticMetric) + resolveStaticComponentRule + **L0 typographyCssParity**(독립 D3 자산, R10 oracle) — 재생성 가능 계층. 파일 헤더 doc "propagation Rust 이관 선결" → "재생성 가능 순수 조회, Rust 산출 dormant 제거" 정정. **검증 exercise**: cargo 185(lib 169+golden 15+doc 1) PASS / clippy 0(native+wasm32) / builder 21+shared 4+specs L0 9 PASS / type-check baseline 69 신규 0. **live behavior exercise 면제**: 제거 대상 전부 배선 0 dormant(빌드된 wasm pkg 에 catalog 미포함 = seam 이 wasm 컴파일된 적도 없음 재확인), 사용자-가시 동작 미발생 → CLAUDE.md "0 live code change". R11 해소. breakdown §2-CAT 재평가 블록 + R11 해소 표시 + Phase 테이블 ③🗑️/P2-PROP❌ + crate 다이어그램 catalog.rs 제거. **다음 = 2-A style.rs 잔여(resolveStyle 조립/implicitStyles) 또는 2-D P3 SpatialIndex 직결 구현**. HIGH — 별도 승인.
- **2-A `resolveStyle` 본체 조립 land — cascade 진입점 순수 이식 (배선 없음)** (사용자 "2-A style.rs 잔여 진행해" → 명시 착수, [[feedback-no-derived-adr-mid-execution]] 해제). **실사 선행(추정 승계 금지)**: `cssResolver.ts:579 resolveStyle` 본체가 store/DOM 의존 0 **순수 함수**임을 실코드 확인 — `getRootComputedStyle()`(store)은 호출자가 `parent_computed` 만들 때만 쓰이고 본체 밖. 의존 헬퍼(`preprocessImportant`:535 / `resolveLogicalProperties` / `parseFontShorthand` / `resolveCascadeKeyword`)는 순수이며 logical/cascade/font-shorthand 는 기이식. **TDD RED-GREEN-REFACTOR**(test-driven-development skill): 3함수 미정의 컴파일 RED → GREEN → REFACTOR(fontSize unitless `parse_leading_f32` 를 style.rs `pub(crate)` 로 단일 정본화). **산출 3(`cascade.rs`)**: `preprocess_important`(`!important` 분리, trimEnd) + `expand_font_shorthand`(resolveStyle 내부 font shorthand 전개, longhand 미덮어씀 가드) + `resolve_style`(조립 진입점 — preprocess_important → resolve_logical(normal/important) → expand_font → parent 복사 → INHERITABLE 17종 순회 normal→important → fontSize em(부모)/rem(16)/px 해석). **edge case 정합 정정(TDD 로 고정)**: 원본 JS unitless `parseFloat(fs) || parent.fontSize` 의 **0 falsy 처리**(`"0"`→부모 16, `"0px"` px분기는 0 유지) 재현. **검증 exercise**: cargo lib 189(이전 121+cascade 조립 계) + golden 15 + doc 1 = 205 PASS / clippy 0(native+wasm32) / 원본 JS 값 대조 13 케이스(none-inherit/override/inherit/initial/unset/important/em/rem/px/unitless/0-falsy/px-0/font-shorthand) 전수 일치. **배선 정정(중요)**: `USE_RUST_LAYOUT_ENGINE=true`(C-2a)로 자체 엔진 layout batch 는 **live** 이지만, DFS 상단 style resolve 는 여전히 JS 잔류(2-B 옵션 A). `tree.rs` 가 `cascade::resolve_style` 호출 0 + `wasm.rs` 미노출 → Rust `resolve_style` 이 JS `cssResolver.ts::resolveStyle` 을 대체하지 않음(테스트 유일 소비자). 이전 세션 요약 "seam 미배선" 은 stale — 자체 엔진 layout 은 live, style resolve 계층만 미배선. **live behavior exercise 면제**: 배선 0줄(tree.rs/wasm.rs 미소비) = 순수 계층, 사용자-가시 동작 미발생 → CLAUDE.md "0 live code change". CHANGELOG 면제(ADR-916 Accepted). **남은 2-A 단위**: `preprocess_style`(비상속 currentColor/키워드 전처리, 순수) + display tag 기반 함수(getElementDisplay 등, tag 도메인 의존) + `implicitStyles.ts`(tag→style 매핑, 2,440줄 — tree.rs 노드 계약 확정 후). **다음 = 2-A `preprocess_style`/implicitStyles 후속 또는 2-B tree.rs 배선(DFS 상단 이관) 또는 2-D P3 구현**. HIGH — 별도 승인.
- **2-A `preprocess_style` 전처리 land — 비상속 cascade 키워드 + currentColor 이식 (배선 없음)** (사용자 "preprocess_style 후속 진행해" → 명시 착수). **실사 선행**: `cssResolver.ts:704 preprocessStyle`(resolve_style 이 상속 속성만 처리하는 것과 달리 비상속 색상 속성의 `currentColor`/`initial`/`unset`/`revert` 를 구체 값으로 변환) 순수 함수 확인. 의존 심볼 3(`css_initial_value`/`is_inheritable_property`/`resolve_current_color`) 기이식, `COLOR_PROPERTIES` Set 만 미이식 → `is_color_property` 로 이식. **TDD RED-GREEN**(test-driven-development skill): `preprocess_style` 미정의 컴파일 RED → GREEN. **산출 2(`cascade.rs`)**: `preprocess_style` + `is_color_property`(:124 COLOR_PROPERTIES = borderColor/backgroundColor/textDecorationColor/outlineColor/boxShadow). **원본 특이 로직 재현**: `initial`/`revert` → 초기값 치환+continue / `unset` → **상속 속성이면 그대로 두고 넘어감**(resolve_style 이 처리, currentColor 미검사) / 비상속이면 초기값 / 문자열 아닌 값·빈 문자열 skip. **검증 exercise**: cargo lib 198(이전 189+preprocess_style 9) + golden 15 + doc 1 = 214 PASS / clippy 0(native+wasm32) / 원본 JS 값 대조 9 케이스(currentColor 치환/boxShadow 복합/non-color untouched/initial/revert/unset-비상속→initial/unset-상속→skip/empty·numeric untouched/initial-missing-fallthrough) 전수 일치. **live 영향 0**: `tree.rs`/`wasm.rs` 미소비 순수 함수(테스트 유일 소비자, resolve_style 과 동일) → CLAUDE.md "0 live code change" live exercise 면제. CHANGELOG 면제(ADR-916 Accepted). **cssResolver.ts 순수 계층 소진**: 남은 2-A 는 전부 tag/spec/store 도메인 의존(`resolveFontStretchWidth` spec SSOT / display tag 함수 / implicitStyles) → 격리 불가, tree.rs(2-B) 노드 계약 선행. **다음 = 2-B tree.rs 배선(DFS 상단 이관 — style resolve 계층 live 화) 또는 2-D P3 구현 또는 implicitStyles(tree.rs 노드 계약 후)**. HIGH — 별도 승인.
- **2-B tree.rs 배선 요청 = stale framing 확인 (이미 완료, 코드 0)** (사용자 "2-B tree.rs 배선 진행해" → 실코드 확인 → AskUserQuestion "이미 완료 확인 — 다른 단위로" 선택). **stale framing 판정([[feedback-listbox-proof-already-landed-stale-framing]] 정면 — 착수 전 grep 의무)**: 요청한 tree.rs layout batch 배선은 **이미 land 완료**. tree.rs 오케스트레이션 4 단위(1/2/3-a/3-b/4) + seam (A)/(B1)/(B2)/(C-1) + (C-2b) dual-run 12/12 diff 0 proof(`bf60f79e6`) + **(C-2a) 런타임 배선 + `USE_RUST_LAYOUT_ENGINE` flip + Chrome MCP live exercise**(`bcd534784`) + 1-E Taffy dead code 제거(`f2ac4860c`) 전부 완료. 자체 Rust 엔진이 이미 live builder 에서 layout batch 계산 중. **재조준 중 발견(보고) — 후속 실측으로 정정됨**: `dualRunLive.test.ts` 로드 실패로 보고했으나, **후속 실측(dualRunLive 복구 요청)에서 실행 방법 오류로 확정** — 루트에서 `pnpm exec vitest run apps/builder/...` 파일 직접 지정 시 루트 vitest(wasm plugin 없음) 실행이라 `.wasm` import 실패. 정상 워크플로(`pnpm test` → `turbo run test` → workspace별 `vitest run`)는 `apps/builder/vitest.config.ts`(`vite-plugin-wasm` 등록)로 실행돼 **12/12 PASS**(`pnpm -F @composition/builder exec vitest run` 확인). 테스트 자체 정상, 복구 불필요([[feedback-git-stash-pop-baseline-measure-hazard]] — 실패를 대상 귀속 전 측정 방법 검증). **코드 0** — 실코드 확인 + 문서만. **다음 = 2-D P3 / implicitStyles**. HIGH — 별도 승인.
- **2-D P3 SpatialIndex 직결 — 이관 대상 제외 (절대좌표=command DFS 종속, 구조적 분리 불가)** (사용자 "2-D P3 SpatialIndex 직결 구현 진행해" → 착수 실사 gap 발견 → AskUserQuestion "scope 재판정 — 절대좌표가 command DFS 종속인가" → "2-D P3 이관 제외 — 근거 기록 후 종료"). **깊은 사고 진입 후 전제·관점 gap 처리(CLAUDE.md literal instruction following + adr-writing M3 추정 vs 실측 gap)**: 설계 v1(계약/oracle만) §443("Rust 가 절대좌표 자체 계산")을 구현하려 착수 실사 → §440("command JS 유지 + SpatialIndex 만 Rust 직결") 구조적 성립 불가 확정. **실측(renderCommands.ts `visitElement`)**: (1) SpatialIndex 소비 절대좌표 `boundsMap`(:426 `absX=parentAbsX+relX`)은 순수 layout 누적이 아니라 **command 생성 DFS 와 불가분 순회** — `getSkiaNode`(visible/transform/contentMinHeight :417) + `sortChildElementsByZIndex`(:539) + `dragRootId`/`deferredDragRoot`(:395,:314) + 자식 재귀 `absX-scrollX`(:549 노드별 scrollOffset 차감) + pagePos 루트 오프셋(:295). (2) §440 실현 = pagePos/scroll/z-sort/contentMinHeight/childrenMap 을 WASM 경계로 넘겨 Rust 가 절대좌표 누적 **재순회** = JS command DFS 1회 + Rust bounds DFS 1회 **이중 순회**. (3) 벤치 `syncSpatialIndex` 재직렬화 **0.099ms(3000 노드, 예산 0.6%)** — 이중 순회 비용(경계 전달 + Rust 재순회)이 이 이득 초과. 절대좌표를 command DFS 에서 떼면 z-sort/drag/scroll/skiaData 조회 태반을 Rust 로 옮기는 것 = §440("command 무관") 모순. **결론**: 2-D 는 [[feedback-rust-migration-candidate-bench-justifies-not-gates]] 의 "이관 후보 전제" 였으나 **벤치(병목 아님) + 구조적 분리 불가(절대좌표=command DFS 종속)** 함꼐면 정당화 축(성능/main-thread/경계 최소화) 모두 소멸 → **이관 대상 제외**(2-C 잘못 짚은 대상 / 2-E 아키텍처 차단과 유사, 2-D 는 "경계 횡단만 분리 불가" 종류). SpatialIndex 갱신 = `syncSpatialIndex`(JS) 유지가 정본(0.099ms 무해). command stream 자체 Rust 화(원안 commands.rs)는 벤치 병목 아님+SkiaNodeData 경계 표면 과대 → 별도 상위 판단(현 breakdown 범위 밖). **차단 메모리 우선 평가**: [[feedback-no-derived-adr-mid-execution]](재판정 후 다른 단위 자동 착수 금지) + [[feedback-execute-adr-surface-minimization]](이중 순회 표면 과대) + [[feedback-rust-migration-candidate-bench-justifies-not-gates]](정당화 축 소멸). **코드 이관 0** — breakdown §2-D 제목 재정의(이관 제외) + P3 재판정 블록 + P3 계약/diff oracle `<details>` 이관 + ADR status log 만, live 영향 0. type-check 무관(문서만). **Phase 2 재평가 최종 종결**: 2-C(안 A 종료)/2-D(이관 제외)/2-E(이관 제외) — 세 렌더 phase 재평가 완료. 남은 실제 Rust 이관 진입점 = **없음(Phase 2 렌더 계층)**. 잔여는 2-A implicitStyles(tag/spec 도메인 의존, tree.rs 노드 계약 선행 = 사실상 JS 잔류) + dualRunLive 복구(테스트 인프라). HIGH — 별도 승인.
- **2-A implicitStyles 이관 가치 재판정 — 이관 대상 제외 (JS 잔류 확정, 코드 0)** (사용자 "2-A implicitStyles 이관 가치 재판정" → 실측 계측 → 이관 제외 결론). **깊은 사고 진입 후 실측 판정(추정 아님 — `applyImplicitStyles` 2,440줄 계측)**: 태그 이름 하드코딩 분기 `containerTag ===`/`child.type ===` **59회**(Menu/TagGroup/TagList/Field/side-label 컴포넌트별 특수 처리) / spec·catalog·rule 조회 **131회**(D3 시각 SSOT catalog COMPONENT_RULES_TABLE 직결) / 노드 그래프 탐색 `findAncestorByTag`·`getChildElements`·`elementById`·`parent_id` **23회** / token 조회 **21회** / synthetic 자식 생성(`__synlabel`) + 자식 style mutation(`injectSideLabel*`/`injectCollectionItemFont`). **순수 layout 계층 아님**: (1) 입력이 store 노드 그래프(`CanvasLayoutNode`+`getChildElements` 콜백+`elementById` Map, 조상/자식 순회) — Rust `tree.rs` 노드 계약 상이. (2) 출력이 자식 Element 그래프 변형(`filteredChildren` 재작성 + `__synlabel` synthetic 삽입) = layout 결과 산출이 아니라 **DFS 입력 트리 자체를 다시 씀**. (3) D3 시각 SSOT(catalog) 131회 read-through → 이관하려면 catalog COMPONENT_RULES_TABLE 전체 Rust 선이관 필요, 그런데 catalog Rust 이관은 §2-CAT 미착수 + P2-PROP 종속(propagation 선결도 아님, [[project-adr916-catalog-not-propagation-prereq]]). **정당화 3축 소멸**: 단일 엔진/SSOT(implicitStyles=catalog 소비자이지 layout 엔진 일부 아님, Rust 이관해도 catalog JS 잔류 시 FFI 왕복만 증가) / main-thread 제거(태그 분기·조상 탐색=layout solve 대비 미미, hot-path 병목 아님) / catalog 선결 부재(catalog Rust 이관 미착수+P2-PROP 종속 → 순서상 착수 불가). [[feedback-rust-migration-candidate-bench-justifies-not-gates]] 정합 — 후보였더라도 정당화 3축 모두 소멸 시 이관 취소, 2-D P3 제외와 동형(순수 layout 아니라 다른 계층 얽힘). **차단 메모리 우선 평가**: [[feedback-no-derived-adr-mid-execution]](재판정 후 다른 단위 자동 착수 금지) + [[feedback-rust-migration-candidate-bench-justifies-not-gates]](정당화 축 소멸) + [[feedback-infra-exists-vs-wired-consumption-path]](catalog lookup 인프라 존재≠implicitStyles 이관 가능). **코드 0** — 실측 계측 + breakdown §2-A 재판정 블록 + ADR status log 만, live 영향 0. type-check 무관(문서만). **2-A 최종**: cssResolver.ts 순수 계층 소진(resolveStyle/preprocessStyle 이관) + implicitStyles 이관 제외 확정 → **2-A 잔여 Rust 이관 진입점 없음**. HIGH — 별도 승인.
- **endgame(Taffy 완전 제거) 재평가 — 제거 보류 (시점 미도래, kill criteria 명문화, 코드 0)** (사용자 "ADR-916 endgame Taffy 완전 제거 재평가" → 실측 재평가 → 제거 보류). **깊은 사고 진입 후 실측 판정(추정 아님)**: Taffy 참조 40+ 파일/768 라인(테스트 제외). **폴백 배선 live(dead 아님)**: `layoutBridge.ts:84` `return new TaffyLayout()` 무조건 도달 + `:71` 자체 엔진 `isAvailable()`(compositionEngine.ts:94 `engine!==null`) false(WASM 미준비=startup init 전/로드 실패) 시 런타임 실발동. **crate 물리 결합**: `composition_wasm` crate(`wasm/src/lib.rs:3-10`)가 **SpatialIndex + TaffyLayoutEngine 동일 crate** → Taffy crate/pkg 물리 삭제 시 SpatialIndex 동반 삭제. **dual-run oracle=Taffy**: `dualRunEngines.ts` 가 Taffy pkg 를 fixture 로드, dualRunLive 12/12 diff 0 proof 의 비교 기준이 Taffy. **3 구조적 장애물**: (1) Taffy=유일 검증 oracle → 제거 시 dual-run CI 회귀 안전망 소멸([[feedback-proof-gate-seam-removal-kill-criteria]] — oracle 제거=kill criteria 부재 전환), (2) crate 물리 결합 → SpatialIndex 별도 crate 분리 선행 필요=endgame 자체보다 큰 리팩토링([[feedback-execute-adr-surface-minimization]] 표면 과대), (3) 폴백=live 안전망 → 1-E 에서 사용자 명시 "폴백 유지" 선택, 자체 엔진 미Implemented(Status=Accepted) 상태 안전망 제거 시기상조. **endgame kill criteria(모두 충족 시 재개 — 안정화 기간 조건은 사용자 결정 2026-07-06으로 제거, 현재 충족 0/3)**: ① 독립 oracle 확보(Taffy-비의존 회귀 기준=golden 확장 또는 CSS getComputedStyle parity) / ② SpatialIndex crate 분리 / ③ ADR-916 Status=Implemented 승격. R4(폴백 로직 이중화 HIGH)는 endgame 까지 잔존 유지(dual-run CI 상시 관리). **1-E 정정**: 원안 §1-E("Cargo.toml taffy 삭제 + taffy_bridge.rs 폐기")는 crate 물리 삭제였으나 실제 land(`f2ac4860c`)는 dead code 제거만 — crate 물리 삭제는 §1-F endgame 으로 이연. **코드 0** — breakdown §1-F endgame 판정 블록 + §1-E 실제 land 범위 정정 + ADR status log 만, live 영향 0. type-check 무관(문서만). HIGH — 별도 승인.
- **SpatialIndex crate 분리 land — endgame kill criteria ② 충족 (1/3)** (사용자 "SpatialIndex crate 분리 착수 가능한지 실측" → 실측 착수 가능 → AskUserQuestion 2건 "지금 착수 — 독립 정리" + "composition-engine 로 이동"). **깊은 사고 진입 후 실측 판정**: `spatial_index.rs`(393줄, 테스트 16)는 **완전 self-contained** — crate 내부 모듈 참조 0(taffy/block/grid/binary) + taffy crate 심볼 0 + 역방향 참조 0, 의존성 std `HashMap/HashSet`+wasm-bindgen 뿐. endgame 재평가의 "crate 물리 결합"은 코드 결합이 아니라 물리 배치만 → 분리=파일 이동+빌드 설정(저위험·소표면). **관점 점검(AskUserQuestion, 차단 메모리 자기-인용)**: SpatialIndex 분리만 미리 진행=dormant([[feedback-no-dormant-foundation-ahead-of-flip]]) vs 코드 결합 0이라 무해한 독립 정리 → 사용자 "지금 착수 — 독립 정리" 선택(hit-test 는 layout/oracle 독립, endgame 과 분리된 정리). **이동 대상 crate(AskUserQuestion)**: 신규 composition-spatial vs composition-engine 편입 → 사용자 "composition-engine 로 이동"(신규 crate/pkg 미생성, [[feedback-execute-adr-surface-minimization]] 최소 표면). **구현**: (1) `git mv spatial_index.rs` → composition-engine/src, lib.rs `pub mod spatial_index`+`pub use SpatialIndex`, 원본 composition_wasm/lib.rs 에서 제거. (2) JS: `compositionEngineWasm.ts` `RawSpatialIndex` 인터페이스+`SpatialIndex` 추가, `spatialIndex.ts` 로드 소스 `getRustWasm()`(Taffy)→`getCompositionEngineWasm()`(자체 엔진), `init.ts` `initSpatialIndex()` 호출을 `initCompositionEngineWasm().then()` 으로 이동. **검증**: cargo test native lib 211 PASS(spatial_index 16 편입)+clippy 0(native+wasm32, aabb_intersects 8-arg allow) / type-check PASS(69) / dualRunLive 12/12 무회귀. 두 pkg 재빌드 후 Taffy pkg SpatialIndex 0건·composition-engine pkg 1건(완전 분리 확증). **Chrome MCP live exercise**(test/type-check 단독 종결 금지 이행): builder 진입 → composition-engine WASM initialized 로그+미초기화 경고 0 → Canvas Button hover 시 **파란 outline**(HoverManager query_point) + click 시 **selection handles+"69×30" bounds 배지**(query_point→선택→bounds) 정상, 콘솔 에러 0 → SpatialIndex 가 composition-engine pkg 에서 hit-test 전체 경로 실작동 확증. **live 영향**: 로드 소스만 이동, API·동작 동일 → 사용자-가시 변화 0. **endgame ② 충족(1/3)** — 나머지 ①·③ 미도래라 Taffy crate/pkg 물리 삭제 여전히 보류. 6 파일(Rust 3+JS 3, pkg gitignore). HIGH — 별도 승인.
- **tree_golden 독립 oracle land — endgame kill criteria ① 충족 (1/3→2/3)** (사용자 "dualRunLive/independent oracle 안정화 먼저" → 관점 정리 → AskUserQuestion 2건 "Chrome 실측 gentest(원안 §105)" + "native cargo golden 상수 고정"). **순환 oracle 문제**: 현 실전-트리 검증 dualRunLive 의 비교 기준(oracle)이 제거 대상 Taffy 자체 → Taffy 를 빼면 실전 트리 회귀를 잡을 독립 기준 부재. Chrome 렌더를 제3 독립 권위(ground truth)로 삼아 해소(typography 도메인 `typographyCssParity.ts` L0 "독립 권위 leg" 패턴의 layout 확장). **추출(설계 A)**: dualRunLive C-2b fixture N1~~N5 를 DOM 재구성(리셋 CSS margin/padding/border 0+box-sizing:border-box → content-box=border-box, NodeLayout=content-box 정합) → `getBoundingClientRect` root-상대 추출 → 사람 검토(gap 누적/grid 1fr=100px/auto row stretch CSS 명세 대조) → float 상수. **대조(설계 B)**: `packages/composition-engine/tests/tree_golden.rs` 신규 — `build_tree_batch`(dualRunLive 동일 JSON 계약) → `compute_layout(root,200,-1)` → `get_layouts_batch` → **조상 offset 누적** 절대 좌표 변환 → root-상대 정규화 → `assert_tree_bounds`(TOL=1.0px). golden.rs(단일 컨테이너 평면)와 분리(트리 계약). **root cause 정정(systematic-debugging)**: 초기 헬퍼가 root offset 만 빼고 중간 조상 offset 미누적 → N2(grid 2열 셀 자식) 100px 발산. tree.rs compute_layout(§417)은 자식 좌표를 **부모 content-box 상대**로 산출하는 정상 계약(taffy_bridge.rs 동일) → 원인은 tree.rs 가 아니라 **테스트 하네스 좌표계**. `parse_parents`+조상 누적 추가로 6/6 GREEN(성급한 tree.rs 수정은 relative 계약 파괴=live 렌더 파손 위험이었음, systematic-debugging 이 차단). **검증**: cargo test lib 211+golden 15+**tree_golden 6**(N1~~N5+field guard)+doc 1 PASS, ignored 0 / clippy 0(native --tests+wasm32). **변조→RED 실증**: N1 n1-b x 30→35(5px 주입) → `node[1].x=30 (want 35, Δ=-5.0px)` FAILED=oracle 실제 회귀 검출 증명 → 즉시 되돌림(커밋 0, `git diff --stat` 무변경). serde_json dev-dep 추가. **live 영향 0**: 신규 test 파일만, tree.rs/live 무변경. **범위=criteria ① 하나만** — ③(Implemented 승격) 미충족이라 Taffy 물리 삭제 여전히 보류(2/3). dualRunLive Taffy leg 유지(안전망) — tree_golden 은 Taffy-비의존 독립 감시 추가이지 Taffy oracle 대체 아님. 3 파일(tree_golden.rs+Cargo.toml+docs). HIGH — 별도 승인.
- **Taffy 완전 제거 land — endgame 완결 (R4 폴백 이중화 HIGH 소멸, 코드 실삭제)** (사용자 "endgame Taffy 물리 삭제 착수 가능한지 실측" → 실측 2단계 분리 판정 → AskUserQuestion "어느 범위로 착수" → "2단계까지 진행해" + "이번에 물리 삭제까지" 승인 → brainstorming → writing-plans(Fable 5) → subagent-driven 7 태스크). **깊은 사고 진입 후 실측 판정(추정 아님)**: Rust crate 인벤토리 정확화 — `composition-layout`(taffy 0.10, Phase 0-A 폐기, live 미소비) + `composition-wasm`(taffy 0.9, 3,578라인, R4 폴백 실체 `rustWasm.ts`→`TaffyLayout`) + `composition-engine`(taffy-free, live 존치). **2단계 분리(관점 lock-in)**: 1단계=composition-layout dead crate(LOW), 2단계=composition-wasm 폴백 crate + R4 안전망 제거(HIGH, 자체 엔진 단독 운영 확정). **Q1=B(재시도 강화 후 hard fail)** — Taffy 폴백 제거, 신규 JS 폴백 미작성, 기존 15초 폴링/재시도 부트스트랩이 보상. **Q2=A(이번에 물리 삭제까지)** — crate 디렉토리 `git rm` 명시 승인. **삭제**: crate 2종 + pkg 452K + JS 13파일(taffyLayout/rustWasm/layoutEngine + wasm-worker 5 + dual-run 5). **재배선(HIGH, R2)**: `createLayoutEngine()` 자체 엔진 단독 반환 + 부팅 게이트 `isRustWasmReady`→`isCompositionEngineReady`(useCanvasRuntimeBootstrap 폴링 구조 유지 + fullTreeLayout:2188 live 게이트) + init.ts Taffy pkg·dead LAYOUT_WORKER 제거 + 타입 이전(LayoutResult→compositionEngine, TaffyStyle→layoutTypes). **보존**: TaffyFlex/Block/GridEngine(이름만 Taffy — 순수 JS 변환, fullTreeLayout live 소비) + persistentTaffyTree.ts. **검증**: type-check baseline 69 신규 0 / composition-engine cargo test 233 PASS(Taffy crate 삭제 후 자체 엔진 무손실) / **Chrome MCP live exercise(test/type-check 단독 종결 금지 이행)** — builder 부팅 + Taffy/WASM 콘솔 에러 0(inlinealert 는 선재 무관) + Skia canvas webgl non-empty + 요소 흐름 배치(8 distinct y 위치, (0,0) 뭉침 없음) + Canvas↔CSS 시각 정합(Text/Badge/Progress 50%/InlineAlert/Button) + `window.TaffyLayout`/`getRustWasm` 전역 부재 → **폴백 없이 자체 Rust 엔진 단독 layout batch 계산 확증**. **R4(폴백 로직 이중화 HIGH) 소멸** — Taffy 폴백 경로 물리 삭제로 로직 이중화 자체 제거. dualRunLive Taffy leg 도 삭제와 함께 소멸, tree_golden(Taffy-비의존 Chrome 실측 oracle)이 tree.rs 회귀 감시 단독 지속. **endgame 완결** — Phase 1 layout 엔진 self-impl(flex/block/grid/tree) + Taffy 완전 제거 완료. commit 15635b194/5a58a2fc4/d5e445e9a/62b5a4a87+f2efd2596/e82931bc8/022f43c5a + 문서. HIGH.
- **명명 잔재 정리 — ADR-923 Phase 6 (2026-09-03, `7f1cf963d`)**: Taffy 완전 제거 뒤에도 JS 어댑터의 `Taffy*` 식별자 (파일 5 · 심볼 30종 — `TaffyStyle` · `PersistentTaffyTree` · `toTaffyDisplay` · `applyCommonTaffyStyle` 등) 가 "Taffy 가 계산한다" 로 읽혀 ADR-923 분석을 잘못 이끌었다 (ADR-923 R10). `displayAdapter` · `flexStyleAdapter` · `blockStyleAdapter` · `gridStyleAdapter` · `persistentLayoutTree` · `Engine*` 타입으로 개명 — 스키마 (`StyleInput`, Taffy 0.9 계보) 는 무변경. 지도: evidence/923-phase6-naming-capability-seed.md §1.
- **후속 ADR-164 (2026-07-25 Implemented)**: 본 ADR 의 성공 기준이 **Taffy 동등성**(dualRunLive diff 0)이었기에 Taffy 시대 TS 보정 레이어(overflow×flexShrink Step 5.7 / min-width:auto 에뮬레이션)가 엔진 교체 후에도 상류에 남았다 — dual-run diff 0 방법론은 상류 보정이 가로챈 입력 차원에 구조적으로 blind ([[feedback-dual-run-diff-zero-blind-to-uncovered-input-dimension]]). [ADR-164](164-engine-ts-compensation-absorption.md) 가 automatic minimum size(CSS-FLEXBOX-1 §4.5)를 엔진에 흡수(`flex.rs` content-based minimum floor + `FLEX_FIELD_COUNT` 18→19 주축 overflow)하고 Step 5.7 을 제거, position:absolute 잔여 2건은 실사용 0건 실측으로 "의도적 미지원" 명문화. 엔진↔TS 경계는 `layout-engine.md` §"TS 잔존 계약" 으로 규칙화.

## Context

**3-Domain 판정**: 본 ADR 은 D3(시각 스타일) consumer 인 Builder(Skia) 렌더 경로의 **내부 구현 계층** 재구축이다. D1(DOM/접근성) / D2(Props/API) 무관. D3 시각 정본 접점은 style resolution 이관(Phase 2-A) 시 **catalog SSOT** (`componentCatalog.ts` + `COMPONENT_RULES_TABLE`, ADR-912/913/914 cutover 완결 — 잔존 spec 은 Frame/Group/Slot 3개 영구 예외) 파생 스타일 값 보존 하나 — /cross-check 로 검증한다. catalog/Generator 확장 없음 (Generator emit 지원 질문 해당 없음).

composition Builder 렌더링 파이프라인은 JS 64,316줄(92%) + Rust 5,633줄(8%) 로 구성된다. Rust 는 외부 라이브러리 Taffy 를 래핑한 layout solve 에 국한되고, 나머지 전 단계(scene graph 동기화 / 스타일 해석 / DFS 오케스트레이션 / 렌더 커맨드 생성 / 텍스트 측정)가 JS main thread 에서 실행된다. 실측 병목 코드 경로:

- `apps/builder/src/builder/workspace/canvas/layout/engines/persistentTaffyTree.ts` (423줄) — dirty 검출을 노드당 JSON.stringify 문자열 비교로 수행
- `apps/builder/src/builder/workspace/canvas/layout/engines/fullTreeLayout.ts` (2,861줄) — batch 직렬화 병목: 초기 빌드 `buildTreeBatch(JSON.stringify(payload))` 전체 트리 직렬화 + 증분 갱신 시 변경 노드당 `updateStyleRaw` JSON.stringify, 2-pass 보정 최악 3× solve (computeLayout 호출부 3곳: `:2343/:2602/:2718`). (참고: per-node createNode/updateStyle/setChildren/computeLayout/getLayout 5-call API 는 비활성 composition-layout 쪽 인터페이스)
- `apps/builder/src/builder/workspace/canvas/skia/renderCommands.ts` (1,091줄) — 매 content 프레임 O(N) DFS + z-sort + boundsMap 재생성
- `apps/builder/src/builder/workspace/canvas/skia/buildSpecNodeData.ts` (1,786줄) — 노드당 조상 체인 탐색 O(N×D)
- `apps/builder/src/builder/workspace/canvas/skia/StoreRenderBridge.ts` (743줄) — detectChangedIds O(N) Map 순회
- 텍스트 측정 — 캐시 미스 시 다중 WASM 왕복 + Canvas2D/CanvasKit 이중 측정 경로

프레임 경로에서 WASM 경계를 5회 횡단한다 (스타일 직렬화 / Taffy solve / 레이아웃 역직렬화 / SpatialIndex 쿼리 / CanvasKit draw).

교체 대상 외부 라이브러리 래핑은 **Taffy 가 유일**하다 (serde / wasm-bindgen 은 플랫폼 인프라). **Skia(CanvasKit) 는 유지** — 픽셀 렌더링 엔진 교체는 본 ADR 범위 밖. 자체 구현 Rust 모듈(block_layout 625줄 / grid_layout 279줄 / spatial_index 393줄 / binary_protocol 1,347줄)이 이미 존재하며, Taffy 0.10 기반 신규 엔진(`packages/composition-layout`, 1,660줄)은 `USE_RUST_LAYOUT_ENGINE=false` 뒤에 비활성 상태다. Worker 인프라(`wasm-worker/` 672줄) 역시 `LAYOUT_WORKER=false` 로 비활성.

**Hard Constraints**:

1. Canvas 60fps @ 1000+ 노드 (CLAUDE.md 성능 기준) — 이관 각 단계에서 프레임타임 회귀 금지
2. 초기 번들 < 500KB — WASM lazy-load 경로 유지, 엔진 WASM 증가 gzip +300KB 이내
3. **BC 수식화**: 기존 프로젝트 문서 100% 가 layout 영향권 — 기준 2단: (a) dual-run **수치** diff ≤ 1px (f32 sub-pixel tolerance, 엔진 간 부동소수점 drift 허용) (b) **시각** diff 0 = 1x zoom device pixel 스크린샷 diff 0 — 수치 drift 가 동일 device pixel 로 라운딩되는 범위만 허용. (a) 통과 + (b) 위반 (예: 0.5px drift 가 픽셀 경계를 넘어 라운딩 차이 유발) 시 (b) 가 우선 — FAIL
4. D3 대칭: Builder(Skia) ↔ Preview(DOM+CSS) 시각 결과 동일 — /cross-check 전수 PASS
5. WASM 경계 횡단 프레임 경로 5회 → 2회 (통합 엔진 batch 호출 + CanvasKit draw)

**Soft Constraints**:

- Rust 유지보수 역량 — 코드 리뷰 가능 인력 제한 (버스팩터)
- CSS Flexbox/Grid spec 자체 구현의 검증 비용 — Taffy 가 축적한 WPT-파생 테스트 자산 상실 위험
- 대규모 코드 생성 도구 (Fable 5 등) 활용 시 생성 코드 검증 부담

## Alternatives Considered

### 대안 A: 현상 유지 + JS 측 점진 최적화

- 설명: Taffy 유지, JS 병목 지점만 개별 최적화 (JSON.stringify → 해시, memoization 확대)
- 근거: 최소 변경 원칙, 회귀 위험 0
- 위험:
  - 기술: L — 검증된 경로 유지
  - 성능: **H** — 노드당 ~5회 WASM 경계 횡단은 JS 최적화로 구조적으로 해소 불가
  - 유지보수: M — 이중 Rust crate (0.9/0.10) + 외부 라이브러리 버전 종속 지속
  - 마이그레이션: L — 변경 없음

### 대안 B: Taffy 유지 통합 (Quick Win 만)

- 설명: composition-layout 배선 + `USE_RUST_LAYOUT_ENGINE=true`, LayoutScheduler 소비 배선 + `LAYOUT_WORKER=true` 로 종료. Taffy 0.10 단일화, 파이프라인은 JS 유지
- 근거: 인프라 코드가 이미 존재 (composition-layout 1,660줄 + wasm-worker 672줄). **단 flag 단독 전환은 무효** — layoutBridge 는 flag true 시 경고 후 TaffyLayout fallback (`layoutBridge.ts:36-44`), persistentTaffyTree 는 factory 미경유 직접 생성 (`persistentTaffyTree.ts:27,84`), scheduler 소비 caller 0건 (`wasm-worker/index.ts:32`) → 엔진 주입 + batch API 정합 + 소비 배선의 소규모 통합 작업 필요. worker 는 BLOCK_LAYOUT/GRID_LAYOUT 가속기만 처리 — Taffy full-tree solve 는 main thread 잔류 (`layoutWorker.ts:33-38`)
- 위험:
  - 기술: L — flag 전환 + 기구현 코드
  - 성능: M — scene/commands/style/text 병목 잔존, 경계 횡단 5회 유지
  - 유지보수: M — Taffy 외부 종속 영구화 (버전 업그레이드마다 layout 결과 변동 리스크 반복 — 0.9→0.10 도 결과 차이 존재)
  - 마이그레이션: L — flag revert 가능

### 대안 C: 일괄 전면 재작성 (big-bang)

- 설명: 단일 composition-engine crate 를 한 번에 작성, 완성 시점에 일괄 전환
- 근거: Figma (C++ 자체 엔진 → WASM, JS 는 UI shell) 업계 전례 — 최종 아키텍처 형태 자체는 검증된 방향
- 위험:
  - 기술: **C** — CSS Flexbox/Grid spec 전체를 중간 검증 지점 없이 일괄 구현 (Taffy 가 수년 축적한 spec compliance 를 단일 사이클에 재구현)
  - 성능: L — 최종 형태는 개선
  - 유지보수: H — 거대 단일 diff, 리뷰 불가
  - 마이그레이션: **C** — 전환 실패 시 전면 롤백, 회귀 원인 국소화 불가

### 대안 D: 단계적 단일 엔진 통합 (Phase 0→1→2, 게이트 기반)

- 설명: Phase 0 (기존 인프라 **배선 + flag 활성화** = 대안 B 내용 흡수 — flag 단독 전환 아님) → Phase 1 (Taffy 제거 — flex.rs 자체 구현 + dual-run 게이트) → Phase 2 (파이프라인 5개 모듈 순차 이관, 모듈별 게이트). 각 단계 독립 검증 + fallback flag 유지
- 근거: Taffy 자체가 Chrome 실측 기반 gentest fixture 로 spec 준수를 검증 — 동일 방법론 포팅 가능. Yoga(Meta) 의 flexbox subset 구현이 spec 불일치 장기 부채가 된 전례 → full spec + Chrome 실측 fixture 방식 채택. Flutter Web CanvasKit renderer (엔진 WASM + CanvasKit draw 분리) 와 동일 구조
- 위험:
  - 기술: **H** — flex.rs 자체 구현. 단 dual-run + WPT-파생 fixture 로 구간별 검증
  - 성능: L — 각 Phase 벤치 게이트
  - 유지보수: M — 이관 기간 JS/Rust 이중 경로 공존
  - 마이그레이션: M — 모듈별 fallback flag, 단계별 롤백 가능

### Risk Threshold Check

| 대안 | 기술 | 성능 | 유지보수 | 마이그레이션 | HIGH+ 개수 |
| ---- | ---- | ---- | -------- | ------------ | :--------: |
| A    | L    | H    | M        | L            |     1      |
| B    | L    | M    | M        | L            |     0      |
| C    | C    | L    | H        | C            |  3 (C×2)   |
| D    | H    | L    | M        | M            |     1      |

루프 판정: C 에 CRITICAL 2건 → 근본적으로 다른 접근 필요 → 대안 D (단계적 + 게이트) 추가로 해소. D 의 잔존 HIGH 1건 (기술 — flex 자체 구현) 은 G2 dual-run 게이트로 관리하며 수용. B 는 HIGH 0 이지만 본 ADR 의 목표 (외부 라이브러리 래핑 제거 + 단일 엔진) 를 달성하지 못함.

## Decision

**대안 D: 단계적 단일 엔진 통합**을 선택한다.

선택 근거:

1. **위험 수용 근거**: 유일한 HIGH (flex.rs 자체 구현) 는 Taffy 와의 dual-run 비교 + Chrome 실측 fixture 로 이관 전 구간에서 검증된다. Taffy fallback flag 가 G2 통과까지 유지되므로 실패 시 손실은 신규 코드 폐기로 한정
2. Phase 0 은 이미 존재하는 인프라 코드의 **배선(엔진 주입 + batch API 정합 + scheduler 소비 배선) + flag 활성화** 로 JSON dirty 검출 제거 + block/grid 가속 경로 worker offload 를 획득 (flag 단독 전환 무효 — 실사 근거는 breakdown Phase 0 참조) — 후속 Phase 의 성능 기준선 측정 확보
3. 최종 상태 (JS ~15,000줄 UI 바인딩 + 단일 WASM batch API + CanvasKit draw) 는 Figma / Flutter Web 에서 검증된 아키텍처

기각 사유:

- **대안 A 기각**: 노드당 ~5회 WASM 경계 횡단은 JS 측 최적화로 구조적으로 해소 불가 — 성능 HIGH 잔존
- **대안 B 기각**: Taffy 외부 종속 영구화 — 본 ADR 의 문제 정의 (외부 라이브러리 래핑 제거) 자체를 미해결. 단, B 의 실행 내용은 D 의 Phase 0 으로 흡수됨
- **대안 C 기각**: CRITICAL 2건 (중간 검증 지점 없는 spec 전체 구현 + 전면 롤백 리스크). 동일 목적지를 D 가 게이트 기반으로 도달

> 구현 상세: [916-unified-rust-engine-breakdown.md](../design/916-unified-rust-engine-breakdown.md)

## Risks

| ID  | 위험                                                                | 심각도 | 대응                                                                                                                                                                                                                                     |
| --- | ------------------------------------------------------------------- | :----: | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1  | flex.rs 자체 구현의 CSS spec 결함 (Taffy WPT-파생 테스트 자산 상실) |  HIGH  | G2 dual-run diff + Chrome gentest 방식 fixture 포팅. 통과 전 Taffy fallback 유지                                                                                                                                                         |
| R2  | 이관 기간 JS/Rust 이중 경로 drift (동일 로직 양측 수정 누락)        |  MED   | 모듈별 cutover 완료 시 JS 경로 즉시 삭제 (dormant 병행 금지)                                                                                                                                                                             |
| R3  | Rust 유지보수 버스팩터                                              |  MED   | 모듈 경계 = CSS spec 章 단위 유지, gentest fixture 가 회귀 안전망                                                                                                                                                                        |
| R4  | 대규모 생성 코드 (Fable 5 활용) 검증 부담                           |  MED   | 모듈당 fixture-first: fixture 작성 → 생성 → dual-run — 생성 코드는 fixture 통과로만 수용                                                                                                                                                 |
| R5  | WASM 번들 증가                                                      |  MED   | G4 사이즈 게이트 (gzip +300KB 이내), wasm-opt + 모듈 분리 로딩 대비                                                                                                                                                                      |
| R6  | text.rs 이관이 Layout=Canvas2D=CSS 정합 원칙 파괴                   |  MED   | canvas-rendering.md §3 규칙 승계 — **측정 경로 한정**: Paragraph 객체 캐싱 금지, 결과값만 LRU (`canvaskitTextMeasurer.ts:122`). render 경로의 관리형 Paragraph 캐시(`nodeRendererText.ts:36`) 는 2-E 비대상·현행 유지. /cross-check 필수 |
| R7  | Taffy 0.9→0.10 전환 (Phase 0-A) 자체의 layout 결과 변동             |  MED   | G1 회귀 fixture 전수 — flag 전환이라도 dual-run 검증                                                                                                                                                                                     |

## Gates

| Gate | 시점                          | 통과 조건                                                                                                | 실패 시 대안                                       |
| ---- | ----------------------------- | -------------------------------------------------------------------------------------------------------- | -------------------------------------------------- |
| G1   | Phase 0 배선 + flag 전환 직후 | 회귀 fixture 전수 PASS (HC3 2단 기준: 수치 ≤ 1px + 1x zoom 스크린샷 diff 0) + 1000노드 60fps 유지        | flag revert (즉시 롤백)                            |
| G2   | Phase 1 flex.rs 완성 시       | dual-run 수치 diff ≤ 1px + 1x zoom 스크린샷 diff 0 (전 fixture, HC3 2단 기준) + Chrome 실측 fixture PASS | Taffy fallback 유지, flex.rs 반복 수정 (제거 보류) |
| G3   | Phase 2 각 모듈 cutover       | /cross-check PASS + 1000노드 프레임타임 개선 실측 + type-check                                           | 해당 모듈 JS 경로 유지 (모듈 단위 보류)            |
| G4   | 각 Phase 빌드                 | WASM gzip +300KB 이내                                                                                    | wasm-opt 재조정 / 모듈 분리 로딩                   |
| G5   | Phase 2 착수 전               | 사용자 scope confirm (5 모듈 분할 — M4 의무) + Phase 1 실측 기반 재판정                                  | Phase 2 를 후속 ADR 로 분리                        |

## Consequences

### Positive

- WASM 경계 횡단 프레임 경로 5회 → 2회 — 직렬화 / GC 압력 구조적 제거
- JSON.stringify dirty 검출 / O(N) 변경 감지 / O(N×D) 조상 탐색 → 해시 · bitfield · 1-pass 로 대체
- 외부 라이브러리(Taffy) 버전 종속 제거 — layout 동작이 자체 fixture 로 고정, 업그레이드 리스크 소멸
- JS 렌더 파이프라인 ~64K줄 → ~15K줄 (UI 바인딩) — canvas 모듈 인지 부하 감소
- 텍스트 측정 이중 경로 (Canvas2D / CanvasKit) 불일치 문제의 구조적 해소 기반

### Negative

- flex/grid/block spec 유지 책임이 composition 으로 이전 — CSS spec 갱신 추적 의무 발생
- 이관 기간 (Phase 1~2) fallback flag 분기 유지 비용
- Rust 코드량 5,633줄 → ~12,000줄+ 추정 — 리뷰 가능 인력 제약
- 회귀 fixture / dual-run 하네스 / 벤치 하네스 신규 구축 비용 (Phase 0 선행 투자)
