# 외부 프로젝트 패턴 대조 — webstudio (2026-09-08)

> [webstudio-is/webstudio](https://github.com/webstudio-is/webstudio) 로컬 체크아웃 `/Users/admin/work/webstudio` (main `05706b542`, 2026-09-06) 를 composition 의 현재 상태 (before) 와 적용 후 형태 (after) 로 대조한 문서. 라이브러리·플러그인 도입이 아니라 **설계·규칙만** 판정한다. 근거는 양쪽 저장소의 파일 경로. 형식은 [EXTERNAL_PATTERN_DELTA_2026-09.md](EXTERNAL_PATTERN_DELTA_2026-09.md) 를 따른다.
>
> webstudio 는 iframe DOM 캔버스 + Remix 빌더 + 코드 생성 publish 구조다. composition 은 Skia 캔버스 + DOM Preview/Publish + Rust 레이아웃 엔진이라 **캔버스 층은 대부분 비적용**이고, 적용 가치는 데이터 모델 · 스타일 시스템 · 품질 게이트 · AI 도구 계약에 몰려 있다. 이전 기록: [STYLE_PARSING.md](STYLE_PARSING.md) §3.1 (2025-12, css-data · atomic CSS 언급) — 그때 표시한 gap (css-tree · mdn-data 부재) 은 2026-09-08 현재도 그대로다 (`apps/builder/package.json` · `packages/*/package.json` 에 두 의존성 0건).

## 0. 요약

| 등급              | 건수 | 내용                                                                                                                                                                                                                                                                                                                                                                            |
| ----------------- | ---: | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **A 권장**        |    5 | ① 생성물 drift 를 `git status` 로 판정하는 단일 게이트 ② 번들 크기 상한 게이트 ③ Styles 패널 스크럽 중 live 미리보기 (ephemeral 채널) ④ 스타일 값 origin 배지 + 조건부 reset ⑤ AI tool 정의를 commandRegistry 에서 생성 + drift 검사. ①②⑤ 는 **동작 변경 0**, ③④ 는 패널 한정 동작 변경                                                                                         |
| **B 조건부**      |    6 | ⑥ 순수 runtime op + accumulator (undo 단위 = op 묶음) ⑦ css-data 값 검증 (`invalid` 1급 값) ⑧ UA 기본 스타일을 데이터로 (측정 1건 선행) ⑨ HTML content model 제약 (D1 경계 판정 필요) ⑩ 공유 style source (D3 스키마 확장 — 결정 지점) ⑪ AI 평가 회귀 (키 필요)                                                                                                                 |
| **C 보류**        |    8 | namespace 분할 patch + CAS 저장 · migration-as-transaction · TS 타입 → prop meta 생성 · 템플릿 JSX DSL · 클립보드 플러그인 체인 (HTML paste) · 조건 breakpoint 시뮬레이션 · 문서 = 프롬프트 · 콘텐츠 해시 캐시 키                                                                                                                                                               |
| **N 비적용·동등** |   13 | iframe emitter 주입 · 아웃라인 rect 분리 · do-not-track marker · Inflation · `@layer`/`:where()` · CSS var ephemeral (Preview) · 롱핸드→숏핸드 merger · Expression/변수/코드 생성 export · 멀티플레이어 · 라벨 승인 · package boundary (`private`) · 빈 리포트 가드 · 로딩 진행률 · Layers 가상화 (composition 이 앞섬) · feature flag 모듈 · hidden 탭 스케줄러 (의도된 pause) |

**착수 순서 (가치 ÷ 비용)**: A① → A② → A⑤ → A④ → A③ → B⑦ (css-tree lazy import 로 번들 예산 확인 후) → B⑧ (Text as `h1` 측정 1건) → B⑥ (ADR) → B⑨ · B⑩ 은 결정 지점 질문 후.

---

## 1. 코드 사실 — webstudio

| 영역          | 사실                                                                                                                                                                                                                                                                                                                                               | 경로                                                                                                                                                                             |
| ------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 데이터 모델   | 모든 도메인이 `Map<id, record>` flat. 트리 순서는 `Instance.children[]` 배열만 (parentId · order 필드 없음). Slot 때문에 선택은 루트까지 경로 `InstanceSelector = id[]`                                                                                                                                                                            | `packages/sdk/src/schema/instances.ts:55-69` · `packages/project-build/src/runtime/instance-path.ts:3`                                                                           |
| 스타일 키     | `styleSourceId:breakpointId:property:state` 값 유도 복합키 → upsert 멱등. `StyleSource = token \| local` 같은 union, `styleSourceSelections[instanceId].values: id[]` 배열 순서 = 우선순위                                                                                                                                                         | `packages/sdk/src/schema/styles.ts:26-33` · `style-sources.ts` · `style-source-selections.ts`                                                                                    |
| 캐스케이드    | 자체 specificity `LAYER-STATE-BREAKPOINT-STYLESOURCE`. declared → cascaded → specified → computed(var) → used 5단계, 값마다 source `default \| preset \| remote \| local \| overwritten`. reset 은 `local \| overwritten` 만 활성                                                                                                                  | `apps/builder/app/shared/style-object-model.ts:105-120` · `style-panel/property-label.tsx`                                                                                       |
| UA 기본값     | `html.css` 를 파싱해 `Map<"tag:property", StyleValue>` 로 생성, 캐스케이드 최하위 레이어로 명시                                                                                                                                                                                                                                                    | `packages/css-data/bin/html.css.ts` · `src/__generated__/html.ts`                                                                                                                |
| 값 검증       | mdn-data + css-tree `lexer.matchProperty` 로 입력 검증, 실패 시 `{type:"invalid"}` — 화면엔 보이되 저장은 차단                                                                                                                                                                                                                                     | `packages/css-data/src/parse-css-value.ts` · `style-panel/shared/use-style-data.ts`                                                                                              |
| ephemeral     | 저장 값을 `var(--<source>-<state>-<prop>, 실제값)` 로 감싸고 드래그 중엔 `documentElement.style.setProperty` 만 → 시트 재생성 0. `$ephemeralStyles` 는 undo · 서버에 안 들어감                                                                                                                                                                     | `apps/builder/app/canvas/shared/styles.ts:290-310, 786` · `use-style-data.ts:75-89`                                                                                              |
| 변경 모델     | 순수 runtime op `(state, input) → {payload: patch[], result, invalidatesNamespaces}`. accumulator 가 N 개 op 을 중간 state 에 적용하며 1 트랜잭션으로 커밋. undo 단위 = 트랜잭션 1                                                                                                                                                                 | `packages/project-build/src/runtime/mutation.ts:16-97` · `apps/builder/app/shared/instance-utils/data.ts:313-352`                                                                |
| 저장          | patch 를 namespace 별 그룹 → 서버는 건드린 컬럼만 select · touched key 만 zod 재검증 · `version` CAS + `lastTransactionId` 멱등. 큐는 직전 tail 과 같은 프로젝트일 때만 병합, 1초 폴링                                                                                                                                                             | `sync/patch/patch-service.server.ts:36-73` · `packages/project/src/db/build-patch-core.ts:93-178` · `sync/command-queue.ts`                                                      |
| 마이그레이션  | 스키마 버전 번호 없음. 로드 시 멱등 `migrateWebstudioDataMutable` 을 runtime op 으로 실행해 diff 가 있으면 **일반 트랜잭션으로 저장**                                                                                                                                                                                                              | `packages/project-build/src/runtime/migrations.ts:14-35`                                                                                                                         |
| content model | `@webstudio-is/html-data` 카테고리 + 합성 카테고리 `non-interactive / non-form / labelable` 로 `button > button` · `form > … > form` 차단. 컴포넌트 meta 는 `contentModel: {category, children, descendants}`. 재귀 시 계산 카테고리를 인자로 넘겨 재계산 회피, 기존 경고는 baseline 으로 빼고 새 경고만 판정                                      | `packages/project-build/src/runtime/content-model.ts` · `matcher.ts` · `packages/sdk/src/schema/component-meta.ts:76`                                                            |
| prop meta     | `react-docgen-typescript` 로 `.tsx` → `__generated__/x.props.ts` 커밋, `.ws.ts` 가 spread 후 부분 override. control 24종 discriminated union                                                                                                                                                                                                       | `packages/generate-arg-types/src/arg-types.ts` · `packages/sdk/src/schema/prop-meta.ts`                                                                                          |
| 템플릿        | `@jsxImportSource @webstudio-is/template` — 런타임은 React jsx 그대로, `JSX.LibraryManagedAttributes` 만 재정의. `renderTemplate` 이 JSX 트리를 instances/props/styles fragment 로 평탄화, element 객체 기준 결정론적 id. 직렬화 불가 값은 교정 힌트 담은 에러                                                                                     | `packages/template/src/jsx.ts` (994 줄) · `jsx-runtime.ts`                                                                                                                       |
| compound      | Tabs 서브파트는 각각 독립 meta. Trigger/Content 짝은 `indexWithinAncestor` (조상 id 선언 → `data-ws-index` 주입) 로 순서 매칭. 편집기 부수효과는 `Hook.onNavigatorSelect` + 저장 안 되는 memory prop                                                                                                                                               | `sdk-components-react-radix/src/tabs.ws.ts` · `hooks.ts` · `packages/react-sdk/src/hook.ts`                                                                                      |
| publish       | 문자열 JSX 코드 생성 (`generateWebstudioComponent`), `createScope` 로 식별자 충돌 회피, `Framework` 인터페이스 (remix / react-router / vike-ssg) 로 타깃 추상화. 컬렉션 반복은 캔버스 헬퍼와 생성기가 같은 파일                                                                                                                                    | `packages/react-sdk/src/component-generator.ts` · `collection-utils.ts` · `packages/cli/src/framework.ts`                                                                        |
| 캔버스 통신   | 동일 출처 iframe 에 emitter 를 전역 주입 후 즉시 `delete`. UI 상태 40여 개는 `NanostoresSyncObject` 로 통째 replace. postMessage 는 DnD·command 만, rAF 배칭. 아웃라인 rect 는 캔버스가 계산, 그리기는 빌더 (zoom 적용)                                                                                                                            | `apps/builder/app/builder/builder.tsx:355` · `shared/sync/sync-stores.ts:423` · `pubsub/raf-queue.ts` · `canvas-tools/`                                                          |
| 스케줄러      | 스타일시트 렌더는 rAF, 백그라운드 탭이면 `setTimeout(50)` 폴백. 붕괴 요소는 `INFLATE_PADDING=75` 로 선택 가능하게                                                                                                                                                                                                                                  | `canvas/shared/styles.ts:454-500` · `canvas/inflator.ts`                                                                                                                         |
| drift 게이트  | 생성기 실행 후 `git status --porcelain -- <paths>` 가 비면 통과 — 게이트 코드 20 줄, 생성물 3계층 (runtime op contract · server-only api metadata · docs) 이 같은 스크립트 사용                                                                                                                                                                    | `scripts/check-clean-files.mjs` · `package.json` `check:generated-api` · `check:generated-docs`                                                                                  |
| 크기 게이트   | `du -sk` 로 fixture 3개 실측 vs 하드코딩 상한, 통과해도 `+/-N kB` 여유 출력                                                                                                                                                                                                                                                                        | `scripts/check-fixture-sizes.ts` · `.github/workflows/main.yml` `check-size`                                                                                                     |
| 시각 회귀     | baseline(merge-base worktree) / current 를 별도 포트 동시 구동 비교. 캐시 키 = harness 소스 + lockfile + Chromium 버전 해시. `comparisons.length === 0` 이면 별도 step 실패. 승인 라벨은 새 커밋에서 자동 제거                                                                                                                                     | `scripts/visual-regression/runtime-hash.ts` · `.github/workflows/visual-regression.yml` · `remove-pr-approvals.yml`                                                              |
| AI/MCP        | MCP 툴 annotation (`permit / readNamespaces / writeNamespaces / retryOnConflict`) 이 runtime op registry 와 같은 소스 → `__generated__` 계약. `man mcp --verbose` 출력을 템플릿에 주입해 공개 문서 생성, 반대로 md 섹션을 파싱해 툴 응답 문구로. 평가는 fixture 프로젝트 + tool trace + 토큰 + `tools/list` 바이트를 베이스라인 대비 회귀율로 판정 | `packages/project-build/src/mcp.ts` · `scripts/docs/generate-mcp-docs.ts` · `packages/cli/src/mcp-guidance.ts` · `packages/cli/evaluations/high-impact/evaluation-regression.ts` |
| 피처 플래그   | boolean export 모듈이 곧 타입. default ∨ env ∨ localStorage OR 합성, 끄는 경로 없음                                                                                                                                                                                                                                                                | `packages/feature-flags/src/flags.ts` · `feature.ts`                                                                                                                             |
| Layers        | `$flatTree` computed 후 **전량 렌더** — 가상 스크롤 없음                                                                                                                                                                                                                                                                                           | `navigator-tree.tsx:229, 1055`                                                                                                                                                   |

## 2. 코드 사실 — composition (2026-09-08)

| 사실                                                                                                                                                                                                                                      | 경로                                                                                                         |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| canonical 은 중첩 `children[]` 트리 (ADR-118). 스타일은 element 인라인 `props.style` (base) + `responsive.styles[bp]`. **공유 style source · element 별 state 스타일 · 스키마 버전 카운터 없음.** 재사용은 `reusable` + `ref` 노드 단위만 | `packages/shared/src/types/composition-document.types.ts` · `responsive.types.ts:318`                        |
| hover/pressed 는 catalog rule 의 `fill` state 에만 존재 (사용자 요소별 저장 스키마 없음)                                                                                                                                                  | `packages/shared/src/catalog/generated/componentRulesTable.ts`                                               |
| 변이는 `canonicalMutationRunner` 4단 (canonical → set → rebuild → history → persist, ADR-184). op 은 store 결합 (`inspectorActions` 등이 store 를 직접 호출)                                                                              | `apps/builder/src/adapters/canonical/canonicalMutationRunner.ts` · `builder/stores/inspectorActions.ts`      |
| history = canonical node event diff (ADR-124), hot 50 + IndexedDB cold. 1차 저장소는 IndexedDB 문서 통째 `put` + 급감 가드 (Supabase 는 auth·데이터 소스)                                                                                 | `stores/history/` · `lib/db/indexedDB/` · `stores/canonical/persistActiveCanonicalDocument.ts`               |
| Styles 패널 `ScrubInput` 은 드래그 중 표시값만 바꾸고 **pointer-up 에 `onCommit` 1회** — 드래그 중 캔버스 live 미리보기 없음. `skipHistory` 류 0건                                                                                        | `panels/styles/components/ScrubInput.tsx:225-240`                                                            |
| 패널 `StyleSource` 타입 4종 (`inline \| computed \| inherited \| default`) 은 있으나 값별 origin 배지·조건부 reset UI 는 없음                                                                                                             | `panels/styles/types/styleTypes.ts:4-8` · `hooks/useStyleSource.ts`                                          |
| `css-tree` · `mdn-data` 의존 0건. 값 검증은 수동 (STYLE_PARSING.md §3.5 와 동일 상태). UA 기본 스타일 데이터 0건 (`html.css` · `userAgent` grep 0)                                                                                        | `apps/builder/package.json` · `packages/*/package.json`                                                      |
| nesting 제약 0건 (`allowedParents` · `requiredAncestor` · `canDrop` grep 0). ADR-142 는 "컴포넌트별 allow-set 을 코드에 두지 않는다" 를 확정. drop 은 런타임 컨테이너 여부만                                                              | `canvas/hooks/useCanvasDragDropHelpers.ts` · `catalog/slotRoles.ts`                                          |
| bindings ~100 파일의 `PropContract` 수기. 팩토리는 `ComponentDefinition{parent, children[]}` 객체 재귀. 표현식 엔진 · 코드 export 0 (publish 는 기능 링크만 — 방침)                                                                       | `packages/shared/src/catalog/bindings/*.binding.ts` · `builder/factories/definitions/`                       |
| 생성물 drift: generated CSS 는 Stop hook 이 재생성 후 diff **알림만** (차단 아님). text-axis-matrix 는 pre-push `--check` 차단. engine-matrix 는 codex 게이트. palette 생성물은 게이트 없음                                               | `.claude/hooks/type-check-gate.sh:38-60` · `.githooks/pre-push:65` · `scripts/codex/engine-matrix-gate.sh:6` |
| 번들 초기 < 500KB 목표 (CLAUDE.md) 는 있으나 **게이트 0건** (`size-limit` · `maxSize` grep 0). 대조는 detached checkout 수동 (메모리 `reference-bundle-delta-baseline-build-detached-checkout`)                                           | `package.json` · `.githooks/pre-push`                                                                        |
| visual-parity 게이트는 측정 테스트 수 바닥값 강제 (빈 리포트 가드 동등). CI 는 `deploy.yml` 1개, PR 없음 (web PR 금지 정책)                                                                                                               | `apps/builder/scripts/visual-parity-gate.mjs` · `.github/workflows/`                                         |
| AI tool 9개는 `tools/definitions.ts` 수기 JSON schema, `runCommand` 가 `commandRegistry` 브리지. 평가 하니스 0. MCP 서버 없음 (schema 호환만)                                                                                             | `services/ai/tools/definitions.ts:362` · `tools/runCommand.ts:72` · `stores/commandRegistry.ts`              |
| Skia 프레임 스케줄러는 `document.hidden` 이면 **의도적으로 pause**. Layers 는 가상화 있음                                                                                                                                                 | `canvas/skia/SkiaCanvas.tsx:1185-1192` · `panels/navigator/tree/TreeBase/VirtualizedTree.tsx`                |
| feature flag 는 함수 모듈 (`isWebGLCanvas` 등 4종)                                                                                                                                                                                        | `apps/builder/src/utils/featureFlags.ts`                                                                     |
| 로딩 진행률: 현재 작업 (`.agent/task-state.json`) 이 Skia 첫 flush 를 readiness 계약으로 묶는 중 — webstudio 의 "데이터 로드 후에만 canvas mount + 3 플래그 ready 비율" 과 같은 방향                                                      | `.agent/task-state.json`                                                                                     |

---

## 3. 적용 판정 — before / after / 예상 장점

등급: **A** 권장 (동작 변경 0 또는 패널 한정) · **B** 조건부 (측정·ADR·결정 지점 선행) · **C** 보류 (트리거 대기) · **N** 비적용·동등.

### 3.1 품질 게이트 · 도구

| #   | 패턴 (webstudio 근거)                                                               | Before (composition)                                                                                       | After                                                                                                                                                                                       | 예상 장점                                                                                                                     | 등급 |
| --- | ----------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | :--: |
| ①   | `git status` drift oracle (`scripts/check-clean-files.mjs`)                         | 생성물 4종 (generated CSS · engine-matrix · text-axis-matrix · palette) 이 각기 다른 게이트, CSS 는 알림만 | `scripts/check-clean-files.mjs` 1개 + pre-push 에서 4 생성기 재실행 → 대상 경로 `git status --porcelain` 비면 통과. 새 생성물은 인자 1개 추가                                               | stale CSS 커밋 (ADR-913 R6 사고 유형) push 단계 차단. 게이트 코드 4벌 → 1벌. 동작 변경 0                                      |  A   |
| ②   | 크기 상한 게이트 (`check-fixture-sizes.ts` — 실측 vs 상한 + 여유 출력)              | 초기 번들 < 500KB 는 문서 목표뿐, 게이트 0. 대조는 detached checkout 수동                                  | `scripts/check-bundle-size.mjs`: `vite build` 산출 초기 청크 합 vs 상한 (kB 하드코딩), 통과해도 `+/-N kB` 출력. pre-push 또는 codex:preflight                                               | 번들 회귀를 push 전에 잡고, 여유 kB 가 매번 보여 예산 소진 추세를 읽는다. 동작 변경 0                                         |  A   |
| ⑤   | runtime registry → 생성 계약 (`generate-runtime-operation-contracts.ts`)            | AI tool 9개 JSON schema 수기 + `runCommand` 브리지. commandRegistry 메타와 tool 정의가 따로 논다           | `commandRegistry` 항목에 `permit / reads / writes` 메타를 두고 `tools/__generated__/definitions.ts` 를 생성, ① 의 drift 게이트에 등록                                                       | 커맨드 추가 시 tool 누락 0, 권한·무효화 정책 1곳. 향후 MCP 서버 노출 시 annotation 그대로 재사용 (compiler-first 방향과 합치) |  A   |
| ⑪   | AI 평가 회귀 (`evaluation-regression.ts` — trace · 토큰 · `tools/list` 바이트 예산) | 평가 하니스 0. systemPrompt·tool 변경의 품질 회귀는 체감으로만                                             | `pnpm ai:eval`: fixture 프롬프트 3개 → tool 호출 trace + 토큰 + tool 정의 바이트 → `results/*.json` 베이스라인 대비 회귀율 (품질 15% · 토큰 50%), 베이스라인 명령 sha 다르면 `incompatible` | 프롬프트·tool 정의 변경이 토큰 비용·호출 순서를 얼마나 바꾸는지 수치화. tool 카탈로그 크기가 예산으로 관리됨                  |  B   |
| —   | 콘텐츠 해시 + 브라우저 버전 캐시 키 (`runtime-hash.ts`)                             | CI 없음 (deploy 1개), 게이트는 로컬 실행                                                                   | CI 도입 시 visual-parity 캐시 키에 harness 소스 + lockfile + Chromium 버전 해시                                                                                                             | 비교 로직 변경 시 캐시 자동 무효화                                                                                            |  C   |
| —   | 문서-스키마 커버리지 단언 (`assertContentEngineReferenceCoverage`)                  | `paletteOracle` · `adr-status-sync-check` 가 부분 수행                                                     | catalog 78 ↔ `PALETTE_ORDER` ↔ 문서 표 집합 대조 vitest 1개                                                                                                                                 | 문서 누락을 테스트 실패로                                                                                                     |  C   |
| —   | 라벨 승인 + 새 커밋 시 자동 소멸 · `private:true` 경계 · 빈 리포트 가드 · flag 모듈 | PR 금지 정책 / 5 패키지 전부 private / parity 바닥값 있음 / `featureFlags.ts` 있음                         | —                                                                                                                                                                                           | 동등 또는 정책상 비적용                                                                                                       |  N   |

### 3.2 스타일 시스템 · 패널

| #   | 패턴 (webstudio 근거)                                                                         | Before (composition)                                                                 | After                                                                                                                                                                                      | 예상 장점                                                                                                                                                    | 등급 |
| --- | --------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | :--: |
| ③   | durable/ephemeral 분리 (`$ephemeralStyles` — undo·서버 미진입, 드래그 중 캔버스만 갱신)       | `ScrubInput` 드래그 중 표시값만, pointer-up 에 commit 1회. 캔버스 live 미리보기 없음 | 드래그 중 `renderState` 의 ephemeral override 채널 (element id + property) 로 Skia 만 갱신, pointer-up 에 canonical commit 1회 유지. Preview 파이프라인·history·persist 는 그대로 1회      | 스크럽 중 캔버스가 따라 움직여 값 탐색 UX 개선. history·persist 부하는 현재와 동일 (1회). RAF 마다 Zustand 갱신 금지 원칙과 합치 (override 는 renderer 입력) |  A   |
| ④   | 값별 origin 배지 + `local \| overwritten` 만 reset (`property-label.tsx`)                     | `StyleSource` 4종 타입은 있으나 배지·reset 조건 UI 없음                              | 각 필드 라벨에 origin 배지 (inline / catalog default / inherited / responsive override) + reset 은 inline·override 에만 활성                                                               | "왜 이 값인가" 를 패널에서 바로 읽음. 사용자가 catalog 기본값을 지우려다 헛도는 일 감소                                                                      |  A   |
| ⑦   | css-data 값 검증 + `invalid` 1급 값 (`parse-css-value.ts` — 보이되 저장 차단)                 | 값 검증 수동, 잘못된 문자열이 canonical 로 진입 가능 → Skia·Preview 가 다르게 해석   | `css-tree` lexer 를 Styles 패널에서 lazy import, 입력 실패 시 필드에 `invalid` 표시 + canonical 저장 차단. mdn-data 는 shorthand 확장 표만 발췌                                            | 잘못된 값의 canonical 진입 0 → CSS↔Skia 발산 원천 1개 제거. 자동완성 키워드 표 무상                                                                          |  B   |
| ⑧   | UA 기본 스타일을 데이터로 (`html.css` → Map, 캐스케이드 최하위 레이어)                        | UA 기본값 데이터 0. Preview 는 브라우저 UA 가 그리고 Skia 는 catalog 만 읽음         | 선행 측정 1건: Text 를 `h1`/`p` 태그로 낸 Preview 와 Skia 의 margin·font-size 를 같은 상태에서 캡처. 격차가 있으면 `uaDefaults.ts` (tag:property Map) 를 Skia implicitStyles 최하위에 주입 | 격차가 있다면 parity 축 1개 봉합. 없다면 (reset 이 덮음) 문서화만                                                                                            |  B   |
| ⑩   | 공유 style source (token = 스타일 세트, `styleSourceSelections[instanceId].values` 배열 순서) | 재사용은 노드 단위 (`reusable` + `ref`) 만. 공통 스타일 편집은 요소마다 반복         | canonical 에 `styleTokens` collection + element `styleSources: id[]` 추가, resolver 가 배열 순서로 병합. `.pen` 호환은 export 시 flatten                                                   | 디자인 시스템급 일관성, 공통 스타일 1회 편집. **D3 스키마 확장 — 결정 지점 (3) SSOT 경계 재판정 질문 선행**                                                  |  B   |
| —   | 조건 breakpoint 시뮬레이션 (`not all` 트릭)                                                   | `themes.darkMode` 스냅샷, Preview 다크 미리보기 경로 미확인                          | Preview iframe 의 다크 미리보기를 media 재작성으로                                                                                                                                         | OS 설정 없이 다크 미리보기                                                                                                                                   |  C   |
| —   | `@layer presets` + `:where()` · CSS var ephemeral · 롱핸드→숏핸드 merger · `listed` 플래그    | 사용자 스타일은 인라인 (항상 이김) · Preview 는 백그라운드 · 인라인엔 merger 불필요  | —                                                                                                                                                                                          | 동등 또는 구조상 비적용                                                                                                                                      |  N   |

### 3.3 데이터 모델 · 상태

| #   | 패턴 (webstudio 근거)                                                                               | Before (composition)                                                                                               | After                                                                                                                                        | 예상 장점                                                                                            | 등급 |
| --- | --------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | :--: |
| ⑥   | 순수 runtime op + accumulator (`mutation.ts` — op 은 store 를 모르고 payload 만, N op = 1 트랜잭션) | `canonicalMutationRunner` 4단은 있으나 op 이 store 결합. AI `batch_design` 은 변경 N 개 = history N 개 (확인 필요) | op 서명을 `(doc, input) → {events, result}` 로 통일, runner 가 op 배열을 받아 events 병합 후 history 1 entry. 기존 액션은 어댑터로 단계 이관 | undo 단위를 호출자가 정함 (AI 일괄 = 1 undo). headless 테스트가 store 없이 op 만 검증. 규모 큼 → ADR |  B   |
| —   | namespace 분할 patch + 컬럼 단위 저장 + CAS + `lastTransactionId` 멱등                              | IndexedDB 문서 통째 put + 급감 가드                                                                                | 서버 문서 저장 (Supabase) 도입 시 events → namespace patch, `version` CAS                                                                    | 저장 비용이 편집 크기에 비례, lost update 차단. **트리거 = 서버 저장 도입**                          |  C   |
| —   | migration-as-transaction (버전 카운터 없이 로드 시 멱등 마이그레이션을 일반 트랜잭션으로 저장)      | `_meta.schemaVersion` 문자열 + export 시 migration                                                                 | 로드 시 마이그레이션 diff 를 canonical events 로 커밋                                                                                        | 마이그레이션이 history·persist 경로 재사용. 트리거 = 다음 스키마 변경                                |  C   |
| —   | 멀티플레이어 릴레이 (`actorId, clientSeq, relayTs` 3중자 · ack/durable · jitter backoff)            | 협업 0, 방침 없음                                                                                                  | —                                                                                                                                            | —                                                                                                    |  N   |

### 3.4 컴포넌트 · 템플릿 · 편집 UX

| #   | 패턴 (webstudio 근거)                                                                                  | Before (composition)                                                                        | After                                                                                                                                                 | 예상 장점                                                                        | 등급 |
| --- | ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- | :--: |
| ⑨   | HTML content model 제약 (`content-model.ts` — html-data 카테고리 + `non-interactive / non-form`)       | nesting 제약 0. ADR-142 가 컴포넌트별 allow-set 을 금지                                     | 컴포넌트별 allow-set 이 아니라 **HTML 스펙 카테고리** (D1) 로 `button > button` · `form > form` · `a > a` 를 drop·paste 시 경고. 기존 경고는 baseline | 접근성 위반 트리 예방. **D1 검증을 빌더가 보유하는가 — 결정 지점 (3) 질문 선행** |  B   |
| —   | TS 타입 → prop meta 생성 (`generate-arg-types` — `__generated__` 커밋 + `.ws.ts` override)             | `PropContract` ~100 파일 수기                                                               | RAC props 타입에서 초안 생성 + 수동 override                                                                                                          | RAC 업그레이드 시 prop drift 감지. D2 는 RSP 참조라 RAC 타입 자동 도출과 어긋남  |  C   |
| —   | 템플릿 JSX DSL (`renderTemplate` — 타입 전용 JSX 확장, 결정론적 id, 교정 힌트 에러)                    | `ComponentDefinition{parent, children[]}` 객체 재귀                                         | 팩토리 정의를 typed JSX 로                                                                                                                            | 작성 편의·IDE 자동완성. `.pen` 정합이 우선이라 이득 작음                         |  C   |
| —   | `indexWithinAncestor` 짝 매칭 · Hook + memory prop (Layers 선택 시 해당 탭 열기)                       | Tabs 짝 방식·선택 시 패널 노출 미확인                                                       | Layers 에서 TabPanel 선택 시 editMode 에 활성 탭 기록 (저장 안 함)                                                                                    | 편집 UX. 확인 후 판정                                                            |  C   |
| —   | 클립보드 플러그인 체인 (mime first-match — html · markdown · figma tokens · webflow)                   | HTML paste 0, `useCopyPaste` 단일 JSON                                                      | `useCopyPaste` 를 플러그인 배열로, html → canonical 변환기 1개                                                                                        | 외부 자산 유입 경로. 트리거 = 사용자 요구 (pencil import 가 정본 경로)           |  C   |
| —   | Expression 엔진 · 데이터 변수 · 코드 생성 export · `Framework` 추상화                                  | 0 — publish 는 기능 링크만 (방침, 빌더 안정화 후)                                           | —                                                                                                                                                     | —                                                                                |  N   |
| —   | iframe emitter 주입 · 아웃라인 rect 분리 · do-not-track marker · Inflation · hidden 탭 setTimeout 폴백 | Skia 캔버스 (iframe 없음) · placeholder + 빈 상자 sweep 있음 · hidden 탭 pause 는 의도된 것 | —                                                                                                                                                     | 구조상 비적용                                                                    |  N   |
| —   | 로딩 진행률 (3 플래그 ready 비율, 데이터 로드 후에만 canvas mount) · Layers 가상화                     | readiness 계약 작업 진행 중 (같은 방향) · 가상화 있음 (webstudio 는 없음)                   | —                                                                                                                                                     | 동등 · composition 이 앞섬                                                       |  N   |

---

## 4. 판정 원칙 (이 대조에서 확인한 것)

- **캔버스 층은 대조 대상이 아니다.** webstudio 의 강점 (iframe 동기화 · ephemeral CSS var · 아웃라인 분리) 은 DOM 캔버스 전제다. composition 은 Skia 라 같은 문제가 다른 형태 (renderer 입력 override) 로 존재한다 — ③ 이 그 번역.
- **게이트는 "재생성 후 clean" 하나로 통일한다.** 생성물마다 게이트를 따로 짜면 알림만 하는 게이트 (generated CSS) 와 차단하는 게이트 (text-axis) 가 섞인다. ① 이 4벌을 1벌로.
- **결정 지점은 두 건뿐이다.** ⑨ (D1 검증의 빌더 보유) 와 ⑩ (D3 스키마에 공유 style source 추가). 둘 다 `.claude/rules/premise-decision-points.md` 의 (3) SSOT 경계 재판정이라 착수 전 질문한다. 나머지는 자율 진행 대상.
- **STYLE_PARSING.md §3.5 의 gap 은 9개월째 그대로다.** css-tree · mdn-data 부재는 2025-12 에 기록됐고 지금도 0건. ⑦ 은 새 아이디어가 아니라 미이행 항목의 재제기다.

## 5. 관련

- [EXTERNAL_PATTERN_DELTA_2026-09.md](EXTERNAL_PATTERN_DELTA_2026-09.md) — fulgur · pretext 대조 (형식 정본)
- [STYLE_PARSING.md](STYLE_PARSING.md) §3.1 · §3.5 — webstudio css-data 최초 언급 (2025-12)
- [PEN_V1.2.8_DELTA_2026-09.md](PEN_V1.2.8_DELTA_2026-09.md) — Pen 대조 (canonical 스키마 정합 기준)
- `.claude/rules/premise-decision-points.md` — ⑨ · ⑩ 질문 규약
- `.claude/rules/ssot-hierarchy.md` — D1/D2/D3 경계
