# OS
.DS_Store
Thumbs.db

# Env
.env
.env.local
.env*.local

# Dependencies (pnpm — npm 실수로 생기는 lock 은 추적하지 않음)
node_modules/
.pnpm-store/
.npm-cache/
package-lock.json

# Build / cache
dist/
.turbo/
.cache/
.vercel
.output
*.tsbuildinfo
/build/

# WASM — canvaskit 복사본 · wasm-pack 산출물 (pnpm prepare:wasm / wasm:build:engine)
apps/builder/public/wasm/
apps/builder/src/builder/workspace/canvas/wasm/target/
apps/builder/src/builder/workspace/canvas/wasm-bindings/composition-engine-pkg/
target-install/

# Tests / coverage / Playwright
test-results/
playwright-report/
blob-report/
coverage/
.playwright-cli/
/output/
# 하니스 덤프. 디렉토리 자체는 열어둠 — visual-parity 의 .gitignore 가 추적됨
**/.artifacts/**
!**/.artifacts/.gitignore

# Logs / temp
logs/
*.log
*.bak
*.tmp
*~

# Editors / local agent IDEs
.idea/
*.sw?
.kiro/
.windsurf/
.roo/

# Storybook
storybook-static/

# tsc -b 가 src 옆에 emit 한 산출물.
# Vite 가 .ts 대신 .js 를 우선 로드해 HMR 이 오염됨 (ADR-915, 커밋 367fe7b3b).
apps/builder/src/**/*.js
!apps/builder/src/**/*.config.js
packages/shared/src/**/*.js
packages/shared/src/**/*.js.map
packages/shared/src/**/*.d.ts
packages/shared/src/**/*.d.ts.map
!packages/shared/src/**/*.config.js
# 손수 작성 ambient 선언 (컴파일 산출물 아님)
!packages/shared/src/css-modules.d.ts

# Agent / worktree / local settings
.worktrees/
.agent/runs/
.claude/settings.local.json
.claude/.spec-rebuild-pending
.claude/.css-regen-pending
.claude/.adr-split-commit-allowed
CLAUDE.local.md
# 옛 .claude/commands 자동 이관 잔재 (정본은 .claude/skills/, 승인 삭제 후 2회 재유입)
.agents/skills/source-command-*/

# Local docs / evidence (커밋 금지)
docs/react-aria-starter/
docs/react-aria/
docs/pencil-extracted/
docs/pencil-copy/
docs/migrations/evidence/
docs/adr/evidence/
